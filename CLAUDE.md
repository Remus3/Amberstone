# Riot Commander — Agent Context

Live League / TFT coaching dashboard. Reads Riot Live Client API, calls Claude Haiku for coaching and Sonnet for vision, writes JSON to `data/`, serves `:8888` HTTPS dashboard on Game-PC's secondary display. RC is tkinter-free; Daemon Slayer (`:8893`) computes real DPS math per champion.

> **Living docs (read at session start):** `docs/ARCHITECTURE.md` · `docs/OPERATIONS.md` · `docs/BRIDGE.md` · `ROADMAP.md` · `docs/API.md`
> **Deep references:** `docs/DAEMON_SLAYER.md` (DS engine · 547 items · ENGINE_VERSION 0.91.0 · all 6 archetype scorers wired) · `docs/AGENTS.md` (Phase 3 framework) · `BACKLOG.md` (aspirational)
> **Architectural decisions:** `docs/adr/` — before re-litigating a past choice, check here first.
> **Dated artifacts** in `docs/_archive/` (excluded from ripgrep searches).

## Topology

| Machine | Tailnet / IP | LAN IP | Role |
|---|---|---|---|
| **Legion** | `legion-rc` / `100.70.22.55` | `192.168.8.230` | Runs RC, supervisor, vision server, web dashboard |
| **Game-PC** | `gamepc-rc` / `100.95.66.128` | `192.168.8.237` | Runs League; Chrome on secondary display (panel is 1920×1280 native @ 100% scale) shows the dashboard |
| **Peer** | `peer-host` / `<peer-tailnet-ip>` | — | Cross-Claude peer; RC↔Peer bridge |

All three in tailnet `tailc150de.ts.net`. Prefer tailnet hostnames. Vision runs in-process at `127.0.0.1:8889`.

## Paths

- Project root: `C:\Riot Commander\`
- Python: `C:\Users\Administrator\AppData\Local\Programs\Python\Python314\python.exe`
- API key: `C:\Riot Commander\API-Key-Claude.txt` (gitignored)
- Health: `C:\Riot Commander\ops\runtime\health.json`
- Logs: `C:\Riot Commander\logs\YYYY-MM-DD.log`

## Hard rules

- **Always `py_compile` before restart.** Syntax errors crash silently under `pythonw.exe`.
- **Atomic writes only:** `tmp.write_text(...); tmp.replace(target)`. Overlays poll mid-write.
- **Never `Stop-Process`.** Hangs MCP pipe. Use `taskkill /F /PID`.
- **Restart via `restart_trigger.txt`** (write any content; supervisor clears + restarts within ~5s).
- **`SCRIPT_DIR` in `app/__init__.py` MUST be `Path(__file__).parent.parent`** (package layout).
- **State assumptions explicitly before coding.**
- **Frozen files** (do not modify without explicit user approval):
  `main.py`, `core/log_setup.py`, `core/moon_proxy.py`, `lcu/lcu_client.py`,
  `core/game_snapshot.py`, `ops/rc_dev_runtime.py`, `ops/rc_supervisor.py`,
  `app/__init__.py`, `app/_loop.py`, `app/_health_monitor.py`, `app/_remediation.py`,
  `app/_state_authority.py`, `app/_overlay_manager.py`, `app/_game_lifecycle.py`,
  `tools/bridge_watcher_classify.py`,
  `tools/bridge_watcher_actions.py`, `tools/bridge_watcher_action_prompt.md`,
  `tools/bridge_watcher_history.py`, `tools/bridge_watcher_install.ps1`,
  `tools/bridge_watcher_hook.ps1`, `tools/bridge_watcher_config.json`,
  `tools/bridge_post_result.py`, `tools/bridge_pull_tasks.py`,
  `tools/process-bridge-tasks.md`, `tools/diagnose.md`, `tools/caveman.md`,
  `dashboard/routes_bridge_pending.py`, `ops/RC-BridgeWatcher.xml`.

## Restart workflow

```
echo restart > restart_trigger.txt
```
Verify: read `ops/runtime/health.json`, confirm new `pid`, `alive=true`, `last_reload_ok=true`.
Hard fallback: `taskkill /F /PID <pid>` then `restart.bat`.

## Session workflow

Scoped sessions — each focused task is one session.
- **End:** commit + update `WAKEUP_NOTES.md` (keep last 2–3 sessions at full fidelity; archive older to `docs/history_notes.md`) + push.
- **Start:** `/clear`, bootstrap from CLAUDE.md + MEMORY.md + git log + WAKEUP_NOTES + `docs/ARCHITECTURE.md` + `ROADMAP.md` (all 4 under 800 lines total).
- `/clear` between Tier items, between coding/reviewing modes, between focus-area switches.

## Web dashboard

`web_dashboard.py` at `:8888` HTTPS. Key endpoints: `/`, `/api/state`, `/api/health/all`, `/api/bridge/pending`, `/api/input`, `/api/command`, `/api/ds-preview`, `/metrics`. Viewed in Chrome on Game-PC secondary at `https://legion-rc:8888/` — design baseline is **standard 1920×1080 with Chrome chrome present** (titlebar + URL bar + bookmarks bar visible, usable viewport ≈ 1920×~920). F11 fullscreen is optional and recovers the chrome chrome — `main` flex-grows into the extra height (no layout pinned to 1280). Cert via `tools/regen_rc_cert.ps1`. Each machine has its own Anthropic API key (`riot-commander-legion`, `riot-commander-gamepc`, `riot-commander-peer`).

## Scheduled tasks (Legion)

Key: `RC-Supervisor` (logon, Administrator, HIGHEST) · `RC-VisionServer` (startup, SYSTEM, HIGHEST) · `RC-BridgeWatcher` (logon, daemon). Full list + Game-PC tasks: `docs/OPERATIONS.md`.

## Vision pipeline

Game-PC `gamepc_screen_agent.py` POSTs frames every 2s to `:8889/upload-frame`. Coaches call `modes.shared_vision._capture_screen()` → GET `:8889/latest-frame`. **`_run_vision()` gates on `_fetch_game_data() is not None`** — vision never fires during lobby/idle. Tiered: OCR first, Sonnet escalation for misses. Calibrate `data/vision_regions.json` to expand OCR coverage.

## Mode detection

`game_reader.py._process_game()` → `core/game_snapshot.py` → mode strings. ARAM Mayhem (`KIWI`) → `MODE_ARAM`.

## Where to find current state

- Live PID + mode + health: `ops/runtime/health.json`
- Current game state: `data/{aram,arena,brawl,tft}_coaching_data.json`
- Recent activity: `logs/YYYY-MM-DD.log`
- Architecture / module map: `docs/ARCHITECTURE.md`
- Ops commands + restart: `docs/OPERATIONS.md`
- Bridge wire format + watcher: `docs/BRIDGE.md`
- Open work: `ROADMAP.md` · Aspirational: `BACKLOG.md` · History: `docs/_archive/CHANGELOG.md`

## Useful commands

Full reference: `docs/OPERATIONS.md`. Quick-start:
```
python -c "import json; print(json.dumps(json.loads(open(r'ops/runtime/health.json').read()), indent=2))"
echo restart > restart_trigger.txt
curl -k https://127.0.0.1:8888/api/health/all
```

## Memory frontmatter — cross-project sync fields

Standard memory files carry `name`, `description`, `type`. Two optional fields
are valid for memories that should ride the RC↔Peer bridge (Phase 1 schema,
`docs io RC peer/RC_PHASE1_LESSON_SCHEMA_2026-05-02.md`):

```yaml
cross_project: true          # opt-in; default false. Only feedback/reference/project eligible.
applies_when: "<trigger>"    # required when cross_project=true; free-form grep-able phrase
does_not_apply_when:         # optional list; receiver skips if any entry matches local context
  - "<neg-trigger>"
```

`type: user` memories are never eligible. Default is OFF — author decides at write-time.
False-negatives are recoverable (edit frontmatter later); false-positives are bridge spam.

## Testing Discipline

Always run the full test suite after schema changes, engine version bumps, or item-effect additions. Avoid data-fragile cross-item comparison assertions; prefer assertions on computed quantities. When stubbing methods accessed via class, wrap with `@staticmethod` correctly.

## Windows Environment Notes

Claude Desktop on Windows may be installed via the Microsoft Store (check `%LOCALAPPDATA%\Packages`) in addition to standard install paths. Use `pythonw.exe` (not `python.exe`) for background daemons to avoid flashing console windows.

## Daemon Slayer Batch Workflow

When continuing Daemon Slayer work: pick the next batch from ROADMAP, implement schema/engine changes, add tests (target green before commit), bump engine version, commit + push, verify live, update hand-off notes.

## Session Wrap-up

When invoked with `/done` or asked to wrap a session: (1) audit pending changes, (2) commit and push, (3) update ROADMAP/CLAUDE/README if relevant, (4) process lessons/WAKEUP_NOTES, (5) run bridge probe, (6) print final banner. Run independent steps in parallel.

## Active priorities

1. ✅ Phase 1 — Knowledge architecture complete (fc1361b, s125+s126): living docs + archmap + 5 ADRs.
2. ✅ Phase 2.1 — champion_profiles.py split complete (8fa11f4): 902→29 LOC + 168 JSONs.
3. ✅ Phase 5 — CI gate + smoke harness complete (d21f533, s128): 380 tests pass, ruff 0 violations.
4. ✅ Phase 4 — Contracts/schemas complete (s129 + s143 + s144): `core/coaching_payload.py` (5 pydantic models, soft-validate in state-builder) + `dashboard/api_schema.py` (HTTP shapes) + `core/bridge_envelope.py` + `docs/API.md` (40 routes); s143 added soft-warn dispatch-level validator in `dashboard/_dispatch.py` (5 paths, 26 tests); s144 closed 4.2 tool-rewrite via the Phase 6 `tools/bridge_cli.py` consolidation.
5. ✅ TFT 17.3 — shipped 0e9617b (s131): Morgana 4g, Anima/Stargazer reworks, Primordian AVOID, AP comps buffed.
5b. ✅ Phase 3 complete (s133+s135+s136+s137): ESM panels, CSS panels, JSDoc typedefs, Playwright snapshots. 6-fixture harness in `tests/snapshot_panels/`; WS stub prevents live-supervisor interference; CI gate live.
5c. ✅ Phase 2.3 — coach_integration split complete (s138): 1225 LOC → `coach_integration/` pkg (_profiles 181 + _sr_prompt 455 + _coach 607 + __init__ 6). 386 tests, ruff clean.
5d. ✅ Phase 2.2 — game_reader split complete (s139): 1474 LOC → `game_reader/` root pkg via `_PollerMixin` + `_NormalizerMixin` (poller 281 + snapshot_normalizer ~700 + mode_router 74 + __init__ 92). 386 tests, ruff clean, RC restarted clean.
5e. ✅ Phase 2.4 — moon_vision_server split complete (s140): 710 LOC → 21-LOC entrypoint shim + `vision_server/` pkg (_config 75 + _stats 65 + _frame 117 + _relay 118 + _inference 264 + _http 245 + __init__ 79). 386 tests, ruff clean, :8889/health verified, build_portable.py patched.
5f. ✅ Phase 7 — Process polish complete (s141 48d11be + s142): phase-markers normalized + archmap phase journal; `scripts/wakeup_prune.py` auto-prunes WAKEUP_NOTES (last 3 sessions, atomic, idempotent); `.githooks/commit-msg` enforces Conventional Commits subject lines (`<type>(<scope>)?!?: <description>`); 412 tests pass.
6. ✅ Phase 6 — Bridge consolidation complete (s144): `tools/bridge_cli.py` (574 LOC) + 7 thin shims (139 LOC) replacing 7 originals (772 LOC); `BridgeMetrics` namespace in `core/prom_metrics.py`; 42 tests pass. State-file consolidation + 5 watcher daemons (`bridge_watcher*.py`) explicitly out of scope. Cron contracts preserved — `process-bridge-tasks.md` skill spec untouched.
7. ✅ Riot API key policy — ADR-006 (s145, 2026-05-09) reverses the "no key" rule for full-team champ-select context. Personal-tier; Web API limited to champ-select + post-game; live in-game advisory stays LCU/LiveClient-only. Implementation tickets are FU02 (`core/riot_api.py`) + FU04 (Personal-tier application).
8. ✅ **FU04** Riot Personal-tier API key — applied + **approved same-day 2026-05-09** (App ID 834837, well inside the documented 2–6 week window). Bundle preserved at `Desktop/FU04-Application-Evidence/`. Key staged into `API-Key-Riot.txt` (gitignored).
9. ✅ **FU02** `core/riot_api.py` + champ-select team-context fan-out — shipped s148 (`dfa13f0`). Personal-tier key resolver + dual token bucket (20/s + 100/120s + 429 cooldown) + SQLite cache (immutable for match data + Account, 5-min TTL for ranks + mastery) + 6 endpoint wrappers + priority-1/priority-2 fan-out via daemon thread + progressive reveal + backend ranked-name-blanking (queue 420/440). 63 new tests, 565 total. `/metrics` exposes `rc_riot_api_calls_total{endpoint,outcome}` + bucket gauges.
10. ✅ **LCU agent → team-context refresh wiring** — shipped s149 (`e7b5af1`). Game-PC `tools/gamepc_lcu_agent.py` POSTs roster (with PUUIDs) to `https://192.168.8.230:8888/api/team-context/refresh` on ChampSelect entry + on lock/swap. Bridge-secret resolver, edge-trigger w/ 3s repost rate-limit, LCU champion-id→name cache. 30 new tests, 595 total. Game-PC deployed live; live-fire verification awaits next CS pop.
10b. ✅ **DS surfacing — header pill + ARAM Build-row fallback + match-record wire** — shipped s152 (`091d18c`). Glanceable `#ds-pill` in header row 2 (info-blue, ◆ glyph, mode-gated to in-game). Next/ARAM `Build` row falls back to `DS: <name> +Ndps` when coach hasn't emitted `item_extra`/`objective`. `performance_tracker.save_rating` folds the engine's last DS pick set into `matches.raw_data["daemon_slayer_picks"]` so calibration analysis loses the JSONL ⨝ on (champion, mode, ~ts). 8 new tests, 624 total. ~~Pre-existing `gameTime` ref-error at `panels/map_state.js:720` still pending (s151 follow-up).~~ **FIXED s173.1 `38ca915`** — bare `gameTime` referenced outside its IIFE scope; surfaced by new adversarial fixture `adv_out_of_range` and removed (duplicate of the correctly-prefixed `MM.gameTime.textContent` one line below).
11. ✅ **FU01** minimap-locate — shipped s168 (2026-05-11). New `agents/_minimap_bbox.py` resolver + `agents/supervisor.py:597` rewire. 3-path order: HTTP `?bbox=` override → `data/vision_regions.json` `_minimap_<mode>` key (NEW) → hardcoded 1920×1080 fallback. Persisted entry shape validated (4 ints, `r>l`, `b>t`); malformed values silently fall back. Behavior byte-identical to pre-FU01 until calibration entries are added. 19 new tests under `tests/fu01_minimap/`. Phase 3 supervisor restart pending to pick it up live.
12. 🚫 **FU03** clipboard helper — superseded by FU04 same-day approval (Personal keys don't expire; daily-renewal helper no longer needed).
13. 🟡 Vision regions calibration — tune `data/vision_regions.json` bboxes. Blocked on live game.
14. 🟡 DS calibration pipeline — blocked on rewind_history.db staleness (last entry Dec 2025).
15. 🟡 gamepc_boot.ps1 hardening — add `RC-WatcherHealthPublisher-GamePC` + `RC-BridgeWatcher-GamePC`.
16. 🟡 Bridge Watcher acceptance-criteria — need 50+ real-traffic samples.
17. ✅ **Mode-flicker chain + DS resolver + LCU agent fixes** — shipped s153/s154/s155/s156/s157/s158 (2026-05-10). s153 (`96bf4ee`) + s157 (`0e3d87a`) mirror the s150 LCU lobby/CS pre-flip into both HTTP `/api/state` and WS push paths via shared `resolve_mode_key` + `apply_preflip_mirror` helpers. s154 (`3a3bf58`) fallback for LCU CS-session missing `gameData.queue.id` during BAN_PICK — uses `/lol-gameflow/v1/session`. s155 (`5c39b9b`) `lock_pick` race-tolerance (already-locked → success) + dashboard reply surfacing via `lcuPollResult`. s156 (`6c4a940`) DS server display-name reverse map (covers Kai'Sa/Kaisa, Wukong/MonkeyKing, Renata Glasc/Renata, apostrophe family) + new `resolve_inventory` that drops trinkets/consumables. s158 (`bd0c88e`) `?dbg=1` mode/view transition log. 15 new tests, live-verified.
18. 🟡 **Active Match view** — steps 1–4 shipped; step 5 pending. Step 1 scaffold (s159 `3f72795` / s160 `57886e1` / s161 `8a857c4`). Step 2 + 3 shipped s170 (`85461e2`). ✅ **Step 4 shipped s171** (`73b66ec`): static SR/ARAM map base from `/data/ddragon/16.8.1/img/map/*` + champion-dot canvas overlay polled from `/api/vision-state` at 500ms + MIA badges when `missing_for_s > 12` + JG-gank warning band when enemy MIA > 20s in ENEMY_JUNGLE/RIVER + view-gated polling so opt-out operators don't burn CPU. **Step 5 pending:** zen-lock in-game + RIGHT NOW fold + bridge-pending → dev-panel button + fleet view removal.
19. ✅ **Pre-Game Lobby v2 redesign + handleChampSelect bug fix** — shipped s162 (`5c019f9`, 2026-05-10). Phase 3 step 1 of the 14-fixture flow. QUEUE / Mains tabs / Party / My Top 8 panels rebuilt; `handleChampSelect` ReferenceError chain closed via `handleLcuEnvelope` wrapper in `main.js`; 5 live menu views removed; 46 fixtures archived; sim banner → corner pill; EventSource stubbed in sim mode.
20. ✅ **Pre-Game Lobby v3 polish + conflict UI + AVG/Match grade** — shipped s163 (`e316291`, 2026-05-10). Phase 3 step 1 LOCKED for both solo + multi-member states (placeholder-driven; no separate flow_02 fixture pass needed). Visual-hierarchy audit subagent ran mid-session; 5 must-fix items shipped (drop shadow, font floors, lane gap, "live" hide, title alignment). Self-row mirror + Primary-lane CONFLICT detection + AVG 5 grade letter (S→D color-coded, KP%-style 5-tier band) + Mains 2x2 Overall + 2x3 AVG/Match (Gold dropped, Vision moved up) + League captain-icon-crown PNG + Phosphor copy SVG + LVL ### : Unranked fallback + JGL→JNG / SUPP→SUP role normalizer. Page fits 1080-viewport without scrollbar. 913 ins / 181 del across 7 files.
21. ✅ **Phase 3 step 2 + step 3 — Champ Select view scaffold + Pick&Ban panel + trade popup** — shipped s164 (`c0e6043`, 2026-05-10). Step 2 (`flow_02_lobby_with_others`) was a renaming pass; step 3 built the new top-level `view-champ-select` page (3-col grid: Allies + Pick&Ban left, My Pick + Builds center, Enemies right). Auto-promotes on `?cs=1` + `phase=ChampSelect`. Pick&Ban Recommendations panel with 3 source rows + 4-button mood toggle + quick-select with lock-after-click (gated to skip BAN_PICK so user can't accidentally ban their own pick). Trade popup: SWAP/TRADE header + CHAMPION/Nth-PICK/ROLE buttons appended to `<html>` to bypass body's `zoom:1.33` with matching `transform:scale`. Enemy "(guess)" tag + cyan countdown timer + 2px round-border (red ban / gold pick). 1986 ins / 4 del across 10 files (3 new).
22. ✅ **Phase 3 step 3 follow-ups — central panel + enemies redesign for ALL game modes** — shipped s165 (`91a42e1`, 2026-05-10). New `_csvDetectMode(cs)` → `sr|aram|arena|brawl` from queue_id + is_aram/is_brawl flags; `section.dataset.csMode` stamped so CSS branches via `[data-cs-mode]`. SR: 3-variant Build Chooser populated (was empty placeholder). ARAM: 5-cell horizontal Bench under My Pick + ARAM Build Chooser; enemy lock/timer reflowed to right edge (was clipping names from SR's absolute-50% layout). Arena: 2-cell allies, central pane = Duo+Augments (3 slots silver/gold/prismatic + augment options wired to `set_augment_intent`), enemies = 3 sub-team cards (TEAM 2/3/4 × 2 champs each). Brawl: 5v5 no roles/bans, Brawl Build Chooser. Pick&Ban hidden on non-SR via CSS rules; grid reflowed. Pick&Ban + Allies panels UNTOUCHED (LOCKED per operator). 3 new fixtures (flow_03b_aram_select / flow_03c_arena_select / flow_03d_brawl_select). 1112 ins / 34 del across 7 files.
23. ✅ **Phase B — LCU agent handlers + champ-select state population** — shipped s166 (2026-05-10). 5 new command handlers in `tools/gamepc_lcu_agent.py` (set_ban_intent / set_pick_intent / request_position_swap / request_pick_order_swap; set_augment_intent stub pending live `/lol-cherry/v1/*` discovery) + new champ-select state fields (active_round / is_brawl / position_swaps / pick_order_swaps / arena_teams / augments scaffold) + per-player summoners on `_team_picks`. 30 new tests under `tests/phase_b_champ_select/` (665 total green).
24. ✅ **Phase 3 step 4 — Loading Screen view scaffold** — shipped s166 (2026-05-10). New top-level `view-loading`, mode-conditional team panels, `?ld=1` opt-in + `phase=GameStart` auto-promote. Briefing card removed before commit per operator. New files: `web/js/panels/loading.js`, `web/css/panels/loading_view.css`, `data/sim/flow_04_loading_screen.json`.
25. ✅ **Backend sweep (s167, 2026-05-11, `1522b90`)** — per `NEXT_SESSION_PLAN_2026-05-10.md`. UI paused; five backend ships in one commit: (P1a) DS per-level DPS curve helper `compute_dps_curve()` 1/6/11/16/18 + `DpsCurvePoint` + 12 tests, ENGINE 0.60.0 → 0.61.0 (`agents/daemon_slayer/dps.py`); (P2) `scripts/rewind_catchup.py` Match-V5 → rewind_history.db + `core/riot_api.get_recent_matches` pagination params (`start`/`startTime`/`endTime`/`queue`/`type`), auto-rotates PUUID via Account-V1 when DB PUUID stale (2846 → 2851 rows); (P8) LCU mastery cache in `tools/gamepc_lcu_agent.py` via `/lol-collections/v1/inventories/<sid>/champion-mastery`, 5-min TTL, surfaced at `state["lcu"]["mastery"]` on Lobby/CS/InGame phases + 10 tests; (P9) `scripts/audit_api_surface.py` + `docs/API_SURFACE_AUDIT.md` (89 internal / 70 LCU / 6 web / 2 LiveClient endpoints); (P7) `scripts/prune_synthetic_matches.py` removed 56 dev-test rows from `match_history.db`. Full sweep: 601 + 949 + 40 green.
26. 🟡 **Deferred post-s168:** (a) ✅ **resolved** — Game-PC `gamepc_lcu_agent.py` redeployed s168 (2026-05-11) via one-shot `http.server` mechanism; s167 endpoint was stale (`/lol-collections/v1/inventories/<sid>/champion-mastery` → 404 on current LCU) — replaced with `/lol-champion-mastery/v1/local-player/champion-mastery`; mastery state-shape was double-nested (`state["lcu"]["lcu"]["mastery"]`) — flattened to top-level `state["mastery"]` so Legion's wrap produces correct `state["lcu"]["mastery"]`. Live-verified at Lobby phase, 40 mastery entries, summoner 3911621734024704. (b) **P6** Sonnet/Haiku → Claude Desktop API key — still needs operator clarification on which key file (current path: `coaches/_base_coach.py:read_api_key()` reads `API-Key-Claude.txt` then `$ANTHROPIC_API_KEY` env); (c) **P1b** Arena augment registry expansion — needs `as_pct` overlay channel (architectural, not 1-hr); (d) **P5** Default DS build #4 — waits on richer rewind data (currently sparse: 5 games since Dec 2025); (e) **P3/P4** History season-WR + Replay tab — UI work, deferred per operator; (f) ✅ **resolved s170** — RC-LCU scheduled-task action path fixed via cross-Claude bridge dispatch (`task-454fde72f190`, Game-PC Claude resolved 63s round-trip). Action now uses absolute python path `C:/Users/Administrator/AppData/Local/Python/pythoncore-3.14-64/python.exe`; running agent (pid 15480) deliberately not restarted — fix applies on next reboot.

27. ✅ **LCU wiring punch list — pre-game lobby + champ-select + DS active match** — shipped s170 (`85461e2`, 2026-05-11). 6 of 7 items closed in one session per operator's "what's needed to finish LCU wiring" audit: (#1) `state.lcu.lobby` forwarder w/ `members[]` + `local_member` + `is_leader` + `party_type` + `queue_name` + `search_state` + `position_preferences`; is_self via summoner-id match; name enrichment via `/lol-summoner/v1/summoners/{sid}`; 31 tests. (#2) `coach_integration/enemy_stats.py` `compute_enemy_stats(mode, level, …)` mode + level scaled curve; 27 tests. (#3) Active Match per-tick DS rerank + CommunityDragon icons. (#4) `GET /api/champ-select/pickban-recs` route querying `rewind_history.db`; 17 tests; 33ms live response with real WR + ban suggestions. (#5) RC-LCU scheduled-task fix via bridge. (#7) Top 8 dummy purge. 788 total green (was 719). **Pending #6:** Cherry/Arena `set_augment_intent` endpoint discovery, blocked on live Arena lobby.
29. ✅ **s171.8 view-router + cache-bust unification** — 6 commits 2026-05-12 (`eba274b`→`876fd01`). (a) 4 ruff fixes (CI unblock + real F601 dup-key bug in `gamepc_lcu_agent.py`). (b) FU01 audit-6 ship: `parse_http_override` validates bbox bounds. (c) CI Node.js 24 bump. (d) Loading-view sticky-guard inference (CS→null→loading) + dodge clear in `web/js/main.js:_viewAutoDerive`. (e) Phase 3 mode-detector LCU phase overlay in `agents/agent2_backend/file_ingest._compute_effective_mode` — warm-Agent-7 prime fires on champ_select/game transitions even when Legion can't see Game-PC LCU lockfile. (f) **Unified asset-hash + auto-reload** in `dashboard/_static.compute_asset_hash` + `dashboard/routes_state._serve_ui_version` — closes the s164→s171.7 cache staleness where two divergent file lists left operator's browser serving pre-s171.7 `champ_select.js` (opt-in gate) for ~10 days. Build-variant persistence shipped alongside (`_csvBuildVariantsFor` + `_csvSaveChoice` → `rc-ingame-build-<champion>` localStorage key, shared with `item_build.js`). 9 file_ingest tests + 289 phase2/phase8/snapshot tests green. (g) **Docs+tests backfill — overnight headless brief** (5 commits `a43981b`→`f3ff7dd`, 2026-05-12 02:25→02:36): `dashboard/view_router_state.py` Python mirror of `_viewAutoDerive` + 27 tests in `tests/test_view_router_state.py`; ROADMAP synced; ADR-008 (`docs/adr/ADR-008-unified-asset-hash.md`) captures the two-allow-list drift lesson; WAKEUP_NOTES rotated (s170 → `docs/history_notes.md`). All 5 commits CI-green on first push.

30. ✅ **s173 anti-drift audit run** — 3 commits 2026-05-12 (`3096e8c` + `907543f` + `139472d`). Daytime ADR-008 follow-up sweeping 10 candidate drift pairs. **Two unifications shipped:** (a) `agents/supervisor._file_post_game_summary` candidates list deferred to new `dashboard._state_builder.MODE_FILES` (deduped tuple from `MODE_TO_FILE.values()` via `dict.fromkeys()`) — closes a third independent enumeration of the coaching JSON file set; (b) `web/js/panels/champ_select.js:1223` legacy `dsMode` ternary deferred to `_csvDsModeFor` — closes the second of two mode→DS-name implementations inside the same module. **Eight hunts skipped/deferred** with rationale documented inline in WAKEUP_NOTES: 1/4/9 already-unified, 6/8 different intents (not drift), 3 touches frozen file, 7 doc-sync not structural, 10 multi-language scope. **Three open audit findings carried forward** for future sessions: frozen-file list duplication, ENGINE_VERSION doc drift, multi-table champion alias unification.

30b. ✅ **s173.1 audit thread continuation** — 5 commits 2026-05-12 (`5c8b53e` + `802ad90` + `38ca915` + `16334b2` + `51e0da7`). Daytime slot following s173. **Closes audit findings #2 + #3** plus 3 bonus items spotted in the area: (a) `5c8b53e` ENGINE_VERSION 0.60.0 → 0.61.0 across 6 living docs (closes finding #2); (b) `802ad90` DS test count 929/911 → 949 cleanup (canonical from `agents/daemon_slayer/` suite); (c) `38ca915` 4 adversarial fixtures + `test_mode_transition` + s151 `gameTime` ref-error fix (closes CLAUDE.md item-10 deferral + BACKLOG `Adversarial sim fixtures`); (d) `16334b2` champion alias unification — new `web/data/champion_aliases.json` canonical source consumed by `items_index.js` + `dashboard.js` async fetch + `daemon_slayer_extract.py` sync read, with `tests/test_champion_aliases.py` 4 regression guards (closes finding #3); (e) `51e0da7` console-pipe `flushQueueAfterSuccess` per-entry removal — fixes pre-emptive `localStorage.removeItem` that lost queued entries on mid-flush failure (closes BACKLOG `Console-pipe localStorage flush failure-recovery loop`). 836 tests green. **Only finding #1 (frozen-file list drift) remains open** — needs operator approval since `tools/process-bridge-tasks.md` is frozen.

28. ✅ **s171 LCU + DS + UX bug crawl** — 8 commits 2026-05-12 (`73b66ec`→`d12a330`). (a) **`lcuCmd`/`lcuPollResult` restored to `main.js` module scope** — 22 callsites had been silently throwing ReferenceError since the s133 Phase 3.1 ESM split (38ac760); all lobby buttons (Find Match / Cancel / queue-change / party toggle) now work. (b) **6 new lobby LCU handlers** (set_party_type / set_position_prefs / invite_player / create_practice_tool / promote_leader / kick_member) + **5 P&B commands added to dashboard allowlist** that were silently 400-rejected since s166 (set_pick_intent / set_ban_intent / request_position_swap / request_pick_order_swap / set_augment_intent). (c) **New champ-select view ships** — lock button on My Pick card, DS-driven build chooser (replaces hardcoded variants), `champSelectViewEnabled()` flipped opt-in→opt-out. (d) **Active-match view** — opt-in→opt-out, `coach.immediate` now renders as RIGHT NOW (was being dropped), freshness guard clears stale prompts between games, **step 4 map overlay** (static SR/ARAM + champion-dot canvas overlay + MIA badges + JG-gank warning). (e) **Enemy-aware DS ranking** (`core/enemy_aware_stats.py`) — `/api/ds-preview` now computes target_armor/_mr/_max_hp from liveclient `allPlayers[i].items[]` via ddragon_items.json; rankings shift as enemies build defense (BoRK +101 vs synthetic 150 armor). (f) **Defensive-pick ranker** (`core/defensive_picks.py`) — classifies enemy threat profile (AD/AP/burst/tank) + recommends from 22-item curated catalog (Maw/Sterak/GA/Plated/Randuin/etc.) when `burst≥5 OR ad≥7 OR ap≥7`. (g) **Top 8 server-side persistence** (`/api/top8`) — moved from localStorage (origin-dependent, wiped on cache-clear) to `data/top8_list.json`. (h) **`/api/mains` backend** — top-N main champions joined from `rewind_history.db.participants` + LCU mastery overlay. (i) **`my_completed` derivation fix** — was reading non-existent `myTeam[i].completed`, now walks `sess.actions[][]`; `local_cell` surfaced. (j) **championPickIntent hover fallback** + **auto_accept default flipped True→False**. (k) **Post-CS view-routing sticky guard** (`_VIEW.gameStarted`) rides through transient phase=null/Lobby during CS→loading→game flip.

31. ✅ **s174 — Phase 1 Tank EHP scorer** (2026-05-12). Kicks off the archetype-expansion plan in `NEXT_SESSION_PLAN_2026-05-12_ARCHETYPE_EXPANSION.md`. New `agents/daemon_slayer/ehp.py` (~440 LOC) sibling of `dps.py`: `compute_ehp()` + `EhpResult` + `rank_items_by_ehp()` + `EhpRankedItem` + `EhpRankResult`. Closed-form League math — `physical_ehp = hp / armor_factor(armor)`, magical mirror via MR, `true_ehp = hp`, `blended_ehp` weighted by caller-supplied `enemy_ad_share` / `enemy_ap_share` (remainder = true). ARAM `aramDamageTaken` modifier folded into all EHP components. Two new server routes (`/ehp`, `/rank-tank`) + `rank_tank_for()` / `ehp_for()` client helpers + `TankRankedItem` dataclass. `core/defensive_picks.py` gained `recommend_defensive_items_via_ehp()` — Option B layer wiring (`_DEFENSIVE_ITEMS` as `only_item_ids` whitelist on the EHP ranker; falls back to heuristic when engine down). ENGINE_VERSION 0.62.0 → 0.63.0. Live-validated: Malphite vs 90/10 AD picks Randuin's > Dead Man's > Sunfire > Jak'Sho > Thornmail; Nasus vs 30/70 AP picks Kaenic Rookern > Warmog's > Jak'Sho > FoN > Heartsteel. Tests: 67 new (44 ehp + 19 rank_tank + 4 server EhpRouteTests) → DS suite 955 → 1022; wider RC suite 839 green. Phase 1 deliberate omissions deferred to 1.5 per plan: shield throughput (Sterak's, Doran's Shield), healing throughput, caster-side enemy pen modeling.

32. ✅ **s175 — Phase 2 Bruiser hybrid scorer** (2026-05-12). Second of the 6 archetype scorers per the same plan. New `agents/daemon_slayer/hybrid.py` (~450 LOC) composing `compute_dps()` + `compute_ehp()` into `hybrid_score = α·dps + β·ehp`: `compute_hybrid()` + `HybridResult` for the scorer, `rank_items_by_hybrid()` + `HybridRankedItem` + `HybridRankResult` for the ranker. Per-champion (α,β) overrides ship in new `agents/daemon_slayer/archetype_weights.json` — 20 bruiser entries (Jarvan IV 0.55/0.45, Darius 0.65/0.35, Garen 0.55/0.45, Camille 0.65/0.35, Renekton 0.65/0.35, Sett 0.55/0.45, Mordekaiser 0.50/0.50, Riven 0.70/0.30, Volibear 0.55/0.45, Nasus 0.50/0.50, Olaf 0.60/0.40, Skarner 0.50/0.50, Hecarim 0.55/0.45, Udyr 0.55/0.45, Vi 0.55/0.45, Xin Zhao 0.60/0.40, Lee Sin 0.65/0.35, MonkeyKing/Wukong 0.60/0.40, Warwick 0.55/0.45, Trundle 0.60/0.40) plus default (0.50, 0.50) fallback. Ranker sort key uses normalized percentage delta (`α · dps_delta/baseline_dps + β · ehp_delta/baseline_ehp`) so the weights stay intuitive across the ~10× DPS/EHP magnitude gap. Two new server routes (`/hybrid`, `/rank-bruiser`) accept optional `alpha`/`beta` override params + `rank_bruiser_for()` / `hybrid_for()` client helpers + `BruiserRankedItem` dataclass. ENGINE_VERSION 0.63.0 → 0.64.0. Live-verified on Legion :8893: Jarvan IV at lvl 11 picks Trinity Force first; Nasus with α=0.2/β=0.8 override surfaces Heartsteel + Warmog's in the top 5. Tests: 44 new (40 hybrid + 4 server HybridRouteTests) → DS suite 1022 → 1066; wider RC suite 839 green.

33. ✅ **s176 — Phase 3 CS archetype-picker UI + dispatcher** (2026-05-12). Third of the 6 archetype scorers per the plan; this is the UI lift that unlocks operator-driven scorer selection. New `core/archetype_picks.py` (~300 LOC) owns the data layer: DDragon-tag → archetype mapping (Fighter→bruiser, Mage→mage, Marksman→carry, Tank→tank, Support→enchanter, Assassin→assassin), per-champion override persistence in `data/cs_archetype_picks.json` via atomic write, and `get_archetype_for(champion)` merging override + tag-default with `source` field. New `dashboard/routes_archetype.py` exposes `GET /api/cs-archetype-pick?champion=X` + `POST /api/cs-archetype-pick {champion, primary, secondary?, source?}` + clear via `{champion, clear: true}`; wired into `dashboard/_dispatch.py`. New `rank_for_primary_archetype()` dispatcher in `core/daemon_slayer_client.py` routes carry/bruiser/tank → ds.dps/ds.hybrid/ds.ehp respectively; mage/assassin/enchanter fall back to ds.dps with `fell_back=True` until Phases 4-6 ship dedicated scorers; returns a canonicalized `{ok, scorer, archetype, ranked, fell_back}` envelope. New picker UI panel in `web/js/panels/champ_select.js` (~150 LOC) — 6-button 3×2 grid in the My Pick card between lock button and build chooser; clicks save to `localStorage.rc-cs-archetype-<champion>` + POST to the new endpoint; unimplemented scorers grayed but still clickable. New CSS section `.csv-archetype-picker` in `web/css/panels/champ_select_view.css`. **Phase 3 deliberately deferred:** state-builder injection (needs server-side champion-id → name resolver, parking lot for coach-wiring session); coach integration (none of aram/arena/brawl coaches read `state.cs_archetype_pick.primary` yet — UI persists the pick, the dispatcher is callable, but coaches still call `rank_for()` directly until the coach-wiring follow-up session); first-purchase-mismatch soft-nudge; in-game switch tab; secondary-archetype refresh on enemy/self item-complete events. Tests: 63 new (33 archetype_picks + 15 routes_archetype + 15 dispatcher) → wider RC suite 1905 → 1968 green; DS suite unchanged at 1066. Live-verified: `/api/cs-archetype-pick?champion=Aatrox` returns DDragon-tag default (bruiser/tank), POST persists override, clear=true rolls back to default.

34. ✅ **s177 — Phase 4a champion ability ingest** (2026-05-12). First of the 3-session Phase 4 (Mage ability DPS scorer) lift per the same archetype-expansion plan; this slot ingests the Meraki Analytics champion ability dataset so Phase 4b can layer the formula evaluator on top. New `tools/daemon_slayer_abilities_extract.py` (~360 LOC) fetches the Meraki bulk champions endpoint (`https://cdn.merakianalytics.com/riot/lol/resources/latest/en-US/champions.json`, 13MB single request) + writes versioned snapshot at `data/daemon_slayer/<patch>/champion_abilities.json` (versioned next to the existing `champions.json` / `items.json` / `items_meraki.json` snapshots). For each champion × ability key (P/Q/W/E/R), each form is normalized to `{name, cooldown[], cost[], damage_type, targeting, affects, is_aoe, damage_blocks[], parse_status}`. Damage blocks carry per-rank scaling fields: `base`, `total_ad_pct`, `bonus_ad_pct`, `ap_pct`, `caster_max_hp_pct`, `caster_bonus_hp_pct`, `target_max_hp_pct`, `target_missing_hp_pct`, `target_current_hp_pct`, `target_bonus_hp_pct`, `target_armor_pct`, `bonus_armor_pct`, `bonus_mr_pct`, `caster_max_mp_pct`. Unit strings map via `_UNIT_TO_FIELD` (with double-space variants pinned for Meraki's text-formatting drift). Non-damage attributes (heal/shield/slow/duration) preserved with `attribute_kind` tag for future scorer expansion. Multi-form abilities preserved verbatim: Aphelios 6× (Q/P weapon stances), Jayce/Elise/Karma/LeeSin/Nidalee 2× per affected key (form swaps), Sylas E 2×. New `agents/daemon_slayer/abilities.py` (~280 LOC) loader exposes `AbilitiesSnapshot.load()` + `AbilityForm` + `DamageBlock` frozen dataclasses + module-level `load_default()` singleton mirroring `ult_rates.py`'s lazy-cache pattern. **Coverage:** 171/172 DDragon champions (Meraki bulk lags Zaahen by one patch — documented), 927 ability forms ingested. Status classifier reports **98.4% ok / 99.3% parsed** (target 80% per the plan) — only 4 unparsed niche aggregates remain (Illaoi.E spirit reflection, Ryze.R legacy field, Trundle.R ult HP-drain, MonkeyKing.W clone-output scalar). ENGINE_VERSION 0.64.0 → 0.65.0. Two extractor bug fixes this session: bulk-key vs per-champion-key Meraki schema mismatch (bulk uses DDragon-style top-level keys like "Aatrox"; per-champion endpoint reverses `id`/`key` fields) — fixed by using bulk's top-level key directly. Empty damage block from aggregate fields (Nidalee Q "Maximum Increased Damage" has no Meraki modifiers) was falling through to "unparsed" — fixed with three-disposition status logic (typed_blocks / unparsed_blocks / empty_blocks). **Phase 4b starts next:** `compute_ability_dps()` evaluator + cast-rate plumbing for P/Q/W/E (extends `ult_rates.py`'s ult-only schema) + mana-economy denominator. Phase 4c wires `rank_items_by_ability_dps()` + `/rank-mage` route + dispatcher integration. Tests: 85 new () → DS suite 1066 → 1151; wider RC suite 902 green. DS server NOT restarted this session — `abilities.py` is only loaded when Phase 4b's evaluator calls into it; `/health` will report 0.65.0 after next ENGINE_VERSION-touching ship.

35. ✅ **s179 — Phase 4c mage ability DPS ranker + `/rank-mage` + dispatcher** (2026-05-12). Third of the 3-session Phase 4 lift; this slot consumes Phase 4b's `compute_ability_dps()` evaluator and ships the per-archetype ranker so the `rank_for_primary_archetype()` dispatcher's mage branch routes to a real scorer instead of falling back to ds.dps. New `rank_items_by_ability_dps()` + `AbilityDpsRankedItem` + `AbilityDpsRankResult` dataclasses appended to `agents/daemon_slayer/ability_dps.py` (~250 LOC of additions) — mirrors `rank_items_by_hybrid` / `rank_items_by_ehp` shape (same `_filter_candidates` pipeline + `delta` / `efficiency` sort keys + `strip_arena_trinkets` + dead-unique dedup + Arena trinket strip), but each candidate is scored by total-ability-DPS gain over the baseline rather than auto-attack DPS or blended EHP. New `/rank-mage` server route in `agents/daemon_slayer/server.py` — body is union of `/ability-dps` and `/rank` parameters; shared `_parse_max_priority` / `_parse_form_index` helpers extracted so `/ability-dps` and `/rank-mage` both parse operator-supplied priority/form overrides identically (list / comma-string / compact "QWE"). New `MageRankedItem` + `rank_mage_for()` + `ability_dps_for()` client helpers in `core/daemon_slayer_client.py`. **Dispatcher mage branch wired:** `rank_for_primary_archetype()` now routes `mage` → `rank_mage_for()` (returns `scorer="ability"`, `fell_back=False`); assassin + enchanter still fall back to ds.dps pending Phases 5-6. ENGINE_VERSION 0.66.0 → 0.67.0. Tests: 40 new (36 in new `test_rank_mage.py` covering basics + scoring + filter-pipeline parity + validation + serialization + mode/amp flow + 8 server route cases; 4 net new in `test_archetype_dispatcher.py` — new `MageRoutingTests` class with 5 tests replaces the old single mage-fallback assertion) → DS suite 1221 → 1257; wider RC 905 → 906 green (was 906 with phase8_smoke failing pre-restart on stale 0.66.0). Live-validated `/rank-mage` on Legion :8893: Veigar lvl 11 naked vs 30 MR → baseline 25.07 ability DPS, primary_scaling=AP, top 5 = Rabadon's +13.58 / Shadowflame +13.26 / Mejai's +11.65 / Stormsurge +11.44 / Void Staff +10.96; BotRK (pure AD) absent from top-3. **Phase 4 lift complete** — `mage` archetype no longer falls back in the dispatcher. **Carried forward to Phase 4d (calibration follow-up):** per-champion `max_priority` overrides JSON (Veigar/Lux always Q-first but Karthus W-first / Akali E-first / Cassiopeia E-first benefit from explicit overrides); per-champion `form_index_overrides` JSON (Aphelios weapon defaults, Jayce stance defaults); coach integration (no coach reads `state.cs_archetype_pick.primary` yet — same as Phases 1/2/3 omission). **Phase 5 (Assassin burst-window) starts next** per the same plan.

36. ✅ **s180 — Phase 5 assassin burst-window scorer + `/rank-assassin` + dispatcher** (2026-05-13). Fifth of the 6 archetype scorers per the same archetype-expansion plan. New `agents/daemon_slayer/burst.py` (~750 LOC) sibling of `ability_dps.py`: `compute_burst_damage()` + `BurstResult` + `ComboCast` + `rank_items_by_burst()` + `BurstRankedItem` + `BurstRankResult`. Per-combo evaluator that walks a caller-supplied `combo_sequence` (default `("Q","W","E","AA","R","AA")`; tokens `AA` / `P` / `Q` / `W` / `E` / `R` / `Q2`-`R2` for repeats at same rank), fires each spell once at level-resolved rank, sums damage blocks via Phase 4b's `_evaluate_block` / `_select_blocks` imports, applies the full Phase 4b amp pipeline (Rabadon's AP amp, Liandry's damage amp, Demonic Embrace HP→AP, Abyssal Mask magic amp on magic-typed spells only, Riftmaker HP→AP, Mejai's stacked AP), and the standard mitigation pipeline (lethality + flat pen + % pen for PHYSICAL; flat + % magic pen for MAGIC; TRUE bypass). Auto-attack hits in the combo contribute the build's per-hit `avg_attack_dmg` from `compute_dps` (post-armor + mode, no on-hit periodic procs — Phase 5.5 deferral). `primary_scaling` classifier inspects un-evaluated damage blocks (AP / AD / HP / MIXED / TRUE) so it's stable across builds. Two new server routes in `agents/daemon_slayer/server.py` (`/burst` + `/rank-assassin`) sharing a new `_parse_combo_sequence` decoder accepting list / dash-string ("Q-W-E-AA-R-AA") / comma-string forms. Sort keys `delta` / `efficiency`. `core/daemon_slayer_client.py` gains `AssassinRankedItem` + `rank_assassin_for()` + `burst_for()` client helpers; `rank_for_primary_archetype()` dispatcher's `assassin` branch now routes to `ds.burst` (returns `scorer="burst"`, `fell_back=False`). ENGINE_VERSION 0.67.0 → 0.68.0. Champion list unblocked: Zed, Talon, Akali, Kha'Zix, Rengar, Fizz, Diana, Kassadin, Katarina, LeBlanc, Qiyana, Pyke, Naafiri, Briar, Yone (all 14 present in the Phase 4a abilities snapshot). Tests: 89 new in DS suite (51 in `test_burst.py` covering token normalization + sequence validation + compute basics + scoring sanity + amp flow-through + mode multiplier + edge cases + server route; 38 in `test_rank_assassin.py` covering ranker basics + scoring + filter pipeline + validation + serialization + mode/amp + 8 server route cases) + 4 net in `test_archetype_dispatcher.py` (new `AssassinRoutingTests` class with 5 tests replacing the old single fallback assertion) → DS suite 1257 → 1346; wider RC 899 green. Live-validated `/rank-assassin` on Legion :8893: Zed lvl 11 vs 80 armor / 30 MR / 2000 max HP → baseline 333.9 burst (primary_scaling=AD), top 8 = IE +250.2 / BT +213.3 / Umbral Glaive +205.6 / LDR +204.6 / Serylda's +203.6 / Bastionbreaker +202.0 / Axiom Arc +191.0 / Youmuu's +191.0 — lethality + armor-pen items dominate as expected for AD assassins. **Phase 5 complete** — 5 of 6 archetype scorers fully wired through the dispatcher; only `enchanter` falls back to ds.dps. **Carried forward to Phase 5.5 (calibration follow-up):** real cooldown sequencing, mana economy modeling, on-hit periodic AA procs (Wit's End, BotRK Mist's Edge — currently `avg_attack_dmg` only captures raw armor+crit AD), per-champion combo templates JSON (Kha'Zix isolation Q, Akali R2-after-R1, Zed shadow R+Q2), conditional damage amps (Ahri R→Q, Zoe E→Q), coach integration (no coach reads `state.cs_archetype_pick.primary` yet — same as Phases 1/2/3/4 omission). **Phase 6 (Enchanter healing throughput, `ds.hps`) is the last remaining scorer** per the plan.

37. ✅ **s181 — Phase 6 enchanter healing throughput scorer + `/rank-enchanter` + dispatcher** (2026-05-13). Sixth and final archetype scorer per the same archetype-expansion plan; closes the dispatcher's `fell_back=True` set entirely. New `agents/daemon_slayer/hps.py` (~625 LOC) sibling of `ehp.py`: `compute_hps()` + `HpsResult` + `HpsItemContribution` for the evaluator; `rank_items_by_hps()` + `HpsRankedItem` + `HpsRankResult` for the ranker; new `EnchanterFormulasSnapshot` + `EnchanterItemFormula` data classes with module-level `load_default_formulas()` + `reset_formulas_cache()` singleton (mirrors `abilities.py` / `ult_rates.py` pattern). Per-item formulas live in new hand-curated `data/daemon_slayer/16.9.1/enchanter_items.json` (~150 LOC) covering 9 enchanter items: Moonstone Renewer 6617 (30% chain amp), Redemption 3107 (active AoE heal 150→350 / +10% H&S power), Mikael's Blessing 3222 (active 100→250 single-target heal + cleanse / +12% H&S power), Echoes of Helia 6620 (Soul Siphon ~50 HP × 0.4 procs/sec + 15% AP scaling), Ardent Censer 3504 (15 ally_buff_credit + 10% H&S power), Staff of Flowing Water 6616 (12 ally_buff_credit + 10% H&S power), Locket of the Iron Solari 3190 (active AoE shield 290→360 / 90s CD), Imperial Mandate 4005 (6 ally_buff_credit for damage proc), Knight's Vow 3109 (10 ally_buff_credit for ally tank-share). Each item carries per-proc base/per-level/AP-scaling for healing and shielding, procs-per-second, targets-per-proc, heal_shield_amp_pct, and ally_buff_credit_per_second (calibrated to ~10 HPS-equivalent). Total throughput = (healing_raw + shielding_raw) × product(1 + amp_pct) × mode_mult + sum(buff_credit). ARAM mode applies `aramShieldsHealing` modifier when present on the champion (defaults to 1.0). Operator can pass `targets_per_proc_override` (e.g. Arena 2v2 → override=1) to retune the "average teammate" assumption. Two new server routes (`/hps` + `/rank-enchanter`) sharing a new `_opt_targets_override` decoder; ranker's `enchanter_only` boolean (default True) restricts the candidate pool to the curated registry + Arena/ARAM mode mirrors by name match. `core/daemon_slayer_client.py` gains `EnchanterRankedItem` + `rank_enchanter_for()` + `hps_for()` client helpers; `rank_for_primary_archetype()` dispatcher's `enchanter` branch now routes to `ds.hps` (returns `scorer="hps"`, `fell_back=False`). ENGINE_VERSION 0.68.0 → 0.69.0. Champion list covered: Lulu, Soraka, Janna, Karma, Sona, Yuumi, Nami, Seraphine, Renata Glasc, Senna (support variant). Tests: 80 new in DS suite (46 in `test_hps.py` covering formula loader + scoring basics + amp pipeline + mode multiplier + targets override + edge cases + server route; 34 in `test_rank_enchanter.py` covering ranker basics + scoring sanity + filter pipeline + validation + serialization + mode/override + 8 server route cases) + 5 new `EnchanterRoutingTests` class in `tests/test_archetype_dispatcher.py` replacing the old single fallback assertion + 1 new EngineDownTests case → DS suite 1346 → 1426; wider RC 899 → 915 green. Live-validated `/rank-enchanter` on Legion :8893: Soraka lvl 11 naked → baseline 0.0 HPS, top 8 = Helia +25.14 / Ardent +15.00 / Staff +12.00 / Locket +11.04 / Knight's Vow +10.00 / Redemption +7.36 / Mandate +6.00 / Mikael's +3.76. With Redemption+Mikael's already built (baseline 12.17 HPS), Moonstone appears at +3.05 — confirms amp compounding works (1.10×1.12×1.30 product). **Phase 6 complete — archetype-expansion plan fully shipped.** All 6 archetypes (carry/tank/bruiser/mage/assassin/enchanter) have their own scorer; dispatcher has no fallbacks. **Plan archive next session:** `NEXT_SESSION_PLAN_2026-05-12_ARCHETYPE_EXPANSION.md` can move to `docs/_archive/`. **Carried forward to Phase 6.5 (calibration follow-up):** real ally-state plumbing (positions, HP, buff uptime), champion-spell healing throughput (Soraka W / Lulu E / Janna E — only ITEM throughput scored), heal_shield_power amp on champion abilities (applies to items only), coach integration (no coach reads `state.cs_archetype_pick.primary` yet — same as Phases 1/2/3/4/5 omission).

39. ✅ **s183 — Dashboard JS scorer-aware unit rendering** (2026-05-13). Follow-up to s182's coach-dispatch wire-in. New `web/js/lib/scorer_units.js` (~35 LOC) single source of truth on the JS side, mirroring the Python `_UNIT_SUFFIX` table in `coach_integration/archetype_dispatch.py:52-59`: exports `scorerUnit(scorer)` + `formatDsDelta(row)`. Maps `dps`→"dps" / `ehp`→"ehp" / `hybrid`→"%" / `ability`→"adps" / `burst`→"burst" / `hps`→"hps". 9 callsites across 5 panel files migrated from inline ``+${Math.round(r.delta_dps)}dps`` to the helper: `web/js/panels/item_build.js` (`#ds-pill` top-pick render + `#ib-ds-block` chip strip), `web/js/panels/champ_select.js` (`_fetchDsPreview` tile reasons + `_csvBuildVariantsFor` Build Chooser cache), `web/js/panels/next.js` (Build-row fallback `DS: <name> +Ndps`), `web/js/panels/active_match.js` (`_dsIcon` tooltip — on-icon caption stays dimensionless to preserve 48px cell). Backend `dashboard/routes_state._serve_ds_preview_post` `result` dict stamps `"scorer": scorer` on every row of the `ranked` response — closes the gap for callers caching just rows (CS Build Chooser `_CSV_DS_CACHE`). `dashboard/api_schema.py` `DsPreviewItem` gains `scorer: str = "dps"`; `DsPreviewResponse` gains `scorer` + `archetype` siblings (s182 backfill); `DsPreviewRequest` gains optional `archetype: str = ""` (s182 backfill). `web/js/lib/state_schema.js` JSDoc typedefs extended. Tests: 7 new in new `tests/test_routes_ds_preview_scorer.py` (stub HTTP handler captures `_send`; dispatcher boundary mocked — no live DS server needed; covers carry/tank/enchanter/hybrid per-row stamp + archetype override propagation + empty-ranked + engine-down 503) → wider RC 948 → 955 green; DS suite unchanged 1426; panel snapshot suite 11 green. ENGINE_VERSION 0.69.0 unchanged (no engine code changed); no DS server restart needed. **Dashboard.js noted dead** (no `<script>` reference in `web/index.html`; only `/js/main.js?v=...` is loaded). **Pre-s182 supervisor backward-compat preserved:** `scorerUnit()` falls back to "dps" when `row.scorer` is absent — JS renders "+Ndps" exactly as pre-s182 for the running pid 15428. **Carried forward:** (a) RC supervisor restart still pending from s182 (operator-driven); (b) DS server :8893 was down at session start (`schannel SEC_E_INVALID_TOKEN` on /health), unrelated to s183 — relaunch via `Start-Process pythonw tools\start_daemon_slayer.py`; (c) calibration analysis pickup is additive — `core/ds_calibration` `ds_picks` rows carry `scorer` per-row from s182's `display_rows` write, downstream consumers see it as new optional dict key.

38. ✅ **s182 — Coach archetype dispatch wire-in + state-builder stamp** (2026-05-13). Cross-phase coach-integration follow-up to the archetype-expansion plan (Phases 1-6 shipped s174-s181); plan archived to `docs/_archive/`. New `coach_integration/archetype_dispatch.py` (~210 LOC) exposes `dispatch_for_coach()` + `CoachDispatchResult` + `display_label()` — resolves the operator's primary archetype for `champion` via `core.archetype_picks.get_archetype_for`, calls `rank_for_primary_archetype()`, returns `CoachDispatchResult` with `picks_str` (scorer-aware unit suffix: "dps"/"ehp"/"%"/"adps"/"burst"/"hps") + `display_rows` (legacy `daemon_slayer_picks` schema {id, name, delta_dps, gold} preserved for dashboard JS backward-compat, plus new `delta` + `scorer` fields for future disambiguation) + raw `rows`/`out`/`archetype`/`scorer`. All 4 mode coaches (SR via `coach_integration/_coach.py`, ARAM via `coaches/aram_coach.py`, Arena via `coaches/arena_coach.py`, Brawl via `coaches/brawl_coach.py`) swapped from direct `daemon_slayer_client.rank_for()` to the helper — DS picks now match the champion's archetype (Lulu/Soraka → ds.hps via healing items; Malphite → ds.ehp via tank items; Veigar → ds.ability via AP items; Zed → ds.burst; Jarvan IV → ds.hybrid; Caitlyn → ds.dps default). User-prompt label dynamic per scorer (`DS top items ({label} ranked, own-items-accounted)`). `dashboard/routes_state._serve_ds_preview_post` routes through dispatcher with optional `archetype` payload override (CS picker UI uses for hover preview); response gains `scorer` + `archetype` siblings. `dashboard/_state_builder.build_state()` stamps `state.cs_archetype_pick` derived from new `_active_champion()` priority (liveclient.champion → coach.champion → lcu.champ_select.local_pick.champion_name → ""), decorative for picker UI; coaches resolve independently. ENGINE_VERSION 0.69.0 unchanged — no DS engine math changed. Tests: 33 new (19 in `tests/test_coach_archetype_dispatch.py` covering empty/engine-down + 6-scorer routing + picks_str format + display_rows shape + archetype propagation + label table + internal helpers; 14 in `tests/test_state_builder_archetype_pick.py` covering `_active_champion` priority order + build_state stamping + error fallback) → wider RC 915 → 948; DS suite unchanged 1426. Live-validated end-to-end via helper invocation: Malphite SR lvl 11 → `Warmog's(+1690ehp,3100g) > Heartsteel(+1521ehp,3000g) > Kaenic Rookern(+1518ehp,2900g)`; Veigar → `Void Staff(+11adps,3000g) > Rabadon's(+11adps,3500g)`; Soraka → `Helia(+25hps,2200g) > Ardent(+15hps,2200g)`; Caitlyn → `BotRK(+69dps,3200g) > Trinity Force(+49dps,3333g)`. **Carried forward:** (a) running RC supervisor needs `restart_trigger.txt` to pick up state-builder + coach changes (operator-driven decision); (b) dashboard JS `#ds-pill` + `#cs-ds-block` + active-match panel still render "+Ndps" for non-DPS scorers (numerically correct, label drift — follow-up session for scorer-aware unit rendering); (c) `core/ds_calibration` `ds_picks` payload now carries `scorer` sibling for future calibration analysis to disambiguate non-DPS rows; downstream consumer scripts unchanged this session.

40. ✅ **s184 — First-purchase archetype-mismatch soft-nudge** (2026-05-13). Closes the s176 Phase 3 deferral list's first item ("first-purchase-mismatch soft-nudge"). After s176 shipped the dispatcher + picker UI + s182 wired the coaches + s183 fixed the JS rendering, the next obvious UX signal: when the operator's actual first completed item doesn't match their picked archetype, surface a one-time soft nudge so they notice the drift. New `core/archetype_mismatch.py` (~270 LOC) exposes `compute_nudge_payload(coach, lc, lcu_snapshot, cs_archetype_pick) -> dict` + `dismiss_nudge(champion) -> bool` + `reset_nudge_state()` + `get_nudge_state_snapshot()`. **Dispatcher-driven mismatch detection** — no hardcoded item→archetype affinity table; calls `rank_for_primary_archetype(champion, primary, top=15)` once per (champion, game_session) pair and stamps "fired" when operator's first non-trivial completed item isn't in the top-15 ids. The bar adapts to per-champion meta automatically: Yasuo crit IE-builds get IE in top-15 of carry rankings, so IE-first on Yasuo doesn't fire; Veigar AP rankings don't have IE, so IE-first on Veigar does fire. **Item filter denylist** (`_NON_SIGNAL_ITEM_IDS`, ~28 IDs): boots variants (1001, 3006, 3009, 3020, 3047, 3111, 3117, 3158), Doran's (1054-1057), trinkets (3340, 3341, 3348, 3363, 3364), consumables (2003, 2031, 2033, 2055, 2138-2150 elixirs), SR support starters (3850-3864 chain), jungle pets (1101-1104). `_first_completed_item_id` walks `lc.owned_item_ids` in order, returns first ID NOT in denylist — empty string when only starters owned → phase=pending. **Per-game-session dedup** via `_session_token` — prefers `lc.game_id` (liveclient `gameData.gameId` surfaced by s184's `_liveclient` extension), falls back to synthetic `{champion}@{start-floor-min}` derived from `time.time() - game_time_s`. Module-level threadsafe `_NUDGE_STATE` cache; restart-wipes by design (matches `rewind_history.db` ephemerality for in-game state). **Default-source skip:** when `cs_archetype_pick.source == "default"` (DDragon-tag fallback, operator hasn't explicitly picked), evaluator returns `{}` — only fires when operator-set (user_cs / user_ingame) or accepted-prior-nudge sources. Three nudge phases: `pending` (no first item yet — dashboard chip hidden), `no_mismatch` (item in top-15, decision cached so dispatcher not re-called), `fired` (mismatch, `message` + `expected_items[3]` populated). **Engine-down fault-tolerance:** dispatcher returning None or empty `ranked` yields empty payload AND leaves cache untouched, so the next poll retries when DS comes back online — important because the live RC supervisor restarted before DS server settles after a same-cycle DS bounce. `dashboard/_liveclient.py` extended with `owned_item_ids` (parallel int-id list to `owned_items` names, same order — needed because state-builder can't reverse a name→id from `owned_items` without a resolver) + `game_id` siblings (`gameData.gameId` from raw liveclient). `dashboard/_state_builder.py` stamps `state.archetype_nudge` next to `state.cs_archetype_pick`; wrapped in try/except so a fault here can't break /api/state. Two new routes in `dashboard/routes_archetype.py`: `GET /api/archetype-nudge` (diagnostic snapshot — useful for `curl` debugging) + `POST /api/archetype-nudge/dismiss {champion}` (operator clicked the chip's X; idempotent — flips phase to "dismissed" without erroring on re-dismiss). `dashboard/api_schema.py` gains `ArchetypeNudgeDismissRequest` + `ArchetypeNudgePayload` pydantic models. ENGINE_VERSION 0.69.0 unchanged (no DS engine math touched). Tests: 54 new (40 in `test_archetype_mismatch.py` — `FirstCompletedItemIdTests` 9 / `SessionTokenTests` 4 / `EngineModeTests` 4 / `ComputeNudgeNoSignalTests` 5 / `ComputeNudgePendingTests` 2 / `ComputeNudgeFiredTests` 6 (happy + dedup + engine-down + re-eval-on-new-session + no-token-empty) / `DismissNudgeTests` 4 / `NudgeResultShapeTests` 1 / `EvaluateDispatcherTests` 5; 5 in `test_state_builder_archetype_nudge.py` covering build_state stamp + empty + pending passthrough + exception-resilience; 9 in `test_routes_archetype_nudge.py` covering GET snapshot + POST happy/400/idempotent + route-registration smoke) → wider RC 1009 (was 955 in s183 wrap); DS suite unchanged 1426; snapshot panel suite 11 green. **Live-verified** end-to-end on Legion :8888 after `echo restart > restart_trigger.txt` picked up the new code (pid 1360, was pid 12144 from the s182+s183 restart earlier this session): `GET /api/archetype-nudge` → `{"ok": true, "state": {}}`; `POST /api/archetype-nudge/dismiss {champion: "NoSuchChamp"}` → `{"ok": true, "dismissed": false, "champion": "NoSuchChamp"}`; `POST /api/archetype-nudge/dismiss {}` → `400 {"error": "champion required"}`; `/api/state.archetype_nudge` present (empty `{}` since no active champion + no game). **Dashboard JS chip renderer deferred to s184.1** — backend-first ship matches the s174-s183 cadence (each phase shipped backend first, JS renderer follow-up). Operator can verify behavior via raw `/api/state` polling until the chip lands. **Carried forward:** (a) JS chip renderer — needs a small yellow info-style chip near `#ds-pill` showing the nudge message + dismiss button calling `POST /api/archetype-nudge/dismiss`. (b) Calibration analysis additive — `nudge_history` rows could be added to `core/ds_calibration` once we have real games + a few fired nudges to tune the top-15 threshold. (c) Per-game-session token edge case: when liveclient hasn't shipped `gameData.gameId` (older Riot LCU builds may omit it on early game ticks), the synthetic fallback is stable across typical 25-minute games but rolls over per minute if game_time_s itself drifts — minor edge case; real signal arrives mid-game when items complete.

42. ✅ **DS data refresh 16.9.1 → 16.10.1** (2026-05-13, `da67555`). Re-extract via `tools/daemon_slayer_extract.py` + `tools/daemon_slayer_abilities_extract.py`; copied hand-curated `enchanter_items.json` forward; flipped `current.txt`. Notable item math: Doran's Bow 6→8 AD, Doran's Helm 110→140 HP, Gluttonous Greaves 650g→700g + 1%/6→0.6%/10 stacks. Arena augments 219→220. DS server restarted; tests 1426 / wider RC 1013 green; `/health` reports patch=16.10.1.

43. ✅ **s185 — Phase 4d per-champion max_priority overrides** (2026-05-13, `d0c604b`). New `agents/daemon_slayer/champion_max_priority.json` (12 entries) — Cassiopeia/Kayle/Akali/Kassadin/Rumble/Anivia → E-Q-W; TwistedFate/Leblanc/Heimerdinger/Lillia → W-Q-E; Karthus/Vladimir → Q-E-W. Loader + singleton cache + `get_max_priority_for(champion_id) -> (priority_tuple, source)` + `_resolve_max_priority(champion_id, explicit) -> (priority_tuple, source)` ship in `ability_dps.py`. `compute_ability_dps` / `compute_burst_damage` signatures relax `max_priority` from `tuple[str,str,str]=("Q","W","E")` to `Optional[Sequence[str]]=None` — None routes through registry. Result types gain `max_priority_source: str` ("override"|"champion"|"default"). Server route `_parse_max_priority` returns Optional. ENGINE 0.69.0 → 0.70.0. Tests: 39 new in `test_max_priority_overrides.py` → DS suite 1426 → 1465; wider RC 1013. Live: Cassi lvl 9 SR baseline 25.08 adps (override E-Q-W) vs 17.82 adps (forced Q-W-E) — 40% lift from Twin Fang at rank 4.

44. ✅ **s186 — Phase 5.5 per-champion combo_sequence overrides** (2026-05-13, `e8a9a1e`). New `agents/daemon_slayer/champion_combo_sequences.json` (15 entries — all 14 canonical assassins + Briar): Zed → Q-W-E-R-Q2-AA (shadow Q double); Yone → Q-Q2-Q3-AA-E-W-R (chain knockup); Akali → Q-AA-E-R-Q2-AA-R2 (R recast in window); Leblanc → Q-W-E-R-Q2-AA (R-mimic-Q); plus Talon/Katarina/Fizz/Khazix/Rengar/Diana/Qiyana/Pyke/Naafiri/Briar/Kassadin canonical openers. Loader + resolver in `burst.py`. `compute_burst_damage` / `rank_items_by_burst` signatures: `combo_sequence: Optional[Sequence[str]] = None`. Result types gain `combo_sequence_source`. Server route `_parse_combo_sequence` returns Optional. ENGINE 0.70.0 → 0.71.0. Tests: 34 new in `test_combo_overrides.py` → DS suite 1465 → 1499. Live: Zed lvl 11 SR baseline 413.33 burst (override) vs 333.89 (forced default) — +24% from shadow Q at rank 4 + lethality scaling. Serylda's Grudge climbs to #2 since armor-pen scales each Q hit.

45. ✅ **s187 — Phase 4e per-(champion, key) form_index overrides** (2026-05-13, `b48ff97`). New `agents/daemon_slayer/champion_form_index.json` (5 entries): Nidalee Q/W/E → 1 (cougar form — Takedown has 5 dmg blocks vs human Javelin's 2), Elise Q → 1 (spider Venomous Bite), Jayce Q → 1 (cannon Shock Blast), Hwei Q/W/E → 1/3/1 (form 0 is the "Subject:" stance setup with zero damage blocks for each key), LeeSin Q → 1 (Resonating Strike recast). Loader + `get_form_index_for(champion_id)` + `_resolve_form_index_overrides(champion_id, explicit)` in `ability_dps.py`; caller dict merges with registry per-key (caller wins, registry fills gaps). Both scorers + result types gain `form_index_source` + `form_index_resolved`. burst.py imports `_resolve_form_index_overrides` from ability_dps. ENGINE 0.71.0 → 0.72.0. Tests: 35 new in `test_form_index_overrides.py` → DS suite 1499 → 1534. Live: Hwei lvl 11 SR ability DPS collapses 11.95 → 0.02 when forced to form 0 — confirms registry is load-bearing (his form-0 stances are unparseable metadata). Nidalee cougar override delivers 39.02 adps (Takedown rank 4 + Pounce rank 2 + Swipe rank 0). **Carried forward:** Aphelios (Meraki bulk parses all 6 Q forms as no_damage — upstream data gap blocks override); Karma mantra (player choice; needs runtime mantra-state plumbing); Khazix evolved (per-game choice; UI picker eventually).

46. ✅ **s188 — Phase 5.6 per-attack on-hit AA procs in burst** (2026-05-13, `9953c23`). Closes s180 carried-forward (on-hit AA procs). New `dps._per_attack_proc_damage()` helper amortizes each `every_n_attacks` proc as `1/N` per AA, post-mit + post-mode + post-amps (magic-typed procs get `magic_amp`; time-based procs skipped — already captured at rotation level). `DpsResult` gains `per_attack_on_hit_damage: float` (default 0.0, surfaced in `to_dict()`); computed once inside `compute_dps` using the same call_ctx + item_effects already resolved for the rotation pipeline. `burst.compute_burst_damage` reads `aa_probe.per_attack_on_hit_damage` and adds it to `aa_base_per_hit` so each AA token in the combo contributes the richer total. Each AA ComboCast row decomposes base + on-hit in notes. ENGINE 0.72.0 → 0.73.0. Tests: 11 new in `test_on_hit_per_aa.py` → DS suite 1534 → 1545. Live-verified: Akali lvl 11 SR +Wit's End AA contribution 105.6 → 187.5 (+82 from 2 AAs procking Fray magic damage); Zed +BotRK AA contribution 53.9 → 165.0 (+111 from 1 AA procking Mist's Edge HP% + BotRK AD scaling into Q/Q2). Backward-compat: builds with no on-hit items see `per_attack_on_hit_damage=0.0` and AA damage exactly equals `avg_attack_dmg` (test_naked_build_aa_unchanged). **Spellblade CD modeling closed in s189**; conditional damage amps (Ahri R→Q, Zoe E→Q) still carried forward.

47. ✅ **s189 — Phase 5.7 Spellblade-in-burst (armed by ability cast, consumed by next AA)** (2026-05-13, `6aa573b`). Closes the s188 Spellblade deferral. Hand-off note had been inaccurate — claimed "Triforce/Lich Bane currently amortized as 1/N per AA" but Spellblade items actually use `every_n_seconds` (3.0 for TF/LB/ER/IBG/DSund, 1.5 for D&D/Sheen/Bloodsong), and the s188 `_per_attack_proc_damage` explicitly skips time-based procs (`if proc.every_n_attacks <= 0: continue`). So in s188-era burst, Spellblade items contributed **zero** to AA damage — a bigger gap than the deferral suggested. The fix: walk the combo with a `spellblade_armed: bool` flag — any ability token (P/Q/W/E/R or repeat variant) sets armed=True; AA token consumes (armed → fire proc → armed=False). Real 1.5s internal CD is irrelevant in a single-combo window because the arming gate is binding (a fresh spell-cast is required to re-arm) — same architectural decision pattern as s180's combo modeling. New `dps._spellblade_per_proc_damage()` sibling of `_per_attack_proc_damage`: iterates `item_effects`, finds the build's `unique_passive_key=="spellblade"` item (already deduped by `collect_effects` — at most one survives — 8 SR items + 6 Arena mirrors + Sheen + Bloodsong), evaluates per-proc damage through the standard pipeline. `DpsResult` gains `spellblade_per_proc_damage: float = 0.0` + `spellblade_item_name: str = ""`. In `burst.py`, `compute_burst_damage` reads the probe + tracks `spellblade_armed` / `spellblade_procs_fired` / `spellblade_damage_total`; AA branch adds proc damage to that ComboCast's `final_damage`. `BurstResult` gains `spellblade_procs` / `spellblade_damage` / `spellblade_item_name` fields. ENGINE_VERSION 0.73.0 → 0.74.0. Tests: 32 new → DS 1577 / RC 1013. Live: Akali +TF burst 787.9→1111.0 (2 procs +211.1); Zed +Divine Sunderer 413.3→654.0 (1 proc +134.0); `/rank-assassin` Essence Reaver climbs #5 → #2. **Sundered Sky Lightshield Strike closed in s190.**

48. ✅ **s190 — Phase 5.8 Sundered Sky Lightshield Strike in burst** (2026-05-13). Closes s189 carry-forward (a). Sundered Sky (6610 + Arena 226610) carries a `PeriodicProc(name="Lightshield Strike", every_n_seconds=8.0)` — same "next AA after ability cast" mechanic but explicitly NOT in the spellblade unique-passive family (engine comment at effects.py:539: "Sundered Sky uses its own 'Lightshield Strike' label, not Spellblade — distinct mechanic, no dedup"). Two architectural decisions: **(1) don't generalize** — second arm-consume helper, not a `is_ability_triggered_aa_proc` flag (two items doesn't justify abstraction; if a third ships, generalize then); **(2) cap at 1 proc per combo** — Sundered Sky's 8s real CD greatly exceeds typical 2-3s burst window. Cap implemented implicitly via `if lightshield_procs_fired == 0: lightshield_armed = True` gate on ability cast — once the proc lands, no re-arming within the combo. New `LIGHTSHIELD_STRIKE_PROC_NAME = "Lightshield Strike"` constant + `_lightshield_strike_per_proc_damage()` helper in `dps.py`; filters by `proc.name == "Lightshield Strike"` rather than `unique_passive_key` (Sundered Sky has none — proc-name signal is more future-proof too: a future item adding a Lightshield Strike variant would auto-match). Same standard pipeline (`resolve_damage` → `_armor_factor` against appropriate resist → mode multiplier → type-selective `magic_amp` for MAGIC only → `damage_amp`). Returns `(per_proc_damage, item_name)`. `DpsResult` gains `lightshield_strike_per_proc_damage: float = 0.0` + `lightshield_strike_item_name: str = ""`; `to_dict()` carries both. `compute_dps` populates after the existing Spellblade block + emits a notes line when present. In `burst.py`, `compute_burst_damage` reads `aa_probe.lightshield_strike_per_proc_damage`; combo walker gains parallel state vars `lightshield_armed` / `lightshield_procs_fired` / `lightshield_damage_total`. AA branch consumes both Spellblade AND Lightshield independently — a build with Sundered Sky + Trinity Force lands BOTH procs on the AA following the first ability cast (validated by `test_dual_proc_aa_row_carries_both`). ComboCast AA notes restructured to compose `base + on-hit + Spellblade + Lightshield Strike` parts; only present parts surface. `BurstResult` gains `lightshield_strike_procs: int = 0` + `lightshield_strike_damage: float = 0.0` + `lightshield_strike_item_name: str = ""` fields. Notes block reports fired count or idle state diagnostic. ENGINE_VERSION 0.74.0 → 0.75.0. Tests: 28 new in `test_lightshield_strike_burst.py` (8 LightshieldHelperTests + 5 DpsResultLightshieldFieldsTests + 12 BurstLightshieldIntegrationTests + 3 ServerBurstRouteLightshieldTests) + 2 version-pin bumps → DS suite 1577 → 1605; wider RC 1013 green. Live-validated `/burst` on Legion :8893 (DS server restarted pid 12704 → 2968): Aatrox lvl 11 vs 80 armor / 30 MR / 2000 HP (default combo Q-W-E-AA-R-AA, 2 AAs): naked 293.7 → +Sundered Sky 519.9 (+226, LS=1× +133) / +Trinity Force 612.5 (+319, SB=2× +244) / **+TF + Sundered Sky 838.7 (+545 = exactly 226+319, clean additive stacking, SB=2× AND LS=1×)**. Q-AA-W-AA test combo (2 AAs, 2 spells): Sundered Sky alone fires once (CD cap honored even with 2 eligible AAs); TF alone fires twice (no cap); combined fires both. **Sundered Sky Lightshield Strike now in burst, conditional damage amps carried to s191.**

64. ✅ **s206 — Phase 5.9.19 cooldown inheritance from form 0 + README cleanup** (2026-05-14). Closes the s205 carry-forward "Engine None-cooldown fallback". Meraki snapshots set `cooldown=None` for every non-form-0 entry of a form-swap ability (Riven R form 1 Wind Slash / Renekton E form 1 Dice / AurelionSol R form 1 The Skies Descend / Qiyana Q form 1 Elemental Wrath). Pre-s206, `_form_cooldown_at_rank` returned a generic 60s default — wrong both for the `per_spell.cooldown` metadata and for the theoretical-fallback DPS conversion when no measured cast rate exists. **Engine signature change:** `_form_cooldown_at_rank(form, rank, fallback_form: AbilityForm | None = None)` — when primary form's cooldown is None/empty, inherits per-rank cooldown from `fallback_form`. Both call sites (`ability_dps.py:1021` + `burst.py:731`) now pass `forms[0] if form_idx != 0 else None`. Form-swap mechanics share the actual game cooldown with their parent form, so inheritance is correct. **Reframe vs s205 carry-forward note:** The hand-off claimed total ability_dps was under-counted ~8.5× — that diagnosis was wrong because all 4 affected entries have `casts_per_sec_source: "measured"` from `spell_cast_rates.json` (covering 172 champs × Q/W/E/R × 3 modes from 2851 rewind matches), so the cooldown is metadata-only for DPS. Actual s206 impact: (a) `per_spell.cooldown` metadata now correctly reports 90s/110s/7s/16s for the 4 affected entries (was uniformly 60s); (b) ComboCast row metadata in burst output now correct; (c) theoretical-fallback DPS conversion (only fires when measured cast rate is absent) now uses the right CD — forward-compat for new form-swap champions before their measured rates ship. ENGINE_VERSION 0.90.0 → 0.91.0. Tests: 26 new in new `test_cooldown_inheritance.py` (CooldownHelperBackwardCompatTests 5 / CooldownHelperFallbackTests 7 / AbilityDpsCooldownInheritanceTests 7 / BurstCooldownInheritanceTests 3 / ServerRouteCooldownTests 4) + 2 ENGINE_VERSION pin bumps in `test_effects_expansion.py` → DS suite 1953 → 1979 (+26); wider RC 1013 green. Live-validated all 4 entries on Legion :8893: Riven R cd 60→90, Renekton E cd 60→16, AurelionSol R cd 60→110, Qiyana Q cd 60→7. DPS values unchanged because measured cps_source dominates — exactly the intended s206 framing. **Plus README cleanup**: 3 paragraphs compressed (line 12 "Daemon Slayer build engine" bullet + line 244 "Daemon Slayer engine — current state" + line 311 RC Tutor table row) — replaced monotonically-growing s174 → s182 enumerations with stable summaries (the current architecture state, not the changelog of how it got there). Same compression pass applied to `docs/DAEMON_SLAYER.md` line 5 status header + `docs/ARCHITECTURE.md` line 151 DS section. **Carried forward to Phase 5.9.20+:** sum-of-blocks bucket (10+ candidates: Heimerdinger W, Thresh E, Taliyah E, Sona Q, Camille W, Katarina R, Malphite W, Malzahar E/R, Kalista E, Jinx R) — schema lift now warranted; nested missing-HP parser bucket (Kindred E, Kayle E, Belveth R execute curve); conditional target-state schema lift (6+ candidates).

63. ✅ **s205 — Phase 5.9.18 form_index + block_index layered expansion — 4 block_index entries + 3 form_index seeds** (2026-05-14, `24ed6aa`). Pure-data continuation following s204's pattern. 4 new (champion, key) block_index entries: 1 truly-new champion (Qiyana) + 3 key extensions on existing (Hwei W, Renekton E, Shaco W). PLUS 3 new form_index seeds (Qiyana Q=1, AurelionSol R=1, Renekton E=1). **Closes the s204 carry-forward "Qiyana Q form_index seed expansion still pending"** — Qiyana Q form 0 and form 1 share identical block 0 Physical Damage values (60-180 + 90% bAD), so form_index alone is a no-op for Qiyana Q; the s205 block_index registry routes (form_index=1, block_index=2) to capture form 1's 'Increased Damage' (96-288 + 144% bAD = 1.6× base + 1.6× bAD scaling) which IS the canonical elementally-empowered primary-target damage. **Third instance of form_index + block_index NET-damage composition** after s203 LeeSin Q + s204 Riven R + s204 Nidalee Q. block_index registry 117 → 118 champions, 181 → 185 entries; form_index registry 6 → 9 champions, 10 → 13 entries. Zero engine code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.89.0 → 0.90.0. **Three sub-patterns:** **Pattern A operator-commits-to-resource form layer (3 entries):** Qiyana Q (form 1 Elemental Wrath + block 2 'Increased Damage' — operator commits to picking up an element via R/W/E before Q; 1.6× base + 1.6× bAD), AurelionSol R (form 1 'The Skies Descend' Empowered Magic Damage — operator commits to full Stardust; 1.25× base + 1.25× AP; no block_index entry needed since default 0 within form 1 is correct), Renekton E (form 1 Dice + block 3 'Total Physical Damage' = block 0 + block 1 sum = full Slice + empowered Dice + 50-Fury Bonus combo; 2.75× form 0 at rank 1; closes Renekton's Q/W/E full-Fury coverage after s197 Q=1 + W=2). **Pattern B multi-hit single-target totals (1 entry):** Hwei W form 3 block 1 'Maximum Magic Damage' (3× block 0 'Bonus Magic Damage' — 3 Stirring Lights converging on one target; same model as Gwen R 9-needle s203 / Ashe Q 5-AA s199 / Naafiri Q all-3-daggers s197). **Pattern C condition-amp vs target-state (1 entry):** Shaco W block 1 'Increased Damage' (2.5× block 0 base + 1.5× AP — Jack in the Box hits already-Feared target; same operator-commits framing as Khazix Q isolation s196 / Zoe E→Q sleep s204 / Vayne E wall-stun s202 / Talon Q champion-vs-minion s199). All 4 verified per-rank against Meraki snapshot. **Multi-key extensions:** Hwei now `{R:3, W:1}`, Renekton now `{Q:1, W:2, R:1, E:3}` (full Q/W/E/R coverage), Shaco now `{E:2, W:1}`. **Live A/B headlines on :8893 (/ability-dps lvl 11 vs 80 armor / 30 MR / 2000 HP — per-spell DPS lifts since most spells aren't the champion's dominant DPS contributor):** Hwei W **+200%** per-spell (1.26 → 3.79 dps; total +12.1%), Shaco W **+175%** per-spell (0.69 → 1.91 dps; total +7.7%), Renekton E **+175%** per-spell (1.15 → 3.16 dps; total +10.4%), Qiyana Q **+60%** per-spell (6.41 → 10.26 dps; total +40.1%), AurelionSol R **+25%** per-spell (1.38 → 1.73 dps; total +2.3%). Per-cast raw damage lifts match Meraki block ratios: Hwei W 40 → 120 (3× verified), Shaco W 20 → 55 (2.5× verified), Renekton E 40 → 110 (2.75× form 0 = block 0 + block 1 sum at rank 1), Qiyana Q 180 → 288 (1.6× verified), AurelionSol R 250 → 312.5 (1.25× verified). **Spell-share dilution** explains the gap between per-spell and total lifts — AurelionSol's R has 110-second CD vs Q/W/E shorter cooldowns, so R's contribution to total ability_dps is small even when correctly captured. **Known engine limitation:** Meraki snapshots set `cooldown=None` for non-form-0 entries (Riven R / Renekton E / ASol R / Qiyana Q all affected) — DS engine falls back to default 60s CD, dampening the total ability_dps lift. Future calibration work could improve None-cooldown handling. Tests: 21 new in `Phase599_18ExpansionTests` class (5 per-entry `_delta_check` + 3 multi-key resolved-shape (Hwei R+W, Renekton all-4-keys + form_index sanity, Shaco E+W) + 3 math-level numeric sanity (Qiyana Q form 1 block 2 = 1.6× block 0, Hwei W block 1 = 3× block 0, Renekton E form 1 block 3 = block 0 + block 1 sum) + 3 backward-compat regression guards (Thresh s203 unchanged, Riven s204 unchanged, Nidalee s204 form_index layering preserved)) + 4 new explicit assertions in `test_known_champion_overrides` (Hwei + Qiyana + Renekton + Shaco) + 1 stale Renekton 3-key assertion converted to .get() subset check + 5 new form_index assertions in test_form_index_overrides (Riven s204 + Qiyana/AurelionSol/Renekton s205) + 2 ENGINE_VERSION pin bumps in `test_effects_expansion` (0.89.0 → 0.90.0) → DS suite 1939 → 1953 (+14); wider RC 1023 → 1024 (phase8_smoke restored post-DS-restart). **Architectural pattern lock-in continued:** twenty-first consecutive override / proc-shape modeling improvement on the same template (s185-s205); fourteenth pure-data batch in the block_index family; third batch to expand the form_index registry (s187 seed → s204 Riven → s205 Qiyana/AurelionSol/Renekton). Cumulative coverage now at 185 (champion, key) entries across 118 champions (69% of the 171-champion roster touched). **Carried forward to Phase 5.9.19+:** sum-of-blocks bucket unchanged but candidate count growing (Thresh E + Taliyah E + Sona Q + Camille W + Katarina R + Malphite W + Malzahar E/R + Kalista E + Jinx R + Heimerdinger W multi-rocket totals — 10+ candidates queued); nested missing-HP parser bucket unchanged (Kindred E, Kayle E, Belveth R execute curve); conditional target-state schema lift bucket (6+ candidates remain); engine None-cooldown fallback for non-form-0 entries (Riven R / Renekton E / ASol R / Qiyana Q under-score by ~8.5× per-cast DPS conversion — calibration follow-up).

62. ✅ **s204 — Phase 5.9.17 block_index expansion — 2 new champs + 6 extensions + form_index seed** (2026-05-14, `1003ed9`). Pure-data continuation following s203's pattern. 8 new (champion, key) entries: 2 truly-new champions (Nidalee, Seraphine) + 6 key extensions on existing (Evelynn Q, Gwen Q, KSante W, Riven R, Syndra W, Zoe E). PLUS Riven added to form_index registry (`champion_form_index.json`) with R=1 routing to form 1 "Wind Slash" — closes the s203 carry-forward "Riven form_index seed needed for form-conditional block_index entries". Registry 115 → 117 champions, 173 → 181 entries. Form_index registry 5 → 6 champions, 9 → 10 entries. Zero engine code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.88.0 → 0.89.0. **Six sub-patterns:** **Pattern A multi-hit single-target totals (3 entries):** Evelynn Q=5 (Hate Spike Total Magic Damage = 1 initial + 2× 3-missile recasts on same target, 7× missile total; 175% AP scaling vs block 0's 25%), Gwen Q=6 (Snip Snip Maximum Damage = 5 small snips + 1 final big snip on focused target; 10.3× block 0 base, 22.5× block 0 AP — same max-stack burst pattern as s203 Gwen R 9-needle), Syndra W=2 (Force of Will Total Mixed Damage = block 0 Magic Damage + block 1 Bonus Damage sum; 1.12× block 0 — marginal but consistent under-count, captures BoW's Bonus Damage on top of Magic Damage). **Pattern B fully-charged amp (1 entry):** KSante W=3 (Path Maker Total Maximum Mixed Damage at full 2s charge = block 0 Physical + block 2 Maximum Bonus True Damage sum; 1.8× block 0 — same fully-charged amp pattern as Vi Q s198 / Sion Q s197 / Pantheon Q s196). **Pattern C champion-vs-minion amp (1 entry):** Seraphine Q=1 (High Note Maximum Champion Damage; 1.75× block 0 base + 1.75× AP — operator burst targets champions, block 1 captures the canonical champion-burst case vs block 0's minion-reduced fallback). Seraphine routes to ds.hps (enchanter) by default but direct /ability-dps queries benefit — revives s198/s202 "enchanter-class" deferral under same archetype-mismatch framing as Talon Q s199 vs minion crit. **Pattern D execute amp layered on form_index (1 entry):** Nidalee Q=1 (Cougar Takedown Maximum Magic Damage at low-HP target = block 1 in form 1 via s187 form_index Q=1 cougar override; 2.75× block 0 base + 1.3× AP — operator commits to using cougar Q on low-HP targets as canonical Nidalee execute, same model as KogMaw R s196 Living Artillery low-HP execute). **Second NET-damage layering** of block_index on form_index registry after s203 LeeSin Q. **Pattern E target-state amp (1 entry):** Zoe E=2 (Sleepy Trouble Bubble Maximum Mixed Damage = 2× block 0 base + 2× AP scaling on sleep-procced target; canonical Zoe E→Q burst combo). First entry reviving the conditional target-state schema lift bucket under unconditional operator-commits framing — same as Khazix Q isolation s196 / Xerath W center-spot s201 / Vayne E wall-stun s202 / Gnar R wall-stun s202. **Pattern F form_index seed expansion (1 entry):** Riven R=1 + new form_index Riven.R=1 (Wind Slash Maximum Physical Damage at max-missing-HP execute; 3× block 0 base + 3× bonus_ad_pct). Riven R form 0 "Blade of the Exile" has ZERO damage blocks (pure buff/empower form) and form 1 "Wind Slash" carries the only damage — clean form_index seed with no information loss from routing past form 0. Closes s203 carry-forward "Riven form_index seed needed". **First new champion added to form_index registry since s187** (Nidalee/Elise/Jayce/Hwei/LeeSin); Qiyana Q remains carried forward for s205+ under the same form_index seed pattern this batch establishes. All 8 entries verified per-rank against Meraki snapshot. **Live A/B headlines on :8893 (/ability-dps lvl 11 vs 80 armor / 30 MR / 2000 HP — TOTAL ability_dps deltas with per-spell preview deltas in parens):** Evelynn Q **+339.7%** (19.78 → 86.98 total; per-spell +667%), Gwen Q **+168.7%** (8.80 → 23.65 total; per-spell +934%), Nidalee Q **+75.6%** (39.02 → 68.51 total; per-spell +175%), Seraphine Q **+53.2%** (11.11 → 17.02 — single-spell champion so per-spell ≈ total), KSante W **+17.8%** (8.48 → 9.99), Syndra W **+4.2%** (28.06 → 29.23 — small marginal lift consistent with 1.12× block ratio), Zoe E **+3.3%** (44.53 → 45.99 — Zoe Q dominates total at 42.17 dps), Riven R **+2.8%** (57.80 → 59.43; was 0.00 baseline pre-s204 — first non-zero Riven R score via form_index seed; small absolute number because Riven's Q dominates at 54.84 dps). **Spell-share dilution** explains the gap between per-spell preview lifts and total lifts for multi-spell champions (Gwen, Riven, Zoe all have dominant non-Q-or-R spells). Tests: 20 new in `Phase599_17ExpansionTests` class (8 per-entry `_delta_check` + 6 multi-key resolved-shape (Evelynn Q+R, Gwen Q+R, KSante W+R, Riven Q+R + form_index sanity, Syndra W+R, Zoe Q+W+E) + 3 math sanity (Gwen Q block 6 ≥5× block 0, Riven R form 0 has zero damage blocks, Nidalee Q form 1 block 1 ≥2× block 0) + 3 backward-compat regression guards (Thresh s203 unchanged, Blitzcrank s203 unchanged, LeeSin s203 form_index layering preserved)) + 8 new explicit assertions in `test_known_champion_overrides` + 5 stale-shape assertions migrated to subset checks (Evelynn / Syndra / Riven / KSante / Zoe — all extended this batch) + 1 `test_rank_assassin_carries_source` updated for new Evelynn shape `{R:1, Q:5}` + 1 `Phase599_15ExpansionTests.test_zoe_both_keys_in_resolved` converted to per-key subset check (Zoe now has 3 keys after s204 added E=2) + 2 ENGINE_VERSION pin bumps in `test_effects_expansion` (0.88.0 → 0.89.0) → DS suite 1919 → 1939 (+20); wider RC 1023 + 1 expected phase8_smoke pass post-DS-restart. **Architectural pattern lock-in continued:** twentieth consecutive override / proc-shape modeling improvement on the same template (s185-s204); thirteenth pure-data batch in the block_index family; second batch to expand the form_index registry (s187 was the seed). Cumulative coverage now at 181 (champion, key) entries across 117 champions (68% of the 171-champion roster touched). **Carried forward to Phase 5.9.18+:** all s203 carry-forwards remain except Riven (closed); **Qiyana Q form_index seed expansion** still pending (same form_index seed pattern Riven shipped — Qiyana Q form 0 base + form 1 elemental-empowered with all 4 damage blocks but engine currently routes to form 0); sum-of-blocks bucket unchanged (Thresh E + Taliyah E + Sona Q + Camille W + Katarina R + Malphite W + Malzahar E/R + Kalista E + Jinx R — 8+ candidates queued); nested missing-HP parser bucket unchanged (Kindred E, Kayle E, Belveth R execute curve); conditional target-state schema lift bucket now down to 6+ candidates after Zoe sleep shipped under operator-commits framing.

61. ✅ **s203 — Phase 5.9.16 block_index expansion — 5 new champs + 5 extensions** (2026-05-14). Pure-data continuation following s202's broadening pattern. 5 truly-new champions (Blitzcrank, Gwen, Kled, LeeSin, Thresh) + 5 key extensions on existing champions (Diana R, Jax R, Kennen W, Smolder E, Vladimir Q). Kled contributes 3 keys (Q + E + R) for 7 net new entries on new champs. Zero engine code changes — s191/s192 walker logic handles all entries transparently. Registry 110 → 115 champions, 161 → 173 entries. ENGINE_VERSION 0.87.0 → 0.88.0. **Six sub-patterns:** **Pattern A multi-hit single-target totals (5 entries):** Diana R=2 filtered (Moonfall full 2-second channel total = initial pull + 4 ticks = 1.7× block 1; raw block 0 'Slow' stripped pre-index), Gwen R=4 (Needlework 9-needle full 3-cast burst = 9× block 0 — all 5 blocks are damage, no filtering needed), Kled Q=2 filtered (Beartrap on Rope 3-stage reel-in = 3× block 0; raw block 1 'modifier' + raw 4 'slow' stripped), Kled E=1 (Jousting 2-strike dash + return = 2× block 0; no filtering needed), Vladimir Q=1 filtered (Crimson Rush at full passive stacks = 1.85× block 0; raw block 1 'Heal' stripped). **Pattern B resource-state amps (1 net new):** Smolder E=1 (Achooo! at max passive stacks = 5× block 0 — operator commits to stack-building, same model as Smolder Q s202 + Renekton Q s197; reverts s198/s202 'Meraki Minimum schema label ambiguity' skip). Vladimir Q also fits this category (Crimson Rush builds via taking damage). **Pattern C active-cast vs passive-zap split (3 entries):** Blitzcrank R=1 (Static Field active burst 275-525 + 100% AP — engine default block 0 was scoring the passive per-zap 50-150 + 30% AP + 2% caster max MP as the R cast value; under-counted realistic R cast by ~3-5×), Kennen W=1 (Electrical Surge active cast burst 70-170 + 80% AP — engine default block 0 was scoring passive per-4th-AA Mark of the Storm detonation as the W cast value), Jax R=1 (Grandmaster's Might active 3-AA enhancement total 100-250 + 100% AP — engine default block 0 was scoring passive every-3rd-AA 75-185 + 60% AP; small lift +1.1% because Jax R has long CD but architecturally correct). **Pattern D max-charge condition amp (1 entry):** Kled R=1 filtered (Chaaaaaaaarge!!! at max charge time = 3× block 2 minimum; raw blocks 0-1 'shield'/'shield' stripped). **Pattern E missing-HP amp layered on form_index (1 entry):** LeeSin Q=1 (form 1 Resonating Strike at max-missing-HP = 2× block 0; composes with s187 form_index=1 routing to Resonating Strike form, then block_index=1 routes to max-missing-HP variant within form 1). **First instance of layering block_index on form_index that adds NET damage** — Jayce Q s194 was the smaller-magnitude precedent. **Pattern F empty-block-0 fix (1 entry):** Thresh E=2 (engine default block 0 has ONLY unparsed `1.7 per Soul collected` scaling — no base, no AP, no tAD — and `_evaluate_block` returns 0 for it; registry routes to filtered idx 2 = raw block 2 = canonical 75-255 + 70% AP magic damage component. **First instance of "empty-block-0" fix pattern** — Thresh routes to ds.hps by default (enchanter); entry here is for direct /ability-dps queries). **Filtered-idx semantics:** 4 of 12 entries have non-damage prefix blocks stripped pre-index — Diana R raw 0 'Slow' → filtered idx 2 = raw 3. Kled Q raw 1 'modifier' + raw 4 'slow' → filtered idx 2 = raw 3. Kled R raw 0-1 'shield' / 'shield' → filtered idx 1 = raw 3. Vladimir Q raw 1 'Heal' → filtered idx 1 = raw 2. All 12 verified per-rank math against Meraki snapshot — `test_gwen_R_block4_matches_9x_block0_base` + `test_kled_Q_filtered_idx2_matches_3x_block0_bAD` + `test_leesin_Q_block1_matches_2x_block0_bAD_in_form1` + `test_thresh_E_block0_evaluates_to_zero` pin four specific math invariants. **Reverts 1 prior skip rationale:** Smolder E (s198/s202 'Meraki Minimum schema label ambiguity' → s203: re-framed under operator-commits-to-max-stacks, identical model to Smolder Q s202 successful reintroduction). **Multiple deliberate skips documented in registry _meta description:** DrMundo Q (block 0 = target_current_hp_pct 20-30% which scales with target HP — engine default is HIGHER vs full-HP targets, only WORSE at low HP; not a clean replacement), Caitlyn Q/R (block 0 is realistic primary-target burst, subsequent blocks are reduced cleave), Ezreal R (block 0 is first-enemy-hit at full damage), Nocturne Q/E (block 0 is direct/full damage), Orianna Q (block 0 first-target full), Pantheon R (raw block 0 non-damage, filtered idx 0 = raw block 1 already correct), Trundle Q (block 1 = AA-bonus only, LESS than block 0 — sum-of-blocks needed), Trundle R (engine default already picks the Maximum tMaxHP% total), Yone W/R + Zed Q (block 0 = primary target, subsequent = reduced), AurelionSol R form 1 (block 1 < block 0 — block 0 is max-center hit), Pyke/Quinn/Senna (uncertain mechanic without explicit Maximum block), Vayne W (passive on-hit AA proc — 0 CD ability doesn't fit per-cast ability_dps; sum-of-blocks anyway for flat + maxHP). **Deferred to schema lifts (8 candidates):** Katarina R (sum-of-blocks across bAD + AP scaling streams), Malphite W (sum of first AA + N subsequent AAs), Malzahar E/R (sum of on-cast + DOT ticks), Kalista E (per-stack accumulation), Jinx R (distance-scaled max via per-100-unit scaling), Belveth R (target_missing_hp_pct execute), Riven R form 1 (needs form_index registry seed for Riven), Qiyana Q (needs form_index registry seed for elemental form). Tests: 25 new in `Phase599_16ExpansionTests` class (12 per-entry `_delta_check` + 6 multi-key resolved-shape (Diana W+R, Jax E+R, Kennen R+W, Smolder W+Q+R+E, Vladimir E+W+Q, Kled Q+E+R) + 4 math sanity (Gwen R 9×, Kled Q 3×, LeeSin Q 2×, Thresh E zero-eval) + 3 backward-compat regression guards (Yunara s202 unchanged, Kennen R s201 unchanged, Smolder W s197 unchanged)) + 10 new explicit assertions in `test_known_champion_overrides` for the 10 affected champions + 2 stale-assertion conversions to .get() subset checks (Vladimir multi-key + Smolder multi-key in earlier Phase599_11 + Phase599_15 expansion classes) + 2 ENGINE_VERSION pin bumps in `test_effects_expansion` (0.87.0 → 0.88.0) → DS suite 1894 → 1919 (+25); wider RC 1024 green post-DS-restart. **Live A/B headlines on :8893 (/ability-dps lvl 11 vs 80 armor / 30 MR / 2000 HP):** Gwen R **+218.4%** (2.76 → 8.80 — 9-needle full burst), Kled all-3-keys-combined **+134.4%** (7.36 → 17.26 — Beartrap + Jousting + Charge full reel), Vladimir Q **+53.8%** (28.09 → 43.19 — Crimson Rush at full stacks), LeeSin Q **+46.4%** (9.99 → 14.62 — Resonating Strike max-missing-HP), Blitzcrank R **+29.4%** (4.60 → 5.96 — active burst vs passive zap), Thresh E **+11.8%** (6.45 → 7.21 — empty-block-0 fix, registry routes past 0-damage block 0), Kennen W **+7.6%** (18.74 → 20.18 — active cast vs passive 4th-AA mark), Smolder E **+6.7%** (20.76 → 22.16 — max-stack Achooo!), Diana R **+5.3%** (16.91 → 17.80 — Moonfall full channel; small because Diana W=2 already dominates), Jax R **+1.1%** (long CD limits per-second contribution). **Architectural pattern lock-in continued:** nineteenth consecutive override / proc-shape modeling improvement on the same template (s185-s203); twelfth pure-data batch in the block_index family. Cumulative coverage now at 173 (champion, key) entries across 115 champions (67% of the 171-champion roster touched). **First "empty-block-0" fix** (Thresh E — DrMundo Q would be similar but rejected because its block 0 has actual scaling via target_current_hp_pct); **first NET-damage layering of block_index on form_index** (LeeSin Q, after s187). **Carried forward to Phase 5.9.17+:** all s202 carry-forwards remain (sum-of-blocks bucket now has 4+ candidates: Thresh E souls+magic component, Taliyah E impact+detonations, Sona Q spell+Power Chord, Camille W flat+max-HP — plus s203-added: Katarina R bAD+AP, Malphite W first+subsequent, Malzahar E/R on-cast+DOT, Kalista E per-stack); **nested missing-HP parser bucket** (Kindred E, Kayle E, Belveth R execute curve); **form_index registry seed expansion** (Riven, Qiyana) needed for next batch to cover form-conditional block_index entries.

60. ✅ **s202 — Phase 5.9.15 block_index expansion — 6 new champs + 12 extensions** (2026-05-14, `723fb1c`). Pure-data continuation following s201's broadening pattern. 6 truly-new champions (Gangplank, Gnar, KSante, RekSai, Vayne, Yunara) + 12 key extensions on existing champions (Zoe W, Akshan R, AurelionSol Q, Nasus R, Poppy E, Renekton R, Rumble Q+R, Smolder Q+R, Viktor E, Yuumi R). Zero engine code changes — s191/s192 walker logic handles all entries transparently. Registry 104 → 110 champions, 143 → 161 entries. ENGINE_VERSION 0.86.0 → 0.87.0. **Five sub-patterns:** **Pattern A multi-hit single-target totals (5 entries):** KSante R=2 (All Out dash + wall-strike 2×), Vayne E=2 (Condemn dash + wall-slam 2.5×), Yunara Q=2 filtered (Combined Passive + Active 2×), Zoe W=1 filtered (3 empowered AAs from Q-W-E rotation 3×), Viktor E=2 (Death Ray double-hit 1.29×). **Pattern B channel/duration totals (7 entries):** Gangplank R=2 (Cannon Barrage 4-wave total 12× per-wave), AurelionSol Q=2 (Breath of Light full 2.5s channel 26× per-tick), Nasus R=1 filtered (Fury of the Sands full 15s target_max_hp_pct), Renekton R=1 filtered (Dominus full 15s aura 30× per-tick), Rumble R=2 (Equalizer max 4s channel 10×), Yuumi R=2 filtered (Final Chapter 2 hits per target 2×), Poppy E=1 filtered (Heroic Charge dash + wall-slam 2×). **Pattern C max-charge / max-distance amps (2 entries):** Akshan R=1 filtered (Comeuppance max-charge bullet 3×), Smolder R=1 filtered (Mouth of the Abyss max-distance 1.5×). **Pattern D resource-state amps (2 entries):** RekSai E=1 (Furious Bite max Fury → true damage 1.25× + armor bypass), Smolder Q=1 (max-stack passive scaling 1.75× — gear-INdependent; block 2 with Infinity Edge is gear-conditional, skipped). **Pattern E wall-stun / charge condition amps (2 entries):** Gnar R=1 filtered (GNAR! wall-stun 1.5×), Rumble Q=2 filtered (Danger Zone overheat Total Enhanced 1.5×). **Reverts 4 prior-batch skip rationales** under operator-commit framing: Gnar R (s198 'wall-stun terrain target-state' → s202: same operator-commits framing as Khazix Q isolation s196 / Xerath W center-spot s201), Vayne E (s198 'wall-stun terrain' → s202: same), Poppy E (s199 'wall-state target condition' → s202: same), Rumble Q (s199 'Danger Zone heat decays mid-fight' → s202: operator commits to overheat Q burst window). **Filtered-idx semantics:** 9 of 18 entries have non-damage prefix blocks stripped pre-index — Nasus R raw 0-2 (Bonus Health / Resistances / Increased Size) → filtered idx 1 = raw 4. Yuumi R raw 0-1 + 5-6 heal blocks → filtered idx 2 = raw 4. Yunara Q raw 1 duration + raw 4-5 modifiers → filtered idx 2 = raw 3. Plus Gnar R / Zoe W / Akshan R / Poppy E / Renekton R / Rumble Q / Smolder R. **6 deliberate skips documented inline:** Syndra W (1.12× too marginal), Camille W (Outer Cone Bonus is target_max_hp_pct ONLY without flat damage; needs sum-of-blocks framework), Yunara W (block 0 'Initial' 5-105 + 85% tAD + 50% AP is HIGHER than block 2 'Total Expanded' 5-65 + 40% tAD + 25% AP — engine default block 0 is canonical), Smolder E (Meraki schema 'Minimum' label ambiguity carried from s198), Sona Q (block 1 is Power Chord bonus on top of block 0 — needs sum-of-blocks), Kayle E (Phase 4a parser limit same as s201 Kindred E drop — block 1 missing-HP coefficient on `target_missing_hp_pct` field requires target_current_hp_pct < 1.0 plumbing). All 18 verified per-rank math against Meraki snapshot. Tests: 28 new in `Phase599_15ExpansionTests` class (18 per-entry `_delta_check` + 5 multi-key resolved-shape (Zoe Q+W, Akshan Q+R, Rumble E+Q+R, Smolder W+Q+R, Viktor R+Q+E) + 2 math sanity (KSante R 2×, Vayne E 2.5×) + 3 backward-compat regression guards) + 13 explicit assertions in `test_known_champion_overrides` for new champions + 8 stale-assertion comment-outs for champions extended this batch (AurelionSol/Akshan/Zoe/Poppy/Renekton/Rumble/Smolder/Nasus/Viktor/Yuumi) + 3 multi-key resolved-shape sanity tests updated in earlier expansion classes (Renekton/Viktor/Poppy now include the new R/E keys) + 2 ENGINE_VERSION pin bumps → DS suite 1866 → 1894 (+28); wider RC unchanged post-DS-restart. **Live A/B headlines on :8893:** AurelionSol Q **+351.6%** (3.29 → 14.85 — Breath of Light full channel is dominant), Renekton R **+68.8%** (11.43 → 19.29 — Dominus full duration aura), Smolder Q **+51.6%** (13.70 → 20.76 — max-stack passive scaling), Nasus R **+48.4%** (9.73 → 14.44), Rumble Q **+18.4%**, Gangplank R **+17.1%**, Rumble R **+9.0%**, RekSai E **+7.1%**, Vayne E **+5.9%**, KSante R **+5.7%**, Yunara Q **+4.0%**, Yuumi R **+3.8%**, Viktor E **+3.1%**, Gnar R **+2.3%**, Smolder R **+2.0%**, Poppy E **+1.8%**, Zoe W **+1.4%**, Akshan R **+0.9%**. Smaller percentages reflect spell-share dilution — Akshan's Q already dominates ability_dps so R contribution is diluted; Zoe's Q dominates so W contribution is diluted, etc. **Architectural pattern lock-in continued:** eighteenth consecutive override / proc-shape modeling improvement on the same template (s185-s202); eleventh pure-data batch in the block_index family. Cumulative coverage now at 161 (champion, key) entries across 110 champions (64% of the 171-champion roster touched). **Carried forward to Phase 5.9.16+:** sum-of-blocks schema lift bucket now has 4+ candidates (Thresh E souls+magic, Taliyah E impact+detonations, Sona Q spell+Power Chord, Camille W flat+max-HP) — lift warranted soon; nested missing-HP parser bucket has 2 candidates (Kindred E, Kayle E); other carry-forwards unchanged (token-variant Aatrox/Gwen, form-swap KSante/Kayn/Hwei, sequence-state Jhin R/Corki R, conditional target-state).

59. ✅ **s201 — Phase 5.9.14 block_index expansion — Fizz/Galio/Garen/Graves/Janna/Jhin/Kennen/Taliyah/Teemo/Viego/Xerath/Yasuo/Ziggs** (2026-05-14, `9365f5c`). Pure-data continuation following s200's rescue batch — broadens coverage to 13 new champions (3 multi-key: Jhin Q+R, Teemo E+R, Xerath W+R) for 16 new (champion, key) entries. Registry 91 → 104 champions, 127 → 143 entries. Zero engine code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.85.0 → 0.86.0. **Four sub-patterns:** **Pattern A multi-hit single-target totals (7 entries):** Graves Q=2 (End of the Line buckshot + return ricochet on same target, 2.89×), Jhin Q=2 (Dancing Grenade Max Final Bounce after 3 chain minion deaths, 2.05×), Kennen R=1 filtered (Slicing Maelstrom all 6+ bolts on 1 target, 7.5× per-bolt), Taliyah Q=2 (Threaded Volley all 5 stones via Worked Ground, 2.6×), Teemo E=2 (Toxic Shot Total Poison full 4-tick DoT, 2.67×), Xerath R=1 filtered (Rite of the Arcane Total Magic all 4-6 bullets on same target, 4-6×), Ziggs E=2 (Hexplosive Minefield Maximum Total all 5 mines focused, 5×). **Pattern B fully-charged amps (4 entries):** Galio W=1 filtered (Shield of Durand 2s commit, 3×), Janna Q=2 (Howling Gale 3s max-charge, 1.55×), Jhin R=1 (Curtain Call Maximum at max distance, 4×), Viego Q=3 (Blade of the Ruined King fully-charged Soul Steal AA, 2×). **Pattern C channel/duration totals (3 entries):** Fizz R=2 (Chum the Waters Gigalodon max-distance, 2×), Garen E=1 (Judgment Increased per spin ramp on stationary target, 1.25×), Teemo R=1 filtered (Noxious Trap full 4-tick poison Total Magic, 4×). **Pattern D resource/positional amps (2 entries):** Xerath W=1 (Eye of Destruction Increased center-spot, 1.67×), Yasuo E=3 (Sweeping Blade Total Combined at max E-stacks, 2×). **Filtered-idx semantics:** 4 entries have non-damage prefix blocks stripped pre-index (Galio W raw 0 'Magic Shield Strength' + 1-2 'Damage Reduction' → filtered idx 1 = raw 4; Kennen R raw 0 'Bonus Resistances' → filtered idx 1 = raw 2; Teemo R raw 0-2 'Bounce Distance Cap' / 'Maximum Charges' / 'Slow' → filtered idx 1 = raw 4; Xerath R raw 0 'Number of Recasts' → filtered idx 1 = raw 2). **Reverts 4 prior-batch skip rationales** by re-framing under the established 'operator commits to canonical-amped condition' model: Xerath W (s198 'positional condition, defer to conditional schema lift' → s201: aligned with Khazix Q isolation s196), Ziggs E (s198 'unrealistic 5-mine focus' → s201: chokepoint commit, same as Ashe Q 5-AA s199), Janna Q (s198 'low ratio + Janna is support' → s201: 1.55× amp consistent under-count, Yuumi Q is also support and shipped s198), Yasuo E (s198 'stacks decay 10s' → s201: operator commits to E-stacking, same as Twitch E 6-stack pre-burst rotation s198). **6 deliberate skips documented inline:** Seraphine Q (Maximum Champion HP threshold; Seraphine routes to ds.hps not ds.ability), Taliyah E (1.04× marginal ratio + misses initial impact block 0), Thresh E (per-soul scaling needs sum-of-blocks framework), Nidalee W (conflicts with s187 form_index=1 cougar Pounce override), Shyvana E (Phase 4a parsing produces malformed interpolated base values), Kindred E (dropped during s201 implementation — Enhanced damage block has identical base + bAD; missing-HP coefficient buried in `unparsed_modifiers` nested-format "X% (+ Y% per Mark) of target's missing health" which Phase 4a parser cannot extract; registry entry would be a no-op until upstream parsing improves). All 16 verified per-rank math against Meraki snapshot. Tests: 24 new in `Phase599_14ExpansionTests` class (16 per-entry `_delta_check` + 3 multi-key resolved-shape (Jhin Q+R, Teemo E+R, Xerath W+R) + 2 math sanity (Jhin R 4×, Ziggs E 5×) + 3 backward-compat regression guards) + 14 explicit assertions in `test_known_champion_overrides` + 2 ENGINE_VERSION pin bumps in `test_effects_expansion` + Yasuo → Tryndamere fixture swap in 7 places (Yasuo landed in registry s201; Tryndamere is now canonical fixture — Q/E single-block, W/R no damage blocks, will never enter registry) → DS suite 1842 → 1866 (+24); wider RC 913 green post-DS-restart. **Live A/B headlines on :8893:** Taliyah Q **+138.2%** (14.53 → 34.63 — 5-stone Worked Ground commit), Graves Q **+104.5%** (7.51 → 15.35), Xerath R **+77.0%** (20.20 → 35.75), Teemo R **+62.5%** (7.80 → 12.68), Viego Q **+52.9%** (16.98 → 25.95), Jhin Q **+36.0%** (15.82 → 21.50), Janna Q **+32.4%** (9.12 → 12.08), Jhin R **+23.2%** (17.46 → 21.50), Yasuo E **+21.5%** (21.30 → 25.87), Kennen R **+11.7%**, Ziggs E **+9.7%**, Fizz R **+8.0%**, Galio W **+7.9%**, Xerath W **+6.0%**, Garen E **+3.5%**, Teemo E **+1.5%**. Viego /burst +88.22 burst damage (Q=3 propagates through burst walker too). **Architectural pattern lock-in continued:** seventeenth consecutive override / proc-shape modeling improvement on the same template (s185-s201); tenth pure-data batch in the block_index family. Cumulative coverage now at 143 (champion, key) entries across 104 champions (61% of the 171-champion roster touched). **Carried forward to Phase 5.9.15+:** all s198/s199/s200 carry-forwards remain unchanged (token-variant for multi-stage Q chains, form-swap, sequence-state, conditional target-state, sum-of-blocks, nested missing-HP parser newly added to bucket via Kindred E).

58. ✅ **s200 — Phase 5.9.13 rescue batch — Ambessa Drain / Anivia Empowered / Lillia / Nilah / Poppy** (2026-05-14, `8f837c0`). Smaller, focused 7-entry batch closing 4 previously-deferred mechanics by matching skip-list entries against improved mechanic understanding. Registry 90 → 91 champions (Ambessa new), 120 → 127 entries. Zero engine code changes. ENGINE_VERSION 0.84.0 → 0.85.0. **Rescues 4 deferred mechanics:** (1) **Ambessa Q/W** (s196/s197/s198 'form swap' deferral dissolved on verification) is actually a Drain-stack resource amp — operator commits to having Drain stacks ready, same model as Renekton Fury (s197). Q=1 'Increased Physical Damage' 2× block 0; W=1 'Increased Physical Damage' 1.5× block 0. (2) **Anivia R** (s195 'channel ticks ambiguous' rescued) is the Empowered phase amp triggered after 1.5s+ channel commit, same model as Belveth E max-charge (s174). Filtered idx 1 = raw block 2 'Empowered Damage per Tick' 3× per-tick (raw block 1 'Slow' is non-damage). (3) **Lillia Q** (s199 'uncertain mechanic' rescued) is Q damage + Dream Dust AA bonus on Q-stacked target — canonical Q-then-AA engage, same as Sett Q empowered-AA-followup pattern (s196). Filtered idx 1 = raw block 3 'Total Mixed Damage' 2× block 2 (raw blocks 0, 1 are Movement Speed). (4) **Nilah Q** (s199 'uncertain 2-stack' rescued) is canonical max-stack-consumed empowered AA — operator commits to building Formless Blade stacks pre-burst, same as Twitch E (s198) and Tristana E (s199) resource-state amp pattern. Q=1 'Maximum Physical Damage' 2× block 0 'Minimum'. Plus 2 net-new entries: **Ambessa E=1** (Lacerate slash + thrust both on target, 2× block 0 — similar to Sejuani W swipe+thrust s198), **Poppy Q=1 (filtered idx 1)** (Hammer Shock out + return wave on same target, 2× raw block 0 — same out+return model as Sivir/Akshan/Xayah Q. Raw blocks 1-3 are Slow + Minion-Damage). **Three sub-patterns:** Pattern A multi-hit single-target totals (Ambessa E, Lillia Q, Poppy Q); Pattern B resource-state amps (Ambessa Q, Ambessa W, Nilah Q); Pattern C channel/duration commit (Anivia R Empowered phase). All 7 verified per-rank against Meraki snapshot. Tests: 17 new in `Phase599_13ExpansionTests` class (7 per-entry `_delta_check` + 5 multi-key resolved-shape (Ambessa 3-key, Anivia 3-key, Lillia/Nilah/Poppy 2-key) + 2 math sanity (Ambessa Q 2×, Nilah Q tAD 2×) + 3 backward-compat asserts) + 6 new explicit assertions in `test_known_champion_overrides` + 2 updated assertions (`test_rank_mage_carries_source` + `test_anivia_E_still_routes_to_block_1` — Anivia resolved now `{Q:2, E:1, R:1}`) + 2 ENGINE_VERSION pins (0.84.0 → 0.85.0) → DS suite 1825 → 1842 (+17); wider RC 913 green post-DS-restart. **Live A/B on :8893 (/ability-dps at lvl 11 vs 80 armor / 30 MR / 2000 HP):** Nilah Q **+82.0%** (5.14 → 9.36 — max-stack empowered AA is Nilah's burst identity), Poppy Q **+75.7%** (11.51 → 20.22), Ambessa Q **+37.1%** (7.36 → 10.09 with all 3 Ambessa entries active), Lillia Q **+19.4%** (21.06 → 25.15), Anivia R **+10.5%** (19.80 → 21.88), Ambessa E **+9.2%**, Ambessa W **+8.0%**. **Multi-key extensions:** Anivia now `{Q:2, E:1, R:1}`, Lillia now `{W:1, Q:1}`, Nilah now `{R:1, Q:1}`, Poppy now `{R:1, Q:1}`. **Architectural pattern lock-in continued:** sixteenth consecutive override / proc-shape modeling improvement on the same template (s185-s200); ninth pure-data batch in the block_index family. **Smaller batch intentional:** 7 entries vs s198's 20 / s199's 17 — focus on rescuing previously-deferred mechanics rather than net-new candidate scanning. **Carried forward:** all s199 carry-forwards remain (token-variant for multi-stage Q chains, form-swap schema, sequence-state schema, conditional target-state schema lift).

57. ✅ **s199 — Phase 5.9.12 block_index expansion** (2026-05-14, `4ee733c`). Pure-data continuation following s198's same multi-pattern model — 17 new (champion, key) entries (6 new champions: Ashe/Shaco/Shen/Swain/Tristana/Xayah + 11 key extensions on existing champions: Aatrox/Fiddlesticks/Karthus/Nautilus/Nunu/Samira/Sejuani/Talon/Udyr/Viktor/Zac). Registry 84 → 90 champions, 103 → 120 entries. Zero engine code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.83.0 → 0.84.0. **Pattern A multi-hit single-target totals (8 entries):** Ashe Q=2 (Ranger's Focus all 5 enhanced AAs on target 5×), Nunu E=1 (Snowball Barrage 3-snowball cap 3×), Samira W=1 (Blade Whirl 2-rotation total 2×), Shen Q=1 (Twilight Assault 3-AA empowered total 3× — registry value 1 = filtered idx 1 = raw block 2 since raw block 0 'Slow' is non-damage and stripped pre-index), Swain Q=2 (Death's Hand all 5 bolts on target at point-blank 2×), Viktor Q=2 (Power Transfer Q ability + empowered AA total 1.7×), Xayah Q=1 (Double Daggers out+return 2×), Zac Q=1 (Stretching Strikes both arms on same target 2×). **Pattern B positional/sweet-spot amps (3 entries):** Aatrox Q=1 (Q1 Edge of the Blade sweet-spot zone 1.7×), Shaco E=2 (Two-Shiv Poison backstab amp 1.5×), Talon Q=1 (Noxian Diplomacy guaranteed crit on champion vs minion 1.5×). **Pattern C resource-state amps (2 entries):** Tristana E=4 (Explosive Charge max-stack detonation after 4 AAs 2× block 1), Udyr Q=1 (Wilding Claw Awakened 2-AA empowered total 2×). **Pattern D channel total (1 entry):** Karthus E=2 (Defile per-second tick 4× per-tick — clamps to last filtered idx since raw block 0 'Mana Restored' is non-damage). **Pattern E direct-hit primary target (2 entries):** Sejuani R=1 (Glacial Prison direct stun ~2×), Nautilus R=2 (Depth Charge primary hit ~2× — clamps to last filtered idx since raw blocks 1+3 are durations). **Pattern F execute amp (1 entry):** Fiddlesticks W=3 (Bountiful Harvest full channel + 12-22% missing-HP execute on low-HP target 2×). All 17 verified per-rank against the Meraki snapshot — `test_ashe_Q_block2_matches_5x_block1_tad` + `test_karthus_E_block2_matches_4x_block1` + `test_tristana_E_block4_matches_2x_block1` pin three exact multipliers. **Filtering note discovered s199:** `_select_blocks` indexed strategy filters out `attribute_kind != "damage"` blocks BEFORE indexing — registry value is the FILTERED damage-block index, not the raw block index. Most entries align because their abilities have all-damage early blocks. Five s199 entries had non-damage early blocks (Ashe Q, Karthus E, Shaco E, Shen Q, Nautilus R) — four (Ashe/Karthus/Shaco/Nautilus) accidentally land on the correct filtered idx via `clamp out-of-range to last` semantics; Shen Q with 5 filtered damage blocks did NOT clamp, so its registry value was corrected from 2 → 1 (filtered idx 1 = raw block 2 = Total Magic Damage). Documented inline in registry `_meta` description + the new Shen test class docstring. Tests: 31 new in new `Phase599_12ExpansionTests` class (17 per-entry `_delta_check` asserting registry routes key to expected filtered idx + total exceeds forced-block-0 baseline; 6 multi-key resolved-shape sanity for Aatrox/Karthus/Nautilus/Samira/Talon/Udyr/Viktor newly multi-key champions; 3 math sanity Ashe Q tAD% 5×, Karthus E base 4×, Tristana E block 4 = 2× block 1; 4 backward-compat asserts for s198 Sylas Q / s192+s196 Akali / s193 Singed / s194 Corki preserved) + 17 new explicit assertions in `test_known_champion_overrides` + 1 updated assertion (pre-s199 Talon `{W:2,R:2}` → `{W:2,R:2,Q:1}`) + 1 relaxed assertion (`test_pre_s198_aatrox_unchanged` now asserts W key alone to survive Q=1 extension) + 2 ENGINE_VERSION pins in `test_effects_expansion.py` (0.83.0 → 0.84.0) → DS suite 1794 → 1825; wider RC 913 green post-DS-restart. **Live A/B on :8893 (/ability-dps at lvl 11 vs 80 armor / 30 MR / 2000 HP):** Shen Q **+113.9%** (1.54 → 3.30 adps — Twilight Assault dominates Shen's ability output), Swain Q **+63.0%** (13.94 → 22.72), Xayah Q **+62.5%** (3.80 → 6.18), Aatrox Q **+56.1%** (11.84 → 18.48 — sweet-spot Q1 zone), Udyr Q **+44.6%** (15.42 → 22.30), Viktor Q **+41.9%** (19.75 → 28.03), Tristana E **+31.7%** (3.22 → 4.23), Fiddlesticks W **+25.3%** (11.81 → 14.79), Zac Q **+22.2%** (12.04 → 14.70), Ashe Q **+16.2%** (4.89 → 5.68), Talon Q **+15.1%** (12.27 → 14.13), Karthus E **+7.1%** (53.20 → 56.99), Shaco E **+7.1%** (14.68 → 15.73), Sejuani R **+5.3%** (10.43 → 10.98), Nunu E **+4.1%** (21.51 → 22.39), Nautilus R **+3.9%** (8.24 → 8.56), Samira W **+3.7%** (7.19 → 7.46). Smaller deltas on Karthus/Sejuani/Nunu/Nautilus/Samira reflect spell-share dilution — their other abilities (Karthus Q, Sejuani Q/W/E, Nunu W, Nautilus Q/W/E, Samira R) already dominate the total adps. **Multi-key extensions:** Aatrox now {W:3, Q:1}, Karthus now {Q:1, E:2}, Nautilus now {E:2, R:2}, Nunu now {W:1, E:1}, Samira now {R:1, W:1}, Sejuani now {W:2, R:1}, Talon now {W:2, R:2, Q:1}, Udyr now {R:1, Q:1}, Viktor now {R:2, Q:2}, Zac now {R:2, Q:1}, Fiddlesticks now {R:1, W:3}. **12 deliberate skips documented inline:** Diana R (multi-target pull, not single-burst), Gnar R (wall-stun terrain target-state), Lillia Q (uncertain mechanic), Poppy E/Q (wall-state target condition), Rumble Q (Danger Zone heat decays mid-fight), Teemo R (4-shroom focus unrealistic), Viktor E (Augment system removed), Yasuo E (stacks decay), Yunara (new champion mechanic unverified), Yuumi R (5-wave model mismatch), Zoe W (stolen-spell resource model), Nilah Q (2-stack mechanic uncertain). **Architectural pattern lock-in continued:** fifteenth consecutive override / proc-shape modeling improvement on the same template (s185-s199); eighth pure-data batch in the block_index family. Cumulative coverage now at 120 (champion, key) entries across 90 champions. **Carried forward to Phase 5.9.13+:** all s198 carry-forwards remain (token-variant for multi-stage Q chains — Aatrox Q1/Q2/Q3 with combo_sequence, Gwen R needlework; form-swap schema — Ambessa, KSante, Kayn, Hwei; sequence-state schema — Jhin R 4th-shot, Corki R Big One); conditional target-state schema lift now has 5+ candidates queued (Zoe sleep, Lux Illumination, DrMundo E missing-HP, Evelynn Q charm, Vayne E wall-stun) — ready when operator commits to schema design.

56. ✅ **s198 — Phase 5.9.11 bruiser/jungler/utility/marksman block_index expansion** (2026-05-14, `b472618` + `5be510c`). Pure-data continuation following s197's same multi-pattern model — 20 new (champion, key) entries (17 new champions + 2 key extensions on Sion and Vladimir; XinZhao contributes 2 entries Q+W). Registry 67 → 84 champions, 83 → 103 entries. Zero code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.82.0 → 0.83.0. **Pattern A multi-hit single-target totals (12 entries):** Sylas Q=3 (Chain Lash 2-pulse 3.33×), XinZhao Q=1 (Three Talon Strike 3 empowered AAs 3×), XinZhao W=2 (Wind Becomes Lightning slash+thrust 3.71× base + 4× tAD), Zac R=2 (Let's Bounce all 4 bounces same target 2.5×), Maokai E=1 (Sapling Toss enhanced dual-hit 2×), Kayn Q=1 (Reaping Slash both passes 2×), Sejuani W=2 (Winter's Wrath swipe+thrust 2.89× base + 4× AP), Neeko Q=2 (Blooming Burst initial + 2 blooms 2.04× base + 1.83× AP), Nasus E=2 (Spirit Fire initial + 5 ticks 2×), Nami E=1 (Tidecaller's Blessing 3 empowered AAs 3×), Ornn R=2 (Call of the Forge God 2-ram 2×), Twitch E=3 (Contaminate at 6 Deadly Venom stacks 4.5× base + 6× per-stack). **Pattern B fully-charged amps (4 entries):** Vi Q=1 (Vault Breaker fully-charged 1.25s 2.5×), Sion R=1 (Unstoppable Onslaught max-speed 2.67×), Irelia W=1 (Defiant Dance fully-charged 2s 3×), Yuumi Q=1 (Prowling Projectile untargeted max-distance 1.62×). **Pattern C resource-state amp (1 entry):** Jax E=1 (Counter Strike at 2 dodge stacks 2×). **Pattern D channel/duration totals (3 entries):** Udyr R=1 (Wingborne Storm full 8 ticks exact 8×), Vladimir W=1 (Sanguine Pool full 4-second exact 4×), Viktor R=2 (Chaos Storm initial + 6 ticks full channel 4.48× base + 5.20× AP). All 20 verified per-rank against the Meraki snapshot. Tests: 29 new in new `Phase599_11ExpansionTests` class (20 per-entry `_delta_check` + 3 multi-key resolved-shape (XinZhao Q+W, Sion Q+R, Vladimir E+W) + 2 math sanity (Udyr R 8×, Vi Q 2.5×) + 4 backward-compat: `test_pre_s198_morgana_unchanged` + `test_pre_s198_aatrox_unchanged` + `test_pre_s198_camille_unchanged` + `test_pre_s198_singed_unchanged`) + 17 explicit assertions in `test_known_champion_overrides` + 2 updated for Sion {Q:2, R:1} and Vladimir {E:1, W:1} + 2 version-pin bumps in `test_effects_expansion.py` → DS suite 1765 → 1794; wider RC `tests/` 913 → 938 green post-DS-restart. **Live A/B on :8893:** Twitch E **+263.0%** (0.32 → 1.17 adps; Contaminate at 6 Deadly Venom stacks is Twitch's entire burst identity), Sylas Q **+118.8%** (7.29 → 15.95), Udyr R **+94.1%** (7.95 → 15.42), Viktor R **+88.6%** (10.47 → 19.75), Kayn Q **+67.5%** (15.22 → 25.50), Vi Q **+63.4%** (9.49 → 15.50), Neeko Q **+58.0%** (15.58 → 24.63), Yuumi Q **+56.4%** (6.90 → 10.79), XinZhao Q+W combined **+119.7%** (7.05 → 15.49), XinZhao W standalone **+31.0%**, Jax E **+22.7%** (12.75 → 15.65), Sejuani W **+15.0%** (9.06 → 10.43), Nasus E **+13.9%** (8.55 → 9.73), Nami E **+13.4%** (10.11 → 11.46), Maokai E **+12.0%** (9.54 → 10.68), Zac R **+9.9%** (10.95 → 12.04), Ornn R **+7.9%** (15.00 → 16.18), Irelia W **+6.0%** (15.07 → 15.97), Sion R **+4.8%** (31.11 → 32.59), Vladimir W **+1.8%** (27.59 → 28.09). Small lifts on Vladimir W / Sion R / Irelia W reflect spell-share dilution (utility-heavy or multi-spell champions where the new entry is correct but total ability output is dominated by other spells). **Bruiser/jungler/utility broadening:** s191-s197 was assassin/mage/marksman-heavy; s198 deliberately broadened to bruiser (Vi, Irelia, Sion, Sejuani, Jax, Maokai, Kayn, Ornn, Sylas), jungler (XinZhao, Udyr, Zac), and utility (Nami, Yuumi, Viktor, Nasus, Neeko, Vladimir, Twitch). **14 deliberate skips documented inline:** Evelynn Q (target-state charm), Vayne E (wall-stun terrain), Vladimir Q (passive-AA on-hit), Xerath W (positional amp), Ziggs E (unrealistic 5-mine focus), Janna Q (utility ult), Seraphine Q (enchanter-class), Yasuo E (stacks decay), Heimerdinger Q/R (turret), Sona R / Lux Q/E/R / Veigar Q (single-block), Smolder Q/E (gear-conditional or schema-ambiguous). **Architectural pattern lock-in continued:** fourteenth consecutive override / proc-shape modeling improvement on the same template (s185-s198); seventh pure-data batch in the block_index family. **Carried forward to Phase 5.9.12+:** all s197 carry-forwards remain (token-variant for multi-stage Q chains, form-swap, sequence-state); conditional target-state schema lift now has 5+ candidates queued (Zoe sleep, Lux Illumination, DrMundo E missing-HP, Evelynn Q charm, Vayne E wall-stun) — ready when operator commits to schema design.

55. ✅ **s197 — Phase 5.9.10 assassin/fighter resource + utility block_index expansion** (2026-05-14, `e7906b3`). Pure-data continuation following s196's same multi-pattern model — 20 new (champion, key) entries across 18 new champions. Registry 49 → 67. Zero code changes — s191/s192 walker logic handles all entries transparently. ENGINE_VERSION 0.81.0 → 0.82.0. **Pattern A multi-hit / channel / mark totals (12 entries):** Aatrox W=3 (Infernal Chains landed + pull-back 2×), Hwei R=3 (Spiraling Despair Maximum Total), Leblanc Q=1 (Sigil + detonation 2×), Leblanc E=1 (Ethereal Chains root + return-tether 2.12×), Lucian R=1 (Culling full channel exact 5×), Mel Q=3 (Radiant Volley 6-projectile total ~10×), Mel R=2 (Golden Eclipse mark detonation ~10×), MonkeyKing R=1 (Wukong Cyclone full 4s spin 8× per-tick), Naafiri Q=2 (Darkin Daggers all 3 same target 4× bAD), Naafiri E=1 (Eviscerate multi-strike 2.91×), MasterYi Q=2 (Alpha Strike same-target focus exact 1.75×), Smolder W=2 (Achooo! 3-hit total 2.1×). **Pattern B fully-charged amps (3 entries):** Nunu W=1 (Biggest Snowball Ever! max-charge exact 5×), Sion Q=2 (Decimating Smash 2s wind-up 2.92×), Briar E=4 (Chilling Scream max + headbutt 2.4× block 2). **Pattern C resource-state amps (3 entries):** Renekton Q=1 (Cull the Meek Empowered at full Fury 1.5× + 1.4× bAD), Renekton W=2 (Ruthless Predator Empowered at full Fury exact 1.5×), Kassadin R=3 (Riftwalk Maximum Bonus at max 4 stacks ~3× + ~1.56× AP). **Pattern D execute / channel-duration amps (2 entries):** Darius R=2 (Noxian Guillotine execute on bleeding target exact 2×), Nilah R=1 (Apotheosis full-duration whirlwind 4× + 4× bAD). **Pattern E multi-charge / multi-fire totals (2 entries):** Poppy R=1 (Keeper's Verdict fully-charged channel 2× + 2× bAD), Rumble E=1 (Electro Harpoon 2-charge dual-fire exact 2× + 2× AP). All 20 verified per-rank against the Meraki snapshot; `test_lucian_R_block1_matches_5x_block0` + `test_renekton_Q_block1_matches_1_5x_block0` pin the exact multipliers. Tests: 32 new in new `Phase599_10ExpansionTests` class (20 per-entry `_delta_check` + 4 multi-key resolved-shape (LeBlanc Q+E, Mel Q+R, Naafiri Q+E, Renekton Q+W) + 2 math sanity + 4 backward-compat: `test_pre_s197_morgana_unchanged` + `test_pre_s197_akali_unchanged` + `test_pre_s197_corki_unchanged` + `test_pre_s197_singed_unchanged`) + 18 explicit assertions in `test_known_champion_overrides` + 2 version-pin bumps in `test_effects_expansion.py` + 9 existing tests repointed (6 "unmapped champion fixture" tests Aatrox → Yasuo across `GetBlockIndexForTests` / `ResolveBlockIndexTests` / `ComputeAbilityDpsBlockIndexTests` / `ComputeBurstBlockIndexTests` / `RankerBlockIndexTests` / `ToDictSerializationTests`; `ServerRouteSourceTests.test_ability_dps_default_source` Aatrox → Caitlyn; 2 `ServerRouteSourceTests` Cassi assertions corrected to {E:1, W:1} which had been silently broken since s196) → DS suite 1733 → 1765; wider RC `tests/` 913 green post-DS-restart. **Live A/B on :8893:** Naafiri Q **+76.8%** (4.90 → 8.66 adps), Kassadin R **+72.3%** (17.18 → 29.60 adps), Sion Q **+52.6%** (20.39 → 31.11 adps), Darius R **+42.3%** burst (591.44 → 841.44), Leblanc Q **+35.1%** (13.56 → 18.32 adps), MonkeyKing R **+30.0%** (6.02 → 7.83 adps), Renekton Q **+22.9%** (9.30 → 11.43 adps), Aatrox W **+17.8%** burst (293.72 → 345.94), Renekton W **+11.8%** (10.22 → 11.43 adps), Lucian R **+9.5%** (7.65 → 8.38 adps), Hwei R **+8.9%** (19.20 → 20.90 adps), Nilah R **+3.4%** (4.97 → 5.14 adps). Concentrated single-spell champions (Naafiri Q, Kassadin R, Sion Q, Darius R) see headline lifts; utility-class champions (Nilah R, Hwei R) see smaller lifts because other spells dominate. **Resource-state simplification:** the s196 carry-forward had flagged "conditional resource-state block_index" as a needed schema lift (5+ candidates accumulated). s197 closes 3 of those (Renekton Q/W full-Fury, Kassadin R max-stack) under the existing unconditional model with explicit "operator commits to canonical resource state" framing — same intuition as Cassi committing to poison or Brand committing to CC. The conditional schema is now needed only for sequence-state cases (Jhin R 4th-shot, Corki R Big One every-4th-missile, Akshan R Comeuppance charge) where the resource state changes per-cast within the same ability invocation. **15 deliberate skips documented inline:** Aatrox Q multi-stage rotation (token-variant + combo_sequence), Akshan R / Kennen R / Jhin R / Kled W (resource/sequence/form), Ambessa Q/W/E / Gwen R / KSante R (form swap), Blitzcrank R / Fizz W/R / Galio W / Graves Q / Malphite W (complex semantics), LeBlanc R (Mimic data gap), Mel E / Renekton E form 1 (form conflict / long-zone). **Architectural pattern lock-in continued:** thirteenth consecutive override / proc-shape modeling improvement on the same template (s185-s197); sixth pure-data batch in the channel/total/charge family. Pattern remains rock-solid for 10-25 entry batches. **Carried forward to Phase 5.9.11+:** (a) **Token-variant for multi-stage Q chains** — Aatrox Q (Q1/Q2/Q3 distinct), Gwen R needlework chain. Same shape as Akali R/R2 (s192). 2+ candidates accumulated. (b) **Form-swap block_index** — Ambessa Q/W/E, KSante R, Kayn Q, Hwei Q/W/E forms. Schema lift needed: `{champ: {key: {form_idx: block_idx, ...}}}`. (c) **Sequence-state block_index** — Jhin R 4th-shot, Corki R Big One. Fits combo_sequence-style modeling, not block_index. (d) all s196 carry-forwards remain unchanged.

54. ✅ **s196 — Phase 5.9.9 extended multi-hit/condition-amp block_index expansion** (2026-05-14, `573fa63`). Pure-data continuation closing the next clean class beyond s195's 13-entry batch. 17 new (champion, key) entries (14 new champions + 3 key extensions on Akali, Cassiopeia, Morgana). Registry 35 → 49 champions. Zero code changes — s191/s192 walker logic handles all new entries transparently. ENGINE_VERSION 0.80.0 → 0.81.0. **Pattern A multi-hit single-target totals (12 entries):** Akali E=2 (E1+E2 Shuriken Flip chain ~3.33×), Akshan Q=1 (Avengerang out+return 2×), Cassiopeia W=1 (Miasma full duration 5×), Chogath E=1 (Vorpal Spikes 3-hit 3×), Draven R=1 (Whirling Death out+return 2×), Lillia W=1 (Watch Out! Eep! center hit 3×), Morgana R=1 (Soul Shackles tether full duration 2×), Nautilus E=2 (Riptide 3-wave same target 2×), Riven Q=1 (Broken Wings Q-Q-Q 3-cast chain 3×), Sett Q=1 (Knuckle Down both empowered AAs 2×), Skarner Q=1 (Shattered Earth empowered 3-hit 3×), Soraka E=1 (Equinox immediate+delayed proc 2×). **Pattern B fully-charged / condition amps (5 entries):** Gragas Q=1 (Barrel Roll fermented 4s hold 1.5×), Karthus Q=1 (Lay Waste solo-target enhanced 2×), Khazix Q=1 (Taste Their Fear isolation amp 2.1×), KogMaw R=1 (Living Artillery low-HP execute 2×), Pantheon Q=1 (Comet Spear fully-charged hurl 2.2×). All 17 verified per-rank against Meraki snapshot — `test_riven_Q_block1_matches_3x_block0` + `test_akshan_Q_block1_matches_2x_block0` pin the exact multipliers. Tests: 25 new in new `Phase599ExpansionTests` class (17 per-entry `_delta_check` + 3 multi-key resolved-shape (Akali Q→E→R/R2 three-key, Cassi E+W, Morgana W+R) + 2 math sanity + 3 backward-compat: `test_pre_s196_morgana_W_unchanged` + `test_pre_s196_akali_R2_unchanged` + `test_pre_s196_corki_unchanged`) + 14 explicit assertions in `test_known_champion_overrides` + 2 version-pin bumps in `test_effects_expansion.py` + 8 existing tests updated to reflect new Cassi/Akali/Morgana shapes (`GetBlockIndexForTests` + `ResolveBlockIndexTests` + `BackwardCompatTests` repointed from Cassi to Veigar as the stable single-key test fixture; `AkaliTokenVariantTests` 3 tests + `ComputeAbilityDpsBlockIndexTests` 2 tests + `RankerBlockIndexTests` + `ToDictSerializationTests` updated for new dict shapes) → DS suite 1708 → 1733; wider RC `tests/` 913 green post-DS-restart. **Live A/B on :8893 (/ability-dps at lvl 11 vs 80 armor / 30 MR / 2000 HP):** Riven Q **+179.0%** (20.42 → 56.98 adps), Pantheon Q **+101.5%** (12.06 → 24.30), Karthus Q **+91.4%** (27.79 → 53.20), Lillia W **+85.5%** (11.35 → 21.06), Akshan Q **+83.5%** (3.99 → 7.32), Skarner Q **+71.5%** (7.95 → 13.64), Khazix Q **+63.9%** (9.26 → 15.18), Akali E **+56.7%** (15.41 → 24.15), Gragas Q **+30.2%** (36.42 → 47.41), Draven R **+28.3%** (2.49 → 3.19), Sett Q **+26.5%** (4.32 → 5.46), Soraka E **+11.3%** (7.61 → 8.47), Nautilus E **+9.7%** (7.51 → 8.24), Chogath E **+5.2%** (20.85 → 21.94), KogMaw R **+4.9%** (15.68 → 16.45), Morgana R **+3.3%** (23.71 → 24.49), Cassi W **+2.4%** (39.48 → 40.44). Concentrated single-spell champions (Riven, Karthus, Pantheon, Akshan) see headline lifts because their core ability is correctly scored; utility/multi-spell champions (Morgana, Cassi) see small lifts because dominant DPS spells are unchanged. **15 deliberate skips documented in registry rationale:** Nidalee Q (s187 form_index conflict), Kassadin R (resource-state stack), Aatrox W (CC-conditional pull-back), Akshan R (Comeuppance charge), Ambessa Q/W/E (form swap), Pantheon W/R (no scaling / engine default OK), Gangplank R (Upgrade choices), Jhin R (4-shot sequence fits combo_sequence), Hwei R (channel deferral), Gwen R / KSante R (form swap), Karthus E/R, Mel Q, Naafiri Q, Olaf Q, Nasus E. **Architectural pattern lock-in continued:** twelfth consecutive override / proc-shape modeling improvement on the same template (s185-s196); fifth pure-data batch in the channel/total/charge family. Pattern stable for routine 10-25 entry batches; rate-limit is operator triage of skip-list growth. **Carried forward to Phase 5.9.10+:** all s195 carry-forwards remain unchanged (target-state conditional block_index now has 4+ candidates; resource-state now has 5+ candidates; both ready for schema lift when operator commits); plus form-conflict entries (Nidalee Q, Hwei R block-3, Gwen R, KSante R) require form-aware conditional block_index when schema ships.

53. ✅ **s195 — Phase 5.9.8 multi-hit/charge/recast block_index expansion** (2026-05-14, `a4e4046`). Pure-data continuation closing the next clean class of single-static-block_index candidates beyond s193 channels + s194 calibration follow-up. 13 new (champion, key) entries across 4 sub-patterns: **(A) Multi-hit single-target totals** (operator focuses all hits on one target): Ahri W=2 (Fox-Fire 3-bolt total), Kaisa Q=2 (Icathian Rain missile-focus), Lulu Q=3 (Glitterlance both passes), Sivir Q=2 (Boomerang Blade out + back), Talon W=2 (Rake out + return), Talon R=2 (Shadow Assault unstealth chain), Velkoz W=2 (Void Rift initial + detonation), Ekko Q=3 (Timewinder out + return). **(B) Fully-charged amps** (operator commits to wind-up): Varus Q=1 (fully-charged Piercing Arrow 1.5×), Zoe Q=1 (long-distance Paddle Star post-E 2.5×), Vladimir E=1 (2-charge Tides of Blood 2× + 4× caster HP scaling). **(C) Recast amps**: Camille Q=2 (Precision Protocol 2nd cast 2×). **(D) CC-conditional duration totals**: Morgana W=3 (Tormented Shadow Maximum Total vs rooted target). Registry: 25 → 35 entries (11 new champions + 2 multi-key extensions: Ahri added W=2 alongside existing Q=1; Vel'Koz added W=2 alongside existing R=1). Zero code changes — s191/s192 walker logic (token-canonical + base-key fallback) handles all new entries transparently. ENGINE_VERSION 0.79.0 → 0.80.0. Tests: 19 new in new `Phase598ExpansionTests` class (13 per-entry `_delta_check` + 3 multi-key resolved-shape (Talon W+R, Ahri Q+W, Velkoz W+R) + 1 Ahri W numeric sanity matching block 0 + 2× block 1 + 2 backward-compat: `test_pre_s195_unmapped_unaffected` + `test_pre_s195_singed_unaffected`) + 11 explicit assertions in `test_known_champion_overrides` + 2 version-pin bumps in `test_effects_expansion.py` → DS suite 1689 → 1708; wider RC `tests/` 913 green. **Live A/B on :8893 (/ability-dps at lvl 11 vs 80 armor / 30 MR / 2000 HP):** Morgana W **+158.5%** (9.17 → 23.71 adps), Zoe Q **+135.8%** (18.63 → 43.93), Sivir Q **+85.0%** (4.47 → 8.27), Ekko Q **+69.8%** (9.68 → 16.44), Camille Q **+69.5%** (5.04 → 8.54), Talon W **+65.2%** (7.43 → 12.27), Kaisa Q **+48.3%** (7.73 → 11.46), Varus Q **+33.6%** (6.09 → 8.14), Lulu Q **+33.5%** (7.59 → 10.13), Vladimir E **+24.1%** (22.23 → 27.59), Velkoz W **+15.3%** (17.04 → 19.65), Ahri W **+11.9%** (17.36 → 19.43), Talon R **+3.8%** (11.83 → 12.27, smaller window because R is rank-3 max). Morgana W (+158.5%) and Zoe Q (+135.8%) are the headline lifts — Morgana W block 3 'Maximum Total Damage' represents the full Tormented Shadow channel against a rooted (Q'd) target which is the canonical Morgana setup; Zoe Q block 1 'Maximum Magic Damage' is the long-distance Paddle Star post-E teleport at 2.5× the close-range minimum. **Regression checks pass:** s194 Corki entry preserved (`test_pre_s195_unmapped_unaffected`); s193 Singed entry preserved (`test_pre_s195_singed_unaffected`); s192 Akali R/R2 token-variant logic intact (`AkaliTokenVariantTests` green); s191 BackwardCompatTests green (unmapped Zed byte-identical with no override = with empty explicit override). **Architectural pattern lock-in continued:** eleventh consecutive override / proc-shape modeling improvement on the same template; fourth pure-data batch in the channel/total/charge family (s191 seed → s193 channels → s194 calibration → s195 multi-hit/charge/recast). Pattern stable enough to support routine "add 5-15 entries per session" batches without engineering risk. **Carried forward:** (a) **Conditional block_index based on target state** — Zoe sleep (E→Q SECOND amp on top of s195's distance amp), Lux Illumination, DrMundo E missing-HP threshold — now 3+ candidates accumulated; schema lift ready when operator commits. (b) **Sum-of-blocks block_index** (DrMundo W detonation, single candidate). (c) **Conditional resource-state block_index** (Corki R Big One, Renekton Q Fury, Aatrox Q chain). (d) all s194 carry-forwards remain.

52. ✅ **s194 — Phase 5.9.7 calibration-follow-up block_index expansion** (2026-05-14, `9a2504d`). Closes the s193 carry-forward calibration list. Pure data batch — no resolver/walker/server code changes; just expands the s191 `champion_block_index.json` registry from 20 → 25 entries (8 new (champion, key) pairs). New entries: Corki {W:1, E:1} (Valkyrie fire-trail full-duration + Gatling Gun 4-second full channel — both Meraki "Total Magic Damage"/"Total Physical Damage"), Hecarim {W:1, E:1} (Spirit of Dread 4-second aura "Total Magic Damage" + Devastating Charge "Maximum Physical Damage" max-charge variant), Jayce {Q:1, W:1} (Shock Blast through Acceleration Gate "Increased Damage" + Lightning Field hammer-form 4-second aura "Total Magic Damage"), Rell {R:1} (Magnet Storm 4-second channel "Total Magic Damage"), DrMundo {W:1} (Heart Zapper 4-second drain channel "Total Magic Damage"; block 2 recast detonation +25% one-time burst is missed by single-block_index schema — documented minor under-count). **Jayce Q is the first block_index that layers on a prior form_index override** — s187 set Jayce.Q form_index=1 (cannon form), s194 sets block_index=1 within that form. Two orthogonal resolvers compose at runtime: form_index selects cannon form 1 → block_index then selects gate-amped block 1 within that form. `test_jayce_Q_block_layers_on_s187_form_index` is the smoke test; A/B confirms +16.2% lift. Skipped: Corki R "Big One Physical Damage" (charge-based every-4th-missile, NOT a per-cast amp; requires conditional resource-state modeling). `_meta.description` extended with Phase 5.9.7 note explaining the calibration-follow-up class + form/block layering. `_meta.rationale` adds entry-by-entry per-tick × duration math verification with the Meraki ATTR names. ENGINE_VERSION 0.78.0 → 0.79.0. Tests: 13 new in new `CalibrationFollowUpExpansionTests` class (one per new (champion, key) entry asserting registry routes to expected block_index + delta-check `_delta_check` helper mirroring s193's pattern; plus `test_corki_both_keys_in_resolved` + `test_hecarim_both_keys_in_resolved` + `test_jayce_both_keys_in_resolved` for multi-key champions; `test_jayce_Q_block_layers_on_s187_form_index` smoke test; `test_pre_s194_unmapped_unaffected` backward-compat guard preserving s193's Singed entry) + 5 new explicit assertions in `test_known_champion_overrides` + 2 version-pin bumps in `test_effects_expansion.py` → DS suite 1676 → 1689; wider RC 1013 → 1024 green. **A/B impact (/ability-dps at lvl 11 vs 80/30/2000):** Jayce W **+31.9%** (22.07 → 29.10 adps), Corki W **+24.9%** (14.20 → 17.73), Hecarim W **+23.2%** (22.44 → 27.65), DrMundo W **+18.1%** (25.79 → 30.46), Jayce Q **+16.2%** (25.04 → 29.10), Corki E **+7.7%** (16.47 → 17.73), Rell R **+4.8%** (10.48 → 10.98), Hecarim E **+1.9%** (27.12 → 27.65 — diluted by Q/W dominance). Lifts are smaller than s193's headline channels (Singed +182%, Fiddle R +125%) because these champions all have multi-spell ability damage contributions; Hecarim's E adds only +1.9% to total because his Q and W already dominate, while his W alone (+23.2%) mirrors the s193 channel pattern. Jayce W +31.9% is the biggest single-spell lift, reflecting how much the hammer-aura full duration outweighs the per-tick value the engine was using pre-s194. **Regression checks pass:** Singed Q registry-resolved block 1 unchanged (s193 entry preserved; `test_pre_s194_unmapped_unaffected` asserts exact `{"Q": 1}` shape); Cassi /ability-dps 39.48 unchanged from s191/s192/s193; Akali /burst 941.71 unchanged from s192 (R=0, R2=2 token-variant logic preserved); Zed (unmapped) burst with no override equals burst with explicit empty override — `BackwardCompatTests` green. **Architectural pattern lock-in:** tenth consecutive override / proc-shape modeling improvement on the same template (s185 max_priority / s186 combo / s187 form / s188 per-AA on-hit / s189 Spellblade / s190 Lightshield / s191 block_index / s192 token-variant / s193 channels / s194 calibration follow-up). Three pure-data batches in the series now (s191 seed, s193 channels, s194 calibration follow-up). Resolver composition (form + block) verified live for Jayce Q — future per-champion entries can compose any combination of max_priority / combo_sequence / form_index / block_index without coupling. **Carried forward to Phase 5.9.8+:** (a) **Conditional block_index based on target state** — Zoe sleep, Lux Illumination, DrMundo E missing-HP threshold, Renekton Q full Fury. Schema lift: `{"<champion>": {"<key>": {"default": 0, "when_target_missing_hp_pct_above": [0.5, 2]}}}`. Defer until 3+ candidates accumulate cleanly. (b) **Sum-of-blocks block_index** — DrMundo W full-channel + recast detonation. Schema lift: `block_index: int | list[int]` where list means sum. Single known candidate; defer until 3+ accumulate. (c) **Conditional resource-state block_index** — Corki R Big One every-4th-missile, Renekton Q full Fury, Aatrox Q chain stage. Same shape as conditional damage amps; defer until 3+ candidates accumulate. (d) all s193 carry-forwards remain unchanged.

51. ✅ **s193 — Phase 5.9.6 channeled-ability block_index expansion** (2026-05-14). Pure data batch — no resolver/walker/server code changes; just expands the s191 `champion_block_index.json` registry from 12 → 20 entries. 8 new champion entries cover the "per-tick → total" gap for channeled / duration abilities where the engine's default `block_strategy="first"` picked the per-tick value but the realistic per-cast contribution is the full-channel total: Alistar E=1 (Trample 80→200 base + 70% AP), AurelionSol E=1 (Singularity 50→150 base + 60% AP), Fiddlesticks R=1 (Crowstorm 750→1750 base + 250% AP at rank 3), MissFortune E=1 (Make It Rain 70→190 base + 120% AP), Samira R=1 (Inferno Trigger 50→250 base spray total), Singed Q=1 (Poison Trail 40→120 base + 85% AP over duration), Velkoz R=1 (Disintegration Ray ~450→800 base + 125% AP full channel), Syndra R=2 (Maximum sphere-stack burst at 7+ stacks). Anivia gets Q=2 added alongside the existing E=1 (block 2 = initial pass + detonation total). `_meta.description` extended with Phase 5.9.6 note explaining the per-tick → total semantics; `_meta.rationale` adds entry-by-entry rank-N math verification (e.g., Singed Q rank 5: 15 per-tick × 8 ticks = 120 base; matches block 1). Skipped-list expanded to document Corki E/W + Hecarim W + Jayce W + Rell R + DrMundo W (same pattern, lower-impact, deferred to calibration follow-up) + Renekton R / Kennen R / AurelionSol Q / Rumble R (modeling caveats per inline rationale). ENGINE_VERSION 0.77.0 → 0.78.0. Tests: 11 new in new `ChanneledAbilityExpansionTests` class (test_alistar_E_routes_to_block_1 + test_aurelionsol_E_routes_to_block_1 + test_fiddlesticks_R_routes_to_block_1 + test_missfortune_E_routes_to_block_1 + test_samira_R_routes_to_block_1 + test_singed_Q_routes_to_block_1 + test_velkoz_R_routes_to_block_1 + test_syndra_R_routes_to_block_2 + test_anivia_Q_routes_to_block_2 + test_anivia_E_still_routes_to_block_1 regression guard + test_singed_Q_total_block_matches_per_cast_math numeric sanity check) + 9 new explicit assertions in `test_known_champion_overrides` + 1 update to `test_rank_mage_carries_source` (Anivia's new {Q:2, E:1} shape) + 2 version-pin bumps in `test_effects_expansion.py` → DS suite 1665 → 1676; wider RC 1013 green. **A/B impact (/ability-dps at lvl 11 vs 80/30/2000):** Singed Q **+182.4%** (4.06 → 11.46 adps); Fiddlesticks R **+125.4%** (5.24 → 11.81 adps); Anivia Q+E **+105.0%** (9.66 → 19.80 adps); AurelionSol E **+64.9%** (1.99 → 3.29 adps); Velkoz R **+23.1%** (13.84 → 17.04 adps); MissFortune E **+16.7%** (9.97 → 11.64 adps); Syndra R **+9.8%** (25.55 → 28.06 adps). Channel-class abilities (Crowstorm, Disintegration Ray, Poison Trail, Singularity, Make It Rain, Trample, Inferno Trigger) were systematically under-counted by 20-180% since the engine's first ability_dps ship in s178 — the Meraki schema was correct, the engine just needed to know which block represented realistic per-cast value. **/rank-mage shifts:** Singed baseline 11.46 (was 4.06 pre-s193) → Rabadon's Deathcap #1 +11.77 (Poison Trail's 85% AP block-1 scaling makes Rabadon's dominant); Fiddlesticks baseline 11.81 (was 5.24 pre-s193) → Rabadon's Deathcap #1 +6.53 (Crowstorm's 250% AP at rank 3 dominates). **Regression checks pass:** Cassi /ability-dps total 39.48 unchanged from s191/s192 (E:1 entry preserved); Akali /burst total 941.71 unchanged from s192 (R=0, R2=2 token-variant logic preserved); all 8 s191 + 1 s192 entries still resolve correctly post-expansion. **Architectural pattern continued from s191/s192:** ninth consecutive override registry on the same template; s193 is the second pure-data batch (after s191's seed entries themselves). The pattern is now stable enough to support routine "calibration-driven" expansion batches — operator can add 5-10 entries per session without engineering risk. **Carried forward to Phase 5.9.7:** Corki E/W, Hecarim W, Jayce W, Rell R, DrMundo W same per-tick → total pattern (+5-20% adps each, lower-impact deferral); + all s192 carry-forwards (conditional block_index based on target state, inter-spell amps, generalized arm-consume framework, Aphelios/Karma/Khazix blockers, real internal CD).

50. ✅ **s192 — Phase 5.9.5 token-variant block_index for Akali R** (2026-05-14). Closes s191 carry-forward (a). Akali R has 3 damage blocks in the Meraki snapshot — block0 "Magic Damage" with 30% AP + 50% bonus AD (R1 initial dash cast), block1 "Minimum Magic Damage" (R2 vs full-HP target, +30% AP), block2 "Maximum Magic Damage" at 90% AP (R2 max-execute scaling via missing-HP curve). Pre-s192, setting `{"Akali": {"R": 2}}` globally in the s191 per-(champion, key) registry would have double-counted R1's execute amp (R1 doesn't have it in-game). Two design decisions: **(1) Extend keys, not values** — `block_index_overrides` stays `dict[str, int]` (no `dict[str, int | list[int]]` complexity). Repeat-variant tokens (Q2/W2/E2/R2) become valid keys alongside base keys (Q/W/E/R). Forward-compatible: any future "Q3 differs from Q1/Q2" entry drops in without code changes. **(2) Token-canonical first, base-key fallback** — `compute_burst_damage`'s combo walker now does `if canonical in block_overrides` (e.g. "R2") BEFORE `elif ability_key in block_overrides` (e.g. "R"). Operator's per-call dict still wins per-token; the registry fills any tokens the caller didn't override. `compute_ability_dps` is unaffected — it iterates `SPELL_KEYS = ("Q", "W", "E", "R")` and `overrides.get(key, 0)` simply doesn't see the R2-keyed entry. Token-variant entries only fire in the burst walker. `champion_block_index.json` registry gains Akali entry `{"R": 0, "R2": 2}`. `_meta.description` extended with Phase 5.9.5 lookup precedence note. Skipped-list updated to clarify Yone Q1/Q2/Q3 single-block correctness, Zed Q vs Q2-shadow correctness, Leblanc Q vs Q2-mimic upstream data gap. ENGINE_VERSION 0.76.0 → 0.77.0. Tests: 7 new in new `AkaliTokenVariantTests` class (registry shape / R1 row block 0 raw=220 / R2 row block 2 raw=420 / total burst with registry > forced R+R2:0 / explicit R2 override wins over registry / R-only override falls through to R2 token / `compute_ability_dps` ignores R2 entry) + pre-existing `test_every_key_is_valid_token` extended valid-key set to `{Q,W,E,R,Q2,W2,E2,R2}` (was Q/W/E/R only) + `test_known_champion_overrides` adds Akali assertion → DS suite 1658 → 1665; wider RC 1013 green. Live-validated on Legion :8893: Akali /burst three-way A/B (combo Q-AA-E-R-Q2-AA-R2 vs 80 armor / 30 MR / 2000 HP) — forced R+R2:0 (pre-s192 model) **787.9**, registry-applied (R=0, R2=2) **941.7**, naive R:2 only (pre-s192 mistake — over-counts R1) **1095.6**. Registry sits exactly halfway: R1 row stays raw=220 final=169.2 (block 0 base + bonus-AD), R2 row jumps to raw=420 final=323.1 (block 2 missing-HP execute). **+153.8 burst (+19.5%)** is the genuine R2 max-execute amp value — exactly `(420-220) × 0.769` mit factor at 30 MR. Akali /rank-assassin top 5: Lich Bane #1 +396.54 (Spellblade × doubled R2 base × 90% AP scaling), Shadowflame #2, Rabadon's #3, Stormsurge #4, Essence Reaver #5. Cassi /ability-dps unchanged at 39.48 (s191 E:1 entry still applies — no regression on other registry entries). **Architectural pattern lock-in (continued from s191):** eighth consecutive override / proc-shape modeling improvement on the same template, and the first one to *extend* a prior registry rather than ship a new one. Schema stayed `dict[str, int]`; valid-key set extended from `{Q,W,E,R}` to `{Q,W,E,R,Q2,W2,E2,R2}`. Pattern is now proven extensible across both rows (more entries) and columns (more granular keys per entry); future conditional-block_index extensions can layer values from `int` → `dict[str, Any]` without disturbing the per-batch resolver contract. **Carried forward to Phase 5.9.6:** (a) conditional block_index based on target state (Zoe sleep amp, Lux Illumination mark, DrMundo E missing-HP threshold, Renekton Q Fury condition — substantial schema lift; defer until 3+ candidates accumulate); (b) all s191 carry-forwards remain.

49. ✅ **s191 — Phase 5.9 per-(champion, key) damage block_index overrides** (2026-05-14). Closes a systematic under-scoring of 11 specific mages + assassins: 248 multi-block (champion, key, form) tuples in `champion_abilities.json` carry block ≥1 with amped/empowered/max/total damage, but pre-s191 `block_strategy="first"` locked every caller to block 0. The Phase 5.9 hand-off mentioned "Conditional damage amps (Ahri R→Q, Zoe E→Q, Akali R1→QE→R2)" but on inspection most of those map to existing combo-sequence (s186) or multi-form (s187) work; the genuine gap is block selection within a chosen form. New `agents/daemon_slayer/champion_block_index.json` seed registry: 12 entries across 11 champions — Cassiopeia E=1 (Total Enhanced vs poisoned), Veigar R=1 (Maximum vs executed), Anivia E=1 (Enhanced vs chilled), Diana W=2 (all 3 Pale Cascade orbs total), Brand W=1 + R=1 (Increased vs CC'd + Total Single-Target), Evelynn R=1 (Empowered sub-30% HP execute), Aurora Q=2 + Belveth E=2 (full-charge maximum), Karma W=1 + Vex R=2 (channel total + total mark explode), Ahri Q=1 (Total Mixed both passes). Loader + resolver in `ability_dps.py` mirror the Phase 4e form_index pattern exactly: `_BLOCK_INDEX_PATH` + singleton-cached `_load_block_index_table` + `get_block_index_for(champion_id) -> (mapping, source)` + `_resolve_block_index_overrides(champion_id, explicit) -> (merged, source)` merger (caller wins per-key, registry fills gaps) + `reset_block_index_cache()` for tests. `_select_blocks` extended with new `"indexed"` strategy + `block_index: int = 0` param (clamps negative→0, out-of-range→last); `_BLOCK_STRATEGIES` extended with `"indexed"`. `compute_ability_dps` + `rank_items_by_ability_dps` + `compute_burst_damage` + `rank_items_by_burst` all gain `block_index_overrides: Optional[dict[str, int]] = None` arg; per-spell loop checks `if key in block_overrides` and switches to `"indexed"` strategy for that one call, keys without an entry honor global `block_strategy`. All 4 result types (`AbilityDpsResult` + `AbilityDpsRankResult` + `BurstResult` + `BurstRankResult`) gain `block_index_source: str` ("override" | "champion" | "default") + `block_index_resolved: dict[str, int]` (the merged map actually used) fields, surfaced in `to_dict()`. Four server routes (`/ability-dps`, `/rank-mage`, `/burst`, `/rank-assassin`) accept new `block_index` JSON dict body field via shared `_parse_block_index` decoder. `_empty_burst` accepts the new kwargs for engine-down / champion-missing paths. ENGINE_VERSION 0.75.0 → 0.76.0. Tests: 53 new in `test_block_index_overrides.py` (RegistryShapeTests 6 / LoaderCacheTests 2 / GetBlockIndexForTests 4 / ResolveBlockIndexTests 5 / SelectBlocksIndexedTests 8 / ComputeAbilityDpsBlockIndexTests 6 / ComputeBurstBlockIndexTests 5 / RankerBlockIndexTests 3 / ToDictSerializationTests 5 / BackwardCompatTests 3 / ServerRouteSourceTests 6) + 2 version-pin bumps in `test_effects_expansion.py` → DS suite 1605 → 1658; wider RC 1013 green. Live-validated all 4 routes on Legion :8893: Cassi /ability-dps total 39.48 vs forced-block-0 27.66 (+43%); Veigar /burst total 807.01 vs forced-R0 614.70 (+31%, Lich Bane climbs to #1 in /rank-assassin at +391.15 — Spellblade amp on now-doubled R block1 AP scaling); Anivia /ability-dps total 14.86 vs forced-block-0 9.66 (+54%). **Deliberately skipped:** Akali R (block2 Maximum is genuinely R2's missing-HP execute scaling, but setting per-(champion, key) would double-count R1 in the combo registry's `Q-AA-E-R-Q2-AA-R2` Akali template — needs per-token-variant overrides, separate feature); AurelionSol R (Empowered lives in form_index=1 not block_index); Caitlyn Q (block1 is REDUCED fallback for non-headshot, block0 IS the realistic burst case); DrMundo E + Renekton Q + Aatrox W + Anivia R (target-state-dependent or per-tick ambiguity — deferred). **Backward-compat verified** via `BackwardCompatTests` — Zed (unmapped) burst with no override equals burst with explicit empty override, byte-identical pre-s191 output. **Architectural pattern lock-in:** seventh consecutive override / proc-shape modeling improvement on the same template (s185 max_priority / s186 combo / s187 form / s188 per-AA on-hit / s189 Spellblade / s190 Lightshield / s191 block_index). Engine surface area now stable for a future "conditional block_index based on target state" lift to slot in without re-architecting. **Carried forward to Phase 5.9.5:** (a) per-token-variant block_index for Akali R; (b) conditional block_index gated on target state (Zoe sleep amp, Lux Illumination mark); (c) all s190 carry-forwards remain.

41. ✅ **s184.1 — Archetype-mismatch chip renderer** (2026-05-13, `912efe1`). Closes carried-forward item (a) from s184: dashboard JS chip near `#ds-pill` that surfaces the first-purchase mismatch nudge. New `web/js/panels/archetype_nudge_chip.js` (~95 LOC) exports `renderArchetypeNudge(state)` — reads `state.archetype_nudge`, phase-gates to `"fired"` (pending/no_mismatch/dismissed all collapse to hidden), sig-guards against 2s-poll DOM thrash. Internal `_dismiss(champion)` POSTs `/api/archetype-nudge/dismiss` then locally hides; X-button click handler wired once at module load. Compact display `⚠ {Primary}? · {firstItem}` with full message + expected top-3 items in `title` tooltip. New `#archetype-nudge-chip` + `#archetype-nudge-chip-text` + `#archetype-nudge-chip-x` elements in `web/index.html` header-row-2, immediately after `#ds-pill`, `hidden` by default with `role="status"`. New `.archetype-nudge-chip` rule in `web/css/panels/map_state.css` (yellow `--warn-soft` bg + `--warn` outline + 16px font); mode-gating mirrors `.ds-pill` exactly (collapsed in `body[data-mode="client"]` / `body[data-mode="tft"]` / `body:not([data-mode])`); `[hidden]` respected via `display: none !important`. `web/js/main.js` gains the panel import + 3 callsites mirroring `renderTeamContext(st)` count exactly (SSE handler / HTTP fallback / LCU poller — all three top-level state-consumers). Tests: 15 new in `tests/test_archetype_nudge_chip_dom.py` (unittest.TestCase-based grep regression guard — IndexHtmlTests 5 / PanelJsTests 4 / MainJsWiringTests 2 / CssTests 4) → 1013 wider RC suite (was 1009 in s184 wrap). **No RC supervisor restart required** — unified asset-hash (s171.8 ADR-008 `compute_asset_hash` walks `web/{js,css}/panels/*`) auto-reloaded the new files on pid 1360; cache-bust hash flipped on first `/api/ui-version` poll after commit. Live-verified end-to-end on Legion :8888: (a) `GET /` returns chip + text span + dismiss button + hidden attr; (b) `GET /api/state` returns `archetype_nudge: {}` (empty since no game); (c) panel JS module serves 3799 bytes with `export function renderArchetypeNudge` + `/api/archetype-nudge/dismiss` path; (d) main.js serves with 3 callsites; (e) CSS rule present; (f) dismiss endpoint round-trips happy/400-missing/400-non-dict; (g) Game-PC monitor 0 dashboard Home overlay (CLIENT mode) shows chip correctly hidden by data-mode rule — no visual regression. **Carried forward:** (a) live-game chip lifecycle validation (pending → fired → dismissed) pending next CS pop + game start; (b) `_TOP_N_THRESHOLD = 15` calibration knob retune once real games yield fired nudges; (c) per-game-session token edge case for older Riot LCU builds; (d) calibration analysis additive — `nudge_history` rows to `core/ds_calibration` for retroactive threshold tuning.

Full open work + future: `ROADMAP.md` + `BACKLOG.md`. Completed work: `docs/_archive/CHANGELOG.md`.
