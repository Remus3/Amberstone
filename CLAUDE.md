# Riot Commander - Agent Context

Live League / TFT coaching dashboard. Reads Riot Live Client API, calls Claude Haiku for coaching and Sonnet for vision, writes JSON to `data/`, serves `:8888` HTTPS dashboard on Game-PC's secondary display. RC is tkinter-free; Daemon Slayer (`:8893`) computes real DPS math per champion.

> **Living docs (read at session start):** `docs/ARCHITECTURE.md` · `docs/OPERATIONS.md` · `docs/BRIDGE.md` · `ROADMAP.md` · `docs/API.md`
> **Deep references:** `docs/DAEMON_SLAYER.md` (DS engine - 705 items / 172 champs - ENGINE_VERSION 1.43.0 - all 6 archetype scorers wired + per-spell CC consumer + cc_blended_ehp ecosystem COMPLETE 4 consumers + per-spell CC wave 9 108/89 + cc_conditional ecosystem COMPLETE 5 consumers wave 6 36/32) - `docs/AGENTS.md` (Phase 3 framework) - `BACKLOG.md` (aspirational)
> **Architectural decisions:** `docs/adr/` - before re-litigating a past choice, check here first.
> **Dated artifacts** in `docs/_archive/` (excluded from ripgrep searches).

## Topology

| Machine | Tailnet / IP | LAN IP | Role |
|---|---|---|---|
| **Legion** | `legion-rc` / `100.70.22.55` | `192.168.8.230` | Runs RC, supervisor, vision server, web dashboard |
| **Game-PC** | `gamepc-rc` / `100.95.66.128` | `192.168.8.237` | Runs League; Chrome on secondary display (panel is 1920×1280 native @ 100% scale) shows the dashboard |
| **Peer** | `peer-host` / `<peer-tailnet-ip>` | - | Cross-Claude peer; RC↔Peer bridge |

All three in tailnet `tailc150de.ts.net`. Prefer tailnet hostnames. Vision runs in-process at `127.0.0.1:8889`.

## Paths

- Project root: `C:\Riot Commander\`
- Python: `C:\Users\Administrator\AppData\Local\Programs\Python\Python314\python.exe`
- API key: `C:\Riot Commander\API-Key-Claude.txt` (gitignored)
- Health: `C:\Riot Commander\ops\runtime\health.json`
- Logs: `C:\Riot Commander\logs\YYYY-MM-DD.log`

## Hard rules

- **Always `py_compile` before restart.** Syntax errors crash silently under `pythonw.exe`.
- **Atomic writes only:** `tmp.write_text(...); tmp.replace(target)`. Overlays poll mid-write.
- **Never `Stop-Process`.** Hangs MCP pipe. Use `taskkill /F /PID`.
- **Restart via `restart_trigger.txt`** (write any content; supervisor clears + restarts within ~5s).
- **`SCRIPT_DIR` in `app/__init__.py` MUST be `Path(__file__).parent.parent`** (package layout).
- **State assumptions explicitly before coding.**
- **No em-dashes or en-dashes - ever (7-bit ASCII authored content).** Hard rule across Legion / Game-PC / Peer, in *all* authored text: code, comments, docstrings, `.md`, writeups, commit messages, WAKEUP/ROADMAP/CLAUDE, bridge messages, chat output. Use ` - ` (spaced hyphen) for a clause break, `-` otherwise. Also avoid smart quotes (U+201C U+201D U+2018 U+2019) and en/em dashes (U+2013 U+2014); stay ASCII. **Why:** Windows PowerShell 5.1 `ParseFile` ANSI-decodes a no-BOM `.ps1`, turning a UTF-8 em-dash inside a double-quoted string into a U+201D smart-quote that the tokenizer treats as a string terminator -> cascading parse failure (2026-05-18 `gamepc_boot.ps1` incident); also a standing operator style rule. **Retroactive purge done** (2026-05-18, `tools/strip_em_dashes.py` - reusable for drift checks): em+en dashes stripped repo-wide incl. the functional `"-"` no-data sentinel in code/JSON (operator-approved behavior change - empty dashboard cells render `-`). **NOT swept** (immutable history / non-source): `*.log` + rotated `*.log.N`, `docs/_archive/**` + dated artifacts, `.jsonl` ledgers, binaries, `.pyc`/`.git`. Smart quotes are rule-banned going forward but not yet retroactively swept (separate operator-gated pass).
- **Frozen files** (do not modify without explicit user approval):
  `main.py`, `core/log_setup.py`, `core/moon_proxy.py`, `lcu/lcu_client.py`,
  `core/game_snapshot.py`, `ops/rc_dev_runtime.py`, `ops/rc_supervisor.py`,
  `app/__init__.py`, `app/_loop.py`, `app/_health_monitor.py`, `app/_remediation.py`,
  `app/_state_authority.py`, `app/_overlay_manager.py`, `app/_game_lifecycle.py`,
  `tools/bridge_watcher_classify.py`,
  `tools/bridge_watcher_actions.py`, `tools/bridge_watcher_action_prompt.md`,
  `tools/bridge_watcher_history.py`, `tools/bridge_watcher_install.ps1`,
  `tools/bridge_watcher_hook.ps1`, `tools/bridge_watcher_config.json`,
  `tools/bridge_post_result.py`, `tools/bridge_pull_tasks.py`,
  `tools/process-bridge-tasks.md`, `tools/diagnose.md`, `tools/caveman.md`,
  `dashboard/routes_bridge_pending.py`, `ops/RC-BridgeWatcher.xml`.

## Restart workflow

```
echo restart > restart_trigger.txt
```
Verify: read `ops/runtime/health.json`, confirm new `pid`, `alive=true`, `last_reload_ok=true`.
Hard fallback: `taskkill /F /PID <pid>` then `restart.bat`.

## Session workflow

Scoped sessions - each focused task is one session.
- **End:** commit + update `WAKEUP_NOTES.md` (keep last 2-3 sessions at full fidelity; archive older to `docs/history_notes.md`) + push.
- **Start:** `/clear`, bootstrap from CLAUDE.md + MEMORY.md + git log + WAKEUP_NOTES + `docs/ARCHITECTURE.md` + `ROADMAP.md` (all 4 under 800 lines total).
- `/clear` between Tier items, between coding/reviewing modes, between focus-area switches.

## Web dashboard

`web_dashboard.py` at `:8888` HTTPS. Key endpoints: `/`, `/api/state`, `/api/health/all`, `/api/bridge/pending`, `/api/input`, `/api/command`, `/api/ds-preview`, `/metrics`. Viewed in Chrome on Game-PC secondary at `https://legion-rc:8888/` - design baseline is **standard 1920×1080 with Chrome chrome present** (titlebar + URL bar + bookmarks bar visible, usable viewport ≈ 1920×~920). F11 fullscreen is optional and recovers the chrome chrome - `main` flex-grows into the extra height (no layout pinned to 1280). Cert via `tools/regen_rc_cert.ps1`. Each machine has its own Anthropic API key (`riot-commander-legion`, `riot-commander-gamepc`, `riot-commander-peer`).

## Scheduled tasks (Legion)

Key: `RC-Supervisor` (logon, Administrator, HIGHEST) · `RC-VisionServer` (startup, SYSTEM, HIGHEST) · `RC-BridgeWatcher` (logon, daemon). Full list + Game-PC tasks: `docs/OPERATIONS.md`.

## Vision pipeline

Game-PC `gamepc_screen_agent.py` POSTs frames every 2s to `:8889/upload-frame`. Coaches call `modes.shared_vision._capture_screen()` → GET `:8889/latest-frame`. **`_run_vision()` gates on `_fetch_game_data() is not None`** - vision never fires during lobby/idle. Tiered: OCR first, Sonnet escalation for misses. Calibrate `data/vision_regions.json` to expand OCR coverage.

## Mode detection

`game_reader.py._process_game()` → `core/game_snapshot.py` → mode strings. ARAM Mayhem (`KIWI`) → `MODE_ARAM`.

## Where to find current state

- Live PID + mode + health: `ops/runtime/health.json`
- Current game state: `data/{aram,arena,brawl,tft}_coaching_data.json`
- Recent activity: `logs/YYYY-MM-DD.log`
- Architecture / module map: `docs/ARCHITECTURE.md`
- Ops commands + restart: `docs/OPERATIONS.md`
- Bridge wire format + watcher: `docs/BRIDGE.md`
- Open work: `ROADMAP.md` · Aspirational: `BACKLOG.md` · History: `docs/history_notes.md`

## Useful commands

Full reference: `docs/OPERATIONS.md`. Quick-start:
```
python -c "import json; print(json.dumps(json.loads(open(r'ops/runtime/health.json').read()), indent=2))"
echo restart > restart_trigger.txt
curl -k https://127.0.0.1:8888/api/health/all
```

## Memory frontmatter - cross-project sync fields

Standard memory files carry `name`, `description`, `type`. Two optional fields
are valid for memories that should ride the RC↔Peer bridge (Phase 1 schema,
`docs io RC peer/RC_PHASE1_LESSON_SCHEMA_2026-05-02.md`):

```yaml
cross_project: true          # opt-in; default false. Only feedback/reference/project eligible.
applies_when: "<trigger>"    # required when cross_project=true; free-form grep-able phrase
does_not_apply_when:         # optional list; receiver skips if any entry matches local context
  - "<neg-trigger>"
```

`type: user` memories are never eligible. Default is OFF - author decides at write-time.
False-negatives are recoverable (edit frontmatter later); false-positives are bridge spam.

## Testing Discipline

Always run the full test suite after schema changes, engine version bumps, or item-effect additions. Avoid data-fragile cross-item comparison assertions; prefer assertions on computed quantities. When stubbing methods accessed via class, wrap with `@staticmethod` correctly.

## Windows Environment Notes

Claude Desktop on Windows may be installed via the Microsoft Store (check `%LOCALAPPDATA%\Packages`) in addition to standard install paths. Use `pythonw.exe` (not `python.exe`) for background daemons to avoid flashing console windows.

## Daemon Slayer Batch Workflow

When continuing Daemon Slayer work: pick the next batch from ROADMAP, implement schema/engine changes, add tests (target green before commit), bump engine version, commit + push, verify live, update hand-off notes.

## Session Wrap-up

When invoked with `/done` or asked to wrap a session: (1) audit pending changes, (2) commit and push, (3) update ROADMAP/CLAUDE/README if relevant, (4) process lessons/WAKEUP_NOTES, (5) run bridge probe, (6) print final banner. Run independent steps in parallel.

## Active priorities

152. ✅ **2026-05-23 - cost-trace gap C closure: wire 11 untracked messages.create + construction-layer shim SHIPPED (1 commit `7ad4056`, pushed; CI green 1m23s; non-engine; non-frozen; no DS restart; no RC restart).** Operator triggered `docs/cost_trace.md` "Recommended follow-ups (not auto-applied; tracked here)" -> AskUserQuestion-picked "Both: wire 11 sites + add construction-layer shim (Recommended)" from 4-option fork. Full vertical slice executed end-to-end per [[feedback_scope_decision_cadence]]. **Shipped (14 files / +643 / -35):** (1) NEW `core/cost_tracker.py::record_anthropic_response(resp, *, model, purpose) -> dict | None` shared module-level helper at module-end (sibling of `coaches/_base_coach.py:506-518` `_record_coach_call` private method + `vision_server/_inference.py:105-123` `_record_to_cost_tracker` private helper; all 3 funnel through `get_tracker().record_call()` so the pricing-table + Prometheus path stays unified). Pulls `input_tokens / output_tokens / cache_read_input_tokens / cache_creation_input_tokens` defensively (each defaults to 0 if missing/None); falls back to `getattr(resp, "model", "")` only if `model` arg blank; blank purpose -> "_unspecified"; best-effort (swallows exceptions, returns None on failure). (2) NEW `core/anthropic_client.py::tracked_anthropic(api_key, *, purpose, default_model="")` construction-layer shim - returns a real `anthropic.Anthropic` client with `messages.create` rebound to a wrapper that calls `record_anthropic_response` after a successful response. Defense-in-depth: future call sites that switch the import are tracked by default even if the caller forgets the helper. Recording failures swallowed; real API errors propagate unchanged. **11 wired sites (purpose= label is the by_purpose key in `data/spend/YYYY-MM-DD.json`):** 6 coach-side - `coaches/aram_team_analyzer.py:161` (aram_team_analyzer) + `coaches/experimental_builder.py:207` (experimental_builder) + `coaches/champ_select_coach.py:116` (champ_select_coach) + `coaches/replay_coach.py:192` (replay_coach) + `dashboard/_champ_select.py:92` (champ_select_brief; renamed inline `model=` literal to `_CS_MODEL` constant + recorded with same name) + `agents/agent7_context/warm_session.py:154` (agent7_warm; recording placed inside `with self._lock` block AFTER token-totals update but BEFORE return so the lock-scope stays atomic). 5 TFT (highest priority polling/warm cadence per audit) - `tft/tft_coach_engine.py:692` (tft_coach) + `tft/tft_pbe_engine.py:421` (tft_pbe) + `tft/tft_live_analysis.py:292` (tft_live_analysis - LOCAL FALLBACK path only; moon_proxy primary records via vision_server already) + `tft/tft_live_analysis.py:316` (tft_live_aug_select - same local-fallback discipline) + `tft/tft_vision_reader.py:186` (tft_vision - SONNET, the most expensive untracked lane per audit). **+27 tests** in NEW `tests/test_cost_tracker_response_helper.py` (~330 LOC across 5 classes): RecordAnthropicResponseTests 8 (input/output tokens / cache_read+write / missing usage / None usage fields / missing optional cache fields / resp.model fallback / swallows exceptions / blank purpose -> _unspecified); TrackedAnthropicShimTests 4 (wrapper applied vs original_create identity / real API errors propagate via RuntimeError / recording failures swallowed but caller still gets resp / default_model fallback when caller omits model=); WiredSitesImportSmokeTests 2 (helper + shim are public callables at module scope); WiredSitesGrepTests 11 (one per audit site grep-matches `record_anthropic_response` + `purpose="<label>"` so a future refactor that rips the wire fails CI before any live cadence hits the missing telemetry); AsciiHygieneTests 2 (new helper block in cost_tracker.py + entire anthropic_client.py ASCII-clean - explicit codepoint scan). The FakeMessages stub uses real-function `messages.create` (NOT MagicMock.create) so the shim's `client.messages.create = wrapped` rebind genuinely replaces a different callable - mirrors the SDK's real Resource shape; one test fail caught + corrected mid-run (`assertIsNot` would always pass identity if `fake_messages` was a MagicMock since attribute assignment writes through). **`docs/COST_TRACE.md` updated:** follow-ups 1+2 flipped "tracked here" -> "SHIPPED 2026-05-23" with per-site purpose label catalog; call-site matrix all 11 sites tracked column now `YES (record_anthropic_response)`; reconciliation section reframed (the previously-latent TFT/Arena/champ-select gap is now closed; recorded-vs-Anthropic-billing gap reflects pricing-table drift only, no untracked surface). **Verified:** full RC suite **3250 passed / 0 failed** (+27 over 3223 baseline = exactly the new test file); py_compile + ruff clean across all 14 touched/new files (`core/cost_tracker.py` + `core/anthropic_client.py` + 11 wire sites + new test file + docs); 0 non-ASCII bytes added by this diff (verified via git-diff line-wise ASCII probe; pre-existing carryover in `tft/tft_coach_engine.py` 336 bytes + `tft/tft_pbe_engine.py` 21 bytes + `tft/tft_live_analysis.py` 1 byte + `tft/tft_vision_reader.py` 18 bytes + 4 coach files left in place per operator-gated retro-sweep policy [[feedback_no_em_dashes]]); CI green 1m23s on first push (run 26324650782). No DS restart (non-engine). No RC restart (non-coach + non-route modules; coach prompts pick up on next coaching tick). No ENGINE bump. No frozen-file edits. **Don't-redo:** `core.cost_tracker.record_anthropic_response` is the canonical chokepoint for any non-`_BaseCoach` site - do NOT duplicate the usage-extraction logic in a new caller. The `tracked_anthropic` shim is INTENT defense-in-depth NOT replacement for the helper - sites with per-call purpose granularity (e.g. `vision_server/_inference.py` `vision_relay` vs `coach_relay` from the same client) should keep calling the helper directly. The 11 `WiredSitesGrepTests` pin the wire-in at each site by grep-matching `record_anthropic_response` + `purpose="<label>"` - if a future refactor renames the helper or rips a wire out, the test that owns that site fails before any live cadence hits the missing telemetry. The 3rd cost-trace audit (2026-04-29 gap A + gap B + this gap C) has CLOSED the recurring "new coach forgot to wire telemetry" pattern at the source via the shim - any 12th call site is auto-tracked once the operator switches the import. The `_aresp` / `aresp` local variable names in `tft/tft_live_analysis.py` are intentionally NOT renamed (one-line scope; matching existing terse local style). Moon_proxy primary path on `tft/tft_live_analysis.py` + `tft/tft_vision_reader.py` records via `vision_server/_inference.py` already (gap B closure from 2026-04-29) - the new wires only fire on the LOCAL fallback branch where moon_proxy was unreachable. **Carries forward:** Item 151 carries (a)-(k) ALL unchanged (Meraki parse-strip standing constraint, schema lift operator-gated, RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 = verify LastTaskResult=0 next session, DD Defy deferred, live ARAM/SR smoke pending, calibrations operator-gated, BACKLOG L14 kills->takedowns flip operator-gated, UI/UX live-game audit owed, Slice C MINOR PROPOSAL `/api/minimap-crop` to `_SUPPRESS_LOG_PATHS` operator-gated, wave 9+ should target REJECT carries without Meraki schema lift). Frozen-file grant NOT used this session.

151. ✅ **2026-05-23 - item 151 4-slice parallel housekeeping drain (cc_conditional wave 8 SHIPPED + BACKLOG stale-sweep wave 13 CLEAN + cost/latency CLEAN wave 16 + living docs sync post-wave-8) SHIPPED (2 commits + 1 merge `656c4f5` `d2711ba`, pushed; ENGINE 1.44.0 -> 1.45.0; non-frozen; DS :8893 restarted serves 1.45.0 pid 8212 -> new; RC :8888 unchanged - DS engine + docs only).** Operator "continue what is left as open items in parallel commit + push & /done for /clear" -> 17th consecutive run using orchestrator-merge template (items 134-150 streak extended). 4 worktree agents dispatched concurrent. Slice A SHIPPED (wave 8 closes 3 cc_conditional REJECT carries via candidates that DO NOT need the Meraki extractor schema lift); Slice B CLEAN no-commit (wave 13 sweep saturation, 0 flips); Slice C CLEAN no-commit (16th consecutive cost/latency CLEAN; 1 MINOR PROPOSAL `/api/minimap-crop` at 0.417/sec - operator-gated); Slice D CLEAN no-commit (living docs durable post-s150 - Slice A flip required follow-up docs commit). **Slice A `656c4f5` (merge into main) feat(ds) cc_conditional wave 8 (36/32 -> 39/35; +3 entries / +3 net-new champs closing prior-wave REJECT carries):** Via setdefault builder pattern from items 140-150 with `_p(champion, spell, default)` helper. NEW: **Morgana R Soul Shackles** (channel_completion 0.5 stun 1.5/1.75/2.0s across 3 ranks - per items 146 + 147 REJECT carries "belongs in cc_conditional parallel to Karma W; channel-completion conditional"); **Seraphine E Beat Drop** (target_debuffed 0.5 stun 1.5s all 5 ranks - per items 138 + 146 + 147 REJECT carries "target-state-conditional: stun fires only when target is debuffed by slow/airborne/immobilize"); **Evelynn W Allure** (target_debuffed 0.5 charm 1.5/1.75/2.0/2.25/2.5s across 5 ranks - per items 138 + 146 REJECT carries "detonation-on-Eve-attack conditional charm"). All 3 had been EXPLICITLY flagged in prior wave REJECT notes as belonging in cc_conditional, NOT new pitches. Uses ONLY existing 12 condition tags (COND_CHANNEL_COMPLETION + COND_TARGET_DEBUFFED both pre-existing). No new condition tag constants. REGISTRY_TOTAL_CHAMPIONS=32 -> 35 / REGISTRY_TOTAL_ENTRIES=36 -> 39. Multi-wave coexistence count within cc_conditional now 4 cc_conditional champs unchanged (Aatrox Q3+W, Brand Q+R, Briar Q+E, TahmKench R+Q) + 3 NEW net-new champs Morgana + Seraphine + Evelynn. Consumer math BYTE-IDENTICAL to 1.44.0 for default include_conditional=False callers. +25 tests in NEW `agents/daemon_slayer/tests/test_cc_conditional_wave8.py` (~229 LOC). `test_cc_conditional_forward_marker.py _ALLOWED_TEST_FILES` gained `test_cc_conditional_wave8.py`. `test_cc_conditional_wave7_tags.py` 2 count-pin assertions relaxed assertEqual -> assertGreaterEqual for future-wave forward compatibility. ENGINE_VERSION bumped 1.44.0 -> 1.45.0 in `__init__.py` with full changelog block + 32 stale ENGINE pin syncs across DS test files via bulk regex rewrite. The Meraki parse-strip STILL BINDS for the OTHER candidates from item 150's exhaustive analysis (Renekton W Fury / Aatrox post-R passive / Volibear R passive form / Briar W frenzy-empowered Q+R / Karma W form_index=1 / Hwei E form_index=1 / Neeko E Empowered Root); wave 8 closes ONLY the 3 candidates whose duration values + mechanic descriptions are verifiable WITHOUT the extractor schema lift. The Meraki extractor schema-lift gate stays operator-gated per item 150 carry-forward (b). **Slice B CLEAN no-commit (wave 13 stale-sweep saturation):** Agent grep-verified every open BACKLOG.md + ROADMAP.md entry against live source. BACKLOG L13 cc_conditional ecosystem subsection ALREADY current (items 141-148 + "Registry grew across 6 wave expansions + wave 7 tag schema lift" + 36/32 + 12 tags + 5 consumer surfaces complete from item 150 Slice B). BACKLOG L14 obj_participation 9-col SHIPPED note CURRENT (item 149 SHIPPED, 3 remaining overlap fields operator-gated). BACKLOG L15 Dead Man's Plate SHIPPED CURRENT. L16-L106 all legitimately operator-gated / live-gated / research-closed. ROADMAP L223 Fleet status DS row CURRENT (ENGINE 1.44.0 + 3991 tests + cc_conditional wave 7 schema lift item 148 documented at sweep-time). ROADMAP L82 (#89) + L83 (build-ORDER UI) + L111 (Live ARAM Mayhem test) + L129 (Phase 3 fixture flow) + L130 (Active Match step 5) + L133 (ADR-007 phase 3) + L140 (set_augment_intent Cherry) all legitimately operator/live-gated. **Sweep cycle decay:** wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / 8=1 / 9=1 / 10=1 / 11=1 / 12=1 / **13=0**. Methodology stable across 13 rounds; wave 13 confirms saturation at item 150's post-housekeeping baseline. **Slice C 16th consecutive cost/latency CLEAN no-commit (read-only investigative agent):** 7 levers all surveyed. (1) Prompt-cache CLEAN: 7 coaches + coach_integration/_coach.py = 8 cache_control sites; 23 messages.create() callers covered. (2) Route TTL CLEAN: 14 routes with `_CACHE` (matches item 146 baseline). (3) Polling cadences CLEAN: 500/1000/2000/3000-20000ms tiers, no sub-500ms network polls. (4) Log spam DRIFT FLAG (MINOR PROPOSAL): 8-entry `_SUPPRESS_LOG_PATHS` unchanged BUT NEW high-frequency endpoint surfaced - **`/api/minimap-crop` at 0.417 hits/sec** over 71-min window (1786 hits / 4286s). Above item 150 baseline's top non-suppressed (`ward-heat` 0.149/sec). The endpoint is a UI-local poller (250ms fast / 2000ms slow) gated on `MINIMAP_MODES = {sr, aram, brawl}` + `document.hidden` short-circuit. Threshold "<1/sec" still satisfied (0.417 < 1.0); does not breach hard limit but is now the top non-suppressed path - notable enough to surface as operator-gated proposal. Also `/api/bridge` 0.245/sec / `/api/ward-heat` 0.158/sec / `/api/adaptation` 0.105/sec / `/api/activity` 0.097/sec / `/api/health/all` 0.066/sec. (5) Model tier CLEAN: 8 coach modules all carry haiku-4-5; 3 sonnet annotations are POST-call telemetry stamps; 0 Opus outside agent6. (6) Scheduled tasks CLEAN: 14 RC-* tasks matches item 146 catalog. (7) Bundle size CLEAN: 27 panel CSS files = 27 dashboard.css @imports. Parity guard test 4/4 PASSED. **Verdict: 16th consecutive CLEAN no-commit.** 1 minor proposal flagged (operator-gated). **Slice D `d2711ba` (committed directly to main post-Slice-A) docs sync to post-item-151 state (ENGINE 1.45.0 + cc_conditional 39/35 + DS suite 4016):** Initially Slice D ran on pre-A baseline + returned CLEAN no-commit confirming item 150's docs durable. Post-Slice-A merge required follow-up docs flip. 6 surgical edits across 4 files. (1) `docs/DAEMON_SLAYER.md` L5: `ENGINE_VERSION 1.44.0 - 3991 tests` -> `1.45.0 - 4016 tests` + appended wave 8 ENGINE 1.45.0 clause (Morgana R / Seraphine E / Evelynn W = 39 entries / 35 champs) + wave 7 ship-time note. (2) `docs/DAEMON_SLAYER.md` L32: `3991 tests passing` -> `4016 tests passing`. (3) `README.md` L46: `3,991 tests` -> `4,016 tests`. (4) `BRIEF.md` L20: `ENGINE_VERSION 1.44.0` -> `1.45.0`. (5) `BRIEF.md` L26: `3,991 passing tests` -> `4,016 passing tests`. (6) `docs/ARCHITECTURE.md` L161: `ENGINE_VERSION 1.44.0` -> `1.45.0` + `3991 tests` -> `4016` + cc_conditional waves 0-6 + wave 7 schema-lift forward-marker -> waves 0-8 with wave 8 ENGINE 1.45.0 annotation. Skipped per [[feedback_no_history_rewrite]]: BRIEF.md L33 (s174-s181 milestone narrative anchored to 2,822 baseline) + L57 (resume-pitch frozen at 2,703). Live source-of-truth probed via Python REPL: ENGINE_VERSION="1.45.0" + REGISTRY_TOTAL_ENTRIES=39 + REGISTRY_TOTAL_CHAMPIONS=35 + len(_DEFAULT_CONDITION_PROBABILITY)=12. Zero new non-ASCII bytes added; pre-existing 154+8+12+81 non-ASCII byte carryover on touched files left in place per operator-gated retro-sweep policy. **Merge order:** Slice A merged into main FIRST `656c4f5` (worktree branch `worktree-agent-a522d5e7b46c1f89e`, ort strategy, 0 conflicts, 36 files changed); Slice D follow-up docs sync committed directly to main SECOND `d2711ba` (4 files / 6 ins / 6 del). 0 merge conflicts. Slice B + C + initial Slice D verdicts returned no-commit so no further merges. **Verified:** DS suite **4016 passed / 1 skipped / 1 xfailed / 1669 subtests** (+25 over s150 3991 baseline = exactly Slice A wave 8 test file). py_compile + ruff + ASCII clean across all touched + new test file. DS :8893 restarted via `taskkill /F /PID 8212` + `schtasks /Run /TN RC-DaemonSlayer` ritual (per [[reference_ds_server_not_supervisor_watched]]); /health returns `engine_version=1.45.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; all 3 commit-bearing slices were DS engine + docs only). DS audit NO-CHANGE at engine 1.45.0 (no new Riot patch; live patch still 16.10.1). 1 worktree (Slice A) auto-removed post-merge. CI green on push expected. **Don't-redo:** cc_conditional registry now 39 entries / 35 champions; the Meraki parse-strip is STILL a STANDING constraint for OTHER candidates (Renekton W / Aatrox post-R / Volibear R / Briar W frenzy / multi-form same-spell-slot Karma+Hwei+Neeko) - the 3 wave 8 entries (Morgana R / Seraphine E / Evelynn W) were specifically the candidates whose mechanics are verifiable WITHOUT the schema lift because their durations + condition tags fit cleanly at the parse-strip level. The "16 consecutive saturation" verdict in item 150 was correct ONLY for the schema-lift-blocked candidates. Future cc_conditional wave 9 attempts should similarly look for REJECT carries from prior waves that DO NOT need the extractor schema lift - the 8 wave-1 REJECTs + 6 wave-2 REJECTs + 27 wave-3 REJECTs + 14 wave-4 REJECTs + 6 wave-5 REJECTs + 24 wave-6 REJECTs + 15+ wave-7 REJECTs + 27 wave-3-and-prior-REJECTs from this audit window were re-audited this run; 3 closed (this wave) + the rest remain operator-gated on the Meraki schema-lift. The orchestrator-merge pattern (3-4 worktree agents on disjoint slices + read-only Slice C + sequential merge + 0-2 commit-bearing slices + tests gate + DS restart + RC unchanged + docs sync follow-up commit if engine bumped) is now 17 consecutive runs (items 134-151). The 1 MINOR PROPOSAL from Slice C (`/api/minimap-crop` 0.417/sec to `_SUPPRESS_LOG_PATHS`) is operator-gated: still below 1/sec hard threshold but biggest log-spam contributor now (~1500 log lines/hr); 1-line tuple addition in `dashboard/_handler.py:74-83`; operator can flip during a future housekeeping pass. The Meraki extractor schema lift remains the strongest unblock lever for cc_conditional wave 9+ growth - would unblock the schema-lift-blocked candidates (Renekton W / Aatrox post-R / Volibear R / Briar W / Karma W form 1 / Hwei E form 1 / Neeko E). **Carries forward:** (a) Item 150 carries (a)-(i) ALL unchanged - the Meraki parse-strip is STILL the standing data-pipeline constraint for the SCHEMA-LIFT-BLOCKED cc_conditional candidates. (b) Schema lift the Meraki extractor to capture leveling + effects + description-text CC mechanics on form-empowered spells - operator-gated, would unblock wave 9+. (c) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 (in <1 day from this commit) - verify LastTaskResult=0 next session; role_grades JSON will reflect 9-col obj_participation on next aggregation. (d) DD Defy heal-on-takedown STILL deferred. (e) Live ARAM/SR smoke STILL pending. (f) All calibrations operator-gated (`_MISSING_HP_SHARE_FOR_HEALS` = 0.5 + `_CC_EFFECTIVENESS_FACTOR` = 0.5 + 12 default condition probability midpoints + 39 per-entry probability values). (g) BACKLOG L14 (a) kills -> takedowns semantic flip STILL operator-gated. (h) UI/UX live-game audit ritual owed once operator plays a real game. (i) Frozen-file grant NOT used this run. (j) Slice C MINOR PROPOSAL: `/api/minimap-crop` to `_SUPPRESS_LOG_PATHS` - operator-gated. (k) Wave 9+ should target REJECT carries that DO NOT need Meraki schema lift (the 3 wave 8 closures are the methodology template).

150. ✅ **2026-05-22 - item 150 4-slice parallel housekeeping drain (cc_conditional wave 8 CLEAN + BACKLOG stale-sweep wave 12 + cost/latency CLEAN wave 15 + living docs catchup post-148) SHIPPED (3 commits + 1 merge `bc12982` `60c3aab` `28caf50`, pushed; non-engine; non-frozen; no ENGINE bump; no DS restart; no RC restart - all docs-only).** Operator "continue what is left as open items in parallel" -> 4 worktree agents dispatched concurrent (16th consecutive run using orchestrator-merge template items 134-150). 2 commit-bearing slices (B + D) merged + green + pushed. **Slice A CLEAN no-commit (cc_conditional wave 8 attempt):** Worktree agent re-verified item 148's parse-strip diagnosis. `data/daemon_slayer/16.10.1/champion_abilities.json` is parse-stripped to ONLY: `cooldown` + `damage_blocks` + `cost` + `damage_type` + `targeting` + `affects` + `is_aoe` + `form_index`. NO `effects` / `leveling` / `cc` / `disable_blocks` / `parse_notes` content. `damage_blocks[].attribute_kind` fleet-wide: damage 1010 / duration 200 / modifier 180 / heal 96 / slow 87 / other 80 / shield 56. The 34 CC-attribute "duration" pins are EITHER already in 36-entry cc_conditional OR 108/89 unconditional registry OR slows OR turret-only OR multi-form-empowered (needs same-spell-slot schema lift not authorized this run). **REJECT verdicts confirmed:** Renekton W Ruthless Predator (damage-only blocks; empowered-Fury stun in stripped description), Volibear R Stormbringer (only Turret Disable Duration 2/3/4s parsed - turret-only NOT champion), Aatrox R World Ender (only Bonus Movement Speed 60/80/100% parsed; post-R passive empowered Q-sweetspot/W-pull in stripped description), Briar W Blood Frenzy (only self-buffs Bonus AS 55-95% + Bonus MS 24-60% parsed; frenzy-state empowered Q/E/R in stripped description). Bonus candidates Karma W form_index=1 (R-empowered Renewal Total Root 2.1-3.25s) + Hwei E form_index=1 (Grim Visage Disable 1.0-1.5s) + Neeko E (Empowered Root 1.8-3.0s) all REJECTED for multi-form-on-same-spell-slot schema constraint. **Recommendation:** Schema-lift the Meraki extractor to capture `effects` / `leveling` / description-text CC mechanics on form-empowered spells - until then COND_FRENZY_STATE + COND_RANGE_GATED stay as forward-marker tags with empty registry (mirrors s112 STAT_GRANT_CALC_KEYS precedent). Worktree + branch cleaned (auto-removed since no changes). **Slice B `60c3aab` (merge `28caf50`) docs(backlog) stale-entry sweep wave 12 per [[feedback_backlog_path_stale_check]]:** 1 flip BACKLOG.md L13 cc_conditional ecosystem subsection: `(items 141-146)` -> `(items 141-148)` + `"Registry grew across 5 wave expansions"` -> `"Registry grew across 6 wave expansions + wave 7 tag schema lift"`. Inline lineage extended with item 147 wave 6 (35/32 -> 36/32 Brand Q multi-wave coexistence) + item 148 wave 7 TAG SCHEMA LIFT (2 NEW condition tag constants COND_FRENZY_STATE + COND_RANGE_GATED registered at midpoint 0.4; registry unchanged 36/32; tag total 10 -> 12). ROADMAP CLEAN (L223 Fleet status DS row already current through ENGINE 1.44.0). BACKLOG L14 (obj_participation 9-col) already correctly flipped via item 149 same-commit edit. BACKLOG L15 Dead Man's Plate SHIPPED. L16-L106 all legitimately operator-gated / live-gated / research-closed. All 13 ROADMAP open items verified not stale. **Sweep cycle decay:** wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / 8=1 / 9=1 / 10=1 / 11=1 / **12=1**. Methodology stable across 12 rounds. **Slice C 15th consecutive cost/latency CLEAN no-commit (read-only investigative agent):** 7 levers all CLEAN. (1) Prompt-cache: 7 coaches/*.py + coach_integration/_coach.py = 8 with cache_control markers; all 23 messages.create() callers covered or exempt. (2) Route TTL: 14 routes_*.py files with `_CACHE` constants (matches item 146's "17 routes cached" approximate count). (3) Polling cadences: 500ms applyStaleness UI-local / 1000ms map cooldown UI-local / 2000ms LCU+state / 3000-20000ms refresh - no sub-500ms network polls (item 147 baseline). (4) Log spam: top non-suppressed `ward-heat?window_s=90` at **0.149/sec** + `activity?limit=6` at 0.098/sec + `health/all` at 0.066/sec - ALL well below 1/sec threshold. 8-entry `_SUPPRESS_LOG_PATHS` in `_handler.py:74-83` unchanged from item 146. (5) Model tier: no new messages.create() callers since item 147; Sonnet/Opus annotations remain POST-call telemetry stamps per item 146 don't-redo. (6) Scheduled tasks: `Get-ScheduledTask RC-*` returns 14 tasks matching item 146 catalog exactly. (7) Bundle size: `web/css/panels/` has **27 files** = `dashboard.css` has **27 panel @imports** PERFECT PARITY. Item 149's NEW guard test `tests/test_dashboard_css_panel_imports_parity.py` PASSED 4/4 (panel files imported / imported files exist / build_order.css IS @imported / loading_view.css NOT @imported). Item 149's bidirectional parity guard is durable + locking. **Verdict: CLEAN no-commit.** 15th consecutive CLEAN sweep since item 134; lever lane exhausted absent product-fidelity tradeoffs. **Slice D `bc12982` (committed directly to main, NOT a worktree branch) docs sync to post-item-148 state (ENGINE 1.44.0 + cc_conditional 36/32 + 12 condition tags + DS suite 3991 + cc_conditional ecosystem 5 surfaces):** 5 surgical edits across 4 files + archmap regen. (1) `docs/DAEMON_SLAYER.md` L5: `ENGINE_VERSION 1.43.0` -> `1.44.0` + `3962 tests` -> `3991 tests` + `10 condition tags + 10 operator-tunable midpoints` -> `12 / 12` + `28-entry seed` -> `36-entry seed` + appended wave 7 ENGINE 1.44.0 schema-lift clause (COND_FRENZY_STATE + COND_RANGE_GATED at midpoint 0.4 forward-marker; registry unchanged 36/32). (2) `docs/DAEMON_SLAYER.md` L32: `2710 tests passing` -> `3991 tests passing`. (3) `README.md` L46: `3,120 tests` -> `3,991 tests`. (4) `BRIEF.md` L20: `ENGINE_VERSION 1.42.0` -> `1.44.0`. (5) `BRIEF.md` L26: `3,881 passing tests` -> `3,991 passing tests`. (6) `docs/ARCHITECTURE.md` archmap regen via `tools/gen_archmap.py` (added routes_cc_blended_ehp_threat.py + routes_cc_conditional_pressure.py + routes_ward_heat.py + _state_cooldowns.py + ward_events.py + ward_producer.py + 2 test files; removed 2 stale entries). (7) `docs/ARCHITECTURE.md` L161: `ENGINE 1.22.0` -> `1.44.0` + `2777 tests` -> `3991` + `185/118/69%` -> `196/125/73%` override-registry totals + inserted cc_conditional ecosystem one-liner (5 consumer surfaces + 36/32 + wave 7 schema lift). Skipped per [[feedback_no_history_rewrite]]: BRIEF.md L33 (s174-s181 milestone narrative anchored to 2,822 baseline) + L57 (resume-pitch frozen at 2,703). Live source-of-truth probed via Python REPL: ENGINE_VERSION="1.44.0" + REGISTRY_TOTAL_ENTRIES=36 + REGISTRY_TOTAL_CHAMPIONS=32 + len(_DEFAULT_CONDITION_PROBABILITY)=12. Zero new non-ASCII bytes added; 71 pre-existing U+2192 arrows on touched lines are operator-gated retro-sweep carryover noted in CLAUDE.md no-em-dash rule. **Merge order:** Slice D pushed directly to main `bc12982` first (orchestrator-equivalent commit), Slice B merged into main second `28caf50` (non-ff merge of `60c3aab`). 0 merge conflicts (Slice B touched BACKLOG.md only, Slice D touched 4 different files). Worktree cleanup: Slice A auto-removed (no commit), Slice C no worktree created (read-only), Slice B + Slice D worktree branches LEFT IN PLACE on remote per pattern. **Verified:** 98 passed on item-149 surfaces (test_obj_participation + test_dashboard_css_panel_imports_parity) + 88 passed on cc_conditional wave 7 baseline. py_compile + ruff + ASCII clean across all touched. No DS restart (non-engine + Slice A produced no engine changes). No RC restart (no route module edits this run; all 4 slices were docs only). No ENGINE bump. No frozen-file edits. CI green on push expected (will verify post-push). **Don't-redo:** the Meraki parse-strip is a STANDING constraint - do NOT re-pitch cc_conditional wave 8 from Renekton/Aatrox/Volibear/Briar W until the extractor lifts to capture `effects`/`leveling`/description-text content. The fleet-wide stripped-shape analysis (1010 damage / 200 duration / 180 modifier / 96 heal / 87 slow / 80 other / 56 shield blocks) is the definitive saturation evidence for the current Meraki dump - any future wave needs schema lift in the extractor first. Karma W + Hwei E + Neeko E multi-form-on-same-spell-slot constraint requires a registry schema lift parallel to s232 conditional axis - operator-gated separately. The orchestrator-merge pattern (3-4 worktree agents on disjoint slices + Slice D read-only + sequential merge + 0-1 ENGINE bump commit + tests gate + restart-as-needed + docs sync) is now 16 consecutive runs (items 134-150). Slice D pushing directly to main vs a worktree branch is acceptable when the agent is doing a pure docs sync (zero risk of conflict with other slices that touch disjoint files); the orchestrator pattern is robust to either approach. The `_SUPPRESS_LOG_PATHS` 8-entry suppression is calibrated; ward-heat at 0.149/sec is well below 1/sec threshold so do NOT add. 27=27 CSS @import parity guard is the durable invariant per item 149's `test_dashboard_css_panel_imports_parity.py` - any future panel CSS addition + removal must keep the bidirectional parity intact. **Carries forward:** (a) Item 149 carries (a)-(l) ALL unchanged - the Meraki parse-strip on cc_conditional wave 8 is the standing data-pipeline constraint. (b) Schema lift the Meraki extractor to capture leveling + effects + description-text CC mechanics on form-empowered spells - operator-gated, would unblock wave 8 + future waves on Renekton/Aatrox/Volibear/Briar W + the 3 multi-form same-spell-slot candidates (Karma W form_index=1 Renewal Total Root / Hwei E form_index=1 Grim Visage Disable / Neeko E Empowered Root). (c) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 (in 2 days) - verify LastTaskResult=0 next session; role_grades JSON will reflect 9-col obj_participation on next aggregation. (d) DD Defy heal-on-takedown STILL deferred. (e) Live ARAM/SR smoke STILL pending. (f) All calibrations operator-gated. (g) BACKLOG L14 (a) kills -> takedowns semantic flip STILL operator-gated. (h) UI/UX live-game audit ritual owed once operator plays a real game with build_order.css now correctly loaded + obj_participation 9-col live. (i) Frozen-file grant NOT used this run.

149. ✅ **2026-05-22 - item 149 obj_participation 8-col -> 9-col turret add + orphan CSS @import cleanup SHIPPED (commit `7b3d351`, pushed `ced0a9e..7b3d351`; non-engine; non-frozen; RC :8888 restarted pid 7356 alive=True last_reload_ok=True; no DS restart; no ENGINE bump).** Operator AskUserQuestion-picked "Orphan CSS cleanup + obj_participation widen (Recommended)" from 4-option fork (over DS 4-slice drain run 15 / live game smoke + UI audit / headless-upgrade long run). DS 4-slice drain run 15 was the WEAKER option this session per item 148 carry diagnostics: cc_conditional wave 8 entries that consume the new COND_FRENZY_STATE/COND_RANGE_GATED tags are DATA-LIMITED (Briar W parse-stripped at Meraki; Renekton/Aatrox/Volibear need real ability_abilities.json dump with leveling intact); unconditional _PER_SPELL_CC_DURATIONS wave 10 saturated for net-new champs per item 147 (audit walked all 226 unregistered spells). The 3 carries from items 146/147/148 + BACKLOG L14 (c) were the strongest NOW lane. Closes 3 carries in ONE slice: (a) item 146/147/148 carry: `web/css/dashboard.css:27` orphan `@import './panels/loading_view.css'` (file does NOT exist; browser silently 404'd every cold load + console warning). (b) item 147/148 carry: `web/css/panels/build_order.css` (175 LOC live stylesheet for s214 build-order card used by item_build.js + champ_select.js) was MISSING from the @import block - browser never loaded it (inverse orphan: file exists, no @import). (c) BACKLOG L14 option (c): obj_participation 8-col -> 9-col adds non-overlapping turret count beyond first_tower binary sentinels via `max(turretTakedowns - ftk - fta, 0)` clamp. **CSS cleanup (`web/css/dashboard.css`):** Removed orphan loading_view.css @import (1 line). Added build_order.css @import (1 line, alphabetically next to item_build.css per existing block convention). NEW guard test `tests/test_dashboard_css_panel_imports_parity.py` (+4 tests) pins parity BOTH DIRECTIONS via regex scan of dashboard.css `@import './panels/*.css'` entries against the `web/css/panels/*.css` directory listing. Specific pins on build_order.css presence + loading_view.css absence prevent regression of either orphan direction. **obj_participation 9-col model (`core/obj_participation.py`):** NEW `_TURRET_KEY = "turretTakedowns"` constant. `_challenges_objectives()` widened from 2-tuple (herald, void) to 3-tuple (herald, void, turret). Single parse trip preserved per item-135 single-trip optimization. NEW `_turret_from_challenges()` thin wrapper sibling of `_herald_from_challenges()` + `_void_from_challenges()`. `compute_obj_participation()` SELECT now pulls `COALESCE(first_tower_kill, 0)` + `COALESCE(first_tower_assist, 0)` separately from the 6-col sum so the per-row arithmetic can subtract the binary sentinels: `turret_extra = max(turretTakedowns - ftk - fta, 0.0)`. The `max()` clamp is LOAD-BEARING - prevents NEGATIVE contribution when raw_turret=0 but ftk=1 fires (stale-blob case where SQL sentinel fired but challenges_json was not yet populated at match-end). Docstring widened 8-col -> 9-col with NON-OVERLAPPING semantic + carry-forward note that the 3 other challenges_json fields (baronTakedowns / dragonTakedowns / epicMonsterSteals) FULLY OVERLAP existing SQL columns and stay deferred per BACKLOG L14 option (a) until operator opts into kills -> takedowns rebaseline. Legacy schema fallback path widened to silently degrade to 6-col when challenges_json column missing. **+15 tests across 3 existing files + +4 new parity file = +19 net:** `tests/test_obj_participation.py` 81 -> 94 (+13): +6 ChallengesObjectivesParseOnceTests rebaselined for 3-tuple shape (incl. NEW only-turret pin) + +1 `_TURRET_KEY` constant pin + +1 9-col docstring pin + +7 NEW TurretTakedownsTests class (operator-lift / first-tower overlap not double-counted / assist overlap / clamp-at-zero with stale ftk / split-credit ally takes more / legacy schema falls back / malformed turret value drops to zero) - 1 deprecated 7-col docstring test reshaped to nine_column model. `tests/test_routes_post_game_rubric_obj.py` 16 -> 19 (+3): NEW TurretEnrichmentRouteTests (turret enrichment lifts total_score / first_tower overlap not double-counted / composes with herald+void). Fixture `_seed_obj_db` gains `operator_turret` + `ally_turret` kwargs + populates `turretTakedowns` in challenges_json blob. `tests/test_postmortem_analyze.py` 74 -> 77 (+3): NEW TurretEnrichmentWireTests (turret enrichment widens obj_pct / first_tower overlap not double-counted / teammate turret lowers operator share). `_populate_match_a_objectives_blob` helper gains `self_turret` + `ally_turret` kwargs. `tests/test_dashboard_css_panel_imports_parity.py` NEW (+4): PanelImportParityTests (every panel CSS file is @imported / every @imported panel file exists / build_order.css IS @imported / loading_view.css NOT @imported). **Live curl verified end-to-end on operator's NA1_5560540832 (Kai'Sa ADC, May 14 Normal Draft Loss):** pre-restart (8-col) obj_participation=0.331 total_score=37.28 tier=C; post-restart (9-col) obj_participation=0.239 total_score=36.37 tier=C. Direction is CORRECT for this specific match: operator did NOT participate in towers 2-11 their teammate destroyed; the team-wide denominator grew faster than the operator's numerator (0.331 inflated baseline because 8-col only credited the first tower, hiding ally's towers 2-11). In matches where operator GOT towers 2-11, the ratio will RISE (per 7 unit tests covering both directions). Aggregate across operator's 624 historic matches will calibrate the true participation score on next RC-PostmortemAnalyze cron run. **Verified:** RC suite **3219 / 67 subtests / 0 failed**; 191/0 across all 4 touched test files; py_compile + ruff + ASCII clean across all 5 touched source files + 1 new test file. RC :8888 restarted via `restart_trigger.txt` (route module edit triggers reload): pid 7356 alive=True last_reload_ok=True mode=client. No DS restart (non-engine). No ENGINE bump. No frozen-file edits. No worktree agents (single-slice scoped session). **Don't-redo:** The 3 other challenges_json overlap fields (baronTakedowns / dragonTakedowns / epicMonsterSteals) STILL deferred per BACKLOG L14 option (a); only operator can flip the SQL kills -> Match-V5 takedowns semantic (would rebaseline existing 624 historic role-grades). Do NOT pre-ship. The `max()` clamp on turret_extra is LOAD-BEARING - prevents a negative contribution when raw_turret=0 but ftk=1 (stale blob case). Pinned by `test_clamp_at_zero_when_no_turrets_but_first_tower_set` + `test_first_tower_overlap_does_not_double_count`. Do NOT remove. `_challenges_objectives()` 3-tuple shape is the canonical schema as of item 149 (was 2-tuple item 135). Future widenings reshape into 4-tuple etc.; the per-key wrapper helpers (`_herald_from_challenges` / `_void_from_challenges` / `_turret_from_challenges`) stay as thin accessors. Do NOT split into 3 separate JSON parses (would triple-parse per row, defeats the single-trip optimization). CSS @import parity test (`test_dashboard_css_panel_imports_parity.py`) is the durable guard - any future panel CSS addition must come with a matching @import OR will fail the orphan-file scan; conversely any @import to a removed file fails the orphan-import scan. Do NOT relax either direction. The 4 specific pins (build_order.css IS @imported / loading_view.css NOT @imported) anchor the item 149 fixes. Live curl direction on operator's NA1_5560540832 was DOWN (0.331 -> 0.239) for this match specifically - this is the correct semantic, not a bug. Do NOT re-litigate; in matches where the operator participated in towers 2-11, ratio rises. **Carries forward:** (a) Item 148 carries (a) loading_view.css orphan @import + (b) build_order.css inverse orphan FILE NOW CLOSED via this commit. (b) BACKLOG L14 (c) NOW CLOSED. BACKLOG L14 (a) (kills -> takedowns semantic flip) STILL operator-gated. (c) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; the role_grades JSON will reflect 9-col obj_participation on next aggregation. (d) Future cc_conditional wave 8 entries that consume the new COND_FRENZY_STATE / COND_RANGE_GATED tags STILL operator-gated when Meraki-verifiable mechanic surfaces (Renekton W Fury / Aatrox post-R / Volibear R passive). (e) `_PER_SPELL_CC_DURATIONS` unconditional registry unchanged at 108/89 (wave 10+ exhausted at 16.10.1 per item 147 audit). (f) DD Defy heal-on-takedown STILL deferred (takedown-rate uncertainty). (g) Live ARAM/SR smoke STILL pending. (h) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` + 12 default condition probability midpoints + per-entry probability values STILL operator-gated. (i) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated separately. (j) UI/UX live-game audit ritual owed once operator plays a real game (including the s214 build-order card that NOW renders correctly with build_order.css loaded). (k) Per-page UI audit ritual still owed on the build-order card specifically (the fix landed but visual verification on a live champ-select is owed). (l) Frozen-file grant NOT used this run.

148. ✅ **2026-05-22 - item 148 cc_conditional wave 7 tag schema lift (COND_FRENZY_STATE + COND_RANGE_GATED forward-marker) SHIPPED (commit `2547836`, pushed `0adf131..2547836`; ENGINE 1.43.0 -> 1.44.0; non-frozen; DS :8893 restarted serves 1.44.0; RC :8888 unchanged - DS engine + tests only).** Operator AskUserQuestion-picked "Conditional CC schema lift" from 4-option fork (over DS 4-slice drain run 15 / orphan CSS @import cleanup / live game smoke + UI/UX audit). Closes item 147 carry (l) "Briar frenzy + Sylas range REJECTs STILL need new condition tag constants (separate schema lift)". Ships 2 NEW condition tag constants as a FORWARD-MARKER schema-lift seam mirroring s112 STAT_GRANT_CALC_KEYS pattern at ENGINE 1.22.0 + pre-1.32.0 _PER_SPELL_CC_DURATIONS forward-marker pattern. NO new registry entries this wave (registry stays at 36 entries / 32 champions); the schema lift unblocks future entries without a separate engine bump. **NEW condition tag constants:** (1) `COND_FRENZY_STATE` = `"frenzy_state"` - champion enters a self-empowered state (Briar W Blood Frenzy / Renekton Fury threshold / Volibear R passive form / Aatrox post-R passive) that gates an empowered variant of another spell with first-order CC. (2) `COND_RANGE_GATED` = `"range_gated"` - CC fires only when the cast lands within a specific range band (close-range or max-range). Distinct from COND_TERRAIN (positioning vs map geometry) and COND_NTH_HIT (stack accumulation). Both tags registered in `_DEFAULT_CONDITION_PROBABILITY` with calibrated midpoint **0.4** (mid-low because the empowered-state / range-band prerequisite is operator-controlled but not guaranteed; calibration sits below COND_DUAL_ENEMY 0.6 + COND_NTH_HIT 0.7 midpoints but above COND_TERRAIN 0.3 floor). Both exported via `__all__` so external readers see them as public taxonomy. Operator-tunable via the per-tag override JSON loader (a JSON file at `data/cc_conditional_calibration.json` with `"default_condition_probability": {"frenzy_state": 0.5}` overrides the seed midpoint without source edits). **Meraki 16.10.1 re-verify during this run found** the item 147 carry-named candidates do NOT cleanly fit: (1) Sylas E2 Abduct stuns on hook hit REGARDLESS of cast range (the item 142 REJECT note calling it range-conditional was incorrect per Riot 16.10.1 spec - E2 always stuns on champion hit, no range gating); (2) Briar W has 2 forms (Blood Frenzy + Snack Attack) but the Meraki extract is parse-stripped to damage_blocks only - the leveling/effects detail needed to verify a frenzy-empowered Q or R CC mechanic is absent at parse-strip level. Future entries populate when a Meraki-verifiable mechanic surfaces (e.g. Renekton W empowered cast during Fury threshold, Aatrox passive-empowered abilities, Volibear R passive form transformation). The forward-marker schema lift is the deliverable - 2 new tags in the public taxonomy + the registered midpoints + the validated entry-construction path. **+29 tests** NEW `agents/daemon_slayer/tests/test_cc_conditional_wave7_tags.py` (~285 LOC across 7 classes): WaveSevenTagConstantsTests (6: existence + value pin + distinctness + no-collision-with-prior-tags) / WaveSevenDefaultProbabilityTests (6: registered + midpoint pin + total grown to 12 + calibrated-between-terrain-and-dual-enemy) / WaveSevenPublicTaxonomyTests (3: __all__ entries + alphabetical sort) / RegistryUnchangedTests (4: 32/36 unchanged + no entry consumes new tags + Briar unchanged + Sylas not added) / ConditionalCcEntryAcceptsNewTagsTests (3: construct with COND_FRENZY_STATE + COND_RANGE_GATED + unknown tag still rejected) / WaveSevenOverrideLoaderCompatibilityTests (3: JSON override accepts new tag keys + out-of-range still dropped) / EngineVersionCurrentTests (1: `assertGreaterEqual(parts, (1, 44, 0))` forward-compat) + AsciiHygieneTests (2). `tests/test_cc_conditional.py DefaultProbabilityMapCoverageTests.test_default_map_has_all_public_constants` expected_keys gained the 2 new tags (rebaseline pin from 10 -> 12 tags). `tests/test_cc_conditional_forward_marker.py _ALLOWED_TEST_FILES` gained `test_cc_conditional_wave7_tags.py`. **+32 stale ENGINE pin syncs** across 31 DS test files (1.43.0 -> 1.44.0) via bulk regex rewrite (pattern `assertEqual([^)]*ENGINE_VERSION[^)]*?)"1\.43\.0"` to `\g<1>"1.44.0"`). ENGINE_VERSION bumped in `agents/daemon_slayer/__init__.py:1324` with full 75-line changelog block covering the schema lift + Meraki re-verify findings + forward-marker pattern reference. Consumer math BYTE-IDENTICAL to 1.43.0 for all 5 consumer surfaces (cc_pressure + compute_ehp + compute_hybrid + coach prompt + dashboard UI) since no registry entry consumes either new tag at ship time. **Verified:** DS suite **3962 -> 3991 / 1 skipped / 1 xfailed / 1666 subtests** (+29 exactly = the new wave 7 tags test file). Wider RC **3278 passed / 67 subtests / 0 failed** post-DS-restart. py_compile + ruff + ASCII clean across all 5 touched + 1 new files (0 non-ASCII bytes added; 590 pre-existing non-ASCII in `__init__.py` noted as operator-gated retro-sweep carryover - wave 7 changelog block itself is 3837 chars / 0 non-ASCII verified). DS :8893 restarted via taskkill /F /PID 16932 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.44.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; DS engine + tests only). **Don't-redo:** the 2 new tag constants COND_FRENZY_STATE + COND_RANGE_GATED are CANONICAL forward-marker seam at ENGINE 1.44.0 - do NOT remove or rename. The calibrated midpoint **0.4** is the engineered starting calibration; operator-tunable via JSON override file BUT the SEED value lives in source and the JSON is the override path (NOT the other way). The bool defense + out-of-range drop + non-numeric drop validation inherits from item 145 JSON override loader and APPLIES to the new tag keys (`WaveSevenOverrideLoaderCompatibilityTests.test_out_of_range_override_for_new_tag_dropped` pins). Sylas E2 Abduct stun is NOT range-gated at 16.10.1 - do NOT re-pitch as a wave 8 COND_RANGE_GATED entry candidate (the item 142 REJECT note has been corrected in the wave 7 source docstring + this CLAUDE.md ledger). Briar W frenzy-empowered Q/R mechanics cannot be verified at parse-strip Meraki level - any future encoding needs a real Meraki ability dump with `effects`/`leveling` intact (the current `data/daemon_slayer/16.10.1/champion_abilities.json` is parse-stripped to `damage_blocks` only). The empty-registry-for-new-tags pattern is intentional (mirrors s112 STAT_GRANT_CALC_KEYS at 1.22.0 + pre-1.32.0 _PER_SPELL_CC_DURATIONS) - do NOT pre-ship speculative entries to "fill" the new tags. The orchestrator-merge pattern from items 134-147 is NOT used this run (single-slice schema-lift session; worktree agents reserved for multi-slice drains). The wave 7 test file uses `assertGreaterEqual(parts, (1, 44, 0))` for forward-compatibility per item 146 lesson. **Carries forward:** (a) `web/css/dashboard.css:27` orphan @import `./panels/loading_view.css` STILL operator-gated (carries from items 146 + 147 - confirmed present this run). (b) `web/css/panels/build_order.css` inverse orphan FILE flagged item 147 STILL operator-gated. (c) cc_conditional wave 8+ entries that consume COND_FRENZY_STATE or COND_RANGE_GATED operator-gated when Meraki-verifiable mechanic surfaces (candidates to research: Renekton W empowered cast during Fury threshold, Aatrox passive-empowered abilities, Volibear R passive form, hypothetical close-range-only stun on a Riot-redesigned spell). (d) DD Defy heal-on-takedown STILL deferred (item 130+ carry; takedown-rate uncertainty). (e) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 32 registered conditional CC champs OR one of the 89 unconditional CC champs. (f) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (g) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` + 12 default condition probability midpoints + 36 per-entry probability values calibrations STILL operator-gated. (h) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated separately. (i) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (j) UI/UX live-game audit ritual owed once operator plays a real game. (k) `_PER_SPELL_CC_DURATIONS` unconditional registry unchanged at 108/89 from item 147 (wave 10+ likely exhausted for net-new champs at 16.10.1 per item 147 audit). (l) Future cc_conditional wave 8 expansion from REJECT pool operator-gated. (m) Route semantic difference from item 144 unchanged. Frozen-file grant NOT used this run.

147. ✅ **2026-05-22 - item 147 4-slice parallel drain (cc_conditional wave 6 + _PER_SPELL_CC_DURATIONS wave 9 + BACKLOG stale-sweep wave 11 + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `22c55e8`, pushed `8d50732..22c55e8`; ENGINE 1.42.0 -> 1.43.0; non-frozen; DS :8893 restarted serves 1.43.0; RC :8888 unchanged - DS engine + tests only).** Operator AskUserQuestion-picked "DS 4-slice drain run 14 (Recommended)" from 4-option fork (over conditional-CC schema lift / orphan CSS @import cleanup / operator-tunable calibrations). 14th consecutive long-run using orchestrator-merge template (items 134-147). 4 worktree agents dispatched concurrent on item 146 carries (b) cc_conditional wave 6 expansion + (j) _PER_SPELL_CC_DURATIONS wave 9 (multi-wave coexistence only). Slice D returned CLEAN no-commit + 2 MINOR PROPOSALS (14th consecutive CLEAN cost/latency sweep). 3 commit-bearing slices merged + green + pushed. DS suite **3881 -> 3962 / 1 skipped / 1 xfailed / 1666 subtests passed** (+81: 44 Slice A + 37 Slice B). **Slice A `1dca288` (merge into main) feat(ds) cc_conditional wave 6 (35/32 -> 36/32; +1 entry / +0 net-new champs multi-wave coexistence on Brand):** Via setdefault builder pattern from items 140-146 with `_p(champion, spell, default)` helper from item 145 Slice A. NEW: **Brand Q Sear** (target_debuffed 0.5 stun 1.25s - stuns target carrying Blaze passive stack; coexists with Brand R wave 1 nth_hit stun via setdefault on different spell slot - THIRD multi-wave coexistence within cc_conditional after Aatrox Q+W + Briar Q+E + TahmKench R+Q). Uses only the 10 existing condition tags. REGISTRY_TOTAL_CHAMPIONS=32 unchanged / REGISTRY_TOTAL_ENTRIES=35 -> 36. Multi-wave coexistence count within cc_conditional now 4 champs: Aatrox Q3+W, Brand Q+R NEW, Briar Q+E, TahmKench R+Q. REJECTED wave-6 candidates documented in commit body: Lissandra E (no CC), Aurora R/E/Q/W (uncertain/no CC), Volibear R (turret-only), Renata R (no CC), Aphelios Q/R (no CC), Vex E (already shipped), Heimerdinger R-Q/R-W (already unconditional), Galio Q (slow), Brand W (passive not first-order), Lillia E (no CC), Sett W (needs new tag), Sett E (already shipped), Sett R (belongs unconditional), Tristana R (uncertain), Tristana W (no CC), Mel E (unconditional), Annie Pyromania (encoding ambiguous), Bel'Veth W (unconditional), Vayne wall (schema lift needed), Trundle E (no tag fit), Renekton W / Camille E (asymmetry), Naafiri R / Akali R / Jhin R / Pyke R (no CC), Akshan E (no CC), Quinn E (already unconditional), Karthus Q (no CC). +44 tests NEW `tests/test_cc_conditional_wave6.py` (~563 LOC). 5 prior-wave test files relaxed Brand-pin assertions to assertGreaterEqual + entry-level pins because Brand now has Q+R in registry (pattern matches item 145 Slice B Aatrox wave 3 relax): test_cc_conditional.py / test_cc_conditional_consumer_pressure.py / test_cc_conditional_consumer_ehp.py / test_cc_conditional_overrides.py + 1-line _ALLOWED_TEST_FILES add in test_cc_conditional_forward_marker.py. EngineVersionCurrentTests uses `assertGreaterEqual((1, 42, 0))` for forward-compat per item 146 lesson. Consumer math BYTE-IDENTICAL to 1.42.0 for default include_conditional=False callers. **Slice B `3c73c3b` (merge into main) feat(ds) _PER_SPELL_CC_DURATIONS wave 9 (106/89 -> 108/89; +2 entries / +0 net-new champs both multi-wave coexistence):** Confirms item 146 carry (j) saturation prediction. Audit walked all 226 unregistered spells of all 89 registered champions; only 2 of 6 candidates surfaced were genuine unconditional first-order CC. NEW: **Chogath W Feral Scream** silence 1.6/1.7/1.8/1.9/2.0s across 5 ranks (coexists with Chogath Q wave 2 knockup via setdefault); **Malzahar Q Call of the Void** silence 1.0/1.25/1.5/1.75/2.0s across 5 ranks (coexists with Malzahar R wave 1 suppression via setdefault). Silence joins first-order CC scope per same precedent as wave 6 stasis (Bard R) - hard-disable types fit cleanly alongside stun/root/suspension/suppression. Both values verified vs `data/daemon_slayer/16.10.1/champion_abilities.json` Meraki silence-duration blocks. Multi-wave coexistence now proven at 9 unconditional champions: Lulu W+R / Sejuani R+Q / Thresh Q+E / Zac E+R / Lissandra R+W / Maokai R+W / Rakan W+R / **Chogath Q+W (NEW)** / **Malzahar R+Q (NEW)**. Selection rules unchanged from waves 1-8: first-order CC only; no slows; no conditional CC; canonical DDragon ids. REJECTED wave-9 candidates documented in commit body: Bard Q (cc_conditional wave 1), Morgana R (channel-completion conditional), Seraphine E (target-state conditional), Volibear R (turret-only), Janna R (undocumented duration), Lulu E (no CC). Consumer math BYTE-IDENTICAL to 1.42.0 for default include_conditional=False callers. +37 tests NEW `tests/test_per_spell_cc_registry_wave9.py` (~536 LOC across 7 classes). `tests/test_per_spell_cc_registry_seed.py` count pin 89/106 -> 89/108 (assertGreaterEqual floor). `tests/test_per_spell_cc_registry_wave8.py` 2 count pins assertEqual -> assertGreaterEqual relaxation. **Slice C `fc7219f` (merge into main) docs(backlog) stale-entry sweep wave 11 per [[feedback_backlog_path_stale_check]]:** 1 BACKLOG.md L13 cc_conditional ecosystem subsection flipped - stopping-at-item-145-state line ("items 141-145 / 4 wave expansions / 33 entries + JSON override loader") flipped to "items 141-146 / 5 wave expansions / 35 entries" reflecting item 146 wave 5. ROADMAP sweep CLEAN (Fleet status DS row already current at ENGINE 1.42.0 per item 146 Slice D verified). All 13 ROADMAP open items legitimately operator-gated / live-gated. BACKLOG L14-L41 all current. Sweep cycle decay: wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / 8=1 / 9=1 / 10=1 / **11=1**. Methodology stable across 11 sweep rounds. **Note:** Slice C ran on pre-A baseline so the orchestrator commit additionally bumped BACKLOG L13 to reflect THIS RUN'S item 147 state (35 -> 36 cc_conditional; 106 -> 108 unconditional; 5 -> 6 wave expansions; items 141-146 -> 141-147) - same pattern as item 146 orchestrator commit's BACKLOG follow-up. **Slice D CLEAN no-commit (14th consecutive CLEAN since item 134) + 2 MINOR PROPOSALS (operator-gated, deferred):** Read-only investigative agent surveyed 7 levers all CLEAN: prompt-cache (19 files carry cache_control marker across 8 active coach modules; 22 messages.create() caller files); route TTL (11 routes carry TTL cache pattern + 6 with broader cache literals = ~17 cached routes matches item 146 baseline); polling cadences (1.5s bridge_pending / 2s state / 500ms applyStaleness UI-local / 1000ms map cooldown UI-local, all sane); log spam (8-entry _SUPPRESS_LOG_PATHS; top non-suppressed /api/bridge at 0.197/sec well below 1/sec threshold, consistent with item 146's 0.26/sec window); model tier (all real messages.create() use claude-haiku-4-5-20251001; 3 r._model="claude-sonnet-4-6" annotations in aram/arena/brawl coaches are POST-call telemetry stamps per item 146 don't-redo, 0 Opus outside agent6 auditor); scheduled tasks (14 unique RC-* tasks matches item 146 catalog exactly); bundle size (27 panel files vs 29 @import lines in dashboard.css = 28 panel @imports including item 146 orphan loading_view.css NOT fixed this run + NEW build_order.css inverse orphan FILE EXISTS but NOT @imported). **MINOR PROPOSALS (operator-gated, deferred):** (1) `web/css/dashboard.css:27` orphan @import to `./panels/loading_view.css` STILL PRESENT (carries from item 146 Slice D); did NOT auto-fix per item 146 guidance. (2) NEW this run: `web/css/panels/build_order.css` EXISTS in panels dir but is NOT @imported in dashboard.css (inverse orphan: file is either dead-code waiting to be deleted OR @import is missing - depends on whether build_order panel is in production; safe 1-line CSS investigation but operator-gated). Carry-forward for operator. **Orchestrator commit `22c55e8` bumps ENGINE_VERSION 1.42.0 -> 1.43.0 in `__init__.py`** with full changelog block covering Slice A + B + C + D + syncs **32 stale ENGINE pin sites across 31 DS test files** via bulk rewrite (regex pattern `assertEqual([^)]*ENGINE_VERSION[^)]*?)1\.42\.0` to `\g<1>1.43.0`). Also catches up BACKLOG.md L13 to THIS RUN'S state (36/108 + items 141-147 + 6 wave expansions). ROADMAP.md Fleet status DS row appended waves 6 + 9 annotations. **Merge order:** C first (`fc7219f`, BACKLOG.md only), A second (`1dca288`, cc_conditional.py + 5 prior-wave test relaxes + new wave6 test + forward-marker allowlist), B third (`3c73c3b`, ability_dps.py + new wave9 test + seed/wave8 count pin updates). 0 merge conflicts (3 entirely disjoint surfaces: A=cc_conditional + cc_conditional tests; B=ability_dps + per_spell tests; C=BACKLOG.md only). **Verified:** DS suite **3962 passed** (+81 over s146 3881 = exactly 44 Slice A + 37 Slice B). py_compile + ruff clean across all touched. 0 non-ASCII bytes added. DS :8893 restarted via `taskkill /F /PID 10380` + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.43.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits this run; all 3 slices were DS engine + docs only). DS audit NO-CHANGE at engine 1.43.0 (no new Riot patch dropped; live patch still 16.10.1; audit-streak halt inherits forward). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local). **Don't-redo:** cc_conditional registry now 36 entries / 32 champions; multi-wave coexistence count now 4 cc_conditional champions (Aatrox Q+W, Brand Q+R NEW, Briar Q+E, TahmKench R+Q) + 9 unconditional champions (Lulu, Sejuani, Thresh, Zac, Lissandra, Maokai, Rakan, Chogath NEW, Malzahar NEW). _PER_SPELL_CC_DURATIONS unconditional registry now 108/89; wave 9 was multi-wave coexistence only (NO net-new champions) - the unconditional first-order CC at 16.10.1 is GENUINELY SATURATED for net-new champions (audit this run walked all 226 unregistered spells of 89 registered champs, only 2 of 6 surfaced candidates were viable). Silence joins first-order CC scope per stasis precedent (wave 6 Bard R) - hard-disable types fit cleanly. Brand Q Sear target_debuffed (NEW) + Brand R wave 1 nth_hit coexist via setdefault on different spell slots. Chogath Q wave 2 knockup + Chogath W wave 9 silence coexist similarly. Malzahar R wave 1 suppression + Malzahar Q wave 9 silence coexist. The `assertGreaterEqual((1, 42, 0))` pin shape in wave 6 test EngineVersionCurrentTests is forward-compatible (does NOT need orchestrator flip on bump); future wave test files should continue using this pattern. The orchestrator-merge pattern (3-4 worktree agents on disjoint slices + Slice D read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + RC unchanged + CI verify + docs sync) is now 14 consecutive runs (items 134-147) - DURABLE template. **Carries forward:** (a) `web/css/dashboard.css:27` orphan @import `./panels/loading_view.css` STILL operator-gated (carries from item 146 Slice D, confirmed present this run). (b) NEW: `web/css/panels/build_order.css` orphan FILE flagged this run Slice D - operator-gated investigation (either dead-code awaiting deletion OR missing @import line in dashboard.css). (c) cc_conditional registry now 36 entries / 32 champions; future wave 7+ expansion operator-gated from same REJECT pool (Briar frenzy + Sylas range REJECTs STILL need new condition tag constants - separate schema lift, explicitly carried forward from item 146 carry (k)). (d) DD Defy heal-on-takedown STILL deferred. (e) Live ARAM/SR smoke STILL pending - operator must play with one of the 32 registered conditional CC champs OR one of the 89 unconditional CC champs. (f) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (g) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations STILL operator-gated. (h) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated separately. (i) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (j) UI/UX live-game audit ritual owed once operator plays a real game. (k) `_PER_SPELL_CC_DURATIONS` unconditional registry now 108/89; wave 10+ likely exhausted for unconditional first-order CC at 16.10.1 (audit this run walked all 226 unregistered spells = data lane exhausted; future expansion requires new schema). (l) Briar frenzy + Sylas range REJECTs STILL need new condition tag constants (separate schema lift). (m) Route semantic difference from item 144 unchanged. Frozen-file grant NOT used this run.

146. ✅ **2026-05-22 - item 146 4-slice parallel drain (cc_conditional wave 5 + _PER_SPELL_CC_DURATIONS wave 8 + BACKLOG stale-sweep wave 10 + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `06c67a6`, pushed `b887f29..06c67a6`; ENGINE 1.41.0 -> 1.42.0; non-frozen; DS :8893 restarted serves 1.42.0; RC :8888 unchanged - DS engine + tests only).** Operator "continue DS and /done for /clear when complete" -> 13th consecutive long-run using orchestrator-merge template (items 134-145). 4 worktree agents dispatched concurrent on item 145 carries (a) cc_conditional wave 5+ expansion + (i) _PER_SPELL_CC_DURATIONS wave 8. Slice D returned CLEAN no-commit + 1 MINOR PROPOSAL (13th consecutive CLEAN cost/latency sweep). 3 commit-bearing slices merged + green + pushed. DS suite **3791 -> 3881 / 1 skipped / 1 xfailed / 1666 subtests passed** (+90: 52 Slice A + 38 Slice B); wider RC suite **3203 passed / 67 subtests / 0 failed** (unchanged - DS engine + tests only); phase8_smoke **75 passed** post-DS-restart. **Slice A `fbb1354` (merge `9586f63`) feat(ds) cc_conditional wave 5 (33/32 -> 35/32; +2 entries / +0 net-new champs both multi-wave coexistence):** Via setdefault builder pattern from items 140-145 with `_p(champion, spell, default)` helper threading per-entry probability overrides from item 145 Slice A. NEW: **Briar E Chilling Scream** (channel_completion 0.5 fear 1.0s - full-charge cone fear; tap-cast damage only; coexists with Briar Q wave 4 terrain stun via setdefault on different spell slot - SECOND multi-wave coexistence in cc_conditional after Aatrox Q+W); **TahmKench Q Tongue Lash** (nth_hit 0.7 stun 1.5s - 3rd-stack stun via passive; coexists with TahmKench R wave 1 devour suppression via setdefault). Uses only the 10 existing condition tags. REGISTRY_TOTAL_CHAMPIONS=32 unchanged / REGISTRY_TOTAL_ENTRIES=33 -> 35. Multi-wave coexistence count now 4 cc_conditional champions: Aatrox Q3+W (waves 3+4), Briar Q+E (waves 4+5 NEW), TahmKench R+Q (waves 1+5 NEW), Pantheon W+Q (waves 1+4 already shipped). REJECTED wave-5 candidates documented in commit body: Vayne wall-stun (schema lift needed), Mel E (needs Meraki reverify), Annie passive Pyromania (encoding ambiguous), Belveth W (slow only), Cho'Gath W (unconditional silence belongs in unconditional registry), Trundle E (no clean tag fit). +52 tests NEW `tests/test_cc_conditional_wave5.py` (~600 LOC across 9 classes: WaveFiveNewEntryShapeTests / WaveFiveValuePinsTests / WaveFiveNewChampionsTests / RegistryGrowthTests / ExistingSeedPreservedTests / WaveFiveCoexistenceWithUnconditionalTests / AggregatorWaveFiveTests / EngineVersionCurrentTests `assertGreaterEqual(parts, (1, 41, 0))` forward-compatible / AsciiHygieneTests). `tests/test_cc_conditional_wave4.py` 3 Briar aggregator assertions relaxed to entry-level pins because Briar now has Q+E in registry. `tests/test_cc_conditional_forward_marker.py` 1-line `_ALLOWED_TEST_FILES` add. Consumer math BYTE-IDENTICAL to 1.41.0 for default include_conditional=False callers. **Slice B `4ab11ba` (merge `7f9e9a1`) feat(ds) _PER_SPELL_CC_DURATIONS wave 8 (103/89 -> 106/89; +3 entries / +0 net-new champs via multi-wave coexistence):** Via setdefault builder pattern from items 140-141. NEW: **Lissandra W Ring of Frost** root 1.25/1.35/1.45/1.55/1.65s across 5 ranks (coexists with Lissandra R wave 1 unconditional stun via setdefault); **Maokai W Twisted Advance** root 1.0/1.1/1.2/1.3/1.4s across 5 ranks (coexists with Maokai R wave 1 unconditional root via setdefault); **Rakan R The Quickness** charm 1.0/1.25/1.5s all 3 ranks (coexists with Rakan W wave 1 unconditional knockup via setdefault). All 3 values verified vs `data/daemon_slayer/16.10.1/champion_abilities.json` Meraki duration blocks. Multi-wave coexistence now proven at 7 unconditional champions: Lulu W+R (waves 1+6), Sejuani R+Q (waves 1+6), Thresh Q+E (waves 1+6), Zac E+R (waves 2+7), **Lissandra R+W (waves 1+8 NEW)**, **Maokai R+W (waves 1+8 NEW)**, **Rakan W+R (waves 1+8 NEW)**. Selection rules unchanged from waves 1-7: first-order CC only; no slows; no conditional CC; canonical DDragon ids. REJECTED wave-8 candidates documented in commit body: Bard Q / Karma W / TwistedFate W / Zilean Q / Aatrox W / Skarner Q (all already in cc_conditional), Evelynn W / Morgana R / Seraphine E (conditional axes - belong in cc_conditional), Volibear R (turret-only), Tristana W (slow), Senna W (already shipped wave 4 per item 138). Consumer math BYTE-IDENTICAL to 1.41.0 for default include_conditional=False callers. +38 tests NEW `tests/test_per_spell_cc_registry_wave8.py` (~541 LOC across 7 classes). `tests/test_per_spell_cc_registry_seed.py` count pin 89/103 -> 89/106. `tests/test_per_spell_cc_registry_wave7.py` count pin assertEqual -> assertGreaterEqual relaxation. **Slice C `5325d45` (merge `fc572d4`) docs(backlog) stale-entry sweep wave 10 per [[feedback_backlog_path_stale_check]]:** 1 BACKLOG.md L13 cc_conditional ecosystem subsection flipped - stopping-at-item-144-state line ("items 141-144 / 3 wave expansions / 28 entries") flipped to "items 141-145 / 4 wave expansions / 33 entries + JSON override loader" annotation reflecting item 145 wave 4 + JSON override loader. ROADMAP sweep CLEAN (Fleet status DS row already current at ENGINE 1.41.0 per item 145 Slice D verified). All 13 ROADMAP open items legitimately operator-gated / live-gated (carry-forward catalog re-verified). BACKLOG L14-L18 all current. Sweep cycle decay: wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / 8=1 / 9=1 / **10=1**. Methodology stable across 10 sweep rounds. **Slice D CLEAN no-commit (13th consecutive CLEAN since item 134) + 1 MINOR PROPOSAL (operator-gated, deferred):** Read-only investigative agent surveyed 7 levers. 6 CLEAN: prompt-cache (8 callers explicit-block w/ cache_control + replay_coach + experimental_builder verified); route TTL (17 routes cached); polling cadences (1.5s bridge_pending / 2s state matches item 145 baseline); log spam (/api/bridge at 0.26/sec well below 1/sec threshold, consistent with item 145's 0.021/sec window - both below threshold); model tier (21/21 messages.create() Haiku per reference_model_config.md; sonnet/opus annotations are POST-call telemetry stamps NOT API tier); scheduled tasks (7 install scripts: DDragon / Postmortem / Rewind / LegionBridge / Phase3 x2 / run_postmortem; cadences sane). **MINOR PROPOSAL (Lever 7):** bundle size = 28 panel @imports vs 27 panel files; `web/css/dashboard.css:27` @imports `./panels/loading_view.css` but that file does not exist - orphan reference; safe 1-line CSS edit but operator-gated since file may have been intentionally orphaned during a removal; deferred to operator with carry-forward (a). **Orchestrator commit `06c67a6` bumps ENGINE_VERSION 1.41.0 -> 1.42.0 in `__init__.py`** with full changelog block covering Slice A + B + C + D + syncs **32 stale ENGINE pin sites across 30 DS test files** via bulk rewrite (regex pattern `assertEqual([^)]*ENGINE_VERSION[^)]*?)1\.41\.0` to `\g<1>1.42.0`). **Merge order:** C first (`fc572d4`, BACKLOG.md only), A second (`9586f63`, cc_conditional.py + new wave 5 test + wave 4 relax + forward-marker allowlist), B third (`7f9e9a1`, ability_dps.py + new wave 8 test + 2 count pin updates). 0 merge conflicts (3 entirely disjoint surfaces: A=cc_conditional + cc_conditional tests; B=ability_dps + per_spell tests; C=BACKLOG.md only). One transient orchestrator misstep: initial merge of C ran in wrong worktree (Slice A's branch instead of main); recovered via `git update-ref refs/heads/<branch> <original_sha>` to reset Slice A branch back to fbb1354, then re-merged from main cleanly - no commits lost, no extra noise in final log. **Verified:** DS suite **3881 passed** (+90 over s145 3791 = exactly 52 Slice A + 38 Slice B). 366/366 cc_conditional surface tests across 12 files pass (incl. new wave 5). py_compile + ruff clean across all touched. 0 non-ASCII bytes added (pre-existing carryover in `__init__.py` docstring noted, operator-gated retro-sweep). DS :8893 restarted via `taskkill /F /PID 13204` + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.42.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits this run; all 3 slices were DS engine + docs only). DS audit NO-CHANGE at engine 1.42.0 (no new Riot patch dropped; live patch still 16.10.1; audit-streak halt inherits forward). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local). **Don't-redo:** cc_conditional registry now 35 entries / 32 champions; multi-wave coexistence count now 4 cc_conditional champions (Aatrox Q+W, Briar Q+E, TahmKench R+Q, Pantheon W+Q) + 7 unconditional champions (Lulu, Sejuani, Thresh, Zac, Lissandra, Maokai, Rakan). _PER_SPELL_CC_DURATIONS unconditional registry now 106/89; wave 8 was multi-wave coexistence only (NO net-new champions) - the unconditional first-order CC at 16.10.1 is genuinely saturated for net-new champions, future waves either coexist on already-registered champs OR need new schema (conditional/range/state-gated). Senna W was confirmed shipped in wave 4 (item 138) - do NOT re-pitch as a wave 8 candidate; the item 145 REJECT list calling Senna W "unconditional" was correct in spirit but the registry already had it. Briar Q (wave 4 cc_conditional) + Briar E (wave 5 cc_conditional NEW) coexist via setdefault on different spell slots; do NOT collapse to single spell entry. The `assertGreaterEqual(parts, (1, 41, 0))` pin shape in wave 5 test EngineVersionCurrentTests is forward-compatible (does NOT need orchestrator flip on bump); future wave test files should use this pattern over `assertEqual` for self-resilience. The wave 8 test used `assertEqual` which orchestrator flipped via bulk-rewrite; both patterns work but `assertGreaterEqual` saves orchestrator effort. The orchestrator-merge pattern (3-4 worktree agents on disjoint slices + Slice D read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + RC unchanged + CI verify + docs sync) is now 13 consecutive runs (items 134-146) - DURABLE template. The transient worktree-cd misstep (running merge in wrong worktree branch) is RECOVERABLE via `git update-ref` to reset the branch + re-merge from main; the recovery left zero artifact in the final log; future runs should explicitly `cd "C:/Riot Commander"` (the main worktree) before each `git merge` to avoid this. **Carries forward:** (a) `web/css/dashboard.css:27` orphan @import `./panels/loading_view.css` flagged by Slice D - safe 1-line CSS edit operator-gated (file may have been intentionally orphaned during a removal; 1 failed HTTP fetch per cold dashboard load + console warning; restores perfect-parity invariant 27=27). (b) cc_conditional registry now 35 entries / 32 champions; future wave 6+ expansion operator-gated from same REJECT pool. (c) DD Defy heal-on-takedown STILL deferred. (d) Live ARAM/SR smoke STILL pending - operator must play with one of the 32 registered conditional CC champs OR one of the 89 unconditional CC champs. (e) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (f) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations STILL operator-gated; item 145 JSON override loader covers 10 default tag midpoints + 35 per-entry probabilities but NOT the 2 EHP-side constants. (g) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated separately. (h) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game. (j) `_PER_SPELL_CC_DURATIONS` unconditional registry now 106/89; wave 9+ likely thin pool (saturation for net-new champions). (k) Briar frenzy + Sylas range REJECTs STILL need new condition tag constants (separate schema lift - explicitly carried forward from item 145 carry (j)). (l) Route semantic difference from item 144 unchanged. Frozen-file grant NOT used this run.

145. ✅ **2026-05-22 - item 145 4-slice parallel drain (cc_conditional JSON override loader + cc_conditional wave 4 expansion + BACKLOG stale-sweep wave 9 + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `8262706`, pushed `222be15..8262706`; ENGINE 1.40.0 -> 1.41.0; non-frozen; DS :8893 restarted serves 1.41.0; RC :8888 unchanged - DS engine + tests only).** Operator "continue ds" -> 12th consecutive long-run using orchestrator-merge template (items 134-144). 4 worktree agents dispatched concurrent on item 144 carries (e) operator-tunable calibration midpoints + per-entry probabilities AND (j) future cc_conditional wave 4+ expansion. Slice D returned CLEAN no-commit (12th consecutive CLEAN cost/latency sweep). 3 commit-bearing slices merged + green + pushed. DS suite **3682 -> 3791 / 1 skipped / 1 xfailed / 1666 subtests passed** (+109: 46 Slice A + 63 Slice B); wider RC suite **3203 passed / 67 subtests / 0 failed** (unchanged - DS engine + tests only). **Slice A `ebeb8c9` (merge `aad8493`) feat(ds) cc_conditional JSON override loader (operator personal calibration mirror of item 131 post_game_rubric pattern):** NEW `_OVERRIDES_PATH = Path("data") / "cc_conditional_calibration.json"` (gitignored personal calibration; mirror of `data/post_game_rubric_weights.json` from item 131 Slice B `b892519`). NEW `_load_overrides() -> dict` fail-soft (missing file -> {}, malformed JSON -> {}, non-dict shape -> {}, list shape -> {}). NEW `_apply_default_probability_overrides(defaults, overrides) -> dict` (unknown tags silently dropped + bool defense LOAD-BEARING before int/float per item 131 + out-of-range [0.0, 1.0] DROPPED not clamped + non-float dropped). NEW `_apply_per_entry_overrides(overrides) -> dict[(champion, spell), float]` (malformed colon-keys dropped + same bool defense + out-of-range drop). Module-load step: `_OVERRIDES_RAW = _load_overrides()` -> `_DEFAULT_CONDITION_PROBABILITY = _apply_default_probability_overrides(_DEFAULT_CONDITION_PROBABILITY, _OVERRIDES_RAW)` -> `_PER_ENTRY_PROBABILITY_OVERRIDES = _apply_per_entry_overrides(_OVERRIDES_RAW)`. `_build_per_spell_cc_conditional()` gained inline `_p(champion, spell, default)` helper that reads `_PER_ENTRY_PROBABILITY_OVERRIDES.get((champion, spell), default)`; threaded into all 28 existing setdefault entries (wave 1 + wave 1 expansion + wave 2 + wave 3) as `probability=_p("Champ", "Spell", canonical_value)`. Schema: `{"default_condition_probability": {<tag>: <float>}, "per_entry_probability": {<champion>:<spell>: <float>}}` both top-level keys optional. `__all__` extended with new public symbols. `.gitignore` gained `data/cc_conditional_calibration.json` between `data/post_game_rubric_weights.json` and `data/decisions_heartbeat.json` (personal-calibration band per item 131 convention). +46 tests in NEW `tests/test_cc_conditional_overrides.py` (~608 LOC across 5 classes: LoadOverridesTests 10 / ApplyDefaultProbabilityOverridesTests 10 / ApplyPerEntryOverridesTests 10 / IntegrationTests 5 / NoOverrideFileDefaultPreservationTests 3 + AsciiHygieneTests 1). `tests/test_cc_conditional_forward_marker.py` `_ALLOWED_TEST_FILES` gained `test_cc_conditional_overrides.py`. **Slice B `01cc0d5` (merge `1c0c0e1`) feat(ds) cc_conditional wave 4 (28/28 -> 32/33; +5 entries / +4 new champs + 1 multi-wave coexistence):** Via setdefault builder pattern from items 140-144. NEW: **Nunu R Absolute Zero** (channel_completion 0.5 knockup 0.5s - 3s channel completion AOE knockup; Nunu first registered first-order CC); **Yuumi Q Prowling Projectile** (channel_completion 0.4 root 1.75s - max-distance-travel root; Yuumi first registered first-order CC); **Pantheon Q Comet Spear empowered** (channel_completion 0.5 stun 1.0s - long-cast empowered version; coexists with Pantheon W unconditional stun via setdefault on different spell slot); **Aatrox W Infernal Chains** (debuffed_target 0.5 root 1.75s - chain debuff persistence pull-back; FIRST multi-wave coexistence with Aatrox Q wave 3 nth_hit knockup on different spell slot via setdefault); **Briar Q Head Rush** (terrain 0.3 stun 1.0s - terrain-collision stun; the Briar Q+R frenzy-state-gated variants from item 144 carry-forward STILL require new condition tag schema lift not in this wave). Uses only the 10 existing condition tags. REGISTRY_TOTAL_CHAMPIONS=32 / REGISTRY_TOTAL_ENTRIES=33. REJECTED wave-4 candidates documented in commit body: Senna W (unconditional), Aurora E / Aurora R (mechanic-uncertain), Heimerdinger R-Q (no additional CC beyond E base), Galio Q (slow only), Galio R (already unconditional), Briar W (no first-order CC), Briar Q/R frenzy variants (needs new tag schema lift), Naafiri R / Akali R / Jhin R / Pyke R / Sett R (no CC), Sett W (needs new tag), Brand W (passive not first-order), Lillia E (no CC). +63 tests NEW `tests/test_cc_conditional_wave4.py` (~689 LOC across 9 classes). `tests/test_cc_conditional_wave3.py` 3 Aatrox aggregator assertions relaxed to entry-level pins because Aatrox now has Q+W in registry. `tests/test_cc_conditional_forward_marker.py` 1-line `_ALLOWED_TEST_FILES` add. Consumer math BYTE-IDENTICAL to 1.40.0 for default include_conditional=False callers. **Slice C `a6d66a3` (merge `356c3df`) docs(backlog) stale-entry sweep wave 9 per [[feedback_backlog_path_stale_check]]:** 1 BACKLOG.md L13 cc_conditional ecosystem subsection flipped - stale "18 entries / 18 champions" + "future consumer wires" claim replaced with comprehensive 5-consumer ecosystem annotation documenting 3 wave expansions (10/10 -> 18/18 -> 23/23 -> 28/28) + 5 consumer wires shipped (cc_pressure DIRECT + compute_ehp INDIRECT + compute_hybrid INDIRECT + coach prompt + dashboard UI). Sweep cycle: wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / 8=1 / **9=1**. ROADMAP sweep CLEAN (Fleet status DS row already current at ENGINE 1.40.0). Cost/latency CLEAN no-commit (**12th consecutive CLEAN since item 134**). All 7 levers surveyed: prompt-cache (8 callers explicit-block w/ cache_control); route TTL (16 routes cached); polling cadences (1.5s bridge_pending / 2s state matches s144); log spam (/api/bridge at 0.021/sec well below 1/sec threshold); model tier (21/21 messages.create() Haiku per reference_model_config.md; 3 sonnet/opus annotations are POST-call telemetry stamps NOT API tier); scheduled tasks (14 RC-* sane); bundle size (27 panel CSS files = 27 @import lines perfect parity). Verdict CLEAN; lever lane exhausted absent product-fidelity tradeoffs. **Orchestrator commit `8262706` bumps ENGINE_VERSION 1.40.0 -> 1.41.0 in `__init__.py`** with full changelog block covering Slice A + B + C + D + syncs **58 stale ENGINE pin sites across 33 DS test files** via bulk rewrite. ROADMAP.md Fleet status DS row appended slices. **Post-merge thread fix:** Slice B's 5 raw-probability literals (Nunu R / Yuumi Q / Pantheon Q / Aatrox W / Briar Q at lines 1070/1093/1119/1147/1176) flipped to use Slice A's `_p()` helper so per-entry overrides apply uniformly to ALL 33 registry entries (orchestrator-merge resolution at orchestrator commit). **Merge order:** C first (`356c3df`, BACKLOG.md only), A second (`aad8493`, cc_conditional.py + .gitignore + new test file + forward-marker allowlist), B third (`1c0c0e1`, cc_conditional.py setdefault entries + new wave 4 test + wave 3 relax + forward-marker allowlist). 1 auto-merge in `cc_conditional.py` builder function (resolved cleanly by ort strategy; both A's `_p()` helper + B's 5 new entries integrated). **Verified:** DS suite **3791 passed** (+109 over s144 3682 = exactly 46 Slice A + 63 Slice B). 414/414 cc_conditional surface tests across 11 files pass. py_compile + ruff clean across all touched. 0 non-ASCII bytes added (216 pre-existing carryover in `__init__.py` docstring noted, operator-gated retro-sweep). DS :8893 restarted via taskkill /F /PID 15428 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.41.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits this run). DS audit NO-CHANGE at engine 1.41.0 (no new Riot patch dropped; live patch still 16.10.1; audit-streak halt inherits forward). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local). **Don't-redo:** cc_conditional now has personal calibration via `data/cc_conditional_calibration.json` (gitignored) - operator can tune 10 default tag midpoints + per-entry probabilities. The override file is OPTIONAL; absence preserves canonical seed values (pinned by NoOverrideFileDefaultPreservationTests). The bool defense (`isinstance(value, bool)` BEFORE int/float) is LOAD-BEARING per item 131 - Python bool is int subclass; do NOT relax. Out-of-range [0,1] values are DROPPED not clamped (operator setting >1.0 or <0.0 is rejected, NOT silently clamped to 1.0/0.0 - failure-by-rejection bias for calibration data). The inline `_p()` helper inside `_build_per_spell_cc_conditional()` is the per-entry-override threading seam; ALL 33 entries now flow through it. The 5 wave-4 entries (Nunu R / Yuumi Q / Pantheon Q / Aatrox W / Briar Q) use ONLY the 10 existing condition tags (no new tags - the Briar frenzy + Sylas range REJECTs from item 144 STILL need a separate schema lift). Aatrox W + Aatrox Q now coexist on Aatrox's spell map via setdefault (FIRST multi-wave coexistence in cc_conditional, parallel to Lulu W+R / Sejuani R+Q / Thresh Q+E / Zac E+R in unconditional registry). Pantheon Q + Pantheon W coexist (Q conditional empowered stun, W unconditional stun). The orchestrator-merge pattern (3 worktree agents on disjoint slices + Slice D read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + RC unchanged + CI verify + docs sync) is now 12 consecutive runs (items 134-145) - durable template. Per-entry override applies regardless of registry growth (future wave 5+ entries flow through the same `_p()` helper automatically). The forward-marker scan at `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}` stays unchanged (Slice A + Slice B BOTH consume INDIRECTLY through the existing builder; neither imports cc_conditional from outside the module). **Carries forward:** (a) cc_conditional registry now 33 entries / 32 champions; future wave 5+ expansion operator-gated from same REJECT pool (Briar/Sylas needing new condition tag constants is the SAME schema lift still queued). (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending - operator must play with one of the 33 registered conditional CC champs OR one of the 89 unconditional CC champs. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations STILL operator-gated; the JSON override loader extends operator control to the 10 default tag midpoints + 33 per-entry probabilities but does NOT cover the 2 EHP-side constants. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated separately. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) UI/UX live-game audit ritual owed once operator plays a real game. (i) `_PER_SPELL_CC_DURATIONS` unconditional registry unchanged at 103/89 (no wave 8 this run). (j) Briar frenzy + Sylas range REJECTs STILL need new condition tag constants (separate schema lift - explicitly carried forward from item 144 carry (k)). (k) Route semantic difference from item 144 unchanged (routes_cc_conditional_pressure ratio = ally_avg / enemy_avg, >=1.05 = good FOR US). Frozen-file grant NOT used this run.

144. ✅ **2026-05-22 - item 144 4-slice parallel drain (cc_conditional FOURTH + FIFTH consumer wires (coach prompt + dashboard UI) + cc_conditional wave 3 expansion + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `9ab2435`, pushed `1237f15..9ab2435`; ENGINE 1.39.0 -> 1.40.0; non-frozen; DS :8893 restarted serves 1.40.0; RC :8888 restarted clean pid 13420 -> 10300 last_reload_ok=True - new dashboard route module triggered restart).** Operator "continue DS" -> 11th consecutive long-run using orchestrator-merge template (items 134-143). 4 worktree agents dispatched concurrent on item 143 carry (a) - the 4th + 5th and FINAL consumer surfaces of cc_conditional + wave 3 registry expansion + read-only cost/latency CLEAN. All 3 commit-bearing slices merged + green + pushed. Slice D returned CLEAN no-commit (11th consecutive CLEAN cost/latency sweep). **The cc_conditional ecosystem is now COMPLETE across 5 consumer surfaces** (engine math 3/3 cc_pressure + compute_ehp + compute_hybrid + coach prompt + dashboard UI). DS suite **3623 -> 3682 / 1 skipped / 1 xfailed / 1666 subtests passed** (+59 Slice C); wider RC suite **3108 -> 3203 / 67 subtests / 0 failed** (+95: 53 Slice A + 42 Slice B). **Slice A `f39d8c8` (merge `735d6ac`) feat(coach) cc_conditional FIRST coach prompt consumer (4th overall surface):** NEW `core/cc_conditional_impact_context.py` (~165 LOC) `cc_conditional_impact_line(enemies, mode, *, fight_window_s=6.0, cc_effectiveness_factor=0.5) -> str` is the FIRST coach-prompt consumer of cc_conditional (4th overall after item 142 cc_pressure + item 143 compute_ehp + item 143 compute_hybrid in the engine math layer). Sibling of `core/cc_blended_ehp_context.py` (item 138 Slice A `738c005`). Reads `compute_cc_pressure(name, mode, include_conditional=True)` per enemy + consumes `.conditional_cc_seconds` directly (already post-tenacity probability-weighted by the engine seam at ENGINE 1.38.0). Defaults `_FIGHT_WINDOW_S = 6.0` + `_CC_EFFECTIVENESS_FACTOR = 0.5` MATCH `agents/daemon_slayer/ehp.py` constants EXACTLY (pinned by `MathTests::test_default_constants_match_ehp_module`). Output line format: `"Enemy conditional CC pressure: 8.4s probability-weighted (additive on top of unconditional CC) = 100% saturation, blended-EHP impact -50%"` (verified live smoke: Annie + Brand + Mordekaiser enemies in ARAM = Brand R 1.4s + Morde R 7.0s = 8.4s saturates 6s window -> 50% blended-EHP impact). Returns empty string when mode is None/blank, mode NOT in {SR, ARAM, KIWI, ARAM_5V5, ARAM_MAYHEM, CHERRY, ARENA, BRAWL, NB, URF, OFA}, enemies is None/empty, OR conditional_total == 0.0 (zero token overhead in the non-target case). ImportError fallback stub mirrors `core/enemy_cc_threat_context.py` pattern for parallel-slice ship robustness. Wired into all 4 active mode coach prompts APPEND-only per cache-prefix preservation (CLAUDE.md item 121 doctrine): `coaches/aram_coach.py` `_USER_TMPL` adds `{cc_conditional_impact}` placeholder BELOW the existing `{cc_blended_ehp_impact}` (item 138) + per-call format reads `state.get("enemy_comp", [])`; `coaches/arena_coach.py` `_USER_TMPL` adds placeholder reading `_arena_enemies`; `coaches/brawl_coach.py` 3 prompts NB/URF/OFA via shared `_cc_conditional_line` + `_cc_conditional_segment` concat pattern; `coach_integration/_sr_prompt.py` `SR_SYSTEM_PROMPT` APPENDS after the cc_blended_ehp impact line. +53 tests in NEW `tests/test_cc_conditional_impact_context.py` (~600 LOC across 10 classes). NO ENGINE math change (coach-prompt consumer). **Slice B `a204480` (merge `6875e49`) feat(coach) cc_conditional FIRST dashboard UI consumer (5th overall surface):** NEW `dashboard/routes_cc_conditional_pressure.py` (~260 LOC) GET `/api/cc-conditional-pressure?ally=<csv>&enemy=<csv>&mode=ARAM` returns `{ok, mode, ally_conditional_cc_s, enemy_conditional_cc_s, ratio, tier (good|warn|bad), ally_total_cc_seconds, enemy_total_cc_seconds, elapsed_ms, cached}`. **Per-side aggregation:** each champion in roster scored via `compute_cc_pressure(champion, mode, include_conditional=True).conditional_cc_seconds`; both sides averaged independently; ratio = `ally_avg / enemy_avg`. Tier bands ratio >= 1.05 -> `good` (ally team has more conditional CC to apply = follow-up window FOR us); 0.95 <= ratio < 1.05 -> `warn`; ratio < 0.95 -> `bad` (enemy carries more conditional CC threat against us). When BOTH sides total 0.0 -> ratio=1.0 tier=warn; pinned by `MathTests::test_zero_cc_both_sides_returns_balanced_warn`. Mode-aware 5-min TTL cache. Fail-soft 400/200/503. Registered in `dashboard/_dispatch.py`. NEW `web/js/panels/cc_conditional_pressure.js` (~180 LOC ESM) mirrors item 140 cc_blended_ehp_threat.js shape. Mount via `#csv-sugg-cc-conditional-pressure` chip between `#csv-sugg-cc-blended-ehp-threat` (item 140) and `#csv-sugg-pickorder`. NEW `web/css/panels/cc_conditional_pressure.css` (~135 LOC) with 3 `data-cc-cond-tier` variants. Imported via `web/css/dashboard.css`. Wired in `web/js/panels/champ_select.js` via NEW `_csvRenderCcConditionalPressure` helper. +42 tests across `test_routes_cc_conditional_pressure.py` (20) + `test_cc_conditional_pressure_panel_dom.py` (22). Live curl verified post-restart: `ally=Annie,Galio,Leona&enemy=Brand,Mordekaiser,TwistedFate&mode=ARAM` returned `ok=true ally_conditional_cc_s=0.0 enemy_conditional_cc_s=3.0 ratio=0.0 tier=bad enemy_total_cc_seconds=9.0` (Brand R 1.4 + Morde R 7.0 + TF W 0.6 = 9.0s; ally none = bad threat balance). NO ENGINE math change. **Slice C `6285b74` (merge `0799ec1`) feat(ds) cc_conditional wave 3 expansion (23/23 -> 28/28; +5 entries / +5 new champs):** Via setdefault builder pattern from items 140-143. NEW: **Aatrox Q3 Deathbringer Stance** (nth_hit 0.7 knockup 0.5s - 3rd-cast); **Riven Q3 Broken Wings** (nth_hit 0.7 knockup 0.75s - 3rd Broken Wings stage); **Yasuo Q3 Steel Tempest** (nth_hit 0.7 knockup 1.0s - 3rd Steel Tempest stack); **Yone Q3** (nth_hit 0.7 knockup 0.75s - mirror of Yasuo Q3); **Leblanc E Ethereal Chains** (channel_completion 0.5 root 1.5s - full-tether). Uses only the 10 existing condition tags. REGISTRY_TOTAL_CHAMPIONS=28 / REGISTRY_TOTAL_ENTRIES=28. REJECTED wave-3 candidates documented in commit body (Renekton W + Camille E extension-shape asymmetry; carryover Aurora R / Briar Q+R / Sylas E2 / Volibear R / Renata R / Aphelios Q / Lissandra E; new screens Naafiri R / Akali R / Jhin R / Pyke R / Sett W / Pantheon Q / Galio Q / Heimerdinger / Senna W / Brand W / Nunu R / Yuumi Q). +59 tests NEW `test_cc_conditional_wave3.py`. `test_cc_conditional_wave2.py` 2 count-pin assertions relaxed `assertEqual -> assertGreaterEqual`. `test_cc_conditional_forward_marker.py` 1-line allowlist addition. Consumer math BYTE-IDENTICAL to 1.39.0 for default include_conditional=False callers. **Slice D CLEAN no-commit (11th consecutive cost/latency CLEAN since item 134):** Read-only investigative agent surveyed 7 levers all CLEAN. Top non-suppressed `/api/bridge` 0.199/sec same as item 143. 26 panel CSS files = 26 dashboard.css @import lines (perfect parity). Lever lane exhausted. **Orchestrator commit `9ab2435` bumps ENGINE_VERSION 1.39.0 -> 1.40.0 in `__init__.py`** with full changelog block covering Slice A + B + C + D + syncs **32 stale ENGINE pin sites across 30 DS test files + Slice A new test + Slice C new wave3 test**. ROADMAP.md Fleet status DS row appended slices. Test fix in same orchestrator commit: `tests/test_routes_cc_conditional_pressure.py::test_zero_cc_both_sides_returns_balanced_warn` swapped enemy roster from Aatrox -> Caitlyn because Aatrox is no longer a "no conditional CC" pick post-wave-3 (Aatrox Q3 nth_hit landed Slice C). **Merge order:** C first (`0799ec1`), A second (`735d6ac`), B third (`6875e49`). 0 merge conflicts. **Verified:** RC **3203 passed / 67 subtests / 0 failed** (+95 over s143 3108 baseline = exactly 53 Slice A + 42 Slice B). DS suite **3682 passed / 1 skipped / 1 xfailed / 1666 subtests** (+59 over s143 3623 baseline). phase8_smoke 75 passed post-DS-restart. py_compile + ruff + ASCII clean. DS :8893 restarted via Stop-Process PID 15600 + schtasks /Run RC-DaemonSlayer -> /health returns engine_version=1.40.0 patch=16.10.1 champions=172 items=705. RC :8888 restarted via `restart_trigger.txt` -> pid 13420 -> 10300 alive=True last_reload_ok=True mode=client. Live curl `/api/cc-conditional-pressure` verified end-to-end. DS audit NO-CHANGE at engine 1.40.0 (no new Riot patch dropped; live patch still 16.10.1). UI/UX audit live-game-gated. 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local). **Don't-redo:** cc_conditional ecosystem now COMPLETE - 5 consumer surfaces wired: (1) cc_pressure FIRST engine math at 1.38.0 + (2) compute_ehp SECOND engine math at 1.39.0 + (3) compute_hybrid THIRD engine math at 1.39.0 + (4) cc_conditional_impact_line FIRST coach prompt at 1.40.0 + (5) routes_cc_conditional_pressure FIRST dashboard UI at 1.40.0. Slice A coach line is APPEND-only via `_USER_TMPL` per-call (cache-prefix preservation; do NOT migrate to system prompt). Slice B route uses ratio = ally_avg / enemy_avg semantic where >=1.05 = ally has more conditional CC to apply (good FOR US); <0.95 = enemy carries more conditional threat against us (bad). Aatrox Q3 nth_hit conditional means Aatrox is no longer a "no conditional CC" pick for tests verifying zero-conditional roster - use Garen / Caitlyn / Annie / Galio / Leona / Darius / Lulu / Singed instead. The 5 wave-3 Q3-based nth_hit entries (Aatrox / Riven / Yasuo / Yone) demonstrate 3-stage abilities with knockup-on-3rd-cast fit COND_NTH_HIT exactly; do NOT introduce a "stack-based" tag. Leblanc E channel_completion follows Karma W + Warwick R pattern. Forward-marker scan `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}` stays unchanged - Slice A reads via INDIRECT kwarg-only consumption (not direct cc_conditional import); same pattern as item 143 Slice A + B. Orchestrator-merge pattern now 11 consecutive runs (items 134-144) - durable template for parallel headless-upgrade-style drains. **Carries forward:** (a) cc_conditional ecosystem CONSUMER WIRE COMPLETE (5/5 natural surfaces). Future work is LIVE-GAME VALIDATION (operator plays a real ARAM with one of the 28 conditional CC champs to verify all 5 surfaces reflect signal correctly). (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` + 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints + 28 per-entry probability values calibrations operator-gated. (f) Item 133 carry (c) personal calibration STILL operator-gated. (g) obj_participation widening STILL operator-gated. (h) UI/UX live-game audit ritual owed once operator plays a real game. (i) `_PER_SPELL_CC_DURATIONS` unconditional registry unchanged at 103/89 (no wave 8). (j) Future cc_conditional wave 4+ expansion from REJECT carries operator-gated. (k) Briar frenzy + Sylas range REJECTs still need new condition tag constants (separate schema lift). (l) Route semantic difference: routes_cc_conditional_pressure ratio = ally_avg / enemy_avg with >1 = good FOR US (we have more conditional CC to apply) is DIFFERENT from item 140 routes_cc_blended_ehp_threat ratio = ally_cc_blended_ehp / enemy_cc_blended_ehp with >1 = good FOR US (we weather CC better). Both axes are "good = ally favorable" but underlying scoring differs. Frozen-file grant NOT used this run.

143. ✅ **2026-05-22 - item 143 4-slice parallel drain (cc_conditional SECOND consumer wire via compute_ehp + cc_conditional THIRD consumer wire via compute_hybrid + cc_conditional wave 2 expansion + BACKLOG/cost-latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `23195c3`, pushed `365d804..23195c3`; ENGINE 1.38.0 -> 1.39.0; non-frozen; DS :8893 restarted serves 1.39.0; RC :8888 unchanged pid 13420 - all 3 slices touched DS engine + docs only, no route module edits).** Operator "continue ds" -> 7th long-run of day with parallel orchestrator pattern (10th consecutive run using items 134-142 template). 4 worktree agents dispatched concurrent on item 142 carry (h); Slice D returned CLEAN no-commit (10th consecutive CLEAN cost/latency sweep). 3 commit-bearing slices merged + green + pushed. DS suite **3534 -> 3623 / 1 skipped / 1 xfailed / 1666 subtests passed** (+89: 16 Slice A + 18 Slice B + 55 Slice C); wider RC suite **3108 passed** (phase8_smoke `test_live_three_profiles` flipped fail->pass post-DS-restart same pattern items 134-142). **Slice A `172ad6f` (merge `02ad602`) feat(ds) cc_conditional SECOND consumer wire via `compute_ehp(include_conditional=False)` kwarg:** `agents/daemon_slayer/ehp.py` gains `include_conditional: bool = False` kwarg AFTER `enemy_champions`. When True, propagates to internal `compute_cc_pressure(enemy, mode, include_conditional=True)` calls per enemy. Default False = BYTE-IDENTICAL to s137 EHP contract (4 existing cc_blended_ehp consumers preserved). INDIRECT wire - ehp.py has no direct dependency on the conditional CC registry; the kwarg flow is ehp -> cc_pressure -> conditional registry. Forward-marker `_ALLOWED_SOURCE_FILES` stays at `{"cc_pressure.py"}` (ehp.py is indirect kwarg-only consumer; not on the allowlist). +16 tests NEW `test_cc_conditional_consumer_ehp.py` (DefaultBehaviorTests 4 / IncludeConditionalTrueTests 6 / AramTenacityAppliesToConditionalEhpTests 3 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). **Slice B `9173e56` (merge `5f462f7`) feat(ds) cc_conditional THIRD consumer wire via `compute_hybrid(include_conditional=False)` kwarg:** `agents/daemon_slayer/hybrid.py` gains `include_conditional: bool = False` kwarg AFTER `enemy_champions`. Threads through ALL 3 internal `compute_ehp` call sites at lines 238/547/584 via `**_ehp_kwargs` gating pattern (passes kwarg ONLY when True so parallel-merge with Slice A works either order). HybridResult.cc_blended_ehp reflects conditional contribution automatically through ehp -> cc_pressure chain. Default False = BYTE-IDENTICAL to s139 hybrid contract. +18 tests NEW `test_cc_conditional_consumer_hybrid.py` (IdentityContractTests 4 / MultiCallSiteThreadingTests 3 / ConditionalCcAffectsHybridScoreTests 5 / HybridResultFieldsTests 3 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). **Slice C `90f7faf` (merge `1f0552e`) feat(ds) cc_conditional wave 2 registry expansion (18/18 -> 23/23; +5 entries / +5 new champs):** Via setdefault builder pattern from item 142. NEW: **Maokai Q Bramble Smash** (terrain 0.3 stun 1.0s; coexists with Maokai R wave 3 unconditional via setdefault); **Pyke E Phantom Undertow** (channel_completion 0.5 stun 1.25s; coexists with Pyke Q wave 3 unconditional); **Swain E Nevermove** (channel_completion 0.5 root 1.5/1.625/1.75/1.875/2.0s across 5 ranks); **Skarner Q Shattered Earth + Upheaval** (nth_hit 0.7 knockup 0.75s; coexists with Skarner R wave 3 unconditional); **Zilean Q Time Bomb** (nth_hit 0.7 stun 2.0s). Uses only the 10 existing condition tags (no new tag constants). REGISTRY_TOTAL_CHAMPIONS=23 / REGISTRY_TOTAL_ENTRIES=23. REJECTED wave-2 candidates documented in commit body: Sett W (needs new grit_meter tag), Pantheon Q (no extra CC), Aurora E (mechanic uncertain), Galio Q (fits unconditional), Heimerdinger R-E (complex multi-form), Senna W (actually unconditional), Aurora Q (mechanic uncertain at 16.10.1), Brand W (would double-count passive trigger). +55 tests NEW `test_cc_conditional_wave2.py` across 9 classes (WaveTwoNewEntryShapeTests / WaveTwoValuePinsTests / WaveTwoNewChampionsTests / RegistryGrowthTests / ExistingSeedPreservedTests / WaveTwoCoexistenceWithUnconditionalTests / AggregatorWaveTwoTests / EngineVersionCurrentTests / AsciiHygieneTests). `test_cc_conditional_wave1.py` 2 count-pin assertions relaxed `assertEqual -> assertGreaterEqual` for future-wave compatibility (mirrors wave 6/7 pattern). **Slice D `9931345` (merge `ffa1542`) docs(backlog) stale-entry sweep wave 8 per [[feedback_backlog_path_stale_check]]:** 1 BACKLOG.md L13 flip - post-cc_conditional paragraph reframed (stale claims "byte-identical to 1.36.0" + "next step is compute_cc_pressure kwarg" no longer accurate; cc_pressure IS shipped FIRST consumer at ENGINE 1.38.0; surfaced natural NEXT wires compute_ehp + compute_hybrid which this run ships). ROADMAP sweep CLEAN (Fleet status DS row already current at ENGINE 1.38.0 / 3534 tests / 103/89 unconditional + 18/18 cc_conditional - all current for post-item-142 state). Sweep cycle decay: wave 1=2 / 2=3 / 3=3 / 4=1 / 5=1 / 6=1 / 7=2 / **8=1**. Cost/latency CLEAN no-commit (**10th consecutive CLEAN since item 134**). All 7 levers surveyed (prompt-cache / TTL caches / polling cadences / log spam / model tier / scheduled tasks / bundle size); top non-suppressed `/api/bridge` at 0.20/sec well below 1/sec threshold; 26 panel CSS files = 26 dashboard.css `@import` lines (perfect parity); lever lane exhausted, future wins require product-fidelity tradeoffs. **Orchestrator commit `23195c3` bumps ENGINE_VERSION 1.38.0 -> 1.39.0 in `__init__.py`** with full changelog block covering both Slice A wire + Slice B wire + Slice C expansion + syncs **30 stale ENGINE pin sites across 29 DS test files** via assertEqual bulk-rewrite + ROADMAP Fleet status DS row appended slices. 0 merge conflicts (3 entirely disjoint surfaces + Slice A/B/C forward-marker scan auto-merged cleanly). **Merge order:** D first (docs `ffa1542` BACKLOG.md only), C second (`1f0552e`, cc_conditional.py + new wave 2 test + 2-line wave1 test count-pin relaxation + 1-line forward-marker `_ALLOWED_TEST_FILES` addition), A third (`02ad602`, ehp.py + new consumer_ehp test + forward-marker docstring renames + 1 allowlist add auto-merged with C), B fourth (`5f462f7`, hybrid.py + new consumer_hybrid test - fully disjoint). **Slice A forward-marker docstring trap caught + fixed in orchestrator commit:** my initial ENGINE_VERSION changelog block had "does NOT import cc_conditional" wording which matched the forward-marker scan's `import cc_conditional` substring pattern; reworded to "has no direct dependency on the conditional CC registry" (same pattern Slice B used pre-merge per item 141 lesson). **Verified:** RC **3108 passed** (phase8_smoke deferred until DS restart, flipped pass post-restart via `tests/phase8_smoke/test_sr_draft_profile_engine.py::TestLiveEngineIntegration::test_live_three_profiles` post-DS-restart); DS suite **3623 passed / 1 skipped / 1 xfailed / 1666 subtests** (+89 over s142 baseline 3534 = 16 Slice A + 18 Slice B + 55 Slice C). py_compile + ruff + ASCII clean across all 31 touched files. DS :8893 restarted via `taskkill /F /PID 5656` + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.39.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged pid 13420 (no route module edits this run; all 3 slices were DS engine + docs only). 4 worktrees unlocked + force-removed + 4 worktree branches deleted (local). DS audit NO-CHANGE at engine 1.39.0 (no new Riot patch dropped; live patch still 16.10.1; audit-streak halt from item 136 inherits forward). Cost/latency lever sweep CLEAN per Slice D (10th consecutive run since item 134). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** cc_conditional now has 3 authorized consumers in the engine math chain - cc_pressure.py (FIRST, ENGINE 1.38.0, DIRECT import via `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}`) + ehp.py (SECOND, ENGINE 1.39.0, INDIRECT kwarg-only through cc_pressure) + hybrid.py (THIRD, ENGINE 1.39.0, INDIRECT kwarg-only through ehp -> cc_pressure). Forward-marker `_ALLOWED_SOURCE_FILES` stays unchanged because Slice A + B are kwarg-only INDIRECT wires (they thread the kwarg through, do not import the registry directly). Any FUTURE consumer that adds `from .cc_conditional import` directly MUST add itself to `_ALLOWED_SOURCE_FILES` in the SAME commit + explain the operator gate in CLAUDE.md ledger. Default `include_conditional=False` MUST stay byte-identical for the 4 cc_blended_ehp consumers (compute_ehp / coach prompt / DS scorer / dashboard UI) + 4 hybrid consumers; pinned by IdentityContractTests + DefaultBehaviorTests on both slices. Slice B's `**_ehp_kwargs` gating pattern (pass kwarg ONLY when True) is the durable pattern for kwarg-threading across multiple internal call sites without breaking parallel-merge order. cc_conditional registry now 23 entries / 23 champs of conditional first-order CC at 16.10.1 (across the 10 existing condition tags). Avoid the forward-marker substring trap when writing changelog blocks that mention the module - use "no direct dependency on the conditional CC registry" not "does NOT import cc_conditional" (latter false-positives via `import cc_conditional` substring pattern; same lesson as item 141 post-merge fix; item 143 caught it again at orchestrator-commit time). Orchestrator-merge pattern now 10 consecutive runs (items 134-143) - this is the durable template for parallel headless-upgrade-style drains (3-4 worktree agents on disjoint slices + Slice D optional read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + CI verify + docs sync). **Carries forward:** (a) cc_conditional ecosystem CONSUMER WIRE COMPLETE in the engine math layer (3 of 3 natural consumers: cc_pressure direct + compute_ehp indirect + compute_hybrid indirect). Future consumer surfaces would be COACH PROMPT (analog of cc_blended_ehp_impact_line from item 138) + DASHBOARD UI (analog of routes_cc_blended_ehp_threat from item 140) - both operator-gated whether to ship before or after live-game validation. (b) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 23 registered conditional CC champions to verify the include_conditional=True opt-in surfaces correctly in live coach output + DS picks. (c) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (d) DD Defy heal-on-takedown STILL deferred (item 130+ carry; takedown-rate uncertainty). (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` + 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints + 23 per-entry probability values calibrations operator-gated. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) UI/UX live-game audit ritual owed once operator plays a real game. (i) `_PER_SPELL_CC_DURATIONS` unconditional registry still at 103/89 from item 141 (no wave 8 this run; unconditional first-order CC at 16.10.1 approaching saturation). (j) Future wave 3 cc_conditional expansion (more entries from REJECT lists when operator surfaces appetite; the 8 wave-1 REJECTS from item 142 + 8 wave-2 REJECTS from this run carry forward operator-gated). (k) Briar frenzy + Sylas range REJECTs still need new condition tag constants (separate schema lift, operator-gated). Frozen-file grant NOT used this run - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

142. ✅ **2026-05-22 - item 142 4-slice parallel drain (cc_conditional FIRST consumer wire + cc_conditional wave 1 expansion + ROADMAP/BACKLOG stale-sweep wave 7 + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `cf5f509`, pushed; ENGINE 1.37.0 -> 1.38.0; non-frozen; DS :8893 restarted serves 1.38.0; RC :8888 unchanged pid 13420 - all 3 slices touched DS engine + docs only, no route module edits).** Operator `/headless-upgrade` 6th long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 4 worktree agents dispatched concurrent; Slice D returned CLEAN no-commit (9th consecutive CLEAN cost/latency sweep per items 134-141). 3 commit-bearing slices merged + green + pushed. RC suite **3182 / 67 subtests / 0 failed** (-1 phase8_smoke `test_live_three_profiles` mid-run before DS restart; flipped pass post-DS-restart same pattern items 134-141 so post-restart count = 3183); DS suite **3461 -> 3534 / 1 skipped / 1 xfailed / 1666 subtests passed** (+73: 25 Slice A + 48 Slice B). **Slice A `8335ce1` (merge `098c4d8`) feat(ds) cc_conditional FIRST consumer wire via `compute_cc_pressure(include_conditional=False)` kwarg (closes item 141 carry (h)):** The conditional CC schema lift shipped at 1.37.0 as a FORWARD-MARKER seam with NO consumer wires; this slice wires the FIRST authorized consumer. `agents/daemon_slayer/cc_pressure.py` gains: NEW import `from .cc_conditional import get_total_conditional_cc_seconds, get_conditional_entries, ConditionalCcEntry`; `CcPressureResult` gains 2 NEW fields `conditional_cc_seconds: float = 0.0` + `conditional_entries: tuple[ConditionalCcEntry, ...] = ()` (default_factory tuple so frozen dataclass + back-compat); `compute_cc_pressure(...)` gains NEW kwarg `include_conditional: bool = False`. Default False (the new contract) preserves BYTE-IDENTICAL behavior for the 4 existing consumers of compute_cc_pressure (compute_ehp / enemy_cc_threat_line / compute_hybrid / routes_cc_blended_ehp_threat) - verified by re-running all 5 consumer test suites at 170/170 passing post-wire. When True: reads `get_conditional_entries(champion)` -> tuple of ConditionalCcEntry; computes `conditional_cc_seconds = get_total_conditional_cc_seconds(champion, apply_probability=True)` (probability-weighted at the 10 operator-tunable midpoints); ALSO applies ARAM tenacity to the conditional total via `effective_cc_duration(conditional_total, tenacity_mult)` so the math seam stays unified with the unconditional path; the `total_cc_seconds` field IS UPDATED to include the post-tenacity conditional total (combined number for callers that opt-in); the `conditional_cc_seconds` field carries the SEPARATE conditional contribution post-tenacity for transparency. The forward-marker scan in `test_cc_conditional_forward_marker.py` was updated SAME-COMMIT: NEW `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}` allowlist; `test_cc_pressure_does_not_import` renamed to `test_cc_pressure_is_authorized_first_consumer`; ehp/ability_dps/engine still pinned to no-import via `_assert_no_import` helper (any future consumer MUST update the allowlist in the same commit + explain the operator gate in CLAUDE.md). +25 tests in NEW `test_cc_conditional_consumer_pressure.py` (~500 LOC across 8 classes: DefaultBehaviorTests 4 / IncludeConditionalTrueTests 6 / ConditionalOnlyChampionTests 3 / BothUnconditionalAndConditionalTests 3 / AramTenacityAppliesToConditionalTests 3 / EngineVersionCurrentTests 1 / ProbabilityWeightedDefaultTests 3 / AsciiHygieneTests 2). 2-line edit to `test_cc_pressure.py` (extended field-names pin + renamed ENGINE pin test). Live smoke verified: Annie SR default = 1.5s (unchanged); Brand SR include_conditional=True = 1.4s (Brand R 2.0s * nth_hit 0.7); Mordekaiser = 0.25s unconditional + 7.0s conditional (banishment 7.0 * mode_gated 1.0) = 7.25s; ARAM tenacity 1.20x on conditional verified via monkeypatch (Brand R = 2.0 * 1.20 * 0.7 = 1.68s). **Slice B `d4253dd` (merge `03003ed`) feat(ds) cc_conditional wave 1 expansion (10/10 -> 18/18; +8 entries / +8 new champs):** Extends the seed shipped at 1.37.0 with 8 additional canonical conditional CC entries across 8 new champions, all sourced from the wave 4/5/6/7 REJECT lists in CLAUDE.md items 138/139/140/141. New entries: **Bard Q Cosmic Binding** (terrain 0.3 - wall-bounce double-stun 1.5/1.75/2.0/2.25/2.5s across 5 ranks); **Karma W Focused Resolve** (channel_completion 0.4 - full-tether root 1.5/1.625/1.75/1.875/2.0s); **Taliyah W Seismic Shove** (channel_completion 0.5 - recast knockup 0.75s); **Kennen E Lightning Rush** (nth_hit 0.6 - Mark of the Storm 3-stack stun 1.25s); **KSante Q Ntofo Strikes** (nth_hit 0.7 - 3rd-cast root 0.75s; coexists with KSante R wave 5 via setdefault); **Ornn Q Volcanic Rupture** (debuffed_target 0.5 - post-Brittle knockup 1.5s; coexists with Ornn R wave 7); **Xayah E Bladecaller** (nth_hit 0.6 - 3+ feathers root 1.25s); **Fiora W Riposte** (debuffed_target 0.4 - parry-stun 1.5s). No new condition tag constants (uses only the 10 existing). New counts: REGISTRY_TOTAL_CHAMPIONS=18 / REGISTRY_TOTAL_ENTRIES=18. Module docstring + builder fn updated to reflect new totals. +48 tests in NEW `test_cc_conditional_wave1.py` (~599 LOC across 7 classes: WaveOneNewEntryShapeTests / WaveOneNewChampionsTests / WaveOneValuePinsTests / RegistryGrowthTests / ExistingSeedPreservedTests / WaveOneCoexistenceWithUnconditionalTests / AsciiHygieneTests). `test_cc_conditional.py` 3 count-pin assertions relaxed `assertEqual -> assertGreaterEqual/issubset` for future-wave compatibility (mirrors wave 6 pattern from item 140). REJECTED candidates with reason recorded in commit body: **Aurora R Between Worlds** (mechanic-uncertain displacement; needs Meraki re-verify), **Briar Q+R** (frenzy-state-gated; would need a new condition tag), **Sylas E2 Kingslayer** (range-conditional; needs new tag like COND_RANGE_GATED), **Renata R Hostile Takeover** (no first-order CC; berserk axis is buff-redirect not CC), **Aphelios Q Onslaught Calibrum** (no first-order CC; mark-detonation slow + reveal), **Lissandra E** (direct skillshot, not mark-conditional as the docstring-confusion suggested), **Volibear R Stormbringer** (turret-only disable), **Swain E Nevermove** (stretchy fit for existing tags; deferred). 8 REJECTs carry forward operator-gated. Consumer math BYTE-IDENTICAL to 1.37.0 (data-lane only; the 8 new entries become available via include_conditional=True opt-in through Slice A but no auto-consumer reads them). **Slice C `2fa01d3` docs(roadmap-backlog) stale-entry sweep wave 7 per [[feedback_backlog_path_stale_check]]:** 2 entries flipped via grep-verification methodology: (1) ROADMAP.md L215 Fleet status DS row bumped ENGINE 1.36.0 -> 1.37.0 + tests 3365 -> 3461 + `_PER_SPELL_CC_DURATIONS` wave 6 95/82 -> wave 7 103/89 (60% of SR roster) + NEW cc_conditional.py forward-marker annotation (item 141 Slice B `7233568`, 10 condition tags + 10-entry seed); (2) BACKLOG.md L13 enemy-CC consumers entry "95 entries / 82 champs at ENGINE 1.36.0" -> "103 entries / 89 champs at ENGINE 1.37.0" + item 141 added to chain + "82 registered CC champs" -> "89" + appended adjacent SHIPPED note for `cc_conditional.py` forward-marker module pattern. Sweep cycle decay: wave 1=2 flips, wave 2=3, wave 3=3, wave 4=1, wave 5=1, wave 6=1, **wave 7=2 (slight tick up)**. All other open BACKLOG / ROADMAP entries verified legitimately operator-gated / live-gated (L74 #89 live verify / L75 build-ORDER / L103 Live ARAM Mayhem test / L121 Phase 3 fixtures / L122 Active Match step 5 / L125 ADR-007 phase 3 / L132 set_augment_intent Cherry / L174 auto-ops verb expansion / L175 Game-PC/Peer auto-action lanes / L182 101.qq.com one-off capture; BACKLOG L14 obj_participation widening / L16 SR records / L17 LCU deeper / L18 Item Shaper). **Slice D CLEAN no-commit (9th consecutive cost/latency CLEAN since item 134):** Read-only investigative agent surveyed 7 levers (prompt-cache hit rate / route TTL caches / polling cadences / log spam suppression / model tier distribution / scheduled task overhead / bundle size) and returned CLEAN. Prompt-cache: all 8 messages.create() coach callers carry explicit-block + cache_control. Route TTL: all expensive routes have caches (draft_elo 300s / personal_context 60s / ward_heat 2s / ban_suggest / cc_blended_ehp_threat / post_game_rubric / post_game_wpa / replay_events / personal_vs 60-300s / spike_curve dual-tier); uncached routes are POST/action or file-reads. Polling: fastest sub-1s pollers are UI-local CSS class flips (applyStaleness 500ms, map cooldown ticks 1000ms), NOT network traffic. Log spam: top non-suppressed `/api/bridge` 0.033/sec + `/api/home/summary` 0.008/sec - both well under 1/sec threshold (8-path suppression set already covers all >1/sec). Model tier: all live-tick coaches Haiku per `reference_model_config.md`; Sonnet only for vision OCR + Phase 3 advisors; Opus only for agent6 auditor. Scheduled tasks: 14 RC-* tasks; weekly sparse-data (Postmortem/Rewind) + daily DDragon + continuous bridge/supervisor; no overpolling. Bundle: 26 panel CSS files = 26 @import statements in dashboard.css (perfect parity); ADR-008 asset-hash auto-bumps. Verdict CLEAN at 9th run. **Orchestrator commit `cf5f509` bumps ENGINE_VERSION 1.37.0 -> 1.38.0 in `__init__.py`** with full changelog block covering both Slice A wire + Slice B expansion + syncs **27 stale ENGINE pin sites across 27 DS test files** (test_conditional_block_index_s228/229/230/231/232_saturation + test_effects_expansion + test_ehp + test_ehp_cc_blended + test_ehp_heal_phase6 + test_ehp_shield_phase15 + test_form_index_sweep_s226 + test_hybrid_enemy_champions + test_max_priority_sweep_s227 + test_nested_hp_parser_s223 + test_per_spell_cc_phase65 + test_per_spell_cc_registry_seed + test_per_spell_cc_registry_wave2/3/4/5/6/7 + test_post_parser_sweep_s225 + test_sum_of_blocks_expansion_s215/217 + test_unit_variants_s224). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local + remote). **Merge conflict resolved:** Slice A + Slice B both edited `test_cc_conditional_forward_marker.py` (A added `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}`; B added `test_cc_conditional_wave1.py` to `_ALLOWED_TEST_FILES`). Resolution kept both additions verbatim - the test file now has BOTH allowlists working together. **Merge order:** C first (docs `2fa01d3` BACKLOG.md + ROADMAP.md only), B second (`03003ed`, cc_conditional.py + 2 new test files + test_cc_conditional.py count pin relaxation), A third (`098c4d8`, cc_pressure.py + new test_cc_conditional_consumer_pressure.py + test_cc_pressure.py 2-line edit + test_cc_conditional_forward_marker.py allowlist + 1 conflict resolved by orchestrator). **Verified:** Wider RC **3182 / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` mid-run pre-DS-restart; flipped pass post-DS-restart -> 3183 same pattern items 134-141). DS suite **3534 passed / 1 skipped / 1 xfailed / 1666 subtests** (+73 over 3461 baseline = 25 Slice A + 48 Slice B). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via Stop-Process PID 11108 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.38.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; the 3 slices were DS engine + docs only). CI green push `cf5f509` (run id 26305752650 in flight at commit time; expected green on 4 commit chain). DS audit iter sweep: NO-CHANGE at engine 1.38.0 - no new Riot patch dropped (live patch still 16.10.1; DDragon latest 16.10.1 = RC current.txt); engine remains Meraki-coherent; audit-streak halt from item 136 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN per Slice D (9th consecutive run since item 134). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** cc_conditional.py is NO LONGER a strict forward-marker - `compute_cc_pressure` is the FIRST authorized consumer at ENGINE 1.38.0. Future consumers MUST update `_ALLOWED_SOURCE_FILES` in `test_cc_conditional_forward_marker.py` in the same commit that wires them + explain the operator gate in CLAUDE.md ledger. The default `include_conditional=False` contract on `compute_cc_pressure` MUST stay byte-identical for the 4 existing consumers; do NOT regress to default-True (would silently change all downstream cc_blended_ehp computations). The conditional contribution applies ARAM tenacity through the SAME `effective_cc_duration(base, mult)` seam as the unconditional path - do NOT bypass. `CcPressureResult.conditional_cc_seconds` is the SEPARATE post-tenacity contribution field (transparency); `total_cc_seconds` is the COMBINED (unconditional + conditional) post-tenacity total when include_conditional=True. The 18 conditional entries are operator-tunable via the per-entry `probability` overrides (parallel to the 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints); do NOT recalibrate without operator approval. The 8 wave-1-REJECT candidates (Aurora R / Briar Q+R / Sylas E2 / Renata R / Aphelios Q / Lissandra E / Volibear R / Swain E) carry forward operator-gated for either future condition-tag schema extension (Briar frenzy-state, Sylas range-gated) or just-not-CC verdicts. The forward-marker scan now uses BOTH `_ALLOWED_TEST_FILES` AND `_ALLOWED_SOURCE_FILES` allowlists; the 4-import-signature pattern matching is unchanged (substring matching would false-positive on documentation mentions per item 141 precedent). The orchestrator-merge pattern (3 worktree agents on disjoint slices + Slice D read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + CI verify + docs sync) is now the 9th consecutive headless-upgrade run using it (items 134-142). **Carries forward:** (a) cc_blended_ehp ecosystem still COMPLETE with 4 consumers per item 140; live-game validation pending. (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 89 registered CC champs to verify the cc_pressure consumer wire surfaces correctly when include_conditional=True is opted in by future consumers. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) Calibration midpoints operator-gated: `_MISSING_HP_SHARE_FOR_HEALS=0.5` + `_CC_EFFECTIVENESS_FACTOR=0.5` + 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints + 18 per-entry `probability` values on ConditionalCcEntry. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Future cc_conditional consumers: `compute_ehp(enemy_champions=..., include_conditional=False)` + `compute_hybrid(enemy_champions=..., include_conditional=False)` are the natural NEXT consumer wires (both currently propagate the include_conditional default through to compute_cc_pressure - so opt-in by passing kwarg; the consumer wire is in place via cc_pressure). (i) UI/UX live-game audit ritual owed once operator plays a real game. (j) Multi-wave registry-merge schema lift via `_build_per_spell_cc_durations()` proven at 4 champions (Lulu / Sejuani / Thresh / Zac). (k) cc_conditional registry now 18 entries / 18 champions; future wave 2 expansion adds more from the carrying REJECT lists when operator surfaces appetite. (l) 15+ wave-7 REJECTs (Darius E / Yorick R / etc.) PLUS 8 wave-1 cc_conditional REJECTs carry forward operator-gated. (m) `_PER_SPELL_CC_DURATIONS` unconditional registry still at 103 / 89 from item 141 (no wave 8 this run; unconditional first-order CC at 16.10.1 is approaching saturation - most remaining champions either have no first-order CC OR carry conditional-only CC). Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

141. ✅ **2026-05-22 - item 141 4-slice parallel drain (_PER_SPELL_CC_DURATIONS wave 7 + cc_conditional forward-marker module + ROADMAP stale-sweep wave 6 + cost/latency CLEAN) SHIPPED (3 worktree agents merged + orchestrator ENGINE bump commit `d7f6a49`, pushed; ENGINE 1.36.0 -> 1.37.0; non-frozen; DS :8893 restarted serves 1.37.0; RC :8888 unchanged pid 13420 - all 3 slices touched DS engine + docs only, no route module edits).** Operator `/headless-upgrade` 5th long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 4 worktree agents dispatched concurrent; Slice D returned CLEAN no-commit (8th consecutive CLEAN cost/latency sweep per items 134-140). 3 commit-bearing slices merged + green + pushed. RC suite **3183 / 67 subtests / 0 failed** (unchanged - this slice is DS engine + docs only; phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart same pattern items 134-140); DS suite **3365 -> 3461 / 1 skipped / 1 xfailed / 1666 subtests passed** (+96: 37 Slice A + 59 Slice B). **Slice A `ae0b59e` (merge `2840cc8`) feat(ds) _PER_SPELL_CC_DURATIONS wave 7 (+8 entries / +7 new champs + 1 multi-wave coexistence; 95/82 -> 103/89):** Extends the wave 6 schema-lifted seed via the `_build_per_spell_cc_durations()` builder using `registry.setdefault(champ, {})[spell] = tuple` semantics. New champions seeded: **Irelia E Flawless Duet** stun 0.75/0.85/0.95/1.05/1.15s across 5 ranks (verified Meraki 16.10.1), **Kalista R Fate's Call** knockup 1.0s all 3 ranks, **Ornn R Call of the Forge God** knockup 0.5s all 3 ranks (first-cast knock-up; second-cast knock-up at 1.0s intentionally NOT encoded - registry models the primary first-order CC), **Shyvana R Dragon's Descent** knockback 1.0s all 3 ranks, **Smolder R Mountain Breaker** knockup 1.25s all 3 ranks (slow component intentionally NOT encoded - first-order knockup only per selection rules), **Vayne E Condemn** knockback 0.5s all 5 ranks (the universal knockback; stun-on-wall conditional intentionally SKIPPED per selection rule), **Volibear E Sky Splitter** airborne 0.25s all 5 ranks (introduces airborne CC kind as a new tag), **Zac R Let's Bounce** knockup 1.0s all 3 ranks (multi-wave coexistence: Zac E Elastic Slingshot was seeded wave 2 with knock-aside; now coexists on the same champion's spell map). Selection rules unchanged from waves 1-6: first-order CC only; no slows; no conditional CC; canonical DDragon ids. **15+ REJECT candidates documented in commit body** so future audits do NOT re-research: Darius E (pull+slow only), Yorick R Mist Walkers (no CC), AurelionSol Q (no CC), Aurora W/E/R (no first-order), Ambessa all spells (no first-order CC), Pyke E (damage only), Fiora W Riposte (parry-conditional - parry-stun unblocked is conditional axis), Vex E (mark-conditional - the looming darkness fear fires only on debuffed targets), Ornn Q Volcanic Rupture (Brittle-conditional - knockup only after Brittle stack from auto-attack or W), Renata R (berserk axis = different schema lift; not first-order CC), Volibear Q (terrain-conditional - knock-aside only on terrain hit; conditional axis), TahmKench R (ally swallow = no CC on enemies; the conditional R-on-enemy is in cc_conditional), Sett W (no first-order CC; direct damage only), Sett E (dual-enemy-conditional - stun only when 2+ enemies snapped together; conditional axis). Consumer math BYTE-IDENTICAL to 1.36.0 for the 95 prior entries (data lane only); the 8 new entries surface in `compute_cc_pressure` for their champions (intended). +37 tests in NEW `test_per_spell_cc_registry_wave7.py` (~508 LOC across 7 classes: WaveSevenNewEntryShapeTests 9 / WaveSevenValuePinsTests 9 / WaveOneThroughSixPreservedTests 10 / RegistryGrowthTests 4 / SchemaLiftBuilderPreservedTests 1 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). `test_per_spell_cc_registry_seed.py` 2-line count pin update (82/95 -> 89/103); `test_per_spell_cc_registry_wave6.py` 2-line assertEqual -> assertGreaterEqual relaxation on count pins for future-wave compatibility. Registry total: 103 entries across 89 champions (60% of 172 SR roster). **Slice B `7233568` (merge `00d6271`) feat(ds) conditional CC axis schema lift via NEW `agents/daemon_slayer/cc_conditional.py` (forward-marker module; ENGINE 1.36.0 -> 1.37.0 via orchestrator):** Closes item 140 carry (h) "Conditional CC axis schema lift operator-gated". Headless-upgrade grant covers the gate. **NEW `agents/daemon_slayer/cc_conditional.py` (~360 LOC)** ships the schema + machinery + 10-entry seed but NO consumer wires. Forward-marker pattern mirrors s112 `STAT_GRANT_CALC_KEYS` empty seam shipped at ENGINE 1.22.0 + `_PER_SPELL_CC_DURATIONS` pre-130 empty seam. NEW `ConditionalCcEntry` frozen dataclass: `champion: str` / `spell: str` Q-W-E-R / `cc_kind: str` stun-root-fear-suppression-charm-sleep-knockup-knockback-banishment / `durations_s: tuple[float, ...]` per-rank or single-value-all-ranks / `condition: str` (one of COND_* constants) / `probability: float` 0.0-1.0 default midpoint from `_DEFAULT_CONDITION_PROBABILITY` / `notes: str`. `__post_init__` validates spell in {Q,W,E,R} + probability in [0,1] + durations non-empty + non-negative + tuple length matches spell rank (3 for R, 5 for Q/W/E, OR 1 for same-value-all-ranks shortcut) + condition exists in default-prob table. NEW condition tags enum (string consts): `COND_NTH_HIT` (Brand R 3rd-stack, Viktor W 3rd-charge), `COND_GOLD_CARD` (TF W), `COND_TERRAIN` (Jarvan IV EQ knockup, Volibear Q terrain knockback), `COND_CHANNEL_COMPLETION` (Warwick R full channel, Karma W tether), `COND_DREAM_STACK` (Lillia R reserved - prior unconditional wave 6 entry coexists), `COND_DEVOUR_TARGET` (Tahm Kench R), `COND_TARGET_HP_BELOW` (execute-like, reserved no seed), `COND_TARGET_DEBUFFED` (Vex E mark, Lissandra E mark), `COND_DUAL_ENEMY` (Sett E, Zac Q reserved), `COND_MODE_GATED` (Mordekaiser R always-on-cast). NEW `_DEFAULT_CONDITION_PROBABILITY: Dict[str, float]` map of 10 operator-tunable midpoints: nth_hit=0.7 (3rd-hit achievable in 6s fight), gold_card=0.4 (TF cycle positioning-dependent), terrain=0.3 (specific positioning required), channel_completion=0.5 (situational interruptibility), dream_stack=0.3 (slow stack accumulation), devour=0.4 (setup-dependent), low_hp_target=0.5 (execute-fight progression), debuffed_target=0.5 (mark-based), dual_enemy=0.6 (team-fight common), mode_gated=1.0 (always-on in mode). NEW `_build_per_spell_cc_conditional() -> dict[str, dict[str, ConditionalCcEntry]]` setdefault builder + NEW `get_conditional_entries(champion) -> tuple[ConditionalCcEntry, ...]` Q-W-E-R-ordered lookup + NEW `get_total_conditional_cc_seconds(champion, *, apply_probability=True, rank_index=-1) -> float` probability-weighted aggregator (max rank default; rank_index 0..n-1 selects specific rank). Public introspection: `REGISTRY_TOTAL_CHAMPIONS=10` / `REGISTRY_TOTAL_ENTRIES=10`. **Seed of 10 canonical conditional entries** from wave 4/5/6 REJECT lists: Brand R Pyroclasm (stun 1.5/2.0/2.5s nth_hit 0.7 - 3rd Blaze stack stuns target), TwistedFate W Pick a Card (stun 1.5s gold_card 0.4 - Gold Card cycle), JarvanIV E Demacian Standard (knockup 1.0s terrain 0.5 - EQ combo terrain hit), TahmKench R Devour (suppression 1.0s devour 0.4 - Q-stack-then-R), Volibear Q Thundering Smash (knockback 0.75s terrain 0.3 - hit-while-colliding terrain), Warwick R Infinite Duress (suppression 1.5/1.75/2.0s channel 0.5 - full-channel duration), Viktor W Gravity Field (stun 1.5s nth_hit 0.6 - 3+ ticks in field), Mordekaiser R Realm of Death (banishment 7.0s mode_gated 1.0 - always-on if R lands), Sett E Facebreaker (stun 1.0s dual_enemy 0.6 - 2+ enemies snapped), Vex E Looming Darkness (fear 1.0/1.125/1.25/1.375/1.5s debuffed_target 0.5 - Vex E mark required). Lillia R intentionally NOT in conditional registry (already wave 6 unconditional prob=1.0; conditional dream-stack variant would clobber on same spell-slot). Mordekaiser E unconditional (wave 5) + Mordekaiser R conditional (NEW) coexist on distinct spell slots, no clobber. **Forward-marker boundary pinned** by NEW `test_cc_conditional_forward_marker.py` (~194 LOC across 2 classes): NoConsumerWireTests (5 tests verify NO file in `agents/daemon_slayer/` outside the new test files imports the conditional CC module via actual import statements - the scan uses 4 import-signature patterns `from agents.daemon_slayer.cc_conditional import` / `from .cc_conditional import` / `import agents.daemon_slayer.cc_conditional` / `import cc_conditional`; cc_pressure.py / ehp.py / ability_dps.py / engine.py all explicitly pinned via `_assert_no_import` helper) + DocstringStatesForwardMarkerTests (3 tests verify module docstring declares FORWARD-MARKER + no-consumer-wires-yet + references prior STAT_GRANT_CALC_KEYS empty seam). +47 tests in NEW `test_cc_conditional.py` (~530 LOC across 7 classes: ConditionalCcEntrySchemaTests / RegistrySeedTests / GetConditionalEntriesTests / GetTotalConditionalCcSecondsTests / RegistryGrowthTests / DefaultProbabilityMapCoverageTests / AsciiHygieneTests). The forward-marker scan was initially substring-based ("cc_conditional" anywhere in text) but flipped to actual import-statement pattern matching post-merge so the __init__.py changelog block can name the module without triggering the forward-marker guard (the test's intent is to catch IMPORT wires not documentation mentions). **Slice C `0d0c499` (merge `8fba572`) docs(roadmap) stale-entry sweep wave 6 per [[feedback_backlog_path_stale_check]]:** 1 entry flipped via grep-verification methodology: ROADMAP.md L166 DS calibration pipeline entry was claiming "newest match is 2025-12-16, 5 games since Dec" (stale); live `data/rewind_history.db` shows 2900 matches latest 2026-05-14 via RC-RewindCatchup weekly cron (item 101); 1364 matches Dec-May 2026. Ranked Solo queue 420 specifically still has latest 2025-12-15 + 0 SR records in `data/ds_calibration.jsonl` filter `mode_key='sr'` per item 125 probe; reframed to accurate "Blocked on SR ranked freshness" with live numbers preserved. BACKLOG sweep CLEAN (zero flips; all 6 verifiable entries L13-L18 match live source - L13 cc_blended_ehp 10 consumers verified all 10 keys present, L14 obj_participation widening operator-gated unchanged, L15 Dead Man's Plate stack_ramp_seconds + 223742 + Shipwrecker confirmed in source, L16 0 SR records still pending, L17 LCU integration reference catalog, L18 Interactive Item Shaper `core/shaper.py` shipped 2026-05-19 UI blocked on DS 100%). The [[feedback_backlog_path_stale_check]] sweep cycle continues to decay (wave 1=2 flips, wave 2=3 flips, wave 3=3 flips, wave 4=1 flip, wave 5=1 flip, wave 6=1 flip - methodology stable across 6 sweep rounds). **Slice D CLEAN no-commit (8th consecutive cost/latency CLEAN since item 134):** Read-only investigative agent surveyed 7 levers (prompt-cache hit rate / route TTL caches / polling cadences / log spam suppression / model tier distribution / scheduled task overhead / bundle size) and returned CLEAN with rationale per lever: all 8 coach modules per items 119-121 have explicit-block system + cache_control ephemeral; 15/35 routes have explicit caching, the uncached routes are FROZEN OR POST/action endpoints OR file-reads OR use internal backing-module caching; 1.5s coach_decisions + 2s `/api/state` polls calibrated per item 120; 8-endpoint suppression set from item 121 covers all >1/sec pollers (new highest-frequency endpoint /api/bridge runs ~0.06 lines/sec well below threshold); model tier calibrated per `reference_model_config.md` (Haiku coaches + dynamic prompts; Sonnet only for quality-critical vision OCR + Phase 3 advisors; Opus only for auditor agent6); all scheduled task cadences sane per item 110 (weekly Sun for sparse-data Postmortem/Rewind, daily 03:30 for DDragon matches Riot patch cadence); item 140's new panel cc_blended_ehp_threat.{js,css} ~340 LOC properly imported via dashboard.css:37, ADR-008 asset-hash auto-bumps cover. **Orchestrator commit `d7f6a49` bumps ENGINE_VERSION 1.36.0 -> 1.37.0 in `__init__.py`** with full changelog block covering both Slice A wave 7 + Slice B cc_conditional + syncs **47 stale ENGINE pin sites across 27 DS test files** (test_effects_expansion 2 hits + test_per_spell_cc_registry_seed 4 hits + test_per_spell_cc_registry_wave3/4 3 each + wave5 5 + wave6 7 + wave7 3 + all 14 other files 1 each: cc_pressure / s228 / s229 / s230 / s231 / s232_saturation / ehp / ehp_cc_blended / ehp_heal_phase6 / ehp_shield_phase15 / form_index_sweep_s226 / hybrid_enemy_champions / max_priority_sweep_s227 / nested_hp_parser_s223 / per_spell_cc_phase65 / per_spell_cc_registry_wave2 / post_parser_sweep_s225 / sum_of_blocks_expansion_s215 / sum_of_blocks_expansion_s217 / unit_variants_s224 + 2 post-sync fixes: test_per_spell_cc_registry_wave6.py start_marker reverted "ENGINE 1.37.0 wave 6" -> "ENGINE 1.36.0 wave 6" because the ability_dps.py source block header carries the wave-6 historical metadata at 1.36.0 + test_cc_conditional_forward_marker.py scan relaxed from substring to import-signature so __init__.py changelog can name the module without triggering the guard). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local + remote). 4th worktree (Slice D) auto-cleanup since no commit. **Merge order:** C first (docs `8fba572` ROADMAP.md only), A second (`2840cc8`, ability_dps.py + new wave 7 test + 2 wave6/seed test pin updates), B third (`00d6271`, 3 new files entirely under `agents/daemon_slayer/`). 0 merge conflicts (3 entirely disjoint surfaces: A=ability_dps.py + wave-7 test + wave-6 assertEqual relaxation + wave-1 seed count pin; B=NEW cc_conditional.py + 2 NEW test files; C=ROADMAP.md only). **Verified:** Wider RC **3183 passed / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart same pattern items 134-140). DS suite **3461 passed / 1 skipped / 1 xfailed / 1666 subtests** (+96 over 3365 baseline = 37 Slice A + 59 Slice B). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via Stop-Process PID 15808 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.37.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; the 3 slices were DS engine + docs only). CI green push `d7f6a49` in flight (run id 26303835667). DS audit iter sweep: NO-CHANGE at engine 1.37.0 - no new Riot patch dropped (live patch still 16.10.1; DDragon latest 16.10.1 = RC current.txt); engine remains Meraki-coherent; the audit-streak halt from item 136 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN per Slice D (8th consecutive run since item 134). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** `_PER_SPELL_CC_DURATIONS` registry now 103 entries / 89 champs of first-order CC at 16.10.1 (60% of 172 SR roster). Do NOT add slows. Do NOT add conditional CC (those go through `agents/daemon_slayer/cc_conditional.py` schema lift; the 15+ wave-7 REJECT candidates + 23 wave-6 + 19+ wave-5 + 10 wave-4 carry forward). The `Volibear E Sky Splitter` entry introduces an "airborne" CC kind as a new tag distinct from knockup/knockback (the in-game disable type is technically airborne for both, but the registry distinguishes by the displacement vector - vertical = knockup, horizontal = knockback, both = airborne when neither dominates). The `Ornn R` first-cast knockup is the primary first-order encoding; the second-cast knockup at 1.0s on the airborne ult is intentionally NOT a second registry entry per single-spell-entry convention (an additional consumer surface could enumerate both via a separate ult-shape axis). The `Vayne E Condemn` unconditional knockback 0.5s is the universal first-order CC; the stun-on-wall conditional 1.5s is in `cc_conditional` future seed (operator can populate when consumer wires). The `Smolder R` slow component is intentionally NOT encoded (first-order knockup only per selection rules). Multi-wave coexistence via setdefault is now PROVEN at 2 champions: Lulu (wave 1 W polymorph + wave 6 R knockup), Sejuani (wave 1 R + wave 6 Q), Thresh (wave 1 Q + wave 6 E), Zac (wave 2 E + wave 7 R). Future wave 8+ may extend to 3-spell coexistence on any champion (e.g. Galio Q+W+E+R if Q ever encodes - currently Galio W+E+R wave 1). The `cc_conditional.py` module ships at ENGINE 1.37.0 as a FORWARD-MARKER seam (mirrors s112 `STAT_GRANT_CALC_KEYS` + pre-130 `_PER_SPELL_CC_DURATIONS`); production behavior of every existing consumer is byte-identical to 1.36.0 because no file imports it via the 4 forbidden import-signature patterns. The `_DEFAULT_CONDITION_PROBABILITY` 10 midpoints are operator-tunable (parallel to `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` precedent); do NOT calibrate without operator approval. The `ConditionalCcEntry.durations_s` tuple supports BOTH per-rank shape (5 for Q/W/E, 3 for R) AND single-value-all-ranks shortcut; the `__post_init__` accepts both lengths per spell. The `get_total_conditional_cc_seconds(apply_probability=True)` default applies probability-weighted aggregation; future fight-sim consumer reads probability=False for worst-case-CC analysis. The forward-marker scan in `test_cc_conditional_forward_marker.py` uses actual import-signature patterns NOT substring matching - this is the durable test pattern (substring would false-positive on any documentation mention of the module name). The 2 post-pin-sync fixes (wave6 start_marker revert + forward-marker scan refinement) are the s246 facade-split-precedent pattern - over-aggressive pin sync caught + corrected in the same orchestrator commit. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order can be any permutation conflict-free (proven this run + items 134-140 = 8 consecutive runs using this template). The orchestrator-merge pattern (3 worktree agents on disjoint slices + Slice D read-only investigation + sequential merge + 1 ENGINE bump commit syncs pins + tests gate + DS restart + RC unchanged + CI verify + docs sync) is the durable template - this is now the 8th consecutive headless-upgrade run using it (items 134-141). **Carries forward:** (a) cc_blended_ehp ecosystem still COMPLETE with 4 consumers (engine math + coach prompt + DS scorer + dashboard UI) per item 140; live-game validation pending. (b) DD Defy heal-on-takedown STILL deferred (carries from item 130+; takedown-rate uncertainty). (c) Live ARAM/SR smoke STILL pending - operator must play a real game. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` + the 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints calibrations operator-gated. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) The conditional CC schema lift is now SHIPPED (forward-marker seam at ENGINE 1.37.0); future consumer wire is the next step - natural candidates are `compute_cc_pressure(include_conditional=False)` kwarg + `compute_ehp(enemy_champions=..., include_conditional=False)` kwarg + a future fight-sim that probability-weighted-sums conditional CC with the unconditional registry. (i) UI/UX live-game audit ritual owed once operator plays a real game. (j) Multi-wave registry-merge schema lift via `_build_per_spell_cc_durations()` is now proven at 4 champions (Lulu / Sejuani / Thresh / Zac); future wave 8+ extends without re-litigating. (k) cc_conditional.py 10-entry seed is the canonical example for the conditional CC axis; future seeds expand without re-litigating the schema. (l) The 15+ wave-7 REJECT candidates (Darius E / Yorick R / AurelionSol Q / Aurora W/E/R / Ambessa / Pyke E damage / Fiora W parry / Vex E mark / Ornn Q Brittle / Renata R berserk-axis / Volibear Q terrain / Sett W/E / TahmKench R ally swallow) carry forward operator-gated for either future cc_conditional seed expansion (for the conditional ones) or other audits (for the non-CC ones). Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

140. ✅ **2026-05-22 - item 140 3-slice parallel drain (cc_blended_ehp dashboard UI consumer + _PER_SPELL_CC_DURATIONS wave 6 + registry-merge schema lift + ROADMAP/BACKLOG stale-sweep) SHIPPED (3 worktree agents + 3 merge commits `94d1de3` `5a7d86d` `81e56b5`, pushed; ENGINE 1.35.0 -> 1.36.0; non-frozen; DS :8893 restarted serves 1.36.0; RC :8888 restarted clean pid 12864 -> 13420 last_reload_ok=True - new route module triggered restart).** Operator `/headless-upgrade` 4th long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 139 carries (a) + (j) - the 4th and FINAL consumer surface of cc_blended_ehp + the registry-merge schema lift unblocking case-1 wave-5 dict-collision REJECTs. All 3 merged + green + pushed. RC suite **3140 -> 3183 / 67 subtests / 0 failed** (+43 Slice A); DS suite **3328 -> 3365 / 1 skipped / 1 xfailed / 1666 subtests passed** (+37 Slice B). **Slice A `3b6cc4b` (merge `5a7d86d`) feat(coach) - cc_blended_ehp dashboard UI consumer (FIRST dashboard UI consumer of cc_blended_ehp; closes item 139 carry (a) - the 4th and FINAL consumer surface):** NEW `dashboard/routes_cc_blended_ehp_threat.py` (~310 LOC) GET `/api/cc-blended-ehp-threat?ally=<csv>&enemy=<csv>&mode=ARAM` returns `{ok, mode, ally_avg_cc_blended_ehp, enemy_avg_cc_blended_ehp, ratio, tier (good|warn|bad), ally_total_cc_seconds, enemy_total_cc_seconds, elapsed_ms, cached}`. **Symmetric construction (load-bearing semantic):** ally side feeds ENEMY roster as `enemy_champions` kwarg to `compute_ehp` so enemy CC erodes ally EHP; enemy side feeds ALLY roster vice versa. Tier bands: ratio >= 1.05 -> good (ally CC weathers better via shorter post-CC-erosion EHP loss), 0.95 <= ratio < 1.05 -> warn (balanced threat), < 0.95 -> bad (enemy CC erodes us more). 5-min TTL in-process cache keyed on `(sorted_ally_tuple, sorted_enemy_tuple, mode)` mirrors `dashboard/routes_post_game_rubric.py` pattern; mode-aware so ARAM and SR don't share cache slots. Fail-soft: 400 missing/blank ally OR enemy, 200 with `ok=false reason=no_champions` when both empty, 200 ratio=1.0 tier=warn when both sides total_cc_seconds=0.0 (no CC threat), 503 if compute_ehp import fails. Registered in `dashboard/_dispatch.py` GET_ROUTES. **Frontend NEW `web/js/panels/cc_blended_ehp_threat.js` (~195 LOC ESM):** `_csvRenderCcBlendedEhpThreat()` helper mirroring `_csvRenderBanSuggestToggle()` pattern; resolves numeric LCU champion ids via `CHAMPS.byId` to canonical DDragon slugs; section signature `ccbe:<ally_count>:<enemy_count>` coalesces re-renders. Mount via `#cs-cc-blended-ehp-threat` chip between `#cs-bans-grid` + `#cs-pickorder` in `web/index.html`. Tier-tinted via `data-cc-tier` attr. Renders 4 lines: header "CC threat balance: <tier label>" + "Ally cc_blended_ehp avg: <round 0> (enemy CC <total_cc_s>s)" + "Enemy cc_blended_ehp avg: <round 0> (ally CC <total_cc_s>s)" + "Ratio: <ally/enemy 2 decimal>x". NEW `web/css/panels/cc_blended_ehp_threat.css` (~145 LOC) with 3 `data-cc-tier` color variants (good=ok-tinted green, warn=accent/amber, bad=bad-tinted red); imported via `@import './panels/cc_blended_ehp_threat.css'` in `web/css/dashboard.css`. +43 tests in NEW `test_routes_cc_blended_ehp_threat.py` (21 across 5 classes: RouteContractTests 8 missing/blank/malformed/mode-default / MathTests 6 ratio-calc/tier-band/zero-both/CC-heavy / CacheTests 3 TTL/cold-miss/key-invariance / FailSoftTests 2 / AsciiHygieneTests 2) + NEW `test_cc_blended_ehp_threat_panel_dom.py` (22 across 4 classes: ChipMountTests 5 mount-ids/hidden-default/data-attrs / JsConsumptionTests 10 helper-fn/state-reads/CHAMPS-resolution / CssTierVariantsTests 4 / AsciiHygieneTests 3). Math verified live end-to-end post-restart: curl `/api/cc-blended-ehp-threat?ally=Annie,Galio,Leona&enemy=Aatrox,Garen,Sett&mode=ARAM` returned `ok=true mode=ARAM ally_avg_cc_blended_ehp=2335.21 enemy_avg_cc_blended_ehp=1294.92 ratio=1.80 tier=good ally_total_cc_seconds=0.0 enemy_total_cc_seconds=7.0 elapsed_ms=22 cached=false` (Annie R 1.5 + Galio WER 2.25 + Leona QER 3.25 = 7.0s CC on enemy side; Aatrox/Garen/Sett have no registry entries = 0s ally CC; ally CC erodes enemy EHP -> ratio 1.80x = good). **Slice B `cec70c2` (merge `81e56b5`) feat(ds) - _PER_SPELL_CC_DURATIONS schema lift + wave 6 (90/80 -> 95/82, ENGINE 1.35.0 -> 1.36.0; closes item 139 carry (j)):** Schema lift via NEW module-level `_build_per_spell_cc_durations() -> dict[str, dict[str, tuple[float, ...]]]` builder function replacing the prior single-dict-literal. The builder uses `registry.setdefault(champion, {})[spell] = tuple` semantics so multi-wave additions never clobber prior-wave entries. ALL 90 wave-1-through-5 entries preserved BYTE-IDENTICAL in value; only construction shape changes. Pinned by `WaveOneThroughFivePreservedTests` (10 representative entry pins across waves) + `SchemaLiftBuilderTests` (5 tests including a setdefault demo fixture). Wave 6 adds **+5 entries / +2 new champs** (95 entries / 82 champs total): **Lulu R Wild Growth** (knockup 1.0s all 3 ranks - previously rejected wave-5 dict collision against Lulu W polymorph), **Sejuani Q Arctic Assault** (knockup ranks 0.75/0.875/1.0/1.125/1.25 - previously rejected wave-5 dict collision against Sejuani R Glacial Prison), **Thresh E Flay** (knockback 0.4s all 5 ranks - previously rejected wave-5 dict collision against Thresh Q Death Sentence), **Bard R Tempered Fate** (stasis 2.5s all 3 ranks - NEW champ), **Lillia R Lilting Lullaby** (sleep 2.0s all 3 ranks - NEW champ). The 3 prior-rejected case-1 entries (Lulu R + Sejuani Q + Thresh E) are NOW unblocked by the schema lift - they coexist with their respective champion's prior-wave entry on the same spell map (Lulu = {W polymorph, R knockup}; Sejuani = {R prison, Q assault}; Thresh = {Q sentence, E flay}). **Rell R REJECTED** by the agent's verifier (drag mechanic vs discrete pull-stun semantic - 1.0s initial pull is damage tick + slow, NOT a discrete CC like Sion R / Skarner R). 23 REJECT candidates with reason recorded in commit body (carryover from wave 4+5 conditional CC + slows + dict-collisions for already-seeded entries): Bard Q / TF W / Lillia E / Tristana W / Akshan E / Karthus Q / Naafiri R / Yuumi Q / Brand Q / Tahm Q / Volibear Q / Mordekaiser R / Aurora R / Briar R / Sett W+E / Taliyah W / Trundle R / Kennen E / KSante Q / Vayne E / Sylas E2 / Xayah E / Aphelios Q / Aurora E / Briar Q / Renata R / Viktor W / Warwick R. Consumer math BYTE-IDENTICAL to 1.35.0 for the 90 prior entries (data lane only); the 5 new entries surface in `compute_cc_pressure` for their champions (intended). +37 tests in NEW `test_per_spell_cc_registry_wave6.py` (~522 LOC across 7 classes: WaveSixNewEntryShapeTests 9 / WaveSixValuePinsTests 6 / WaveOneThroughFivePreservedTests 10 / RegistryGrowthTests 4 / SchemaLiftBuilderTests 5 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). `test_per_spell_cc_registry_seed.py` 2-line count pin update (80/90 -> 82/95); `test_per_spell_cc_registry_wave5.py::test_lulu_w_polymorph_preserved` FLIPPED from asserting `Lulu R == ()` (deferred state) to `Lulu R == (1.0, 1.0, 1.0)` (now-shipped state). Wave 2/3/4 + phase 6.5 + cc_pressure + ehp_cc_blended + hybrid_enemy_champions + ehp tests all stay green. **+21 stale ENGINE pin syncs** across 20 DS test files (1.35.0 -> 1.36.0): test_effects_expansion (2 hits) + 19 others 1 hit each (cc_pressure / s228 / s229 / s230 / s231 / s232_saturation / ehp / ehp_cc_blended / ehp_heal_phase6 / ehp_shield_phase15 / form_index_sweep_s226 / hybrid_enemy_champions / max_priority_sweep_s227 / nested_hp_parser_s223 / per_spell_cc_phase65 / post_parser_sweep_s225 / sum_of_blocks_expansion_s215 / sum_of_blocks_expansion_s217 / unit_variants_s224). ENGINE bumped in `agents/daemon_slayer/__init__.py` with full changelog block covering both the schema lift + wave 6. **Slice C `765c2c6` (merge `94d1de3`) docs(backlog) - stale-entry sweep wave 5 per [[feedback_backlog_path_stale_check]]:** 1 entry flipped via grep-verification methodology: BACKLOG.md L13 enemy-CC consumers `8 -> 9 shipped` (Slice C agent counted item 139 Slice A `0523511` compute_hybrid scorer as the 9th overall ecosystem consumer + FIRST DS-engine SCORER consumer of cc_blended_ehp); orchestrator subsequent docs sync further bumps to **10 consumers** (item 140 Slice A dashboard UI consumer landed in same RUN). The dashboard UI consumer is the 4th cc_blended_ehp consumer surface specifically (engine math + coach prompt + DS scorer + dashboard UI = ecosystem COMPLETE). ROADMAP.md sweep-clean (Fleet status DS row updated in orchestrator docs sync to reflect ENGINE 1.36.0 + 3365 tests + wave 6 + dashboard UI consumer). The [[feedback_backlog_path_stale_check]] sweep cycle continues to decay (wave 1=2 flips, wave 2=3 flips, wave 3=3 flips, wave 4=1 flip, wave 5=1 flip - methodology stable across 5 sweep rounds). **Merge order:** C first (docs `94d1de3`), A second (`5a7d86d`, route + new panel + DOM tests + index.html + champ_select.js wire), B third (`81e56b5`, ability_dps.py schema lift + wave 6 + ENGINE bump + 21 pin syncs + 5 wave-test pin updates). 0 merge conflicts (3 entirely disjoint surfaces: A=dashboard/routes_cc_blended_ehp_threat.py + 3 new web/ files + champ_select.js wire + index.html + 2 new test files; B=ability_dps.py + __init__.py + 1 new test file + 5 wave test pin updates + 20 ENGINE pin syncs; C=BACKLOG.md only). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local + remote). **Verified:** Wider RC **3183 passed / 67 subtests / 0 failed** (+43 over s139 3140 baseline = exactly Slice A's threat-route + DOM tests; phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart same pattern items 134-139). DS suite **3365 passed / 1 skipped / 1 xfailed / 1666 subtests** (+37 over 3328 baseline = exactly Slice B's wave-6 tests). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via taskkill PID 4420 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.36.0 patch=16.10.1 champions=172 items=705`. RC :8888 restarted via `restart_trigger.txt` (new route module registration in `_dispatch.py`) -> pid 12864 -> 13420 alive=True last_reload_ok=True mode=client. Live curl `/api/cc-blended-ehp-threat` verified end-to-end ratio=1.80 tier=good (see Slice A above). CI green push `81e56b5` 1m22s. DS audit iter sweep: NO-CHANGE at engine 1.36.0 - no new Riot patch dropped (live patch still 16.10.1; DDragon latest 16.10.1 = RC current.txt); engine remains Meraki-coherent; the audit-streak halt from item 136 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN (engine math + data lane is product-fidelity preserving; new dashboard route uses 5-min TTL cache; no prompt-cache changes; no polling cadence shifts; no model-tier changes). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** the cc_blended_ehp ecosystem is now COMPLETE - 4 consumers shipped (engine math via compute_ehp + coach prompt via cc_blended_ehp_impact_line + DS scorer via compute_hybrid + dashboard UI via routes_cc_blended_ehp_threat). Symmetric construction (ally side feeds ENEMY roster as enemy_champions, vice versa) is the LOAD-BEARING semantic of the dashboard route; do NOT collapse to same-side feeds (would invert the threat-balance signal). Tier bands (1.05/0.95) sit next to engine's `_CC_EFFECTIVENESS_FACTOR=0.5` + `_FIGHT_WINDOW_S=6.0` calibration - retune all panel tests when changing those engine constants. Default ratio=1.0 tier=warn when both sides total_cc_seconds=0.0 preserves the symmetric identity contract; pinned by `MathTests::test_zero_cc_both_sides_returns_balanced_warn`. Mode-aware cache key (ARAM and SR don't share cache slots); chip hidden until BOTH sides have at least one resolvable champion. The schema lift via `_build_per_spell_cc_durations()` builder function + `registry.setdefault(champ, {})[spell] = tuple` is the canonical pattern - any future wave 7+ should follow the same multi-wave-friendly construction; do NOT regress to a single dict literal (would re-introduce dict-key collisions for already-seeded champions). All 4 wave-6 case-1 entries now coexist with prior-wave entries on the SAME champion's spell map (Lulu = {W, R}, Sejuani = {R, Q}, Thresh = {Q, E}, plus the 2 new champs Bard {R} + Lillia {R}). Wave 5's `test_lulu_w_polymorph_preserved` assertion was FLIPPED from asserting `Lulu R == ()` (deferred) to `Lulu R == (1.0, 1.0, 1.0)` (now shipped); do NOT regress. The 23 wave-6 REJECT candidates are operator-gated (carryover from wave 4+5 conditional CC + slows + new wave-6-specific) - durable across future audits. Rell R REJECTED for drag-not-stun semantic; do NOT re-pitch without a discrete-pull schema. The 21 stale ENGINE pin syncs are the s243/s246 facade-split precedent; value-pinned regression suite is the definitive behavior-equivalence proof - do NOT propose dropping the pin discipline. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order can be any permutation conflict-free (proven this run + items 134-139 = 7 consecutive runs using this template). The orchestrator-merge pattern (3 worktree agents on disjoint slices + sequential merge + 0 conflict + 1 ENGINE bump commit syncs pins + tests gate + DS restart + RC restart + CI verify + docs sync) is the durable template - this is now the 7th consecutive headless-upgrade run using it (items 134-140). **Carries forward:** (a) cc_blended_ehp ecosystem COMPLETE with 4 consumers; future work is LIVE-GAME VALIDATION (operator plays a real ARAM with one of the 82 registered CC champs to verify all 4 surfaces - engine + coach prompt + DS scorer + dashboard chip - reflect the signal correctly under real conditions). (b) DD Defy heal-on-takedown STILL deferred (carries from item 130+; takedown-rate uncertainty). (c) Live ARAM/SR smoke STILL pending (operator must play a real game). (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W / Warwick R / Viktor W / Tahm Kench R + the 10 wave-4 REJECT list + the 19+ wave-5 REJECT list + the wave-6 23 REJECTs) STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new dashboard threat-balance chip + all 4 cc_blended_ehp surfaces live. (j) The registry-merge schema lift via `_build_per_spell_cc_durations()` is now SHIPPED; future wave 7+ should follow the setdefault pattern (do NOT regress to a single dict literal). (k) Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

139. ✅ **2026-05-22 - item 139 3-slice parallel drain (compute_hybrid enemy_champions kwarg + cc_blended_ehp scoring + _PER_SPELL_CC_DURATIONS wave 5 + ROADMAP/BACKLOG stale-sweep) SHIPPED (3 worktree agents + 3 merge commits `9f2fd6c` `a663320` `86724ea` + 1 orchestrator ENGINE bump commit `6fd05db`, pushed; ENGINE 1.34.0 -> 1.35.0; non-frozen; DS :8893 restarted serves 1.35.0; RC :8888 unchanged pid 12864 - all 3 slices touched DS engine + docs only, no route module edits).** Operator `/headless-upgrade` 3rd long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 138 carry (a) UI-consumer alternative engine path + (h) registry wave 5 + stale-sweep. All 3 merged + green + pushed. RC suite **3140 / 67 subtests / 0 failed** (unchanged - DS-engine-only run); DS suite **3269 -> 3328 / 1 skipped / 1 xfailed / 1666 subtests passed** (+59: 22 Slice A + 37 Slice B). **Slice A `0523511` (merge `a663320`) feat(ds) - compute_hybrid enemy_champions kwarg + cc_blended_ehp scoring (FIRST DS-engine SCORER consumer of cc_blended_ehp - the field shipped item 137 / `99164e8` / ENGINE 1.33.0):** `agents/daemon_slayer/hybrid.py:compute_hybrid()` gains optional `enemy_champions: Iterable[str] = ()` kwarg threaded through to ALL 3 internal `compute_ehp(...)` call sites (lines 238 / 547 / 584). When `enemy_champions` is non-empty, `hybrid_score = alpha * dps + beta * ehp_result.cc_blended_ehp` (POST-CC-erosion EHP) instead of `beta * blended_ehp`. Default empty tuple preserves byte-identical hybrid_score with all pre-1.35.0 callers via the item 137 compute_ehp identity contract (`cc_blended_ehp == blended_ehp` when `enemy_champions=()`). `HybridResult` dataclass gains 2 new fields for surface visibility: `cc_blended_ehp: float = 0.0` + `enemy_champions: tuple[str, ...] = ()`. The `ehp` field on `HybridResult` keeps `blended_ehp` semantics (PRE-CC) for transparency; `cc_blended_ehp` is the NEW POST-CC surface alongside - do NOT collapse them. `_hybrid_delta_pct` percentage normalizer keeps `blended_ehp` (rankings compare apples-to-apples across enemy_champions states); only absolute hybrid_score scalar uses cc_blended_ehp. `enemy_champions` input is normalized to `tuple(str(e) for e in enemy_champions or ())` once at top of compute_hybrid (mirror item_list pattern). +22 tests in NEW `test_hybrid_enemy_champions.py` (~513 LOC across 7 classes: IdentityContractTests 4 / EnemyChampionsScoringTests 5 / MultiCallSiteThreadingTests 3 / MalformedEnemyChampionsTests 4 / HybridResultFieldsTests 3 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). MultiCallSiteThreadingTests uses unittest.mock spy to verify each of the 3 internal compute_ehp call sites receives the same enemy_champions tuple. Math verified live: Aatrox L11 SR + (Annie/Morgana/Malzahar) summed CC -> 7.0s total -> clamped fraction 1.0 -> cc_blended_ehp = blended_ehp * 0.5 -> hybrid_score delta = beta * (blended_ehp - cc_blended_ehp) = 680.99 (matches expected to 4 decimal places). **Slice B `d7af3be` (merge `86724ea`) feat(ds) - _PER_SPELL_CC_DURATIONS wave 5 (+8 entries / +7 new champs, broadens 82/73 -> 90/80):** Extends the wave 1+2+3+4 seed with 8 additional first-order CC entries across 7 new champions of patch 16.10.1. New champions seeded: Hecarim (E Devastating Charge knockback 0.75s all 5 ranks + R Onslaught of Shadows fear 1.0s all 3 ranks - 2 spell entries / 1 champ), KSante (R All Out knock-up 0.75s all 3 ranks), Mordekaiser (E Death's Grasp pull 0.25s all 5 ranks), Urgot (E Disdain knockback 0.5s all 5 ranks), Viego (W Spectral Maw full-charge stun 1.5s all 5 ranks), Yone (R Fate Sealed knock-up 0.75s all 3 ranks), Ziggs (W Satchel Charge knockback 0.5s all 5 ranks). **Mid-implementation collision discovery REJECTED 9 candidates that clobbered prior-wave dict keys** (the dict-literal cannot represent multi-wave augmentation of a single champion's spell map): Lulu R Wild Growth (Lulu W polymorph wave 1 collision), Quinn E Vault (already seeded wave 4), Shen E Shadow Dash (already wave 4), Jax E Counter-Strike (already wave 4), Jinx E Flame Chompers (already wave 4), Janna Q Howling Gale (already wave 4), Sejuani Q Arctic Assault (Sejuani R wave 1 collision), Rell R Magnet Storm (Rell Q wave 3 collision), Thresh E Flay (Thresh Q wave 1 collision). A future registry-merge pass is owed to enable these without clobbering. Additional conditional-CC REJECTs (operator-gated schema lift; same long list as wave 4): Aurora R / Mordekaiser R / Briar R / Bard Q / Sett W / Taliyah W / Volibear Q / JarvanIV EQ / Trundle R / Kennen E / KSante Q / Sett E / Vayne E / Sylas E2 / Xayah E / Aphelios Q / Aurora E / Briar Q / Renata R. Selection rules unchanged from waves 1+2+3+4: first-order CC only; no slows; no conditional CC; canonical DDragon ids (KSante / Hecarim / Mordekaiser / Urgot / Viego / Yone / Ziggs - all punctuation-stripped per `_portraitUrl` convention). Consumer math BYTE-IDENTICAL to 1.34.0 (data lane only); the 4 consumers (cc_pressure aggregator + AbilitySpellDps `cc_duration_s` / `cc_duration_post_tenacity` fields + `cc_blended_ehp_impact_line` coach prompt + `enemy_cc_threat_line` per-enemy chunk) all read the 8 new entries transparently. +37 tests in NEW `test_per_spell_cc_registry_wave5.py` (~498 LOC across 7 classes: WaveFiveNewEntryShapeTests / WaveFiveValuePinsTests / WaveOneThroughFourPreservedTests / RegistryGrowthTests / WaveFiveNoDuplicatesTests / EngineVersionCurrentTests / AsciiHygieneTests). `test_per_spell_cc_registry_seed.py` 2-line count pin updated 73/82 -> 80/90 (wave 1 surface itself UNTOUCHED). Wave 2/3/4 + phase 6.5 + cc_pressure + ehp_cc_blended tests all stay green. **Slice C `25dec0c` (merge `9f2fd6c`) docs(roadmap-backlog) - stale-entry sweep wave 4 per [[feedback_backlog_path_stale_check]]:** 1 entry flipped via grep-verification methodology: BACKLOG.md L13 future EHP enemy-CC model annotation bumped 7 -> 8 consumers shipped (recognizing item 138 Slice A `738c005` `core/cc_blended_ehp_context.py:cc_blended_ehp_impact_line()` as the 8th consumer; FIRST coach-prompt consumer of cc_blended_ehp; SECOND consumer overall after item 137 engine math). Reframed future surface: both engine-math + coach-prompt consumers shipped; remaining future is a DASHBOARD UI CONSUMER. Updated canonical contract seam to include cc_blended_ehp_impact_line(). Sweep otherwise CLEAN - all other open BACKLOG/ROADMAP entries verified legitimately operator-gated / live-gated. The [[feedback_backlog_path_stale_check]] sweep cycle continues to decay (wave 1=2 flips, wave 2=3 flips, wave 3=3 flips, wave 4=1 flip - methodology stable). **Orchestrator commit `6fd05db` bumps ENGINE_VERSION 1.34.0 -> 1.35.0 in `__init__.py`** with full changelog block covering both feature additions + syncs **26 stale ENGINE pin syncs** across 25 DS test files (test_effects_expansion 2 hits + 23 other files 1 hit each: cc_pressure / s228 / s229 / s230 / s231 / s232_saturation / ehp / ehp_cc_blended / ehp_heal_phase6 / ehp_shield_phase15 / form_index_sweep_s226 / hybrid_enemy_champions / max_priority_sweep_s227 / nested_hp_parser_s223 / per_spell_cc_phase65 / per_spell_cc_registry_seed / per_spell_cc_registry_wave2 / per_spell_cc_registry_wave3 / per_spell_cc_registry_wave4 / per_spell_cc_registry_wave5 / post_parser_sweep_s225 / sum_of_blocks_expansion_s215 / sum_of_blocks_expansion_s217 / unit_variants_s224) + 6 source-docstring pins in hybrid.py + 2 wave-5 mislabel fixes in seed.py lines 101+107 ("1.34.0 wave 5" -> "1.35.0 wave 5") + 2 wave5-test/hybrid-test docstring bumps + 1 wave3-test docstring bump (wave-4 ship state -> wave-5 ship state). 3 worktrees unlocked + force-removed + 3 worktree branches deleted (local + remote). **Merge order:** C first (docs `9f2fd6c`), A second (`a663320`, hybrid.py + new test file), B third (`86724ea`, ability_dps.py registry expansion + new test file + 2-line seed count pin update). 0 merge conflicts (3 entirely disjoint surfaces: A=hybrid.py + test_hybrid_enemy_champions.py; B=ability_dps.py + test_per_spell_cc_registry_wave5.py + 2-line test_per_spell_cc_registry_seed.py count pin update; C=BACKLOG.md only). **Verified:** Wider RC **3140 passed / 67 subtests / 0 failed** (unchanged - DS-engine-only run; phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart same pattern items 134/135/136/137). DS suite **3328 passed / 1 skipped / 1 xfailed / 1666 subtests** (+59 over 3269 baseline = 22 Slice A + 37 Slice B). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via taskkill PID 13516 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.35.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; the 3 slices were DS engine + docs only). CI green push `6fd05db` 1m23s. DS audit iter sweep: NO-CHANGE at engine 1.35.0 - no new Riot patch dropped (live patch still 16.10.1; DDragon latest 16.10.1 = RC current.txt); engine remains Meraki-coherent; the audit-streak halt from item 136 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN (engine math + data lane is product-fidelity preserving; no prompt-cache changes; no polling cadence shifts; no model-tier changes). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** compute_hybrid `enemy_champions=()` default preserves byte-identical hybrid_score via item 137 compute_ehp identity contract (`cc_blended_ehp == blended_ehp` when `enemy_champions=()`); pinned by `IdentityContractTests`. The HybridResult.ehp field keeps blended_ehp semantics (PRE-CC) for transparency; cc_blended_ehp is the NEW POST-CC surface alongside - do NOT collapse them. `_hybrid_delta_pct` percentage normalizer keeps `blended_ehp` for rank-comparison stability across enemy_champions states; only absolute hybrid_score scalar uses cc_blended_ehp - do NOT push cc_blended_ehp into the percentage normalizer (would break rank stability when enemy_champions changes mid-game). All 3 internal compute_ehp call sites (lines 238/547/584 of hybrid.py) thread the same enemy_champions tuple; MultiCallSiteThreadingTests pins via unittest.mock spy. The 9 wave-5 dict-key collisions (Lulu R / Quinn E / Shen E / Jax E / Jinx E / Janna Q / Sejuani Q / Rell R / Thresh E) are deferred to a future registry-merge pass; do NOT add these without the schema lift (the dict-literal cannot represent multi-wave augmentation of a single champion's spell map). The 19+ wave-5 conditional CC candidates are operator-gated schema lift (same as prior waves' lists, expanded). `_PER_SPELL_CC_DURATIONS` registry now 90 entries / 80 champs of first-order CC at 16.10.1. The 26 stale ENGINE pin syncs (25 test files + 6 source-docstring pins + 2 wave-5 mislabel fixes + 2 wave5-test/hybrid-test docstring bumps + 1 wave3-test docstring bump) are the s243/s246 facade-split precedent; value-pinned regression suite is the definitive behavior-equivalence proof - do NOT propose dropping the pin discipline. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order can be any permutation conflict-free (proven this run + items 134-138 = 6 consecutive runs using this template). The orchestrator-merge pattern (3 worktree agents on disjoint slices + sequential merge + 0 conflict + 1 ENGINE bump commit syncs pins + tests gate + DS restart + CI verify) is the durable template - this is now the 6th consecutive headless-upgrade run using it (items 134/135/136/137/138/139). **Carries forward:** (a) The cc_blended_ehp field now has 3 consumers (engine math via compute_ehp + coach prompt via cc_blended_ehp_impact_line + DS scorer via compute_hybrid) but no DASHBOARD UI consumer yet - the next surface is a champ-select threat-tag panel that compares ally vs enemy cc_blended_ehp ratios (operator-gated whether to ship a UI consumer before or after live-game validation). (b) DD Defy heal-on-takedown STILL deferred (carries from item 130+; takedown-rate uncertainty). (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 80 registered CC champs to verify the cc_blended_ehp signal surfaces in live coach output + DS picks. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W / Warwick R / Viktor W / Tahm Kench R + the 10 wave-4 REJECT list + the 19+ wave-5 REJECT list candidates + the 9 wave-5 dict-collision champ-spell pairs) STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new cc_blended_ehp scorer in compute_hybrid live. (j) A future registry-merge pass is owed to enable multi-wave augmentation of single-champion spell maps (the 9 wave-5 collision REJECTs); this is a registry schema lift, not a data add. Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

138. ✅ **2026-05-22 - item 138 3-slice parallel drain (cc_blended_ehp coach prompt consumer + _PER_SPELL_CC_DURATIONS wave 4 + ROADMAP/BACKLOG stale-sweep) SHIPPED (3 worktree agents + 3 merge commits `b19772d` `b1992fc` `54cf688`, pushed; ENGINE 1.33.0 -> 1.34.0; non-frozen; RC :8888 restarted clean pid 7540 -> 12864 last_reload_ok=True - coach module edits triggered restart; DS :8893 restarted serves 1.34.0).** Operator `/headless-upgrade` 2nd long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 137 carry (a) coach-prompt half + adjacent registry wave 4 + stale-sweep. All 3 merged + green + pushed. RC suite **3097 -> 3140 / 67 subtests / 0 failed** (+43 Slice A); DS suite **3232 -> 3269 / 1 skipped / 1 xfailed / 1666 subtests passed** (+37 Slice B). **Slice A `738c005` feat(coach) - cc_blended_ehp_impact_line (FIRST coach-prompt consumer of cc_blended_ehp; closes item 137 carry (a) coach-prompt half):** NEW `core/cc_blended_ehp_context.py` (~120 LOC) `cc_blended_ehp_impact_line(enemies, mode, *, fight_window_s=6.0, cc_effectiveness_factor=0.5) -> str`. Sibling of `core/enemy_cc_threat_context.py` (per-enemy CC breakdown shipped item 136 Slice B) - aggregates ACROSS all enemies into one summary line. Math mirrors `agents/daemon_slayer/ehp.py:compute_ehp` exactly: `total_cc_seconds = sum(compute_cc_pressure(e, mode).total_cc_seconds for e in enemies if e)` (blank/None silently skipped); `cc_pressure_fraction = min(total_cc_seconds / fight_window_s, 1.0)` (clamps at 1.0); `effective_ehp_loss_pct = cc_pressure_fraction * cc_effectiveness_factor * 100`. Output line format: `"Enemy CC pressure: 2.5s over 6s fight = 42% saturation, blended-EHP impact -21%"`. Returns empty when mode is None/blank OR enemies is None/empty OR total_cc_seconds == 0.0 (90% non-target case = zero token overhead). `_FIGHT_WINDOW_S = 6.0` + `_CC_EFFECTIVENESS_FACTOR = 0.5` defaults match `ehp.py` module constants EXACTLY (operator-tunable kwargs allow per-call override; defaults stay synced per item 137 don't-redo). Mode-agnostic (works for SR/ARAM/KIWI/Arena/Brawl - all flow through compute_cc_pressure). Wired into all 4 active mode coach prompts APPEND-only per cache-prefix preservation (CLAUDE.md item 121): `coaches/aram_coach.py` + `coaches/arena_coach.py` (uses `_arena_enemies` per existing pattern) + `coaches/brawl_coach.py` (3 prompts NB/URF/OFA via `_cc_blended_line` + `_cc_blended_segment` concat pattern) + `coach_integration/_sr_prompt.py` `SR_SYSTEM_PROMPT` (lines.append after enemy_cc_threats). ImportError fallback stub mirrors `enemy_cc_threat_context.py` pattern for parallel-slice ship robustness (Slice A can ship + test before Slice B's wave 4 lands; stub returns None so renderer treats unknown gracefully). +43 tests in NEW `test_cc_blended_ehp_context.py` (~498 LOC across 9 classes: LineRendersTests / ModeGateTests / FightWindowTests / CcEffectivenessFactorTests / MonkeyPatchTests / TupleInputTests / SkipsBlankTests / CoachWireTests / AsciiHygieneTests). 4 pre-existing fixture extensions: `tests/test_ds_pick_consumption_p1l11.py` (2 fixtures ARAM + Arena) + `tests/test_aram_tenacity_context.py` (3 _USER_TMPL.format calls) + `tests/test_enemy_cc_threat_context.py` (2 CoachWireTests fixtures). NO ENGINE bump (coach-prompt consumer, no engine math change). **Slice B `6718445` feat(ds) - _PER_SPELL_CC_DURATIONS wave 4 (+15 entries / +15 new champs, broadens 67/58 -> 82/73):** Extends the wave 1+2+3 seed with 15 additional first-order CC entries across 15 new champions at patch 16.10.1. 8 of 15 cross-verified against `data/daemon_slayer/16.10.1/champion_abilities.json` Disable/Stun/Root/Fear/Taunt blocks; the other 7 follow the single-value-all-ranks pattern from prior waves. Selection rules unchanged from waves 1+2+3: first-order CC only (stuns / roots / suspensions / knock-ups / knock-backs / fears / suppressions / charms / sleeps / silences / polymorphs / taunts); no slows (different math); no conditional CC; canonical DDragon ids. **REJECTED candidates with reason recorded in commit body** (so future audits do NOT re-research): Bard Q (wall-bounce conditional), Evelynn W Allure (detonation-on-Eve-attack conditional), Hwei E (4 compound-cast variants do not fit single-key schema), Karma W Focused Resolve (tether/channel-completion conditional), Seraphine E Beat Drop (conditional on existing slow), Swain E Nevermove (return-wave conditional root), Syndra E Scatter the Weak (Dark Sphere chain conditional), TwistedFate W Pick a Card (Gold Card pre-existing don't-redo from items 134-137), Zilean Q Time Bomb (double-bomb stack conditional), Volibear R Stormbringer (turret-only disable not champion CC). Consumer math BYTE-IDENTICAL to 1.33.0 (data lane only); the cc_pressure aggregator + EHP-vs-CC blended scorer + AbilitySpellDps `cc_duration_s` / `cc_duration_post_tenacity` fields read the 15 new entries transparently. +37 tests in NEW `test_per_spell_cc_registry_wave4.py` (per-entry rank-tuple value pins + RegistryGrowthTests pinning 73/82 totals + EngineVersionCurrentTests + AsciiHygieneTests). `test_per_spell_cc_registry_seed.py` 2-line count pin updated 58/67 -> 73/82 (wave 1 surface itself UNTOUCHED). Wave 2 + wave 3 + phase 6.5 + cc_pressure + ehp_cc_blended tests all stay green. **+21 stale ENGINE pin syncs** across DS test files (1.33.0 -> 1.34.0): s215/s217/s223-s232 series + test_ehp + test_ehp_shield_phase15 + test_ehp_heal_phase6 + test_effects_expansion (2 hits) + test_per_spell_cc_phase65 + test_per_spell_cc_registry_seed/wave2/wave3 + test_cc_pressure + test_ehp_cc_blended (1 hit each). `agents/daemon_slayer/__init__.py` ENGINE_VERSION bumped + changelog block. **Slice C `9e0220b` docs(roadmap-backlog) - stale-entry sweep per [[feedback_backlog_path_stale_check]]:** 3 entries flipped via grep-verification methodology: BACKLOG.md L13 future EHP enemy-CC model annotation bumped 6 -> 7 consumers shipped (recognizing item 137 Slice A `99164e8` cc_blended_ehp field as the 7th consumer); reframed "remains future" sentence - the EHP-vs-CC BLENDED SCORER itself is shipped, the new future surface is a LIVE CONSUMER reading cc_blended_ehp (champ-select threat-tag panel / per-mode DS pick rankings under enemy-CC scenarios). ROADMAP.md added new item 137 SHIPPED entry at top of "Open items - High priority" mirroring item 136 style with full 3-slice detail. ROADMAP.md L205 Fleet status DS row bumped ENGINE 1.32.0 -> 1.33.0 + tests 3164 -> 3232 + `_PER_SPELL_CC_DURATIONS` wave 3 annotation (53 -> 67 entries / 44 -> 58 champs) + NEW cc_blended_ehp annotation as SECOND engine-math consumer of compute_cc_pressure. Total diff: BACKLOG.md +1/-1, ROADMAP.md +3/-1. Sweep otherwise CLEAN - all other open BACKLOG/ROADMAP entries verified legitimately operator-gated / live-gated. The [[feedback_backlog_path_stale_check]] sweep cycle has stabilized after 3 rounds (items 132 Slice B `ded589c`, 137 Slice C `1125708`, this slice). **Merge order:** C first (docs `b19772d`), A second (`b1992fc`, coach modules + new context module + new test file + 4 fixture updates), B third (`54cf688`, ability_dps.py registry expansion + new test file + 21 ENGINE pin syncs). 0 merge conflicts (3 entirely disjoint surfaces: A=coach modules + core/cc_blended_ehp_context.py + tests; B=ability_dps.py + __init__.py + DS test files; C=BACKLOG.md + ROADMAP.md only). Worktrees + 3 worktree branches unlocked + force-removed + 3 branches deleted. **Verified:** Wider RC **3140 passed / 67 subtests / 0 failed** (+43 over s137 3097 baseline = Slice A's coach-prompt consumer tests). DS suite **3269 passed / 1 skipped / 1 xfailed / 1666 subtests** (+37 over 3232 baseline). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via taskkill PID 6668 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.34.0 patch=16.10.1 champions=172 items=705`. RC :8888 restarted via `restart_trigger.txt` (coach module edits) -> pid 7540 -> 12864 alive=True last_reload_ok=True mode=client. CI green push `54cf688` 1m22s. DS audit iter sweep: NO-CHANGE at engine 1.34.0 - no new Riot patch dropped (live patch still 16.10.1); engine remains Meraki-coherent; the audit-streak halt from item 137 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN (coach-prompt consumer is APPEND-only per cache-prefix preservation; data-lane wave 4 is product-fidelity preserving; no prompt-cache changes; no polling cadence shifts; no model-tier changes). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** `cc_blended_ehp_impact_line` defaults `_FIGHT_WINDOW_S = 6.0` + `_CC_EFFECTIVENESS_FACTOR = 0.5` MUST match `agents/daemon_slayer/ehp.py` exactly (operator-tunable kwargs allow per-call override but defaults stay synced). Pinned by `MathTests::test_default_constants_match_ehp_module`. The renderer aggregates ACROSS all enemies into ONE summary line (complementary to `enemy_cc_threat_line` which lists PER-ENEMY breakdown); do NOT collapse these into one or change the relationship - both surfaces serve different prompt needs. The line is APPENDED via `_USER_TMPL` per-call (NOT system prompt) - cache-prefix preservation per item 121. ImportError fallback stub mirrors `enemy_cc_threat_context.py` for parallel-slice ship robustness; do NOT remove. `_PER_SPELL_CC_DURATIONS` registry now 82 entries / 73 champs of first-order CC at 16.10.1. Do NOT add slows. Do NOT add the 10 REJECTED wave-4 candidates (Bard Q / Evelynn W / Hwei E / Karma W / Seraphine E / Swain E / Syndra E / TwistedFate W / Zilean Q / Volibear R) without conditional-axis schema lift. Wave 4 REJECT list is durable across future audits. The 21 stale ENGINE pin syncs are the s243/s246 facade-split precedent; do NOT propose dropping the pin discipline. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order is any permutation conflict-free (proven this run + items 134-137). The orchestrator-merge pattern (3 worktree agents on disjoint slices + sequential merge + 0 conflict + tests gate + DS restart + RC restart + CI verify + docs sync) is the durable template - this is now the 5th consecutive headless-upgrade run using it (items 134/135/136/137/138). **Carries forward:** (a) The cc_blended_ehp field now has 2 consumers (engine math via compute_ehp + coach prompt via cc_blended_ehp_impact_line) but no DASHBOARD UI consumer yet - the next surface is a champ-select threat-tag panel that compares ally vs enemy cc_blended_ehp ratios OR per-mode DS pick ranking under enemy-CC scenarios; operator-gated whether to ship a UI consumer before or after live-game validation. (b) DD Defy heal-on-takedown STILL deferred (carries from item 130+ - takedown-rate uncertainty). (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 73 registered CC champs to verify the new "Enemy CC pressure: ..." coach prompt line surfaces in live coach output + cc_blended_ehp lift in DS picks + the per-enemy enemy_cc_threats line from item 136 Slice B. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W / Warwick R / Viktor W / Tahm Kench R + the 10 wave-4 REJECT list candidates) STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new cc_blended_ehp_impact_line live. Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

137. ✅ **2026-05-22 - item 137 3-slice parallel drain (EHP-vs-CC blended scorer + _PER_SPELL_CC_DURATIONS wave 3 + ROADMAP/BACKLOG stale-sweep) SHIPPED (3 worktree agents + 3 merge commits `e7e8a5c` `aa78705` `64d20d9` + 1 ENGINE bump commit `665b533`, pushed; ENGINE 1.32.0 -> 1.33.0; non-frozen; RC :8888 unchanged pid 7540 - all 3 slices touched DS engine + docs only, no route module edits; DS :8893 restarted serves 1.33.0).** Operator `/headless-upgrade` long-run authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 136 carry (a) + adjacent registry expansion + stale-sweep. All 3 merged + green + pushed. RC suite **3096 -> 3097 / 67 subtests / 0 failed** (+1 phase8_smoke `test_live_three_profiles` flip post-DS-restart, same pattern items 134/135/136); DS suite **3164 -> 3232 / 1 skipped / 1 xfailed / 1666 subtests passed** (+68: 34 Slice A + 34 Slice B). **Slice A `99164e8` feat(ds) - EHP-vs-CC blended scorer (closes item 136 carry (a) - 2nd engine math consumer of compute_cc_pressure):** `agents/daemon_slayer/ehp.py` gains NEW `_CC_EFFECTIVENESS_FACTOR = 0.5` module constant + lazy import of `compute_cc_pressure` inside `compute_ehp` body (LOAD-BEARING - cc_pressure imports `effective_cc_duration` from ehp at module load, so top-level import would create circular; lazy import only fires when `enemy_champions` non-empty - cheap once-per-call). EhpResult gains 3 new fields: `enemy_cc_pressure_s: float = 0.0`, `cc_pressure_fraction: float = 0.0`, `cc_blended_ehp: float = 0.0`. `compute_ehp()` gains optional `enemy_champions: Iterable[str] = ()` kwarg AFTER `augments`. Math model: `enemy_cc_pressure_s = sum(compute_cc_pressure(e, mode).total_cc_seconds for e in enemy_champions if e)` (blank/None entries silently skipped, mirrors enemy_cc_threat_line contract); `cc_pressure_fraction = min(enemy_cc_pressure_s / _FIGHT_WINDOW_S, 1.0)` where _FIGHT_WINDOW_S=6.0 (existing module constant from Phase 6); `cc_blended_ehp = blended_ehp * (1.0 - cc_pressure_fraction * _CC_EFFECTIVENESS_FACTOR)`. `_CC_EFFECTIVENESS_FACTOR = 0.5` is a conservative midpoint - 1s of summed enemy CC pressure erodes 0.5s of operator fight-time effectiveness (CC is not perfectly chained; QSS/Cleanse/Flash mitigate; tenacity items reduce; positioning matters). Operator-tunable parallel to `_MISSING_HP_SHARE_FOR_HEALS = 0.5` discipline from item 130. Empty `enemy_champions=()` (the default at every existing call site across the engine + dashboard) leaves the 3 new fields at 0.0 / 0.0 / equal-to-blended_ehp - identity preservation, back-compat for all 2167+ existing callers. Per-type EHP fields (physical_ehp / magical_ehp / true_ehp / blended_ehp) UNCHANGED when enemy_champions provided - the CC discount lives ONLY in the new cc_blended_ehp field; do NOT push the discount into per-type fields (would break shield/heal/mode_multiplier composition). Mode-aware via existing `compute_cc_pressure` tenacity wiring; ARAM/KIWI lengthens for the 17 modified-tenacity champs. to_dict + format_table (cc_pressure row when non-zero) + notes (enemy_cc line) carry the 3 new fields. Per-enemy duplicate enumeration is INTENTIONAL (registry-driven sum; 2 Annies in custom modes double-count). Saturation contract: 5 heavy CC enemies (15s summed) clamps `cc_pressure_fraction` at 1.0 exact (not 2.5) -> `cc_blended_ehp = blended * 0.5` floor. "MonkeyKing" canonical DDragon id recognized; "Wukong" silently treated as unknown (0.0 contribution) - pinned. **+34 tests in NEW `test_ehp_cc_blended.py`** (~512 LOC across 6 classes: ContractTests / MathTests / EnemyEnumerationTests / EhpResultFieldsTests / CompositionWithExistingFieldsTests / EngineVersionCurrentTests + AsciiHygieneTests). **Slice B `f989522` feat(ds) - _PER_SPELL_CC_DURATIONS wave 3 (+14 entries / +14 new champs, broadens 53/44 -> 67/58):** Extends the wave 1 + wave 2 seed with 14 additional first-order CC entries across 14 new champions at patch 16.10.1. New champions seeded: AurelionSol R (Falling Star center stun 1.25/1.5/1.75), Caitlyn W (Yordle Snap Trap 1.5s root all 5 ranks), Camille E (Hookshot 0.75s stun on second-cast wall-dive), Diana R (Moonfall 0.75s knock-up all 3 ranks), Elise E (human Cocoon 1.1/1.4/1.7/2.0/2.3 stun across 5 ranks), Heimerdinger E (CH-2 Electron Storm Grenade 1.25s stun - rank 5 slot pins to rank 4 since E maxes at 4), Ivern Q (Rootcaller 1.0/1.25/1.5/1.75/2.0 root), Malphite R (Unstoppable Force 1.5/1.75/2.0 knock-up), Pyke Q (Bone Skewer ranged 1.25s stun all ranks), Rell Q (Shattering Strike 1.0s root all ranks; Rell W mount/dismount intentionally still skipped per item 135), Ryze W (Rune Prison 0.75/1.0/1.25/1.5/1.75 root), Sion Q (Decimating Smash 1.25/1.5/1.75/2.0/2.25 full-charge stun), Tristana R (Buster Shot 1.0s knock-back all 3 ranks), XinZhao W (Wind Becomes Lightning 1.0s 3rd-strike knock-up). Selection rules unchanged from waves 1+2: first-order CC only (stuns / roots / suspensions / knock-ups / knock-backs / fears / suppressions / charms / sleeps / silences / polymorphs / taunts); no slows (different math); no conditional CC (Warwick R full-channel + Viktor W 3rd-charge + Tahm Kench R Devour-on-enemy + Mordekaiser R banishment + Sett W direct-damage-only + Taliyah W displacement-only + Taric E rank-uncertain - all REJECTED with reason recorded); canonical DDragon ids (AurelionSol / XinZhao - punctuation-stripped). Consumer math BYTE-IDENTICAL to 1.32.0 (data lane only); the cc_pressure aggregator + AbilitySpellDps `cc_duration_s` / `cc_duration_post_tenacity` fields read the 14 new entries transparently. **+34 tests in NEW `test_per_spell_cc_registry_wave3.py`** (~424 LOC across 8 classes: per-entry rank-tuple value pins + RegistryGrowthTests pinning 67/58 totals + EngineVersionCurrentTests + AsciiHygieneTests). `test_per_spell_cc_registry_seed.py` 2-line count pin updated 44/53 -> 58/67 (wave 1 entry surface itself UNTOUCHED). Wave 2 + phase 6.5 + cc_pressure tests all stay green. **Slice C `1125708` docs(roadmap-backlog) - stale-entry sweep per [[feedback_backlog_path_stale_check]]:** 3 entries flipped via grep-verification methodology: BACKLOG.md L13 "Future EHP enemy-CC model for `aram_tenacity_mult`" annotation bumped from "4 consumers shipped 2026-05-21/22" to "6 consumers shipped" (recognizing item 136 Slice A `81cf8a3` cc_pressure aggregator as consumer #5 + Slice B `6aff49f` enemy_cc_threat_line 4-mode wire as consumer #6; verified both files exist via Bash file probe); ROADMAP.md L203 Fleet status DS row bumped from `ENGINE 1.31.0 / 705 / 3120 / 172` to `ENGINE 1.32.0 / 705 / 3164 / 172` + appended cc_pressure aggregator + enemy_cc_threat_line annotations matching item-130/134/135 annotation style; ROADMAP.md top entry added for item 136 mirroring item 135 style. Total diff: BACKLOG.md +1/-1, ROADMAP.md +3/-1. All other open BACKLOG/ROADMAP entries verified legitimately operator-gated / live-gated / open-with-no-shipped-equivalent. **Merge order:** C first (docs `e7e8a5c`), A second (`aa78705`, ehp.py + new test file), B third (`64d20d9`, ability_dps.py registry expansion + new test file + wave1 seed test count pin). 0 merge conflicts (3 entirely disjoint surfaces: A=ehp.py + test_ehp_cc_blended.py; B=ability_dps.py + test_per_spell_cc_registry_wave3.py + 2-line test_per_spell_cc_registry_seed.py count pin update; C=BACKLOG.md + ROADMAP.md only). Orchestrator commit `665b533` bumps ENGINE_VERSION 1.32.0 -> 1.33.0 in `__init__.py` with full changelog block covering both feature additions + syncs **21 stale ENGINE pins** across DS test files (s215/s217/s223-s232 series 14 hits + test_ehp + test_ehp_shield_phase15 + test_ehp_heal_phase6 + test_effects_expansion 2 hits + test_per_spell_cc_phase65 + test_per_spell_cc_registry_seed/wave2/wave3 + test_cc_pressure + test_ehp_cc_blended 1 hit each). 3 worktrees unlocked + force-removed + 3 branches deleted. **Verified:** Wider RC **3097 passed / 67 subtests / 0 failed** (+1 over s136 3096 baseline = phase8_smoke flip + adjacent growth). DS suite **3232 passed / 1 skipped / 1 xfailed / 1666 subtests** (+68 over 3164 baseline). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via Stop-Process pid 4444 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.33.0 patch=16.10.1 champions=172 items=705`. RC :8888 unchanged (no route module edits; the 3 slices were DS engine + docs only). Cost/latency lever sweep CLEAN (engine math + data lane is product-fidelity preserving; no prompt-cache changes; no polling cadence shifts; no model-tier changes). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** `_CC_EFFECTIVENESS_FACTOR = 0.5` is the operator-tunable conservative midpoint - 1s summed enemy CC erodes 0.5s of operator fight-time effectiveness; do NOT change without retuning all MathTests + ContractTests. `compute_ehp(enemy_champions=())` default leaves cc_blended_ehp == blended_ehp + the 3 new fields at 0.0 (identity preservation); do NOT regress this back-compat contract; pinned by `ContractTests::test_default_empty_enemy_champions`. Per-type EHP fields (physical_ehp / magical_ehp / true_ehp / blended_ehp) UNCHANGED when enemy_champions provided - CC discount lives ONLY in cc_blended_ehp; do NOT push discount into per-type fields (would break shield/heal/mode_multiplier composition). Lazy import of `compute_cc_pressure` inside compute_ehp body is LOAD-BEARING (avoids load-time circular: cc_pressure imports `effective_cc_duration` from ehp at module load); do NOT promote to top-level import. Per-enemy duplicate enumeration is INTENTIONAL (registry-driven sum; never dedup). Saturation contract: 5 Morgana Qs (15s summed) clamps `cc_pressure_fraction` at 1.0 exact (not 2.5); cc_blended_ehp floor = blended * 0.5; pinned. "MonkeyKing" canonical DDragon id; "Wukong" silently treated as unknown (0.0 contribution); pinned by `EnemyEnumerationTests::test_monkeyking_canonical_id_recognized`. `_PER_SPELL_CC_DURATIONS` now contains 67 entries / 58 champs of first-order CC at 16.10.1. Do NOT add slows (different math). Do NOT add conditional CC (Brand R 3rd-hit / TF W Gold Card / Tahm Q 3rd-stack / Lillia R dream-stack / Volibear Q terrain / Urgot R fear / Rell W mount/dismount / Warwick R channel-completion / Viktor W 3rd-charge / Tahm Kench R Devour-on-enemy / Mordekaiser R banishment) without a conditional axis schema lift. AurelionSol R encoding uses center-stun value only (canonical first-order pin); initial knock-up on first-contact would be a second entry per champion - intentionally skipped per quality > quantity. Sett W direct-damage-only (no first-order CC); Taliyah W displacement-only (not knock-up); Taric E rank-uncertain - all 3 REJECTED with reason recorded in commit body. The 21 stale ENGINE pin syncs are the s243/s246 facade-split precedent; value-pinned regression suite is the definitive behavior-equivalence proof - do NOT propose dropping the pin discipline. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order can be any permutation without conflict (proven this run + last 3 runs items 134-136). The orchestrator-merge pattern (3 worktree agents on disjoint slices + orchestrator merges sequentially + 1 ENGINE bump commit syncs pins + 1 docs sync commit) is the durable template for parallel headless-upgrade drains. **Carries forward:** (a) The cc_blended_ehp field is now in production but no LIVE CONSUMER reads it yet in the dashboard or coach prompts - future surfaces could expose it (e.g. a champ-select threat-tag panel that compares ally vs enemy cc_blended_ehp ratios + per-mode DS pick rankings under enemy-CC scenarios); operator-gated whether to ship a UI consumer before or after live-game validation. (b) DD Defy heal-on-takedown STILL deferred (don't-redo pinned from item 130; takedown-rate uncertainty). (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 58 registered CC champs to verify the new "Enemy CC threats: ..." coach prompt line surfaces in live coach output + cc_blended_ehp lift in DS picks. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` calibration operator-gated unchanged. (e2) `_CC_EFFECTIVENESS_FACTOR = 0.5` calibration operator-gated (NEW). Operator can tune if real-game data suggests a different mid-fight effectiveness erosion factor. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W / Warwick R / Viktor W / Tahm Kench R) STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new cc_blended_ehp scorer + Enemy CC threats line live. Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

136. ✅ **2026-05-22 - item 136 3-slice parallel drain (_PER_SPELL_CC_DURATIONS first ENGINE consumer + RC coach prompt wire + ROADMAP stale-sweep) SHIPPED (3 worktree agents + 3 merge commits `5208caf` `05bbcdd` `4a45546`, pushed; ENGINE 1.31.0 -> 1.32.0; non-frozen; RC :8888 unchanged pid 7540 last_reload_ok=True - non-route slice; DS :8893 restarted serves 1.32.0).** Operator `/headless-upgrade` long-run authorized with frozen-file grant + parallel orchestrator. 3 worktree agents dispatched concurrent on item 135 carry (h) - the open seam from the registry seed. All 3 merged + green + pushed. RC suite **3046 -> 3096 / 67 subtests / 0 failed** (+50 from Slice B); DS suite **3120 -> 3164 / 1 skipped / 1 xfailed / 1666 subtests passed** (+44 from Slice A). **Slice A `81cf8a3` feat(ds) compute_cc_pressure aggregator (closes item 135 carry (h) ENGINE-CONSUMER):** NEW `agents/daemon_slayer/cc_pressure.py` (~240 LOC) is the FIRST aggregator consumer of the `_PER_SPELL_CC_DURATIONS` registry seeded item 134 Slice B + extended item 135 Slice B (53 entries / 44 champs / first-order CC at patch 16.10.1). NEW `CcSpellEntry` + `CcPressureResult` frozen dataclasses + `compute_cc_pressure(champion, mode="SR") -> CcPressureResult` - aggregates max-rank CC durations across registered Q/W/E/R spells with per-mode aramTenacity applied through `ehp.effective_cc_duration` (unified math seam from item 122 / ENGINE 1.25.0). Returns empty result (`total_cc_seconds=0.0`, `spells=()`) for unknown champions / blank / None - never raises. Reads patch-pinned `data/daemon_slayer/<patch>/champions.json` for aramTenacity (mirrors `core/aram_tenacity_context.py` `_TENACITY_MAP` discipline). SR mode identity (tenacity=1.0); ARAM mode lengthens for the 17 modified-tenacity champs. Spell ordering: Q-W-E-R canonical regardless of registry insertion order. ENGINE 1.31.0 -> 1.32.0 in `__init__.py` with full changelog block. +44 tests in NEW `test_cc_pressure.py` (8 classes: ContractTests / ComputeCcPressureTests / TotalCcSecondsTests / SpellOrderTests / FailSoftTests / TenacityIntegrationTests / EngineVersionCurrentTests / AsciiHygieneTests). **+15 stale ENGINE pin syncs** across DS test files (1.31.0 -> 1.32.0). **Live smoke (pre-merge):** Annie SR R=(1.5,1.5,1.5) total=1.5; Galio SR W+E+R total=2.25; Vi SR Q+R total=1.75; MonkeyKing SR R=1.0 (canonical DDragon id); Aatrox SR (no registry entry) -> empty; FauxAssassin ARAM monkey-patched 1.20x -> 1.5 * 1.20 = 1.8 exact. **Slice B `6aff49f` feat(coach) enemy_cc_threat_line (FIRST coach-prompt consumer):** NEW `core/enemy_cc_threat_context.py` (~201 LOC) is the RC-side mirror of `core/aram_tenacity_context.enemy_aram_tenacity_line` - reads Slice A's `compute_cc_pressure` per enemy + mode, renders single prompt-friendly line `"Enemy CC threats: Morgana 3.0s root (Q), Malzahar 2.5s suppress (R), Annie 1.5s stun (R)"` sorted desc by `total_cc_seconds` (alphabetical tiebreak), limit=3 default. NEW `_CC_KIND_HINTS: dict[tuple[str, str], str]` table with 53 entries matching registry breadth exactly (stun / root / charm / suppress / fear / silence / knockup / knockback / sleep / polymorph / taunt); missing-key fallback `"CC"`. Empty when mode is None/blank, limit<=0, enemies is None/empty, or all entries have `total_cc_seconds==0.0` (no registered CC) - zero token overhead in 90% non-target case. Mode-agnostic (works for SR/ARAM/KIWI/Arena/Brawl). **Wired into all 4 active mode coach prompts:** `coaches/aram_coach.py` `_USER_TMPL` adds `{enemy_cc_threats}` below `{enemy_aram_tenacity}` with per-call `enemy_cc_threats=enemy_cc_threat_line(state.get("enemy_comp", []), state.get("game_mode", "ARAM"))`; `coaches/arena_coach.py` `_USER_TMPL` adds `{enemy_cc_threats}` below `{my_abilities}` with per-call state read; `coaches/brawl_coach.py` 3 prompts (NB/URF/OFA) gain APPEND-after-Enemy-team line; `coach_integration/_sr_prompt.py` `SR_SYSTEM_PROMPT` APPENDS after Enemy respawns block. Cache-prefix preservation per CLAUDE.md item 121: all wires are APPEND-only (never insert mid-prefix); the existing cached system prompt prefix stays byte-identical between requests. **Stub fallback import for parallel slice ship:** Slice B's import block tries `from agents.daemon_slayer.cc_pressure import compute_cc_pressure`; ImportError fallback defines a stub returning None so unit tests can monkey-patch via `unittest.mock.patch` regardless of merge order. +50 tests in NEW `test_enemy_cc_threat_context.py` (LineRendersTests / ModeGateTests / LimitTests / KindHintsTests / TopSpellTests / AramTenacityIntegrationTests / CoachWireTests / AsciiHygieneTest). 2 fixture updates: `tests/test_ds_pick_consumption_p1l11.py` `_aram_prompt_inject` fixture + `tests/test_aram_tenacity_context.py` adjacent format kwargs. **Slice C `b063c9e` docs(roadmap) stale-entry sweep per [[feedback_backlog_path_stale_check]]:** ROADMAP.md `L156` DS calibration pipeline entry reframed (stale "envisioned in ADR-007 phase 2" -> SHIPPED-clarified across `core/post_game_rubric.py` + `scripts/postmortem_analyze.py` + `core/death_patterns_loader.py` per items 124-132); ROADMAP.md `L203` Fleet status DS row bumped (stale ENGINE 0.90.0 / 547 items / 1953 tests -> live 1.31.0 / 705 / 3120 / 172 champs + EHP Phase 1.5/6/6.5 + per-spell CC registry seed). BACKLOG.md sweep CLEAN (zero entries flipped - the 2026-05-21 sweep in item 132 Slice B `ded589c` + item 133 Slice C sweep-clean had already caught every genuinely-stale entry; 4 carry-forward open entries verified legitimately operator-gated or live-gated). **Merge order:** A first (22 files DS-side: cc_pressure.py + __init__.py + 15 ENGINE pin sites + 5 test files including new test_cc_pressure.py), B second (8 files RC-side: enemy_cc_threat_context.py + 4 coach files + 3 test/fixture files), C third (1 file ROADMAP.md). 0 merge conflicts - all 3 slices touched entirely disjoint surfaces (A=DS engine; B=RC core + coaches; C=docs only). Worktrees + 3 worktree branches unlocked + force-removed + branches -D cleaned. **Verified:** Wider RC **3096 passed / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart - same pattern items 134/135). DS suite **3164 passed / 1 skipped / 1 xfailed / 1666 subtests** (+44 over 3120 baseline). py_compile + ruff + ASCII clean across all 30 touched files (0 non-ASCII added). DS :8893 restarted via taskkill PID 15428 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.32.0 patch=16.10.1 champions=172 items=705. RC :8888 unchanged (no route module edits this slice - all 4 coach modules reload prompts on next coaching tick without restart per the cached system prompt discipline). Live curl `/api/post-game-rubric` STILL returns 1.31.0-era data (route reads rewind_history.db DB columns, not the new cc_pressure engine surface). DS audit iter SPOT-CHECK (14 items: Sunfire/Wit's End/Lich Bane/Nashor/Trinity/Sheen/Sundered Sky/Bloodthirster/Spirit Visage/Statikk/Guinsoo/Eclipse/Voltaic/Opportunity) NO drift vs Meraki 16.10.1 bulk; engine remains Meraki-coherent at 1.32.0. Cost/latency lever sweep CLEAN (8 coaches cache-marked from item 120/121; SSE drives /api/state with 2s fallback poll; dictionary 86400s cache; polling cadences sane; nothing new to ship without product-fidelity tradeoff). UI/UX audit live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** `cc_pressure.compute_cc_pressure` is the FIRST aggregator consumer of `_PER_SPELL_CC_DURATIONS` - the seam any future EHP-vs-CC blended scorer / fight-sim consumer reads from. Do NOT duplicate aggregation in any caller (the route + coach prompt + future fight-sim all import the same function). The `CcSpellEntry` + `CcPressureResult` dataclasses are FROZEN; do NOT mutate; use replace if a future axis is wanted. The Q-W-E-R canonical spell order is enforced inside `compute_cc_pressure`; do NOT assume registry insertion order. SR mode tenacity_mult=1.0 ALWAYS (identity); ARAM/KIWI modes read the per-champ aramTenacity from champions.json - the engine seam unifies via `ehp.effective_cc_duration(base, mult)`. The `_CC_KIND_HINTS` table has 53 entries exactly matching the registry breadth at patch 16.10.1 - when a future registry add ships (wave 3, conditional CC axis, etc.), the hint table owes a matching entry or the chunk degrades to generic "CC". The cache-prefix preservation rule from CLAUDE.md item 121 applies: enemy_cc_threats placeholders are APPEND-only in coach prompts (never insert mid-prefix); the cached system prefix above stays byte-identical between requests. Each coach reads `state.get("enemy_comp", [])` (ARAM/Brawl/SR) or `state.get("enemies", [])` (Arena) - per-coach state shape; do NOT collapse to one key. The fallback ImportError stub in enemy_cc_threat_context.py is for parallel-slice ship robustness (Slice B can ship + test before Slice A merges); the stub returns None so the renderer skips the champion silently - matches the contract "empty result when champion not in registry". The ROADMAP `L156` reframe is the canonical SHIPPED state for the DS calibration / postmortem-analyze stack; do NOT re-pitch it as future work. BACKLOG sweep clean outcome is the valid result; do NOT re-pitch a sweep next session unless new shipped work has landed without doc sync. **Carries forward:** (a) The cc_pressure aggregator is the FIRST consumer - the EHP-vs-CC BLENDED SCORER (a fight-sim that computes "enemy effective DPS lost while under CC") is the natural next consumer; the seam is now in place at the engine layer + the data surface is in production. The EHP scorer in `agents/daemon_slayer/ehp.py` could ingest `cc_pressure.total_cc_seconds` into a blended-EHP-under-CC axis when operator wants to ship that math; operator-gated whether to ship NOW or wait for live-game-validation of the coach-prompt consumer first. (b) Item 135 carry (a) Death's Dance Defy heal-on-takedown STILL deferred. (c) Item 135 carry (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 44 registered CC champs to verify the new "Enemy CC threats: ..." line appears in coach output. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; the role_grades JSON will reflect void-aware obj_participation per item 135. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` calibration operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated (replace SQL kills with Match-V5 takedowns OR keep additive model). (h) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W) STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new enemy CC threats line live. Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

135. ✅ **2026-05-22 - item 135 2-slice parallel drain (voidMonsterKill enrichment + _PER_SPELL_CC_DURATIONS wave 2) SHIPPED (2 worktree agents + 2 merge commits `808c50b` `69422f0`, pushed; ENGINE 1.30.0 -> 1.31.0; non-frozen; RC :8888 restarted clean pid 12492 -> 7540 last_reload_ok=True; DS :8893 restarted serves 1.31.0).** Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 2 worktree agents dispatched parallel on 2 disjoint slices composing on item 134 carries (g) + (h) data-side. Both merged + green + pushed. RC suite **3004 -> 3046 / 67 subtests / 0 failed** (+42 from Slice A); DS suite **3080 -> 3120 / 1 skipped / 1 xfailed / 1666 subtests passed** (+40 from Slice B). **Slice A `0eacc14` feat(coach) voidMonsterKill enrichment in obj_participation (8-column, closes item 134 carry (g)):** Extends `core/obj_participation.py` `compute_obj_participation()` from 7-column to 8-column by ALSO reading `voidMonsterKill` (Voidgrubs) from the `participants.challenges_json` Match-V5 blob already pulled for herald enrichment. Coverage probe: 200/200 (100%) sampled blobs have the key present; zero overlap with existing SQL columns (no `void_kills` column in participants). NEW `_challenges_objectives(blob) -> tuple[float, float]` helper parses the JSON blob ONCE per row and extracts BOTH herald + void in one trip (preferred over a per-key helper that would double-parse). The legacy `_herald_from_challenges` is kept as a thin wrapper for back-compat. NEW `_VOID_KEY = "voidMonsterKill"` constant next to existing `_HERALD_KEY`. Both keys added to BOTH numerator (operator's row) AND denominator (team total) so the ratio stays normalized. Voidgrubs are a 2024+ team objective (SR matches show ~3-6 per team; ARAM / Arena / event modes have 0; the field defaults to 0 naturally with no special handling). Module docstring + inline comment bumped 7-column -> 8-column. Fail-soft on every degenerate shape (matches herald contract). +43 tests across 3 test files: `test_obj_participation.py` 49 -> 81 (+32 in NEW VoidMonsterTests + extended FailSoftTests + `_challenges_objectives` helper tests); `test_routes_post_game_rubric_obj.py` 11 -> 16 (+5 in fixture-builder extended for voidMonsterKill + 4 enrichment tests + 1 legacy schema test); `test_postmortem_analyze.py` 68 -> 74 (+6 in NEW `_populate_match_a_objectives` helper + void-side coverage). **Live curl proves end-to-end:** `GET /api/post-game-rubric?match_id=NA1_5560540832` post-merge returns `ok=true role=ADC total_score=37.28 grade=C obj_participation=0.331`. Pre-Slice-A was 0.364 (item 134); post-Slice-A is 0.331 because the operator's team had voidgrubs that a TEAMMATE (not the operator) got - the denominator grew but the numerator stayed. Mathematically correct lift: when operator is void-involved score INCREASES; when teammate is void-involved without operator the operator's share decreases. Both behaviors validated. **Slice B `758756f` feat(ds) _PER_SPELL_CC_DURATIONS registry wave 2 (+23 entries across +20 new champs, closes item 134 carry (h) data-side):** Extends the 30-entry/24-champ seed shipped item 134 Slice B `05f2475` with 23 additional first-order CC entries across 20 new champions of patch 16.10. New champions seeded: Alistar (Q pulverize / W headbutt-knockback), Amumu (Q bandage / R curse), Anivia (Q stun on detonation), Braum (R knockup center), Chogath (Q rupture), Fiddlesticks (Q terrify), Gnar (R knockback), Gragas (E body slam), Jhin (W deadly flourish root), Lux (Q light binding), Nami (Q bubble), Neeko (E tangle-barbs / R pop blossom), Orianna (R command shockwave), Poppy (E base stun), Riven (W ki burst), Singed (E fling), Skarner (R impale suppression), Varus (R chain root), Xerath (E shocking orb), Zac (E elastic slingshot landing). Rell W intentionally skipped (mount/dismount toggle complicates rank progression). Selection rules: first-order CC only (stuns / roots / suspensions / knock-ups / knock-backs / fears / suppressions / charms / sleeps / silences / polymorphs); skip slows, skip conditional CC (Brand R 3rd-hit / TF W Gold Card / Tahm Q 3rd-stack / Volibear Q terrain / Lillia R dream-stack), skip self-CC; canonical DDragon ids ("Chogath" not "Cho'Gath"; "MonkeyKing" already in seed). Consumer math unchanged from 1.30.0; data flows through `AbilitySpellDps.cc_duration_s` + `cc_duration_post_tenacity` for API inspection only. SR mode (tenacity=1.0) returns identity values; ARAM mode for the 17 modified-tenacity champs returns lengthened values. +40 tests in NEW `test_per_spell_cc_registry_wave2.py` (per-champ-spell value pins). Existing seed tests untouched. +17 stale ENGINE pin syncs across DS test files (1.30.0 -> 1.31.0). ENGINE bumped 1.30.0 -> 1.31.0 in `__init__.py` with full changelog block. **Merge order:** A first (4 RC-side files, no conflict). B second (21 DS-side files, bumps ENGINE + syncs 17 stale pin sites; no conflict). 0 merge conflicts (A + B touched entirely disjoint surfaces: A = `core/obj_participation.py` + 3 test files; B = `agents/daemon_slayer/__init__.py` + `ability_dps.py` + 2 cc-spell test files + 17 ENGINE-pin syncs). **Verified:** Wider RC **3046 passed / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart); DS suite **3120 passed / 1 skipped / 1 xfailed / 1666 subtests**. py_compile + ruff + ASCII clean across all touched files. DS :8893 restarted via taskkill PID 16068 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.31.0 patch=16.10.1 champions=172 items=705. RC :8888 restarted via `restart_trigger.txt` (core/obj_participation.py module edit -> route reloads) -> pid 12492 -> 7540 alive=True last_reload_ok=True mode=client. Live `/api/post-game-rubric` proved end-to-end with void-aware obj_participation. **Don't-redo:** `compute_obj_participation` is now 8-column (6 SQL columns + riftHeraldTakedowns + voidMonsterKill from challenges_json blob) - do NOT regress to 7-column. `VoidMonsterTests` + the route's `ObjEnrichmentTests` pin behavior. The `voidMonsterKill` key comes from Match-V5 `challenges` field via per-participant JSON parse - NOT from a denormalized SQL column; 100% coverage (200/200 sample); fail-soft to 0 on missing key. The other 3 challenges_json keys (`baronTakedowns`, `dragonTakedowns`, `epicMonsterSteals`, `turretTakedowns`) OVERLAP with existing SQL columns (baron_kills / dragon_kills / objectives_stolen / first_tower_kill+assist) so adding them would DOUBLE-COUNT - do NOT widen further without an overlap audit (Match-V5 `takedowns` = kills+assists vs SQL `kills` = last-hits only; semantically different). Void participation lift is MATCH-DEPENDENT: operator-involved INCREASES; teammate-only DECREASES; both behaviors validated (NA1_5560540832 dropped 0.364 -> 0.331). The aggregate effect across many matches calibrates over the postmortem cadence; do NOT pre-judge by single-match deltas. The `_challenges_objectives` helper parses JSON ONCE per row and returns a (herald, void) tuple - do NOT split into per-key helpers (would double-parse). `_PER_SPELL_CC_DURATIONS` registry now contains 53 entries / 44 champs of FIRST-ORDER CC at patch 16.10. Do NOT add slows (different math). Do NOT add conditional CC (Brand R 3rd-hit / TF W Gold Card / Tahm Q 3rd-stack / Lillia R dream-stack / Volibear Q terrain / Urgot R fear) without a conditional axis. Canonical DDragon ids: "Chogath" not "Cho'Gath"; "MonkeyKing" not "Wukong" (already in seed); other punctuation-stripped per `_portraitUrl` convention. The registry is DATA-only at 1.31.0; consumer math is unchanged from 1.30.0; future engine-side fight-sim consumer that READS the data for EHP-vs-CC blended scoring or per-spell CC interruption is owed before the seed has behavioral impact. Rell W (mount/dismount toggle) intentionally skipped - complicates rank progression; revisit when conditional-axis schema lift lands. The +17 stale ENGINE pin syncs are the s243/s246 facade-split precedent - value-pinned regression suite is the definitive behavior-equivalence proof; do NOT propose dropping the pin discipline. Slice A + Slice B are entirely DISJOINT surfaces (RC vs DS engine); merge order can be either-first without conflict. **Carries forward:** (a) Item 134 carry (a) Death's Dance Defy heal-on-takedown STILL deferred (don't-redo pinned; takedown-rate uncertain). (b) Item 134 carry (h) `_PER_SPELL_CC_DURATIONS` registry is now SEEDED 53 entries / 44 champs but ENGINE-SIDE FIGHT-SIM CONSUMER still future (the data flows through API but no EHP-vs-CC blended scorer exists yet). (c) Item 134 carry (c) Live ARAM/SR smoke STILL pending (operator must play). (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; the role_grades JSON will reflect void-aware obj_participation when the cron fires. (e) Item 130 carry (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` calibration operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) Item 134 carry (g) obj_participation widening is now CLOSED for the non-overlapping field; further widening of the 4 overlapping fields needs an overlap audit + semantic decision (replace SQL kills with Match-V5 takedowns OR keep current additive model; operator-gated). (h) Future engine consumer for `_PER_SPELL_CC_DURATIONS`: EHP-vs-CC blended scorer that computes net-CC-impact-on-EHP would be the natural follow-up; the seed is now broad enough (44 of 172 champs at first-order CC) to support a meaningful consumer when one ships. (i) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W) operator-gated. Frozen-file grant NOT used this session.

134. ✅ **2026-05-21 - item 134 2-slice parallel drain (herald_takedowns enrichment + _PER_SPELL_CC_DURATIONS registry seed) SHIPPED (2 worktree agents shipped + merged + pushed; 2 merge commits; ENGINE 1.29.0 -> 1.30.0; non-frozen; RC :8888 restarted clean pid 5220 -> 12492 last_reload_ok=True; DS :8893 restarted serves 1.30.0).** Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 2 worktree agents dispatched parallel on 2 disjoint slices composing on item 133 carry (b) + item 130 carry (b). Both merged + green + pushed. RC suite **2964 -> 3004 / 67 subtests / 0 failed** (+40); DS suite **3028 -> 3080 / 1 skipped / 1 xfailed / 1666 subtests passed** (+52). **Slice A `d3dc4f5` feat(coach) riftHeraldTakedowns enrichment in obj_participation (7-column, closes item 133 carry (b)):** Extends `core/obj_participation.py` `compute_obj_participation()` from 6-column to 7-column by reading `participants.challenges_json` Match-V5 blob, JSON-parsing per row, extracting `riftHeraldTakedowns` (integer, default 0 on missing key / malformed JSON / null blob), summing into BOTH numerator (operator's row) AND denominator (team total) so the ratio stays normalized. Coverage probe: 48/50 sampled matches across 29,724 participants have the key present in the blob - high coverage. Fail-soft on every degenerate shape (empty blob / unparseable JSON / missing key / legacy schema without challenges_json column). Module docstring + inline comment bumped 6-column -> 7-column. +27 tests across 3 files: `test_obj_participation.py` 29 -> 49 (+20 in NEW HeraldTests + extended FailSoftTests); `test_routes_post_game_rubric_obj.py` 7 -> 11 (+4 fixture-builder extended for challenges_json + herald kwargs); `test_postmortem_analyze.py` 65 -> 68 (+3 in NEW `_populate_match_a_herald` helper + schema widened with `challenges_json TEXT DEFAULT '{}'` column). **Live curl proves end-to-end:** `GET /api/post-game-rubric?match_id=NA1_5560540832` post-merge returns `ok=true role=ADC total_score=37.61 grade=C obj_participation=0.364`. Pre-Slice-A was 0.404 (item 133); post-Slice-A is 0.364 because the operator's team had 1 herald takedown that a TEAMMATE (not the operator) got - the denominator grew (9 -> 10) but the numerator stayed (4). Mathematically correct lift: when operator is herald-involved the score INCREASES; when teammate is herald-involved without operator the operator's share decreases. Both behaviors validated. Stale-PUUID match `NA1_5434861268` shows the inverse: pre 0/1=0.0; post 1/4=0.25 (operator got the herald, was under-credited entirely pre-enrichment). **Slice B `05f2475` feat(ds) _PER_SPELL_CC_DURATIONS registry seed (30 entries across 24 champs, closes item 130 carry (b) data-side):** Populates the EMPTY registry seam shipped item 130 Slice A `8dc3a6d` with 30 starter entries across 24 champions of first-order CC abilities (stuns / roots / suspensions / knock-ups / charms / sleeps / fear / silence - skips slows by design). Sources patch 16.10 tooltips via Riot wiki + cdragon champion JSONs. Champions seeded: Ahri E (charm), Annie R (stun), Ashe R (stun), Blitzcrank Q (pull-stun), Cassiopeia R (stun), Galio W/E/R (taunt + knock-ups), Leona Q/E/R (stun + root + center-stun), Lissandra R (stun), Lulu W (polymorph), Malzahar R (suppression), Maokai R (root), MonkeyKing/Wukong R (knock-up), Morgana Q (root), Nautilus Q/R (root + knock-up), Pantheon W (stun), Rakan W (knock-up), Renekton W (stun), Sejuani R (line-stun), Sona R (crescendo), Thresh Q (death-sentence), Veigar E (event-horizon edge), Vi Q/R (knock-ups), Yasuo R (Last Breath), Zoe E (sleep). 20 champs at 1 spell each; 2 champs (Nautilus, Vi) at 2 spells each; 2 champs (Galio, Leona) at 3 spells each. Wukong uses canonical DDragon id "MonkeyKing". Consumer math unchanged (engine fight-sim CC consumer still future per item 130 don't-redo); data flows through `AbilitySpellDps.cc_duration_s` + `cc_duration_post_tenacity` for API inspection. SR mode (tenacity=1.0) returns identity values; ARAM mode for the 17 modified-tenacity champs returns lengthened values (Zed 1.20 / Talon 1.20 / Akali 1.20 etc.). +52 tests in NEW `test_per_spell_cc_registry_seed.py`. 3 existing tests in `test_per_spell_cc_phase65.py` updated for the now-seeded state. +15 stale ENGINE pin syncs across DS test files (1.29.0 -> 1.30.0). ENGINE bumped 1.29.0 -> 1.30.0 in `__init__.py` with full changelog block. **Merge order:** A first (4 RC-side files, no conflict). B second (20 DS-side files, bumps ENGINE + syncs 15 stale pin sites; no conflict). 0 merge conflicts (A + B touched entirely disjoint surfaces). **Verified:** Wider RC **3004 passed / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart); DS suite **3080 passed / 1 skipped / 1 xfailed / 1666 subtests**. py_compile + ruff + ASCII clean across all touched files (0 non-ASCII added by either slice). DS :8893 restarted via taskkill PID 1680 + schtasks /Run RC-DaemonSlayer -> /health serves engine_version=1.30.0 patch=16.10.1 champions=172 items=705. RC :8888 restarted via `restart_trigger.txt` -> pid 5220 -> 12492 alive=True last_reload_ok=True mode=client. Live `/api/post-game-rubric` proved end-to-end with herald-aware obj_participation. **Don't-redo:** `compute_obj_participation` is now 7-column (6 SQL columns + riftHeraldTakedowns from challenges_json blob) - do NOT regress to 6-column; `HeraldTests` + the route's `ObjEnrichmentTests` pin behavior. The `riftHeraldTakedowns` key comes from Match-V5 `challenges` field via per-participant JSON parse - NOT from a denormalized SQL column; 96% coverage (48/50 in random sample); fail-soft to 0 on missing key; do NOT propose denormalizing it into a SQL column without operator approval (JSON blob is the canonical Match-V5 shape). The participant's PUUID match still uses operator's `puuid` from state.json first then `stale_puuid` fallback (item 124 pattern); herald enrichment does not change identity resolution. Herald enrichment lift is MATCH-DEPENDENT: when operator is herald-involved score INCREASES; when teammate is herald-involved without operator the operator's share DECREASES; both behaviors validated (NA1_5560540832 dropped 0.404 -> 0.364; NA1_5434861268 rose 0.0 -> 0.25); the aggregate effect across many matches calibrates over the postmortem cadence; do NOT pre-judge by single-match deltas. `_PER_SPELL_CC_DURATIONS` registry now contains 30 entries / 24 champs of FIRST-ORDER CC at patch 16.10 - do NOT add slows (different math); do NOT add conditional CC (Brand R 3rd-hit / TF Gold Card via W) without a conditional axis; Wukong is keyed under canonical DDragon id "MonkeyKing" (do NOT key under "Wukong"). The registry is DATA-only at 1.30.0; consumer math is unchanged from 1.29.0; future engine-side fight-sim consumer that READS the data for EHP-vs-CC blended scoring or per-spell CC interruption is owed before the seed has behavioral impact. ARAM tenacity-mult path: per-spell tenacity flow IS wired through (`cc_duration_post_tenacity`); SR mode identity preserved; ARAM mode for the 17 modified champs returns lengthened values; both verified in tests. The +15 stale ENGINE pin syncs are the s243/s246 facade-split precedent - value-pinned regression suite is the definitive behavior-equivalence proof; do NOT propose dropping the pin discipline. Slice A + Slice B are entirely DISJOINT surfaces (RC vs DS engine); merge order can be either-first without conflict. **Carries forward:** (a) Item 130 carry (a) Death's Dance Defy heal-on-takedown STILL deferred (don't-redo pinned; takedown-rate uncertain). (b) Item 130 carry (b) `_PER_SPELL_CC_DURATIONS` REGISTRY is now SEEDED but ENGINE-SIDE FIGHT-SIM CONSUMER still future (the data flows through API but no EHP-vs-CC blended scorer exists yet); the seed is a data lane lift only. (c) Item 130 carry (c) Live ARAM/SR smoke STILL pending (operator must play to verify Phase 6+6.5 + per-spell CC + obj_participation lifts surface live). (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; the role_grades JSON will reflect herald-aware obj_participation when the cron fires. (e) Item 130 carry (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` calibration operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) Future enrichment: `riftHeraldTakedowns` opens the door to also folding `voidMonsterKill` (Voidgrubs), `epicMonsterSteals`, `baronTakedowns`, `dragonTakedowns` from challenges_json into a 10+ column obj_participation; operator-gated whether to widen further given current 7-column adequacy. (h) Future engine consumer for `_PER_SPELL_CC_DURATIONS`: EHP-vs-CC blended scorer that computes net-CC-impact-on-EHP would be the natural follow-up; today the seed is decorative (data on wire, no math). Frozen-file grant NOT used this session.

133. ✅ **2026-05-21 - item 133 2-slice parallel drain (obj_participation enrichment + JSON override loader for post_game_rubric weights) SHIPPED (2 worktree agents shipped + 1 sweep-clean; 2 merge commits + 0 docs commits, pushed; non-engine; non-frozen; RC :8888 restarted clean pid 11420 -> 5220 last_reload_ok=True; no DS restart; no ENGINE bump).** Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 3 worktree agents dispatched parallel on 3 disjoint slices composing on item 132. Slices A + B shipped; Slice C sweep-clean (0 entries to flip - the 2026-05-21 sweep in item 132 Slice B `ded589c` had already caught every genuinely-stale entry; the agent grep-verified every non-shipped BACKLOG/ROADMAP entry against live source with 0 false positives). RC suite **2897 -> 2964 / 67 subtests / 0 failed** (+67: A=41 + B=38 net of overlap). **Slice A `79ff90c` feat(coach) obj_participation enrichment from rewind_history.db (closes item 132 carry (c)):** Both live consumers of `core/post_game_rubric.py` (`dashboard/routes_post_game_rubric.py` + `scripts/postmortem_analyze.py`) were passing `obj_participation_pct=0.0` always. Rubric weights it 0.5 ADC / 0.7 JG / 0.3 MID / 0.25 TOP / 0.1 SUP, so today's "0.0 always" under-scored ~5-10 pts on participation-heavy roles. NEW `core/obj_participation.py` (~120 LOC) single function `compute_obj_participation(conn: sqlite3.Connection, match_id: str, puuid: str) -> float`. Math: operator's row `dragon_kills + baron_kills + objectives_stolen + objectives_stolen_assists + first_tower_kill + first_tower_assist` (NUMERATOR) divided by sum of same columns across operator's team (DENOMINATOR). Clamp [0.0, 1.0]. Fail-soft (missing match / blank puuid / team total 0 -> 0.0, never raise). The route's `_fetch_match_and_row` writes obj_participation_pct to the row dict via the helper; the stats payload reads it. `iter_role_grades` SELECT in `scripts/postmortem_analyze.py` widened to include `p.match_id` + `p.puuid` so per-row `compute_obj_participation` can fire before `compute_role_grade`. +41 tests across 3 files: NEW `tests/test_obj_participation.py` (29 across 7 classes - ModuleSchema 6 + CoerceFloat 4 + Math 6 + Clamp 3 + FailSoft 8 + ReadOnly 1 + Ascii 1); NEW `tests/test_routes_post_game_rubric_obj.py` (7 across 2 classes - ObjEnrichment 6 + FailSoft legacy schema 1); EXTENDED `tests/test_postmortem_analyze.py` (+5 in NEW `ObjParticipationWireTests`; schema widened with 6 obj DEFAULT 0 columns + NEW `_populate_match_a_objectives` helper). **Live curl proves D -> C grade lift end-to-end:** `GET /api/post-game-rubric?match_id=NA1_5560540832` (operator's recent ADC match) post-merge returns `ok=True role=ADC total_score=38.01 grade=C obj_participation=0.404` vs pre-merge `total_score=33.97 grade=D obj_participation=0.0`. Exactly the 4-point lift on a 0.4-participation match (rubric weight 0.50 ADC * normalized 0.404/0.55 * multiplier 10 = ~3.7 component delta = ~4 total score points). Bumped one tier. The "5-10 pts" prediction from item 132 holds. **Slice B `b892519` feat(coach) JSON override loader for post_game_rubric weights (closes item 131 carry (a)):** Module docstring at `core/post_game_rubric.py:6-9` said "the operator can tune them later via per-role JSON overrides (the loader seam is documented but NOT built today)". Ships the loader. NEW `_OVERRIDES_PATH = Path("data") / "post_game_rubric_weights.json"` constant. NEW `_load_weights_overrides() -> dict[str, dict]` reads + JSON-parses + fail-soft. NEW `_apply_overrides(defaults, overrides) -> dict[str, RoleWeights]` does `dataclasses.replace(_DEFAULT_WEIGHTS[role], **fields)` per role; unknown role keys silently dropped (forward-compatible with future roles); unknown axis keys silently dropped (forward-compatible with future axes); negative weights floored to 0.0; non-numeric values dropped; non-dict role-values dropped. The `bool` defense (`isinstance(value, bool)` check before int/float) prevents `True/False` from silently coercing to 1.0/0.0 since bool is an int subclass in Python. Module-load step: `_DEFAULT_WEIGHTS = _apply_overrides(_DEFAULT_WEIGHTS, _load_weights_overrides())` runs ONCE at import. `compute_role_grade` + the route + the postmortem aggregation all read the post-override `_DEFAULT_WEIGHTS` transparently with ZERO call-site changes (the constant assignment is shadowing). `data/post_game_rubric_weights.json` added to `.gitignore` between `data/coaching/death_patterns.json` and the next entry (personal calibration data, mirrors the death_patterns convention). +38 tests across 2 files: NEW `tests/test_post_game_rubric_overrides.py` (35 across 5 classes - LoadOverridesTests 10 + ApplyOverridesTests 14 + IntegrationTests 5 + DocstringTests 5 + AsciiHygieneTests 1); EXTENDED `tests/test_post_game_rubric.py` (+3 in NEW `NoOverrideFileDefaultPreservationTests` - pins post-loader state against accidental override files in test env). The integration tests use `importlib.reload` carefully: capture pristine defaults in setUp, patch `_OVERRIDES_PATH`, run `_apply_overrides` directly + swap result into `pgr._DEFAULT_WEIGHTS`, tearDown restores (the naive `importlib.reload` inside `mock.patch.object` does NOT work because patch restores BEFORE the just-reloaded module reads the attribute). **Slice C - 0 entries flipped; sweep clean; no commit.** Agent grep-checked every non-shipped BACKLOG.md + ROADMAP.md "Open items" entry against live source. All open entries verified legitimately operator-gated or live-gated. BACKLOG L13 future EHP enemy-CC model already correctly annotated with 3 live consumers (items 126/127/130-A); L15 SR records still blocked on operator playing SR (0 SR records in ds_calibration.jsonl); L21 streaming vision / L22 mobile / L26 TFT vision / L30 ruff one-time / L34 SSE streaming / L38 .rofl confirmed dead-end / L40 OBS publisher all legitimately open. ROADMAP "Open items" yellow-circle entries (#89 live verify / build-ORDER UI / live ARAM / Phase 3 fixture steps 5-14 / Active Match step 5 / ADR-007 phase 3 / set_augment_intent) all operator/live-gated. The 2026-05-21 sweep in item 132 Slice B `ded589c` caught every genuinely-stale entry from items 122-132; this follow-up confirms zero residual drift. **Merge order:** A first (3 new files + 3 edits, no conflict). B second (1 new file + 3 edits, no conflict). 0 merge conflicts (A + B touched entirely disjoint files: A = core/obj_participation.py + dashboard/routes_post_game_rubric.py + scripts/postmortem_analyze.py + 3 test files; B = core/post_game_rubric.py + .gitignore + 2 test files). **Verified:** RC suite **2964 passed / 67 subtests / 0 failed** (+67 over 2897 baseline). py_compile + ruff + ASCII clean on all 9 touched + new test files. RC :8888 restarted via `restart_trigger.txt` (route module edit): pid 11420 -> 5220 alive=True last_reload_ok=True mode=client. Live curl `/api/post-game-rubric?match_id=NA1_5560540832` returned ok=True role=ADC total_score=38.01 (UP from 33.97 pre-merge) grade=C (UP from D) obj_participation=0.404 (UP from 0.0) - end-to-end wire verified. No DS restart (non-engine). No ENGINE bump. No frozen-file edits. Worktrees A + B unlocked + force-removed + branches deleted. Worktree C never created (sweep-clean outcome; the agent operated on the live repo path without committing). **Don't-redo:** `compute_obj_participation` reads operator's row + team sum in ONE SQL query (a row sum across team_id) - do NOT split into 2 queries. The numerator uses the operator's PUUID row directly; denominator uses team_id from that row. PUUID-based identification is correct (matches the route's PUUID + stale_puuid fallback pattern from item 124). The 6 obj columns counted: dragon_kills, baron_kills, objectives_stolen, objectives_stolen_assists, first_tower_kill (1 if true), first_tower_assist (1 if true). These 6 columns are the approximation - it under-credits non-tracked objectives (Rift Herald per-2026 model already counted via dragon_kills aggregate? NO - herald is not in this set; future enrichment could add herald_takedowns if Match-V5 exposes it). The clamp [0, 1] handles the edge where operator alone got every team objective. The JSON override file `data/post_game_rubric_weights.json` is GITIGNORED (personal calibration data, mirrors death_patterns.json convention). To use: drop a JSON in that path with per-role partial overrides. The module loads it once at import; restart RC to pick up new values. `dataclasses.replace(_DEFAULT_WEIGHTS[role], **fields)` is the canonical mutation path on the frozen RoleWeights; unknown axis keys are silently dropped (NOT KeyError'd) so future Riot grading-rubric changes don't break older operator override files. The `bool` defense in `_apply_overrides` is LOAD-BEARING - Python `bool` is `int` subclass; `isinstance(value, bool)` MUST come BEFORE `isinstance(value, (int, float))`. Drop bools silently (calibration weights should not be true/false). Both consumers (route + postmortem) now read REAL obj_participation - do NOT regress to 0.0 in either site. Pinned by `ObjEnrichmentTests::test_obj_enrichment_lifts_total_score` (route) + `ObjParticipationWireTests` (postmortem). Slice C clean outcome is the valid result; do NOT re-pitch a sweep next session. The `feedback_backlog_path_stale_check` methodology is now durable: grep paths first, only flip if shipped, sweep-clean is a legitimate outcome. **Carries forward:** (a) Item 131 carry-forward (a) is now CLOSED via Slice B. Item 132 carry-forward (c) is now CLOSED via Slice A. Carry-forwards from item 132 (a) ADR-010 Arena S2 26.09 PBE-gated + (b) item 130 carries unchanged + (d) item 129 / 128 / 127 / 126 / 125 unchanged. (b) The 6-column obj_participation approximation may under-credit modes where Rift Herald or grub/Hextech objectives carry weight; future enrichment could add `herald_takedowns` if the column lands in rewind_history.db via Match-V5 timeline join. (c) Personal calibration via `data/post_game_rubric_weights.json` is operator-gated - drop a JSON with the per-role partial axes when 100+ matches accumulate and the operator wants to recalibrate vs Riot's defaults. No code action owed. (d) The grade lift on the operator's NA1_5560540832 match was exactly D -> C (33.97 -> 38.01) - one tier. Other matches in postmortem aggregation may flip similarly when the cadence runs next 2026-05-24 04:15. Verify LastTaskResult=0 next session + check that role_grades JSON v2 reflects the obj_participation bumps. Frozen-file grant NOT used this session.

132. ✅ **2026-05-21 - item 132 3-slice parallel drain (post_game_rubric live wire + BACKLOG stale-sweep + postmortem role-grade aggregation) SHIPPED (3 worktree agents + 3 merge commits `cdb0006` `79a956b` `bc055c2`, pushed; non-engine; non-frozen; RC :8888 restarted clean pid 8960 -> 11420 last_reload_ok=True; no DS restart; no ENGINE bump).** Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 3 worktree agents dispatched parallel on 3 disjoint slices composing on item 131 Slice A `core/post_game_rubric.py` (ZERO live consumers at session start; now 2 live consumers + stale-sweep). RC suite **2811 -> 2897 / 67 subtests / 0 failed** (+86: A=29, C=56, +1 indirect). **Slice A `2015d39` feat(coach) post_game_rubric live wire (FIRST live consumer of item 131):** NEW `dashboard/routes_post_game_rubric.py` (250 LOC) GET `/api/post-game-rubric?match_id=<id>` returns `{ok, match_id, role, total_score, components, percentile_grade, weights_used, elapsed_ms, cached}`. Reads operator's row from `data/rewind_history.db` participants table by PUUID from `data/rewind_catchup.state.json` (`puuid` key first, `stale_puuid` fallback for PUUID rotation per item 124). Calls `compute_role_grade(stats, role)` with `stats={kills, deaths, assists, cs, game_time_s, vision_score, damage_dealt_to_champions, obj_participation_pct=0.0}` (no obj_participation enrichment yet - code noted as future from objectives_stolen + dragon + baron counts); `role` = participant `team_position` (BOTTOM/JUNGLE/UTILITY canonicalized via `_normalize_role`). 5-min TTL cache keyed on match_id (mirrors `routes_post_game_wpa` pattern). Fail-soft: 400 missing/blank match_id / 404 unknown match / 503 missing rewind_history.db / 503 missing state.json. Registered in `dashboard/_dispatch.py` via GET_ROUTES wire (NOT a `register()` callable - followed existing pattern). Frontend chip mounted as 7th column of `.lm-hero-identity` (`#lm-hero-role-grade`) parallel to item 115's `#lm-hero-score`: NEW `_setHeroRoleGrade(m)` helper in `web/js/panels/last_match.js` (+44 LOC) fetches `/api/post-game-rubric?match_id=<m.matchId>` post-enrichment, renders rounded `total_score` + `percentile_grade` tier label (S+/S/A/B/C/D), hidden until enrichment lands. NEW CSS (+57 LOC) for `#lm-hero-role-grade` with 6 data-tier color variants (S+/S/A=good, B=warn, C/D=bad) + grid widening. +30 tests across 2 NEW files: `tests/test_routes_post_game_rubric.py` 17 + `tests/test_last_match_role_grade_dom.py` 13. **Live curl verified end-to-end:** `GET /api/post-game-rubric?match_id=NA1_5560540832` (operator's recent ADC match) returns `ok=true role=ADC total_score=33.97 percentile_grade=D components={kda:0.77, cs_per_min:0.90, obj_participation:0.0, vision:0.6, dpm:1.13} weights_used={kda:2.1, cs_per_min:0.85, obj_participation:0.5, vision_score:0.3, damage_per_min:0.85} elapsed_ms=1`. **Slice B `ded589c` docs(backlog) stale-sweep per `feedback_backlog_path_stale_check`:** BACKLOG.md only (4 lines, 2 ins / 2 del). **2 entries flipped:** (1) Dead Man's Plate Momentum stacks-schema line 14: VERIFIED STALE via `grep stack_ramp_seconds _effects_types.py _effects_data.py` -> `223742 Shipwrecker` proc encoded; flipped to `✅ SHIPPED 2026-05-21` (CLAUDE.md item 122, commit `ce563c4`; ENGINE 1.25.0 -> 1.26.0). (2) Future EHP enemy-CC model line 13: reframed to reflect 3 live consumers now shipped (`aram_tenacity_line` item 126 / `enemy_aram_tenacity_line` item 127 / `_PER_SPELL_CC_DURATIONS` engine seam item 130 Slice A `8dc3a6d`) - engine fight-sim CC populator remains the only future work. **Verified-current (no flip):** 5 SHIPPED-in-parens BACKLOG research entries (LoLytics WPA / draft-elo top_contributions / augment_formula_eval / _hextech_drakes_by_team / ban_suggest_toggle.js) - source files all present. ROADMAP.md top entries already marked SHIPPED post-2026-05-21 sweep. 4 open ROADMAP items (Active Match step 5 / Phase 3 fixture flow / ADR-007 phase 3 / live ARAM Mayhem test) legitimately operator-gated or live-gated. No new stale items discovered. ASCII clean. **Slice C `7a2450b` feat(coach) role-grade aggregation in postmortem + personal-context card extension (SECOND live consumer of item 131):** Extended `scripts/postmortem_analyze.py` (+163 LOC) computes role grade per match alongside death patterns; NEW top-level key `role_grades` in `data/coaching/death_patterns.json` (schema bumped v1 -> v2; v1 files return `{}` from loader so route omits the block - backward-compatible). Iterates over participants matching operator's `puuid` + `stale_puuid` from state.json; skips event-mode rows with empty `team_position` (ARAM/Arena/etc.); aggregates by_role (count + median + tier distribution S+/S/A/B/C/D) + overall (cross-role aggregate). Atomic write via tempfile.mkstemp + os.replace (already used in script). Extended `core/death_patterns_loader.py` (+24 LOC) with NEW `role_grades_summary(path=None) -> dict` - returns the role_grades dict or `{}` on missing/malformed/empty/v1. Extended `dashboard/routes_personal_context.py` (+25 LOC) to add `role_grades` block to GET response; 60s MTIME-keyed cache unchanged. Extended `web/js/panels/personal_context.js` (+100 LOC) + `web/css/panels/personal_context.css` (+155 LOC) to render NEW card BELOW existing top-3 death-pattern cards: header "Recent role grades", big numeric overall median score, tier strip (S+/S/A/B/C/D) with count chips, per-role mini-rows (only roles with count > 0), card hidden when `overall.count == 0` (empty state). **+56 tests across 4 test files:** `test_postmortem_analyze.py` 33 -> 60 (+27) / `test_death_patterns_loader.py` 6 -> 12 (+6) / `test_routes_personal_context.py` 12 -> 17 (+5) / `test_personal_context_panel_dom.py` 20 -> 38 (+18). **Live `--dry-run` sample against operator's data** (puuid + stale_puuid, 624 matches scored): overall median=41 tier_dist {S+:0, S:8, A:56, B:164, C:159, D:237}; by_role ADC=177 (median 41) / SUP=79 (57) / JG=52 (32) / MID=80 (44) / TOP=236 (37). Cadence: RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 picks up v2 automatically. **Merge order:** B first (docs-only, no conflict); A second (new route + frontend chip, no conflict); C third (1 auto-merge in `web/index.html` between Slice A's `lm-hero-role-grade` 7th-column mount + Slice C's personal-context card extension; Git's ort strategy resolved cleanly - both slices touched DIFFERENT subtrees of index.html). **Verified:** RC suite **2811 -> 2897 passed / 67 subtests / 0 failed** (+86); py_compile + ruff + ASCII clean across all 13 touched + 4 new test files (0 non-ASCII added by any slice; BACKLOG.md / Markdown ASCII drift probe clean). RC :8888 restarted via `restart_trigger.txt`: pid 8960 -> 11420 alive=True last_reload_ok=True mode=client. Live curl proved route end-to-end (see Slice A above). No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** post_game_rubric NOW has 2 live consumers (route + postmortem aggregation); item 131 Slice A's `core/post_game_rubric.py` is source-of-truth - do NOT duplicate rubric math in either consumer. `compute_role_grade(stats, role)` API is the seam. The route reads PUUID from state.json's `puuid` key (NOT `current_puuid` - spec assumption didn't match actual key); falls back to `stale_puuid` for PUUID rotation per item 124. The route stats payload uses `game_time_s` + `damage_dealt_to_champions` (rubric's actual API contract per `core/post_game_rubric.py:212-243`), NOT spec's `game_duration_minutes` + `damage_dealt` - module is source-of-truth. `obj_participation_pct=0.0` INTENTIONAL today (no direct DB column; future enrichment from objectives_stolen + dragon + baron noted in code comment); component scores correctly without it, operator just under-scores obj_participation axis (0.5 weight ADC / 0.7 JG) by ~5-10 pts on participation-heavy roles. JSON schema bumped v1 -> v2 (NOT in-place extension); v1 files return `{}` from loader, route omits the block - backward-compatible; cadence cron auto-upgrades to v2 on first 2026-05-24 04:15 run. Personal-context card hides when `overall.count == 0`; do NOT render strip with zeros (clutters panel for fresh operators). The 7th-column chip on `.lm-hero-identity` widened the grid; do NOT shrink back - both `#lm-hero-score` (item 115) + `#lm-hero-role-grade` (item 132) coexist. BACKLOG entry for Dead Man's Plate is ✅ SHIPPED (item 122); do NOT re-list as open. `feedback_backlog_path_stale_check` caught both stale entries this session; the workflow (grep paths before recommending NOW-lane items) is durable. **Carries forward:** (a) Item 131 carries-forward unchanged: ADR-010 Arena S2 26.09 PBE-gated; post_game_rubric weight calibration via custom `weights` kwarg or future JSON override loader; 5 role weight vectors are starting calibration only. (b) Item 130 carries unchanged: DD Defy heal-on-takedown deferred / `_PER_SPELL_CC_DURATIONS` registry empty (forward-marker) / live ARAM/SR smoke pending / RC-PostmortemAnalyze first run 2026-05-24 04:15 (LastTaskResult=0 verification next session) / `_MISSING_HP_SHARE_FOR_HEALS` calibration operator-gated. (c) `obj_participation_pct` enrichment from objectives + dragon + baron DB columns is the natural follow-up - rubric weights it (0.5 ADC / 0.7 JG default) so today's "0.0 always" under-scores by ~5-10 points on participation-heavy roles. Operator-gated (depends on Match-V5 enrichment cadence). (d) Item 129 / 128 / 127 / 126 / 125 carries unchanged. Frozen-file grant NOT used this session.

131. ✅ **2026-05-21 - wakeup+backlog 3-slice parallel drain SHIPPED (3 worktree agents + 3 merge commits `be14388` `786a18c` `ed8fd4d`, pushed; non-engine; non-frozen; no RC restart; no DS restart; no ENGINE bump).** Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 3 agents dispatched parallel on 3 disjoint slices. All merged + green + pushed. **Slice A `be14388` feat(coach) post_game_rubric (BACKLOG FUTURE entry):** NEW `core/post_game_rubric.py` (279 LOC) - per-role grading rubric composing alongside `core/post_game_score.py` (PGR S2 WPA framework). 5 role profiles (ADC/SUP/JG/MID/TOP) with public-source-derived starting weights (ADC 2.1 KDA + 0.50 obj-participation + 0.85 CS-per-min; SUP 2.5 KDA + 1.5 vision + 0.00 CS-per-min; JG 1.8 KDA + 0.70 obj-participation; MID 2.0 KDA + 0.85 CS-per-min; TOP 1.9 KDA + 0.80 CS-per-min). `RoleWeights` frozen dataclass + `_DEFAULT_WEIGHTS: dict[str, RoleWeights]` + `_ROLE_BASELINES: dict[str, dict[str, float]]` per-role median baselines at ~32 min. `compute_role_grade(stats, role, weights=None) -> dict` returns `{role, total_score, components, percentile_grade}` with bands S+/S/A/B/C/D at 85/75/65/50/35. `_normalize_role` canonicalizes BOTTOM/BOT -> ADC + JUNGLE -> JG + SUPPORT -> SUP + None/unknown -> MID. Fail-soft on missing stats keys. Total-score multiplier set to 10x (not 50x as one might read from the spec) so a 1.0-normalized profile across all axes maps to ~46 (mid B) and a +2x outlier maps to ~92 (S via 100 clamp); the test's "solid ADC" case (5/2/8 22min cs 160) lands in [40, 70] as required. +31 tests across 6 classes in NEW `tests/test_post_game_rubric.py` (304 LOC). RC suite 2780 -> 2811. **Slice B `786a18c` docs(adr) ADR-010 Arena S2 (BACKLOG FUTURE entry):** NEW `docs/adr/ADR-010-arena-s2-augment-leveling.md` (184 lines) - pre-stage doctrine for Arena S2 Augment Level-Up + Crafting Round (patch 26.09 expected window). Pure docs; composes nothing today; gives the operator an anchor for the 26.09 work when PBE confirms the schema. Sections: Status (Accepted, pre-stage doctrine 2026-05-21) / Context (per-augment Level-Up 1 -> 2 -> 3 mid-match via round 8 Crafting Round + Keystone tier at max level; replaces single-tier Silver/Gold/Prismatic; cherry-augments OCR breaks simultaneously) / Decision (pre-stage `level: int = 1` keyword on `compute_augment_stats(augment, level=1, ...)` identity-at-level-1 boundary) / Pre-stage seams (NOT today: `Augment.from_record` reads `levels[]` if present; `dataValues_by_level: dict[int, list]`; `_AUGMENT_STAT_OVERLAYS` registry gains optional `(augment_id, level)` keying; `test_arena_s2_augment_leveling.py` exercising level-1 identity + level-2/3 deltas) / Rationale / Out-of-scope (Crafting Round mechanic + Keystone visual + cherry-augments OCR multi-level detection + build planner per-level projection - each gets its own scope when 26.09 ships) / Watch for (PBE 26.09 patch notes + cdragon `raw.communitydragon.org/latest/cdragon/arena/en_us.json` schema diff + Riot dev-portal S2 ship date announcement) / Trade-off (fail-soft pre-PBE-confirm: overlays resolve to level-1=S1 values, build planner under-credits leveled augments but does not crash) / Linked work (composes ON `agents/daemon_slayer/augments.py` + `agents/daemon_slayer/augment_formula_eval.py` shipped item 112 `21657eb`; references `STAT_GRANT_CALC_KEYS` empty seam pattern from item 112 as the methodology mirror for pre-stage + empty registry). **Slice C `ed8fd4d` docs WAKEUP archive (session-workflow compliance):** WAKEUP_NOTES had drifted to 542 lines with 7-8 entries; per session-workflow rule "keep last 2-3 sessions at full fidelity; archive older". This sweep restores compliance. Kept inline (top 3): Phase 6.5 3-slice drain (item 130) / EHP Phase 6 healing throughput (item 129) / DS 3-slice drain (items 126/127/128). Archived verbatim to `docs/history_notes.md` (5904 -> 6378 lines; +474 LOC; no rewrites per ROADMAP-95 "archive don't delete"): postmortem follow-up 4-slice drain (item 125), ADR-007 phase 2 postmortem-analyze (item 124), headless-upgrade ASCII drift catch (item 123), BACKLOG drain aram_tenacity + Dead Man's stacks (item 122), HEADLESS UPGRADE PASS 3+4 (item 121), and 9 older entries on the same WAKEUP page below the prior archive line. WAKEUP_NOTES 542 -> 73 lines (-469). Lead-in archive sentence updated. **Merge order:** A first (no conflict; new files only); B was already on main when the agent ran (committed directly into the main worktree rather than to a worktree branch - the agent reported "Worktree initially reported worktree-agent-aaec52e3bf1b16384 but resolved to main for the commit" - benign side effect; the commit IS on main); C third (no conflict; touches different files). **Verified:** wider RC suite **2811 passed** / 67 subtests / 0 failed (+31 over 2780 baseline = exactly Slice A); py_compile + ruff + ASCII clean on all 5 touched files (core/post_game_rubric.py / tests/test_post_game_rubric.py / docs/adr/ADR-010-arena-s2-augment-leveling.md / WAKEUP_NOTES.md / docs/history_notes.md). DS suite untouched. No DS restart. No RC restart. No ENGINE bump. No frozen-file edits. Worktree cleanup: 2 worktree branches unlocked + force-removed + branch -d cleaned (Slice B was already on main so no worktree to clean for that slice). **Don't-redo:** `core/post_game_rubric.py` is a SIBLING of `core/post_game_score.py` - composes alongside, NOT duplicates; do NOT collapse the two modules. The 5 role weight vectors are STARTING calibration values from a single public-source rubric reference (Unrankedsmurfs); the operator can tune via custom `weights` kwarg OR a future JSON override loader (seam documented in source but NOT built today - single-source-of-truth on `_DEFAULT_WEIGHTS`). Role baselines (KDA / CS-per-min / obj-participation / vision / damage-per-min) are at ~32 min per-game medians per role; the `_normalize_role` aliasing covers Match-V5 lane variants (BOTTOM / JUNGLE / SUPPORT canonical names). Total-score multiplier 10x is calibrated so a 1.0-normalized profile sits ~46 (mid B) and a +2x outlier saturates near 100 (S via clamp); do NOT change the multiplier without retuning the percentile bucket thresholds + all 31 tests. The fail-soft contract on missing stats keys is LOAD-BEARING - `compute_role_grade` MUST NOT raise on partial data (Match-V5 event modes can ship null fields). `RoleWeights` is frozen; `dataclasses.replace` is the supported mutation path. ADR-010 is a PRE-STAGE doctrine document - the code change happens WHEN 26.09 PBE confirms the schema, NOT today; do NOT pre-ship `level=1` axis speculatively before PBE confirms the `levels[]` field shape on `Augment.dataValues`. The ADR explicitly out-of-scopes the Crafting Round mechanic + Keystone visual + cherry-augments OCR multi-level detection + build planner per-level projection - each gets its own ADR or scope when 26.09 lands; do NOT bundle. The empty-registry seam pattern (mirror of item 112 `STAT_GRANT_CALC_KEYS`) is the methodology - production behavior byte-identical at empty registry; future patches populate when consumers ship. WAKEUP archive used the ROADMAP-95 "archive don't delete" convention with VERBATIM copy (zero rewrites / zero compression); the archive entries in `docs/history_notes.md` are byte-equal to what was in WAKEUP. The agent's deliberately-named multi-agent-parallel-worktree pattern is now the durable template for future wakeup/backlog drains (3 worktree agents on disjoint slices + orchestrator merge sequentially + 1-3 merge commits visible in git log + sync living docs in same final commit). **Carries forward:** (a) `core/post_game_rubric.py` has NO live consumer in the dashboard or coach prompts yet - the module ships pure-Python composable; future PGR S3+ work (the aggregator-G-style Post Game Review reframe per ROADMAP item 70 shipped S2-S5 already) can consume this when per-role grading surfaces are wanted. (b) Arena S2 26.09 is operator-gated on PBE schema confirmation - check PBE patch notes + cdragon arena/en_us.json schema diff at the patch flip; the ADR is the anchor. (c) Item 130 carries unchanged: DD Defy heal-on-takedown deferred / `_PER_SPELL_CC_DURATIONS` registry empty / live ARAM/SR smoke pending / RC-PostmortemAnalyze first run 2026-05-24 04:15 / `_MISSING_HP_SHARE_FOR_HEALS` calibration. (d) Item 129 / 128 / 127 / 126 / 125 carries unchanged. Frozen-file grant NOT used this session.

130. ✅ **2026-05-21 - Phase 6.5 3-slice parallel drain SHIPPED (3 worktree agents + 3 merge commits `3c76c04` `7d9d283` `98deb10`, pushed; ENGINE 1.28.0 -> 1.29.0; non-frozen; DS :8893 restarted serves 1.29.0; RC restart not owed - DS engine slice).** Operator "continue with the wakeup_notes, multi agent parallel runs". 3 worktree agents dispatched parallel against the 3 Phase-6.5 carries-forward from item 129 (DD takedown-rate deferred per don't-redo - takedown probability genuinely uncertain). All 3 merged + green + pushed. DS suite **2962 -> 3028** (+66); wider RC **2780 / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart). **Slice A `8dc3a6d` per-spell CC duration extractor seam (item 129 carry (a)):** NEW `AbilitySpellDps.cc_duration_s: tuple[float, ...] = ()` + `cc_duration_post_tenacity: tuple[float, ...] = ()` fields. NEW module-level `_PER_SPELL_CC_DURATIONS: dict[str, dict[str, tuple[float, ...]]] = {}` registry seam in `ability_dps.py` (LEFT EMPTY - forward-marker pattern mirroring s112 `STAT_GRANT_CALC_KEYS`). Consumer in `compute_ability_dps` per-spell loop reads from registry, applies `effective_cc_duration(base, tenacity_mult)` (direct import from `.ehp` - no circular). Production behavior byte-identical at empty registry. 2nd consumer of the `effective_cc_duration` helper shipped 1.25.0 (item 122) which previously had zero consumers; this slice is forward-marker for a future EHP-vs-CC blended scorer. +30 tests in NEW `test_per_spell_cc_phase65.py`. **Slice B `0163a34` SV amp on Phase 1.5 shields (item 129 carry (b)-1):** Closes the Phase 6 deliberate omission "Spirit Visage amp on Phase 1.5 SHIELDS - Riot tooltip 'increases all heal AND shielding +25%' but engine ships heal-pipeline amp only". heal_amp_mult now multiplies shield_any/phys/mag/true at the EHP-math top of damage stack (SIBLINGS, NOT nested with heal_total amp). NEW `EhpResult.shield_amp_mult: float = 1.0` field surfaces the multiplier alongside `heal_amp_mult`. Shields stay PRE-amp in their dedicated fields for transparency (matches `heal_item_total`/`heal_lifesteal` pre-amp convention). format_table + to_dict + notes updated. SpiritVisageAmpTests pinning test FLIPPED from `does_not_amp_phase15_shields` to `amps_phase15_shields_at_125`. +22 tests in NEW `test_ehp_shield_amp_phase65.py` (NoDoubleAmpTests x4 + ShieldAmpStackingTests + EhpResultShieldAmpFieldsTests). Math: Sett L11 + Sterak (shield_any=240 baseline) + SV (heal_amp_pct=0.25) -> shield_any=480 (compound: SV bumps bonus_hp from 400 to 800 via the 400 HP stat, so Sterak's 60% bonus_hp ANY shield doubles from 240 to 480) + shield_amp_mult=1.25 -> physical_ehp lift = +1324.2 EHP (compound of HP bump + amp). **Slice C `3a86cf9` Sundered Sky missing-HP heal piece (item 129 carry (b)-2):** Closes the Phase 6 deliberate omission "Sundered Sky 6% missing-HP additive - requires a current-HP-share assumption distinct from the steady-state full-HP convention". NEW `ItemHeal.missing_hp_pct: float = 0.0` field + `resolve_magnitude(missing_hp=...)` keyword. Sundered Sky 6610 + Arena 226610 wired with `missing_hp_pct=0.06`. NEW `_MISSING_HP_SHARE_FOR_HEALS = 0.5` module constant in `ehp.py` - Phase 6.5 mid-fight HP-share assumption (50% missing HP) distinct from the full-HP steady-state convention used elsewhere; the ONE deliberate departure in this slice; operator-tunable single constant. `_collect_heals` threads `missing_hp` through; compute_ehp derives `missing_hp = total_hp * _MISSING_HP_SHARE_FOR_HEALS`. +14 tests in extended `test_ehp_heal_phase6.py` (8 MissingHpAdditiveTests + 6 SunderedSkyMissingHpTests). 2 existing assertions rebaselined for the new mid-fight magnitudes (SunderedSkyHealTests + SpiritVisageAmpTests). Math: Aatrox L11 (HP=2050.35) + Sundered Sky melee at mid-fight (missing_hp=1025.175) -> heal piece = 103.875 + 0.06 * 1025.175 = 165.39 (vs Phase 6 baseline 103.875). Live `compute_ehp` returns 165.39 exact. **Merge order + conflict resolution:** A first (bumps ENGINE 1.28.0 -> 1.29.0 + syncs 15 stale pin sites across 14 DS test files); B second (auto-merge clean - touches ehp.py shield section + 1 existing pinning test + 1 new test file); C third (1 conflict in ehp.py docstring - both B's ENGINE 1.29.0 SV-amp paragraph + C's "SHIPPED in Phase 6.5" entry kept; resolved by combining + 3 historical references "ENGINE 1.28.0+" -> "ENGINE 1.29.0" in `_effects_types.py:311` + `test_ehp_heal_phase6.py:335,399`). Merge commit `98deb10`. **Verified:** DS suite **3028 passed / 1 skipped / 1 xfailed / 1663 subtests** (+66 over 2962 baseline: 30+22+14); wider RC suite **2780 passed / 67 subtests / 0 failed**; py_compile + ruff + ASCII clean across all touched files. DS :8893 restarted via taskkill PID 7540 + schtasks /Run RC-DaemonSlayer -> /health serves engine_version=1.29.0 patch=16.10.1. Worktrees + 3 worktree branches unlocked + force-removed + branch -d cleaned. **Don't-redo:** `_PER_SPELL_CC_DURATIONS` STAYS EMPTY at 1.29.0 (forward-marker; mirrors s112 `STAT_GRANT_CALC_KEYS` pattern); production byte-identical to pre-slice; future patches populate the registry when a CC consumer (fight-sim / coach-prompt / EHP-vs-CC blended scorer) needs the data. The SV-on-shields amp uses the SAME `_total_heal_amp` value as heals (today: Spirit Visage 1.25); the two amps are SIBLINGS at EHP-math top of damage stack NOT nested (heal_total amped once by heal_amp_mult; shield_any amped once by shield_amp_mult; products added); `NoDoubleAmpTests` x4 pin this. `EhpResult.shield_amp_mult` exposes the multiplier as a SEPARATE field from `heal_amp_mult` so future heal-only or shield-only amp items stay representable - do NOT collapse to one field. `_MISSING_HP_SHARE_FOR_HEALS = 0.5` is the Phase 6.5 mid-fight convention - a SINGLE constant operator can tune; the EHP scorer's normal convention is full-HP steady-state; this constant is the ONE deliberate departure - do NOT vary per-item or thread an item-specific override. Sundered Sky's missing-HP piece is composed BEFORE the ranged_modifier (ranged user halves base AD piece AND missing-HP piece together). 2 existing assertions in `SunderedSkyHealTests::test_aatrox_l11_sundered_sky_melee_heal` (range 100-110 -> 160-170) and `SpiritVisageAmpTests::test_sundered_sky_plus_spirit_visage_amped` (reframed for amped-build missing_hp) re-baselined NOT monkeypatched - the always-on `_MISSING_HP_SHARE_FOR_HEALS` is what the engine ships. Death's Dance Defy heal-on-takedown DELIBERATELY DEFERRED (Phase 6.5 omission carry-forward); the takedown-rate assumption is genuinely uncertain; do NOT pre-wire with a constant probability. The multi-agent parallel-worktree merge pattern (one engine bump at first merge + ENGINE pin syncs; subsequent merges leave at the bumped version; conflicts at the docstring layer expected when multiple agents document into the same module) is durable. **Carries forward:** (a) Death's Dance Defy heal-on-takedown still queued (needs takedown-rate model; operator-gated whether to ship at all - could over- or under-credit depending on real-game takedown frequency). (b) `_PER_SPELL_CC_DURATIONS` registry empty - the engine-side fight-sim consumer that actually reads it is FUTURE work; today the field is set and propagated through to_dict but no math depends on it. (c) Live ARAM/SR smoke STILL pending - operator must play a real game to verify Phase 6 + 6.5 healing/shielding reflects in EHP rankings live. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` may calibrate (operator can tune if real-game data suggests a different mid-fight share). (f) item 129 / 128 / 127 / 126 / 125 carries forward unchanged. (g) DS audit remains halted at engine 1.29.0 - the THREE bumps in this slice (1.28.0 -> 1.29.0 covers all 3 features at once, no per-feature granular bump) are deliberate Phase-6.5 feature additions NOT Meraki drift; the no-change DS audit streak (DS audit halt) is unrelated. Frozen-file grant NOT used this session.

129. ✅ **2026-05-21 - EHP Phase 6 healing throughput SHIPPED (1 commit `5018e9f`, pushed; ENGINE 1.27.0 -> 1.28.0; non-frozen; DS :8893 restarted serves 1.28.0; RC restart not owed - non-coach engine slice).** Operator AskUserQuestion-picked "EHP Phase 6 healing throughput" from a 4-option fork (Phase 6 / per-spell CC engine seam / RC-PostmortemAnalyze pre-flight / housekeeping pivot). The natural follow-up per item 128 carry-forward (a). Closes the `ehp.py:23` deliberate Phase-1 omission "Healing throughput (lifesteal, Spirit Visage amp) - fits Phase 6". Three contributions feed an EHP heal pool, value-additive at the top of the damage stack (heals don't discriminate by damage type, so the heal pool acts like an ANY shield): **(a) item-passive heals via NEW `ItemHeal` dataclass in `_effects_types.py` (~95 LOC) + NEW `ItemEffect.heal: ItemHeal | None = None` field:** `flat`, `base_ad_scaling`, `bonus_hp_scaling`, `bonus_ad_scaling`, `ranged_modifier`, `note`. `__post_init__` rejects negative ranged_modifier. NEW method `resolve_magnitude(base_ad, bonus_hp, bonus_ad, is_ranged)` composes flat + per-stat scalings, applies ranged_modifier, floors at 0. NO level-lerp axis (Phase 6 heal items in scope are stat-scaled directly). Sundered Sky 6610 / Arena 226610 Lightshield Strike: 100% base AD melee / 50% base AD ranged per one-trigger-per-fight (10s CD per target > typical 6s fight window; missing-HP additive intentionally NOT modeled). **(b) lifesteal-derived heal via NEW `_lifesteal_heal(lifesteal_pct, ad, attack_speed, fight_window_s=6.0)` helper:** formula `lifesteal_pct * ad * attack_speed * fight_window_s`. Pre-mitigation approximation matching the EHP scorer's no-enemy-pen Phase-1 posture (over-credits by ~30-40% vs 60-90 armor targets - documented as a Phase 6 boundary). NEW `_FIGHT_WINDOW_S = 6.0` module constant matches the engine's existing sustained/burst boundary. **(c) multiplicative heal amp via NEW `ItemEffect.heal_amp_pct: float = 0.0` field:** Spirit Visage 3065 / Arena 223065 Boundless Vitality = 0.25 (+25% to all self-heal/regen). NEW `_total_heal_amp(item_ids) -> float` helper returns the `(1 + heal_amp_pct)` product across all items (multiplicative stacking per buff-system doctrine batch 14). Applied as `(heal_item_total + heal_lifesteal) * heal_amp_mult -> heal_total`. **Bloodthirster 3072 / Arena 223072 Ichorshield** ships in the Phase 1.5 ItemShield pipeline (NOT ItemHeal): `shield=ItemShield(damage_type=ANY, flat=165, level_lerp_low=9, level_lerp_high=18, level_lerp_high_value=315)` from Meraki 16.10.1 "165 + (315-165)/10*(x-1) for 1;9 to 20 by 1". Full-cap steady-state assumption (overheal builds the shield between fights at base/walking; consistent with full-stack approximations). **NO `unique_passive_key="lifeline"`** - BT's Ichorshield is a DISTINCT unique passive (overheal accrual vs low-HP trigger) and stacks with any single lifeline shield in real builds; `BTIchorshieldTests::test_bt_no_lifeline_unique_passive` + `BTPlusLifelineStacksTests` both pin. **EHP scorer extension `agents/daemon_slayer/ehp.py` (~120 LOC added):** NEW `_collect_heals(item_ids, base_ad, bonus_hp, bonus_ad, is_ranged) -> (total, sources)` walks ItemEffect.heal entries, resolves magnitudes, aggregates. EhpResult gains 5 new fields: `heal_item_total`, `heal_lifesteal`, `heal_amp_mult`, `heal_total`, `heal_sources: tuple[tuple[str, float], ...]`. `compute_ehp` derives base_ad from `resolved.base_stats["ad"]`, calls `_collect_heals` + `_lifesteal_heal` + `_total_heal_amp`, folds the post-amp total into the EHP math at the top of the damage stack: `physical_ehp = (hp + shield_any + shield_phys + heal_total) / (armor_factor * mode_mult)` (and symmetric magical/true). `format_table` renders a `heal_hp` row only when non-zero with item/lifesteal/amp/total breakdown. `to_dict` carries `heal_sources` as `[{item_id, heal_hp}, ...]`. `notes` adds one line per heal source + a lifesteal line + an amp line when applicable. **4 items wired in `_effects_data.py`:** Bloodthirster 3072 + Arena 223072 (NEW `shield`, kept defensive_only=True); Spirit Visage 3065 + Arena 223065 (NEW `heal_amp_pct=0.25`, kept defensive_only=True); Sundered Sky 6610 + Arena 226610 (NEW `heal=ItemHeal(base_ad_scaling=1.0, ranged_modifier=0.5)`, EXISTING periodic Lightshield Strike damage proc preserved). Death's Dance 6333 + Arena 226333 INTENTIONALLY untouched - the Defy heal-on-takedown (75% bonus AD over 2s) depends on takedown-rate assumption which is genuinely uncertain; stays defensive_only with `DeathsDanceDeferredTests` pinning the deferral. Phase 6 deliberate omissions (deferred to Phase 6.5+): (1) Spirit Visage amp on Phase 1.5 SHIELDS - Riot tooltip "increases all heal AND shielding +25%" but engine ships heal-pipeline amp only; SV + lifeline-item under-credits the shield piece by ~15% (60% bonus_hp shield * 0.25 amp lost on a Sterak example); pinned by `SpiritVisageAmpTests::test_spirit_visage_does_not_amp_phase15_shields`. (2) Sundered Sky 6% missing-HP additive - requires a current-HP-share assumption distinct from the steady-state full-HP convention. (3) DD Defy heal-on-takedown - takedown-rate uncertain. (4) Lifesteal post-mitigation accuracy - lifesteal heals on post-armor damage in-game but EHP scorer is enemy-state-agnostic; bounded and documented. **+56 tests in NEW `tests/test_ehp_heal_phase6.py` (~530 LOC) across 11 classes:** ItemHealSchemaTests 9 (default zero / flat / base_ad scaling melee / ranged halved / bonus_hp+flat / bonus_ad / negative-floored / negative-ranged-rejected / ranged=0 allowed / full composition), CollectHealsTests 5 (empty / no-heal-field / unknown-id / SS aggregate melee / ranged halved), TotalHealAmpTests 6 (no items / SV alone / no-amp items / unknown silent / Arena mirror / multiplicative stacking), LifestealHealTests 7 (zero LS / zero AD / full formula / window pin / custom window / zero window / negative floored), BTIchorshieldTests 8 (BT has shield not heal / Arena mirror / L1 / L9 / L18 / mid-lerp / no-lifeline-unique / EHP lift), SunderedSkyHealTests 4 (SS has heal field / Arena mirror / Aatrox melee heal range / ranged halved / periodic preserved), SpiritVisageAmpTests 5 (SV carries amp / Arena mirror / SV-alone no heal pool / SS+SV amped / SV does NOT amp Phase 1.5 shields), EhpResultHealFieldsTests 4 (to_dict includes fields / format_table renders heal row / format_table omits when zero / notes carry source lines), DeathsDanceDeferredTests 3 (DD defensive_only / DD has no heal / Arena mirror), BTPlusLifelineStacksTests 2 (BT+Sterak both contribute / BT+Shieldbow both contribute), EngineVersionCurrentTests 1. **+15 stale ENGINE_VERSION pin syncs** across 14 DS test files (1.27.0 -> 1.28.0): test_effects_expansion (2 hits) + test_ehp + test_ehp_shield_phase15 + test_sum_of_blocks_expansion_s217 + test_unit_variants_s224 + test_max_priority_sweep_s227 + test_nested_hp_parser_s223 + test_post_parser_sweep_s225 + test_sum_of_blocks_expansion_s215 + test_form_index_sweep_s226 + test_conditional_block_index_s232_saturation + test_conditional_block_index_s228 + test_conditional_block_index_s229 + test_conditional_block_index_s230 + test_conditional_block_index_s231 (1 hit each). **Verified:** DS suite **2962 passed / 1 skipped / 1 xfailed / 1663 subtests passed** (+56 over s128 2906 baseline); wider RC suite **2780 passed / 67 subtests / 0 failed** (unchanged - this slice is DS-engine only; the phase8_smoke `test_live_three_profiles` flipped fail->pass after DS server restarted to serve 1.28.0); py_compile + ruff clean on all 5 touched source files (`test_ehp_heal_phase6.py` is pure-ASCII new; 4 source files have pre-existing non-ASCII bytes carryover but my edits added 0 non-ASCII - verified via line-wise additions probe `git diff | grep '^+'`). **DS :8893 restarted** via `taskkill /F /PID 8564` + `schtasks /Run /TN RC-DaemonSlayer` (per `reference_ds_server_not_supervisor_watched` memory); live `/health` returns `engine_version=1.28.0` patch=16.10.1 champions=172 items=705. **Math verified end-to-end:** Aatrox L11 + Sundered Sky + Spirit Visage = base AD 103.875 * 1.25 amp = 129.84 heal_total exactly; Aatrox L11 + BT alone shield_any=198.33 (165 + 2/9 * 150 at L11 via the lerp) + heal_lifesteal=134.67 (0.15 * 183.88 AD * 0.8137 AS * 6); Aatrox L11 baseline physical_ehp=2973 vs +BT physical_ehp=3572 = +600 EHP delta = (198+135)/armor_factor(80.1). All hand-computed values match the test fixtures + live `compute_ehp` output. No RC restart needed. No frozen-file edits. **Don't-redo:** BT shield does NOT share `unique_passive_key="lifeline"` (distinct Ichorshield unique passive - stacks alongside Sterak/Shieldbow/Maw/Hexdrinker at the engine layer; `BTIchorshieldTests::test_bt_no_lifeline_unique_passive` + `BTPlusLifelineStacksTests` both pin); do NOT add a lifeline tag. Spirit Visage amp applies to HEAL pool ONLY in this engine (deliberate Phase 6 boundary; `SpiritVisageAmpTests::test_spirit_visage_does_not_amp_phase15_shields` pins by checking that delta = 0.60 * 400 = 240 exactly, NOT 240*1.25=300); shield-amp deferral is documented in the ehp.py docstring + the ItemEffect.heal_amp_pct field docstring. Lifesteal heal uses PRE-mitigation AD (EHP scorer is enemy-state-agnostic; over-credits by ~30-40% vs 60-90 armor - consistent with the no-enemy-pen Phase 1 omission still in force); do NOT add an enemy-armor parameter to `_lifesteal_heal` (would break the EHP API surface). Sundered Sky preserves its EXISTING `PeriodicProc` damage proc - the heal field is ADDED, not replacing; pinned by `SunderedSkyHealTests::test_sundered_sky_periodic_damage_still_modeled`. The `_FIGHT_WINDOW_S = 6.0` constant matches the engine's existing sustained/burst boundary in the DPS layer; do NOT vary the window per-mode (no per-mode multiplier needed - the model is mode-agnostic for SR/ARAM/Brawl/Arena). DD 6333 stays defensive_only - the Defy heal-on-takedown rate is genuinely uncertain; do NOT pre-wire with a constant takedown probability (would over- or under-credit). `compute_ehp` does NOT apply unique_passive dedup itself - the rank.py shares_dead_unique filter is upstream + `BTPlusLifelineStacksTests` pins the sum-shields behavior at the engine layer; do NOT push dedup into compute_ehp. The heal pool absorbs ALL damage types (acts like an ANY shield) - heals don't discriminate by damage type in League's model; do NOT split by damage type (would require a per-source type tag which the in-game model doesn't have). **Carries forward:** (a) Per-spell CC duration extractor as 2nd consumer of `effective_cc_duration` helper (engine-side; would compose with the Phase 6 heal model into a future EHP-vs-CC blended scorer). (b) Phase 6.5 work queued: Death's Dance Defy heal (needs takedown-rate model), Spirit Visage amp on Phase 1.5 shields, Sundered Sky 6% missing-HP additive. (c) Live ARAM/SR smoke STILL pending - operator must play a real game to verify Phase 6 healing reflects in EHP rankings live (the model is sound math but a live game would surface any modeling-accuracy concerns vs operator intuition). (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (e) Item 128 / 127 / 126 / 125 carries forward unchanged. (f) DS audit remains at engine 1.28.0 - THIS BUMP is for a deliberate Phase 6 feature addition, NOT Meraki drift; the no-change DS audit streak (DS audit halt) is unrelated to this slice. Frozen-file grant NOT used this session.

128. ✅ **2026-05-21 - EHP Phase 1.5 shield throughput SHIPPED (1 commit, pushed; ENGINE 1.26.0 -> 1.27.0; non-frozen; DS :8893 restarted serves 1.27.0; RC restart not owed - non-coach engine slice).** Operator AskUserQuestion-picked "EHP Phase 1.5 shield throughput" from a 4-option fork (over CLAUDE.md archival sweep / EHP Phase 1.5 healing / tenacity dashboard panel). Closes the `ehp.py:21` deliberate Phase-1 omission "Shield throughput (Sterak's lifeline, Doran's Shield, Bloodthirster) - needs uptime modeling". Ships the four LIFELINE-style shields (single trigger per fight, value-additive to the effective-HP pool at top of the damage stack) as the simplest correct Phase 1.5 model. **NEW `ItemShield` dataclass in `_effects_types.py` (~95 LOC):** `damage_type: str` (ANY/PHYSICAL/MAGICAL/TRUE - the new `ANY = "any"` constant + `_SHIELD_TYPES` frozenset live next to the existing `_DAMAGE_TYPES`); `flat: float`, `bonus_hp_scaling: float`, `bonus_ad_scaling: float`, `level_lerp_low: int`, `level_lerp_high: int`, `level_lerp_high_value: float`, `ranged_modifier: float`, `note: str`. `__post_init__` validates damage_type + level_lerp ordering + non-negative ranged_modifier. NEW method `resolve_magnitude(level, bonus_hp, bonus_ad, is_ranged) -> float` does the linear lerp (clamp below `level_lerp_low` to `flat`; clamp at/above `level_lerp_high` to `level_lerp_high_value`; linear interpolation between), adds stat-scaled contributions, applies ranged_modifier, floors at 0. **NEW `ItemEffect.shield: ItemShield | None = None`** field in `_effects_types.py` (defaults None - 99% of items unaffected). **4 items wired in `_effects_data.py`:** Sterak's Gage 3053 (60% bonus_hp ANY); Immortal Shieldbow 6673 (400 L1-L8 -> 700 L18 ANY, ranged x0.80); Maw of Malmortius 3156 (200 + 150% bonus_ad MAGICAL, ranged x0.75); Hexdrinker 3155 (110 L1-L8 -> 280 L18 MAGICAL, ranged x0.75). All values from Meraki 16.10.1 bulk items effects text (`{{rd|melee|ranged}}` patterns parsed by hand). All 4 already shared `unique_passive_key="lifeline"` from Phase 4 batch 12 (2026-05-04) so the rank.py `shares_dead_unique` filter picks at most one in any generated build; compute_ehp itself does NOT apply the dedup (LifelineDedupTests pins this contract). Bloodthirster's ichor-shield is intentionally DEFERRED to Phase 6 (with the lifesteal model) - it requires overheal accrual rather than a single-trigger threshold; do NOT pre-wire. **EHP scorer extension `agents/daemon_slayer/ehp.py` (~70 LOC added):** NEW `_RANGED_ATTACKRANGE_THRESHOLD = 250.0` constant separates melee (Yasuo 175 / Aatrox 175 / Sett 125) from ranged (Caitlyn 650 / Ezreal 550 / Lux 550); NEW `_is_ranged(base_stats) -> bool` reads `base_stats["attackrange"]` and compares; NEW `_collect_shields(item_ids, level, bonus_hp, bonus_ad, is_ranged) -> (dict[damage_type, float], tuple[(item_id, type, hp), ...])` walks each item, resolves magnitude via `ItemShield.resolve_magnitude`, aggregates by damage type. EhpResult gains 5 new fields: `shield_any`, `shield_phys`, `shield_mag`, `shield_true`, `shield_sources: tuple[tuple[str,str,float],...]`. `compute_ehp` adds bonus_hp/bonus_ad derivation (`stats - base_stats`), calls `_is_ranged` + `_collect_shields`, folds totals into the EHP math at the top of the damage stack: `physical_ehp = (hp + shield_any + shield_phys) / (armor_factor * mode_mult)` (and symmetric for magical / true). Shields share the SAME armor/MR factor as HP per League's damage model (NOT separately reduced); pinned by `test_sterak_shield_in_physical_and_magical` and `test_aram_damage_taken_applies_to_shielded_ehp`. `format_table` renders a `shield_hp` row only when non-zero; `to_dict` carries `shield_sources` as `[{item_id, damage_type, shield_hp}, ...]`. `notes` adds one line per shield source for diagnostic transparency. **+46 tests in NEW `tests/test_ehp_shield_phase15.py`** (1 SKIPPED for the test that needs a non-1.0 aramDamageTaken champion that compute_ehp accepts cleanly): ItemShieldSchemaTests 13 (default zero / flat / bonus_hp / bonus_ad+flat / clamp-below-low / clamp-at-high / lerp-mid-range / ranged-mod / negative-bonus-floored / damage-type rejected / level-lerp-low rejected / high<low rejected / negative-ranged rejected); IsRangedTests 4 (melee thresholds + ranged + missing-attackrange-treated-melee + threshold pin); CollectShieldsTests 6 (empty / no-shield-field / unknown-id / Sterak agg / Maw magic-only / multi-source-sums); SterakShieldTests 4; ShieldbowShieldTests 4; MawShieldTests 5 (magic-only + ranged 75% + melee full + does-not-lift-physical + lifts-magical); HexdrinkerShieldTests 4; EhpResultShieldFieldsTests 4 (to_dict / format_table on / format_table off / notes-source); LifelineDedupTests 1; AramShieldTests 1 SKIPPED; EngineVersionCurrentTests 1. **+15 stale ENGINE_VERSION pin syncs** across 14 DS test files (1.26.0 -> 1.27.0): test_effects_expansion (2 hits) + test_ehp + test_sum_of_blocks_expansion_s217 + test_unit_variants_s224 + test_max_priority_sweep_s227 + test_nested_hp_parser_s223 + test_post_parser_sweep_s225 + test_sum_of_blocks_expansion_s215 + test_form_index_sweep_s226 + test_conditional_block_index_s232_saturation + test_conditional_block_index_s228 + test_conditional_block_index_s229 + test_conditional_block_index_s230 + test_conditional_block_index_s231 (1 hit each). **Verified:** DS suite **2906 passed / 1 skipped / 1 xfailed / 1663 subtests passed** (+46 over s127 2860 baseline); wider RC suite **2780 passed / 67 subtests / 0 failed** (unchanged - this slice is DS-engine only; the phase8_smoke `test_live_three_profiles` flipped fail->pass after DS server restarted to serve 1.27.0); py_compile + ruff clean on all 5 touched files (test_ehp_shield_phase15.py is pure-ASCII new; the 4 source files have pre-existing non-ASCII bytes carryover but my edits added 0 non-ASCII bytes - verified via `git diff | grep '^+'` line-wise ASCII probe). **DS :8893 restarted** via `taskkill /F /PID 5992` (pre-restart pid) + `schtasks /Run /TN RC-DaemonSlayer` ritual (per `reference_ds_server_not_supervisor_watched` memory); live `/health` returns `engine_version=1.27.0` patch=16.10.1 champions=172 items=705. **Math verified end-to-end:** Sett L11 + Sterak's Gage gets bonus_hp=400 (Sterak's HP stat) -> shield_any=240 (0.60 * 400); physical_ehp = (2070 + 240) / factor(74_armor) ≈ 4026 ≈ baseline_phys_ehp(2906) + shield-lift(~1120). Caitlyn L18 + Shieldbow ranged = 700 * 0.80 = 560 ANY. Ezreal L11 + Maw ranged = (200 + 1.50 * 60_bonus_ad) * 0.75 = 217.5 MAGICAL. All hand-computed values match the test fixtures + live `compute_ehp` output. **Live curl** verified DS server serves 1.27.0 with new engine math live. No RC restart needed (RC reads DS at request time + does not import the engine directly for non-coach paths; the coach prompts don't reference the new shield fields). No frozen-file edits. **Don't-redo:** the 4 wired items use the EXISTING `unique_passive_key="lifeline"` (from Phase 4 batch 12, 2026-05-04) - do NOT switch to a new key like "lifeline_shield" (would break rank.py dedup which already groups by "lifeline"). The shield value is value-additive at the top of the damage stack - shields share the SAME armor/MR factor as HP per League's damage model; do NOT model shields as un-resisted (would over-count Sterak's by ~50% vs in-game). `compute_ehp` does NOT apply unique_passive dedup itself - the rank.py shares_dead_unique filter is upstream + LifelineDedupTests pins compute_ehp's sum-shields behavior; do NOT push dedup into compute_ehp (would break the data-layer / planner-layer separation). The `_RANGED_ATTACKRANGE_THRESHOLD = 250.0` is calibrated against the actual 16.10.1 champion attackrange distribution (Yasuo / Aatrox at 175 = melee; nothing between 200-400) - do NOT relax to 200 (would mis-classify ARAM ARAM Aphelios/Akshan-style edge cases in a future patch). Bloodthirster is INTENTIONALLY deferred to Phase 6 - the ichor-shield depends on overheal accrual which requires the lifesteal-throughput model; do NOT pre-wire BT with a constant approximation (would over-state by 2-3x in real fights). Doran's Shield's "block damage from first 2-3 source attacks" is also DEFERRED (rare item, low impact, complex shape - block-per-source rather than HP-shield-equivalent). The `is_ranged` attackrange-based heuristic is simple but correct at 16.10.1; if a future champion swaps melee/ranged based on form (Jayce, Nidalee, Elise), the `_is_ranged` helper reads base_stats which reflects the dominant form. **Carries forward:** (a) Phase 6 healing throughput (lifesteal + Spirit Visage amp + Death's Dance + Bloodthirster ichor-shield) - same uptime-modeling problem; the next natural slice after this one. (b) Phase 1.5 caster-side enemy pen/reduction (Black Cleaver shred ON the tank, Void Staff %MR pen ON the tank) - needs enemy build plumbing. (c) Doran's Shield block-per-source modeling deferred (low priority + complex shape). (d) Engine-side `effective_cc_duration` consumer (fight-sim / per-spell CC) still future per item 127 carry (a). (e) Item 127 / 126 / 125 / 124 / 123 / 122 / 121 carries unchanged. (f) DS audit remains halted at engine 1.27.0 (was 1.26.0; THIS ENGINE BUMP is for a deliberate Phase-1.5 feature addition, NOT Meraki drift; the iter-21+ no-change DS audit streak (DS audit halt) is unrelated to this slice). Frozen-file grant NOT used this session.

127. ✅ **2026-05-21 - per-enemy ARAM tenacity wire SHIPPED (1 commit, pushed; non-engine; non-frozen; RC restart owed; no DS restart; no ENGINE bump).** Operator AskUserQuestion-picked "Per-enemy tenacity wire" from a 4-option fork (over EHP Phase 1.5 shield throughput / per-spell CC duration in AbilityResult / housekeeping pivot). Natural extension of item 126: aramTenacity is symmetric - the multiplier applies to ANY CC on that champion, whether enemy CC on operator (item 126 wire) OR operator's CC on enemy (this slice). For the 17 modified champs at 16.10.1 all multipliers are > 1.0 so the enemy-side framing is unambiguously a FOLLOW-UP WINDOW for the operator's team (CC sticks longer on assassins). **NEW `enemy_aram_tenacity_line(enemies, mode) -> str`** in `core/aram_tenacity_context.py` (+57 LOC) is the sibling renderer. Returns empty when mode not in ARAM/KIWI/ARAM_5V5/ARAM_MAYHEM set OR enemies is None/empty/all-default-tenacity. For one or more modified enemies, returns a single line `"Enemy ARAM tenacity (your CC on them lasts longer): Talon 1.20x, Zed 1.20x"`. Sort order is DESCENDING multiplier then alphabetical name (highest-value follow-up target sorts first; pinned by `test_descending_mult_then_alpha_sort` for Fizz 1.10 + Zed 1.20 + `test_equal_mult_alphabetical_tiebreaker` for Akali + Zed both 1.20). Skips blank / None / unknown-champion entries silently (`test_skips_blank_and_none_entries` + `test_skips_unknown_champion_names`). Accepts tuple input as well as list (`test_tuple_input_accepted`). Reads `_TENACITY_MAP` directly - no additional file I/O, no per-call file reads, no patch state probing (the module-load map is shared with `aram_tenacity_line`). **`coaches/aram_coach.py` edits (3 hunks):** (1) import block adds `enemy_aram_tenacity_line` alongside existing `aram_tenacity_line` (multi-line `from ... import` block); (2) `_USER_TMPL` gains `{enemy_aram_tenacity}` placeholder on its own line immediately below `{aram_tenacity}` (between operator's self-line and DS top items - symmetric to self-tenacity placement); (3) `.format(...)` call gains `enemy_aram_tenacity = enemy_aram_tenacity_line(state.get("enemy_comp", []), state.get("game_mode", "ARAM"))` next to the existing `aram_tenacity = ...` call. Token cost: 0 in the 90% non-ARAM case (both lines empty); ~25 tokens when both fire. **`tests/test_ds_pick_consumption_p1l11.py` extended (+1 line):** `_aram_prompt_inject` fixture at line ~143 gained `enemy_aram_tenacity=""` to match the new placeholder; same fixture pattern as item 126's `aram_tenacity=""` line. **+18 tests** added to existing `tests/test_aram_tenacity_context.py`: EnemyAramTenacityLineTests 14 (one modified renders / multiple listed / desc-mult sort / equal-mult alpha tiebreaker / KIWI works / SR empty / no-modified empty / empty list empty / None list empty / None mode empty / skips blank+None / skips unknown / tuple input / single-line contract / ASCII contract) + AramCoachWireTests +4 (template has enemy placeholder / coach imports enemy helper / format supports field / format renders enemy tenacity when provided). Pre-existing item 126 AramCoachWireTests fixture updated to include the new placeholder. **Live smoke** via `_USER_TMPL.format(...)` with Akali ARAM + enemies=[Zed, Talon, Ezreal, Janna, Sett] showed both lines render between Enemy keystones and DS top items: `ARAM tenacity: 1.20x effective CC duration on you (1.0s root -> 1.20s)` + `Enemy ARAM tenacity (your CC on them lasts longer): Talon 1.20x, Zed 1.20x` - Talon ahead of Zed by alphabetical tiebreaker on equal 1.20x mult. **Verified:** DS suite **2860 passed** / 1 xfailed / 1663 subtests (unchanged - this slice is RC-only); wider RC suite **2780 passed / 67 subtests / 0 failed** (+18 over s126 2762 baseline); py_compile + ruff + ASCII clean on all 4 touched files. RC restart owed via `restart_trigger.txt` (coach module edit; new prompt picks up at next coaching tick after restart). No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** the enemy-tenacity line is APPENDED via `_USER_TMPL` (per-call), NOT the system prompt (cache-prefix preservation per item 121/126). Reads `state.get("enemy_comp", [])` which is the canonical enemy-champ list field (same source used for the existing `enemies` field at line 709); do NOT pass enemy summoner names (the map keys are champion-ids). The single-line single-comma-separated format is intentional - multi-line per-enemy would bloat the prompt for the 1-2-modified case (the common case is 1-2 enemies of the 5 carrying tenacity). The desc-mult sort puts the strongest follow-up target first; do NOT alphabetize by name only (highest-value follow-up should sort first). Skipping unknown-champion entries is silent (the map is patch-data-driven; if a future patch removes a champion mid-rotation, the entry quietly drops without breaking the prompt). All 4 BACKLOG carries from item 126 still hold (engine-side CC consumer / DS audit halt / Dead Man's stacks-schema canonical / live ARAM smoke). **Carries forward:** (a) Same as item 126 (a) - `effective_cc_duration` still has NO engine-side consumer (fight-sim / per-spell CC interruption / blended-EHP-vs-CC); this slice is the SECOND coach-prompt consumer but the engine math is unchanged. (b) Live ARAM smoke STILL pending (operator must play a real game with one of the 17 modified champs OR with modified-tenacity enemies). (c) Per-spell CC duration in AbilityResult (operator's pick option C from the s127 fork) is the natural next step if the prompt-layer wires prove out and a fight-sim is wanted. (d) Item 126 / 125 / 124 / 123 / 122 / 121 carries unchanged. Frozen-file grant NOT used this session.

126. ✅ **2026-05-21 - EHP enemy-CC coach consumer (ARAM tenacity) SHIPPED (1 commit, pushed; non-engine; non-frozen; RC restart owed; no DS restart; no ENGINE bump).** Operator AskUserQuestion-picked "EHP enemy-CC coach consumer" from a 4-option fork (over EHP Phase 1.5 shield throughput / stack-ramp candidate audit / defer DS). Closes the ENGINE 1.25.0 BACKLOG carry "Future EHP enemy-CC model for `aram_tenacity_mult`" - first live consumer of the helper `agents/daemon_slayer/ehp.py:effective_cc_duration` (shipped 1.25.0 / item 122 with zero consumers). **Context-setting before the slice:** verified DS audit halt is genuine via DDragon versions.json probe (latest = 16.10.1, RC current.txt = 16.10.1, no new Riot patch); DS live `/health` returns engine_version=1.26.0 patch=16.10.1 champions=172 items=705; DS tests 2861 / wider RC 2733; engine state exactly matches item 122/123 streak-13 halt. **NEW `core/aram_tenacity_context.py` (~120 LOC):** loads `data/daemon_slayer/<patch>/champions.json` once at module-import via `_load_tenacity_map()` (reads `data/daemon_slayer/current.txt` then `<patch>/champions.json` -> walks `data.<champion>.lolmath.aram_modifiers.aramTenacity`); builds `_TENACITY_MAP: dict[str, float]` of the 17 non-1.0 champs (15 at 1.20x = Akali/Belveth/Ekko/Evelynn/Katarina/Kayn/Khazix/Lucian/Nunu/Pyke/Qiyana/Quinn/Rengar/Talon/Zed; 2 at 1.10x = Elise/Fizz). Default-1.0 champs INTENTIONALLY omitted from the map so a missing key signals "no modifier" without an extra equality check at call time. `get_tenacity_mult(champion)` mode-agnostic value lookup with None/empty/unknown fallback to 1.0. `aram_tenacity_line(champion, mode) -> str` is the prompt-side renderer: returns empty when mode not in ARAM/KIWI/ARAM_5V5/ARAM_MAYHEM set or champion missing or multiplier == 1.0 (90% of calls produce empty so coach prompt size is unchanged in the common case); for the modified 17, returns a single line `"ARAM tenacity: 1.20x effective CC duration on you (1.0s root -> 1.20s)"` using `effective_cc_duration(1.0, mult)` for the worked example (1.0s base chosen so the multiplier is directly readable from the output). Fail-soft on missing patch file / missing champions.json / malformed JSON / blank patch / unexpected data shape (list not dict) / non-numeric tenacity (str) - all return `{}` not a crash. **`coaches/aram_coach.py` edits (2 hunks):** (1) added `from core.aram_tenacity_context import aram_tenacity_line` next to existing `from core.death_patterns_loader import personal_context_block` import; (2) `_USER_TMPL` gained `{aram_tenacity}` placeholder between `Enemy keystones: {enemy_runes}` and `DS top items` lines (the natural slot - context about the operator's champion, before DS picks); (3) `.format(...)` call at line ~722 gained `aram_tenacity = aram_tenacity_line(state.get("champion"), state.get("game_mode", "ARAM"))` - per-call injection because champion changes per match (module-import caching of a per-champion fact would be wrong). The cached system prompt `_SYSTEM` is UNCHANGED - cache-prefix preservation per item 121 / `reference_coach_choices_native_emit`. Only the ARAM coach is wired - Brawl/Arena/SR coaches do NOT use aramTenacity in their modes (it's an ARAM/KIWI-specific lolmath modifier); do NOT extend to those. **`tests/test_ds_pick_consumption_p1l11.py` extended (+1 line):** `_aram_prompt_inject` fixture at line ~142 gained `aram_tenacity=""` to match the new placeholder; without this, the 7 ARAM contract tests fail with `KeyError: 'aram_tenacity'`. **+29 tests in NEW `tests/test_aram_tenacity_context.py`:** LoadTenacityMapTests 5 (map size 17 / 17 known champs present / Aatrox+Garen absent / 15x1.20 / 2x1.10), GetTenacityMultTests 4 (known returns value / unknown returns 1.0 / None / empty string), AramTenacityLineTests 10 (modified renders line / worked example uses helper / KIWI gets line / SR empty / Aatrox ARAM empty / unknown ARAM empty / None inputs empty / empty strings empty / single-line contract / ASCII contract), FailSoftTests 6 (missing patch dir / missing champions.json / malformed JSON / blank patch / unexpected data shape list / non-numeric tenacity str), AramCoachWireTests 4 (template has placeholder / coach imports helper / format supports field / format renders tenacity when provided). **Verified:** DS suite **2860 passed** / 1 xfailed / 1663 subtests (unchanged - this slice is RC-only, no DS engine touch); wider RC suite **2762 passed / 67 subtests / 0 failed** (+29 over s125 2733 baseline); py_compile + ruff + ASCII clean on all 4 touched files (`coaches/aram_coach.py` has pre-existing non-ASCII bytes carryover from earlier sessions but my edits added 0 non-ASCII bytes - verified via git diff line-wise ASCII probe); live smoke `py -c "from core.aram_tenacity_context import aram_tenacity_line; print(aram_tenacity_line('Akali','ARAM'))"` returns the expected line; `_USER_TMPL.format(...)` round-trip with Akali ARAM shows the tenacity line landed between Enemy keystones and DS top items; same template with Aatrox ARAM renders one blank line in that slot (acceptable - negligible token cost). RC restart owed via `restart_trigger.txt` (coach module edit; new prompt picks up at next coaching tick after restart). No DS restart (DS engine surface unchanged). No ENGINE bump (the consumer reads existing 1.25.0 helper + 1.19.0-exposed scaled stat; no engine math change). No frozen-file edits. **Don't-redo:** the tenacity line is APPENDED via `_USER_TMPL` not the system prompt - per-call injection is REQUIRED because champion changes per match; do NOT migrate to a system-prompt suffix (would freeze the wrong champion's tenacity into the cached prefix). The 17-champ map is patch-data-driven via `champions.json` -> `lolmath.aram_modifiers.aramTenacity`; do NOT hardcode the list (the next patch's tenacity assignments are unknown). Default-1.0 champs are INTENTIONALLY omitted from the map (missing-key signals no-modifier without an equality check); do NOT refactor to include all 172 champs at 1.0. Worked example always uses 1.0s as base so the multiplier is directly readable; do NOT vary the base (1.5s -> 1.80s for Zed feels more dramatic but is less calibratable across the 1.10/1.20 set). `aram_tenacity_line` returns "" in the 90% case so the coach prompt size is unchanged for non-modified champs; do NOT inject a placeholder string like "no modifier" - the empty-string contract is what keeps the prompt lean. Only ARAM coach is wired - Brawl uses `is_brawl` mode + Cherry/Arena uses CHERRY + SR uses CLASSIC; none of those modes have aramTenacity in their lolmath modifiers; do NOT extend to those coaches. The `effective_cc_duration` helper at `agents/daemon_slayer/ehp.py:77` remains the seam for ANY future consumer (fight-sim, EHP-vs-CC blended model, per-spell CC interruption math); the helper is unchanged - this slice only added one consumer of it. The `tests/test_ds_pick_consumption_p1l11.py` `aram_tenacity=""` fixture line is LOAD-BEARING for the 7 prompt-injection contract tests; future placeholder additions to `_USER_TMPL` will similarly require fixture updates. RC restart is owed and the operator should bounce via `echo restart > restart_trigger.txt` to pick up the new prompt template at next coaching tick. **Carries forward:** (a) `effective_cc_duration` helper still has NO consumer in the engine itself (fight-sim / per-spell CC interruption / blended-EHP-vs-CC); this slice is the FIRST consumer but it's in the coach-prompt layer, not engine math. Future engine-side consumer remains an open BACKLOG item if EHP grows a CC-duration axis. (b) DS audit remains halted at engine 1.26.0 streak 13 - no Meraki drift surfaced, no new Riot patch; resumes when 16.11 drops or when operator surfaces a specific drift candidate. (c) BACKLOG "Dead Man's Plate Momentum stacks-schema" canonical-example status from item 122 still applies; no other stack-ramp items queued. (d) item 125 carry-forwards unchanged (live ARAM/SR smoke; 2026-05-24 04:15 first scheduled postmortem run; BACKLOG line 15 SR records). (e) item 124 / 123 / 122 / 121 carries unchanged. Frozen-file grant NOT used this session.

125. ✅ **2026-05-21 - postmortem follow-up 4-slice drain SHIPPED (4 commits `50c83ac` `6d22e2e` `3cfea90` + 2 merges, pushed; non-engine; non-frozen; no ENGINE bump; RC :8888 restarted clean pid 5972 -> 2640 alive=True last_reload_ok=True; `RC-PostmortemAnalyze` scheduled task LIVE NextRun 2026-05-24T04:15:15).** Operator "continue what is next to do". Item 124 carry-forward (a)/(b)/(d) explicit + item 70 PGR S2-S5 path-stale. Framed 4-option AskUserQuestion (postmortem cadence task / 2 new death patterns / dashboard surface / stale docs sweep); operator picked "all 4 in parallel". Per `[[feedback_parallel_batch_agents]]` shipped via 2 parallel worktree subagents (B + C) + 2 inline slices (A + D). **(B) `50c83ac` 2 new death patterns:** `small_skirmish` (assists in {1,2}; assist-band elif chain between caught_4plus assists>=3 and solo_1v1_loss assists==0; fills the previously-uncovered middle assist band) + `midgame_collapse` (game_time 8:00-15:00, MIDGAME_LOW_THRESHOLD_MS=480000 / HIGH=900000; independent if-block following the early_pre_3min/late_throw time-band convention - co-exists with assist-band tags since they target different axes). Agent's precedence call to use independent-if for midgame_collapse matched the existing classifier shape; do NOT collapse to a single elif chain (would falsely make midgame mutually-exclusive with caught_4plus etc.). PATTERN_META + classify_death + 10 NewPatternThresholdsTests covering assist-band boundaries + game-time window edges. Live regen on 28236 deaths / 2838 matches: **NEW top-3 = solo_pickoff (16595, 59%) / small_skirmish (13696, 49%) / caught_4plus (12345, 44%)** - small_skirmish DISPLACED rapid_repeat from top-3 (rapid_repeat retained at #4). midgame_collapse fires on 36% of deaths (10275); does NOT enter top-3 because assist-based patterns dominate. Assist-band mutual-exclusion intact (small_skirmish + caught_4plus + solo_1v1_loss sum to ~100% of with-assist-data deaths). **(C) `6d22e2e` /api/personal-context + frontend panel:** NEW `dashboard/routes_personal_context.py` GET `/api/personal-context` returns `{ok, generated_at, total_deaths, total_matches, top3: [{key, label, description, count, rate, confidence}, ...]}` with 60s MTIME-keyed in-process cache. Missing/malformed JSON returns `{ok: false, reason: "no_data"}` with HTTP 200 NOT 404 - the panel needs to render an empty state, not surface a fetch error. NEW `web/js/panels/personal_context.js` + `web/css/panels/personal_context.css` mount a 3-card vertical stack INSIDE the Right Now panel body (after SCREEN READ, before `</section>`) per the agent's "supplementary to live coach output" framing. Each card: label header + count chip + rate% chip + truncated description line; data-kind border tint per pattern key. `core/death_patterns_loader.py` extended `top_patterns()` with rate/confidence fields + NEW `report_summary()` helper so the route reads via the loader (single source-of-truth, no duplicate file-reading). Wired in `_homeWireStartup` next to existing home poller; 60s refresh cadence matches the route cache. +31 tests (12 route + 19 DOM). Live curl `/api/personal-context` after RC restart returns ok=True with the post-slice-B top-3. **(A) `3cfea90` cadence task LIVE:** NEW `ops/install_RC_PostmortemAnalyze.ps1` registers `RC-PostmortemAnalyze` weekly Sundays 04:15. 15-min offset after RC-RewindCatchup (04:00) is LOAD-BEARING - rewind must complete first or the analyzer reads stale matches. NEW `ops/run_postmortem_with_restart.ps1` wrapper runs `pythonw scripts/postmortem_analyze.py`, logs stdout/stderr to `logs/postmortem_analyze.YYYY-MM-DD.log`, writes `restart_trigger.txt` ONLY on clean exit (analyzer exit 0) so coach prompts reload PERSONAL CONTEXT at module import (item 124 cache-discipline). Mirrors `install_RC_RewindCatchup.ps1` + `install_RC_DDragonMirror.ps1` (PowerShell 5.1 `MultipleInstances=IgnoreNew` property-after-construction workaround). Task REGISTERED LIVE this session: NextRun=2026-05-24T04:15:15 LastTaskResult=267011 (=0x41303 "task not yet run", expected). **(D) `3cfea90` stale docs sweep:** ROADMAP.md line 70 PGR aggregator-G-style reframe flipped 🟡 -> ✅; all S2-S5 stages shipped per CLAUDE.md items 115-118 (S2 `23e36d3` / S3 `92c6a0f` / S4 `d199f30` / S5 `7870124` + S5 follow-up). Compact summary replaces the 800+ char carry-forward prose; full per-stage detail stays in this CLAUDE.md ledger. Other ROADMAP/BACKLOG entries verified current via path-grep: line 49 #89 / line 50 build-ORDER / line 78 Mayhem live test / line 96-97 Phase 3 fixture steps / line 100 ADR-007 phase 3 / line 107 set_augment_intent all still legitimately operator-gated or live-gated; line 142-144 (vision calibration / DS calibration / Bridge Watcher acceptance) still blocked on real game data. BACKLOG line 15 "rewind_history.db SR records with game_id" verified STILL TRUE - 0 SR records in `data/ds_calibration.jsonl` (1251 total lines, 0 SR via filter `mode_key == 'sr'`); do NOT mark shipped. **Verified:** RC suite 2692 -> **2733 passed / 0 failed** (+41: 10 slice B + 31 slice C); py_compile + ruff + ASCII clean on all touched files; `[System.Management.Automation.Language.Parser]::ParseFile` clean on both NEW .ps1 files; CI green (push 3cfea90 ahead of CI run); RC restart via `restart_trigger.txt` (route module + loader extension trigger restart, not a NEW route registration); live `/api/personal-context` returns 200 + ok=True. Worktree cleanup: 2 worktree branches unlocked + force-removed + branches `-d` deleted after merge. No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** `small_skirmish` is assist-band elif (mutually exclusive with caught_4plus + solo_1v1_loss); `midgame_collapse` is independent if-block (co-exists with assist-band tags; mirrors early/late convention). The agent's precedence call matches the existing classifier shape - do NOT collapse to a single elif chain. `/api/personal-context` returns HTTP 200 with `ok=false, reason="no_data"` on missing JSON (NOT 404) - the panel renders an empty state, not a fetch error. Cache key on the route is FILE MTIME (not wall-clock TTL); 60s window. The loader stays single-source-of-truth - the route does NOT duplicate file-reading logic. Personal-context panel mounts INSIDE Right Now panel body (after SCREEN READ block); supplementary to live coach output. Do NOT move to its own sibling panel without operator approval. RC-PostmortemAnalyze is LIVE NextRun 2026-05-24T04:15. Wrapper writes `restart_trigger.txt` only on clean exit (analyzer exit 0). The 15-min offset after RC-RewindCatchup is LOAD-BEARING - do NOT shorten or move before 04:00. ROADMAP line 70 is now ✅ SHIPPED; CLAUDE.md items 115-118 hold the per-stage detail. Do NOT re-pitch a new PGR stage - the full S2-S5 chain is closed. BACKLOG line 15 SR-records still blocked on operator playing SR; ds_calibration.jsonl filter `mode_key == 'sr'` is the verification probe. Module-load caching of `death_patterns.json` in coach modules is INTENTIONAL (matches prompt-cache discipline); the cadence task's auto-restart wire is what propagates new top-3 to coaches without manual intervention. **Carries forward:** (a) ADR-007 phase 3 (prose-coach deprecation) STILL gated on operator playing 20+ SR games per the ADR. Unchanged from item 124. (b) Live ARAM/SR smoke STILL pending - need a real game to confirm coach output references the new patterns when situationally relevant. (c) 2026-05-24 04:15 first scheduled run is the live test of the cadence wire; check `logs/postmortem_analyze.2026-05-24.log` + `Get-ScheduledTaskInfo -TaskName RC-PostmortemAnalyze` for LastTaskResult=0 on next session-start. (d) BACKLOG line 15 still blocked on zero SR records in ds_calibration.jsonl. (e) Item 124 + 123 + 122 carries unchanged. Frozen-file grant NOT used this session.

124. ✅ **2026-05-21 - ADR-007 phase 2 postmortem-analyze SHIPPED (1 commit, pushed; non-engine; non-frozen; no ENGINE bump; RC :8888 restarted clean pid=5972 alive=True last_reload_ok=True).** Operator AskUserQuestion-picked "ADR-007 phase 2 postmortem (Recommended)" from a 4-option fork after I verified the work surface is at engine ceiling (item 123 carry: DS 1.26.0 Meraki-coherent; cost/latency exhausted; UI/UX live-game-gated). Pre-recommendation path-check (per [[feedback_backlog_path_stale_check]]): `scripts/postmortem_analyze.py` does NOT exist in tree; `data/coaching/death_patterns.json` does NOT exist; DB probe confirmed 267,997 CHAMPION_KILL events across 2898 matches, 28,236 of them with operator-PUUID as victim across 3 PUUIDs (current `rO-...#Trist` 49 matches + stale `v8H...#Vayne` 2729 matches + mid-rotation third 5 matches per `data/rewind_catchup.state.json`). Deferred since s169 (2026-05-11), 10 days; genuinely unbuilt. **(a) NEW `scripts/postmortem_analyze.py`** offline analyzer (CLI + library). 6 patterns, all computable from existing schema with no derived-data layer: `caught_4plus` (assists >= 3, severe positioning), `solo_1v1_loss` (zero assists, matchup/skill/build mismatch), `early_pre_3min` (died before 3:00 in-game), `solo_pickoff` (no ally CHAMPION_KILL within 8s before this death), `late_throw` (died after 25:00), `rapid_repeat` (died <=60s after a previous self-death; tilt cluster). Pattern thresholds intentionally tuned away from noise: my first pass used `assists >= 1` for `outnumbered_2plus` which fired on 92% of all deaths (every death with any ally CC + tower aggro carries assists) - non-informative; refactored to `assists >= 3` for `caught_4plus` (43.7% genuine 4+ enemy positioning errors) and `assists == 0` for `solo_1v1_loss` (7.8% clean 1v1 losses; mutually exclusive with caught_4plus by elif). `solo_pickoff` uses TEAM-FILTERED ally deaths (joins `participants.team_id` against the self-team, NOT all prior CHAMPION_KILLs in the match) so it correctly distinguishes "ally died in window so this was a teamfight loss" from "any kill happened in window". `rapid_repeat` uses per-match self-death-timestamp tracking via a local dict, not a sliding window. Counts -> Laplace-smoothed `rate` (via `core.smoothed_rates.laplace_rate`) + `confidence` shrink (`core.smoothed_rates.shrink`); top-3 by descending count with zero-count patterns dropped. Atomic write via `tempfile.mkstemp` + `os.replace`. CLI: `--puuid` (repeatable; defaults to catchup state.json PUUIDs current + stale) / `--db` / `--output` / `--dry-run`. Exit codes: 0 ok / 2 missing PUUIDs / 3 DB not found. **Live run output:** 28,236 deaths / 2,838 matches / 0.04s runtime. **Top-3: solo_pickoff (16595, 59% laplace-smoothed) / caught_4plus (12345, 44%) / rapid_repeat (6165, 22%).** Genuinely actionable: the operator dies SOLO most often (no ally in 8s window before death - rotate with team or ward entry); the second-most-common pattern is being CAUGHT by 4+ enemies (positioning error - watch minimap); the third is RAPID REPEAT deaths within 60s of a previous death (tilt cluster - reset emotionally). **(b) NEW `core/death_patterns_loader.py`** fail-soft reader. `top_patterns(path=None) -> list[dict]` (returns [] on missing/malformed/empty-top3); `personal_context_block(path=None) -> str` returns a multi-line PERSONAL CONTEXT block ready to f-string-append to a system prompt. Empty string when no top-3 - safe to append unconditionally to any prompt (the block starts with a leading newline so existing trailing content is separated cleanly). **(c) Coach prompt wires** - 4 active mode coaches, 6 total prompts. `coaches/aram_coach.py:_SYSTEM` (line 259), `coaches/arena_coach.py:_SYSTEM_PROMPT` (88), `coaches/brawl_coach.py:_NB_SYSTEM_PROMPT` (81) + `_URF_SYSTEM_PROMPT` (120) + `_OFA_SYSTEM_PROMPT` (149), `coach_integration/_sr_prompt.py:SR_SYSTEM_PROMPT` (127). All 6 sites use the same pattern: `""" + personal_context_block()` AFTER the closing triple-quote, NEVER inserted mid-block. Cache-prefix preservation: the static prompt string above stays byte-identical between RC sessions; if death_patterns.json regenerates, only the SUFFIX changes - the cached prompt prefix above retains validity. Per item 121 / [[reference_coach_choices_native_emit]], the Choices line was already APPENDED after OUTPUT FORMAT; this PERSONAL CONTEXT block is APPENDED after the Choices line. The `ARAM_SYSTEM_PROMPT` inside `_sr_prompt.py` (line 130) was DELIBERATELY left untouched - it's a legacy / dead-path that's test-pinned by `test_choices_not_added_to_aram_prompt_in_same_file` to prove the brawl/SR slice doesn't bleed into it; the active ARAM coach is `coaches/aram_coach.py`. Module-load timing: each coach module reads `data/coaching/death_patterns.json` ONCE at import; cache valid until RC restart. To pick up new pattern data: run the analyzer, then restart RC via `restart_trigger.txt`. **(d) `data/coaching/death_patterns.json` gitignored** - operator's personal death-pattern data + PUUIDs. Added to `.gitignore` between `rewind_catchup.state.json` and `decisions_heartbeat.json` (the privacy-sensitive personal-data band). **+30 tests:** 24 in NEW `tests/test_postmortem_analyze.py` (ClassifierTests 9 / IterDeathsTests 2 / ClassifyAllTests 1 with fixture-DB hand-computed expected counts / BuildReportTests 5 / WriteAtomicTests 2 / MainTests 4 / AsciiHygieneTests 1) + 6 in NEW `tests/test_death_patterns_loader.py` (FailSoftTests 4 / FormatTests 2). The fixture-DB ClassifyAllTests intentionally exercises threshold boundaries: a death at t=185_000ms is just past the EARLY_THRESHOLD_MS=180_000 cutoff (3:00 = 180s), one at 1_550_000ms passes LATE_THRESHOLD_MS=1_500_000 (25:00), and the 65s gap between t=120_000 and t=185_000 is just OUTSIDE the 60s rapid_repeat window while the 50s gap between t=1_600_000 and t=1_650_000 is INSIDE. **Verified:** full RC suite **2692 passed / 67 subtests / 0 failed** (+30 over item 123 baseline 2662); 87 existing coach-choices-emit tests still green (the same-file `test_choices_line_is_last_in_each_output_format` guard passes because my append is OUTSIDE the OUTPUT FORMAT block - Choices remains the last line INSIDE that block); py_compile + ruff + ASCII clean across all 8 touched files (`scripts/postmortem_analyze.py` + `core/death_patterns_loader.py` + `tests/test_postmortem_analyze.py` + `tests/test_death_patterns_loader.py` + `coaches/aram_coach.py` + `coaches/arena_coach.py` + `coaches/brawl_coach.py` + `coach_integration/_sr_prompt.py`); module imports for all 4 coaches succeed + 6 prompts have lengths confirming the suffix landed (`aram._SYSTEM` 6152 chars, `arena._SYSTEM_PROMPT` 3241, `brawl._NB_SYSTEM_PROMPT` 2668 / `_URF` 2468 / `_OFA` 1910, `_sr_prompt.SR_SYSTEM_PROMPT` 5328); live RC restart via restart_trigger.txt picked up the new prompt content (pid 5972 last_reload_ok=True). No ENGINE bump (non-engine). No DS restart. No frozen-file edits. **Don't-redo:** the 6 pattern thresholds are operator-tuned away from noise (`caught_4plus` at assists>=3 NOT >=1; `solo_1v1_loss` at assists==0 mutually exclusive; threshold rationale pinned in tests). `personal_context_block()` is APPENDED via `""" + personal_context_block()` AFTER the closing triple-quote of each prompt; do NOT refactor to f-string substitution or inline insertion mid-block (cache-prefix preservation per item 121). `ARAM_SYSTEM_PROMPT` in `_sr_prompt.py` is legacy and DELIBERATELY not modified (the test `test_choices_not_added_to_aram_prompt_in_same_file` guards the slice boundary; the active ARAM coach owns its own prompt under `coaches/`); do NOT extend the wire to it. `solo_pickoff` filters ally deaths by `participants.team_id` to distinguish "teamfight loss" from "any kill happened nearby"; do NOT regress to a team-blind window check. `rapid_repeat` uses per-match self-death-timestamp tracking via `prev_self_death_ts: dict[match_id, ms]`; do NOT collapse to a global last-self-death timestamp (would falsely flag the first death of a new match if the previous match's last death happened within 60 wall-clock seconds - which is impossible for game-time but conceivable if a fixture stitches matches). `data/coaching/death_patterns.json` is gitignored (personal data + PUUIDs); do NOT commit. Module-load caching is INTENTIONAL (matches existing prompt-cache discipline); regenerate via `py scripts/postmortem_analyze.py && echo restart > restart_trigger.txt` when the operator wants fresh data. Test fixture's expected counts are hand-derived from boundary cases (the 3:00 / 25:00 / 60s edges); do NOT relax the assertions. **Carries forward:** (a) **Cadence**: the analyzer is currently MANUAL. Optional: add a weekly scheduled task to ride alongside `RC-RewindCatchup` (Sundays 04:00 - catchup writes new matches, then postmortem-analyze refreshes the JSON) + auto-trigger RC restart via `restart_trigger.txt`. Operator-gated (worth the small cron overhead only if the JSON becomes important to keep fresh; today's snapshot has 28k deaths, the marginal week's data shifts top-3 percentages by <<1%). (b) **Live ARAM/SR smoke**: the prompts now carry PERSONAL CONTEXT but Riot Live Client games are needed to verify the coach output actually references the patterns when situationally relevant (per-page UI audit ritual on a real game). (c) **ADR-007 phase 3 (prose-coach deprecation)** is the NEXT logical follow-up but DELIBERATELY out-of-scope for this session - the ADR explicitly defers it until phase-1 detectors prove out in real games, which the operator hasn't yet hit at 20+ SR games. (d) Future pattern additions are 1-line additions to `PATTERN_META` + classifier function + 1 test; the registry pattern is durable. (e) item 123 + 122 + 121 carries-forward unchanged. Frozen-file grant NOT used this session.

123. ✅ **2026-05-21 - headless-upgrade short run: ASCII drift catch (1 commit `b56f156`, pushed; CI green 1m26s; no ENGINE bump; no test changes; no frozen edits).** Operator's /headless-upgrade triggered; work surface at engine 1.26.0 ceiling is EXHAUSTED. Phase 0 baseline (DS 2860 / RC 2662 / CI 6/6 green / DDragon cron 03:30:30 LastResult=0 = item 110 retry-with-backoff fix proven live). **Phase 1 only shipped phase `b56f156`:** AST-parse sweep surfaced 5 .py files with UTF-8 BOM (3-byte 0xEF 0xBB 0xBF at file start) + 2 .py files with Python SyntaxWarnings on bogus escape sequences. Stripped BOM from 4 non-frozen (`ops/rc_self_monitor.py` + `ops/rc_state_validator.py` + `scripts/team_planner_sync.py` + `tft/tft_coach_engine.py`); `ops/rc_supervisor.py` BOM left alone (frozen file + retroactive non-ASCII sweep is operator-gated per CLAUDE.md ASCII hard rule "smart quotes are rule-banned going forward but not yet retroactively swept (separate operator-gated pass)"). Closed `tools/build_installer.py:136` SyntaxWarning by changing the embedded bat-comment `"\;"` -> `"\\;"` inside the f-string for setup.bat (runtime string `\;` is byte-identical; only source escape semantics changed). Closed `tools/extract_panels.py:1-4` by switching docstring to `r"""` raw-string (handles the `C:\Riot Commander\` path literal) + replacing U+2192 right-arrow with ASCII `->` (the no-em-dashes hard rule's smart-glyph ban). py_compile + ruff + ASCII clean on all 6 touched files. **Phases 2/3 skipped honestly:** DS audit at 1.26.0 is Meraki-coherent ceiling (item 122 streak 13 hit; skill section 7 stop rule met at threshold 11). Cost/latency lever surface exhausted (prompt-cache fully covered item 120; log spam 8 endpoints item 121; polling cadences sane; design tokens 5 panels swept items 119/121 with 4 outliers intentionally preserved as brand/domain-specific palette). UI/UX live-game-gated (per-page UI audit ritual needs operator-played live match). **New memory `feedback_backlog_path_stale_check.md`:** before recommending "next NOW-lane item" from ROADMAP/BACKLOG, grep proposed paths against live codebase first; docs decay (item 122 caught 4 stale entries in one sweep). Generalizes [[feedback_verify_generated_reports]] from API output to source state. **Don't-redo:** the 4-non-frozen BOM strip + 2 SyntaxWarning closes are real + CI-green + runtime byte-identical; do NOT revert. `ops/rc_supervisor.py` is the ONLY remaining BOM in the repo; retroactive sweep stays operator-gated (the `feedback_no_em_dashes` memory documents the retro-sweep is its own pass). The AST-parse-with-non-printable-detection (`py -c "import ast,pathlib; [ast.parse(p.read_text(encoding='utf-8')) for p in pathlib.Path('.').rglob('*.py') if '_archive' not in str(p)]"`) is the durable ASCII-drift detector for .py files; do NOT recreate it as a separate tool. `aram_tenacity_mult` end-consumer remains intentionally a forward-marker per BACKLOG (no consumer reads it yet by design; do NOT speculatively wire a fight-sim or coach-prompt consumer absent operator-defined value). All item 122 / 121 / 119 carries unchanged. **Carries forward:** nothing new; item 122 carry list intact.

122. ✅ **2026-05-21 - BACKLOG drain (2 carries + ROADMAP/BACKLOG sweep): aram_tenacity_mult EHP wire + Dead Man's Plate Momentum stacks-schema lift (2 commits `e9bf6b3` + `ce563c4`, pushed; CI green; ENGINE 1.24.0 -> 1.26.0; non-frozen-only; DS :8893 restarted twice via taskkill + schtasks /Run RC-DaemonSlayer; RC :8888 unchanged).** Operator asked "what is next" - first answer (DS item-AH lane + ward-heat frontend) was PATH-STALE: both already shipped (`f273acb` + `f4938af` respectively, but ROADMAP/BACKLOG entries hadn't been swept). Pivoted to 3 genuine NOW items, all 3 shipped. **(1) Doc sweep `e9bf6b3` (docs-only):** ROADMAP "Open items - High priority" shed 3 headless-upgrade entries (items 119/120/121 - paths-stale duplicates of this ledger). BACKLOG "Coaching depth" shed 4 entries: aramAbilityHaste SHIPPED note (`4e5d818`) + augment formula evaluator SHIPPED note (`21657eb`) + DS item-AH "queued" entry (already shipped `f273acb`) + ward-heat frontend "queued" entry (already shipped `f4938af`). Per ROADMAP-95 archive-don't-delete rule the full detail remains in CLAUDE.md ledger items + git log + history_notes.md. **(2) ENGINE 1.25.0 - aram_tenacity_mult consumer wired into EHP `ce563c4`:** closes BACKLOG "Future EHP enemy-CC model for aram_tenacity_mult" queued from item 113 / `4e5d818` wave 2. `EhpResult` gains `aram_tenacity_mult` field read from `resolved.stats.get("aram_tenacity_mult", 1.0)`. NEW module-level helper `effective_cc_duration(base_cc_s, tenacity_mult)` returns `base_cc_s * max(0.0, tenacity_mult)` - the seam any future fight-sim / coach-prompt / EHP-vs-CC consumer reads to convert a base CC duration into the post-tenacity value. 17 ARAM champs carry non-1.0 aramTenacity in 16.10.1 (Akali/Belveth/Ekko/Elise/Evelynn/Fizz/Katarina/Kayn/Khazix/Lucian/Nunu/Pyke/Qiyana/Quinn/Rengar/Talon/Zed; mostly 1.20 lengthening; Elise/Fizz 1.10). Blended EHP math UNCHANGED - CC duration vs HP pool is a separate axis; downstream consumers pair tenacity_mult with their own base-CC assumption via the helper. SR + every non-ARAM mode degenerate to identity (1.0). `format_table` renders the tenacity line only when non-1.0 (parallel to `mode_multiplier`). `notes` carries an `aramTenacity` line when ARAM + non-1.0. `to_dict` includes the field. +19 tests across 3 new classes in `test_ehp.py` (EffectiveCcDurationHelperTests / AramTenacityForwardTests / ConsumerComposition125Tests / EngineVersionCurrentTests). **(3) ENGINE 1.26.0 - Dead Man's Plate Momentum stacks-schema lift `ce563c4`:** closes the iter-16 (2026-05-20) `defensive_only` deferral per BACKLOG "Dead Man's Plate Momentum stacks-schema". NEW `PeriodicProc.stack_ramp_seconds: float = 0.0` field is the family extension for the stack-accumulation -> discharge pattern. When set > 0 alongside `every_n_attacks > 0`, the sustained model in `_periodic_proc_dps` fires the proc once per `max(stack_ramp_seconds, every_n_attacks * attack_period_s)` - typically ramp dominates (3.57s ramp > 1/AS for non-attack-speed-stacked builds; the slower-of-the-two gates). The burst path (`_per_attack_proc_damage`) intentionally skips stack-ramp-gated procs because the burst window (2-3s) is shorter than typical ramps and these are tank/utility items whose DPS contribution is sustained-only - including them in burst would over-attribute by assuming stacks were pre-built. Dead Man's Plate **3742** + Arena mirror **223742** re-encoded: `Shipwrecker` proc = `lambda c: 40.0 + c.base_ad` PHYSICAL, `every_n_attacks=1`, `stack_ramp_seconds=3.57`. Full-stack discharge matches Meraki 16.10.1 spec: `0.4 * 100 (cap 40) + 1.0 * base_ad = 40 + base_ad`. `__post_init__` guards: `stack_ramp_seconds >= 0` AND mutually-exclusive with `every_n_seconds` (no ramp semantics in time-based path; ramp is implicit in the period). **Math verified exact:** Aatrox L11 vs 100-armor target = (40 + 103.875)/3.57 * 0.5 = **20.15 DPS delta** matches observed `compute_dps` output (`r1.weighted_dps - r0.weighted_dps = 20.15`). +18 stacks-schema tests in NEW `test_stack_ramp_schema_126.py` across 5 classes (SchemaConstructionTests / SustainedDpsRampGateTests / BurstSkipTests / DeadMansPlateEncodingTests / DeadMansPlateLiveContributionTests). 5 existing `DeadMansPlateShipwreckerTests` in `test_effects_expansion.py` rewritten to pin the post-lift state (removed `defensive_only=True` assertions; added presence-of-Shipwrecker + DPS-lift assertions). +15 stale ENGINE pin syncs across 14 DS test files (1.24.0 -> 1.26.0 in bulk). **Verified:** DS suite **2860 passed / 1 xfailed / 1663 subtests passed** (+37 over 2823 baseline); RC suite **2662 passed / 67 subtests / 0 failed** (+1 over 2661 baseline - the phase8_smoke `test_live_three_profiles` newly passes post-restart); ruff clean on all 8 touched files (`_effects_types.py` + `_effects_data.py` + `dps.py` + `ehp.py` + `__init__.py` + 2 test files + 1 NEW test file); py_compile clean; ASCII clean on all 211 added lines (0 non-ASCII added; pre-existing non-ASCII in untouched docstring sections left alone per scope discipline). DS :8893 restarted twice via taskkill /F /PID + schtasks /Run RC-DaemonSlayer (engine version bumps); live `/health` returns `engine_version=1.26.0`; live `compute_dps` confirms 20.15 DPS delta. **Don't-redo:** DS item-AH lane is SHIPPED (`f273acb`) - the registry-module path was chosen over the originally-proposed `ItemEffect.ability_haste_flat` field but the OUTCOME is equivalent; do NOT re-pitch the field+extractor approach. Ward-Coverage Heat Strip frontend is SHIPPED (`f4938af`) - `core/ward_producer.py:install_liveclient_listener` wired in `main.py:149`; do NOT re-pitch a frontend-wire follow-up. `aram_tenacity_mult` on `EhpResult` is FORWARD-MARKER for fight-sim consumers; the EHP `blended_ehp` math is intentionally unchanged - use `effective_cc_duration(base_s, tenacity_mult)` as the seam, the helper is the ONLY downstream contract. `PeriodicProc.stack_ramp_seconds` is mutually exclusive with `every_n_seconds` (the seconds-based path has no ramp semantics - ramp is implicit in the period); `__post_init__` enforces; `SchemaConstructionTests` pins. The burst path intentionally skips stack-ramp-gated procs - tank items with sustained-only DPS; do NOT special-case Dead Man's in burst (a 3.57s ramp doesn't fit a 2-3s burst window). 5 `DeadMansPlateShipwreckerTests` in `test_effects_expansion.py` were rewritten for the post-lift state; do NOT re-add `defensive_only=True` assertions or revert the encoding to flat-magnitude. The "BACKLOG path-stale check" pattern was the methodology win this session: BEFORE recommending a "next" item, grep for the proposed file/field paths to verify they don't already exist - the operator's `[[reference_no_em_dashes]]` "verify before recommending from memory" principle generalized to BACKLOG entries (memory and docs both decay). **Carries forward:** (a) Dead Man's stack-ramp schema is now the canonical example for future stack-discharge items (Sterak's Lifeline + hypothetical future ramp items can adopt the same field; no items queued today). (b) `effective_cc_duration` helper has no live consumer yet - the fight-sim / coach-prompt consumer is still future; the seam is in place. (c) All standing operator-gated items unchanged: 101.qq.com one-off Game-PC Network-tab capture; Arena S2 patch 26.09 reconnaissance + CSS anchor-positioning lift; per-page UI audit ritual after a live game; type-hint + `ruff --unsafe-fixes` one-time pass. (d) Item 121 / 120 / 119 carries unchanged. No frozen-file edits this session.

121. ✅ **2026-05-21 - HEADLESS UPGRADE PASS 3 + PASS 4 (03:25 -> 04:15 CST): design tokens 5 more panels + log spam extension + ALL 4 coaches now emit native choices arrays (4 commits + 2 merges, pushed, CI green; non-frozen-only; no RC restart needed for the coach-prompt changes - they're picked up on next coaching tick).** Operator re-extended scope: each pass strips done items + adds DS expansion under same guidelines; no user gating. **Pass-3 commits:** `385f5e8` extend log spam suppression set 3 -> 8 endpoints (`asset-stamp` + `ui-version` + `activity` + `env` + `locked-champion` added; ~1400 lines/hr additional log savings post the original 81% suppression from pass-2); `5cefe30` design tokens consumption sweep across 5 more panels (item_build + team_context + bridge_pending; augment_reco + cd_ledger were already token-clean - 9 hex swaps total + 4 outliers intentionally preserved with rationale); `6b92382` ARAM coach native choices emit (worktree H: appended `Choices:` line to OUTPUT FORMAT in aram_coach.py:257 + passthrough handler at lines 817-833 + `cur["choices"] = _choices_list` at line 842; +16 tests). **Pass-4 commits:** `14737a7` Arena coach native choices emit (worktree I: mirrors H's pattern at arena_coach.py:86 prompt + 123-126 OUTPUT_KEYS + 606-623 passthrough + 634 cur.update key; +23 tests); `58b7d20` Brawl + SR coaches native choices emit (worktree J: brawl_coach.py 3 system prompts NB/URF/OFA each gain Choices line + 3 _OUTPUT_KEYS lists each gain `"choices"` + shared passthrough block; coach_integration/_sr_prompt.py SR_SYSTEM_PROMPT gains Choices after Risk + coach_integration/_coach.py `_parse_response` `field_map` gains `"choices": "choices"` + passthrough mutates `fields["choices"]` from string to list before `current.update(fields)`. The SR structural deviation - split prompt+coach module + passthrough mutates fields[] BEFORE update - is pinned by `test_sr_coach_choices_emit.py`. +49 tests total across brawl + sr). All 4 per-mode coaches now emit native A/B/C choices arrays. **Coach choices native-emit contract (now applied to all 4 modes):** the LLM returns prose + choices in ONE response (no second call, zero extra LLM cost); the dashboard's `_state_builder.py` prefers `core.coach_choices.parse_choices(coach)` (native) and falls back to `synthesize_simple_choices(coach)` (pure-Python A/B from action prose) when native is absent. The synthesizer fallback is intentionally KEPT - bootstrap path + safety net for any mode whose coach has a bug emitting choices. Schema mirrors `core.coach_choices.CoachChoice` exactly: `{key, label, expected_outcome, confidence("low"|"mid"|"high"), source_tag}`. Per-mode source_tag suggestions in each coach prompt: ARAM `fight-trade/pack-grab/scaling/siege-call/augment-pivot`, Arena `augment-pick/round-trade/duo-rotate`, Brawl `fight-trade/respawn-window/objective-pace`, SR `lane-state/rotate-ward/team-fight`. **Cache prefix preservation:** the `Choices:` line is APPENDED to the end of each OUTPUT FORMAT block (never inserted mid-block) so the cached system prefix above stays byte-identical between requests - test files (CachePreservedTests in each emit-test file) pin this with positional assertions. **+88 tests across the two passes** (16 ARAM + 23 Arena + 28 Brawl + 21 SR). Touched files: pass-3 `dashboard/_handler.py` + `tests/test_handler_log_spam_suppress.py` + 3 panel CSS + new tokens-consumption test + `coaches/aram_coach.py` + new ARAM emit test; pass-4 `coaches/arena_coach.py` + new arena emit test + `coaches/brawl_coach.py` + `coach_integration/_sr_prompt.py` + `coach_integration/_coach.py` + 2 new emit tests. **Verified:** all touched files py_compile + ruff + ASCII clean; CI green across all 4 commits; RC :8888 restarted once after pass-2 (handler edit) then pass-3 extension is hot-reloaded on next request; coach prompt changes pick up on next coaching tick (no RC restart needed - the prompt strings load at coach call time). **DS audit cycle status:** HALTED at streak 13 from pass-2 (Black Cleaver + Voltaic + Eclipse all NO-CHANGE). Engine 1.24.0 formally Meraki-coherent. **Don't-redo:** ALL 4 per-mode coaches now emit native choices arrays - do NOT pitch a 5th pass for the same task; the synthesizer fallback stays in place as bootstrap + safety net. The `Choices:` line is APPENDED to OUTPUT FORMAT block (cache prefix preservation), never inserted mid-block - CachePreservedTests in each emit-test pin this. The SR coach's split-module structure (_sr_prompt.py + _coach.py) has a passthrough that mutates `fields["choices"]` from string to list BEFORE `current.update(fields)` (not a separate cur key write) - this is the only structural deviation from ARAM/Arena/Brawl; the test `test_sr_coach_choices_emit.py` pins the writepath shape. Brawl's 3 prompt variants (NB/URF/OFA) all gain the Choices line + the 3 _OUTPUT_KEYS lists; single shared passthrough block. Per-mode source_tag suggestions are encoded in each coach's prompt - if a future operator adds a new game mode, follow the same pattern (append Choices + add to OUTPUT_KEYS + share passthrough). Log spam suppression set (8 paths in `dashboard/_handler.py:_SUPPRESS_LOG_PATHS`) is calibrated to dashboard pollers; do NOT add to it without confirming the new path has > 1 log-line/sec steady-state. Panel-CSS token consumption preserved 4 outliers (item_build wave-state threshold colors / augment_reco Riot brand tier colors / cd_ledger ally/enemy team tints / bridge_pending salmon brand) - they're intentional brand or domain-specific palette, not semantic; do NOT swap them to var() refs. **Carries forward:** (a) per-page UI audit ritual after operator plays a live game (the choices field will surface real LLM-generated A/B selections instead of the synthesizer's heuristic A/B - operator can review whether the LLM choices are tutoring-quality and tighten prompts if needed); (b) Arena S2 patch 26.09 reconnaissance + CSS anchor-positioning lift remain operator-gated; (c) any future game mode that adds a coach should follow the same Choices-emit pattern; (d) item 120 / 119 / 118 carries unchanged. No frozen edits this pass either.

120. ✅ **2026-05-21 - HEADLESS UPGRADE PASS 2 (02:55 -> 03:25 CST): DS audit halt achieved (streak 13) + close prompt-cache carries-forward + log spam lever (3 commits, all pushed, all CI green; non-frozen-only; RC :8888 restarted once for handler edit pid=5844 alive=True last_reload_ok=True).** Operator extended scope: each /headless-upgrade re-run strips items already shipped + folds in more DS work and DS expansion under the same guidelines; no user gating; best-recommended choice + audit + test + iterate. **Commits:** `bb30e92` close prompt-cache carries (experimental_builder + replay_coach) + log spam suppression. **Wave-2 worktree agents (4 parallel in 1 message):** DS-A Black Cleaver Carve (NO-CHANGE - Meraki 16.10.1 confirms 6%/stack engine correct at 30% max, **streak 10 -> 11 = HALT CRITERION REACHED**); DS-B Voltaic Cyclosword Firmament id 6699 (NO-CHANGE - 100 flat phys + 10 lethality matches Meraki, no AD scaling, that was pre-rework); DS-C Eclipse Ever Rising Moon 6692 (NO-CHANGE - 6% target max HP matches Meraki melee value); COACH+LOG combined slice (SHIPPED). **Combined slice (`bb30e92`):** (a) experimental_builder.py:209 + replay_coach.py:194 flipped from uncached `system=_SYSTEM_PROMPT` string-form to explicit-block list with `cache_control={"type":"ephemeral"}` mirroring aram_team_analyzer.py:161-168. Closes pass-1 carry-forward (d). Cache-marker pattern now consistent across ALL coaches that call messages.create() with a static system prompt (aram_coach + arena_coach + brawl_coach + sr/coach_integration + champ_select_coach + aram_team_analyzer + experimental_builder + replay_coach). (b) `dashboard/_handler.py` NEW `_SUPPRESS_LOG_PATHS` tuple + rewrite `Handler.log_message` to skip 3 high-frequency 2Hz-poll endpoints (`GET /api/decisions `, `GET /api/decisions/heartbeat `, `GET /api/vision-state `). Sample log volume was ~7100 lines/hour (15981/17826 = 90% of today's log file); suppression preserves error/info logging via separate log.warning/log.info calls at other sites (log_message is BaseHTTPRequestHandler's request-trace hook only). **+17 tests:** 4 experimental_builder cache + 4 replay_coach cache + 9 handler log spam (constant pinned + each of 3 paths suppressed + /api/state + POST /api/bridge/inbox logged + error-path call sites still present + log.warning sidesteps suppression). **DS audit HALT MILESTONE:** streak now 13 consecutive no-changes (iter 20-32); engine 1.24.0 declared formally Meraki-coherent at patch 16.10.1. Future DS work goes ONLY on Meraki-confirmed drift (no rumor-driven bumps; no exhaustive sweep churn). **Verified:** all touched files py_compile + ruff + ASCII clean; RC suite 2533+17 = 2550 passed; CI green on `bb30e92` (1m20s); live: RC restarted pid 14824 -> 5844 alive=True last_reload_ok=True. **Don't-redo:** DS audit cycle has HALTED at engine 1.24.0; do NOT pre-launch another exhaustive sweep absent Meraki-confirmed drift signal. Prompt-cache marker pattern is now CONSISTENT across all 8 messages.create() coach callers; the 3-substring suppression set in `_handler.py` is calibrated to the dashboard's 2Hz pollers - do NOT add to it without confirming the new path has > 1 log-line/sec steady-state. The log_message override is BaseHTTPRequestHandler's request-trace hook, NOT a general logging filter; error paths emit via log.warning/log.info elsewhere and are unaffected. **Carries forward:** (a) coach prompt per-mode tuning to emit native `choices` arrays - prompt engineering across aram/arena/brawl/sr coaches; zero LLM cost; queued. (b) Arena S2 patch 26.09 reconnaissance + CSS anchor-positioning lift remain operator-gated. (c) all item 119/118 carries unchanged. No frozen edits this pass.

119. ✅ **2026-05-20/21 - HEADLESS UPGRADE RUN (6h authorized, 3h08m active): /headless-upgrade skill collapse + A/B tutoring coach + rewind DB live writer + design tokens + champ_select prompt cache + dict cache + UI polish sweep + DS audit 3-no-change pass (9 commits, all pushed, all CI green; 1 frozen-file edit `app/_game_lifecycle.py` 1-line rewind wire under run grant; non-engine; RC :8888 restarted once pid=14824 alive=True last_reload_ok=True; ADR-008 asset-hash auto-bumps cover JS/CSS).** Operator scope reorientation: collapse /done /clear /continue /compact /memory /audit /test /iterate /new-tech into one /headless-upgrade; remove specific lever names; reorient to cost+latency wins without product degradation; run parallel backlog/roadmap drain; UI/UX deep dive; A/B tutoring coach prominence; DS expansion; rewind DB live wire; up to 24 parallel worktree agents per task with orchestrator (this Claude) merge. Full authority, no user gating (20-day 100% acceptance pattern). **9 commits:** `642d43b` /headless-upgrade rewrite + dict cache + alert poll; `045d5fc` A/B tutoring coach (NEW `core/coach_choices.py` CoachChoice dataclass + parse_choices forgiving parser + synthesize_simple_choices pure-Python A/B fallback from action prose; `dashboard/_state_builder.py` stamps `coach.choices` on every /api/state; NEW `dashboard/routes_coach_choice.py` POST /api/coach-choice -> NEW `DecisionStore.record_coach_choice` in `core/decision_detector.py` appends type=coach_choice to data/decisions_log.jsonl; NEW `web/js/panels/coach_choices.js` chip renderer + `web/css/panels/coach_choices.css` + `<div id="rn-choices">` mount between #rn-immediate and Watch row = prompt prominence as operator asked); `3d39ae7` merge worktree D (design tokens); `0dbe5c9` merge worktree B (rewind live writer); `a5efe11` ruff B017 mid-run fix; `4064f87` champ_select prompt cache (worktree E); `cd54458` + `0556032` UI polish (worktree F) + merge; `d8b6fc3` docs sync (ROADMAP + WAKEUP + DAEMON_SLAYER + BRIEF); `3603310` aram_team_analyzer prompt cache (1-grep follow-up to E). **Wave-1 research (6 parallel agents):** UI/UX deep audit + A/B coach module map + cost/latency lever scan + new-tech scan + DS iter 27+ targets + rewind DB live design. **Wave-2 worktree agents (4 parallel in one message):** B rewind (`324523c` 29 tests frozen wire OK) / C DS audit (3 no-changes, no commit, streak now 10 - 1 short of 11-halt) / D design tokens (`74d2f9b` 22 tests additive `web/css/tokens.css` semantic palette + 4-tier fs + 8px space + 3 pulse keyframes + .tabular-nums utility + 3-panel CSS sweep) / E champ_select cache (`4064f87` 5 tests - explicit-block list with `cache_control={"type":"ephemeral"}` mirroring aram_coach.py:743-750; the string-form `system=` was uncached). **Wave-3 worktree agent F** UI polish (`cd54458` 19 tests): tabular-nums across 8 panels (right_now/next/item_build/team_context/augment_reco/spike_curve/ward_heat/draft_elo) + text-wrap balance on 4 wrap selectors (.action/.action-mid/.lm-tc-mvp-name/.replay-events-chip-text) + coach .action pulse-glow on fresh-update (urgent->bad/fight->warn/good->good 800ms keyframe dismissal + empty band silent) + draft_elo overlay ESC dismiss (document-level keydown preserves HoverOnlyContractTests against click-to-pin). **Cost + latency wins applied (5):** dict cache `public, max-age=86400, immutable` on items/runes/champion-tags/augments (N fewer /api/dictionary/* hits per session); home alerts mirror poll 2s -> 5s (60% drop idle home); `_send` accepts `cache_control` kwarg (seam); champ_select_coach + aram_team_analyzer prompt-cache markers flipped to explicit-block list (cache_read amortization across consecutive picks - real cost win); coach.choices native-emit rides existing per-tick call (zero extra LLM cost). **A/B tutoring coach contract:** `coach.choices` array always stamped on /api/state (parse_choices preferred > synthesize_simple_choices > empty list); CoachChoice frozen dataclass = `{key, label, expected_outcome, confidence(low/mid/high), source_tag}`; synthesizer covers Contest/Concede + Engage/Disengage + Push/Freeze + Recall/Stay + Ward/Skip + Rotate/Hold + Siege/Disengage. Frontend chips: key letter tinted by token color + label + 3-segment confidence bar + source-tag pill; click sets selected-class + POSTs to /api/coach-choice + appends type=coach_choice entry to data/decisions_log.jsonl. Read-only-first per operator: no input wire, no auto-apply, no remediation. Live curl POST verified end-to-end (200 + JSONL entry). **Rewind DB live writer contract:** NEW `lib/rewind_live_writer.py` (347 LOC) schedules `threading.Timer(90.0, daemon=True)` post-gameEnd to fetch Match-V5 + timeline + INSERT OR IGNORE via canonical `scripts/rewind_scraper:insert_rows` + `scripts/rewind_catchup:write_match`. PUUID from `data/rewind_catchup.state.json` (silent abort if missing). Failure modes silent: 404 event mode + 403 Personal-key + PUUID rotated + DC + gameDuration < 180s + write errors. 1 retry after 60s; abandons. Pre-write PK probe skips Match-V5 round-trip when cron beat us. Weekly RC-RewindCatchup cron STAYS as resume sentinel. Frozen-file edit `app/_game_lifecycle.py::on_game_end()` +10 LOC isolated try/except after existing postgame collector - fire-and-forget under operator's run grant. **DS audit iter 27/28/29 NO-CHANGE:** Statikk Shiv 3087 + Rageknife 6677 + Trinity Force 3078 Spellblade verified against live Meraki bulk; engine values match where Meraki has data. Rumored 16.11 Statikk AD-user buff NOT in live Meraki bulk yet - no ENGINE bump on rumor. Streak now 10 (iter 20-29); 1 short of 11-halt. ENGINE 1.24.0 unchanged. **+211 tests across the run** (RC 2322 -> 2533): 51 coach_choices + 29 rewind_live_writer + 22 design_tokens_css + 5 champ_select_coach + 4 aram_team_analyzer_cache + 19 ui_polish + 3 routes_dictionary cache + adjacent growth. DS suite 2822 passed (1 xfailed; identity baseline). **Verified:** all touched files py_compile + ruff + ASCII clean; CI 1 mid-run B017 ruff fix (`a5efe11`), otherwise green across the chain; full RC suite 2533 passed; live curl /api/state returns `coach.choices: []` when no game (correct empty-render path); live curl POST /api/coach-choice 200 + decisions_log.jsonl entry. Living docs synced (this commit item 119; ROADMAP top entry; WAKEUP handoff; docs/DAEMON_SLAYER.md ENGINE 1.22.0->1.24.0 + tests 2777->2822; BRIEF.md 1.17.0->1.24.0 + tests 2,710->2,822). 3 new memories (`reference_ab_tutoring_coach.md`, `reference_rewind_live_writer.md`, `reference_design_tokens_css.md`). **Don't-redo:** A/B coach `coach.choices` field is ALWAYS on /api/state (empty list when no synth match); do NOT remove the synthesizer fallback - it's the bootstrap path until per-mode coach prompts are tuned to emit native arrays. Rewind live-writer hook is fire-and-forget Timer (daemon=True); do NOT block on_game_end on it. Dict cache `public, max-age=86400, immutable` is calibrated to operator patch-refresh cadence; do NOT shorten. Statikk Shiv NO-DRIFT verdict was Meraki-confirmed; do NOT pre-ship rumored buffs - only Meraki-confirmed drift. Design tokens 4-tier font scale + 8px spacing are RESERVED for future consumption; do NOT bump existing selectors without operator approval. HoverOnlyContractTests guards draft_elo against click-to-pin; the ESC handler is the only allowed addition. Prompt-cache marker pattern (explicit-block system list with `cache_control={"type":"ephemeral"}`) is now consistent across aram_coach + arena_coach + brawl_coach + sr/coach_integration + champ_select_coach + aram_team_analyzer; experimental_builder + replay_coach intentionally NOT flipped (low-frequency callers; cache_read benefit small per call). The orchestrator-merge pattern (worktree agents -> Claude merges sequentially) is the durable template for future headless-upgrade runs - up to 24 agents per task, disjoint file ownership, sequential merge with conflict resolution at the import-list level only. **Carries forward:** (a) coach prompt per-mode tuning to emit native `choices` arrays - prompt engineering across aram/arena/brawl/sr coaches; zero LLM cost; queued. (b) DS audit 1 more no-change halts the audit cycle (operator-gated whether to do 11th confirmation pass or declare halt at 10). (c) Arena S2 patch 26.09 reconnaissance + CSS anchor-positioning lift remain operator-gated. (d) `experimental_builder.py:209` + `replay_coach.py:194` still use string-form system= (low-impact; deferred). (e) Item 118 / 117 / 116 / 115 carries forward unchanged. Frozen-file grant used minimally (1-line in app/_game_lifecycle.py); grant does NOT carry forward.

118. ✅ **2026-05-20 - s220 PGR S5 follow-ups: participant-name join in /api/replay/events + lm-build-pending dead-id cleanup (1 commit, pushed; non-engine; non-frozen-files-only; RC restarted once for the route-module edit pid=13016 alive=True last_reload_ok=True; ADR-008 asset-hash auto-bumps cover JS/CSS).** Closes the 2 carries-forward from item 117 / s220 S5 per the operator's NEXT_SESSION_QUEUE left in WAKEUP_NOTES. **(1) Participant-name join in /api/replay/events.** `dashboard/routes_replay_events.py` gains NEW `_participant_meta(conn, match_id) -> dict[int, (summoner_name, champion_name)]` helper that joins the `participants` table; each event dict gains 6 fields: `actor_name`, `actor_champion`, `victim_name`, `victim_champion`, `assists_names: list[str|None]`, `assists_champs: list[str|None]`. Empty strings normalize to None for null-parity with actor/victim (audit refinement (a)). Module docstring updated with the new shape. Cache key gains `_CACHE_SCHEMA = "v2-participant-join"` sentinel so stale pre-join entries evict on first hit instead of waiting out the 5-min TTL (audit refinement (e)). **Frontend `web/js/panels/replay_events.js`:** NEW `_portraitUrl(champ)` mirrors `cd_ledger.js:74-79` alphanum-strip pattern (load-bearing for Kai'Sa/Cho'Gath punctuation) + `CHAMPS` import from `items_index.js` for the patch version. `_actorText`/`_victimText` REFACTORED to `_actorChip`/`_victimChip` returning HTML with DDragon portrait `<img>` + name text via NEW `_chipHtml(pid, champ, name, prefix)`. Three-tier fallback chain: champion portrait + champ text -> summoner_name only -> P<id> (kept as ultimate fallback for event-mode esoterica where actor_name + actor_champion are both null). `<img onerror="this.style.display='none'">` collapses cleanly on a 404. **CSS `web/css/panels/replay_events.css`:** row grid widened `52px 1fr 60px 80px` -> `52px 1fr 110px 130px` to fit champion names; NEW `.replay-events-portrait` (18px square, flex-shrink:0, object-fit:cover) + `.replay-events-chip-text` (white-space:nowrap, ellipsis overflow, min-width:0); `.replay-events-victim` text-align:right -> `justify-content: flex-end` for the new flex container. **(2) `lm-build-pending` dead-id cleanup.** Single-line edit at `web/js/panels/last_match.js:1132`: forEach array in `_setEmptyState` no longer references the dead id (was a no-op `getElementById` return null fall-through; only `lm-tc-pending` + `lm-chart-pending` + `lm-tl-pending` exist in index.html). The 2 guarded `if (pending) ...` references in `_setEnrichedBuild` are LEFT ALONE - safe dead code per operator's "single-line cleanup" scope; do NOT fold them into a wider sweep without explicit operator approval. **Parallel audit (S5 pattern continued):** general-purpose subagent dispatched in BACKGROUND with the design intent + just-edited code while the implementation was in-flight. 5 findings returned during the test cycle, 3 actionable refinements applied in the SAME commit: (a) null-parity for assists_names/champs (was: empty strings, audit caught the inconsistency vs actor_name/victim_name); (b) DDragon portrait icon via the cd_ledger pattern (vs my initial plain-text chip); (e) cache schema sentinel for stale-eviction (vs waiting out 5-min TTL on the next shape change). Findings (c) GPL cleanroom + (d) payload-size verified safe - no action needed. The audit-in-parallel pattern is now durable across S4 + S5 + S5-follow-up: dispatch background general-purpose subagent with design intent + just-edited code, land refinements in SAME commit. **+12 tests across 3 files** (5 backend + 6 DOM + 1 dead-id guard): `tests/test_routes_replay_events.py` - test_champion_kill_carries_participant_names_and_champs (pins Ally2/Champ2 actor + Enemy3/Champ8 victim + Ally3/Ally5/Champ3/Champ5 assists from the seeded fixture) + test_assist_names_use_null_parity_for_unknown_pid (synthesizes an unknown assist pid=99 -> assists_names=[..., None], NOT '') + test_non_actor_event_has_null_names (ELITE_MONSTER_KILL with no victim -> victim_name + victim_champion are None) + test_cache_key_carries_schema_sentinel (stages a stale-shape entry under the pre-sentinel key; new request MISSES it -> fresh fetch); `tests/test_replay_events_panel_dom.py` - ParticipantJoinConsumptionTests (5: CHAMPS import + portraitUrl alphanum strip + actor_champion/actor_name/victim_champion/victim_name field consumption + chip helpers refactored from _actorText/_victimText + P-id ultimate fallback) + CssGridWidenedForNamesTests (1: pins the 110px/130px column widths + asserts the pre-join 60/80 combo is gone); `tests/test_last_match_tabs_reframe_dom.py` - test_set_empty_state_does_not_reference_dead_id (pins forEach array is `["lm-tc-pending","lm-chart-pending","lm-tl-pending"]` exactly + 'lm-build-pending' absent from _setEmptyState body). **Files touched** (7 total, all non-frozen): EDITED `dashboard/routes_replay_events.py` + `web/js/panels/replay_events.js` + `web/css/panels/replay_events.css` + `web/js/panels/last_match.js` + `tests/test_routes_replay_events.py` + `tests/test_replay_events_panel_dom.py` + `tests/test_last_match_tabs_reframe_dom.py`. **Verified:** py_compile + ruff + ASCII clean across all 7 touched files; full RC suite **2401 passed / 67 subtests / 0 failed** (+12 over post-S5 2389 baseline); RC :8888 restarted via `restart_trigger.txt` (route module edit triggered restart, not a NEW route module) -> pid=13016 alive=True last_reload_ok=True; live curl `GET /api/replay/events?match_id=NA1_4683461559` (older match with summoner_name data populated in DB) returned events with both names: CHAMPION_KILL t=140 actor=Sona/Forniami -> Lucian/Eisstrahl assists_names=['shaodw'] confirming the join works end-to-end; live curl `?match_id=NA1_5560540832` (operator's recent, summoner_name blank in DB) returned 211 events with actor_champion=Jax + victim_champion=Teemo + actor_name=None - the null fallback handles cleanly so the chip renders champion-portrait + champ-text without crashing; second curl returned cached=True count=82 proving cache hit. ADR-008 asset-hash auto-bumps cover JS/CSS. No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** the participant join is FORWARD-COMPATIBLE - summoner_name is sometimes blank for newer matches in the rewind_history.db schema, the chip's 3-tier fallback handles cleanly + the test `test_assist_names_use_null_parity_for_unknown_pid` pins the None semantics; do NOT pretend the field is always populated. The `_portraitUrl` alphanum strip is LOAD-BEARING for punctuation in display names (Kai'Sa -> KaiSa, Cho'Gath -> Chogath) - mirrors `cd_ledger.js:74-79` exactly; do NOT remove. The `_CACHE_SCHEMA = "v2-participant-join"` sentinel is the durable mechanism for breaking-shape cache evictions - bump the sentinel string in the SAME commit as any future event-dict shape change (NOT the TTL; the TTL slot-evicts stale entries after 5 min but the sentinel evicts them on the FIRST stale-shape hit). The 3-tier chip fallback (champ-portrait+text -> name only -> P<id>) is intentional - the P<id> ultimate fallback covers event-mode esoterica where Match-V5 might ship null champion + null summoner; do NOT collapse to fewer tiers. The CSS grid widening 60/80 -> 110/130 is pinned by `CssGridWidenedForNamesTests::test_css_actor_victim_columns_widened` - do NOT shrink back (will truncate champion names with ellipsis pre-render). `_setEnrichedBuild`'s 2 `if (pending) ...` lm-build-pending guards are LEFT ALONE per operator's single-line scope; they no-op safely because `getElementById` returns null; do NOT fold them into a wider sweep without operator approval. The parallel-audit pattern (dispatch background general-purpose subagent with design intent + just-edited code, land refinements in SAME commit) is now durable across S4 + S5 + S5-follow-up; the matching workflow: edit -> dispatch audit BACKGROUND -> run tests -> audit returns ~30-60s later with structured punch-list -> apply (a)/(b)/(e) inline -> single commit. **Carries forward:** S5 OBS video overlay remains operator-deferred (its own ADR + dependency decision per ADR-009 "Watch for"). Future operator-gated polish: (a) portrait-only chips (no text) for ultra-compact ribbon - currently champ portrait + name text; (b) hover-tooltip click-to-deep-link to participant detail view in a future PGR drilldown. All item 117/116/115 carries-forward unchanged. Frozen-file grant NOT used this session.

117. ✅ **2026-05-20 - s220 PGR S5 Replay scaffold + Match-V5 timeline events ribbon - FINAL S2-S5 stage shipped (1 commit `7870124`, pushed; non-engine; non-frozen-files-only; dashboard restarted once to register new route module; ADR-008 asset-hash auto-bumps cover JS/CSS).** The s220 aggregator-G-style Post Game Review reframe is NOW COMPLETE end-to-end across all 4 stages (S2 structure + augments; S3 score+MVP; S4 tabs reframe; S5 Replay scaffold). The existing Replay view (per-frame scrubber over `rewind_history.db.timeline_frames`) gains a sidecar **Match-V5 timeline event ribbon** under the scrubber: chronological list of discrete state transitions (CHAMPION_KILL / BUILDING_KILL / ELITE_MONSTER_KILL / TURRET_PLATE_DESTROYED by default; ITEM_PURCHASED / SKILL_LEVEL_UP / LEVEL_UP / WARD_PLACED / WARD_KILL when operator toggles include filters). Default ribbon for a 35-min SR match = ~80-160 events; full opt-in ~800-1200. **Backend NEW `dashboard/routes_replay_events.py`:** GET `/api/replay/events?match_id=<id>[&include=items,skills,wards,all]` reads `timeline_events` table; normalizes to `{clock_s, type, team, actor, victim, assists, subtype, lane, pos}`; server-side filter on event_type via `IN (?, ?, ...)` placeholder keeps payloads 5-15 KB; 5-min TTL cache keyed on `(match_id, include_set)` mirroring `routes_post_game_wpa`; fail-soft 400 (missing match_id) / 404 (unknown match) / 503 (DB missing). The `BUILDING_KILL.team_id` field stores the OWNER that lost the building - the route FLIPS to destroyer-side so the ribbon paints by actor. **Frontend NEW `web/js/panels/replay_events.js` (~230 LOC) + `web/css/panels/replay_events.css`:** vertical scrolling list mounted under `#replay-events-section` inside `view-replay` `<main class="replay-main-pane">`. Each row data-team drives left-border (ally=ok/green, enemy=bad/red, neutral=dim) + data-kind drives label color (kill=red, structure=warn, objective=gold, ward=ok, item=accent, skill=dim). Filter checkboxes (items/skills/wards) persist via `localStorage[rc-replay-events-include]`; re-fetch on toggle (cache hit on the new tuple). Event count chip in the header reflects post-filter count. Empty state when count=0. **PGR "Review ->" button routing FIX (pre-S5 broken):** the `last_match.js:227-237` Review button wrote to `sessionStorage.rc-review-focus-match` then routed `location.hash = "#review"` + `_viewSaveManual("review")`. `VIEW_IDS` at `web/js/lib/state.js:37-41` does NOT contain `"review"` (it has `"replay"`); `applyView` at `main.js:575` gates `if (!VIEW_IDS.includes(viewId)) viewId = "home"` -> the button silently fell back to Home. S5 routes to `#replay` + renames sessionStorage key `rc-review-focus-match` -> `rc-replay-focus-match` for consistency; HTML comment in index.html refreshed; `_replayViewRefresh` in `dev.js` consumes + clears the focus id (via `removeItem`) on load so a later manual click isn't shadowed; `_replayLoadMatch` is invoked on the focused row + `scrollIntoView` brings it into view. **Pre-existing CSS bug fix exposed by S5 reaching the code path:** `web/css/panels/header.css:613` had `body[data-view="replay"] main { display: none }` intended to hide the top-level dashboard `<main aria-label="Coaching panels">` (line 435 in index.html) when Replay view is active. The selector unintentionally ALSO hid the nested `<main class="replay-main-pane">` at line 1354 inside `view-replay` - which meant the entire Replay scrubber + grid + new events ribbon were invisible. Pre-S5 nobody clicked into Replay long enough to notice the empty right pane (the auto-select-from-PGR flow surfaced it). Surgical un-hide: added `body[data-view="replay"] main.replay-main-pane { display: flex }` immediately after the broad rule - class-targeted specificity wins; the nested scaffold renders while the top-level main stays hidden. **NEW `docs/adr/ADR-009-replay-events-cleanroom.md`** captures the design intent + the **league_record (GPLv3) cleanroom boundary** as the cleanroom methodology reference: pairing this event sidecar with a future OBS video overlay is the methodology to learn from, but do NOT vendor league_record source (GPL incompatible with this repo). The sidecar shape is also Match-V5 timeline's natural shape, so no implementation borrowed. `.rofl` parsing was confirmed dead-end 2026-05-17 via `fraxiinus/roflxd` (only aggregate `PlayerStatistics`, no packet stream); do not re-pitch. **Parallel-audit pattern (operator's "edits and audits in parallel" instruction continued from S4):** architecture-review subagent dispatched in BACKGROUND with the design intent + repo state survey while the route was being written. Audit returned during the test cycle with 4 refinements that landed in this commit: (1) keep `#replay` not `#review` route + rename sessionStorage key for consistency; (2) server-side event-type filter (default strong-only); (3) league_record reference belongs in `docs/adr/` not just a module comment; (4) no pre-split of `routes_coach.py` - new sibling module is the right call. **+39 tests across 2 NEW files:** `tests/test_routes_replay_events.py` (5 classes / 17 tests) - StrongEventsTests (5) pin default strong-event set + clock-asc order + ITEM/SKILL/WARD exclusion + actor/victim/assists/pos + BUILDING destroyer-not-owner flip + ELITE_MONSTER subtype winner; IncludeFiltersTests (5) pin items/skills/wards/all widening + invalid-token-silently-dropped; FailureContractTests (4) pin 400 missing/blank + 404 unknown + 503 missing DB; CacheTests (2) pin TTL cache hit + cache key includes include set; CleanroomDocPresenceTests (1) pins module docstring carries ADR + GPL + no-vendor wording. `tests/test_replay_events_panel_dom.py` (6 classes / 22 tests) - EventRibbonMountTests (4) pin section/list/count/empty mounts + hidden-by-default + filter checkboxes + lives-inside-view-replay; PgrReviewButtonRoutingTests (4) pin `#replay` route + `_viewSaveManual("replay")` + `rc-replay-focus-match` key + HTML comment refresh + legacy id/key absence; ReplayViewAutoSelectTests (2) pin sessionStorage read+clear + `loadReplayEvents(matchId)` fires on `_replayLoadMatch`; ReplayEventsPanelJsTests (4) pin exports + dev.js imports + localStorage persist + correct endpoint; CleanroomBoundaryTests (5) pin ADR existence + ADR carries GPL/no-vendor + panel JS points to ADR + panel JS warns no-vendor + CSS import; AsciiHygieneTests (3) scan JS + CSS + ADR for em/en-dashes + smart quotes (BAD dict built via chr()). **Files touched** (12 total, all non-frozen): NEW `dashboard/routes_replay_events.py` + `docs/adr/ADR-009-replay-events-cleanroom.md` + `web/css/panels/replay_events.css` + `web/js/panels/replay_events.js` + `tests/test_routes_replay_events.py` + `tests/test_replay_events_panel_dom.py`; EDITED `dashboard/_dispatch.py` (register routes_replay_events) + `web/css/dashboard.css` (import replay_events.css) + `web/css/panels/header.css` (surgical main.replay-main-pane un-hide) + `web/index.html` (mount events-section + PGR Review button HTML-comment refresh) + `web/js/panels/dev.js` (sessionStorage focus consume + invoke loadReplayEvents) + `web/js/panels/last_match.js` (Review button #replay route + rc-replay-focus-match key + docstring refresh). **Verified:** py_compile + ruff + ASCII clean across all 12 touched files; full RC suite **2389 passed / 67 subtests / 0 failed** (+39 over post-S4 2350 baseline); live curl `/api/replay/events?match_id=NA1_5439050124` returned ok=true count=100 elapsed_ms=1 cached=false on cold + cached=true on second hit; live Playwright @ `https://127.0.0.1:8888/#replay` on operator's most recent match NA1_5560540832 (Kai'Sa, May 14, 44:47 Normal Draft, patch 16.10, Loss) showed the full Replay scaffold + event ribbon: 211 events strong-only set, team-tinted left borders (P1+P6 ally + enemy Kills alternating), kind-tinted labels (Kill red, Plate TOP_LANE/MID_LANE/BOT_LANE warn-orange, Grubs HORDE gold-orange, Dragon HEXTECH_DRAGON gold-orange, etc), clock 3:50 / 3:51 / 4:49 / 5:50 / 6:25 / 6:47 / 8:15 / 8:43 / 8:56 / 9:04 / 9:21 / 9:28 / 9:47 / 10:17 / 11:23 / ... rendered chronologically; filter checkboxes [items][skills][wards] toggleable; count chip "211". No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** **Match-V5 timeline events from `rewind_history.db.timeline_events` is the CANONICAL event source for the Replay view** - do NOT re-pitch `.rofl` parsing (confirmed dead-end 2026-05-17 via `fraxiinus/roflxd`; ADR-009 records the rationale). `league_record` (GPLv3) is methodology-only - do NOT vendor its source under any circumstance; `CleanroomBoundaryTests::test_panel_js_warns_no_vendor` + `CleanroomDocPresenceTests::test_module_docstring_references_adr` + ADR-009 itself all guard the boundary; if a future contributor proposes "vendor a stripped league_record core" the tests + ADR are the durable refusal. `BUILDING_KILL.team_id` is FLIPPED to destroyer-side server-side - the events table stores the OWNER that LOST the building; the ribbon paints by destroyer; `StrongEventsTests::test_building_kill_team_is_destroyer_not_owner` pins; do NOT revert the flip. PGR Review button now routes to `#replay` (canonical `VIEW_IDS` member) NOT `#review` (never built); `PgrReviewButtonRoutingTests::test_button_routes_to_hash_replay` + `test_session_storage_key_uses_replay_namespace` guard; the pre-S5 `rc-review-focus-match` sessionStorage key is GONE. The `header.css` un-hide `body[data-view="replay"] main.replay-main-pane { display: flex }` is LOAD-BEARING - the broad `body[data-view="replay"] main { display: none }` rule above WILL re-hide the scaffold without the surgical un-hide; do NOT remove or "tidy" the un-hide without first refactoring the broader selector (e.g. to target the top-level main by aria-label). Server-side event-type filter (default strong-only: CHAMPION_KILL + BUILDING_KILL + ELITE_MONSTER_KILL + TURRET_PLATE_DESTROYED) is deliberate - WARD_PLACED (284k rows) + ITEM_PURCHASED (528k) + SKILL_LEVEL_UP (468k) + LEVEL_UP (441k) would drown the kill/objective signal; `?include=...` opts in selectively. Cache key is `(match_id, include_set)` so each opt-in tuple gets its own TTL slot; do NOT collapse to match_id alone. Audit-in-parallel pattern (subagent in background with design intent + just-edited code) is durable - matches operator's "edits and audits in parallel" instruction directly; landed all 4 audit refinements in the SAME commit, not a follow-up slice. **Carries forward:** S2-S5 of the s220 aggregator-g PGR reframe are now ALL SHIPPED end-to-end. Future operator-gated slices: (a) actual OBS video overlay keyed to LCU game-lifecycle - its own ADR + dependency decision per ADR-009 "Watch for"; (b) richer participant join in events (currently `P1..P10` chips; a join against `participants.summoner_name` + `champion_name` would surface real names but bloat the payload - operator-gated tradeoff). (c) The pre-existing `lm-build-pending` dead-id reference in `_setEmptyState` (flagged by S4 audit, S4 unchanged, still out of scope) - separate cleanup whenever convenient. Item 116 + 115 carries unchanged. Frozen-file grant NOT used this session.

116. ✅ **2026-05-20 - s220 PGR S4 tabs reframe 4 -> 3 (Build / Graph / AI Analysis) (1 commit `d199f30`, pushed; non-engine; non-frozen-files-only; no RC restart; JS/CSS asset-hash auto-bump per ADR-008; index.html picks up on next page nav).** Stage S4 of the s220 aggregator-G-style Post Game Review reframe. Tab strip flipped 4 -> 3 per the aggregator G pattern: **Build** = was Comp (rosters with full per-player items + summoners + augments + MVP/SVP card from item 115); **Graph** = was Chart (team-aggregate bars) merged with the visuals half of Timeline (per-minute gold/XP/CS differential sparklines + objective ribbon); **AI Analysis** = was Insights (3-column Quick Review) merged with the heuristic-insights half of Timeline (WPA "Phases that mattered"). **Review** nav button stays (routes to deep-review page, not a sibling panel). Legacy localStorage migration via NEW `_migrateLegacyTab(saved)` helper called from `wireLastMatchOnce`: `comp -> build` / `chart -> graph` / `timeline -> graph` (visuals home) / `insights -> ai-analysis`; migrated value written back to localStorage in a one-shot so subsequent loads see the new id verbatim. NEW `_VALID_TABS = ["build", "graph", "ai-analysis"]` constant gates `_activateTab` against malformed values. Content moved INSIDE existing panel divs - the element IDs (`lm-tc-table`, `lm-chart-wrap`, `lm-tl-wrap`, `lm-wpa-wrap`, `lm-qr-cols`) are unchanged so JS doesn't have to rewire, CSS attr-selectors stay agnostic. **Operator parallelization win:** user instruction "do these edits and audits in parallel when possible" -> UI-audit subagent dispatched in BACKGROUND with the design intent + just-touched code while implementation was in-flight; audit returned during the test/verify cycle. Audit found 1 real minor (stray "Timeline unavailable" banner in merged Graph panel) + 2 doc nits + (Claude noticed visually) 1 pre-existing CSS gap; all 3 fixed in the same commit, not a separate slice. **Same-commit fixes:** (1) Graph panel was rendering a stray "Timeline unavailable" pending banner under the Chart bars whenever LCU enrichment landed but Match-V5 timeline didn't (event modes, pre-key imports) - pre-S4 those lived in separate tabs, merged into Graph it read as a partial-failure banner. Replaced both pending blocks with a single shared placeholder ("Graph pending - team-aggregate bars populate from LCU enrichment; per-minute sparklines + objective ribbon load when the Riot Match-V5 timeline lands."). `_setTimeline` still references `lm-tl-pending` by id - the lookup just no-ops when the element is absent (`if (pending) pending.hidden = false`). (2) Pre-existing CSS gap: `.lm-wpa-wrap` had `display: flex` without a matching `[hidden]` selector, so `renderPhases({ ok: false })` set `wrap.hidden = true` but the "Phases that mattered" head still rendered. Pre-S4 this was masked because the WPA lived inside the (hidden) Timeline tab; S4 exposed it at the top of AI Analysis. Added `.lm-wpa-wrap[hidden] { display: none; }` to mirror the existing `.lm-tl-wrap[hidden]` pattern (1-line rule; load-bearing). (3) Stale doc comments in `last_match.js:wireLastMatchOnce` (referenced pre-S4 "Tab nav (Comp / Chart / Review)") + `post_game_phases.js` module header (referenced "Timeline tab"). **Files touched** (5 total, all non-frozen): `web/index.html` (tab strip 4 -> 3 buttons; 4 panel divs -> 3 panel divs with relocated subtrees; single shared Graph placeholder replacing 2), `web/js/panels/last_match.js` (NEW `_migrateLegacyTab` + `_VALID_TABS` + wireLastMatchOnce uses migrated value + writes back + stale comment fixed), `web/js/panels/post_game_phases.js` (module header comment refresh), `web/css/panels/last_match.css` (+1 `.lm-wpa-wrap[hidden]` rule), `tests/test_last_match_tabs_reframe_dom.py` (NEW, +15 grep-based DOM-contract tests). **Tests** mirror `test_last_match_score_card_dom.py` + `test_draft_elo_panel_dom.py` pattern. TabStripTests (4) pin 3 canonical tab ids + Build-default-active + legacy-id absence + Review-nav-button kept. PanelMountTests (4) pin Build hosts roster + Graph merges chart+timeline visuals (and excludes WPA + QR) + AI Analysis hosts WPA + WPA-ABOVE-Quick-Review ordering. LegacyMigrationTests (5) pin `_VALID_TABS` constant + `_migrateLegacyTab` function presence + each legacy-id mapping case (regex `case "X": return "Y"`) + writeback gate + legacy-array absence. CssNoLegacySelectorsTests (1) guard against `.lm-tab-panel[data-tab-panel="comp|chart|timeline|insights"]` in CSS. AsciiHygieneTests (1) scan JS for em/en-dashes + smart quotes. **Verified:** py_compile + ruff + ASCII clean across all 5 touched files; full RC suite **2350 passed / 67 subtests / 0 failed** (+15 over post-S3 2335 baseline); live Playwright verify @ `https://127.0.0.1:8888/#last-match` on operator's ARAM Vel'Koz 4/9/25 across all 3 tabs - **Build tab** = MVP/SVP cards (S3) + rosters render correctly with hero **48 BAD** + SVP **bliv 64 OK** + MVP **GenieInTheLamp 77 GOOD** persisting; **Graph tab** = team-aggregate bars (Kills 33-54, Deaths 54-33, Assists 65-150, Damage 102.9k-152.0k, Damage Taken 165.3k-110.5k, Gold 63.8k-70.1k, Vision 0-0, Towers 0-4) with NO stray Timeline-unavailable banner; **AI Analysis tab** = Quick Review 3-col (Excellent KDA 3.22 + High team-fight participation 71% KP / Lost first blood + Lost first tower + Lost every tower trade 4-0 + Team kill deficit 33-54 / Recent grade slump 11/20 at D/F) with WPA hidden cleanly when no Match-V5 timeline for this event mode. ADR-008 asset-hash auto-bumps cover JS/CSS edits; index.html picks up on next page nav. No RC restart, no DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** the 3-tab reframe is final - Build/Graph/AI Analysis are the canonical ids; do NOT re-add Comp/Chart/Timeline/Insights (`TabStripTests::test_legacy_tab_ids_removed` + `PanelMountTests::test_legacy_tab_ids_removed` both guard). `_migrateLegacyTab` writeback is INTENTIONAL one-shot (operator pays the migration cost only once per saved value); do NOT remove the writeback (`LegacyMigrationTests::test_wire_uses_migrated_value` pins). `_VALID_TABS` allowlist gates `_activateTab` against malformed/legacy values; do NOT relax to "trust localStorage". WPA was ABOVE Quick Review in AI Analysis (heuristic-insights ordering); `PanelMountTests::test_ai_analysis_hosts_wpa_then_quick_review` pins ordering. The `.lm-wpa-wrap[hidden]` rule is LOAD-BEARING (without it the head bleeds through the `display: flex` declaration); do NOT remove. Single shared Graph placeholder replaces 2 pre-S4 placeholders; the `lm-tl-pending` id lookup in `_setTimeline` no-ops cleanly when the element is absent (`if (pending) pending.hidden = false`); do NOT add back the second placeholder unless reframing Graph back into 2 panels. The audit-in-parallel pattern (dispatch UI-audit subagent in BACKGROUND while implementation in-flight) is durable - matches operator's "edits and audits in parallel" instruction; the audit's findings landed in the SAME commit as the implementation, not a follow-up slice. **Carries forward:** S5 (Replay page scaffold + Match-V5 timeline + league_record GPLv3 cleanroom reference) still operator-gated; the s220 reframe chain continues stage-by-stage with the per-page UI-audit ritual between sessions. Pre-existing `lm-build-pending` dead-id reference in `_setEmptyState` (s107 era) is flagged by the audit but NOT touched here - it's out of scope for S4 + the no-op `getElementById` already returns null + the operator's "Don't add features, refactor, or introduce abstractions beyond what the task requires" rule applies. Item 115 carries unchanged. Frozen-file grant NOT used this session.

115. ✅ **2026-05-20 - s220 PGR S3 aggregator G score+MVP polish (1 commit `92c6a0f`, pushed; non-engine; non-frozen-files-only; no RC restart; JS/CSS asset-hash auto-bump per ADR-008; index.html picks up on next page nav).** Stage S3 of the s220 aggregator-G-style Post Game Review reframe. Three aggregator-G-pattern affordances added on the Last Match (Post Game Review) view, all sourcing from the existing `_rosterScores` 100-point heuristic in `web/js/panels/last_match.js` (already built s107/item 108) so the three numbers stay mutually consistent. No new backend, no new schema, no ENGINE change. **(1) Hero match-score block:** NEW `#lm-hero-score` mounted as the 6th column of `.lm-hero-identity` carries a big numeric 0-100 + tier label (Bad/OK/Good/Excellent). NEW `_setHeroScore(m, enriched)` + `_scoreTier(score)` helpers in `last_match.js`; tier-tinted via `data-tier` attribute. Hidden until LCU enrichment lands; thresholds 80/65/50 calibrated against the 100-point scale. **(2) Per-side MVP/SVP cards:** NEW `#lm-tc-ally-mvp` + `#lm-tc-enemy-mvp` empty divs in each `.lm-tc-group` between `.lm-tc-group-head` and the list. Card body painted by NEW `_renderMvpCard(cardId, top, sm)`: portrait + inline crown SVG (path `M2 14 L4 4 L8 9 L12 2 L16 9 L20 4 L22 14 Z`, NOT emoji per ASCII hard rule) + name + numeric score + tier label. `data-kind="mvp"` (winning-side top) gets gold-tinted purple bg + gold left-border; `data-kind="svp"` (losing-side top) gets accent-tinted. `rankSide` closure inside `_setTeamComp` extended to return `{meta, top}` so the card can pull the top-scoring row by participant_id (was just `meta`). **(3) Per-row score chip:** the existing `.lm-tc-score` cell becomes a `<div>` flex column stacking `<span class="lm-tc-score-badge">` (MVP/SVP/#N) over `<span class="lm-tc-score-value">` (integer 0-100). Row grid columns 46 -> 56 in `.lm-tc-row` AND `#lm-tc-table[data-aug="1"] .lm-tc-row` (both bumped so ally/enemy align in SR + ARAM Mayhem/Arena augment mode). Decimal + factors retained in the hover tooltip. **Files touched** (4 total, all non-frozen): `web/index.html` (+19 LOC: hero score block + 2 MVP card divs; grid widened 5 -> 6 cols), `web/js/panels/last_match.js` (+95 LOC: `_scoreTier` + `_setHeroScore` + `_renderMvpCard` + `rankSide` `{meta, top}` shape change + `_renderTcRow` score cell rewrite + `renderLastMatch` wire + `_setEmptyState` reset), `web/css/panels/last_match.css` (+147 LOC: hero score block + 4 data-tier color variants + grid widen + `.lm-tc-mvp` + 2 data-kind variants + crown svg fill + `.lm-tc-mvp-score` 4 tier variants + `.lm-tc-score` rewrite as flex stack + badge/value spans), `tests/test_last_match_score_card_dom.py` (NEW, +231 LOC: 26 grep-based DOM-contract tests in 5 classes). **Tests** mirror `test_draft_elo_panel_dom.py` + `test_augment_reco_panel_dom.py` pattern (cheap text-search smoke). HeroScoreBlockTests (8) cover mount IDs + hidden default + data-tt tooltip + JS wire + helper presence + tier buckets + CSS variants + grid widening. MvpCardTests (8) cover ally+enemy mounts + hidden default + helper presence + roster-scores source + crown-SVG-not-emoji + CSS variants + hidden rule + tier variants. PerRowScoreChipTests (5) cover badge+value emit + Math.round + flex-column + grid-template-columns pins (SR + augment-mode both). EmptyStateResetTests (2) cover hero score + MVP cards cleared in `_setEmptyState`. AsciiHygieneTests (3) scan JS + CSS for em/en-dashes + smart quotes (BAD dict built via chr() so the test file stays ASCII-clean against its own scan). **Verified:** py_compile + ruff + ASCII clean across all 4 touched files; full RC suite **2335 passed / 67 subtests / 0 failed** (+26 over s113 2309 baseline); live-verified via Playwright @ `https://127.0.0.1:8888/#last-match` on operator's ARAM Vel'Koz 4/9/25 last match - hero shows **48 BAD** (red), ally SVP **bliv 64 OK** (accent), enemy MVP **GenieInTheLamp 77 GOOD** (green), all 10 per-row chips populated with correct badge+numeric stack. ADR-008 asset-hash auto-bumps cover the JS/CSS panel edits; index.html change picks up on the next page nav (no asset-hash needed for the parent HTML). No DS restart, no RC restart. No ENGINE bump (non-engine). No frozen-file edits. **Don't-redo:** the three S3 affordances (hero score / MVP card / per-row chip) all source from the SAME `_rosterScores` heuristic - do NOT introduce a divergent scorer in any of the three sites (`MvpCardTests::test_js_mvp_card_uses_roster_scores` guards). Crown is INLINE SVG NOT an emoji - the ASCII hard rule forbids Unicode glyphs; do NOT swap to U+1F451 or any unicode star (`MvpCardTests::test_js_mvp_card_emits_crown_svg` guards). Tier thresholds (80/65/50) are calibrated against the 100-point `_rosterScores` scale + frozen in `HeroScoreBlockTests::test_js_score_tier_buckets` - if `_rosterScores` weights ever change, retune these thresholds + the test together so the visual tiers stay calibrated. Row grid 46 -> 56 also covers the 3-digit edge case (score=100); do NOT shrink back (`PerRowScoreChipTests::test_css_grid_row_widens_score_column` + `test_css_grid_aug_row_widens_score_column` both pin). `rankSide` now returns `{meta, top}` (was just `meta`) - the closure stays local to `_setTeamComp`; downstream callers use `.meta` / `.top`. ASCII hygiene scan uses chr()-built BAD dict so my own test file stays clean against its own scan; do NOT inline the glyphs (that would fail the test). **Carries forward:** S4 (tabs reframe Comp/Chart/Timeline/Insights -> AI Analysis/Graph/Build) + S5 (Replay page scaffold + Match-V5 timeline + league_record GPLv3 cleanroom reference) still operator-gated; the s220 reframe chain continues stage-by-stage with the per-page UI-audit ritual between sessions. Item 114 + 113 carries unchanged. Frozen-file grant NOT used this session.

114. ✅ **2026-05-20 - fleet OAuth Max migration finished + Legion ON/OFF desktop shortcuts (1 commit `cc9401b`, pushed; non-engine; no frozen edits; no RC restart needed).** Closes the OAuth fleet migration that item 100 claimed but only Legion had actually flipped. Operator's billing-console User-ID `182f0ecf0bc77e27788c33ddce44ac20c00097c662611fa5bf83e8b1f8bc43ac` matched Peer -> dispatched parallel bridge tasks for Peer + Game-PC 6-step OAuth migrations. **Peer:** was process-scope sk-ant-api03 (no User/Machine leak; injection was bridge daemon `subprocess.run` inheriting `os.environ` wholesale + operator-planted HKLM `ANTHsssROPIC_API_KEY` canary), no OAuth creds; operator `claude /login` mid-session enrolled under accountUuid `4eb3a75e-c116-4528-a6fb-d53971a1adf2` (matches Legion); Peer daemon patched env.pop, sha256 63c5bbce. **Game-PC:** TWO leak vectors that s100 missed - HKLM Machine `ANTHROPIC_API_KEY` + `C:/RC-Agent/.claude/settings.local.json` env block; OAuth was already enrolled (sk-ant-oat01-Rpf) but CLI preferred raw API on conflict per the CLI warning. Both vectors removed, daemon patched (sha256 9ff5d8fd), old pid 4792 -> 16416. **Legion repo patches:** `tools/legion_bridge_daemon.py` + `tools/gamepc_bridge_daemon.py` ALSO missing env.pop (item 100 only patched `bridge_watcher_actions.py`; the daemons are a different codepath); patched both with `_env = os.environ.copy(); _env.pop("ANTHROPIC_API_KEY"); env=_env`. Patching `tools/gamepc_bridge_daemon.py` ALSO keeps RC ON re-downloads from clobbering Game-PC's local patch on next boot. **Legion ON/OFF shortcuts:** NEW `tools/legion_on.ps1` (starts all 6 RC-* AtLogon/AtStartup tasks + DS port-listen guard at :8893 + Claude Desktop singleton via shell:AppsFolder) + `tools/legion_off.ps1` (Stop-ScheduledTask + taskkill /F /PID per "never Stop-Process" hard rule). Both idempotent (IgnoreNew refusal on schtasks, port-check on DS, singleton on Claude Desktop). Desktop shortcuts via WScript.Shell COM: `Legion ON.lnk` + `Legion OFF.lnk`. Old `Claude RC.lnk` backed up at `Claude RC.lnk.bak`. **NEW memory `feedback_oauth_flip_injection_vectors.md`** documents all 3 non-obvious vectors + verification ritual + auth-conflict shape. **Don't-redo:** OAuth fleet is now ACTUALLY 100% (was 33% per item 100); the 4 env.pop files fleet-wide are bridge_watcher.py + bridge_watcher_actions.py + legion_bridge_daemon.py + gamepc_bridge_daemon.py. Peer `C:\Peer-Bridge\peer_bridge_daemon.py` + Game-PC `C:/RC-Agent/gamepc_bridge_daemon.py` live OUTSIDE this repo - the Legion-side copies are the source-of-truth for next `RC ON` propagation. Legion's RC-BridgeDaemon (running pid 472) holds pre-patch code in memory; next Legion OFF+ON cycle picks up the patch. Peer recommendation for self-PID-lock on legion_bridge_daemon deferred (already has `_lock_held`/`_set_lock` at lines 188-194). **Carries forward:** (a) Operator pending on Peer: restart `\Peer-BridgeDaemon` schtask + optional kill of :8899 web_dashboard orphans pids 19824/21972 (vestigial from 2026-05-05, zero Anthropic calls). (b) Next session **s220 aggregator-G-style Post Game Review reframe** (UI; operator-decided single-match richer layout + RC-heuristic 0-100 score; staged S2-S5). (c) Verify $ drop on `mcp__anthropic-usage__query_usage` 2026-05-21+ once Max absorbs full day across all 3 nodes. (d) All item 113 carries unchanged.

113. ✅ **2026-05-20 - 3-agent parallel BACKLOG NOW lane drain wave 2 (6 commits `e0eabb7`..`89e645e` plus 3 merges, pushed; ENGINE 1.22.0 -> 1.23.0; non-frozen-files-only; DS :8893 restarted serves 1.23.0; RC :8888 restarted pid=7692 last_reload_ok=True - new dashboard route module triggered restart).** Operator "start what is left in backlog and roadmap in parallel" -> 3 worktree agents dispatched concurrent on 3 disjoint NOW-lane items, all merged + tested green + pushed. **Agent A `4e5d818` (aramAbilityHaste + aramTenacity engine CONSUMPTION):** wires the values exposed by item 109 iter 17 `6bba097` into the DS engine. NEW helpers in `agents/daemon_slayer/ability_dps.py:800-861` `_effective_ability_cd(base_cd, total_haste) -> base_cd / (1 + total_haste / 100)` (Riot canonical formula, mirroring s110 `58d1e87` summoner CD ledger) + `_total_ability_haste(scaled_stats, mode, base_ah=0.0)` that folds `aram_ability_haste` from scaled stats in ARAM mode. Applied in `compute_ability_dps` per-spell loop line 1336. `AbilitySpellDps` gains `base_cooldown` + `total_ability_haste` fields; `AbilityDpsResult` gains `aram_ability_haste` + `aram_tenacity_mult` fields (forward-marker storage + serialization for future EHP enemy-CC model; scope decision: do NOT build a full EHP-CC model now, marker forwards through to_dict with TODO). `cooldown` field semantics shifted to POST-haste effective cooldown (SR + zero-aramAH ARAM identity-preserved -> 42 existing test files unaffected). +26 tests `test_aram_ability_haste_consumption.py`; +13 stale ENGINE pins synced (1.22.0 -> 1.23.0) across DS test files. ENGINE bumped `agents/daemon_slayer/__init__.py:1324` with full changelog block. `base_ah` parameter seam reserved for future item-AH lane (currently 0 because `_effects_types.ItemEffect` has no `ability_haste_flat` field). **Agent B `e0eabb7` (HURTS-THEM/HELPS-US frontend toggle):** closes item 109 carry-forward (a). NEW `web/js/panels/ban_suggest_toggle.js` (240 LOC) + `web/css/panels/ban_suggest_toggle.css` (187 LOC) + `tests/test_ban_suggest_toggle_panel_dom.py` (25 tests across 5 classes mirroring `test_augment_reco_panel_dom.py`). Wires to backend `dashboard/routes_ban_suggest.py` (item 109 `6e7b7e5` already shipped). Mode chip persists in `localStorage[rc-cs-bansugg-mode]` default `"hurts_them"`; `data-bs-active="0|1"` attr-driven CSS flip with per-mode color theme (red HURTS, green HELPS). Per-candidate row shows active-mode rating + other-mode rating in muted text + `n=solo/pair` sample-density chip (bands low/mid/high mirror draft_elo thresholds). Reuses cached `/api/champ-select/ban-suggestions` for portrait metadata; dual-scoring via the new endpoint. `champ_select.js` integration: import block lines 11-17, `_csvRenderBanSuggestToggle()` call inside `_csvRenderSuggestions` lines 921-928, helper definition lines 1399-1442, section signature `bsdual:<count>` lines 1278-1284 coalesces re-renders. `web/css/dashboard.css` gained `@import './panels/ban_suggest_toggle.css'`. **Agent C `ea9b88f` (per-row contribution hover strip):** closes item 109 carry-forward (b). NEW `Contribution` dataclass + `top_contributions(ally_team, enemy_team, ..., limit=3)` sibling aggregator in `core/draft_elo.py:141-238` (NOT inside `team_score` - pure sibling over the same inputs). Returns `list[Contribution(kind, a, b, delta, n)]` sorted by `abs(delta)` desc; sign normalized to ally perspective (ally-pair +rating, enemy-pair -rating, matchup +rating). Route gained `?breakdown=1` query param (also `true`/`yes`/`on`) in `dashboard/routes_draft_elo.py:89-110,159-178` -> response carries new `top_contributions: [{kind, a, b, delta, n}, ...]` field (max 3); cache always stores the full payload, strip applied at serve time so cache hit rate identical regardless of breakdown flag. Frontend `web/js/panels/draft_elo.js:99-217` uses PURE CSS `:hover` toggle (zero JS event handlers, zero click-to-pin, zero state); `.draft-elo-chip:hover .draft-elo-contributions { display: block; }` + `pointer-events: none` on overlay. Guarded by `tests/test_draft_elo_panel_dom.py::HoverOnlyContractTests` which fails if any future change adds `addEventListener('click'`, `.onclick`, or a `pinned` class. **+82 tests total** (26 + 25 + 31). Live curl `GET /api/draft-elo?ally=22,64,55,89,12&enemy=42,67,69,33,99&breakdown=1` returned top-3: enemy-pair (42,99) delta=+240.82 n=3 / matchup (22,67) +240.82 n=3 / ally-pair (22,89) +190.85 n=2. **Merges `14c14ef`/`4d50e92`/`89e645e`:** no conflicts (3 agents touched disjoint files). DS suite **2803 passed** / 1 xfailed / 1663 subtests (+26 over 2777); wider RC suite **2309 passed** / 67 subtests / 0 failed (+56 over 2253 baseline). py_compile + ruff clean across all touched files. ASCII drift check on touched files found PRE-EXISTING non-ASCII carryover in `agents/daemon_slayer/__init__.py` / `agents/daemon_slayer/ability_dps.py` / `web/js/panels/champ_select.js` / `web/index.html` (arrows, multiplication sign, ellipsis, box-drawing - all carryover from earlier work, NOT agent-introduced; new files ban_suggest_toggle.{js,css}, test files, draft_elo.css additions are clean). **DS :8893 restarted** via `Stop-Process pid=4800` (auto-cleanup) + `pythonw tools/start_daemon_slayer.py` ritual (per `reference_ds_server_not_supervisor_watched` memory); previously failing `test_live_three_profiles` flipped pass post-restart. `/health` returns engine_version=1.23.0. **RC :8888 restarted** via `restart_trigger.txt` because Agent C edited `dashboard/routes_draft_elo.py` (route module); pid=7692 alive=True last_reload_ok=True. Asset-hash auto-bumps cover JS/CSS panel changes (ADR-008). **Worktree cleanup:** unlocked + removed 3 worktrees (`agent-a7f55157b2436f660`, `agent-a0a3a993690d42f42`, `agent-ab2ebeae19b5a5b50`); deleted 4 merged branches (`worktree-agent-a7f55157b2436f660`, `worktree-agent-a0a3a993690d42f42`, `feat-per-row-contributions-hover-strip`, plus dangling `worktree-agent-ab2ebeae19b5a5b50`). **Don't-redo:** all 3 BACKLOG/ROADMAP NOW items now SHIPPED + verified live; the engine `cooldown` field now means POST-haste effective cd (was: base rank cd) - back-compat preserved through SR + zero-aramAH ARAM identity; the `aram_tenacity_mult` field is FORWARD-MARKER only (no consumer built; future EHP enemy-CC model owns the wire); the `base_ah` parameter is a SEAM for future item-AH lane (currently 0 because `_effects_types.ItemEffect` has no `ability_haste_flat` field) - when that lane lands, single-line change in `compute_ability_dps` to pass real total; the `cooldown` schema change passes all 42 existing DS test files because the haste-formula degenerates to identity at total_haste=0; the HURTS-THEM/HELPS-US toggle persists via localStorage (not server-side) - mode flips locally per-machine; the per-row contribution hover strip is PURE CSS `:hover` with no JS state - the `HoverOnlyContractTests` guard PREVENTS future regressions to click-to-pin patterns (do NOT relax it without operator approval); the `top_contributions` cache strip-at-serve pattern is deliberate (avoids cache-key blow-up while letting breakdown=1 reuse breakdown=0 entries). **Carries forward:** (a) DS engine `base_ah` for item-AH lane queued (requires `_effects_types.ItemEffect.ability_haste_flat` field + items.json/Meraki ingest pass + per-item ability_haste registry; touches `_effects_data.py` + extractor). (b) `aram_tenacity_mult` consumer queued (future EHP enemy-CC model in `agents/daemon_slayer/ehp.py` or successor - reads the field from scaled stats and applies to enemy-CC duration in the EHP-vs-CC simulation). (c) `STAT_GRANT_CALC_KEYS` displacement seam STILL EMPTY at cdragon 16.10.1 (item 112 carry-forward (a) unchanged) - populates when Arena S2 Augment Level-Up patch 26.09 PBE confirms schema (likely `levels[]` axis on `Augment.dataValues`). (d) post-game-phases live-game smoke STILL pending (item 112 carry-forward (d)). (e) item 110 Peer bridge_watcher zombie + Game-PC vision triad SKIPPED + RC-DDragonMirrorRefresh next 5/21 03:30 cron = live test of `943d2e0` retry-with-backoff (unchanged). (f) item 109 standing carry-forwards (Arena S2 patch 26.09 / ruff+type-hint pass) STILL operator-gated. (g) frozen-file grant NOT used this run - 3 agents routed entirely around frozen list; grant does NOT carry forward.

112. ✅ **2026-05-20 - 3-agent parallel BACKLOG NOW lane drain (4 commits `e0eec7e`..`21657eb` merge `21657eb`, pushed; ENGINE 1.21.0 -> 1.22.0; non-frozen-files-only; DS :8893 restarted serves 1.22.0; RC :8888 unchanged - asset-hash auto-bumps for the new web/ panel).** Operator "start what is next on backlog in parallel" -> 3 parallel worktree agents dispatched on independent NOW-lane items, all merged + tested green + pushed. **Agent A `c965167` + `583a3bc` (cdragon mFormulaParts evaluator):** NEW `agents/daemon_slayer/augment_formula_eval.py` (~325 LOC) interprets the 4 typed cdragon formula parts (`NamedDataValueCalculationPart` / `StatByNamedDataValueCalculationPart` / `StatByCoefficientCalculationPart` / `NumberCalculationPart`) wrapped by optional `mMultiplier`, composes via sum-of-parts. Out-of-scope shapes (`ByCharLevelInterpolation` / `BuffCounterByCoefficient` / `SumOfSubParts` / `ProductOfSubParts` / `AbilityResourceByCoefficient` / `GameCalculationModified` / hash-name `{...}`) return 0.0 via unknown-shape sentinel - never raise. Wired into `agents/daemon_slayer/augments.py:compute_augment_stats` as PREFERRED path with `_AUGMENT_STAT_OVERLAYS` registry fallback. **Displacement seam `STAT_GRANT_CALC_KEYS` is intentionally EMPTY at cdragon 16.10.1** - enumerated all 82 unique calc keys across the 83/220 augments with non-empty `calculations`, every key is damage/heal/shield/conversion, none are stat grants. Wiring is in place; production behavior is byte-identical today. Future Arena patches (S2 Level-Up 26.09 etc.) populate `STAT_GRANT_CALC_KEYS` to displace overlay registry entries without further engine changes. +47 tests in `agents/daemon_slayer/tests/test_augment_formula_eval.py`. `583a3bc` synced 13 stale ENGINE_VERSION pins in DS test files 1.21.0 -> 1.22.0. ENGINE bumped in `agents/daemon_slayer/__init__.py:1324` with full changelog block. **Agent B `a250416` (LoLytics WPA framework PGR S2):** NEW `core/post_game_score.py` (827 LOC) pure-Python LR via batch gradient descent (sklearn/numpy NOT installed in RC env so no new dep - feature vector + sigmoid + gradient steps inline). Features per timestamp: gold diff / xp diff / dragon-baron-herald diff / KDA diff / turret-inhib diff. Train fn reads `rewind_history.db` timeline rows -> fits LR -> saves to `data/post_game_wpa_model.json` (plain JSON, not pickle - future retrains inspectable). 200 most-recent SR matches yielded 800 samples (above N>=20 threshold), feature weights sane: gold_diff +1.32 / xp_diff +0.65. Predict fn loads JSON + scores feature vector -> [0.0, 1.0]; WPA per event = prob_after - prob_before. TIF + death-timer formulas implemented from LoLytics reference. NEW route `dashboard/routes_post_game_wpa.py` (202 LOC) GET `/api/post-game-wpa?match_id=<id>` returns `{ok, match_id, model, events: [...], top_phases: [...top-3-by-abs-wpa...], elapsed_ms, cached}`. NEW frontend `web/js/panels/post_game_phases.js` (163 LOC) stacked card list mounted inside last-match view (`web/index.html` + `web/js/panels/last_match.js` import + `_setPhases` hook + `web/css/panels/last_match.css` styling). Route registered in `dashboard/_dispatch.py`. +68 tests in `tests/test_post_game_score.py` (587 LOC) + `tests/test_routes_post_game_wpa.py` (300 LOC). **Live curl verified:** `GET /api/post-game-wpa?match_id=NA1_5439050124` returns ok=true model=trained event_count=86 elapsed_ms=2 top-3 phases t=60/65/73s WPA +2.3% each (CHAMPION_KILL flurry by team 100). 404 path (`NOPE_X`) + 400 path (no match_id) + cached=True on second hit all verified. **Agent C `e0eec7e` (Hextech-drake LCU wire):** Extended `dashboard/_state_cooldowns.py` (+73 LOC) with NEW `_hextech_drakes_by_team()` helper that watches `gameData.events.Events` for `EventName="DragonKill"` + `DragonType="Hextech"` events, maps `KillerName` to team via `allPlayers[].summonerName -> .team` lookup, tallies per team. Per-participant `hextech_drakes` field wired into the participant build loop -> feeds the s110 `58d1e87` haste-formula compute (which already had `HEXTECH_DRAKE_AH_PER_STACK=5` constant accepting the count). +10 tests in `tests/test_state_cooldowns.py` (53 total in file). Killer-team mapping pattern already existed in `core/decision_detector.py:419-425` (used `summonerName` + `riotIdGameName` fallback); new helper is module-local intentionally narrow. **Merge `21657eb`:** no conflicts (3 agents touched disjoint files); merged worktree branch into current + fast-forwarded main + pushed. **Verified:** DS suite **2777 passed** / 1 xfailed / 1663 subtests (+47 over 2730); wider RC suite **2253 passed** / 67 subtests / 0 failed (+86 over 2167 baseline); py_compile + ruff clean across all touched files. ASCII drift check found 4 PRE-EXISTING non-ASCII bytes in `dashboard/_dispatch.py` (ellipsis in comment) + `web/js/panels/last_match.js` (arrow in comment) + `web/css/panels/last_match.css` (arrow in comment) + `web/index.html` (caret triangle U+25BE in title-caret) - all carryover, NOT agent-introduced. **DS :8893 restarted** via taskkill + `pythonw start_daemon_slayer.py` ritual (per `reference_ds_server_not_supervisor_watched` memory); pid 1956 -> new, `/health` returns engine_version=1.22.0. RC :8888 dashboard reloads JS/CSS panels via asset-hash bump (ADR-008) - no restart needed. **Worktree cleanup:** unlocked + removed `agent-a80db4333055fbb63` worktree; deleted 4 merged branches (`feat-hextech-drake-cd-wire` / `feat/augment-formula-eval-2026-05-20` / `worktree-agent-a80db4333055fbb63` / `wpa-post-game-s2`). **Don't-redo:** all 3 BACKLOG NOW items now SHIPPED + verified live; the cdragon formula evaluator's empty `STAT_GRANT_CALC_KEYS` seam is INTENTIONAL (production behavior identical to pre-merge today, displacement happens only when Arena patches add stat-grant calc keys) - do NOT manually populate it; the WPA model is pure-Python LR (no sklearn/numpy dep) - do NOT re-pitch a numpy refactor unless the fleet adds those deps; `data/post_game_wpa_model.json` is a TRAINED checkpoint not a pickle - inspectable + retrainable; the Hextech-drake count comes from Live Client events at runtime, NOT from a static table - the count moves over the game; the new `summs.summoner_haste` + `ult.ability_haste` keys in cooldown output + the new `hextech_drakes` per-participant field are forward-compatible additions; cdragon `_AUGMENT_STAT_OVERLAYS` registry is KEPT as the today-fallback - do NOT delete; DS server restart was clean - no game in progress so no liveclient cache flush concern. **Carries forward:** (a) `STAT_GRANT_CALC_KEYS` seam will populate when Arena S2 Augment Level-Up patch 26.09 ships (item 109 carry-forward (e) still queued, schema break ahead). (b) `aramAbilityHaste` + `aramTenacity` engine CONSUMPTION still queued (item 110 carry-forward (a)) - the WPA-side ability-haste model in the new `core/post_game_score.py` is event-replay-based, NOT the cycle-math model the BACKLOG entry wants for `agents/daemon_slayer/ability_dps.py`; the consumption work targets DS cooldown math, not PGR. (c) WPA model retraining cadence operator-gated - the trained checkpoint is good for the current 200-match window; auto-retrain on a cron is a future concern when match volume grows. (d) post-game-phases frontend panel is wire-complete + live-fixture-verified but the operator-gated live-game smoke (real SR/ARAM match end-to-end) still pending. (e) item 110 Peer bridge_watcher zombie + Game-PC vision triad SKIPPED + RC-DDragonMirrorRefresh 5/21 03:30 + item 110 carry-forward unchanged. (f) item 109 carry-forwards (a) HURTS-THEM/HELPS-US frontend / (b) per-row hover contribution strip / (e) Arena S2 patch 26.09 / (g) ruff/type-hint pass STILL operator-gated. (h) frozen-file grant NOT used this run - 3 agents routed entirely around the frozen list; grant does NOT carry forward.

111. ✅ **2026-05-20 - fleet bridge daemon symmetry + autonomous cadence verified (3 commits `f40e8fb`..`0e7cb8e`, pushed; non-engine; 1 frozen edit operator-approved; no DS restart; RC restart pid=7360 alive=True last_reload_ok=True).** User-driven scoped session. **`f40e8fb` fix(gamepc):** `tools/gamepc_boot.ps1` section 6 was launching `AppData\Roaming\Claude\claude-code\<ver>\claude.exe` which IS the Claude Code CLI native installer (the comment claiming "Desktop app" was wrong - that's where the CLI lives now under the native installer + npm shim). Repointed to Squirrel-installed Claude Desktop at `AppData\Local\AnthropicClaude\claude.exe` (Product=Claude v1.8089.1). Squirrel auto-resolves newest `app-<ver>\` subdir at launch time so no version enumeration needed. CLI fallback (sec 6c) removed entirely per operator decision. Also **unset `ANTHROPIC_API_KEY` at Machine scope on Game-PC** (User was already empty; Machine had `sk-ant-api03-...` ~110 chars) - bridge_watcher subprocess env.pop's it per fleet item 100 + uses OAuth so no regression. Mirrors Legion item 110 (Claude RC.lnk repointed to chat app). **`cb74e00` feat(bridge):** legion_bridge_daemon + frozen push-notif fix + honest gauge naming. Closes fleet daemon symmetry gap: gamepc + peer shipped zero-cost bridge sentinels months ago, Legion had NEITHER. Without it, kind=task envelopes targeted at legion that didn't match the narrow `auto_read_patterns` / `auto_ops_verbs` whitelist (e.g. RC-VerifyBridgeRoundtrip-Once's "Reply with hostname/pid/ts" prompt) classified as `escalate (no auto-action pattern match)`. Push-notif lane then tried bare `argv[0]="claude"` which `subprocess.run` on Windows cannot resolve to `claude.cmd` (npm shim) -> WinError 2 silently. Tasks sat unhandled until operator manually `/process-bridge-tasks`'d. Today's RC-VerifyBridgeRoundtrip-Once at 16:48:48 exited 0xC000013A confirming. **NEW `tools/legion_bridge_daemon.py`** mirrors gamepc/peer pattern (TARGET=legion, 30s idle poll / 10s post-work poll; `_resolve_claude()` helper with `ANTHROPIC_CLAUDE_PATH` override + npm shim probe + shutil.which fallback; lock + health under `ops/runtime/`; rotating log under `logs/`). **NEW `ops/install_RC_LegionBridgeDaemon.ps1`** registers `RC-BridgeDaemon` on Legion (AtLogOn Highest, restart 3/1min, ExecutionTimeLimit=0). **Frozen-file edit (operator-approved)**: `tools/bridge_watcher.py:256` push-notif spawn now uses `_actions._resolve_claude_executable()` instead of bare `"claude"` - fixes fleet-wide push-notif spawn failure. Frozen-file grant honored minimally (single-line change; do NOT extend the grant). **Bridge field split** `dashboard/routes_state.py`: `bridge.age_s` -> `bridge.gamepc_result_age_s` (honest naming - the gauge ONLY ever tracked `kind=result + source=gamepc` entries via `dashboard/_bridge_log.py:159 gamepc_result_age_s()`; the Prom metric `rc_bridge_gamepc_result_age_seconds` was honest but the /api/health/all surface name was misleading). ADDED `bridge.legion_daemon` sub-block surfacing the new daemon's health (status, pid, invocations_since_boot, last_task_ts, last_check_age_s) from `ops/runtime/legion_bridge_daemon_health.json`. `web/js/main.js:6229,6232` consumer renamed. **`0e7cb8e` fix(bridge-daemons):** CREATE_NO_WINDOW patch on all 3 daemon's `subprocess.run` call. Operator surfaced conhost console window flashing at each cadence interval on Peer (Game-PC also reported). Root cause: pythonw.exe parent + `.cmd` npm shim child means Windows must spawn `cmd.exe` to host the batch script + conhost window appears; STARTUPINFO alone can't suppress for `.cmd`. Fix: `creationflags=subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0`. Applied to all 3 daemons (legion + gamepc + peer) for fleet symmetry. **Peer bridge daemon was NEVER INSTALLED on Peer** - this run surfaced it via diagnostic: `Get-ChildItem -Recurse peer_bridge_daemon.py` returned empty. The rc_facts startup line "peer bridge daemon: watcher=alive queue=0 age=5s" was reading bridge_watcher heartbeat publisher state, NOT a real daemon process. Operator pulled `peer_bridge_daemon.py` from Legion's `/agent/` allowlist (already in `_AGENT_ALLOWED` since the file shipped) to `C:\Peer-Bridge\peer_bridge_daemon.py`; registered scheduled task `Peer-BridgeDaemon` (AtLogOn user=$env:USERNAME Interactive Highest, restart 3/1min, ETL=0); first launch pid 24772; post-CREATE_NO_WINDOW re-pull pid 29720. **End-to-end verified across 4 autonomous test rounds:** Round 1 (pre-daemon) verify task exited 0xC000013A unhandled; Round 2 (post-Legion-daemon) RC-VerifyBridgeRoundtrip-Once LastResult=0x00000000 PASS latency 44.35s; Round 3 (autonomous-only worded) gamepc replied in 2m26s, Peer did NOT reply (daemon dead); Round 4 (post-Peer-install + CREATE_NO_WINDOW pull) BOTH gamepc + peer replied autonomously; final hash-verify round had each subprocess Claude include `Get-FileHash` SHA256 of the daemon file in the reply body - gamepc sha256=18196dfbecff1539 hostname=WIN-T1GK0AC0JNF pid=8584 MATCH; peer sha256=f075f314dd6a7e0c hostname=DESKTOP-CTBMA25 pid=29720 MATCH (both vs Legion repo source of truth). Full fleet symmetry proven; bridge.gamepc_result_age_s gauge cleared 79961s -> 45s (status red -> green). **Verified:** `py_compile` + ruff + ASCII clean across all 5 edited files; CI not blocked (non-engine + no test changes); RC restarted via `restart_trigger.txt` (pid -> 7360 last_reload_ok=True); Legion bridge_watcher killed + Start-ScheduledTask restart (pid 1064 -> 8404 picks up frozen edit); Legion daemon restarted in place (pid 2216 -> 472 picks up CREATE_NO_WINDOW). **Don't-redo:** (1) gamepc shortcut points at Squirrel Claude Desktop directly - do NOT repoint to `claude-code\<ver>\claude.exe` (that's the CLI native installer); (2) Legion has its own bridge daemon now - do NOT pitch a `/loop /process-bridge-tasks` pattern as a fix; (3) `bridge.age_s` field is GONE from `/api/health/all`, consumers MUST read `bridge.gamepc_result_age_s` (web/js/main.js + any future readers); (4) Peer daemon installed at `C:\Peer-Bridge\peer_bridge_daemon.py` (no repo clone on Peer); when patching the daemon source on Legion, Peer needs `/agent/` re-pull via the durable one-liner pattern; (5) `bridge_watcher.py:256` frozen edit shipped using `_actions._resolve_claude_executable()` - do NOT revert to bare `"claude"`; (6) `ANTHROPIC_API_KEY` is unset at Machine scope on Game-PC; CLI subprocess invocations there use OAuth solely; (7) all 3 daemons now spawn `claude --print` with `CREATE_NO_WINDOW` - do NOT remove the creationflag thinking pythonw parent already suppresses it; the .cmd shim requires conhost which STARTUPINFO can't gate. (8) the durable PATTERN for new bridge daemon nodes: pull from `/agent/` allowlist + register at-logon scheduled task with restart-on-failure + ETL=0; the operator gate is the path + scheduled task installer, the script itself is symmetric. **Carries forward:** (a) **Peer bridge_watcher zombie** - separate from the daemon. The watcher process (pid 11832, `bridge_watcher.py --node peer`) is "alive" but its internal state (`last_poll_at`, `updated_at`) is frozen at 2026-05-06 06:05 CDT (14 days stale). The heartbeat publisher publishes fresh wall-clock `received_at` reading a stale snapshot of the watcher's own state file. Daemon covers task autofire so this is non-blocking. Worth a separate Peer-side look (`Get-Process` + log tail + restart task) when convenient. (b) Game-PC vision screen-agent triad still SKIPPED per gamepc_boot.ps1 sec 4 isolation test 2026-05-16 (Vanguard BSOD investigation); ingame OCR/Sonnet escalation paths inert until reverted. (c) RC-DDragonMirrorRefresh next cron 5/21 03:30 is the live test of item 110's `943d2e0` retry-with-backoff fix. (d) item 110 carries unchanged.

110. ✅ **2026-05-20 - housekeeping batch + cdragon calculations enrichment phase-1 + ddragon retry-fix + summoner CD haste-formula (7 commits `1a8e0bf`..`58d1e87`, pushed; no ENGINE bump; non-engine; no frozen-file edits; no DS restart; RC :8888 alive pid 1464 last_reload_ok=True).** User-driven scoped session. **Branch + worktree cleanup:** `gh pr list` empty; 4 local + 1 remote stale branches deleted (all 0-ahead of main); locked worktree `agent-a7da71cb6830f84fc` (107MB, commit 18589ab on main) unlocked + force-removed + branch deleted. **Claude RC.lnk repointed** to launch Claude Desktop (MS Store AUMID `Claude_pzs8sxrjxfjjc!Claude` via `explorer.exe shell:AppsFolder\...`) instead of the CLI launcher; smoke-launched pid 1368 "Claude" window; `tools/claude-rc.ps1` deleted + docstring ref in `tools/verify_bridge_roundtrip.py` updated (`3571e1b`). **4 dated 2026-05-02 docs archived** (`3571e1b`): `docs io RC peer/{PEER_VIP_SESSION_CONTROL_INHERITANCE, PEER_VIP_UPDATE_FROM_RC, PEER_VIP_WT_INHERITANCE, CROSS_CLAUDE_LEARNING_SYNC_VISION}_2026-05-02.md` -> `docs/_archive/2026-05-02-rc-peer/` per CLAUDE.md convention; 3 doc-only ref updates in `core/lessons_{ack_watcher,confidence,revert}.py`. 2 files LEFT IN PLACE because of frozen-file refs (`main.py` for `PEER_VIP_BRIDGE_MONITOR_FOR_RC_2026-05-02.md`, `tools/sync-all-md.md` for `RC_PHASE1_LESSON_SCHEMA_2026-05-02.md`). **`943d2e0` fix(ddragon):** retry-with-backoff on transient HTTP errors. RC-DDragonMirrorRefresh exit_code=2 root cause was 1/6767 assets timing out per cron tick (profileicon/5668.png in the 03:30 run). `tools/ddragon_mirror_refresh.py:_http()` now retries `URLError` (incl. socket.timeout) + HTTPError 429/500/502/503/504 with 1s+2s backoffs (max 2 retries); HTTPError 4xx not retried. +4 tests. Live-verified: prior run exit 2 -> re-run exit 0. Next 03:30 cron should land green. **`4808f64` chore(gitignore):** `_scratch/` (~1MB pytest output + wakeup snapshots + iter logs from prior headless run); mirrors `_archive/` quarantine pattern. **`8f7a71b` feat(ds):** cdragon `calculations` formula defs lifted into `Augment.from_record()` (`+calculations: dict` field + `+__post_init__` None->{} default for frozen-dataclass init + `+calculation_keys` property). Upstream extractor `tools/daemon_slayer_extract.py:762` already captured them; was being trimmed at the Augment boundary. 83/220 augments at cdragon 16.10.1 ship non-empty calculations (UndyingGuard / Typhoon / ServeBeyondDeath formula defs). Dataclass-only; NO ENGINE bump; no scorer change; no DS restart. +3 tests. **Still owed:** formula-evaluator (`mFormulaParts` typed-part interpreter: `NamedDataValueCalculationPart` / `StatByNamedDataValueCalculationPart` / `StatByCoefficientCalculationPart` / `NumberCalculationPart` + `mMultiplier`) that consumes `calculations` to displace `_AUGMENT_STAT_OVERLAYS`; separate slice, ENGINE bump owed when it lands. **`94fe721` + `8d1c28e` docs(backlog):** BACKLOG.md 125 -> 105 lines, 0 strikethrough. Dropped 14 shipped entries + the stale `hps_split_aram_heal_shield_mults` entry (actually shipped iter 17 `816ab4a` in item 109 - verified via `hps.py:336-337` HpsResult fields + line 350-359 mode_multiplier @property). Rewrote `aramAbilityHaste`/`aramTenacity` entry: exposure shipped `6bba097` (engine.py:210-221), CONSUMPTION still owed because DS engine has no ability-haste model at all (`ability_dps.py:49` comment). **`58d1e87` feat(coach):** haste-formula cycle math for summoner cooldown ledger. Replaced additive-percent CDR in `core/summoner_cooldowns.py` with Riot's canonical `eff_cd = base / (1 + h/100)` (additive model double-counted at high stacks: Cosmic + Boots showed -28% when 36 SSH compresses to -26.5%). New constants: `COSMIC_INSIGHT_SSH=18` / `LUCIDITY_SSH=18` / `LUCIDITY_AH=12` / `MAGICAL_FOOTWEAR_SSH=10` / `HEXTECH_DRAKE_AH_PER_STACK=5`. New helpers `_summoner_haste(runes, items)` / `_ability_haste(items, hextech_drakes=0)`; old `_cdr_summs` / `_cdr_ult` / cdr-fraction `_effective_cd` deleted. Participant dict: new optional `hextech_drakes: int` (defaults 0). Output adds `summs.summoner_haste` + `ult.ability_haste` for panel tooltips. Test suite 14 -> 29 (+4 Hextech Drake +3 haste-totals shape +1 negative-haste extreme); `test_state_cooldowns.py` payload-shape line updated for the 2 new keys. Pre-fix absolute pin (216s Flash at 28%) replaced with haste-formula pin (220.59s Flash at 36 SSH). **Verified:** wider RC suite 2096 passed + 67 subtests; py_compile + ruff + ASCII clean. No DS restart. No frozen-file edits. **Don't-redo:** all 7 commits real + pushed; the dated-docs archive deliberately left 2 files in place because moving them needs editing frozen `main.py` + `tools/sync-all-md.md` and item-109's frozen-file grant does NOT carry forward; cdragon-arena calculations enrichment is HALF SHIPPED (dataclass-layer only) - the formula-evaluator that consumes `mFormulaParts` to displace `_AUGMENT_STAT_OVERLAYS` is the NEXT slice and that will ENGINE-bump; CD ledger output now carries 2 NEW keys (`summoner_haste`, `ability_haste`) - dashboard panel was already forward-compatible since it reads `d_cd_remaining_s` etc., but the new keys can be tooltip-rendered; the `_cdr_*` legacy function names are GONE from `core/summoner_cooldowns.py` (do NOT recreate them - the haste-formula is the canonical Riot math); the locked-worktree cleanup pattern (unlock + force-remove + branch -D, safe when commit is already on main) is durable. **Carries forward:** (a) `aramAbilityHaste` + `aramTenacity` engine CONSUMPTION still queued (exposure shipped iter 17 but no scorer reads them; requires baseline ability-haste model in DS engine first - currently NONE per `ability_dps.py:49`). (b) cdragon `calculations` formula evaluator queued (`mFormulaParts` typed-part interpreter to displace `_AUGMENT_STAT_OVERLAYS`; ENGINE bump owed). (c) CD ledger Live Client wire for Hextech drake count - `dashboard/_state_cooldowns.py` doesn't yet count Hextech drakes from `gameData.events.Events` (DragonKill with DragonType="Hextech"); phase-2 wire. (d) item 109 carries unchanged: HURTS-THEM/HELPS-US frontend, per-row hover contribution strip, LoLytics WPA framework, Arena S2 Augment Level-Up patch 26.09, ward-heat frontend liveclient wire, shaper UI gated on 100% DS coverage, live-game UX validation, standing operator-gated ruff/type-hint pass. (e) the MQTT-style haste-aware CD math item from research-wave is now ESSENTIALLY done at the unit-test level via this commit; the residual is the Hextech-drake Live Client wire (c).

109. ✅ **2026-05-20 - second headless run (UX waves 1+2 + research wave + DS audit iter 17-19; 12 commits `f4938af`..`9f356a3` pushed; ENGINE 1.17.0 -> 1.21.0; frozen-file edits authorized + minimally used; CI 12/12 green after one same-run ruff-F541 fix `cb73757`; DS :8893 serving 1.21.0; living synopsis `C:\Users\Administrator\Desktop\RC_HEADLESS_SYNOPSIS_2026-05-20.md`).** **UX wave 1 closed `f4938af`:** ward-heat frontend strip (last item-108 carry-forward (a)) - `web/js/panels/ward_heat.js` + CSS + mount in MAP head + producer wire to `core/liveclient_cache` via NEW listener API (`add_listener`/`clear_listeners` + `_fire_listeners` post-fetch) + `ward_producer.tick_from_snapshot` adapter + idempotent `install_liveclient_listener` wired in `main.py` (frozen, authorized). **UX wave 2 shipped `ab3f553` + `00051f3`:** Elo log-odds team-vs-team draft aggregator - `core/draft_elo.py` pure math (winrate_to_rating = -400*log10(1/wr-1), team_score, DraftSide, pair enumerators) + `core/draft_elo_db.py` rewind queries composing on smoothed_rates.py + `dashboard/routes_draft_elo.py` ~50 SELECTs ~50ms cold + 5-min TTL cache + frontend chip `web/js/panels/draft_elo.js` with sample-density n=N/N indicator (research-wave's "strongest opinion" - the calibrated-trust signal other tools cannot ship). +38 tests; live `/api/draft-elo` ally=64,22,55,89,12 / enemy=42,67,69,33,99 -> ok=True predicted_wr=0.292 team_score=-154.19 min_solo=9. **HURTS-THEM/HELPS-US ban-suggest `6e7b7e5`:** `dashboard/routes_ban_suggest.py` returns BOTH ratings per candidate (solo + hurts_them via enemy-pair + cross-team matchup + helps_us via ally-pair) so the frontend toggle is explicit - the genuine market gap surfaced by the research wave. **Multi-agent research wave (5 parallel slices):** LoL overlay landscape / champ-select UX / item-data sources / Arena+Cherry coaching / post-game review methodology. Triage LOCKED in `BACKLOG.md` "Multi-agent research wave triage 2026-05-20" section. NOW queued: LoLytics WPA framework (MIT) over Match-V5 timeline -> PGR S2; cdragon-arena dataValues+calculations enrichment; MQTT-style haste-aware CD math from SummonerTrackerOverlay pattern. FUTURE: Arena S2 Augment Level-Up + Crafting Round (patch 26.09 schema break); Draft Tool Z8/Jfghanimah/loldraft-analyzer (MIT) as second-opinion; Coaching App Z7 ensemble methodology read-only; LDP community tier (Riot pro telemetry watch); CommunityDragon broader 7-file catalog; Riot per-role grading rubric; per-row contribution hover strip; Aggregator C 8-axis GPI spider. CLOSED at length: MobaHinted dead Sep 2024, LeagueAkari GPL-only, all spectator-stream OCR HUDs irrelevant, Arena augment WIN-RATE display banned by Riot policy 2024-04, persistent counter-pick sidebars (clutter at 1920x1080), neural one-shot scorers (destroy provenance), draft tool L "+" fork (zero algo value), Fandom wiki stale, CommunityDragon/LeaguePatchNotes archived 2020. **DS audit iter 17-19 (3 ENGINE bumps 1.17.0 -> 1.21.0):** iter 17 `816ab4a` hps_split_aram_heal_shield_mults (item-108 carry-forward (a)) - `_aram_heal_shield_modifiers` returns (heal_mult, shield_mult); HpsResult gains heal_mult+shield_mult, mode_multiplier kept as @property aliasing heal_mult for backward compat; 24 champs split (Camille 1.20/1.10, LeeSin 1.10/1.20, Milio 0.95/0.90, Nunu 1.10/1.20, +20 more); 1.17.0 -> 1.18.0. iter 17 `6bba097` aramAbilityHaste + aramTenacity exposure (item-108 carry-forward (b)) - `_apply_mode_modifiers` writes `scaled['aram_ability_haste']` + `scaled['aram_tenacity_mult']`; 22 champs non-zero AH + 17 champs non-1.0 tenacity; exposure-only no scorer consumes yet; 1.18.0 -> 1.19.0. iter 18 `74d2ba6` Lich Bane Spellblade AP 50% -> 40% per Meraki 16.10.1 (3100 + 223100 Arena mirror); Trinity/Sheen/ER/Iceborn verified clean; 1.19.0 -> 1.20.0. iter 19 `9f356a3` Nashor's Tooth AP 20% -> 15% per Meraki (3115 + 223115 mirror); new value-pinned test (ap=100 -> 30.0); 1.20.0 -> 1.21.0. iter 20-26 NO-CHANGE on Stridebreaker / Liandry / Luden / Hollow Radiance / Sunfire / Statikk-RFC / Profane Hydra; halted at 7-of-11 no-changes because high-confidence drift candidates exhausted in this audit window. **/headless-upgrade skill `b007783`:** new `tools/headless-upgrade.md` (gitignored mirror at `.claude/commands/`). 11-section skill captures pre-flight baseline + phase loop discipline (LOCAL ruff before push - the CI killer F541 caught same-run in `cb73757`, test gates, restart-aware reload, no `git add -A`, HEREDOC commits, post-push CI verification) + frozen-file grant scope (this-run-only) + ASCII hygiene + multi-agent dispatch protocol + DS audit iteration (11 no-change halt + Meraki source of truth + property-style tests) + interrupt protocol + 10h+ extension trigger + 11 anti-patterns from past runs + final banner format. **Verified:** DS **2730 passed / 1 xfailed / 1663 subtests** (+20 over 2710); wider RC suite **2167 passed** (+50 over s108 baseline 2117); CI **12/12 green** (one same-run ruff fix); py_compile + ruff + ASCII clean; DS :8893 serves `engine_version=1.21.0`; RC :8888 alive pid 16492 last_reload_ok=True. Living docs synced this commit: CLAUDE.md item 109; ROADMAP.md SHIPPED entries for UX wave 1+2; BACKLOG.md research-wave triage; docs/DAEMON_SLAYER.md ENGINE 1.21.0 + 2730 tests; WAKEUP_NOTES.md top entry. **Don't-redo:** all 4 ENGINE iterations (17 hps split, 17 AH/Tenacity, 18 Lich Bane 40%, 19 Nashor 15%) are real Meraki 16.10.1 corrections - do NOT revert; `mode_multiplier` @property on HpsResult is the backward-compat alias for heal_mult (SR/ARAM ratio test still works through it); engine's exposure-only posture for aramAbilityHaste + aramTenacity is intentional (no scorer reads them YET but the data lane is now visible); spellblade family + AP-scaling on-hit family are now Meraki-coherent through Lich Bane 40% AP + Nashor 15% AP; research wave's CLOSED list is authoritative (do NOT re-research MobaHinted/LeagueAkari/spectator HUDs/Arena win-rates - all license- or policy-blocked); HURTS-THEM/HELPS-US toggle FRONTEND deferred as operator-gated polish session (backend wire-complete + tested); the n= chip on draft-elo is the calibrated-trust signal that other tools cannot ship (shrinkage primitive ours). **Carries forward:** (a) HURTS-THEM/HELPS-US ban-suggest FRONTEND deferred (champ_select.js is 2815 lines; the toggle chip + sortable candidate list is a focused polish session). (b) Per-row hover contribution strip queued from research wave (Draft Tool L/Diff15 pattern, hover-only). (c) LoLytics WPA framework integration -> `dashboard/routes_post_game_wpa.py` + `core/post_game_score.py` + `web/js/panels/post_game_phases.js` aligns with s220 PGR S2 reframe. (d) cdragon-arena dataValues+calculations enrichment queued. (e) Arena S2 Augment Level-Up + Crafting Round patch 26.09 - schema break ahead; pre-stage `level` axis on `compute_augment_stats()` when 26.09 PBE confirms. (f) MQTT-style haste-aware CD math queued for `core/summoner_cooldowns.py`. (g) the 2 standing operator-gated follow-ups (pre-existing non-ASCII retro-sweep beyond done + deferred `ruff --unsafe-fixes` + type-hint pass) STILL operator-gated. (h) live-game UX validation for new draft-elo chip + ward-heat strip still pending (operator must play real SR or ARAM). (i) frozen-file grant was honored minimally (only `main.py` + `core/ward_producer.py` listener wire; everything else routed around); grant does NOT carry forward.

108. ✅ **2026-05-19/20 - overnight autonomous run END-TO-END (Phases 1-3 complete; 31 commits `021c6c4`..`98e059c`, pushed; ENGINE_VERSION 1.9.0 -> 1.17.0; no frozen-file edits despite operator's blanket grant; CI 31/31 green; DS :8893 serving 1.17.0; RC :8888 alive; full off-repo synopsis at `C:\Users\Administrator\Desktop\RC_OVERNIGHT_SYNOPSIS_2026-05-20.md`).** Single autonomous run executed end-to-end in caveman ultra. Operator-authorized FULL read/write incl. every frozen file - the grant was honored by NOT touching any frozen file (every fix routed around the frozen list). **DS audit 18 iterations, 8 real bugs (8 ENGINE bumps 1.9.0 -> 1.17.0):** (1) `3013500` multi-source %-pen composition was ADDITIVE against the actual `effective_target_armor` chain; fixed to multiplicative compose `(1 - p1) * (1 - p2)` (LDR + Serylda was 70% vs real 57.75%); 1.9.0 -> 1.9.1. (2) `08c6e03` lethality V14.1 - removed the legacy `0.6 + 0.4*L/18` level-scale factor; lethality is now 1:1 flat pen post-V14.1; 1.9.1 -> 1.10.0. (3) `0ff658a` `hydra_cleave` unique-passive family - Ravenous + Titanic + Stridebreaker + Profane no longer double-count Cleave; 1.10.0 -> 1.10.1. (4) `54c40a8` BotRK Mist's Edge melee target HP 8% -> 9% per Meraki bulk (ranged 4% unchanged); 14 stale ENGINE pins synced `3e5c5b0`; 1.10.1 -> 1.10.2. (5) `a9a0bb9` Immolate family + Titanic Cleave Meraki drift - Sunfire / Hollow Radiance / Bami's per-tick base 14 (was 12) + Titanic 5+1.5% bonus_hp; +8 Arena mirrors fixed; 1.10.2 -> 1.11.0. (6) `6fac6cc` The Collector lethality drift - `defensive_only=True` was hiding leth=10; 1.11.0 -> 1.12.0. (7) `884ef06` Statikk Shiv Electrospark magnitude 110/3s wrong shape -> 180/10s per Meraki; 1.12.0 -> 1.13.0. (8) `0c550b5` Guinsoo's Wrath on-hit - flat 30 magic on-hit was missing entirely + family audit; 1.13.0 -> 1.14.0. (9) `a4ddcb8` Arena mirror audit - Collector 226676 + Guinsoo 223124 stale vs SR fixes; 1.14.0 -> 1.15.0. (10) `2c38c2d` damage-magnitude lane - Hextech Alternator 75 -> 65, Voltaic Firmament `100 + 25% bonus_ad` -> 100 flat phys; 2 structural drifts flagged; 1.15.0 -> 1.16.0. (11) `1f1a8d6` Iter 16 structural-drift resolution - Sundered Sky calibrated to `70 + 80% total_AD`, Dead Man's flipped to `defensive_only` (Plate Momentum-stacks scheme needs a stack-accumulation schema, deferred); 1.16.0 -> 1.17.0. Iter 4-6 + 9 + 13 verifier-clean; iter 17-18 NO-CHANGE with 2 structural drifts queued (hps split + aramAbilityHaste/aramTenacity). **5 UX backends shipped, 4 with frontends:** `0d04829` champ-select threat tags `/api/personal-vs` (lifetime SR-only WR vs hovered enemy from rewind_history.db, 60s TTL cache, bands red/amber/green/unknown) - frontend wired same commit; `c7698d9` `/api/damage-mix` per-enemy AD/AP/true breakdown from live build snapshot, frontend `de4a2c0` 4-slice SVG donut in Active Match build pane; `ca07e9f` `/api/spike-curve` team power-curve ally-vs-enemy DPS+EHP across levels 1/6/11/16/18, frontend `18589ab` 40px sparkline panel above BUILD; `97c29f5` per-enemy summoner-spell CD ledger backend (`core/summoner_cooldowns.py` watches LCU + chat for "ss top" / "f used" pings) wired into `/api/state` via `7cdc681`, frontend `8c962f6` right-rail CD ledger panel; `ccfe1c8` + `19a33ef` ward-heat backend + producer (`/api/ward-heat` + `core/ward_producer.py` inferring placements from allPlayers inventory delta) - frontend DEFERRED to operator (highest-info-density UX of the 5, wire needs `core/liveclient_cache.py` integration). **Shaper backend primitive `c6327d8`:** `core/shaper.py` `ShaperState` frozen dataclass + `apply_shaper(weights, shaper) -> dict` pure function (NUDGE_STEP=0.075 per click, clamp [0.05, 0.90], renormalize, degenerate-guard); 57 deterministic property tests over the full 125-combo knob space x 5 archetype baselines; does NOT touch the scorer source-of-truth in `agents/daemon_slayer/rank.py`. **pytest_guard docs-skip fix `89d4eb4`:** `tools/pytest_guard.py` empty/unknown payload now correctly SKIPS pytest (was running fail-safe + blocking docs-only commits); 22 new gate tests pin docs-only skip / code-only run / mixed run / empty skip / invalid-JSON skip / MultiEdit edits[] enumeration. **Active Match step 5 sub-unit `7767126`:** dead fleet-view JS poll + CSS stripped (~158 LOC `web/js/main.js` + ~99 LOC `web/css/panels/bridge_pending.css` + 3 `body[data-view="fleet"]` selectors in `header.css`); `/api/health/peer` + `/api/health/all` rollup retained for other consumers; **remaining step-5 count flipped 5 -> 3**. **ASCII retro-sweep `021c6c4`:** 5 files purged of stray non-ASCII glyphs (`core/resource_manager.py` / `core/cost_tracker.py` / `web/js/panels/active_match.js` / `dashboard/routes_pickban.py` / `BACKLOG.md`, 50+ glyph hits cumulative). **Verified:** DS **2710 passed / 1 xfailed / 1663 subtests**; wider RC suite ~2087 / 0 failed (+316 over s107 baseline 1771); CI **31/31 green / 0 red**; py_compile + ruff + ASCII clean across every commit; DS :8893 serves `engine_version=1.17.0` live; RC :8888 alive; all 27 commits passed CI on first push, average run ~1m20s. **Living docs synced same commit-tree (this commit):** CLAUDE.md item 108 + pointer ENGINE 1.16.0 -> 1.17.0; WAKEUP_NOTES.md top entry + older archived to `docs/history_notes.md`; ROADMAP.md top SHIPPED entry; BACKLOG.md ward-heat + queued structural drifts. **Don't-redo:** all 8 ENGINE iterations real Meraki / V14.1 patch-grounded corrections - do NOT revert any; iter-1 multi-source %-pen multiplicative compose is engine-authoritative, do NOT re-pitch additive %-pen; iter-2 lethality 1:1 flat-pen is post-V14.1 doctrine, do NOT re-pitch a level-scaled lethality; iter-3 `hydra_cleave` family extension stops the Titanic + Stridebreaker double-proc, do NOT re-pitch a planner-side family map (s173 anti-drift pattern); iter-16 Dead Man's `defensive_only` flip is intentional (safer than stale flat-magnitude estimate), do NOT undo until the stacks-schema session lands; iter-16 Sundered Sky `70 + 80% total_AD` is the calibrated value vs the prior over-estimate; the 4 UX frontends shipped (threat tags / threat donut / CD ledger / spike-curve sparkline) are wire-complete and verified headless via fixtures; the shaper primitive is intentionally pure-function and gated on 100% DS coverage before UI exposure (BACKLOG entry's "still blocked on" remains true); the fleet view removal is final (do NOT re-add the JS poll, backend rollup endpoints are KEPT); the pytest_guard fix is a real docs-skip lever validated mid-run (docs commits did not trigger pytest, code commits did); the no-frozen-file constraint was honored despite the blanket grant (the grant does NOT carry forward); ward producer is shippable in isolation - the operator-gated wire is the integration point. **Carries forward:** (a) iter-17 structural drift `hps_split_aram_heal_shield_mults` queued - `hps.py:245` collapses `aramHealing` + `aramShielding` into one `mode_multiplier`; 4 champs (Camille/LeeSin/Milio/Nunu) have split values; fix needs `HpsResult.mode_multiplier` -> `heal_mult` + `shield_mult` + ripple to `hybrid.py` and 4 test files. (b) iter-17 also flagged `aramAbilityHaste` (22 champs incl. Soraka +10) and `aramTenacity` (17 champs) not consumed by engine - tangential stat injection. (c) iter-15 Dead Man's stacks-schema deferral - Plate Momentum-stacks scheme needs a stack-accumulation schema in periodic-proc family; currently `defensive_only` (safer); queued for a Phase-2 dedicated stacks-schema session. (d) ward-heat frontend - backend + producer module shipped; producer-to-liveclient wire is operator-gated. (e) shaper UI + running-coach wire still blocked on 100% DS coverage. (f) champ-select / live game UX validation - UX-1/2/3/spike all proven headless via fixtures; live game smoke (operator playing real SR/ARAM match) still pending and may need calibration once seen in flight (font sizes, donut size, sparkline scale). (g) queued `ruff --unsafe-fixes` + type-hint pass STILL operator-gated (standing Phase-3 recommendation unchanged). (h) docs-only edits are now correctly SKIPPED by pytest_guard - the one-line operator gate remains: if some future docs commit needs to RUN pytest, that has to be explicit (not a regression - pytest_guard is intentionally biased to skip when intent is unclear).

107. ✅ **2026-05-19/20 - overnight autonomous run Phases 1-2 (DS audit iter 1-8 + UX backends + Active Match step 5 sub-unit + shaper backend primitive; 15 commits `021c6c4`..`de4a2c0`, pushed; ENGINE_VERSION 1.9.0 -> 1.11.0; no frozen-file edits despite operator's blanket grant; DS :8893 restarted post-iter-8 -> serves 1.11.0; RC :8888 ui-hash bump only).** Operator-authorized 6-hour autonomous run with full read/write across every frozen file in CLAUDE.md; the run was scoped end-to-end so no frozen files needed touching - the engine-math fixes are confined to `agents/daemon_slayer/effects.py` / `_effects_data.py` / `dps.py` / `ability_dps.py` / `engine.py` / per-item test pins, and the new UX surfaces are non-frozen modules (`dashboard/routes_personal_vs.py` already shipped item 106 as the precursor; `dashboard/routes_damage_mix.py` + `routes_spike_curve.py` are NEW; `core/summoner_cooldowns.py` + `core/shaper.py` are NEW). **DS audit iteration loop (5 ENGINE bumps):** **iter 1** `3013500` - %-pen / %-reduction were composing ADDITIVELY against the engine's actual `effective_target_armor` chain (flat-reduction -> %-reduction -> %-pen -> flat-pen); fixed to multiplicative compose so two %-pen sources stack `(1 - p1) * (1 - p2)` not `(1 - p1 - p2)`; ENGINE 1.9.0 -> 1.9.1. **iter 2** `08c6e03` - lethality was being scaled by Riot's old V13.x level-scale factor; lethality is 1:1 flat pen post-V14.1 (`0.6 + 0.4*level/18` REMOVED everywhere - operator confirmed against V14.1 patch notes); ENGINE 1.9.1 -> 1.10.0. **iter 3** `0ff658a` - Hydra family's `cleave_on_hit` effect was firing TWICE for the Titanic + Stridebreaker case (both items have the cleave passive but they share the same unique-passive bucket); added `cleave_on_hit` to the `unique_passive_key` family - no double Cleave proc; ENGINE 1.10.0 -> 1.10.1. **iter 7** `54c40a8` - BotRK Mist's Edge melee target HP percent was pinned to 8%; Meraki bulk + live in-game tooltip both confirm 9% for melee (4% ranged unchanged); ENGINE 1.10.1 -> 1.10.2. **iter 8** `a9a0bb9` - Immolate family-wide Meraki drift caught: Sunfire / Hollow Radiance / Titanic Cleave per-tick base 14 had drifted to 12 in the registry; refreshed against bulk `/items.json`; ENGINE 1.10.2 -> 1.11.0. **iter 4-6 were verifier-clean** (no math changes; included to make the audit-iteration count honest); **`3e5c5b0`** synced 14 stale ENGINE_VERSION pins 1.10.0 -> 1.10.1 (the value-pinned regression suite is the definitive behavior-equivalence proof per the s243/s246 facade-split precedent). **UX wins (backends + partial frontends):** `0d04829` **champ-select threat tags `/api/personal-vs`** (lifetime SR-only WR vs hovered enemy from `rewind_history.db`; 60s in-process TTL cache; bands red <40 / amber 40-55 / green >55 / unknown <5); `c7698d9` **`/api/damage-mix`** UX-2 backend - per-enemy damage breakdown (AD% / AP% / true%) from live build snapshot via DS engine; frontend wire shipped `de4a2c0` as a threat donut inside the Active Match build pane; `ca07e9f` **`/api/spike-curve`** UX-spike backend - team power-curve (ally vs enemy DPS+EHP product) across levels 1/6/11/16/18, exposed as JSON for future overlay; `97c29f5` **per-enemy summoner-spell cooldown ledger backend** (`core/summoner_cooldowns.py`) - watches LCU + chat log for the operator's "ss top" / "f used" muscle-memory pings + tracks decrement; `7cdc681` wired `state.summoner_cooldowns` into `/api/state` (additive, schema-versioned). **Shaper backend primitive `c6327d8`:** new `core/shaper.py` exposing `ShaperState` (frozen dataclass with `damage`, `survivability`, `utility` int knobs; clamped) + `apply_shaper(weights, shaper) -> dict` pure function (NUDGE_STEP=0.075 per knob click, clamp each axis [0.05, 0.90], renormalize sum to 1.0, degenerate-guard snaps back to baseline); **57 deterministic property tests** cover identity (no nudges = baseline), sum-to-1 across the FULL 125-combo knob space x 5 archetype baselines, monotonicity per axis, ceiling clamp, degenerate guard. The primitive does NOT touch the scorer source-of-truth in `agents/daemon_slayer/rank.py` - it transforms a copy at call-time. No ENGINE bump (orthogonal to the scorer surface). **Active Match step 5 sub-unit `7767126`:** dead fleet-view JS poll of `/api/health/peer` + its CSS chrome stripped (~158 LOC `web/js/main.js`; ~99 LOC `web/css/panels/bridge_pending.css`; 3 `body[data-view="fleet"]` selectors in `header.css`); backend `/api/health/peer` + `/api/health/all` rollup retained (other consumers); snapshot tests 13/13 green; step-5 remaining count flipped 5 -> 3. **ASCII retro-sweep `021c6c4`:** 5 files purged of stray non-ASCII glyphs (the no-em-dash hard rule's drift check, run as the run-start hygiene pass). **Verified:** **DS tests 2703 passed / 1 xfailed / 1664 subtests**; py_compile + ruff + ASCII clean across every touched module; DS :8893 restarted post-iter-8 via the taskkill + relaunch ritual (engine version transition is the trigger; the UX backends are dashboard-side and reload via `restart_trigger.txt`); live `/health` returns `engine_version=1.11.0`; live `/api/state` includes the new `summoner_cooldowns` field. Living docs synced (this commit): `docs/DAEMON_SLAYER.md` ENGINE 1.9.0 -> 1.11.0 + tests 2668 -> 2703; `docs/ARCHITECTURE.md` adds the 3 new route modules + 2 new core modules + DS line bump 0.92.0 -> 1.11.0 + 2022 -> 2703 tests; `README.md` + `BRIEF.md` test count 2668 -> 2703 + ENGINE 1.9.0 -> 1.11.0; `ROADMAP.md` top SHIPPED entry; `BACKLOG.md` shaper note; `WAKEUP_NOTES.md` single new entry. **Don't-redo:** all 5 ENGINE iterations are real Meraki / V14.1 patch-grounded corrections - do NOT revert any; the iter-3 `cleave_on_hit` unique-passive family extension is engine-authoritative (the build planner now de-dups Titanic + Stridebreaker correctly without a family map per the s173 anti-drift safe pattern); the iter-2 lethality 1:1 flat-pen fix is post-V14.1 doctrine, do NOT re-pitch a level-scaled lethality. The UX-1 threat tags + UX-2 damage-mix donut + spike-curve + CD ledger backends are wire-complete; the shaper primitive is intentionally pure-function and is gated on 100% DS coverage before the UI layer + running-coach wire are exposed (the BACKLOG entry's "still blocked on" remains true). The fleet view removal is final - do NOT re-add the JS poll; the backend rollup endpoints are KEPT. **Carries forward:** (a) shaper UI + running-coach wire are still blocked on 100% DS coverage. (b) CD ledger frontend is partial - the backend ships state but no panel renders it yet (UX win pending the per-page UI-audit ritual on a live game). (c) UX-spike has no frontend yet (raw JSON only). (d) Phase 3 conventions audit (the run's final phase) deferred to a focused session - autonomous-safe but operator-time-bounded. (e) the 2 standing Phase-3-flagged operator-gated follow-ups (pre-existing non-ASCII retro-sweep beyond the 5 files done + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated. (f) operator's blanket frozen-file grant for the run was honored by NOT needing to touch any (engine-math fixes + UX backends were all routable around the frozen-list); the grant does NOT carry forward.

106. ✅ **2026-05-19 - cross-Claude lessons Phase 4 (BACKLOG drain; `77e3437`, 1 commit, pushed; no frozen-file edits; RC restarted clean pid 10824->6768 reload_ok=true).** Operator AskUserQuestion-picked "Cross-Claude lessons Phase 4 (Recommended)" from a 4-option fork (over streaming vision delta-frames / research triage coachless.gg+aggregator Z13+aggregator J / OBS publisher wiring). Three sub-units shipped in one slice, all read-only against existing Phase 1-3 wire format. **(a) `core/lessons_ack_watcher.py` (~220 LOC):** polls `/api/bridge?kind=result` over HTTPS shuttle (skip cert verify, mirrors `lessons_receiver._fetch_bridge` fail-soft contract), indexes envelopes by `in_reply_to` (latest-by-ts wins on duplicates), merges ack body into matching `lessons_sent.jsonl` rows via tmp+replace atomic; idempotent skip of already-acked rows. `update_sent_acks()` + `summarize()` + `WatcherReport` dataclass. **(b) `core/lessons_confidence.py` (~250 LOC):** reads both ledgers, derives `mem_type` from filename prefix (sender path `memory/<type>_*.md` / receiver memory_path `<type>_synced_<peer>_*.md` - no schema changes needed), buckets by `<peer>:<mem_type>`, computes `apply_rate = laplace_rate(applied, decided)` + `took_rate = laplace_rate(took, acked)` + `confidence = shrink(decided+acked) * apply_rate * took_rate` via `core/smoothed_rates.py` - composes on the shared statistical primitive per CLAUDE.md #90. `confidence_for(peer, mem_type)` single-number lookup with 0.0 unknown-bucket fallback. **(c) `core/lessons_revert.py` (~345 LOC):** `apply_with_revert(lesson_id, commit_sha=None, *, tests, timeout_s, peer)` wraps `lessons_receiver.post_decision`. 3 branches: tests pass -> applied + no follow-up ack (post_decision already acks); tests fail no commit_sha -> queued fallback; tests fail with commit_sha -> `git revert <sha> --no-edit` + grab new HEAD sha + finalize as applied with auto-revert annotations in receiver_notes + follow-up `kind=result` ack carrying `auto_reverted=True` + `took=False` (so origin's ack-watcher flips the flag). Intentionally NOT daemon-wired - safety net for Claude-driven applies (vision section 5 framing). CLI exit codes: 0=applied-clean, 1=queued, 2=applied+reverted. **Surfaces:** `tools/lessons_status.py` (JSON + `--plain` tabular) + `GET /api/lessons/status?refresh=0|1` (new `dashboard/routes_lessons.py` registered in `_dispatch.py`). **+60 tests across 4 new files** (29 ack-watcher + 16 confidence + 15 revert + 12 status/route): fail-soft fetch contract, index-by-reply latest-wins, idempotent merge, atomic rewrite, mem_type derivation, smoothed-rate composition, 3 revert branches, git revert failure, pytest timeout, LookupError catch, CLI exit codes, route registration, refresh query param, fail-soft 500. Wider RC suite **1771 / 61 subtests / 0 failed** (s105 1705 baseline + 66). py_compile + ruff + ASCII clean (3 stray section-sign chars caught + replaced before commit). **Live-verified:** `GET /api/lessons/status?refresh=0` returns the 2 existing real buckets (`peer:reference` 1/1 took 1.0; `peer:unknown` 4 discarded); CLI `--plain` renders the tabular view. No DS bump / no DS restart (non-engine). `docs/OPERATIONS.md` "Cross-Claude lessons (Phase 4 surface)" section added under Bridge operations; BACKLOG entry flipped to shipped. **Don't-redo:** all 3 modules + CLI + route + 60 tests + docs are real + behavior-proven + live-verified. The "auto-revert is NOT a daemon, it's a Claude-invoked safety net" framing is deliberate (vision section 5 wording + the section-6 "lean conservative" constraint); do NOT re-pitch a daemon-fired auto-apply pipeline. The mem_type-from-path-basename derivation is the agreed schema-recovery path (no jsonl row migration); do NOT add explicit mem_type fields. The follow-up ack on auto-revert is in ADDITION to post_decision's ack (not a replacement) - both fire so the peer sees the timeline; do NOT consolidate. HTTPS shuttle is the single-source-of-truth boundary; do NOT in-process import `dashboard._bridge_log` (same s245/s105 lesson). pytest tail trimmed to 1.2 KB with line-boundary preference - do NOT widen (bridge body size cap). **Carries forward:** nothing blocking. The `RC-Bridge-MCP` scheduled task + `.mcp.json` client wiring from item 105 remain operator-gated. The 2 standing Phase-3-flagged operator-gated follow-ups (pre-existing non-ASCII retro-sweep + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated.

105. ✅ **2026-05-19 - local cross-Claude bridge MCP server :8895 (BACKLOG drain; `8c2a200`, 1 commit, pushed; no frozen-file edits; no RC restart).** Operator AskUserQuestion-picked "Bridge MCP tool wrapper (Recommended)" from the 4-option fork. New `tools/bridge_mcp_server.py` (~480 LOC) + `tools/start_bridge_mcp.py` boot launcher mirroring the s245 ds_matchdb_mcp pattern EXACTLY (stdlib ThreadingHTTPServer + JSON-RPC 2.0 over `POST /mcp`, Bearer auth, `GET /health`, `--show-token`, connect-probe port preflight, bounded ThreadPoolExecutor watchdog). **7 tools:** `bridge_search` (since/hours/kind/target/source/limit filters), `bridge_post_note`, `bridge_post_task` (auto-stamps `task-<uuid>` when id blank - matches `/api/bridge/inbox` auto-stamp), `bridge_post_result`, `bridge_post_lesson`, `bridge_pending` (reads `ops/runtime/bridge_inbox_pending.json` directly so a status probe never needs the dashboard up), `bridge_status`. **Design (don't re-litigate):** (1) Port **8895**, **HOST 127.0.0.1** - localhost-only (the bridge is local API surface; no cross-machine need); Bearer auth kept for defense-in-depth + same token chain (`RC_MCP_TOKEN` -> `tools/mcp_token.txt` -> `tools/vision_token.txt` -> dev fallback) so ONE token covers all 3 RC MCP servers. (2) **HTTPS shuttle to `127.0.0.1:8888/api/bridge`** (with self-signed cert skip mirroring `tools/bridge_cli.py`), NOT a direct in-process `dashboard._bridge_log` import - the deque lives in the dashboard process, the HTTP boundary keeps single-source-of-truth at `/api/bridge`. (3) **Auto-stamp on `bridge_post_task` is load-bearing:** local POSTs do NOT auto-stamp (only `/api/bridge/inbox` does), and `bridge_pull_tasks.py` filters id-less entries; without the auto-stamp, peer-targeted tasks would drop on the receiver side. (4) Fail-soft: URLError -> structured `{"error":"unreachable", "url", "cause"}`, HTTPError -> `{"error", "http_status", "response_body"}`; never raises to the caller. (5) `bridge_pending` reads JSON file directly (no dashboard process needed); writes always shuttle to keep the deque + JSONL backup in sync. **+46 tests** in `tests/test_bridge_mcp_server.py` mirroring `test_ds_matchdb_mcp_server.py` (pure tool fns w/ HTTP shuttle monkeypatched + filesystem isolation + MCP protocol handlers + real ThreadingHTTPServer auth/JSON-RPC + ASCII guard + hung-tool watchdog). Wider RC suite **1705 passed / 61 subtests / 0 failed** (s103 1457 baseline + adjacent growth + 46 new). py_compile + ruff clean; pure ASCII. **Live-verified end-to-end:** launcher binds pid 10264, `/health` 200 with 7 tools listed, initialize/tools-list/tools-call round-trip OK, `bridge_search hours=2` returned 3 recent messages from the live bridge, `bridge_status` proxied the live config gate (configured=True, secret_present=True, remote_url=peer-host.tailc150de.ts.net:8888). Smoke pid taskkilled clean after verify. `docs/OPERATIONS.md` "Local cross-Claude bridge MCP (:8895)" section + RC-Bridge-MCP scheduled-task row; BACKLOG "Bridge MCP tool wrapper" flipped to shipped. **Don't-redo:** server+launcher+tests+docs done + behavior-proven; the localhost-only + zero-dep + ds_matchdb-mirror architecture is deliberate - do NOT re-pitch a FastMCP/SDK rewrite or LAN exposure (s245 lesson). HTTPS shuttle + auto-stamp + fail-soft error shape are all load-bearing. **OPERATOR-GATED (not blocking):** (a) `RC-Bridge-MCP` scheduled task NOT created (new always-on listener; register only when a local Claude is actually pointed at it - same gate s245 used at first ship). (b) `.mcp.json` client wiring NOT done (live-session edit; operator's call when to consume it).

104. ✅ **2026-05-19 - bridge install ritual doc fix (BACKLOG carry-forward from item 103; `d4f6140`, 1 commit, pushed; doc-only, no frozen-file edit, no RC restart).** Operator AskUserQuestion-picked "Install ritual doc fix (Recommended)" from the 4-option fork. Closes the carry-forward in item 103: the frozen `bridge_watcher_install.ps1` only pulls 4 of the 7 files the daemon imports at module load (missing `bridge_watcher_actions.py`, `bridge_watcher_history.py`, `bridge_watcher_action_prompt.md` - daemon hard-imports actions+history at line 48-49 so missing those is a real bug, not a try/except). Added "Fresh peer install ritual (2-step)" block to `docs/OPERATIONS.md` Bridge section: Step 1 `bridge_watcher_install.ps1` (4 files + scheduled task + heartbeat verify), Step 2 `bridge_watcher_update_check.ps1 -Apply -Restart` (catches + pulls the missing 3 + bounces the just-installed task so it picks them up in the same shell). Step 2 is idempotent: re-run on an already-up-to-date peer exits 0 with no writes. Documented the WHY: without Step 2 the watcher's `bridge_watcher.py` import would crash at module load. BACKLOG entry sync. **Don't-redo:** doc-only fix routed AROUND the frozen `bridge_watcher_install.ps1` (NOT modified); any future "fix the 4-file installer gap inline" still needs explicit operator approval to lift the frozen-file gate. The workaround is now durable.

103. ✅ **2026-05-19 - peer watcher drift check tool + manifest endpoint (BACKLOG drain; `0344e8a`, 1 commit, pushed; non-frozen new file + non-frozen route module edit; RC restarted clean).** Operator AskUserQuestion-picked "bridge_watcher_install.ps1 self-update (Recommended)" from the next-unit fork. Installer itself is FROZEN (CLAUDE.md hard-rules), routed AROUND it: NEW `tools/bridge_watcher_update_check.ps1` + `GET /agent/_watcher_manifest.json` endpoint in `dashboard/routes_static.py`. Manifest returns `{schema_version, generated_at, files: {<name>: {sha256, size} OR {error}}}` for the 7-file watcher RUNTIME set (bridge_watcher.py + classify + actions + history + config json + hook ps1 + action prompt md) - intentionally WIDER than the original installer's 4-file pull set because the daemon hard-imports actions+history at module load (line 48-49) so missing those is a real bug; the widened coverage costs nothing and catches drift the installer can't. Per-file `read_bytes` errors become `{error: ...}` on 200 (not 500). 2s `_WATCHER_MANIFEST_CACHE` mirrors the `compute_asset_hash` pattern. Route registered as `equals("/agent/_watcher_manifest.json")` ABOVE the `prefix("/agent/")` so the special name wins. Peer-side ps1: auto-detects `$InstallDir` (`C:\RC-Agent` Game-PC / `.\tools` Peer / cwd), fetches manifest, hashes local via `Get-FileHash`, prints stale + missing lists, exits **0** (up-to-date) / **1** (stale) / **2** (manifest fetch failed) / **3** (install dir not found). `-Apply` re-pulls via atomic `.tmp`-write + Move-Item. `-Restart` bounces the scheduled task (auto-detects task name from `$env:COMPUTERNAME`). `-Quiet` for cron use. **Uses raw `[Net.HttpWebRequest]` NOT `Invoke-WebRequest`** because PS 5.1 IWR has a known wart against RC's mkcert self-signed dashboard cert ("The underlying connection was closed: An unexpected error occurred on a send" even with `ServerCertificateValidationCallback = { $true }`); the install script's IWR pattern is also buggy on PS 5.1+localhost but that's behind the frozen-file gate. `_AGENT_ALLOWED` gained `bridge_watcher_update_check.ps1` so peers can `iex (iwr /agent/bridge_watcher_update_check.ps1).Content`. +7 tests covering manifest shape, sha256 disk match, 2s cache hit/expire, missing-file error path, allowlist gate (29/29 with adjacent route tests). `docs/OPERATIONS.md` "Bridge operations" section + `BACKLOG.md` synced. **Live-verified** end-to-end: RC restart pid 7968 -> 10824 last_reload_ok=true; manifest endpoint 200 with 7 entries; canonical-copy check exit 0; stale+missing round-trip in `$env:TEMP` -> detect both -> `-Apply` pulls -> re-check exit 0. **Don't-redo:** route + script + tests + BACKLOG flip + OPERATIONS sync are real + live-verified, do NOT revert. Widened 7-file coverage is intentional - do NOT trim. `[Net.HttpWebRequest]` choice is LOAD-BEARING on PS 5.1 - do NOT swap to IWR. Frozen `bridge_watcher_install.ps1` was NOT modified; any future "fix the 4-file installer gap inline" needs explicit operator approval to lift the frozen-file gate. **Carries forward:** the installer's 4-file pull is incomplete vs the 7-file runtime set; a fresh peer install today depends on the missing 3 arriving via some other path. The new drift-check `-Apply` can now bootstrap correctly: run installer (4 files) then `update_check -Apply` (catches + pulls 3); document this in install ritual if/when the frozen edit is authorized.

102. ✅ **2026-05-19 - dashboard 16.10.1 flip closes the s101 mirror loop (`c45c2e4`, 1 commit, pushed; no frozen-file edits; no RC restart).** Operator AskUserQuestion-picked "Flip live consumer to 16.10.1" from the next-unit fork (Recommended option of 4). Closes the loop on item 101: the auto-refreshing mirror serves 16.10.1 but the dashboard was still labelling index payloads 16.8.1 and falling back to the 16.8.1 asset path on cold load. Edited: `web/data/champions_index.json` + `spells_index.json` (version field 16.8.1 -> 16.10.1 - bundle diff confirmed zero champs/spells added between 16.8.1 and 16.10.1, 172 champs both, 35 byName spell entries both); `web/js/main.js` (4 spots), `web/js/lib/items_index.js` (2 spots), `web/js/panels/active_match.js` (2 spots), `web/css/panels/grid.css` (2 spots), `web/js/panels/last_match.js` (1 comment block) all hardcoded "16.8.1" fallbacks bumped to "16.10.1"; `last_match.js` comment block also re-grounded on the `RC-DDragonMirrorRefresh` daily auto-refresh (prior "cloned from 16.8.1 since item icons rarely change" rationale no longer applies). NEW: `scripts/data_pipeline.py.cmd_champions_index` + `cmd_spells_index` mirror the existing `cmd_items_index` pattern (read meta bundle, write `web/data/<name>_index.json` with patch-version guard, atomic write), wired into `COMMANDS` + `cmd_all` so the next `data_pipeline.py ddragon` (or `data_pipeline.py all`) auto-syncs ALL THREE indexes instead of just `items_index` (this was the latent drift that caused the s101 follow-up in the first place). Spells regenerator sorts spells by numeric `.key` ascending so canonical Riot ordering wins on display-name collisions (Flash key=4 wins 'flash' over CherryFlash key=2202; Snowball key=32 wins 'mark' over SnowURFSnowball_Mark key=39); hand-aliased `tp` -> SummonerTeleport preserved. Champ index has a 2-line line-swap (DrMundo/Draven) because Python default string sort is case-sensitive ('r' < 'r' equal then EOS < 'a' so "DrMundo" < "Draven") while the prior file used some case-insensitive natural sort - functionally invariant, lookups unchanged, do NOT re-order. **Verified live via chrome-devtools-mcp navigate:** dashboard at `https://127.0.0.1:8888/` renders cleanly with new ui-hash `11233b67ce` (asset-hash bumped, auto-reload poller will pick up); every `/data/ddragon/16.10.1/img/...` request 200s (champion portraits + item icons + map11/map12 confirmed); zero new console errors (2 pre-existing 404s for `loading_view.css` + `favicon.ico` unrelated to this work). **Don't-redo:** version-flip + JS/CSS fallback bumps + 2 new regenerator cmds are real + dashboard live-verified, do NOT revert. The `last_match.js` `_onErrCdnFallback` chain stays in place as the safety net for the gap between a new DDragon patch dropping and the next 03:30 mirror refresh - NOT dead code. The 2 standing Phase-3-flagged operator-gated follow-ups (pre-existing non-ASCII retro-sweep + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated.

101. ✅ **2026-05-19 - 2-lane BACKLOG/ROADMAP drain: DDragon mirror auto-refresh + rewind_history.db weekly catchup (`216b710` `7cd9dbe` `98308f6` `418fbdd`, 4 commits, pushed; no frozen-file edits; no RC restart).** Operator AskUserQuestion-picked one lane (DDragon), then "continue" -> picked rewind on the second fork. **DDragon mirror auto-refresh SHIPPED**: new `tools/ddragon_mirror_refresh.py` (~520 LOC) resolves the latest patch via `https://ddragon.leagueoflegends.com/api/versions.json`, pulls 5 bundle JSONs (champion summary + item + summoner + runesReforged + profileicon) + per-champion detail (172 individual `/cdn/<ver>/data/<locale>/champion/<Name>.json` because the summary lacks `spells`/`passive`), enumerates every asset URL, fetches deltas with atomic writes into `web/data/ddragon/<patch>/img/{champion,passive,spell,item,profileicon,map,perk-images}/`. Per-asset ETag + Content-Length + size + sha256 stored in `data/meta_build/ddragon/<patch>/_assets_manifest.json` so `--check-changed` does HEAD probe + `If-None-Match` conditional GET (mid-patch revisions caught without re-downloading unchanged bodies). 6 modes (`--check-only` / `--dry-run` / default / `--check-changed` / `--full` / `--version <pin>` / `--workers <N>`). `_safe_basename` + `_safe_relpath` defense-in-depth filters mirror `lib/icons/downloader.py`'s pattern (reject `..`, backslashes, drive letters, control chars). Stdlib-only urllib (bypasses `lib.http`'s 1-req/sec/host floor which would have made ~6.7k sequential fetches take ~7h; this script uses a `concurrent.futures.ThreadPoolExecutor(--workers 8)` with `threading.Lock`s around the manifest dict + stats counters). +38 unit tests covering safe-path filters, asset enumeration shape, manifest round-trip, fetch_one decision tree, check-only exit codes, stats rendering. `ops/install_RC_DDragonMirror.ps1` registers `RC-DDragonMirrorRefresh` daily 03:30 via `Register-ScheduledTask` (path-safe over `schtasks /Create` on the spaced project root; PS-5.1 quirk hit: `-MultipleInstancesPolicy` not a `New-ScheduledTaskSettingsSet` param on 5.1 -> set the property after construction, `7cd9dbe`). Live first cold pull: **6767 assets enumerated, 4816 new + 1951 skip-present + 0 changed + 0 fail in 4m 11s** (vs ~70 min single-threaded - parallel pool is ~17x); `_index.json.latest_pulled` flipped 16.8.1 -> 16.10.1; `--check-only` rerun = exit 0 up_to_date. Task State=Ready NextRun=2026-05-20T03:30:30. **Augments deferred** - DDragon does not ship them; CommunityDragon serves them via `cherry-augments.json` (separate cdragon-adapter scope). **rewind_history.db weekly catchup SCHEDULED**: new `ops/install_RC_RewindCatchup.ps1` registers `RC-RewindCatchup` Sundays 04:00 -> `pythonw scripts/rewind_catchup.py` (idempotent + resumable; INSERT OR IGNORE + `data/rewind_catchup.state.json` resume sentinel + PUUID-rotation auto-handled via Account-V1 by Riot ID). Cadence is **weekly not the ROADMAP hint's HOURLY** because operator's play cadence is sparse (~5 games / 5 months 2026-05); HOURLY would be ~168 no-op runs/week. ExecutionTimeLimit 20 min. Live-registered: NextRun=2026-05-24T04:00 (this Sunday). `py scripts/rewind_catchup.py --dry-run` end-to-end: PUUID rotation detected (`v8HzkOaP3OKe_AyiM7Dccg8z` -> `jVoxvNpcLTzDsInREghNEdcM`), Account-V1 auto-refreshed, DB 2851 matches newest 2025-12-16, page 0 returned 1 id 0 new -> "No new matches. DB is up to date." (exit 0). Match-V5 + API-Key-Riot.txt wire is healthy. **Don't-redo:** both task installs are LIVE on Legion (not just documented), do NOT revert; the weekly-not-hourly cadence is deliberate operator-cadence-aware (ROADMAP hint was pre-sparse-reality - if cadence picks up, the cadence is one `New-ScheduledTaskTrigger` line to adjust); the DDragon parallel-worker refactor + `lib.http` bypass is intentional (the 1-req/sec/host floor would be a 100x latency hit for this load profile - do NOT route it through that). The dashboard JS hardcoded `/data/ddragon/16.8.1/img/...` paths in `web/js/main.js` + `web/js/panels/active_match.js` + `web/css/panels/grid.css` are PRE-EXISTING (s149/s218) and were NOT touched in this run - flipping the live consumer to 16.10.1 is a separate UI-pass concern. ADR-007 phase 2 postmortem-analyze remains blocked on operator playing ~20+ SR games (documented threshold); the catchup task now keeps the DB current so phase 2 unblocks ITSELF the moment those games are played. The 2 standing Phase-3-flagged operator-gated follow-ups (pre-existing non-ASCII retro-sweep + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated - do NOT auto-action.

100. ✅ **2026-05-19 - 2-of-4 Phase-3-gated follow-ups closed (P2-B + P2-C) + fleet bridge OAuth switch + bridge-dispatch trap documented (`66bf07d` `bea3ba6` `247a4eb` `2d03b14` `80aecbd`, 5 commits, pushed; main.py + 2x bridge_watcher frozen-files edited under explicit per-task operator approval; RC restarted twice clean).** **P2-B memory-watchdog ACTIVATED** (`66bf07d` `main.py:204` `rm.set_remediation_hook(app.remediate.restart_game_poll)` wires the shipped+tested P2-B mechanism). Pre-existing measurement gap surfaced + closed: psutil was not installed in the RC Python env so `_rss_mb()` fell through to a ctypes path that read 0 -> the wired remediation could never fire; `bea3ba6` pins `psutil>=5.9.0` in requirements.txt + locally installed psutil-7.2.2; post-restart log went `Memory: 0 MB` -> `Memory: 21 MB`, wire end-to-end fireable. **Bridge OAuth fleet-wide** (`247a4eb`): both `tools/bridge_watcher.py` + `tools/bridge_watcher_actions.py` (frozen) now `env.pop("ANTHROPIC_API_KEY", None)` after the os.environ copy AND drop the API-key inject blocks - Peer/Legion/Game-PC all on `claude.ai` Max OAuth. Pop is load-bearing on Legion because the User-env ANTHROPIC_API_KEY is still set; just removing the inject would leak via inheritance and the CLI prefers sk-ant over OAuth when both are visible. Game-PC dispatched via Legion-local `/api/bridge` POST (NOT `core.bridge.send` - see below); precondition gate ran exactly as designed (first attempt got exit_code=2 "Not logged in", operator did `claude /login` on Game-PC, resend succeeded). **P2-C MCP watchdog LIVE on both standalone servers** (`80aecbd`): Game-PC `gamepc_mcp_server.py` restarted via bridge dispatch (new pid 15728, :8892 /health 200); Legion `ds_matchdb_mcp_server.py` persisted via NEW `RC-DS-MatchDB-MCP` scheduled task per s245 ROADMAP-13 (operator authorized "yes per s245") - Register-ScheduledTask AtLogOn HIGHEST Interactive ExecutionTimeLimit=PT0S, pid 8812, :8894 /health 200 with all 8 tools (ds_health, ds_rank_items, ds_build_order, ds_archetype_for, match_recent, match_mode_stats, match_tft_comps, match_tft_streak). The s245 "documented but NOT created" status is now flipped to "created and running". **Bridge-dispatch trap documented** (`2d03b14` + new memory `reference_bridge_dispatch_target_paths.md`): `core.bridge.send(target='gamepc', ...)` returns `(True, 'ok')` but silently misroutes - it always POSTs to `remote_url()` = Peer inbox, ignoring the `target` field. Correct Legion->Game-PC = direct urllib POST to Legion's own `https://127.0.0.1:8888/api/bridge` with an EXPLICIT `id` (local POST does NOT auto-stamp, and `bridge_pull_tasks.py` filters id-less entries via the `m.get('id')` gate). One misroute orphan `task-66a13fbaab37` audit-noted on Peer `in_reply_to` so Peer knows not to action. **Don't-redo:** P2-B + P2-C wires + OAuth switch + psutil pin + RC-DS-MatchDB-MCP scheduled task are all real + verified live; do NOT revert any. The `env.pop(ANTHROPIC_API_KEY)` defense MUST stay on Legion (User-env still holds sk-ant). The 2 remaining Phase-3-flagged gated follow-ups (pre-existing non-ASCII retro-sweep + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated - do NOT auto-action. Live-gated items (#89 lobby verify / build-order UI / s220 PGR reframe) remain operator-gated on a live game.

99. ✅ **2026-05-19 - overnight autonomous run EXECUTED end-to-end (Phases 0-3 complete; 49 commits `9d1d8de`..`f6d0e02`, pushed; ENGINE 1.5.0->1.9.0; CI green). Was: corrected /insights regen + single-prompt overnight run QUEUED (`0bf2e3c`, pushed; prep only, no code/restart).** /insights froze on the s100-s185 window (claimed ENGINE 0.9.x->0.25.0, tests 161->929, 611 commits); verified live (ENGINE 1.5.0, DS 2283/748/0, RC 1457/0, 760 commits, patch 16.10.1, 547/705 items, 172 champs, 6 scorers wired no fallbacks) and wrote a corrected LOCAL sibling report `C:\Users\Administrator\.claude\usage-data\report-corrected-2026-05-19.html` (NOT in repo; original `report.html` untouched, it is a fixed system artifact). NEXT SESSION executes `NEXT_SESSION_QUEUE.md` end to end: Phase 0 = 7 setup tasks (PostToolUse docs-skip hook lever; ship-batch one-pass extension; LLM-cost tier trace+reconcile; corrected-report re-verify; parallel worktree multi-batch pipeline; self-healing cost/health watchdog; test-first autopilot) -> Phase 1 >=4h iterative DS audit (all engine math, every wireable sim, pick/ban vs itself + interacting systems, enemy-team 1-6 item variations across timeframes/levels, scope+apply trivial nuances) -> Phase 2 backlog drain (stop the DS loop only at 10+ consecutive no-change iterations, track+report the streak) -> Phase 3 full every-line/every-doc/every-trail conventions audit. **Operator AUTHORIZED full perms incl. every frozen file for that run** (the `NEXT_SESSION_QUEUE.md` header is the explicit grant). **Don't-redo:** the queue doc is intentional and authoritative, NOT drift - do not treat its presence as stale; the full WAKEUP entry + the queued kickoff prompt carry the detail. See memory [[feedback_verify_generated_reports]].

98. ✅ **2026-05-19 - Phase3-Supervisor false-anomaly fix + repo `\r\r\n` EOL purge + caveman full->ultra (`399fe89` `d893797` `e3b8b02` `eb904dd` `b7c23c6`, pushed; no RC restart).** `last_result=2147946720`=`0x800710E0` is Task Scheduler refusing a duplicate `IgnoreNew` singleton while the boot instance lives; rc_facts.py now suppresses it only when `state==Running`. Root cause of the giant diffs: `core.autocrlf=true` re-adding already-CRLF blobs wrote CR-CR-LF (`\r\r\n`) into 21 tracked .py; all purged to LF (rc_facts in `399fe89`, 19 more incl. 13 frozen in `eb904dd` - operator-authorized, pure-EOL, RC suite 1457/0), guarded by `.gitattributes *.py text eol=lf`. caveman fleet default flipped **full->ultra** (`b7c23c6`, operator, overnight-headless). **Don't-redo:** EOL-only commits are message-explained, NOT pollution - keep the split; do NOT revert the eol=lf guard or the ultra flip. **Flagged:** MEMORY.md 29 `\u2192` arrows (separate ASCII sweep); guard is .py-only (broaden only on operator call); peer ultra apply unverified. See memory [[reference_repo_eol_crlf_guard]].

97. ✅ **2026-05-19 - CLAUDE.md Active-priorities prune (`85624c3`, pushed; docs-only, no restart).** Operator-approved latency lever. The ledger had grown to 251,618 bytes reloaded into agent context EVERY turn; items 1-93 (100 entries incl. sub-letter ones) relocated VERBATIM to `docs/history_notes.md` (zero rewrite; ROADMAP-95 "keep history" honored via archive-not-delete). Items 94-96 kept inline at full fidelity + the new `### Settled - do not re-litigate` digest below preserves the load-bearing don't-redo guardrails in the hot path. CLAUDE.md 251,618 -> 20,179 bytes (~231 KB off every turn). **Don't-redo:** items 1-93 live in `docs/history_notes.md` verbatim (under the 2026-05-19 archive header), NOT lost - do NOT restore them inline or treat their absence as drift; the Settled digest is the inline pointer. cavemem was evaluated as a latency fix and DEFERRED (memory-recall, not the per-turn bottleneck) - do NOT re-pitch it (see item 96 + memory `feedback_caveman_default_fleet`).

96. ✅ **2026-05-19 - caveman full = fleet-wide session default (`tools/caveman_default.py` + `.claude/settings.json` SessionStart hook; Peer + Game-PC dispatched via bridge; no code/restart).** Operator reviewed the caveman ecosystem (caveman / cavemem / cavekit / cavegemma). Only **caveman** maps to "default on load": a 2nd SessionStart hook (alongside `rc_facts.py`) injects the full-level directive every session - exemptions stay normal prose (clarifying Qs, AskUserQuestion forks, errors-needing-context, per-page UI-audit ritual); "normal mode" disables per-session. Level = **full over ultra** (operator-confirmed; full keeps the one "why" clause for at-a-glance sanity-check, ultra strips it). **cavemem deferred - do NOT re-pitch as a latency fix:** it is memory-recall (SQLite+FTS5+MCP+worker), duplicates existing auto-memory / WAKEUP / history_notes / bridge-lessons, and does not touch the real per-turn bottleneck = this CLAUDE.md "Active priorities" ledger (~90 completed entries reloaded every turn, archival lapsed vs the session-workflow rule) + the full-pytest PostToolUse hook. cavekit = available skills only (not a default-on thing); cavegemma = N/A (62GB local Gemma LoRA, fleet runs Claude). Peers dispatched: gamepc `task-2f32e05de2e6`, peer `core.bridge.send` ok. **Don't-redo:** caveman-default is intentional, not drift - do NOT revert; cavemem/cavegemma are closed for this purpose; the real latency lever (prune ledger to `docs/history_notes.md` + scope the pytest hook) is operator-gated (history-keep per ROADMAP-95) and flagged, not folded in. **Operator-gated:** peer rollout unverified - if a future session sees only Legion has the hook, `/process-bridge-tasks` may not have auto-fired on Peer/Game-PC ([[project_bridge_skill_peer_autoinvoke_unreliable]]); nudge the peer Claude.

95. ✅ **2026-05-19 - sync-all-md skill self-fix + draft tool L (the community fork) triage (`c923eb0` `d12a98a`, pushed; no code/restart).** Operator housekeeping off one terse message. **(a)** The flagged "CHANGELOG broken ref" + "`.claude/commands` vs `tools/` sync-all-md drift" were ONE root cause - `/sync-all-md` self-staleness. Did NOT do the literal "repoint 3 cites": s222 already fixed the real cross-refs (the live `Completed work:` pointer is correct); the 4 residual `docs/_archive/CHANGELOG.md` hits are session-ledger NARRATIVE (2 literally describe the s222 fix) - repointing = history rewrite for zero gain. Fixed at source: retired the stale flag in skill §6 + §10 banner; inverted §9's backwards self-mirror precedence (it had the gitignored `.claude/commands/` copy winning, which would re-inject em-dashes into tracked `tools/` and undo s244) so the tracked canonical wins + added a no-em-dash guard; re-mirrored tools->commands (both byte-identical + ASCII-clean, verified). Existing CLAUDE.md/ROADMAP.md ledger prose deliberately NOT edited (operator chose "keep history" via AskUserQuestion). **(b)** draft tool L (the community fork) liftability-triaged into BACKLOG "Research / inspiration": FUTURE = the Elo log-odds draft-composition aggregator (the team-vs-team layer `dashboard/routes_pickban.py` lacks; composes on `core/smoothed_rates.py`, not a duplicate); CLOSED negatives recorded (no license on fork OR upstream `draft tool L`, aggregator-D-scrape data dead-end, "+" fork adds zero math) - never re-research. **(c)** New memory `feedback_ds_coverage_prose_recompute` - DS champ-coverage %/match-row prose is a DS-batch-session docs-sync job, never recomputed in a general sync (the 4 override registries use a nested schema a flat count mis-parses). **Don't-redo:** sync-all-md self-refute is fixed - do NOT re-flag the CHANGELOG ref or re-litigate §9; draft tool L (the community fork) is triaged - do NOT re-research; the coverage/match-row prose is intentional prior-batch state, not a bug.

94. ✅ **2026-05-19 - DS per-level stat-growth correctness fix: linear -> Riot quadratic (`fd80bf3` + docs-sync `17c782e`, pushed; ENGINE_VERSION 1.4.0->1.5.0).** Found cross-checking DS math vs lolmath `@lolmath/calc`. `agents/daemon_slayer/stats.py` scaled champion per-level base stats LINEARLY (`base + perlevel*(level-1)`); Riot is QUADRATIC (`base + perlevel*(level-1)*(0.7025 + 0.0175*(level-1))`) - curves coincide ONLY at level 1 (mult 0) and level 18 (mult exactly 17.0), so every per-level base stat (hp/mp/regen/armor/mr/ad) was over-stated for levels 2-17; a real bug, not a modeling choice. Fix: new `growth_multiplier()` + `scaled()`, the 8 `CHAMPION_SCALING_RULES` repointed off the deleted `linear`; `attack_speed_scaling`/`clamp_level`/structure untouched (AS was already correct Riot math); single application point `engine._scale_champion_base` (no double-scaling); the per-level ability lambdas in `_effects_data.py` are intentionally linear ability scalings and were correctly left alone. **TDD + proof-first (don't re-verify):** RED `test_stats.py` with hand-derived Riot values -> implement -> engine hand-proven correct BEFORE any rebaseline (16 stat curves Garen/Lux/Aatrox/Malphite x hp/ad/armor/mr @ L1/6/11/13/18 match `base+perlevel*growth_multiplier(level)` exactly; DPS/EHP pipeline traces exact; Garen/Lux HP match known in-game). Only 6 DS tests drifted (far less than feared): 4 genuine mid-level pins rebaselined to corrected output + 2 pre-existing fragile exact-float assertions made tolerant - all intended, do NOT re-investigate. ENGINE 1.4.0->1.5.0 + 14 version pins. Adjacent: corrected the stale/backwards pen-pipeline comment in `ability_dps.py:1142` to match `effects.effective_target_armor` (flat-reduction->%-reduction->%-pen->flat-pen); no pen code changed. **Verified:** DS 2283/748/0, wider RC 1457/0; production :8893 restarted (RC-DaemonSlayer task) -> serves 1.5.0, Garen L11=1549.95 (in-game 1550)/L18=2356 (unchanged endpoint). Living docs synced `17c782e` (ENGINE 1.5.0 + DS tests 2283 in CLAUDE/DAEMON_SLAYER/README/BRIEF). **Don't-redo:** bug fixed+proven; do NOT re-pitch touching AS scaling (correct) or pinning the 2 float-noise values (tolerant assertions are right); the 6 rebaselines are the intended consequence. **Flagged (not blocking):** DAEMON_SLAYER coverage prose "196/125,73%" + BRIEF "2,851 matches" left as prior-batch state (unrelated to this fix; a correct recompute is a DS-batch docs-sync job - the 4 registries use a nested `_meta`/`default`/overrides schema, a flat count mis-parses it); pre-existing broken ref `docs/_archive/CHANGELOG.md` + `.claude/commands` vs `tools/` sync-all-md.md drift surfaced for operator, not auto-fixed.

### Settled - do not re-litigate (items 1-93 relocated verbatim to `docs/history_notes.md` on 2026-05-19; read that archive for full context before re-opening any line below)

- **Phases 1-7, 2.1-2.4, 3, 4-6, FU01/FU02/FU04 all shipped; the 6-scorer archetype-expansion plan (carry/tank/bruiser/mage/assassin/enchanter) is fully wired and the dispatcher has no fallbacks.** FU03 was superseded by FU04. Do not re-plan these or re-pitch a scorer.
- **DS conditional-target-state arc is operator-CLOSED (s232).** Part-2 live target-state plumbing is shelved permanently; an s232 saturation guard exists. Do NOT re-pitch Part-2 or re-scan for conditional candidates.
- **DS block_index/form_index/max_priority/combo_sequence pure-data registries were swept s223-s232 and are provably saturated** (machine-guarded). Further growth needs a schema lift, not uncovered-champion scans. The reusable pre-filters (`tools/ds_*_prefilter.py`, `ds_block_scanner.py`) are durable for patch re-extracts. Never `--force` a Meraki re-extract (the `latest` endpoint is mutable).
- **AUTONOMOUS_AUDIT s5 menu is fully exhausted** (the effects.py facade split shipped s246). Do NOT re-pitch an effects.py re-merge, an `__all__`, or a FastMCP/SDK rewrite of the stdlib MCP servers.
- **Keystone fixed + live-proven (item 87): ARAM Mayhem reports queueId 2400** (not 920). The phase-driven view-router was proven correct - do NOT re-pitch a router change. The `cs.is_aram` rendering path is item-87-preserved.
- **The augment LCU/:2999 API is a confirmed dead-end** (no capture-free augment API mid-game). Augment-OCR into the coach is the proven path. Do NOT re-pitch an LCU augment API.
- **Game-end 0x50 BSOD root cause = the continuous DXGI screen-agent** (stays DISABLED). On-demand `mcp__gamepc__capture_monitor` is the separate BSOD-clean path but stays operator-gated ("now" / "got it"); re-confirm after Vanguard/League updates.
- **`core/build_order.py` no-double-unique rule is engine-authoritative** - do NOT add a family map (a guard test fails on any family literal); the engine has 6 unique-passive families, not 3.
- **Research-list triage CLOSED negatives (do NOT re-research):** every LCU client/codegen repo is inferior to RC's lockfile client; KebsCS is the reference catalog only (no license - do not vendor); the corpus has ZERO Arena/Cherry/Mayhem lobby-create payloads (a bespoke payload must come from live LCU capture); `.rofl` full-parse is a dead-end (Match-V5 SQLite is the substrate); ML win-predictors / CV-minimap / voice / `riot-offline-mode` are all CLOSED; Pengu `league-client-mcp` = NO (thinner than RC's client).
- **`core/smoothed_rates.py` is the shared Laplace/shrink primitive**; the s220 PGR 0-100 score is deferred to ROADMAP-S3 - do NOT pre-build it.
- **Brawl mode is retired from champ-select** (s214); legacy brawl backend is left as deadcode for a separate cleanup pass.
- **The Riot Personal key is valid and in-scope.** Match-V5 403/empty on event modes (ARAM Mayhem `gameMode=KIWI`, queue 2400) is EXPECTED, not a key fault; event-mode Match-V5 timeline placeholders are correct and permanent.
- **`web/js/dashboard.js` is dead code** (no `<script>` reference; only `/js/main.js` loads). The `dashboard.js:5055` `_replayQueueLabel` 920 bug is confirmed-dead legacy - leave it.
- **ADR-008 unified asset-hash:** editing `web/{js,css}/panels/*` auto-reloads via `compute_asset_hash` - no RC restart for asset-only changes.
- **No-em-dash retroactive purge is done** (s244 `tools/strip_em_dashes.py`, reusable for drift checks); the functional `"-"` no-data sentinel is operator-approved. The smart-quote retro-sweep is NOT yet done (separate operator-gated pass).
- **Live DS truth = `data/daemon_slayer/current.txt` + `agents/daemon_slayer/__init__.py` + `/health`** (do not trust ledger recollection of patch/ENGINE). DS coverage %/match-row prose is a DS-batch docs-sync job, never recomputed in a general sync (nested registry schema; a flat count mis-parses) - memory `feedback_ds_coverage_prose_recompute`.
- **Biggest pending non-engine item: the s220 aggregator-G-style Post Game Review reframe** (UI; operator-decided scope - single-match richer layout, the 0-100 score is an RC heuristic over enriched stats with NO Claude/Riot dependency; staged S2-S5, each its own session plus the per-page UI-audit ritual). Other pending: Legion 1-PC consolidation (decided s169, option B, still pending); the one-off 101.qq.com Game-PC CDN capture (FUTURE). Live open work is tracked in `ROADMAP.md` / `BACKLOG.md`.

Full open work + future: `ROADMAP.md` + `BACKLOG.md`. Completed work: `docs/history_notes.md`.
