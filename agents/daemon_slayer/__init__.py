"""Daemon Slayer — local item-build engine.

Phase 3: stat math + auto-attack DPS + item ranker, exposed over a
local HTTP server on `:8893` (`server.py`). The package is still
importable for direct use without spinning the server.
"""

ENGINE_VERSION = "0.5.0"
