# M-05 APPLIED - Path A (defer-delete) - 2026-06-28

Operator chose **Path A (defer-delete)** for the audit-11 M-05 finding
(Game-PC SMB push deadcode). Mirrors the d019 Brawl-retirement pattern:
retire from flow, leave the backend neutered for a future cleanup pass,
KEEP the gatekeeper safety net.

## What changed

- `agents/agent2_backend/smb_push.py`
  - `share_reachable()` is now hard-`False` (no network probe) - the share
    has no receiving end post ADR-011/012.
  - `push()` and `trigger_forwarder_restart()` raise a retired `RuntimeError`
    (`_RETIRED_MSG`) as their first statement. The implementations are
    preserved unreachable below the raise for the historical contract until
    the eventual delete pass.
  - The pure helper surface (`_sanitise_label`, the `Smb*` exception
    taxonomy) is untouched and stays importable - `test_quality_pass.py`
    imports `_sanitise_label`.
- `agents/state/resolved_decisions.json` gains `phase3-d026`
  ("Game-PC SMB push deadcode pending cleanup pass (ADR-011/012)").
  `version` stays `phase3-1.1` (appending a decision is schema-compatible;
  the fail-closed validator + `test_supervisor.py:36` pin 1.1).
- Charters reworded to the post-ADR-012 reality (point at the Legion-local
  filesystem): `agents/agent2_backend/charter.md` (mandate bullet + the
  "Cross-machine operations" section, which also dropped a U+2192 glyph),
  `agents/agent3_testing/charter.md` (SMB skip-gate bullet),
  `agents/agent5_ui/charter.md` (web-push bullet),
  `agents/agent6_auditor/charter.md` (no-cross-machine-authority bullet).
- `agents/agent3_testing/suite/test_smb_push.py` rewritten: the forever-skip
  integration tests are replaced with a retired-contract pin (share False,
  push/trigger raise, helpers still importable). RED-first, then green.

## What was deliberately KEPT (Path A, not Path B)

- The Agent 0 gatekeeper `P-audit-h1` traversal guard
  (`agents/agent0_gatekeeper/evaluator.py`) + its probes
  (`agents/agent6_auditor/_audit_probes.py`) - the guard gates `Task`
  dataclasses with crafted UNC strings and is independent of `smb_push`, so
  it survives as the standing safety net. `test_audit_probes.py` +
  `test_agent0.py` stay green.

## Verification (Tier-1)

- `py_compile` clean; `ruff check` clean (no W605, no unreachable flag).
- `resolved_decisions.json` parses; version `phase3-1.1`, 26 decisions.
- `pytest test_smb_push.py test_quality_pass.py test_supervisor.py
  test_audit_probes.py test_agent0.py` -> 34 passed.

No ENGINE bump / DS / Share / RC restart (Phase 3 agent tree, not runtime
DS engine). A future quiescent-point pass can execute Path B (delete the
module + guard + charter lines) in one swing.
