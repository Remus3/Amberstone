# APPLIED - L-02 lockfile.<pid>.tmp orphan cleanup

Landed: commit `c93e383e` "fix(supervisor): L-02 retry os.replace + reap orphan
lockfile tmps" (before this artifact-commit; the proposal + report files were
left untracked at landing time - committed now as records).

Delivered vs contract:
1. 3-attempt PermissionError retry + 60ms backoff on os.replace - YES
   (agents/_supervisor_common.py:220-227).
2. finally-cleanup of the .tmp sibling on final failure - YES.
3. Startup sweeper `_reap_orphan_lockfile_tmps()` in acquire_lock() - YES
   (:255 def, :287 call site).

Tests: tests/test_supervisor_common_l02.py - 8 tests (exceeds the 3-test plan:
first-attempt success, no-tmp-after-success, retry-twice-then-succeed,
final-failure-raises-and-cleans, dead-pid-removed, live-pid-preserved,
malformed-name-skipped, mixed-dead-and-live).

Cleanup verified 2026-07-17: agents/state/ contains 0 lockfile.*.tmp orphans
(the 5 observed 2026-07-08..07-11 are gone; reaper ran on supervisor lock
acquisition as designed).

Status: CLOSED.
