# M-04 disposition - applied 2026-06-28 (main session, acting as Agent 1)

## What landed

`agents/state/resolved_decisions.json` gained all five drafted entries
verbatim (date-corrected where the draft used a placeholder):

- phase3-d021 ADR-011 Legion 1-PC consolidation (decided_at 2026-05-29)
- phase3-d022 101.qq.com duo-synergy live-wire (decided_at 2026-06-02 -
  the PROPOSAL.md draft said "2026-06-xx"; real date from
  history_notes.md item 277)
- phase3-d023 DS live truth (decided_at 2026-06-01)
- phase3-d024 ADR-012 bridge decommission (decided_at 2026-06-24)
- phase3-d025 overlay all-panels doctrine (decided_at 2026-06-27)

`locked_at` advanced 2026-04-22 -> 2026-06-28. The substantive M-04 /
M-04-bis drift is CLOSED: every operator-locked decision the audit cited
is now in the source-of-truth file (decisions count 20 -> 25).

## Why `version` stays phase3-1.1 (the part the proposal missed)

The proposal + REESCALATION + master-plan WP-F5-M04 all say to bump
`version` -> `phase3-1.2` and bump `EXPECTED_DECISIONS_VERSION` in
`agents/_supervisor_common.py` ONLY. That fix is INCOMPLETE and would
have bricked RC boot:

`EXPECTED_DECISIONS_VERSION` is checked, fail-CLOSED (raise RuntimeError
on mismatch), in THREE more sites the proposal did not mention:

1. `ops/rc_supervisor.py:61` - a FROZEN file (hard-rule: explicit
   operator approval required). `_verify_decisions_version()` raises on
   mismatch at supervisor `loop()` start -> RC would refuse to boot.
2. `agents/agent3_testing/suite/test_supervisor.py:36` asserts
   `data["version"] == "phase3-1.1"` -> red on a bare JSON bump.
3. `agents/_supervisor_common.py:43` - the one site the proposal named.

All four sites must move in lockstep or the JSON version is mismatched
against a fail-closed validator. Because one is FROZEN, the formal
version bump is operator-gated and was NOT done here. Adding the
decision entries at version 1.1 is schema-compatible (the decisions
array is data; the version field gates contract/schema migration, which
did not change) and satisfies both validators + the test unchanged.

## Bonus defect found + fixed - generator wiped the whole register

`agents/state/resolved_decisions.json` is gitignored runtime state. Its
canonical seeder is `ops/phase3_setup.py`, whose `RESOLVED_DECISIONS`
literal carried NO `decisions` key yet `main()` did an unconditional
atomic rewrite ("always rewrite - locked register"). Net effect: running
`python ops/phase3_setup.py` (the documented "to restore" step) would
have DELETED all 25 decisions - a worse drift than M-04 itself, and a
direct violation of the module's own "idempotent - safe to re-run"
docstring. The decisions array had no git-tracked source at all; it
lived only in the runtime file via audit backfills.

Fixed durably (NOT by hand-porting 25 decisions into a second literal,
which would re-create the M-04 drift class one layer down): extracted a
pure `merge_resolved_decisions(existing)` that PRESERVES an existing
`decisions`/`version`/`locked_at` register and only seeds an empty list
on a fresh scaffold; `main()` now reads-then-merges. RED-first regression
`tests/test_phase3_setup_idempotent.py` (3 tests). Dry-run vs the live
file confirms a re-run now preserves all 25 (was: would write 0).

## Remaining (operator-gated)

If a formal phase3-1.2 migration marker is wanted, one slice must touch
all four sites together (incl the frozen `ops/rc_supervisor.py`). Until
then the file is functionally complete at 1.1 with drift closed.
