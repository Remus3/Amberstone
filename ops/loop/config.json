{
  "repo_root": "C:\\Riot Commander",
  "control_dir": "C:\\Riot Commander\\ops\\loop\\control",
  "ceiling_usd": 200.0,
  "_ceiling_note": "ceiling_usd caps GEMINI spend only (director+auditor calls). Claude/executor spend is UNCAPPED per operator. DEEP-AUDIT run 2026-06-11: operator set NO spend limit; rail raised to 200 as a runaway backstop only.",
  "max_cycles": 100,
  "cycle_deadline_sec": 5400,
  "poll_sec": 5,
  "dry_run": false,
  "ignore_no_progress": false,
  "clear_each_cycle": true,
  "session_jsonl": "",
  "directive_suffix": "OPERATOR RUN FOCUS (2026-07-03 /orchestrated-run relaunch, 100 cycles, multi-agent orchestrated): docs/ORCHESTRATION_PLAN.md is the PRIMARY work source. Pick the next OPEN session top-down - the curated 'ORCHESTRATED-RUN RELAUNCH 2026-07-03' queue (ORUN1-ORUN5) comes FIRST. Each ORUN row says VERIFY-PREMISE FIRST: fresh-probe the premise (grep cited file:line + live /api/state + ops/runtime/health.json), and if the unit is already shipped record a CLEAN no-commit with evidence in the Findings log instead of re-doing it (the R73 / cycle-13 stale-digest guard). When the curated queue drains, apply the director_prompt.md REFILL PROTOCOL - this run does NOT stop on a drained plan: SYNTHESIZE one self-directed unit, append it as a DIRECTOR REFILL row (id R<N>, N > 76, Status WIP), then work it. Rotate: DS sweep/audit-iteration -> research+competitor lift (Section 7b) -> un-audited Electron-OVERLAY surface (5-phase audit + Playwright ui_recon visual; the Chrome :8888 dashboard is RETIRED as an audit surface) -> haiku-zero lane advance -> cost/latency lever. A DS-sweep refill MUST come from a FRESH adversarial Meraki-vs-registry refute pass: the R66 residual list is EXHAUSTED this patch (R70 Finding - do NOT re-pick #1-#6; #7 shield-lerp is next-patch-ingest gated). DS seams ship DEFAULT-OFF, Meraki + data/rewind_history.db WIN anchored, ENGINE bump + DS :8893 restart + Share sync in the SAME commit; the live default-ON flip is EXCLUDED - append to docs/LIVE_GAME_GATED_SYNC.md. EXCLUDE the deferred Cluster A AP-in-ARAM set (Zilean/Shaco/Kayle/Seraphine). Frozen-file + .md rewrites AUTHORIZED this run; operator-gated forks -> gemini PART C recommended path, NEVER block; truth_gate PROCEED before every multi-slice commit; commit + push origin/main + CI green + /done each cycle.",
  "claude_window_title": "Claude",
  "gemini_model": "gemini-3-pro-preview",
  "gemini_cmd": "gemini",
  "transcript_dir": "C:\\Users\\Administrator\\.claude\\projects\\C--Riot-Commander",
  "gemini_price_per_mtok": {"input": 2.0, "output": 12.0},
  "price_per_mtok": {
    "opus":   {"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5},
    "sonnet": {"input": 3.0,  "output": 15.0, "cache_write": 3.75,  "cache_read": 0.3},
    "haiku":  {"input": 1.0,  "output": 5.0,  "cache_write": 1.25,  "cache_read": 0.1},
    "default":{"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5}
  }
}
