{
  "repo_root": "C:\\Riot Commander",
  "control_dir": "C:\\Riot Commander\\ops\\loop\\control",
  "ceiling_usd": 200.0,
  "_ceiling_note": "ceiling_usd caps GEMINI spend only (director+auditor calls). Claude/executor spend is UNCAPPED per operator. DEEP-AUDIT run 2026-06-11: operator set NO spend limit; rail raised to 200 as a runaway backstop only.",
  "max_cycles": 100,
  "cycle_deadline_sec": 5400,
  "poll_sec": 5,
  "dry_run": false,
  "ignore_no_progress": false,
  "clear_each_cycle": true,
  "session_jsonl": "",
  "directive_suffix": "OPERATOR RUN FOCUS (2026-06-17, ~unbounded cycles): drive ALL open ROADMAP items to completion. PRIMARY work source = docs/ORCHESTRATION_PLAN.md 'OPERATOR REFILL 2026-06-17' sessions (DSP1-10 DS permutation swarm, HZU1, LGS1, OPEN1/2), top-down. Centerpiece: maximize DS logic across ALL context permutations (self runes + summoner spells + ally team + ally runes/auras + enemy team + enemy runes) per docs/DS_PERMUTATION_SWARM_PLAN.md - BUCKET do not enumerate; ship each seam DEFAULT-OFF, Meraki + data/rewind_history.db WIN anchored, ENGINE bump + DS :8893 restart + Share sync in the SAME commit; the live default-ON flip is EXCLUDED - append it to docs/LIVE_GAME_GATED_SYNC.md so the operator runs every live-gated item in sync. Adapt to lolmath at minimum (DSP9); implement findings to the smallest benefit. Frozen-file + .md rewrites AUTHORIZED this run; operator-gated forks -> gemini PART C recommended path, NEVER block the operator; truth_gate PROCEED before every multi-slice commit; commit + push + CI green + /done each cycle. ROUND 2 REFILL 2026-06-17 (post-NO_WORK): the plan now has OPEN rows RF1-RF5 under the 'OPERATOR REFILL 2026-06-17 ROUND 2' section - pick them top-down; RF1-RF4 are DS cross-eval residual clusters (bruiser/enchanter/tank scorer lanes burying WIN-correlated items per ops/audit/ds_perm_swarm/report/dsp10_consolidated.md) under the same default-OFF + Meraki/rewind WIN-anchored + ENGINE bump + DS :8893 restart + Share sync contract; RF5 is a test-hermeticity sweep; EXCLUDE the deferred Cluster A AP-in-ARAM set (Zilean/Shaco/Kayle/Seraphine).",
  "claude_window_title": "Claude",
  "gemini_model": "gemini-3-pro-preview",
  "gemini_cmd": "gemini",
  "transcript_dir": "C:\\Users\\Administrator\\.claude\\projects\\C--Riot-Commander",
  "gemini_price_per_mtok": {"input": 2.0, "output": 12.0},
  "price_per_mtok": {
    "opus":   {"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5},
    "sonnet": {"input": 3.0,  "output": 15.0, "cache_write": 3.75,  "cache_read": 0.3},
    "haiku":  {"input": 1.0,  "output": 5.0,  "cache_write": 1.25,  "cache_read": 0.1},
    "default":{"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5}
  }
}
