{
  "repo_root": "C:\\Riot Commander",
  "control_dir": "C:\\Riot Commander\\ops\\loop\\control",
  "ceiling_usd": 200.0,
  "_ceiling_note": "ceiling_usd caps GEMINI spend only (director+auditor calls). Claude/executor spend is UNCAPPED per operator. DEEP-AUDIT run 2026-06-11: operator set NO spend limit; rail raised to 200 as a runaway backstop only.",
  "max_cycles": 100,
  "cycle_deadline_sec": 5400,
  "poll_sec": 5,
  "dry_run": false,
  "ignore_no_progress": false,
  "clear_each_cycle": true,
  "session_jsonl": "",
  "directive_suffix": "OPERATOR RUN FOCUS (2026-06-17 relaunch, 100 cycles, multi-agent orchestrated): the ORCHESTRATION_PLAN sessions (A1-F1 / HZ-* / OVL* / DSV* / UIX* / DSP1-10 / RF1-5) are DRAINED - all DONE, zero OPEN/WIP rows. Per the operator relaunch directive 'open items + find new research items to implement and lift and ui audit; continue with ahk; when empty do more ds sweeps and other research + lifts; 100 cycles; commit+push+ci' - this run does NOT stop on a drained plan. Apply the director_prompt.md REFILL PROTOCOL: each cycle SYNTHESIZE one new self-directed unit and instruct the executor to FIRST append it to docs/ORCHESTRATION_PLAN.md as a new DIRECTOR REFILL row (id R<N>, Status WIP), then work it. Rotate: DS sweep/audit-iteration vs Meraki truth -> research+competitor lift (Section 7b) -> un-audited UI surface (5-phase audit + Claude_Preview vs /api/state) -> haiku-zero lane advance -> cost/latency lever. DS seams ship DEFAULT-OFF, Meraki + data/rewind_history.db WIN anchored, ENGINE bump + DS :8893 restart + Share sync in the SAME commit; the live default-ON flip is EXCLUDED - append to docs/LIVE_GAME_GATED_SYNC.md. EXCLUDE the deferred Cluster A AP-in-ARAM set (Zilean/Shaco/Kayle/Seraphine). Frozen-file + .md rewrites AUTHORIZED this run; operator-gated forks -> gemini PART C recommended path, NEVER block; truth_gate PROCEED before every multi-slice commit; commit + push + CI green + /done each cycle.",
  "claude_window_title": "Claude",
  "gemini_model": "gemini-3-pro-preview",
  "gemini_cmd": "gemini",
  "transcript_dir": "C:\\Users\\Administrator\\.claude\\projects\\C--Riot-Commander",
  "gemini_price_per_mtok": {"input": 2.0, "output": 12.0},
  "price_per_mtok": {
    "opus":   {"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5},
    "sonnet": {"input": 3.0,  "output": 15.0, "cache_write": 3.75,  "cache_read": 0.3},
    "haiku":  {"input": 1.0,  "output": 5.0,  "cache_write": 1.25,  "cache_read": 0.1},
    "default":{"input": 15.0, "output": 75.0, "cache_write": 18.75, "cache_read": 1.5}
  }
}
