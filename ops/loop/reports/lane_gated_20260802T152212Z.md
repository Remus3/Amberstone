# Lane 9 (Headless-Gated) cycle report - 2026-08-02T15:22Z

IN-GAME  ~4 min  (ARAM Mayhem, gameMode KIWI == queue 2400; Lee Sin, observed 15:18Z->15:22Z, game_time 6:06->8:18)  |  IDLE  ~6 min (PREP, before the operator queued)

DRAINED  0 rows.
  Honest zero. No GATE 3 row was cleanly, live-tickable this game (detail under BLOCKED).
  A cycle that rides a game but closes nothing because the drainable rows are blocked is
  the correct output when the blocks are real (skill section 0 + 2).

READY  6 rows: G8-01, G8-02, G8-03, G8-04, G8-05, G8-06 (the whole JADE gate).
  All six CHECK field paths verified against source (via a read-only research agent, then
  independently re-read by me from the files). Committed two Tier-0 doc edits (301ab395,
  0371b6be):
    - G8-03 field path was DRIFTED and would have returned nothing in-game:
      activePlayer.championName does not exist. Corrected to liveclient.champion +
      allPlayers[].championName / rawChampionName (_liveclient.py:186,289-292,87-100,288).
    - G8-04 queueId -> lcu.champ_select.queue_id / lcu.lobby.queue_id, with the mapped jade
      ids 4300-4311 / 4320 / 4321 (queue_modes.py:97-117) written in.
    - G8-06 count phrasing: band is 162 rows; 151 is the map-12 subset (mode_variants.py:9,49).
    - Added a GATE 8 READY note, then LIVE-CONFIRMED the corrected paths against the running
      ARAM/KIWI client: activePlayer carried ONLY summonerName/riotIdGameName (no championName),
      liveclient.champion=Lee Sin, allPlayers[0].championName=Swain,
      rawChampionName=game_character_displayname_Swain, liveclient.game_mode=KIWI.
  NET: when a real JADE game finally exists, all six rows are copy-paste, and the one broken
  path that would have wasted the in-game window is fixed.

REFUTED  self-adversarial SUBSTITUTION pass, 0 improper ticks.
  - Confirmed I ticked no row on the compute half. The live-confirm proves the field paths
    RESOLVE; it does NOT answer the JADE-specific VALUE question, so no G8 row is closed.
  - Independently re-read every anchor the subagent asserted (queue_modes.py, _state_builder.py,
    mode_variants.py, _liveclient.py) from source before writing it into the doc; one subagent
    framing was corrected (the doc's 3260/3261/3262 citation was already right - those are the
    UNmapped ids - so I did not "fix" a correct line).

BLOCKED  rows and what they wait on:
  - G8-01/02/03/05/06 - a real live JADE game (Riot has not shipped the queue; may never to NA).
  - G3-13 (augment render) - the ARAM coach is CREDIT-PAUSED (coach.immediate = "ARAM coach
    paused - add API credits"), so no augment reco can render, AND the Mayhem augment window was
    already past at game_time 6:06. Unobservable this game.
  - G2-38 (build top-1 hysteresis stability) - coach.item_build / item_extra are EMPTY under the
    credit pause, so there is no recommendation to sample. Not sampleable this game.
  - G3-12 (RC_ARAM_STATE_DEBOUNCE default-ON validation) - needs a default flip + RC restart.
    DELIBERATELY NOT DONE: will not disrupt the operator's live game (skill section 2, no restart).

OPERATIONAL NOTE (not a gated row, no action taken): the ARAM coach is in credit-paused degraded
mode. The friendly "coaching paused - add API credits" message is rendering exactly as CLAUDE.md
mandates (raw API errors never surfaced), so this is working-as-designed degradation, not a defect.
It does, however, gate the only two ARAM rows that were even partially drainable this game.

BRANCH  lane/gated, clean. Commits: 301ab395, 0371b6be. No /done (operator-present sibling only).
