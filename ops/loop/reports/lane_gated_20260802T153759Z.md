# Lane 9 (Headless-Gated) cycle report - 20260802T153759Z

IN-GAME  ~2 mins  (mode aram, ARAM Mayhem KIWI, Lee Sin L16, game_time 15:00 at join)  |  IDLE rest
DRAINED  0 rows          honest empty drain - see WHY below
READY    3 items         G3-11 + G3-13 measured-and-annotated; reusable in-game probe shipped
REFUTED  0 claims        made no tick to refute (adversarial gate ran anyway - VERIFIER PASS)
BLOCKED  the whole GATE 3 in-game set this window - three independent blockers, below

## What was up
A live ARAM Mayhem (gameMode KIWI, RC MODE_ARAM = GATE 3) game was already in progress when this
cycle woke: Lee Sin L16, game_time 15:00, ally `[Kled, Lee Sin, Fiddlesticks, Zilean, Veigar]` vs
enemy `[Swain, Fizz, Miss Fortune, Anivia, Caitlyn]`. RC healthy (pid 23004, alive, last_reload_ok).
The game ENDED mid-cycle (mode_key -> client -> aram/in_game=False) - the "one pass, does not come
back" reality; no new game had started by cycle end.

## WHY 0 rows drained (each verified live, not assumed)
Joined at 15:00, so champ-select was gone (G3-01/02/03 unreachable). Of the GATE 3 in-game rows, most
are already EYEBALL-CLOSED (G3-05/06/07) or carry NO LIVE RESIDUAL until code lands (G3-04/08/09/10),
per the doc. The three that could in principle have moved this game were each independently blocked:

1. VISION DEAD. `:8889/latest-frame` = 0 bytes and `/api/state` `screen_read` = `no_fresh_frame`.
   This blocks EVERY pixel / OCR / augment-render row (G3-13, G2-29, any OVERLAY-PIXEL) regardless of
   timing. Recorded as a precondition on G3-13.
2. HAIKU COACH CREDIT-PAUSED. `coach.immediate` = "Immediate: (ARAM coach paused - add API credits)"
   - the friendly degraded-mode string (Error Handling rule working: no raw API error surfaced). Any
   Haiku-output row is unobservable while credits are exhausted.
3. SUBSTITUTION, measured. G3-11 (cc_blended) lists "a TANK/BRUISER PICK against a real CC comp" as a
   closing path. That EXACT condition occurred live (Lee Sin bruiser vs a real CC comp; deterministic
   `fight_rule` even surfaced "Caitlyn 1.5s CC (W), Anivia 1.2s stun (Q)"). Yet `/api/state` carries
   NO cc field - the engine computes the value but no live surface serves it. Playing the right game
   does not close it; wiring the cc panel (code) does. This is the substitution trap the hard gate
   warns about, so the row stays open and its "play the right game" path is re-filed as dead.

Also: G3-12 (RC_ARAM_STATE_DEBOUNCE DEFAULT-ON) cannot be validated - the code default is `"0"` (OFF)
at `coaches/aram_coach.py:59`, so the DEFAULT-ON flip is un-made; observing this game is another OFF
observation, which the row forbids closing on.

## Live observations worth keeping (not ticks)
- Deterministic coach served correctly WHILE Haiku was paused: `coach_source` populated, `action`
  refreshed across polls ("Your lvl-16 spike" -> "All in now"), `fight_rule` computed enemy-comp CC
  threats. Supports the A+B deterministic north-star; no gated row closes on it.
- G7-04 census point: zero `st-*` / adaptation fields in `/api/state` during live ARAM (post-game-only
  this mode). Feeds the accrual rail; never closed on one game.
- GATE 8 (JADE) is fully PREP'd + live-confirmed against this same KIWI client last cycle (15:18Z);
  nothing more is doable on a KIWI proxy - JADE VALUES need a real JADE game, which does not exist.

## PREP shipped this cycle (Tier-1 tool + Tier-0 doc)
- NEW `tools/gated_live_probe.py` - one-shot live-game evidence probe: dumps the evidence-contract
  inputs (mode+gate, real comp, coach_source + Haiku-credit-paused, vision `frame_bytes`, cc-panel /
  st-* serving-surface scans, minimap dots, zoi) and flags the two verdicts that decide drainability
  (dead frame -> pixel rows blocked; empty serving surface -> substitution trap). READ-ONLY, fail-soft,
  never ticks. Validated live (idle branch proven against the post-game state; in-game branch proven
  against the real observed values). `tests/test_gated_live_probe.py` - 8 tests, all pass.
- `docs/LIVE_GAME_GATED_SYNC.md`: added an "IN-GAME FIRST STEP" pointer to the probe in "How to drain",
  and measured PREP annotations on G3-11 (substitution finding + re-file consequence) and G3-13 (vision
  precondition). NO row ticked - verifier-confirmed (two pure-`+` prose hunks, header counts untouched).

## Gates
- Verifier (independent, read-only): VERIFIER PASS - files exist, py_compile+ruff clean, 8/8 tests,
  glyph-clean, tool runs without traceback, and NO gated row was ticked/closed.
- Resync: not run - 0 ticks means the drain counts / session estimate are unchanged (verifier confirmed
  header counts untouched). `.claude/workflows/live-gated-resync.js` is for after a tick.
- Tier: Tier-1 (new read-only probe + its tests) + Tier-0 (doc). No engine / schema / ENGINE_VERSION /
  Share-mirror surface touched. Committed on lane/gated in the worktree; no /done (operator-present only).
