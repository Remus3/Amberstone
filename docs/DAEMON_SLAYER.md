# Riot Commander - Daemon Slayer Build Engine

Local DPS-math service on `:8893`. Computes actual damage-per-second for any champion × item × target combination using real stat math. No API cost per query.

**Status: FUNCTIONALLY COMPLETE** - ENGINE_VERSION 1.215.0 - 8490 tests - patch 16.14.1.

## Engine substrate & registries

- `mana_sim.py` finite-mana bounded rotation + `rune_procs.py` keystone/proc rune layer + `self_shred.py` target_shred DPS uplift + `scenario_matrix.py` cross-interaction invariant sweep + `ability_hps.py` v2 passive-P/target-relative heal-shield
- 547/547 DDragon purchasable items
- patch 16.14.1
- Test golden fixtures: `data/daemon_slayer/16.10.1` + `16.11.1` are the only two permanent frozen snapshot dirs (deep-audit P2b gemini ruling, 2026-06-11). New disk pins target 16.11.1; never mint a new fixture dir; 16.9.1 is retired. Guard: `tests/test_ds_fixture_policy.py`.
- cc_pressure aggregator + EHP-vs-CC blended scorer + `compute_hybrid(enemy_champions=...)` cc_blended_ehp scorer + dashboard UI consumer via `dashboard/routes_cc_blended_ehp_threat.py` (cc_blended_ehp ecosystem COMPLETE with 4 consumers: engine math + coach prompt + DS scorer + dashboard UI)
- `_PER_SPELL_CC_DURATIONS` registry 108 entries / 89 champs after wave 9 (item 147 ENGINE 1.43.0: +2 multi-wave coexistence Chogath W silence + Malzahar Q silence; silence joins first-order CC scope per wave 6 stasis precedent; saturated for net-new champions at 16.10.1 - audit walked all 226 unregistered spells of 89 champs) via `_build_per_spell_cc_durations()` setdefault builder
- `agents/daemon_slayer/cc_conditional.py` ships ConditionalCcEntry dataclass + 13 condition tags + 13 operator-tunable midpoints + `coexists_with_unconditional` flag + consumer MAX-rule semantics + NEW `notes` field per spell form via item 188 Slice A schema lift + **72-entry registry across waves 0-23** (full per-wave lineage in the Changelog below). **cc_conditional ecosystem now COMPLETE with 5 consumer surfaces shipped:** (1) cc_pressure FIRST engine math at ENGINE 1.38.0 via `compute_cc_pressure(include_conditional=False)` kwarg; (2) compute_ehp SECOND engine math at ENGINE 1.39.0 via `compute_ehp(include_conditional=False)` kwarg; (3) compute_hybrid THIRD engine math at ENGINE 1.39.0 via `compute_hybrid(include_conditional=False)` kwarg threading 3 internal compute_ehp call sites; (4) cc_conditional_impact_line FIRST coach prompt at ENGINE 1.40.0 via NEW `core/cc_conditional_impact_context.py` wired into all 4 active mode coach prompts APPEND-only; (5) routes_cc_conditional_pressure FIRST dashboard UI at ENGINE 1.40.0 via NEW `dashboard/routes_cc_conditional_pressure.py` + champ-select chip `web/js/panels/cc_conditional_pressure.js` mounted between cc_blended_ehp_threat (item 140) and pickorder. Default `include_conditional=False` preserves byte-identical behavior for all 4 existing consumers; True opts into probability-weighted conditional sum with ARAM tenacity through unified `effective_cc_duration` seam. **All 6 archetype scorers wired** through `rank_for_primary_archetype()` - carry → `ds.dps`, tank → `ds.ehp`, bruiser → `ds.hybrid`, mage → `ds.ability`, assassin → `ds.burst`, enchanter → `ds.hps`. Coach integration via `coach_integration/archetype_dispatch.py`: all 4 mode coaches (SR/ARAM/Arena/Brawl) inject scorer-aware DS picks before each Haiku call; legacy `daemon_slayer_picks` shape preserved for dashboard compat. Per-(champion, key) override registries cover 73% of the 171-champion roster: `champion_max_priority.json` / `champion_combo_sequences.json` / `champion_form_index.json` / `champion_block_index.json` (196 entries / 125 champions). The single-int/list `block_index` registry is **provably saturated** for new champions (s223 5-way scan of all 47 uncovered champs → 0 candidates) AND exhausted on unmapped keys of covered champs (s224→s225 pre-filter over all 125, re-run post-parser-fix → only Bel'Veth R + Varus W were clean finds); further growth needs the conditional-target-state schema lift or upstream Meraki data fixes. The sibling `champion_form_index.json` (12 champions after the s226 sweep - Swain R / Briar W / Evelynn E added: the canonical recast/empowered form the engine was defaulting away from) is likewise swept; remaining multi-form gaps are R-gated or contextual transforms (Heimer W/E, Gnar, RekSai, Skarner Q). `champion_max_priority.json` (15 champions after the s227 audit - Brand/Talon/Fiddlesticks W-first added) and `champion_combo_sequences.json` are **play-pattern registries**: the numeric pre-filter over-flags (its level-11 optimum ≠ real in-game max order - e.g. it flags Azir W-first vs the universal Q-max), so they are meta-curated, NOT numeric-swept; combo_sequence was assessed adequate for its reset/chain purpose (no clean adds). Extractor `_canonicalize_unit()` (s223) strips Meraki's nested conditional `(+ ...)` parentheticals; `_UNIT_TO_FIELD` (s224) additionally pins the text-drift health-unit family (double-space / "the target's" / caster pronoun+name) - together these recovered the %-HP damage component (previously trapped in `unparsed_modifiers`) on ~22 champions incl. Gwen Q·R, Varus W, Trundle R + Fiddlesticks Q (the latter two were 0 entirely), Cho'Gath E, K'Sante W, Sett Q, Sejuani W, Zac Q. Cooldown inheritance from form 0 when non-form-0 has `cooldown=None` (Riven R / Renekton E / AurelionSol R / Qiyana Q - s206).

## Changelog

- _Changelog 1.145.0-1.156.0 (the R7/R9/R12/R14/R17/R39/R41 + item 513/515 default-OFF scorer seams - incl. cc_conditional durations_floor_s, the anti-tank level-ramp %max-HP endpoints + the R39 %current-HP endpoints (Senna P 1:10), the R35 percent-DR EHP consumer, the 1.154.0 live-flip seam-flag HTTP wiring across /rank, and the R41 ally mark-detonation seam (Leona P Sunlight 32:151, assume_ally_detonation default-OFF)): per-version detail lives in `agents/daemon_slayer/CHANGELOG.md` + `Share/CHANGELOG.md` + `docs/LEDGER.md`; the status line above tracks live (1.208.0) while this summary changelog resumes in full at 1.144.0 below._
- **ENGINE 1.144.0: Arena boots map30 mirror (build-table data correctness).** The precomputed Arena (map 30) build-order tables carried the bare 3xxx tier-2 boots ids (Berserker's `3006`, Mercury's `3111`, etc.), every one of which is `maps.30=False` - illegal on the Arena map. `core/build_order._select_boots` now remaps the resolved tier-2 boot to its `22`-prefixed map30-legal Arena mirror (`_BOOTS_ARENA_MIRROR`: 3006->223006, 3009->223009, 3020->223020, 3047->223047, 3111->223111, 3158->223158, each verified `maps.30=True`) on Arena/CHERRY, mirroring the existing SR tier-3 upgrade branch; ARAM (map 12) keeps the 3xxx boot (map12-legal). All three Arena build tables (flat + comp-archetype + anti-tank/anti-squishy variants) regenerated boots-only; SR/ARAM byte-identical. No ranker math change (the item scorers never import `core/build_order`); the engine bump is build-table provenance. Source data: Riot Data Dragon.
- **ENGINE 1.143.0: Arena ability-haste drift correction (16.12.1 normalization).** The hand-maintained item ability-haste registry (`_item_ability_haste.py`, the canonical engine-side AH source since Data Dragon `item.json` stats do not expose `AbilityHaste`) was pinned at an earlier patch. Three `22`-prefixed Arena mirror items that Riot normalized DOWN in 16.12.1 were corrected against live Data Dragon: Imperial Mandate `224005` 35->15, Iceborn Gauntlet `226662` 10->15, Serylda's Grudge `226694` 10->15 (the SR/ARAM canonicals were already 15). NEW `ops/audit/item_ah_drift_check.py` re-derives the registry from Data Dragon and diffs it (reusable per-patch drift guard). Feeds `compute_ability_dps` Arena cooldowns. Source data: Riot Data Dragon.
- **ENGINE 1.142.0: F2 cost-aware-top gate (DEFAULT-OFF / byte-identical).** The item rankers sort by `sort_by="delta"` (absolute DPS/EHP gained, which scales with item cost), so the 6000g ARAM/Arena mega-item Void Immolation (`223069`, maps 12+30) floated to RANK 1 in every comp cell on the hybrid/bruiser + ehp/tank scorers - a ranking-surface artifact, not a build-correctness claim. NEW optional `cost_ceiling` param on the shared `rank._filter_candidates` drops any candidate whose `gold.total` strictly exceeds the ceiling, threaded through `rank_items` (dps) / `rank_items_by_ehp` (tank) / `rank_items_by_hybrid` (bruiser); the other 4 callers (mage/assassin/enchanter/beam) leave it unset. None (the default) is byte-identical. The live `rank.py` / `server.py` flip stays validation-gated (`docs/LIVE_GAME_GATED_SYNC.md`). +12 tests. Source data: Riot Data Dragon / CommunityDragon / Meraki Analytics.
- **ENGINE 1.141.0: B1 melee-applicability gate (DEFAULT-OFF / byte-identical).** The auto-attack DPS path (`dps.py` / `hybrid.py`) carried no attack-range gate, so Runaan's Hurricane's two extra bolts - a ranged-basic-only on-hit - were credited on melee autos, over-valuing Runaan's on melee champions. NEW `PeriodicProc.ranged_only` flag (set on Runaan's `3085` + Arena `223085` Wind's Fury) + `CallContext.is_melee` (engine-derived from the champion record `attackrange < dps.MELEE_RANGE_CEILING` = 350, which separates short-melee from the shortest ranged carry) + an `apply_melee_aa_gate` kwarg threaded through `compute_dps` -> `_phase_weighted_dps` -> `_rotation_attack_dps` -> `_periodic_proc_dps`, plus `_per_attack_proc_damage` (burst) and `compute_hybrid`. When the gate is on and the wielder is melee, a `ranged_only` proc contributes 0; default-off the flag is never read (byte-identical) and ranged champions are unaffected even when on. The live scorer-dispatch flip stays validation-gated (`docs/LIVE_GAME_GATED_SYNC.md`). +9 tests. Source data: Riot Data Dragon / CommunityDragon / Meraki Analytics.
- **ENGINE 1.140.0: DSP5/6/7 live-context consumers + anti-tank P3.2 caster-stat producer (DEFAULT-OFF / additive).** The three DSP substrate seams (summoner-spell / enemy-rune / ally-grant registries) gained a consumer layer in NEW `agents/daemon_slayer/dsp_live_consumers.py`: `summoner_fight_adjustments` (self + enemy summoner set -> Heal/Barrier EHP, Cleanse tenacity, Ghost/Heal MS, self-Exhaust incoming-DR, enemy Ignite antiheal), `enemy_rune_threat` (enemy rune set -> Press-the-Attack incoming-amp EHP divisor, Conqueror ramp, Grasp + Second Wind poke-sustain, antiheal), `ally_protected_ehp` (an ally's enchanter shield/heal flat-HP grant + resist grant folded into `compute_ehp`). NEW `antitank.compute_antitank_live` resolves a champion's AP/AD from its live build via `build_champion` and feeds the anti-tank scorer's P3.2 caster-stat scaling. Each is byte-identical on an empty / naked context; the live default-ON wires stay validation-gated (`docs/LIVE_GAME_GATED_SYNC.md`). +15 tests. Source data: Riot Data Dragon / CommunityDragon / Meraki Analytics / LoL wiki.
- _Changelog gap 1.130.0-1.139.0 (the DSP3-DSP11 + RF1-RF6 headless-swarm seams): the canonical per-version entries live in `Share/CHANGELOG.md` + `agents/daemon_slayer/CHANGELOG.md` + `docs/LEDGER.md` (items 464-487). This summary doc's changelog was not kept in lock-step through those cycles; entries resume in full here at 1.140.0._
- **ENGINE 1.129.0: DSP2 Cluster-B off-class WIN-exemption seam (DEFAULT-OFF).** The DS permutation-swarm WIN-anchor flagged Ezreal SR as the worst outcome-divergent champ-mode: the item-213 ranged-marksman off-class deny-set (`rank.OFFCLASS_MARKSMAN_ITEM_NAMES`) hard-strips Sheen-line / on-hit items (Trinity Force, Spear of Shojin, Black Cleaver) from EVERY ranged marksman - right for a crit ADC (Caitlyn/Jinx/Sivir) but wrong for a Sheen/ability caster-marksman: Trinity Force is Ezreal's most-built item (rewind ARAM n=219) yet was excluded from the candidate pool; Spear of Shojin / Black Cleaver are real Corki/Smolder/Senna builds. No kit-data axis exists for "wants Sheen" (the damage distribution is AD/AP only), so the exemption is WIN+usage anchored: `ops/audit/ds_perm_swarm/build_marksman_offclass_exempt.py` distills the cross-eval empirical (rewind WIN) data into `agents/daemon_slayer/marksman_offclass_exempt.json` (exempt at n>=30 AND wr>=mode_baseline-3). NEW DEFAULT-OFF `rank_items(exempt_offclass_by_win=True)` subtracts the exempt names from the deny-set so they re-enter the pool; byte-identical when off (the DSV1-4 precedent), crit ADCs untouched. 4 caster-marksmen exempted (Corki/Ezreal/Senna/Smolder); the live default-ON flip is validation-gated (`docs/LIVE_GAME_GATED_SYNC.md`). +12 hermetic tests. Source data: rewind match-history outcomes; Riot Data Dragon / CommunityDragon / Meraki Analytics.
- **ENGINE 1.128.0: Aphelios dps zero-output fix - degenerate basic=0 scenario fallback.** `compute_dps` scores only the basic-attack portion of laning-scenario rotations; Aphelios's upstream lolmath scenario encodes basic=0 in every phase, so weighted_dps collapsed to 0.0 -> every item delta 0 -> the carry ranker degenerated to an all-Doran's/0.0 ranking. FIX: fall back `weighted_dps = raw_attack_dps * mode_mult` when ALL phases have zero basic-attack DPS AND mode_mult>0 (the gate preserves the ARAM-disabled contract: aramDamageDealt=0 -> mode_mult=0 -> stays 0). Fires for 4 champs at 16.12.1 (Aphelios live + casters Cassiopeia/Fiddlesticks/Sylas latent); byte-identical for champs with real basic-attack rotations. Found in the comprehensive per-champion cross-eval (`ops/audit/ds_cross_eval/`). +4 regression tests. Source data: CommunityDragon / Meraki Analytics laning scenarios.
- **ENGINE 1.127.0 (item 437): DSV4 - Spear of Shojin Focused Will ability-amp valuation (P6-G5 / G2-residual bucket-B scorer-valuation). The ability scorers had no seam for Spear of Shojin's Focused Will, a stacking ability/passive damage amp (3% per stack, max 4 stacks = 12%); the item was `defensive_only` "ability damage not DPS-modeled", so an ability-reliant build that bought Shojin saw zero damage value from it. Focused Will amps ABILITIES and PASSIVES only, never basic attacks. NEW `ItemEffect.ability_damage_amp_per_stack` / `ability_damage_amp_max_stacks` (0.0 / 0 default = byte-identical, appended at the END) + `effects.total_ability_damage_amp` helper + `dps._ASSUMED_ABILITY_AMP_STACKS = 4`. A DEFAULT-OFF `assume_ability_amp` seam on `compute_ability_dps` (multiplies the spell ability sum; the item DoT procs are NOT amped), `compute_burst_damage` (multiplies ONLY the ability total, never the auto-attack total), and `rank_items_by_burst` (threads both call sites); `compute_dps` (the auto-attack scorer) is untouched. Data: Spear of Shojin 3161 + Arena 223161 pinned to 0.03 / 4. Ships DEFAULT-OFF (byte-identical when off); the live rank scorers do not pass `assume_ability_amp=True` yet - the flip is a separate real-game-validation-gated step. +14 Meraki-anchored tests. Source data: Meraki Analytics 16.12.1.**

- **ENGINE 1.126.0 (item 433): DSV3 - lethality-vs-sustained-AD burst-ranker valuation (P6-G5 residual). `rank_items_by_burst` defaulted `target_armor=0.0`, and against zero armor `effective_target_armor` floors its penetration tail at zero, so lethality (flat armor pen, Riot V14.1 1:1) added nothing to a ranked item's delta and a raw-AD item out-ranked a lethality item - but a burst assassin's real target is a squishy carry WITH armor. NEW DEFAULT-OFF `assume_squishy_target` seam: when ON and the caller did not pin a positive `target_armor`, a representative squishy-carry armor (`_assumed_squishy_target_armor`: 22 base + 4.5 per level -> 67 at L11) is substituted for the baseline and every candidate so lethality flows through and out-values raw AD; an explicit `target_armor>0` is respected. The lethality math is unchanged - only the ranker target assumption. Byte-identical when off; the live `rank.py` flip stays validation-gated. +7 characterization tests (difference-of-differences). Source data: Riot Data Dragon / Meraki Analytics 16.12.1.**

- **ENGINE 1.125.0 (item 432): DSV2 - takedown / kill-state item-passive valuation (P6-G5 residual). The burst + auto scorers had no seam for the on-takedown item passives, so Hubris (Eminence bonus AD) and The Collector (5% execute) were under-ranked among lethality items. NEW DEFAULT-OFF kill-state OFFENSE seam: `ItemEffect.takedown_bonus_ad_base` / `takedown_bonus_ad_per_stack` (Hubris 15 + 2 per stack) + `execute_max_hp_pct` (Collector below 5% max HP) + `effects` aggregators + `dps._ASSUMED_TAKEDOWN_STACKS = 1` + an `assume_takedown` kwarg on `compute_dps` / `compute_burst_damage` / `rank_items_by_burst`. Hubris bonus AD folds into the AA rotation + ability scaling; the Collector execute is credited as a 5% target-max-HP TRUE finisher (burst only - a one-shot execute is not sustained DPS). Death's Dance carries NEITHER field: its takedown payoff is the Defy heal, already valued on the survivability axis (ENGINE 1.57.0), so the seam is a deliberate no-op for it (no phantom double-count). Byte-identical when off; live rank flips stay validation-gated. +16 Meraki-anchored tests. Source data: Meraki Analytics 16.12.1.**

- **ENGINE 1.124.0 (item 431): DSV1 - AP damage-over-time burn valuation (P6-G5 residual). `compute_ability_dps` (the AP/mage item scorer) mirrored `compute_dps`'s item amp + pen handling but never folded the item PERIODIC procs, so ability-triggered AP burn DoTs (Liandry's Torment, Blackfire Torch, Demonic Embrace) were invisible to the mage ranking even though the auto scorer has valued them since Phase 4. `PeriodicProc` gains an `ability_dot` flag (default False = byte-identical) + `_periodic_proc_dps` gains `ability_dot_only`; `compute_ability_dps` folds ability_dot proc DPS into the ability total. ADD Liandry's Torment (2% target max HP/s) + FIX Blackfire's Baleful Blaze (Meraki total 60 + 6% AP over 3s = 10 + 1% AP per 0.5s tick, correcting a tooltip tick-count mis-read) + TAG Demonic's Azakana's Gaze; a curated `ability_dot` tag keeps tank Immolate auras / physical spellblades / on-cast nukes out of the AP ranking. Live: Veigar's Liandry's moved from ~#23 to #1 at a realistic target max HP. +8 Meraki-anchored tests. Source data: Meraki Analytics 16.12.1.**

- **ENGINE 1.123.0 (item 423): P6 lolmath build-engine parity G4 - boots-pool refresh to the 16.12.1 tier-3 boots. DDragon 16.12.1 added Summoner's-Rift-only tier-3 upgraded boots (Spellslinger's Shoes / Gunmetal Greaves / Armored Advance / Chainlaced Crushers / Crimson Lucidity / Swiftmarch / Immortal Path) and pulled Mobility Boots + Symbiotic Soles from the store; the build planner still emitted only legacy tier-2 boots (Mercury's Treads / Berserker's Greaves), diverging from the lolmath parity oracle. `core/build_order._select_boots` now upgrades the resolved tier-2 family to its tier-3 form on SR (map 11) via the new `_BOOTS_SR_UPGRADE` map (each DDragon `into` verified map11=True / map12=False / map30=False) - ARAM (map 12) and Arena (map 30) have no tier-3 form and keep tier-2 - and the assassin default moves off the out-of-store Mobility Boots to Ionian -> Crimson Lucidity. No engine MATH change: the ranker is byte-identical and never imports `core/build_order`; the ability-haste + tenacity registries and `items.json` already carried the tier-3 ids, so all 9 build_orders tables (flat + HZ-B1 + HZ-B2 variants, sr/aram/arena) regenerated identically except the synthetic boots slot. Verified 9/9 tables: SR all tier-3, ARAM/Arena all tier-2, 0 Mobility. Deferred: Arena should use the 22xxxx boots mirror (3xxx are map30=False). Source data: Riot Data Dragon.**

- **ENGINE 1.122.0 (item 421): P6 lolmath build-engine parity G1 - kit-damage-axis archetype correction. DDragon CLASS tags encode ROLE, not the AD-vs-AP axis a kit scales on, so `core/archetype_picks` resolved AP-scaling kits to an AD scorer (Fighter -> bruiser on Gwen; Marksman -> carry on Teemo) and an AD assassin to the AP enchanter scorer (Pyke) - the build engine then built the WRONG damage axis. The DEFAULT archetype is now re-based against `champions.json` `lolmath.damage_distribution` (the ground-truth axis), below any operator pick; tank stays axis-neutral. 18 default-source champions re-base (11 named by the lolmath-vs-DS sweep + 7 AP assassins it missed - Akali/Ekko/Evelynn/Fizz/Kassadin/Katarina/LeBlanc - plus Pyke AP->AD). No engine MATH change: the scorer code is byte-identical; this re-selects WHICH scorer a tag default reads, so all build_orders tables were regenerated. Source data: Riot Data Dragon.**

- **ENGINE 1.121.0 (item 414): EHP sustain contract-gap closure - an explicitly-named effective-survivability term that folds the damage-conversion VAMP heal (lifesteal / spellvamp / omnivamp) into the EHP. Closes the long-standing strict-xfail (lifesteal/spellvamp/omnivamp resolve as wireable stats but had no NAMED effective-EHP sustain output). The EHP scorer has folded the lifesteal heal pool into `blended_ehp` since ENGINE 1.28.0; this lift adds the NAME + the previously-unconsumed spellvamp/omnivamp stats via a SIBLING layer (the `cc_blended_ehp` pattern - `blended_ehp` / `physical_ehp` / `magical_ehp` / `true_ehp` stay BYTE-IDENTICAL). NEW module helper `_vamp_heal_pool` generalises `_lifesteal_heal` (kept as a thin back-compat wrapper); `compute_ehp` adds an additive `_blend_with_heal(heal_scalar)` closure (reuses the main-blend locals so `_blend_with_heal(heal_total)` == `blended_ehp` exactly) + 5 NEW `EhpResult` fields (`effective_ehp_with_sustain` = blend incl the full vamp pool, `ehp_without_sustain` = vamp heal stripped / item-passive heals + shields kept = the raw EHP, `sustain_ehp_delta`, `heal_spellvamp`, `heal_omnivamp`) + a `"sustain"` to_dict block + a format_table row. No SR item in 16.12.1 resolves spellvamp/omnivamp as a stat, so `effective_ehp_with_sustain == blended_ehp` on every current build (lifesteal already in `blended_ehp`); the divergence is latent / future-proof. The vamp heal reuses the lifesteal AA-throughput proxy (a documented lower-bound for spellvamp's ability-damage basis). PURELY ADDITIVE / byte-identical at default. +18 tests `test_ehp_sustain_contract.py` + the former strict-xfail flipped to a passing regression. Source data: Riot Data Dragon / CommunityDragon 16.12.1.**

- **ENGINE 1.118.0 (item 309): Extended-dueling / 1v1 sustained-fight scorer - the SEVENTEENTH SCORED axis and the one that owns the PROLONGED-1v1 / attrition dimension (the burst scorer measures front-loaded damage and the DPS scorer measures raw output against a fixed dummy; neither answers whether the kit WINS a long duel past the burst window). The COMPOSITE who-out-ramps / re-cycles / out-heals / out-lasts a single opponent: duelists who ramp + reset + heal + endure (MasterYi/Tryndamere/Jax/Aatrox/Yone/Fiora/Nasus/Warwick/Vladimir) vs burst / artillery / utility champs who win-or-lose in the first few seconds (Leblanc/Veigar/Lux/Xerath/Janna/Sona). NEW `agents/daemon_slayer/extendedduel.py`: a per-(champion, source, kind) `ExtendedDuelEntry` registry + `_EXTENDEDDUEL_KIND_WEIGHT` 4-tier [RAMP 1.0 / RESET 0.85 / DUELHEAL 0.75 / ENDURE 0.6] + `_EXTENDEDDUEL_CADENCE_MULT` 3-tier [SUSTAINED 1.0 / PERIODIC 0.8 / BURST 0.65] + `_EXTENDEDDUEL_CONDITIONAL_PROB=0.5` + `compute_extendedduel` -> `ExtendedDuelResult`. Each mechanism carries a 0..1 magnitude; `duel_score` = sum of kind_weight * cadence_mult * magnitude (conditional at the midpoint), `top_kind` labels the strongest duel tool, `ramps` flags any RAMP mechanism (the champion gets STRONGER the longer the fight runs - do not commit to a long 1v1 against her). A slot may carry two kinds (Aatrox P = RAMP + DUELHEAL; Nasus R = RAMP + DUELHEAL; Renekton fury = RESET + ENDURE). NEW `/extended-duel` route (POST+GET). PURELY ADDITIVE - reads no scorer, read by none, so every existing route is BYTE-IDENTICAL. SELECTIVE but BROAD 171-champ fan-out (10 classify + 10 completeness critics) -> 283 entries / 111 champions / 81 conditional / 63 ramping across kinds RAMP 81 / ENDURE 76 / DUELHEAL 75 / RESET 51 + cadence SUSTAINED 97 / PERIODIC 95 / BURST 91. + `tools/ds_extendedduel_build.py` (reusable validate/inject generator) + `extendedduel_registry_notes.json` provenance sidecar (Share-excluded). Live: MasterYi 2.74 / Tryndamere 2.38 / Jax 2.12 / Aatrox 2.11 / Yone 1.66 / Fiora 1.65 / Nasus 1.60 (top duelists) vs Leblanc / Veigar / Lux / Xerath / Janna 0.0 (burst/artillery/utility floor). +32 tests `test_extendedduel_item309.py`. NEXT (Phase D): an auto-pairing consumer crediting extended-duel into a live matchup / "do not take this lane into a long 1v1" verdict; tune the kind weights + cadence mults after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**

- **ENGINE 1.117.0 (item 308): Anti-tank / %HP-damage + resist-shred scorer - the SIXTEENTH SCORED axis and the one that owns the TANK-MELT dimension (the DPS / burst scorers measure raw damage against a fixed dummy; none asks whether the kit cares how much health and armor the enemy stacked). %max-HP carries / resist-shredders / %current-HP threats (Rumble/KSante/Sion/Trundle/Vayne/KogMaw) vs flat-damage champs whose output ignores enemy health and resists (MasterYi/Annie/Lux). NEW `agents/daemon_slayer/antitank.py`: a per-(champion, source, kind) `AntiTankEntry` registry + `_ANTITANK_KIND_WEIGHT` 4-tier [MAX_HP 1.0 / SHRED 0.85 / CURRENT_HP 0.75 / PERCENT_PEN 0.65] + `_ANTITANK_CADENCE_MULT` 3-tier [SUSTAINED 1.0 / PERIODIC 0.8 / BURST 0.65] + `_ANTITANK_CONDITIONAL_PROB=0.5` + `compute_antitank` -> `AntiTankResult`. Each mechanism carries a 0..1 magnitude; `antitank_score` = sum of kind_weight * cadence_mult * magnitude (conditional at the midpoint), `top_kind` labels the strongest tool, `shreds_resist` flags any SHRED/PERCENT_PEN (the team-wide resist-shred signal). Every kind gets STRONGER the tankier the target; pure %-missing-health executes are EXCLUDED as finishers. A slot may carry two kinds (Trundle R = %max-HP drain + resist steal). NEW `/anti-tank` route (POST+GET). PURELY ADDITIVE - reads no scorer, read by none, so every existing route is BYTE-IDENTICAL. SELECTIVE 171-champ fan-out (8 classify + 4 completeness critics) -> 102 entries / 78 champions / 38 conditional across kinds MAX_HP 63 / SHRED 25 / CURRENT_HP 9 / PERCENT_PEN 5. + `tools/ds_antitank_build.py` (reusable validate/inject generator) + `antitank_registry_notes.json` provenance sidecar (Share-excluded). Live: Rumble 1.42 / KSante 1.24 / Sion 1.21 / Trundle 0.99 / Vayne 0.95 (top tank-melters) vs MasterYi / Annie / Lux 0 (flat-damage floor). +32 tests `test_antitank_item308.py`. NEXT (Phase D): an auto-pairing consumer crediting anti-tank into a live draft / itemization verdict (buy %HP / shred vs an enemy tank line); tune the kind weights + cadence mults after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**

- **ENGINE 1.116.0 (item 304): Ally-amplification / buff-throughput scorer - the FIFTEENTH SCORED axis and the one that owns the TEAM-BUFF-THROUGHPUT dimension (every prior axis scores what a champion does to an ENEMY, to a piece of GROUND, or to her OWN body; this is the MIRROR of self-sustain pointed at teammates). How much COMBAT VALUE a champion grants her ALLIES - the shields, heals, steroids, hard-saves, and haste she pumps OUTWARD (Taric/Milio/Nami/Janna/Senna/Lulu) vs selfish carries who pump nothing (MasterYi/Zed/Vayne/Darius). NEW `agents/daemon_slayer/allyamp.py`: a per-(champion, source) `AllyAmpEntry` registry + `_ALLYAMP_KIND_WEIGHT` 5-tier [PROTECT 1.0 / SHIELD 0.85 / HEAL 0.75 / STEROID 0.6 / HASTE 0.45] + `_ALLYAMP_SCOPE_MULT` 3-tier [TEAM 1.0 / DUO 0.8 / SINGLE 0.65] + `compute_allyamp` -> `AllyAmpResult`. `allyamp_score` = sum of kind_weight * scope_mult * magnitude (conditional at the 0.5 midpoint), `top_kind` labels the strongest ally-buff, `saves_ally` flags any PROTECT mechanism (a hard save - invuln / revive / untargetable placed on an ally). NEW `/ally-amp` route (POST+GET). PURELY ADDITIVE. SPARSE 171-champ fan-out -> 74 entries / 43 champions / 30 conditional across kinds SHIELD 19 / HEAL 18 / HASTE 15 / STEROID 14 / PROTECT 8. + `tools/ds_allyamp_build.py` + `allyamp_registry_notes.json` (Share-excluded). Live: Taric 1.20 / Milio 1.14 / Nami 0.82 / Janna 0.82 (top ally-buffers) vs MasterYi / Zed / Vayne 0. +30 tests `test_allyamp_item304.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**

- **ENGINE 1.115.0 (item 303): Objective / structure-damage scorer - the FOURTEENTH SCORED axis and the one that owns the MACRO / SIEGE dimension (every prior axis scores combat vs a champion or a piece of ground; this scores throughput vs the OBJECTS that win the game - turrets / structures + epic monsters). NEW `agents/daemon_slayer/objdamage.py`: a per-(champion, source) `ObjDamageEntry` registry + `_OBJDAMAGE_KIND_WEIGHT` 5-tier [STRUCTURE_BONUS 1.0 / MONSTER_BONUS 0.9 / SUSTAINED_DPS 0.7 / SUMMON_DPS 0.55 / BURST_SECURE 0.45] + `_OBJDAMAGE_SCOPE_MULT` 3-tier [BOTH 1.0 / MONSTER 0.85 / STRUCTURE 0.85] + `compute_objdamage` -> `ObjDamageResult`. `objdamage_score` = sum of kind_weight * scope_mult * magnitude (conditional at the 0.5 midpoint), `top_kind` labels the strongest mechanism, `pressures_structures` flags any BOTH/STRUCTURE-scope mechanism. NEW `/objective-damage` route (POST+GET). PURELY ADDITIVE. FULL-roster 171-champ fan-out -> 285 entries / 171 champions / 66 conditional across kinds SUSTAINED_DPS 219 / BURST_SECURE 28 / SUMMON_DPS 18 / MONSTER_BONUS 16 / STRUCTURE_BONUS 4. + `tools/ds_objdamage_build.py` + `objdamage_registry_notes.json` (Share-excluded). Live: KogMaw 1.27 / Belveth 1.21 / Kindred 1.15 / Volibear 1.15 (top objective threats) vs Soraka / Yuumi / Janna 0.11. +32 tests `test_objdamage_item303.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**

- **ENGINE 1.114.0 (item 302): Zone-control / area-denial scorer - the THIRTEENTH SCORED axis and the one that owns the SPATIAL-persistence dimension (every prior axis scores WHAT a champion does to a target; this scores how it denies and reshapes SPACE for a DURATION). NEW `agents/daemon_slayer/zonecontrol.py`: a per-(champion, source) `ZoneControlEntry` registry + `_ZONECONTROL_KIND_WEIGHT` 5-tier [TERRAIN 1.0 / FIELD 0.85 / TRAP 0.7 / SUMMON 0.6 / DISPLACE 0.45] + `_ZONECONTROL_PERSISTENCE_MULT` 3-tier [SUSTAINED 1.0 / TIMED 0.8 / BRIEF 0.55] + `compute_zonecontrol` -> `ZoneControlResult`. `zonecontrol_score` = sum of kind_weight * persistence_mult * magnitude (conditional at the 0.5 midpoint), `top_kind` labels the strongest mechanism, `controls_terrain` flags any TERRAIN mechanism. NEW `/zone-control` route (POST+GET). PURELY ADDITIVE. SPARSE 171-champ fan-out -> 98 entries / 83 champions / 45 conditional across kinds FIELD 55 / SUMMON 12 / TERRAIN 11 / TRAP 10 / DISPLACE 10. + `tools/ds_zonecontrol_build.py` + `zonecontrol_registry_notes.json` (Share-excluded). Live: Anivia 1.32 / Illaoi 0.96 / Viktor 0.95 / Karthus 0.86 (top zoners) vs MasterYi / Zed / Talon 0. +29 tests `test_zonecontrol_item302.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**

- **ENGINE 1.113.0 (item 301): Effective threat-range scorer - the TWELFTH SCORED axis and the one that owns the REACH dimension (every prior scored axis scores WHAT a champion does in a fight; this one scores from HOW FAR AWAY it can threaten damage or CC). Artillery pokers / long ADCs who threaten from screens away (Xerath/Caitlyn/Lux/Ziggs/Velkoz) vs melee-only juggernauts who must close to do anything (Garen/Udyr/MasterYi/Olaf). NEW `agents/daemon_slayer/threatrange.py`: a per-(champion, source) `ThreatRangeEntry` registry (source = P/Q/W/E/R or BASE) + `_THREATRANGE_BAND_WEIGHT` 6-tier reach table [GLOBAL 1.1 / ARTILLERY 1.0 / LONG 0.75 / MEDIUM 0.5 / SHORT 0.3 / MELEE 0.15] + `_THREATRANGE_KIND_MULT` 4-tier threat-type table [BURST 1.0 / CC 1.0 / SUSTAINED 0.9 / POKE 0.7] + `_THREATRANGE_CONDITIONAL_PROB=0.5` + `compute_threatrange` -> `ThreatRangeResult`. Each mechanism carries a 0..1 magnitude (how much of the champion's threat it carries / how reliably it lands at that reach); `threatrange_score` = sum of band_weight * kind_mult * magnitude (conditional credited at the midpoint), `top_band` labels the band of the highest-value mechanism (the longest REAL threat), `is_artillery` flags any ARTILLERY/GLOBAL-band mechanism (the long-siege signal a poke consumer keys on). NEW `/threat-range` route (POST+GET). PURELY ADDITIVE - reads no scorer, read by none, so every existing route is BYTE-IDENTICAL. EXHAUSTIVE 171-champ fan-out (10 classify + 10 completeness critics) -> 725 entries / 171 champions (full roster) / 185 conditional across kinds CC 261 / BURST 191 / SUSTAINED 183 / POKE 90 + bands LONG 191 / MEDIUM 159 / MELEE 138 / SHORT 133 / ARTILLERY 71 / GLOBAL 33. + `tools/ds_threatrange_build.py` (reusable validate/inject generator) + `threatrange_registry_notes.json` provenance sidecar (Share-excluded). Live: Xerath 2.28 / Caitlyn 1.94 / Lux 1.84 / Ziggs 1.81 / Velkoz 1.71 (top artillery) vs Garen 0.33 / Udyr 0.51 / MasterYi 0.60 (melee-only); a champion with no ranged threat is small-all-melee, an unknown is 0. +29 tests `test_threatrange_item301.py`. NEXT (Phase D): an auto-pairing consumer crediting threat-range into a live poke / siege / draft-range verdict; tune the band weights + kind mults after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.112.0 (item 300): Wave-clear / AoE-shove scorer - the ELEVENTH SCORED axis and the one that owns the lane-priority / roam-window / objective-setup TEMPO dimension (every prior scored axis measures combat against a CHAMPION target; this one scores throughput against a wave of MINIONS). How fast and how safely a champion clears a wave and shoves a lane - decisive ranged shovers (Zyra/Ziggs/AurelionSol/Heimerdinger/Brand/Sivir/Karthus) vs no-AoE last-hitters who concede priority (Vayne/Warwick/Yuumi/Zed/Talon). NEW `agents/daemon_slayer/waveclear.py`: a per-(champion, source) `WaveclearEntry` registry (source = P/Q/W/E/R or BASE) + `_WAVECLEAR_KIND_WEIGHT` 5-tier [FULL_AOE 1.0 / AOE_DOT 0.85 / MULTI_HIT 0.6 / CLEAVE_AA 0.5 / SINGLE_TARGET 0.25] + `_WAVECLEAR_RANGE_MULT` 3-tier shove-safety [GLOBAL 1.1 / RANGED 1.0 / MELEE 0.7] + `_WAVECLEAR_CONDITIONAL_PROB=0.5` + `compute_waveclear` -> `WaveclearResult`. Each mechanism carries a 0..1 magnitude (share of the wave it removes); `waveclear_score` = sum of kind_weight * range_mult * magnitude (conditional credited at the midpoint), `top_kind` labels the strongest mechanism, `ranged_shove` flags any RANGED/GLOBAL clear (the safe-shove signal a tempo consumer keys on). NEW `/waveclear` route (POST+GET). PURELY ADDITIVE - reads no scorer, read by none, so every existing route is BYTE-IDENTICAL. EXHAUSTIVE 171-champ fan-out (10 classify + 10 completeness critics) -> 376 entries / 171 champions (full roster) / 27 conditional across kinds MULTI_HIT 173 / SINGLE_TARGET 94 / FULL_AOE 62 / AOE_DOT 31 / CLEAVE_AA 16 + bands RANGED 196 / MELEE 176 / GLOBAL 4. + `tools/ds_waveclear_build.py` (reusable validate/inject generator) + `waveclear_registry_notes.json` provenance sidecar (Share-excluded). Live score: Zyra 2.12 / Ziggs 1.82 / AurelionSol 1.80 / Heimerdinger 1.77 / Brand 1.41 (top shovers) vs Vayne 0.03 / Warwick 0.03 / Yuumi 0.05 (no AoE); a champion with no clear is 0. +28 tests `test_waveclear_item300.py`. NEXT (Phase D): an auto-pairing consumer crediting wave-clear into a live lane-priority / "shove and roam" / draft tempo verdict; tune the weights + range multipliers after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.111.0 (item 299): Scaling / power-curve scorer - the TENTH SCORED axis and the ONLY one that models the TIME dimension (every other scored axis is a static now-snapshot of one capability). A champion's power TRAJECTORY across the game - weak-now-hypercarry-late (Veigar/Kassadin/Vayne/Nasus/Kayle) vs early-lane-bully-falls-off (Renekton/Draven/Pantheon/LeeSin/Elise). NEW `agents/daemon_slayer/scaling.py`: a per-(champion, source) `ScalingEntry` registry (source = P/Q/W/E/R or BASE) + `_SCALING_KIND_WEIGHT` 5-tier [INFINITE_STACK 1.0 / FORM_SPIKE 0.8 / RATIO_HYPERSCALE 0.8 / ITEM_RELIANT 0.5 / EARLY_FRONTLOAD 0.4] + per-kind early/mid/late ramp `_SCALING_STAGE_RAMP` (stack/ratio/item ramps up to 1.0 late, FORM_SPIKE flat once unlocked, EARLY_FRONTLOAD decays) + `_SCALING_CONDITIONAL_PROB=0.5` + `compute_scaling` -> `ScalingResult`. Each mechanism tags the `online_stage` it keys on (`_stage_contribution` returns 0 for any earlier stage) + a 0..1 magnitude, folded into early_power/mid_power/late_power; `scaling_score`=late_power (higher = stronger end-state), `scaling_slope`=late_power-early_power (signed trajectory: + scales up, - falls off). NEW `/scaling` route (POST+GET). PURELY ADDITIVE - reads no scorer, read by none, so every existing route is BYTE-IDENTICAL. EXHAUSTIVE 171-champ fan-out (10 classify + 10 completeness critics) -> 258 entries / 171 champions (full roster) / 42 conditional across kinds EARLY_FRONTLOAD 68 / FORM_SPIKE 62 / RATIO_HYPERSCALE 62 / ITEM_RELIANT 50 / INFINITE_STACK 16. + `tools/ds_scaling_build.py` (reusable validate/inject generator) + `scaling_registry_notes.json` provenance sidecar (Share-excluded). Live slope: Vladimir +1.84 / Karthus +1.40 / Nasus +1.305 / Kayle +1.01 (scale up) vs Draven -0.29 / Renekton/Pantheon/LeeSin/Elise -0.27 (fall off); a champion with no scaling identity is 0. +28 tests `test_scaling_item299.py`. NEXT (Phase D): an auto-pairing consumer crediting scaling-mismatch into a draft / "play for late" / tempo verdict; tune the ramp shapes + weights after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.110.0 (item 298): Sustain / vamp-throughput scorer - the NINTH SCORED axis. How much effective HP a champion CLAWS BACK during a fight by converting damage to health (lifesteal / omnivamp / spellvamp / channelled drains) plus self HP-regen steroids. NEW `agents/daemon_slayer/sustain.py`: `SustainEntry` registry + `_SUSTAIN_KIND_WEIGHT` 5-tier [OMNIVAMP 1.0 / LIFESTEAL 0.8 / DRAIN 0.8 / SPELLVAMP 0.6 / REGEN 0.4] + `compute_sustain` -> `SustainResult` (sustain_score / conditional_sustain_score at the 0.5 midpoint / total_sustain_score / raw_sustain_units), normalised to effective-HP units (1.0 unit = 300 HP recovered over a fight): a vamp kind = vamp_pct * 2000 HP, a REGEN steroid = pct_max_hp * 2200 + flat_hp. Distinct from the healing-throughput axis (`ability_hps` + `_passive_heal_overrides`, which own flat / ratio ABILITY heals); this owns the damage-conversion + self-regen attrition the heal axis never measured. NEW `/sustain` route (POST+GET). PURELY ADDITIVE. EXHAUSTIVE 171-champ fan-out (10 classify + 10 completeness critics) -> 56 entries / 45 champions / 25 REGEN / 37 conditional. + `tools/ds_sustain_build.py` + `sustain_registry_notes.json` (Share-excluded). Live `/sustain`: Warwick 11.67 (top) / Aatrox 6.99 / Nasus 1.28 / Karthus 0. +20 tests `test_sustain_item298.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.109.0 (item 297): Mobility / gap-close / kiting scorer - the EIGHTH SCORED axis. How much a champion can REPOSITION her own body (dashes / blinks / leaps / movement-speed steroids / brief untargetable hops) - the gap-close-and-kite dimension no prior scorer measured. NEW `agents/daemon_slayer/mobility.py`: `MobilityEntry` registry + `_MOBILITY_KIND_WEIGHT` 5-tier [BLINK 1.0 / DASH 0.8 / LEAP 0.8 / UNTARGET_REPOSITION 0.7 / MS_STEROID 0.5] + `compute_mobility` -> `MobilityResult` (mobility_score / conditional_mobility_score at the 0.5 midpoint / total_mobility_score / raw_mobility_units), normalised to Flash-units (1.0 unit = 400 units of instant displacement; a single displacement capped at 1500 so a global teleport does not dwarf the axis; a sustained MS buff credited over a 3.0s window). NEW `/mobility` route (POST+GET). PURELY ADDITIVE. EXHAUSTIVE 171-champ fan-out (10 classify + 10 completeness critics) -> 254 entries / 150 champions / 105 MS steroids / 130 conditional. + `tools/ds_mobility_build.py` + `mobility_registry_notes.json` (Share-excluded). Live `/mobility`: Rammus 7.42 (Powerball tops the roster) / Aatrox 3.075 / Karthus 0. +22 tests `test_mobility_item297.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.108.0 (item 296): Meraki content-freshness guard - source-adoption WIN 2. The Meraki `latest` champions endpoint is mutable but its CONTENT is frozen at a past game patch; the snapshot `fetched_at` reflects the FETCH wall-clock and lies about data age. `tools/daemon_slayer_abilities_extract.py` now surfaces the honest signal: `_meraki_content_patch()` takes the newest per-record `patchLastChanged` (YY.MM, numeric-sorted so 25.15 > 25.9) and stamps it as `meraki_content_patch` on `champion_abilities.json`; a pinned `_EXPECTED_MERAKI_CONTENT_PATCH` trips a loud WARNING to re-pin + re-validate ability ratios + gold/golden tests when Meraki refreshes. `tools/daemon_slayer_extract.py` propagates that content patch into `manifest.json` `meraki_items.content_patch`. Regenerated `champion_abilities.json`: content patch 25.15, DATA + COVERAGE byte-identical to the prior snapshot (171 champions / 927 forms) - the `latest` content is genuinely frozen, so zero ability-ratio change and zero engine-behavior risk; only the provenance field was added. The gold/golden tests were already patch-pinned (gold-efficiency pins a vendored snapshot + derives expected at runtime; golden-e2e hand-derives from first principles), so the gold-pin step was a verified no-op. Also closed a prior-tier completion gap: a route test hardcoded a flat-windup combo duration that the 1.107.0 AA-windup offset tier shifted; replaced with the structural clock-end identity (`duration_s == last action t + its cast_time`) so it is robust to windup drift. +12 freshness/manifest tests. Long-term re-source of ability ratios from Data Dragon / CommunityDragon remains future work. Source data: Meraki Analytics (frozen `latest`) / Riot Data Dragon 16.11.1.**
- **ENGINE 1.107.0 (item 295): AA-windup OFFSET TIER - source-adoption WIN 1. The `wiki_stats` sidecar left 110 champions on the flat 0.25s AA-windup default; 109 are now recovered from the wiki's published `attack_delay_offset` via `windup_fraction = 0.300 + offset`, stored as seconds at base AS `(0.300 + offset) / as_base` (combo.py is a FIXED-windup model). Fraction validated 4/4 EXACT vs the wiki published Windup% (Caitlyn/Ashe/Vayne/Jinx). `tools/daemon_slayer_wiki_stats_extract.py` parses `attack_delay_offset` + `as_base` from the same ChampionData raw block (signed parse - the unsigned scalar regex dropped the leading minus most offsets carry) and adds a `wiki_offset` provenance tier in `_merge_fill` between cdragon and the flat default. Regenerated `wiki_stats.json`: `_offset_cast_fills` 109 / `_default_cast_fills` 1 (only Alistar has neither an explicit cast time nor an offset) / `_with_cast_measured` 61 -> 170. Consumers byte-identical: `combo.py` + `data_loader.py` already prefer any positive sidecar `attack_cast_time`, so the 109 per-champ windups flow with no engine-code change. +6 offset-tier tests; a data-fragile combo-timeline pin replaced with a computed cast-time invariant. Source data: community wiki (Module:ChampionData) / CommunityDragon / Riot Data Dragon 16.11.1.**
- **ENGINE 1.106.0 (item 294): Offensive CC-OUTPUT / lockdown scorer - the SEVENTH SCORED axis and the MIRROR of the survivability CC axes (item 290 champion CC-mitigation / item 292 spell-shield): those score how much enemy CC a champion SURVIVES, this scores how much CC she APPLIES to enemies as a first-class offensive-utility metric. The per-spell CC DURATION substrate (`_PER_SPELL_CC_DURATIONS`, consumed defensively by `cc_pressure`) already existed; durations alone do not encode lockdown VALUE (a 1.5s suppression != a 1.5s slow), so this lift adds the missing schema: a NEW hand-authored per-(champion, spell) CC-KIND registry + a kind -> weight table. NEW `agents/daemon_slayer/cc_output.py` (`CcOutputEntry` registry + `_CC_KIND_WEIGHT` 4-tier table [1.0 hard-disable stun/airborne/charm/fear/taunt/sleep/stasis/polymorph/suppression; 0.6 action-restrict root/silence/ground/disarm/blind; 0.5 displacement knockback/pull; 0.2 soft slow/nearsight/cripple] + `_CC_OUTPUT_CONDITIONAL_PROB=0.5` availability midpoint + `compute_cc_output` -> `CcOutputResult` [lockdown_score / conditional_lockdown_score / total_lockdown_score / total_cc_seconds]). NEW `/cc-output` route. PURELY ADDITIVE - the new scorer reads no existing scorer and is read by none, so every existing route is BYTE-IDENTICAL (the opt-in is the new endpoint itself). CC durations reused VERBATIM from `_PER_SPELL_CC_DURATIONS` wherever it carries the spell (108 reconciled, single source of truth); slows + conditional CC it excluded are hand-authored from verbatim patch-16.11 prose. EXHAUSTIVE 171-champ fan-out (12 parallel classify agents + 12 completeness critics) -> 315 entries / 161 champions / 71 conditional across 18 CC kinds. + `tools/ds_cc_output_build.py` (reusable validate/reconcile/inject generator) + `cc_output_registry_notes.json` provenance sidecar (Share-excluded). Live `/cc-output`: Leona 3.05 / Amumu 3.4 / Malzahar 3.7 / Annie cond 2.5. +21 tests `test_cc_output_item294.py`. NEXT (Phase D): an auto-pairing consumer that credits a champion's lockdown into a team-fight/draft verdict; a per-target-tenacity output discount; tune weights + midpoint after a live re-rank. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.105.0 (item 293): GAP-2 effects-text GUARANTEED-SURVIVAL WINDOW registry - the NINTH survivability axis and the SECOND EHP-NUMERATOR term (after the item-288 revive). A SELF window where the champion cannot be damaged/killed (untargetable/stasis/invuln) voids ALL incoming damage for `window_s`, so it adds the avoided `window_s / _FIGHT_WINDOW_S` (6.0s) damage FRACTION to the EHP numerator - the operator-chosen BOUNDED additive model (the revive shape), capped at the whole fight + amortized by availability, NOT a divergent uptime model. This CLOSES the "guaranteed-survival window (option 2)" that items 288/290/292 repeatedly flagged as the remaining unmodeled seam ("a cannot-be-hit/cannot-die window = infinite EHP for its duration"); the additive fraction keeps it FINITE. NEW `_passive_survival_window_overrides.py` (`SurvivalWindowEntry` + `survival_window_multiplier` + 2 midpoints `_SURVIVAL_WINDOW_ULT_PROB=0.35` / `_SURVIVAL_WINDOW_BASIC_PROB=0.5`). NEW `compute_ehp(apply_survival_window=False)` default-off byte-identical, folded into the same numerator step as the revive (`combined_revive *= survival_window_mult`) + `EhpResult.survival_window_mult` + to_dict; threaded `rank_items_by_ehp` + `compute_hybrid` + `rank_items_by_hybrid` + `/ehp` `/rank-tank` `/hybrid` `/rank-bruiser`. Uniform numerator multiplier -> re-ranks NOTHING (like the revive). EXHAUSTIVE 171-champ scan -> 10 self windows: 6 ults (Tryndamere R 5s / Kindred R 4s / Taric R 2.5s self / Kayle R 2.5s self / Lissandra R 2.5s self / Xayah R 1.5s) + 4 basics (Vladimir W 2s / Elise E Rappel 1.95s / Fizz E 0.75s / Mel W 0.75s). EXCLUSIONS: cast-bound OFFENSIVE dashes (Zed R/Camille R/Master Yi Q/Maokai W/Galio R/Sion R), pets/clones (Shaco/Wukong/Azir/Elise W/Caitlyn/Lulu/Neeko), post-death frenzy (Sion P/Karthus P/KogMaw P - item 288 class), revive-domain (Zac P/Zilean R/Kayn P), ally/target/enemy-applied (Kalista R/TahmKench R/Urgot R/Poppy R/Bard R), conditional/positional invuln (Xin Zhao R/Pantheon E/Gwen W), sustained attach (Yuumi W), windup-only stasis (Ekko R/Ryze R/Yone E). Live `/ehp`: Tryndamere L18 blended 4829.16 (swm 1.0) off -> 6237.66 (swm 1.2917) on, ratio 1.2917 = 1+(5/6)*0.35; Caitlyn control byte-identical. +30 tests `test_passive_survival_window_overrides_item293.py`. Default-on flip = Phase D. The clean SELF survivability schema-lift lane (heal/shield/DR/resist/revive + ally + CC-mitigation + spell-shield + survival-window) is now EXHAUSTED across both EHP scorers. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.104.0 (item 292): GAP-2 effects-text SPELL-SHIELD / block-one CC registry - the EIGHTH survivability axis and the SECOND that feeds the cc_blended discount (not an EHP numerator/denominator term). Item 290's tenacity (SEVENTH axis) scales the DURATION of every eaten CC; a spell-shield instead negates ONE incoming CC INSTANCE entirely (cooldown-availability-gated). NEW `_champion_spell_shield_overrides.py` (`SpellShieldEntry` + `champion_spell_shield_fraction` + 3 midpoints `_SPELL_SHIELD_REACTIVE_PROB=0.2` / `_SPELL_SHIELD_DURATION_PROB=0.25` / `_SPELL_SHIELD_PARRY_PROB=0.12`). SEPARATE seam from tenacity: the block fraction is its OWN multiplicative discount on the POST-tenacity pressure (`cc_total *= 1 - frac`), NOT the `effective_cc_duration` tenacity seam, under its OWN `compute_ehp(apply_spell_shield=False)` flag + `EhpResult.spell_shield_frac`; threaded `rank_items_by_ehp` + `compute_hybrid` + `rank_items_by_hybrid` + `/ehp` `/rank-tank` `/hybrid` `/rank-bruiser`. EXHAUSTIVE 171-champ scan -> 4 self entries: Sivir E + Nocturne W (reactive 1.5s block-the-next-effect), Fiora W (sub-second parry), Morgana E self-cast (5s magic-gated CC-immunity shield). EXCLUSIONS: champion-innate tenacity/immunity (item 290), cast-bound engage immunity (Galio R + the 290 dash set), area/projectile walls (Yasuo W/Shen W), untargetable/invuln/stasis (the item-293 survival-window axis), Morgana E ally cast (ally-grant domain item 289). Live `/ehp`: Sivir vs [Leona] cc_press 3.25 -> 2.6 (x0.8), spell_shield_frac 0.0 -> 0.200; Ashe control unchanged. +18 tests `test_champion_spell_shield_item292.py`. Default-on flip = Phase D. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.103.0 (item 290): GAP-2 effects-text CHAMPION-INNATE CC-MITIGATION registry - the SEVENTH survivability axis and the FIRST that is NOT an EHP term at all (it feeds the cc_blended discount, not the EHP numerator/denominator). The six prior axes (heal/shield throughput, DR denominator mult, resist-stat denominator add, revive numerator mult - all SELF - plus the ally resist+revive grants) all move Effective HP. THIS axis moves the OTHER lever: `cc_blended_ehp` discounts EHP by the enemy CC the caster eats; a champion with an INNATE tenacity / CC-immunity ability eats LESS, so her CC-adjusted EHP is discounted LESS. The clean CC-survival HALF of the "guaranteed-survival" family the ally-grant (item 289) + DR registries flagged as excluded (a tenacity is a finite DURATION scale; the damage-immunity half = invuln/stasis = infinite EHP stays a different unmodeled seam). NEW `_champion_cc_mitigation_overrides.py` (`CcMitigationEntry` + `champion_cc_tenacity_fraction`) is the champion-ability sibling of the two tenacity sources cc_blended already credits (`_item_tenacity` + the ARAM `_TENACITY_MAP`), combined MULTIPLICATIVELY with the item source on the same `effective_cc_duration` seam. `compute_ehp(apply_champion_tenacity=False)` seam + `EhpResult.champion_tenacity_frac`; default-off byte-identical, threaded through `rank_items_by_ehp` + `compute_hybrid` + `rank_items_by_hybrid` + the `/ehp` `/rank-tank` `/hybrid` `/rank-bruiser` routes. EXHAUSTIVE 171-champ scan -> exactly 3 self entries: Garen W (60% tenacity 0.75s, brief-window midpoint), Olaf R (100% CC immunity 3s active, active-ult midpoint `_CC_IMMUNITY_ACTIVE_PROB=0.3`), Malzahar P (100% CC immunity passive, `_CC_IMMUNITY_PASSIVE_PROB=0.4`). EXCLUSIONS: cast-bound dash/channel immunity (Sion R/Warwick R/Pantheon R/Kled R+P/Galio R/Briar R - offensive engage window), spell-shields (Fiora W/Morgana E - block-one sub-axis), ally-targeted (Milio R - ally-grant domain), one-time cleanse (Kled P/Alistar R; Alistar R's 45/55/65% DR is the `_passive_mitigation` axis), Bard R false positive (immune TARGETS). +15 tests `test_champion_cc_mitigation_item290.py`. Live `/ehp`: Olaf vs a heavy-CC comp cc_blended_ehp rises flag-on; flag-off byte-identical. Default-on flip = Phase D. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.102.0 (item 289): GAP-2 effects-text ALLY-TARGETED survivability grant registry - the SIXTH survivability axis and the FIRST that scores a DIFFERENT champion than the caster. The five SELF axes (heal/shield throughput, DR denominator mult, resist-stat denominator add, revive numerator mult) all raise the CASTER's EHP; an ALLY-TARGETED grant rides a TEAMMATE - the value is added to the PROTECTED ALLY's EHP. The "grant rides an ally, not the caster - the Orianna-E ball-attached class" the item-264..272 resist + item-288 revive registries both flagged. NEW `_passive_ally_grant_overrides.py` (`AllyGrantEntry` + `ally_resist_grant` + `ally_revive_multiplier` + `_ALLY_REVIVE_PROB=0.3`) seeds 4: Orianna E (6/12/18/24/30 ally armor+MR by E rank), Braum W (20-40 ally base by W rank), Taric W (6-10% of GRANTER total armor, ARMOR ONLY, percent-of-granter), Renata W (ally revived to 100% max health -> numerator mult). GENERIC consumer seam: `compute_ehp(external_resist_armor=, external_resist_mr=, external_revive_multiplier=)` default 0.0/0.0/1.0 byte-identical, route-reachable on `/ehp`. `EhpResult` += `ally_grant_armor`/`_mr`/`_revive_mult`. EXCLUSIONS: ally shields/heals (=ability_hps), ally invuln/untargetable windows (Taric R/Kindred R/Kayle R/Tahm Kench R/etc. - a guaranteed-survival window NOT a finite EHP mult), flat-heal ally revives needing ally max HP (Zilean R/Akshan W deferred to Phase D), Ornn P false positive. Auto-pairing (which live ally <- which granter) is the Phase D consumer job. Live `/ehp`: Caitlyn L18 phys 4963.53 -> Orianna +30/+30 + Renata x1.30 phys 7388.20. +28 tests `test_passive_ally_grant_overrides_item289.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.101.0 (item 288): GAP-2 effects-text REVIVE / second-life registry - the FIFTH survivability axis and the FIRST EHP-NUMERATOR term (heals/shields are throughput; flat-% DR divides the EHP denominator; a resist-stat grant raises the armor/MR denominator). A REVIVE is a death-triggered SECOND HP POOL worth `(1 + revived_fraction)` of the single-life EHP when up. This is the "different non-EHP-denominator seam" item 272 flagged for Anivia P (its -40:20 egg armor/MR stays the item-272 resist-axis EXCLUSION; this models the REVIVE, not the egg resist). NEW `_passive_revive_overrides.py` (`PassiveReviveEntry` + `revive_multiplier`) + `compute_ehp(apply_passive_revive=False)` seam multiplies the per-type EHP NUMERATOR by `1 + revived_hp_fraction(level) * _REVIVE_PROB`, applied BEFORE `blended_ehp` so blend + cc_blended inherit it. The revived FRACTION is EXACT from ability text; only the availability+survival midpoint (`_REVIVE_PROB=0.4` - long CD + must-survive-the-resurrection-window) is the assumption (Phase D feeds live passive-CD + egg survival). EXHAUSTIVE roster scan (death-triggered self-revive restoring a sustained HP pool): exactly Anivia P Rebirth (restores ALL health, 240s CD -> fraction 1.0 -> x1.40) + Zac P Cell Division (10:50% by level, 300s CD, bloblet-gated -> level_scaled -> x1.04 L1 to x1.20 L18). EXCLUSIONS: post-death decaying frenzy no sustained pool (Sion/Karthus/KogMaw P), ally-targeted revive (Zilean R / Renata W / Akshan W), GA item revive. Build-independent numerator multiplier (uniform scale) so it does NOT re-rank an item-ranker. Threaded through `rank_items_by_ehp` + `rank_items_by_hybrid` + `compute_hybrid` + the `/ehp` `/rank-tank` `/hybrid` `/rank-bruiser` routes. Live `/ehp`: Anivia L11 blended 2011.42 off -> 2815.98 on (x1.40). DEFAULT `apply_passive_revive=False` byte-identical. +25 tests `test_passive_revive_overrides_item288.py`. Source data: Riot Data Dragon / CommunityDragon 16.11.1.**
- **ENGINE 1.100.0 (item 278): Passive-damage cadence routing consumer (DS gaps roadmap section 2 "the gated piece"). The passive-damage registry tagged every entry with a cadence string that was metadata-only - the injected P-form synthetic block was inert in EVERY scorer (`compute_ability_dps` skips the P slot, `compute_dps` never read the registry), so `apply_passive_damage` changed no ranking anywhere. NEW: `compute_dps(apply_passive_damage=False)` opt-in seam (default-off byte-identical) routes an allowlisted every-AA `on_hit` passive's per-hit bonus onto the auto-attack cadence - evaluated through the canonical `to_damage_block` + `_evaluate_block` + `AbilityContext.from_build` path, mitigated by the entry's `damage_type` (`_armor_factor` shared resistance curve + `magic_amp`), folded into `per_attack_on_hit_damage` AND the steady DPS (`per_hit * eff_as`) across every phase. NEW `_AA_ROUTED_ON_HIT_KEYS` allowlist + `aa_routed_on_hit_entry()` in `_passive_damage_overrides.py`. v1 routes ONLY the verified every-AA entries (Warwick Eternal Hunger, Orianna Clockwork Winding); the mark-consume (Lux), internal-CD (Ziggs), and empowered-first-hit (Akali / Kha'Zix) `on_hit` entries are NOT routed (their amortization needs the structured cadence + a live re-rank check) and carry forward inert. `/dps` route gains the `apply_passive_damage` body param. The default-on flip stays gated. ALSO: cc-conditional consumer parity resolved (DS gaps roadmap section 3) - `get_total_conditional_cc_seconds` documented raw-by-design (does NOT apply the coexistence MAX-rule the live pressure consumer applies) + a parity test pins the intended divergence; no computed value changed there. +19 tests (`test_passive_damage_aa_cadence.py` + `test_cc_conditional_consumer_parity.py`). DEFAULT `apply_passive_damage=False` byte-identical. Source data: Riot Data Dragon / Meraki Analytics 16.11.1.**
- **ENGINE 1.99.0 (item 272): GAP-2 PER-STACK UNBOUNDED RESIST-STAT grant - the last cleanly headless-buildable resist-grant mode. A self bonus-armor grant that scales LINEARLY with a slow game-long accumulator (Thresh souls) with NO cap, so unlike the BOUNDED per-stack cases (Garen W cap 30/30, Graves E cap 8, Wukong P cap 5 - all seeded "at the cap") it cannot be capped; it needs an assumed steady-state count. NEW per-stack seam on `PassiveResistEntry`: `per_stack_armor` / `per_stack_mr` * `assumed_stacks` (`_ASSUMED_SOUL_COUNT=25`, the item-249 `assumed_stacks` convention on the EHP seam) summed alongside the flat-add + percent halves; default 0.0 -> flat/percent entries unchanged; NO new EHP threading. SEEDED 1: Thresh P Damnation (1 bonus armor per soul, ARMOR ONLY - the +1 AP per soul is offensive; no MR; permanent; Thresh's innate "armor does not grow per level" makes souls his ONLY armor scaling). Exhaustive roster scan: the SOLE per-stack-UNBOUNDED self-resist grant (every other per-stack resist is BOUNDED). The effects-text RESIST-grant lane is now COMPLETE across all 6 source modes (flat / rank-scaled-block / percent-of-resist / unlabeled-multi-stat-block / form-occupancy / per-stack-unbounded); the 2 remaining exclusions (Anivia P resurrection, Orianna E ball-attached) each need a different seam. DEFAULT `apply_passive_resist=False` byte-identical. +19 tests `test_passive_resist_per_stack_item272.py`. Source data: Riot Data Dragon / Meraki Analytics 16.11.1.**
- **ENGINE 1.98.0 (item 271): GAP-2 FORM-OCCUPANCY-gated RESIST-STAT grant - the last clean headless resist-grant mode. A self bonus armor/MR grant present ONLY in one stance of a 2-form toggle; the other stance has ZERO of it, so unlike K'Sante All Out / Kayn R (gate-independent base) it cannot be seeded gate-independently. Amortized by `_FORM_OCCUPANCY_PROB=0.5` (a roughly even 2-form split), reusing `conditional_probability` - NO new schema field. SEEDED 1: Jayce R Transform Mercury Hammer (5/15/25/35 by-level armor==MR via `_step_per_level`, Hammer-stance only; +7.5% bonus-AD sub-term omitted; Cannon-stance R only shreds the target). Exhaustive roster scan: the SOLE form-gated self flat-resist grant. The effects-text RESIST-grant lane is now COMPLETE across all 5 source modes (flat / rank-scaled-block / percent-of-resist / unlabeled-multi-stat-block / form-occupancy); the 3 remaining exclusions (Anivia P resurrection, Thresh P per-stack, Orianna E ball-attached) each need a different seam. DEFAULT `apply_passive_resist=False` byte-identical. +24 tests `test_passive_resist_form_gated_item271.py`. Source data: Riot Data Dragon / Meraki Analytics 16.11.1.**
- **ENGINE 1.94.0 (item 265): Lane A 1v1 matchup engine (`matchup.py` `compute_matchup`, NEW `/v2/matchup` route). The deterministic trade/all-in/back-off/even verdict that substitutes for an LLM trade judgment - composes the existing pure scorers (burst into the opposing defences + ehp + mana-gate) into a net-HP-swing + verdict. Additive: no existing scorer output changes. Verdict thresholds `_ALL_IN_KILL_THRESHOLD=1.0 _TRADE_MARGIN=0.10 _EVEN_BAND=0.05`. +13 tests `test_matchup_lane_a.py` (mirror=even, symmetry negates net_swing, level/item advantage, monotone-in-level). Powers the off-engine precompute lanes (build-order tables + laning verdict adapter).**
- **ENGINE 1.93.0 (item 264): GAP-2 effects-text RESIST-STAT grant registry (`_passive_resist_overrides.py`, `compute_ehp(apply_passive_resist=...)`). The FOURTH survivability axis after heal throughput / shield throughput / damage-reduction denominator. A resist grant is bonus armor/MR from an ability or innate passive that is NOT in the resolved stat block; it raises the armor/MR denominator DIRECTLY (added before `_armor_factor`), the axis item 261 excluded from the DR registry as "a DIFFERENT axis than a damage multiplier". Threaded through both EHP-bearing scorers (`rank_items_by_ehp` + `/ehp` + `/rank-tank` + `compute_hybrid` + `rank_items_by_hybrid` + `/hybrid` + `/rank-bruiser`). SEEDED 6 (4 permanent + 2 active amortized): Garen W 30/30, Wukong P 6:10 armor, Shyvana P 5/5, Sejuani P 10/10, Gwen W 22/22, Pantheon E 5:30. KEY FINDING: a flat resist add goes through the non-linear `_armor_factor`, so unlike item 261's DR (uniform scale, rank-invariant) it is NOT a uniform EHP scale -> the item-rankers CAN re-rank flag-on. DEFAULT `apply_passive_resist=False` byte-identical. +30 tests `test_passive_resist_overrides_item264.py`.**
- **ENGINE 1.79.0 (item 247 / gap-plan Phase C2): AA-empowerment amp seam in `compute_dps`. The 5 `base="aa"` AmpEntry champs (Caitlyn W / Fiora E / Jayce W f1 / Sivir W / Nidalee Q), registered item 239 but with NO consumer, are now wired into the AA scorer: `compute_dps(apply_ability_amps=False default = byte-identical)`; when True NEW `_aa_amp_multiplier` scales ONLY the base-AA component (item proc DPS unamped), threaded `compute_dps -> _phase_weighted_dps -> _rotation_attack_dps` (`aa_empower_amp`) + the per-hit display values; route-reachable via the `/dps` server route. `rank_at_level` imported function-level (dps <-> ability_dps cycle break). FORWARD-MARKER: the 5 entries are placeholder `(0.0,)` so the seam is INERT today - wired + route-reachable + byte-identical; Phase D authors the amortized per-champ value + flips `always_on` / a condition live. +8 tests `test_aa_empower_seam_item247.py`.**
- **ENGINE 1.78.0 (item 247 / gap-plan Phase C3): GAP-2 exotic passive registry, 6 of 8 authored default-OFF in `_passive_damage_overrides.py` from verbatim 16.11.1 effects_descriptions. Additive schema: NEW `_step_per_level` (3-tier even-thirds level step for slash-notation); PassiveDamageEntry scaling fields widen to `float | tuple` (per-level coefficient rides target_max_hp_pct / total_ad_pct); `to_damage_block` coerces either + wires target_current_hp_pct. SEEDED: Aatrox P (4%:8% target max HP lerp) / JarvanIV P (8% target current HP flat; min/cap inert in champ band) / Zed P (6/8/10% target max HP step; below-50% fire gate not modeled - magnitude gate-independent) / Caitlyn P (60/90/120% AD step; +crit-chance AD multiplier omitted = AA-crit seam) / Ekko P (30:140 + 90% AP; every-3rd-stack cadence metadata) / Gangplank P (50:250 + 100% bonus AD TRUE over 2.5s dot; +crit term omitted). STAGED own-slice: Gwen P (bilinear AP-on-HP needs a core evaluator term) + Kai'Sa P (per-Plasma-stack ramp needs a stack-count decision). DEFAULT BYTE-IDENTICAL (seam injects only under `apply_passive_damage=True`). +31 tests `test_passive_damage_exotic_item247.py`.**
- **ENGINE 1.77.0 (item 246 / gap-plan Phase C1): staged-amp block-index routing for Hwei Q f2, gated under `apply_ability_amps` (default byte-identical). The 2 STAGED `damage_amp_self` candidates whose "Maximum ..." block already models the charged/isolated ceiling are reconciled by ROUTING the block-index (never an amp - that would double-count): (a) Sion Q was already resolved pre-seam by `champion_block_index.json {Q:2}` (the s191 routing selects "Maximum Physical Damage" by default), STAGED entry removed, flag on/off byte-identical; (b) Hwei Q f2 Severing Bolt now wired via NEW `_STAGED_AMP_BLOCK_ROUTES` -> under the flag routes to damage-block 1 "Maximum Damage" (isolated/immobilized + max-missing-HP ceiling, which Meraki pre-bakes flat == block0 * "Maximum Damage Increase" %, so NO separate missing-HP coefficient needed - the route IS the ceiling). `compute_ability_dps` consults `_staged_amp_block_route_for` ONLY when `apply_ability_amps=True`, precedence over `block_index_overrides` for that (champ,key,form). AurelionSol W stays STAGED (cross-spell seam, deferred). Live :8893 proof: Hwei Q f2 raw 160 (off) -> 560 (on). +10 tests `test_staged_amp_block_route_item246.py`.**
- **ENGINE 1.76.0 (item 243): non-coachable joke/anvil item deny-set in the rank pool. `rank.py` `_NON_COACHABLE_ITEM_IDS = {994403 Golden Spatula, 663064 Veigar's Talisman}` skipped unconditionally in `_filter_candidates` (every mode + scorer). Root: DDragon mis-flags Golden Spatula `maps['12']=True` (ARAM) with a full all-stats block, so the DPS scorer ranked it #2 in a live ARAM Varus build; item 213 stripped it from curated `champion_loadouts.json` but the live rank pool reads the DDragon maps flag directly. +7 tests `test_non_coachable_deny_item243.py`; `test_rank_mode_legality_p1l23` expected-set now subtracts the deny-set.**

> Changelog: newest first, ONE line per ENGINE bump - never append to a prior version's line.

### V2 substrate + wiring (ENGINE 1.64.0 - 1.74.0)

- ENGINE 1.74.0 (item 237) bruiser cc_blended ranking - the symmetric completion: compute_hybrid NEW `apply_build_tenacity:bool=False` (tenacity-accurate hybrid_score scalar) + rank_items_by_hybrid NEW `score_by="blended"`(default,byte-id)|`"cc_blended"` (the delta_pct SORT key now uses the cc_blended-ehp delta over the cc_blended baseline) + `apply_build_tenacity:Optional[bool]=None` tri-state (None -> ON for cc_blended); HybridRankedItem +cc_blended_ehp/+delta_cc_blended_ehp, HybridRankResult +score_by; /rank-bruiser exposes score_by/apply_build_tenacity (400 on bad), /hybrid keeps the plain-bool scalar; DEFAULT byte-identical (Sett vs ["Ashe"] Wit's End blended #24 -> cc_blended #21); the cc_blended/tenacity arc is now COMPLETE (tank + bruiser scorers both tenacity-aware); see `docs/_archive/DS_V2_PLAN.md`
- ENGINE 1.72.0 (item 236) opt-in CC-adjusted EHP ranking: `rank_items_by_ehp` NEW `score_by="blended"`(default,byte-identical)|`"cc_blended"` + `enemy_champions`/`include_conditional` (re-added as LIVE consumers - unlike item 235 which reverted them as inert on the blended ranker) + `apply_build_tenacity:Optional[bool]=None` (tri-state: None -> ON for cc_blended/OFF for blended) ranks tanks on the enemy-CC-lockdown-adjusted EHP; NEW `_item_tenacity.py` per-item tenacity registry (17 items, description-parsed since DDragon strips the stat like ability haste; Anathema's enemy-debuff+Silvermere active+Elixir consumable EXCLUDED) + `total_item_tenacity` League-MULTIPLICATIVE stacking, consumed by `compute_ehp(apply_build_tenacity=False)` which scales the CASTER-eaten enemy CC by (1-build tenacity) via the `effective_cc_duration` seam so a tenacity item (Sterak's/Mercury's) shrinks its own lockdown -> rises under cc_blended (vs a non-saturating CC comp; ultra-heavy comp saturates the 6s cap so no tenacity helps - correct); EhpRankedItem +cc_blended_ehp/+delta_cc_blended_ehp, EhpRankResult +score_by; /rank-tank exposes score_by/enemies/include_conditional/apply_build_tenacity; DEFAULT byte-identical (Malphite vs ["Ashe"] Sterak's blended #5 -> cc_blended #2)
- ENGINE 1.71.0 (item 235) self-audit wiring + parameterize pass threads all 6 opt-in scoring flags END-TO-END into the rank_* wrappers + :8893 routes (apply_mode_modifiers -> rank_items/+/dps + rank_items_by_ehp/+/rank-tank + compute_hybrid+rank_items_by_hybrid/+/hybrid+/rank-bruiser + beam_search_build/+/beam + compute_dps_curve + compute_fight_report; aoe_targets_hit -> rank_items_by_burst/+/rank-assassin+/burst; gate_ammo + apply_ability_haste -> compute_fight_report/+/v2/fight-report -> mana_sim) + wires the previously routeless `enemy_champions` (NEW route arg `enemies`) + `include_conditional` into /ehp +/hybrid +/rank-bruiser so the item-137/143 cc_blended_ehp discount is finally route-reachable (REVERTED from rank_items_by_ehp/+/rank-tank - inert there, EHP ranker deltas on PRE-cc blended_ehp) - ALL OPT-IN/byte-identical at default (full DS suite unchanged); DRY: NEW shared `_item_ability_haste.effective_cooldown(base_cd, ah)` single-source for the Riot base/(1+AH/100) haste-CDR formula (ability_dps._effective_ability_cd + mana_sim now delegate) + total_item_ability_haste delegates per-item to the previously no-consumer item_ability_haste accessor; NEW behavior-neutral GET /modifier-summary diagnostic route surfaces `modifier_blocks.summarize_modifiers` over the live AbilitiesSnapshot (180 modifier blocks bucketed pve_only/target_shred/defensive_self/damage_amp_self/other) - reads only
- ENGINE 1.70.0 (item 234) mana_sim NEW opt-in `apply_ability_haste:bool=False` CDR model -> reduces every cooldown-bearing slot via Riot canonical `base_cd/(1+AH/100)` from `total_item_ability_haste` (same registry the live ability scorer uses), applied uniformly to both passes; byte-identical when off / no-AH; binds only on a cooldown-REPEATING sequence (Lux Q-AA-Q-AA-Q @35 AH cd x0.741 dur 18.25->13.58 dps 56->75); STATIC-CD honest verdict NOT used to gate haste-immunity (QWER coverage 3 abilities with a mislabel - Amumu Q scales with haste - too unreliable); all 6 buckets now RESOLVED (5 wired + static-CD honest-no-consumer + cc_tags operator-CLOSED)
- ENGINE 1.69.0 (item 233) missile travel-time bucket -> NEW `missile.py` `spell_travel_time` (distance/missile_speed; speed bands gate the MIX <400 artifact->None / >=5000 instant->0.0; `_distance_from_geometry` rejects the cone_distance=100/0 CDragon placeholder sentinels + a <200 cast_radius floor, real cast_radius like Lux E 295 exact) consumed by fight_report NEW 7th MISSILE section (APPROXIMATE - default-distance-dominated) + spell_missile_speed accessor; rune target_hp tags Cut Down 8017 target_hp_above / Coup de Grace 8014 target_hp_below METADATA (keystone_amp keeps 1.08 UNCONDITIONAL - burst-window approximation; target_hp_pct kwarg parity; byte-identical); burst role-from-attackrange (>350=ranged) into rune calls so Lethal Tempo 8008 ranged 6-24 vs melee 9-30 (only affects an explicit runes=[8008] call, live /rank passes none); static-CD ability_static_cd accessor DATA-STAGED honest no-consumer (no haste model); flips gate_ammo/apply_mode_modifiers/aoe_targets_hit assessed ALL stay default-OFF (each re-ranks, operator-gated) - 5 of 6 buckets wired + static-CD accessor-staged, cc_tags operator-CLOSED
- ENGINE 1.68.0 (item 232) wires 3 more owed sidecar buckets ALL OPT-IN/byte-identical at default: geometry -> NEW `geometry.py` AoE-multiplier (burst `aoe_targets_hit:int=1`, AoE-shaped ability x min(N,cap) cap 5) + recharge -> NEW `recharge_ledger.py` time-step charge-availability (consumed by fight_report 6th section) + mode_modifiers -> dps/ehp dmg_dealt/dmg_taken MULTIPLIERS for urf/ofa/usb/nb (ARAM keeps legacy lolmath) + ar/swift hp_lvl/dam_lvl/arm_lvl/as_lvl/hp_base/arm_base growth ADDENDS via `engine._scale_champion_base` + `_resolve_mode_addends` (`apply_mode_modifiers:bool=False` opt-in on compute_dps/ehp + build_champion; Arena re-ranks ONLY flag-on); + rune PROC-SIGNATURE LIFT (`RuneProc.condition` + `caster_hp_pct`/`game_time_s`/`role`/`bonus_as` kwargs on compute_rune_proc_damage + keystone_amp; ships Last Stand 8299 stacking_amp caster_hp_below gate amp 1.0 at full HP / Absolute Focus 8233 + Gathering Storm 8236 adaptive stat-grants excluded from burst total - all byte-identical default; Lethal Tempo 8008 role/bonus_as; registry 16 -> 19) - 4 of 6 owed buckets wired, missile + static-CD owed, cc_tags operator-CLOSED
- ENGINE 1.67.0 (item 231) wires item 225's `ammo` charge sidecar (cdragon_spell_stats.json) into `mana_sim` behind opt-in `gate_ammo` (per-slot charge ledger gates no-ammo casts status="no_ammo"; byte-identical when omitted; 1 of 6 owed sidecar buckets) + closes the rune per_attack expansion honestly (DDragon 16.11.1 well DRY - Last Stand 8299 / Absolute Focus 8233 / Gathering Storm 8236 caster-state/game-time-gated, documented exclusions, 0 net-new runes)
- ENGINE 1.66.0 (item 230) `rune_procs` per_attack expansion 14 -> 16 (Hail of Blades 9923 TRUE 4-20+0.08bAD+0.06AP / Lethal Tempo 8008 adaptive 9-30 melee at max stacks; Fleet Footwork 8021 HONEST-EXCLUDED heal+MS sustain only) + `scenario_matrix` mana_bounded_dps + rune_burst metrics + optional `runes` wired into `rank_items_by_burst` + live POST `/rank-assassin` byte-identical when omitted
- `rune_procs` wired into `burst`/`combo` behind an optional `runes` param byte-identical when omitted + NEW `fight_report.py` unified V2 report served at POST `/v2/fight-report` ENGINE 1.65.0
- ability_hps v2 wired into live `ds.hps` ENGINE 1.64.0

### cc_conditional waves (ENGINE 1.40.0 - 1.61.0)

- **wave 23 ENGINE 1.61.0 (item 202 `084cc50`): Hecarim R Onslaught of Shadows distance-gated fear 1.5s primary COND_RANGE_GATED 0.4 coexists_with_unconditional=True (SECOND consumer of wave 7 forward-marker COND_RANGE_GATED tag after Maokai R wave 18; coexists with unconditional Hecarim R 1.0s baseline in `_PER_SPELL_CC_DURATIONS`; FIRST Hecarim cc_conditional entry anywhere); 72 entries (63 primary + 8 sidecar) / 57 champs at 16.10.1; tag count 13 unchanged; +27 tests `test_cc_conditional_wave23.py`; consumer math byte-identical to 1.60.0 for default include_conditional=False; REJECTS Jhin W / Anivia Q vague duration + Heimerdinger E spatial-not-tactical + Lillia R COND_DREAM_STACK still empty + Lissandra W/R + Jinx E unconditional**
- **wave 22 ENGINE 1.60.0 (item 199 `c16c3a2`): Rell W form_index=1 Ferromancy: Mount Up empowered-AA stun sidecar COND_CHANNEL_COMPLETION 0.4 0.6s flat all 5 W ranks (2-cast cycle form 0 -> form 1 within 3.5s -> empowered-AA charge-delivery; FOURTH same-spell-slot multi-form-coexistence sidecar after Karma W 0+1 / Hwei E 0+1+2 / Sylas E 0+1; coexists with wave 15 Rell W form 0 sidecar entry on same (Rell, W) slot via form_index discriminator; mechanic evidence Meraki cast_time=0.25 form 1 vs cast_time=0.625 form 0; the 0.4s fling excluded mirroring form 0's exclusion of its own 0.4s knockup); 71 entries (62 primary + 8 sidecar) / 56 champs at 16.10.1; tag count 13 unchanged; +45 tests `test_cc_conditional_wave22.py`; consumer math byte-identical to 1.59.0 for default include_conditional=False**
- **wave 21 ENGINE 1.59.0 (item 189 `4d60420`): cc_conditional re-audit of prior wave REJECT carries against item 188 `notes` field schema lift; 68/55 -> 70/56 (62 primary + 7 sidecar); +2 primary entries / +1 net-new champion - Xin Zhao becomes 2-slot champion (Q + R landing in same wave); SHIP Xin Zhao Q Three Talon Strike 3rd-attack knockup primary COND_NTH_HIT 0.7 0.75s flat all 5 ranks (FIRST Xin Zhao Q first-order CC anywhere; COND_NTH_HIT docstring's own canonical motivating example) + Xin Zhao R Crescent Sweep target-NOT-Challenged stun primary COND_TARGET_DEBUFFED 0.5 0.75s flat all 3 ranks INVERSE-gated semantic (SECOND INVERSE-gated COND_TARGET_DEBUFFED after Briar R wave 18; corrects wave 15 REJECT carry; FIRST Xin Zhao R first-order CC anywhere); 38 REJECT verdicts (~11 unconditional candidates belonging in `_PER_SPELL_CC_DURATIONS`; rest false-positive regex hits / self-CC / cross-references); +51 tests `test_cc_conditional_wave21.py`; consumer math byte-identical to 1.58.0 for default include_conditional=False**
- **wave 20 ENGINE 1.58.0 (item 188 Slice A `7530002`): Meraki extractor `notes` field schema lift per spell form (purely additive parallel to cast_time/effects_descriptions/parent_resource); re-extracted 16.10.1 (171 champs / 927 forms / 98.8% notes coverage); 67/54 -> 68/55 (61 primary + 7 sidecar); +1 entry / +1 net-new champion = Vayne; SHIP Vayne E Condemn terrain-collision stun 1.5s primary COND_TERRAIN 0.3 coexists_with_unconditional=True (SECOND consumer of COND_TERRAIN after Ornn E wave 15; coexists with 0.5s knockback in `_PER_SPELL_CC_DURATIONS`); 3 schema-lift-verified REJECTS Kindred E (slow 1s + missing-HP crit damage only no hard CC) + Diana P (pure AS bonus no CC) + Vayne P (pure MS bonus no CC); +37 tests `test_cc_conditional_wave20.py`; consumer math byte-identical to 1.57.0 for default include_conditional=False**
- **wave 19 ENGINE 1.56.0 (item 177): Meraki extractor state-tracking schema lift - `tools/daemon_slayer_abilities_extract.py` captures `parent_resource: str | None` per spell form via NEW `_build_form(parent_resource=...)` signature threaded from champion-level `payload["resource"]` (FURY / BLOOD_WELL / FRENZY / RAGE / HEAT / ENERGY / GRIT / MANA); re-extracted 16.10.1 (171 champs / 927 forms / 99.0% ok_rate); 0 net-new registry entries (67/54 unchanged); ALL 6 brief-named wave-7 schema-lift carries verified ALREADY-SHIPPED via prior waves (Renekton W wave 9 frenzy_state + Karma W form 1 wave 10 sidecar + Hwei E form 2 wave 10 + Neeko E wave 1) + 3 REJECT-verified (Aatrox post-R minion-only + Volibear R turret-only + Briar W self-buff); schema lift ships as forward-marker infrastructure for future COND_FRENZY_STATE consumers beyond Renekton W + Karma W + Gnar W; +30 tests; consumer math byte-identical to 1.55.0 for default include_conditional=False**
- **wave 18 ENGINE 1.55.0 (item 176): NEW `coexists_with_unconditional: bool = False` field on ConditionalCcEntry + consumer-side MAX-rule semantics in `compute_cc_pressure()` (when True, conditional entry coexists with same-spell-slot unconditional entry in `_PER_SPELL_CC_DURATIONS`; consumer credits MAX per slot - never double-sums); +2 primary entries / +0 net-new champs Maokai R Nature's Grasp distance-gated root range_gated 0.4 coexists_with_unconditional=True (FIRST consumer of COND_RANGE_GATED forward-marker tag since wave 7; Maokai becomes 2-slot cc_conditional champion - Q wave 2 + R wave 18) + Briar R Certain Death Hematomania impact fear target_debuffed 0.5 (SECOND 3-slot cc_conditional champion after TahmKench wave 13 - Q wave 4 + E wave 5 + R wave 18; FIRST Briar R first-order CC anywhere); 67 entries (60 primary + 7 sidecar) / 54 champs at 16.10.1; per-tag consumer growth COND_RANGE_GATED +1 (first consumer; closes wave 7 schema-lift forward-marker contract) + COND_TARGET_DEBUFFED +1; default include_conditional=False byte-identical to 1.54.0**
- **wave 17 ENGINE 1.54.0 (item 175): COND_TRAVERSE condition tag schema lift (13th tag at midpoint 0.3) + Taliyah E Unweaver's Wall traversal knockup 0.75s primary nth_hit (multi-wave coexistence with Taliyah W wave 1 - Taliyah becomes 2-slot cc_conditional champion); 65 entries (58 primary + 7 sidecar) / 54 champs at 16.10.1; per-tag consumer growth COND_TRAVERSE +1 (first consumer of new tag); default include_conditional=False byte-identical to 1.53.0**
- **wave 16 ENGINE 1.53.0 (item 174): Garen Q Decisive Strike empowered-AA silence 1.5s nth_hit (first Garen first-order CC anywhere) + Syndra E Scatter the Weak Dark-Sphere knockback stun 1.25s target_debuffed (first Syndra first-order CC anywhere) + Udyr E Blazing Stampede empowered-AA pounce stun 0.75s nth_hit (first Udyr first-order CC anywhere) + KSante W Path Maker recast channel_completion stun 1.0s midpoint (0.5-1.75s range; multi-wave coexistence with KSante Q wave 1); 64 entries (57 primary + 7 sidecar) / 54 champs at 16.10.1; per-tag consumer growth COND_NTH_HIT +2 + COND_TARGET_DEBUFFED +1 + COND_CHANNEL_COMPLETION +1; default include_conditional=False byte-identical to 1.52.0**
- **wave 15 ENGINE 1.52.0 (item 173): Zac Q Stretching Strikes 2nd-strike different-target nth_hit root 0.5s + Ornn E Searing Charge terrain-collision stun 1.25s (FIRST consumer of COND_TERRAIN forward-marker tag since wave 7; multi-wave coexistence with wave 4 Ornn Q) + Rell W form_index=0 Ferromancy Crash Down channel_completion stun 0.8s sidecar after 0.625s leap cast_time; 60 entries (53 primary + 7 sidecar) / 51 champs at 16.10.1; per-tag consumer growth COND_NTH_HIT +1 + COND_TERRAIN +1 (first consumer) + COND_CHANNEL_COMPLETION +1; default include_conditional=False byte-identical to 1.51.0**
- **wave 14 ENGINE 1.51.0 (item 172): Meraki extractor cast_time schema lift - `tools/daemon_slayer_abilities_extract.py` captures `cast_time: float | None` per spell form via NEW `_normalize_cast_time(raw)` helper; re-extracted 16.10.1 (171 champs / 927 forms / 99.0% ok_rate); SHIP Jayce E form_index=0 Thundering Blow cast-time root sidecar COND_CHANNEL_COMPLETION 0.25s (FIRST Jayce CC registration anywhere; closes item 170 carry); 57 entries (51 primary + 6 sidecar) / 49 champs at 16.10.1; default include_conditional=False byte-identical to 1.50.0; REJECT Maokai R distance-gated (would double-count existing unconditional entry; same-spell-slot conditional-vs-unconditional coexistence machinery STILL operator-gated) + LeeSin R cast-time 0.25s (unconditional on primary target belongs in `_PER_SPELL_CC_DURATIONS`) + KSante R cast-time 0.4s (self-buff immunity not target CC)**
- **wave 13 ENGINE 1.50.0 (item 171): Sejuani E Permafrost 4-Frost-stack stun nth_hit 1.0s + Renata Q Handshake recast bystander stun channel_completion 0.5s + Shaco R Hallucinate clone-death box fear channel_completion 1.0s + Fizz R Chum the Waters lure-on-champion knockup target_debuffed 1.0s + Warwick E Primal Howl recast fear channel_completion 1.0s (joins wave 0 Warwick R) + TahmKench W Abyssal Dive emerge stun channel_completion 1.0s (FIRST 3-slot cc_conditional champ - W + Q wave 5 + R wave 0) + Aphelios Q form_index=3 Gravitum Binding Eclipse expunge root target_debuffed 1.0s sidecar; 56 entries (51 primary + 5 sidecar) / 48 champs at 16.10.1; per-tag consumer growth COND_NTH_HIT +1 + COND_TARGET_DEBUFFED +2 + COND_CHANNEL_COMPLETION +4; default include_conditional=False byte-identical to 1.49.0**
- **wave 12 ENGINE 1.49.0 (item 170): Singed E Fling Mega-Adhesive overlap root primary target_debuffed (Meraki damage_blocks per-rank Root Duration [1.0/1.25/1.5/1.75/2.0]s; coexists with Singed E unconditional 1.0s knockback in different registry) + Alistar E Trample 5-stack stun primary nth_hit 1.0s (closes ability_dps.py:1225-1228 carryover) + Sylas E form_index=1 Abduct sidecar channel_completion stun 0.5s (first Sylas first-order CC anywhere; closes item 148 wave 7 schema-lift REJECT note); 49 entries (45 primary + 4 sidecar) / 43 champs at 16.10.1; default include_conditional=False byte-identical to 1.48.0**
- **wave 11 ENGINE 1.48.0 (item 156): Sion R Unstoppable Onslaught primary channel_completion stun 1.0s (extends COND_CHANNEL_COMPLETION consumer set) + Gnar W form_index=1 Wallop sidecar Mega-form-gated frenzy_state stun 1.25s (3rd consumer of COND_FRENZY_STATE forward-marker tag); 46 entries (43 primary + 3 sidecar) / 40 champs at 16.10.1; default include_conditional=False byte-identical to 1.47.0**
- **wave 10 ENGINE 1.47.0 (item 154): same-spell-slot sidecar registry schema lift `_PER_SPELL_CC_CONDITIONAL_FORMS: Dict[champion, Dict[(spell, form_index), ConditionalCcEntry]]` parallel to primary registry; ConditionalCcEntry gains optional `form_index`; `get_conditional_entries()` merges default-first then form_index ASC; new per-form override key shape `Champion:Spell:FormIndex`; closes Karma W form_index=1 R-empowered Renewal root (2.35/2.45/2.55/2.65/2.75s + Mantra bonus, COND_FRENZY_STATE 2nd consumer) + Hwei E form_index=2 Gaze of the Abyss root (1.2/1.4/1.6/1.8/2.0s, COND_CHANNEL_COMPLETION); 44 entries (42 primary + 2 sidecar) / 38 champs at 16.10.1; default include_conditional=False byte-identical to 1.46.0**
- **wave 9 ENGINE 1.46.0 (item 153): Meraki extractor schema lift adds `effects_descriptions` field per form record (captures description-text mechanics absent from leveling[]/damage_blocks); Renekton W Ruthless Predator frenzy_state (first consumer of wave 7 forward-marker COND_FRENZY_STATE) + Hwei E form_index=1 Grim Visage Disable + Neeko E Empowered Root = 42 entries / 38 champs at 16.10.1; 4 schema-lift-verified REJECTS Karma W form 1 + Aatrox R + Volibear R + Briar W**
- **wave 8 ENGINE 1.45.0 (item 151): Morgana R Soul Shackles channel_completion + Seraphine E Beat Drop target_debuffed + Evelynn W Allure target_debuffed = 39 entries / 35 champs at 16.10.1**
- **wave 7 ENGINE 1.44.0 (item 148): schema-lift only - 2 NEW condition tags COND_FRENZY_STATE + COND_RANGE_GATED at midpoint 0.4 forward-marker seam; registry unchanged at 36 entries / 32 champs at wave 7 ship time; wave 8 grew to 39 / 35**
- **wave 6 ENGINE 1.43.0: Brand Q target_debuffed (coexists with Brand R wave 1) = 36 entries / 32 champs at 16.10.1**
- **wave 5 ENGINE 1.42.0: Briar E (coexists with Briar Q wave 4) / TahmKench Q (coexists with TahmKench R wave 1)**
- **wave 4 ENGINE 1.41.0: Nunu R / Yuumi Q / Pantheon Q / Aatrox W (FIRST multi-wave coexistence) / Briar Q**
- **wave 3 ENGINE 1.40.0: Aatrox Q3 / Riven Q3 / Yasuo Q3 / Yone Q3 / Leblanc E**
- **waves 0-2 (pre-version seed)** seed 10: Brand R / TwistedFate W / JarvanIV E / TahmKench R / Volibear Q / Warwick R / Viktor W / Mordekaiser R / Sett E / Vex E + wave 1: Bard Q / Karma W / Taliyah W / Kennen E / KSante Q / Ornn Q / Xayah E / Fiora W + wave 2: Maokai Q / Pyke E / Swain E / Skarner Q / Zilean Q

## Module map (`agents/daemon_slayer/`)

| File | Purpose |
|---|---|
| `__init__.py` | `ENGINE_VERSION` constant; `start_server()` entry point |
| `server.py` | Stdlib `ThreadingHTTPServer`; 31 routes - core `/health`, `/snapshot`, `/stats`; the DPS + build rankers `/rank`, `/dps`, `/beam`, `/ehp`; the 6 archetype rankers `/rank-tank`, `/hybrid`, `/rank-bruiser`, `/ability-dps`, `/rank-mage`, `/burst`, `/rank-assassin`, `/hps`, `/rank-enchanter`; the additive scored-axis routes `/anti-tank`, `/extended-duel`, `/sustain`, `/mobility`, `/scaling`, `/waveclear`, `/threat-range`, `/zone-control`, `/objective-damage`, `/cc-output`, `/ally-amp`, `/modifier-summary`; the DSP live-input producer routes `/summoner-fight-adj`, `/enemy-rune-threat`, `/ally-protected-ehp` (OQ18) |
| `effects.py` | Re-export facade (s246 split) - the 14 effect-aggregation logic fns (`collect_effects`, `total_*`, `effective_target_armor/mr`) + full public-surface re-export. Logic only |
| `_effects_types.py` | **s246** - schema types + damage-type constants (`CallContext`, `DamageFn`, `PeriodicProc`, `ItemEffect`, `PHYSICAL/MAGICAL/TRUE`). Zero deps |
| `_effects_data.py` | **s246** - the `ItemEffect` registry: 547 entries, DDragon purchasable coverage COMPLETE. Patch-pinned per `current.txt`; refresh on patch bump |
| `dps.py` | `CallContext` dataclass + `compute_dps()` - stat walk, armor/MR pen, on-hit, periodic procs, damage amps |
| `ehp.py` | **Phase 1 (s174)** - `compute_ehp()` + `EhpResult` + `rank_items_by_ehp()` + `EhpRankResult` - Tank EHP scorer; HP / armor_factor math with caller-supplied AD/AP/true enemy shares; ARAM `aramDamageTaken` modifier folded in |
| `hybrid.py` | **Phase 2 (s175)** - `compute_hybrid()` + `HybridResult` + `rank_items_by_hybrid()` - Bruiser hybrid scorer; composes `compute_dps` × `compute_ehp` weighted by per-champion (α,β) from `archetype_weights.json`; normalized-percentage-delta sort keeps weights intuitive across the ~10× DPS/EHP magnitude gap |
| `abilities.py` | **Phase 4a (s177)** - `AbilitiesSnapshot.load()` + `AbilityForm` + `DamageBlock` + `load_default()` singleton - champion ability data loader for the Meraki bulk ingest at `data/daemon_slayer/<patch>/champion_abilities.json`. 171/172 champions × P/Q/W/E/R (multi-form preserved); per-rank `cooldown`/`cost`/`damage_blocks[]` with typed scaling fields (`base`, `total_ad_pct`, `bonus_ad_pct`, `ap_pct`, `caster_max_hp_pct`, `caster_bonus_hp_pct`, target HP family, `target_armor_pct`, `bonus_armor_pct`, `bonus_mr_pct`, `caster_max_mp_pct`) |
| `ability_dps.py` | **Phase 4b + 4c (s178/s179)** - `compute_ability_dps()` + `AbilityDpsResult` + `AbilitySpellDps` + `AbilityContext` (4b evaluator) **plus** `rank_items_by_ability_dps()` + `AbilityDpsRankedItem` + `AbilityDpsRankResult` (4c ranker). Per-spell evaluator resolves Q/W/E/R at the canonical rank-at-level for the operator's max-priority order (Q-first default, configurable); sums first damage block's per-rank scaling × resolved caster context; applies mode multiplier + AP cross-derivations + damage amps + per-spell magic_amp on magic-typed spells; routes mitigation per damage type. Multiplies post-mitigation damage by measured `casts/sec` from `cast_rates.get_spell_casts_per_sec` (rewind-derived); falls back to `1/cooldown × mana_uptime` when no data. Ranker uses the same `_filter_candidates` pipeline as `rank_items` / `rank_items_by_hybrid` (purchasable + mode-legal + budget + terminal-only + dead-unique dedup) and scores each candidate by total-ability-DPS delta over the baseline. Sort keys: `delta` (raw) + `efficiency` (per 1k gold). `/ability-dps` + `/rank-mage` routes on `:8893` |
| `burst.py` | **Phase 5 (s180)** - `compute_burst_damage()` + `BurstResult` + `ComboCast` (per-combo evaluator) **plus** `rank_items_by_burst()` + `BurstRankedItem` + `BurstRankResult` (assassin ranker). Walks a caller-supplied `combo_sequence` (default `("Q","W","E","AA","R","AA")`; tokens `AA` / `P` / `Q` / `W` / `E` / `R` / `Q2`-`R2` for repeats at same rank), fires each spell once at level-resolved rank via shared `_evaluate_block` / `_select_blocks` from `ability_dps`, applies full Phase 4b amp pipeline (Rabadon, Liandry, Demonic Embrace, Abyssal Mask magic-only, Riftmaker HP→AP, Mejai's stacked AP) + mitigation pipeline (lethality + flat + % pen). Auto-attacks contribute build's per-hit `avg_attack_dmg` from `compute_dps` (post-armor + mode, no on-hit periodic procs - Phase 5.5 deferral). `/burst` + `/rank-assassin` routes on `:8893` |
| `hps.py` | **Phase 6 (s181)** - `compute_hps()` + `HpsResult` + `HpsItemContribution` (evaluator) **plus** `rank_items_by_hps()` + `HpsRankedItem` + `HpsRankResult` (enchanter ranker) **plus** `EnchanterFormulasSnapshot` + `EnchanterItemFormula` (curated formula loader + singleton `load_default_formulas()`). Total throughput = (healing_raw + shielding_raw) × product(1 + amp_pct) × mode_mult + sum(buff_credit). Per-item formulas from `data/daemon_slayer/<patch>/enchanter_items.json` (9 enchanter items hand-curated: Moonstone 30% chain amp, Redemption AoE heal 150→350, Mikael's single-target heal + cleanse, Helia Soul Siphon 50 HP × 0.4/s, Ardent +15 ally_buff_credit, Staff +12, Locket AoE shield 290→360, Mandate +6, Knight's Vow +10). ARAM `aramShieldsHealing` modifier applied if present. Operator override `targets_per_proc_override` retunes "average teammate" assumption (Arena 2v2 → 1). `/hps` + `/rank-enchanter` routes on `:8893` |
| `data/daemon_slayer/<patch>/enchanter_items.json` | (s181) Hand-curated per-item heal/shield/buff formula registry - 9 entries each carrying `heal_per_proc_base/per_level/ap_scaling`, `procs_per_second`, `targets_per_proc`, mirror fields for shielding, `heal_shield_amp_pct`, `ally_buff_credit_per_second` |
| `hybrid.py` | **Phase 2 (s175)** - `compute_hybrid()` + `HybridResult` + `rank_items_by_hybrid()` + `HybridRankResult` - Bruiser hybrid scorer (α·dps + β·ehp); per-champion (α,β) overrides in `archetype_weights.json`; ranker sorts by normalized percentage delta |
| `archetype_weights.json` | (s175) Per-champion bruiser α/β table - 20 entries covering Jarvan IV, Darius, Garen, Camille, Renekton, Sett, Mordekaiser, Riven, Volibear, Nasus, Olaf, Skarner, Hecarim, Udyr, Vi, Xin Zhao, Lee Sin, MonkeyKing/Wukong, Warwick, Trundle; default (0.5, 0.5) |
| `stats.py` | Champion base-stat + per-level growth + DDragon stat-key map |
| `engine.py` | `build_champion()` - leveled base + items + augments → resolved stat block |
| `rank.py` | `rank_items()` - single-slot DPS ranker over filtered candidate pool |
| `beam.py` | `beam_search_build()` - full-build beam search returning top-N complete builds |
| `data_loader.py` | Versioned `DataSnapshot` loader; reads `data/daemon_slayer/<patch>/` |
| `ult_rates.py` | Per-champion cast-rate lookup. Legacy `get_ult_casts_per_sec` (R-only, reads `ult_cast_rates.json`) preserved for Malignance Hatefog backward compat; `get_spell_casts_per_sec(champion, key, mode)` (Phase 4b, s178) reads `spell_cast_rates.json` for all 4 active spells; both derived from rewind_history.db via `scripts/build_spell_cast_rates.py`; 172 champions × 4 spells × 3 mode buckets |
| `tests/` | 11701 tests passing |

## Key data types

- **`ItemEffect`** - frozen dataclass: `periodics`, `damage_amp_pct`, `armor_reduction_pct`, `mr_reduction_pct`, `giant_slayer_*`, `unique_passive_key`, `defensive_only` flag
- **`PeriodicProc`** - `every_n_attacks` or `every_n_seconds`; `bonus_damage` is `(CallContext) -> float`
- **`CallContext`** - `base_ad, bonus_ad, level, ap, target_max_hp, caster_max_hp, caster_bonus_hp, targets_in_rotation, caster_max_mp, caster_bonus_armor, caster_lethality, ult_casts_per_sec`
- **`unique_passive_key`** - prevents double-counting when multiple items share named passives (e.g. `"spellblade"`, `"immolate"`)

## DS-before-Haiku pattern (required in all coaches)

DS `rank_for()` must run **before** `messages.create()` so Haiku sees per-champion DPS-ranked picks in the user turn.

```python
_ds_rows = None
_ds_picks_str = "unavailable"
try:
    _ds_rows = _ds_client.rank_for(champion=champ, level=level, item_ids=owned_ids, mode="ARAM", top=5)
    _ds_picks_str = " > ".join(f"{r.item_name}(+{r.delta_dps:.0f}dps,{r.gold}g)" for r in _ds_rows) if _ds_rows else "none"
except Exception as e:
    logger.debug("daemon_slayer pre-call: %s", e)
# user turn includes DS top items line
# post-Haiku: reuse _ds_rows for cur["daemon_slayer_picks"] - no second engine call
```

## Phase 3 archetype picker + dispatcher (s176)

Operator-facing scorer selection lands in three layers:

- **Storage**: `core/archetype_picks.py` - DDragon-tag → archetype default + per-champion override persisted to `data/cs_archetype_picks.json`. Six canonical archetypes: `carry`, `bruiser`, `tank`, `mage`, `assassin`, `enchanter` - all six implemented and wired through `rank_for_primary_archetype()` (carry → ds.dps, bruiser → ds.hybrid, tank → ds.ehp, mage → ds.ability, assassin → ds.burst, enchanter → ds.hps); no dispatcher fallbacks remain (s174-s181).
- **REST**: `GET /api/cs-archetype-pick?champion=X` returns merged pick (override OR default). `POST /api/cs-archetype-pick {champion, primary, secondary?, source?}` persists. `POST {champion, clear: true}` rolls back to default.
- **Dispatch**: `core.daemon_slayer_client.rank_for_primary_archetype(champion, archetype, ...)` returns `{ok, scorer, archetype, ranked, fell_back}`. Coaches read the picked archetype via `coach_integration/archetype_dispatch.py` (s182); all 4 mode coaches inject scorer-aware DS picks before each Haiku call.
- **UI**: 6-button 3×2 picker grid in the My Pick card of the champ-select view. Clicks save to `localStorage.rc-cs-archetype-<champion>` + POST. Unimplemented scorers grayed but still clickable.

## Coach integration status

| Coach | DS call | Position | Pre-DS rules pruned |
|---|---|---|---|
| `coaches/aram_coach.py` | ✅ | Before Haiku | ✅ (−37% system prompt) |
| `coaches/arena_coach.py` | ✅ | Before Haiku | ✅ |
| `coaches/brawl_coach.py` | ✅ | Before Haiku | ✅ |
| `coach_integration.py` (SR) | ✅ | Before Haiku | - |
| TFT | N/A | N/A | N/A |

## Permanently deferred items (3)

1. **Lightning Braid** - no formula in Meraki; also DPS-negative (−20% ability damage reduction)
2. **Kinkou Jitte** - directional weakpoint; positional geometry unmodelable
3. **Mejai's Arena mirror** - no Arena ID in DDragon (3041 SR only)

**Key insight**: check Meraki `passives[].cooldown` before deferring any "ability-triggered" item. If an item CD exists, use `every_n_seconds=CD` - no ability-frequency data needed.

## Calibration pipeline

`core/ds_calibration.py` appends picks per game tick to `data/ds_calibration.jsonl`. After 50+ games, run calibration analysis: join log vs `rewind_history.db` on (champion, mode, ~ts). Status: accumulating.

## Arena-specific: Arcane Sweeper

Every Arena player receives Arcane Sweeper in the trinket slot - exclude it from DS candidate pool when `mode=ARENA`. The beam search filters `defensive_only` items; Arcane Sweeper is handled separately.
