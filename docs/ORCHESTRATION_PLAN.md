# Orchestration Plan - Gemini-Directed Fanout Run

LIVING DOC. The gemini director reads this each cycle and picks the next OPEN
session (top-to-bottom, phase order A -> F). The executor cycle updates it:
flip the picked session OPEN -> WIP -> DONE, fill the Commit sha, and append any
newly discovered work to the Findings log at the bottom. When no session is OPEN,
the director emits NO_WORK and the loop self-terminates.

Per-cycle contract (enforced by ops/loop/director_prompt.md):
orchestrator multi-agent fanout (disjoint-file worktree subagents, sole merger,
verifier-gate each slice before merge) -> TDD (failing test first) -> py_compile
before any restart -> full pytest suite green -> UI sessions also pass the 5-phase
fixture audit (STRUCTURE / TYPOGRAPHY / HIT-TARGETS / ASCII / HIERARCHY) + a
overlay visual check via the Playwright ui_recon harness + :8810 static preview (NOT Claude_Preview vs :8888, cannot attach per R2) -> commit with a descriptive message ->
push to origin/main -> /done ritual (append docs/LEDGER.md, sync ROADMAP + this
file) -> "C:\Users\Administrator\AppData\Local\Programs\Python\Python314\python.exe" ops/loop/done_sentinel.py. No AskUserQuestion; auto-pick safest option.

ASCII only. No em-dashes, en-dashes, or smart quotes.

## Sessions

NOTE (2026-07-17 mdclean C6): every round below is fully DONE; row bodies were relocated
verbatim to docs/ORCHESTRATION_FINDINGS_ARCHIVE.md. Headers stay as anchors. With zero OPEN
sessions the director emits NO_WORK until a new round is appended above the EXCLUDED list.

### OPERATOR OPEN-ITEM QUEUE 2026-07-01 (director picks these FIRST, top-down)

DONE - All rows DONE (final row LEDGER 875 inline; SHAs 505274c1/e14eedbd). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - OPERATOR REFILL 2026-06-17 (DS permutation swarm + drive ALL open items)

DONE - All rows DONE (DSP permutation swarm; LEDGER 876 + 889). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - OPERATOR REFILL 2026-06-17 ROUND 2 (post-NO_WORK refill; DS cross-eval residual clusters + test hermeticity)

DONE - All rows DONE (in-doc DONE markers; cross-eval residual cleanup). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - DIRECTOR REFILL 2026-06-18

DONE - All rows DONE (in-doc DONE markers). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - DIRECTOR REFILL 2026-06-19

DONE - All rows DONE (in-doc DONE markers). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - DIRECTOR REFILL 2026-06-22

DONE - All rows DONE (b49ef1a7 verified). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - DIRECTOR REFILL 2026-06-25

DONE - All 43 rows DONE (LEDGER 763). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - ORCHESTRATED-RUN RELAUNCH 2026-07-03 (curated; director picks these FIRST, top-down)

DONE - All 51 rows DONE (LEDGER 784 + 903; newest R129 = LEDGER 903). Rows relocated 2026-07-17 -> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md.

## Sessions - DIRECTOR REFILL 2026-07-19

| ID | Theme | Scope | Status | Commit |
|----|-------|-------|--------|--------|
| R130 | ops-gemini-audit | Restore the dead RC-GeminiAudit nightly critic (ESCALATION DECISION option (a) FULL). ENGINE-IMPACT NONE - tooling + docs only, no DS path. Fanout 2 disjoint worktree slices + a third opened by the executor after the pre-flight found main RED. **TWO INDEPENDENT FAULTS, only one proven.** Fault A PROVEN + FIXED: `ops/runtime/gemini_last_audit.txt` pinned `edd76db3...`, a worktree-slice sha that never survived cherry-pick (`git cat-file -e` exit 128), so `gemini_audit.ps1:32` loaded it into `$Since`, the `$Since..HEAD` range failed, `$commits` came back empty and the script took the "nothing to audit" branch and exited 0 having written NO review and NO log line - which is why the newest review on disk was 2026-06-21 (marker mtime Jun 21 03:03, exactly matching) and `logs/gemini_audit.log` did not exist at all. 28 nights silent. Fix = `Test-CommitResolves` validates the marker and self-heals to `HEAD~10` with a loud WARN, plus an unconditional `START pid=...` first write so no future run can be unattributable, plus a `SKIP code=0` log on the previously-silent no-commits branch. Fault B (the observed `0xC000013A` / STATUS_CONTROL_C_EXIT last result) is UNPROVEN and deliberately NOT papered over: six hypotheses were refuted with evidence - Node/TTY stdin (probe returned OK exit 0 under Task Scheduler), `InteractiveToken` session teardown (5 sibling InteractiveToken tasks ran 3:00:00 / 3:30 / 3:45 / 4:00 / 4:17 the SAME night, all result 0, one of them one second earlier), the dangling marker itself (that path exits 0), oversized stdin (20k/60k/120k all OK), the missing `-NoProfile` (profile.ps1 parses clean), and a logoff/shutdown kill (no 1074/6005/6006/6008/41/42 events 02:00-05:00). The task was therefore NOT re-registered - the registration hypothesis is refuted, so changing it would be unevidenced churn on a working ops surface. The new START line makes the next occurrence diagnosable: START with no following line = killed mid-run, no START at all = died before line 20. Marker repaired on disk in the main checkout (gitignored runtime state, not committed); end-to-end proof run wrote a real 2616-char review. Docs half: STEP D ticked, and the status line corrected from a bare "(Ready)" - the scheduler idle state, not evidence of a healthy run - to an honest time-anchored UNVERIFIED that self-retires when a review newer than 2026-06-21 actually appears. Also corrected a read-only framing leak the slice found unprompted: the config claimed Gemini "writes findings to a review file" while GEMINI.md, the prompt template and the runner header all say the runner is the sole writer. No credential was entered or stored at any point - a `schtasks /RU /NP` prompt was killed via `taskkill /F /PID` and the no-password principal used instead. | DONE | `aa90e9b4` + `fb88c7d8` |
| R131 | ci-nightly-red | NOT from the directive - opened by the executor because the R130 pre-flight found main RED and section 1 says fix red FIRST. Nightly `ci` run 29682372934 (`nightly-full-suite`, event schedule) failed 3 tests, and it was NOT new: red for 12 consecutive days, runs 28935372989 through 29682372934, with 29639429338 failing the identical 3. Both files pass in ISOLATION on Legion, which is what made it look like one bug; it was two. The push `check` job is green and always was, because it never runs `tests/` whole - it fires four separate pytest invocations (`ci.yml:136-148`) and puts `tests/snapshot_panels/` in its own process, so it physically cannot co-locate a Playwright suite with an `asyncio.run()` test. Only the nightly's single-process `pytest tests/ agents/daemon_slayer/tests/` (`ci.yml:58`) can. Neither job installs pytest-randomly (`ci.yml:52`, `ci.yml:80`) so CI order is deterministic alphabetical; locally it IS installed, hence `-p no:randomly` to match. **Failure 1-2 = genuine ordering pollution, pair PROVEN:** `tests/snapshot_panels/` -> `tests/test_coach_poll_offload_hot03.py`. `playwright.sync_api` drives a greenlet-backed ProactorEventLoop on the MAIN thread and never clears asyncio's per-thread running-loop marker; `pw_browser` is session-scoped so the marker outlives the suite, and `asyncio.run()` then refuses to start. The marker is LIVE, not stale - clearing it from a conftest autouse teardown makes snapshot_panels itself time out, so that approach was tried and reverted (`tests/conftest.py` confirmed untouched at merge). Known polluter class, already documented at `tests/test_lcu_loop_resilience.py:35-52` with three files immune via a local `_run_coro`; HOT-03 (added 2026-07-09) simply missed the pattern. The `never awaited` warning blamed on `test_comp_hp_lean_dsv5.py` is a downstream GC artifact - that file is innocent. **Failure 3 was NOT ordering pollution at all** - `plan_build_order` was called with no `rank_fn`, fell through to the `:8893` HTTP client and returned None (`core/build_order.py:630-634`). CI can never satisfy it: `daemon_slayer_client.ensure_running()` spawns with `creationflags=0x08000000`, which raises ValueError on Linux and is swallowed. It passed on Legion only because a live engine answers there. Fixes are test-side only: a `_run_poll_loop` helper mirroring the three siblings (loop-thread ident captured INSIDE the worker so the assertion still tests real offload, proven non-tautological by a mutation probe), and a `_dps_rank_fn` injected via the existing `rank_fn=` seam so the Jhin test runs genuine scorer math with no live server. No production code, no ENGINE bump, no skip/xfail, no loosened assertion, no seed pin. Verified by orchestrator: proving pair 382 passed (was 2 failed / 380), conftest untouched, Share mirror --check green. FUTURE: snapshot_panels keeps leaking the marker, so any future bare `asyncio.run()` sorting after it fails the same way - four near-identical local helpers now exist and want consolidating. | DONE | `cf71753f` |
| R132 | ds-sweep-defensive | DS sweep of the DEFENSIVE / tank half of the item registry (R119 swept offensive + utility, so this is the disjoint complement, not a duplicate). Fanout 4 read-only research agents on disjoint item sets: (a) Kaenic Rookern + Jak'Sho, (b) HP-stacking / HP-scaling (Heartsteel, Warmog's, Overlord's Bloodmail, Sterak's, Titanic, Riftmaker), (c) defensive runes (Aftershock / Grasp / Second Wind / Bone Plating / Conditioning / Overgrowth / Revitalize / Unflinching / Shield Bash / Demolish), (d) armor-MR-aura tanks (Randuin's, Frozen Heart, Thornmail, Sunfire, Hollow Radiance, Unending Despair, Force of Nature, Spirit Visage, Zeke's, Locket, Knight's Vow, Anathema's, Dead Man's Plate, Iceborn, Fimbulwinter) with special attention to the damage-AVOIDED-via-uptime-denial axis (crit-damage reduction, attack-speed slow aura, percent DR vs one champion, slow fields) which is distinct from flat DR. Every agent carried the operator-CLOSED don't-redo set (survivability axes COMPLETE on both EHP scorers, cc_blended_ehp 4 consumers, cc_conditional 5 consumers, Term A ally-granted EHP, RM-91 / RM-92 / RM-94 / RM-96 / RM-97 already logged, DS_SWEEP 173/173 CLOSED, conditional-target-state operator-CLOSED) plus the four probe traps (/rank is the CARRY scorer, body key `items` not `item_ids`, empty-build under-ranking, ZERO-target defaults killing percent-max-HP items, top=40 truncation). Merger implements exactly ONE target behind a DEFAULT-OFF seam, TDD failing test first, offline characterization only. **RESULT: the sweep found FOUR verified gaps, not one - the tank ITEM space is genuinely dense (agent (d) alone returned 8 REFUTEs against modelled axes R77 / R80 / R86 / R105 / R106 / R107 / R111 / R124), but the RUNE space is a hole.** DS models runes as OFFENSE ONLY: `ehp.py` grep for `rune|perk|keystone` = ZERO matches, `rank.py` = ZERO, and 7 live Resolve rune ids (8429 / 8242 / 8451 / 8453 / 8473 / 8463 / 8465) return 0 hits across the whole package. Aftershock 8439 IS registered in `rune_procs.py:611` but its own formula string at :620 admits verbatim `"(resist-bonus side not modeled)"`. Built the resist half only: new `_rune_resist_grants.py` (8439 / 8429 / 8242) mirroring `_item_resist_grants.py`, folded into `eff_armor` / `eff_mr` at `ehp.py:1684-1685` behind DEFAULT-OFF `apply_rune_resist_grants` + a `rune_ids` kwarg, threaded through `hybrid.py` + all 4 `server.py` routes. **The Aftershock CAP was the trap and the sweep agent missed it** - its report gave the formula as an uncapped `45 + 75% of Bonus Resists`, but the live 16.14.1 longDesc ends `"Resistance bonus from Aftershock capped at: 80-150 (based on level)"`. The orchestrator caught it by reading the raw longDesc instead of the agent's paraphrase; uncapped, a level-18 tank at 150 bonus armor reads 157.5 against a cap of 150, so shipping the paraphrase would have over-credited precisely the high-resist cohort the feed exists for. Modelled as `min(45 + 0.75 * bonus, cap(level))` with `cap = 80 + 70*(level-1)/17`, pinned by a discriminating test. Default-OFF inertness independently proven: the full build-order regen diff across 6 tables is EXACTLY `engine_version` + `generated_at`. Overgrowth / Revitalize / Grasp-self / Second-Wind-self / Bone Plating / Guardian / Font of Life deliberately NOT built (different output fields or unresolved DDragon template vars) - spec'd as RM-101. | DONE | `e6a84734` |
| R133 | ds-arena-mirror-magnitudes | NOT from the directive - opened by the executor because the R132 sweep surfaced a WRONG SHIPPED CONSTANT, which outranks a missing feature. `_item_resist_grants.py` seeded its Arena / prismatic mirror rows by copying the SR base magnitude; all four carried the note "base nominal". The registry docstring claimed "each of the four ids was confirmed present in the index before adding" - PRESENT was verified, MAGNITUDE was never read. Three of four rows were wrong, each verified by the orchestrator directly against `data/daemon_slayer/16.14.1/items.json` before any code was written: **226665** Jak'Sho Arena grants **40%** of bonus resist, not the base 30% (under-credit; the mirror also carries 35/35 flat statics, not 45/45); **224401** Force of Nature Arena grants **50** flat MR, not the base 70 (over-credit - and it is not a retune but a DIFFERENT passive, Absorb / Dissipate at max 10 stacks with immobilizing effects granting +2, vs base Steadfast at 8); **663059** Cloak of Starry Night map-11 grants **10%**, half its map-30 sibling's 20% (over-credit; its secondary DR cap is likewise halved 25% vs 50%, an axis that stays uncredited). **663058** Shield of Molten Stone was the one row where the copy happened to land right at 20%. All values identical in 16.13.1, so never patch drift - wrong since the rows were seeded. **The suite had the defect PINNED:** `test_arena_mirrors_credit_same_as_base` asserted `base == mirror`, encoding base-nominal seeding as an invariant; replaced with directional assertions plus a new `test_item_resist_mirror_magnitudes_r133.py` that parses each row's expected magnitude from THAT id's own tooltip via an offline `DataSnapshot.load()`, so a future base-copy fails the moment base and mirror disagree. Read-only audit of the six other "base nominal" registries found most fine (Zhonya's, GA, both spell shields, Fimbulwinter ARAM) and several unverifiable because DDragon elides the number - but surfaced one further defect, `_item_bonus_hp_amp.py:89` crediting Warmog's Vitality 0.12 to Arena mirror 443083 whose tooltip has NO Vitality clause at all (spec'd RM-102, not built blind). | DONE | `ad16ba65` |
| R135 | ds-sweep-movespeed | DS sweep of League movement-speed diminishing returns (soft caps above 415 / 490, soft floor below 220) vs Meraki/DDragon truth, plus a mirror + magnitude sweep of MS item grants in the Arena / ARAM registries. Directive declared ENGINE-IMPACT BUMP. Fanout 2 read-only sweep agents on disjoint scopes (worktree isolation deliberately skipped - read-only agents cannot conflict, so it buys nothing). **BOTH lanes came back CLEAN on the directive's own thesis, for two different measured reasons, and the shipped fix came from neither.** Soft-cap lane is TRIPLE-INERT: the piecewise is correct as stated (verified against `wiki.leagueoflegends.com` raw, continuous at 220/415/490, plus an unreachable 4th band below 0) and no repo code applies it, but `core/champion_movespeed.est_ms` never needs it (roster base MS is 315 Rell to 355 Master Yi across all 173 champs, and the sole consumer `core/mia_reachability.py:165` passes NO items); `hybrid.py:272 _ms_utility_multiplier` does see item-inclusive MS (Darius + Swifties + DMP + FoN = 426.6) but `assume_ms_utility` is DEFAULT-OFF with no production caller and `_MS_UTILITY_DPS_CAP = 0.15` saturates at 1.30x base, bounding the error to 0.794 percentage points inside a 27-unit window and exactly zero above 442; and a third site the directive never named, `ability_dps.py:293 caster_bonus_ms`, is default-ON but populated by exactly ONE block roster-wide (Janna W, 1.09 magic damage overstatement on an off-class PD build). Item-registry lane: **zero** missing mirrors and **zero** confirmed magnitude drift across 99 MS-bearing items - and it established that `items_meraki.json` carries no `stats` key at all (0 of 320 entries), so DDragon is the SOLE MS source and Meraki cannot cross-check magnitudes. **The one real defect was found incidentally by lane 2: `_item_index()` hit `continue` on zero-MS entries BEFORE the name `setdefault`, so a canonical 4-digit id granting no MS never claimed its own display name and the next mode mirror answered for it** - `item_ms("Warmog's Armor")` returned 4% from Arena `443083` while canonical `3083` grants none, likewise Gargoyle Stoneplate 10% (`443193`) and Rite of Ruin 4% (ARAM `123430`); all three reproduced by the orchestrator against raw `data/meta/ddragon_items.json` before any code was written. The existing suite could not have caught it: it pins only "Boots of Swiftness", whose canonical `3009` HAS movespeed and so already claimed its name key - the single tested name is the one name outside the failing class. Fix claims the name key before the early-out. Zephyr and Gambler's Blade deliberately untouched (no 4-digit canonical exists for either, and `3172` is Gunmetal Greaves NOT Zephyr - Riot repurposed the suffix, so `"22"+base == mirror-of-base` is a FALSE pairing rule), leaving a bare name there correctly mode-ambiguous and by-id-only. **ENGINE-IMPACT premise CORRECTED to NONE**: the shipped fix is in `core/`, has no DS import, and no DS path reads it - the precommit gate independently agreed ("no mirrored DS source staged - skipping Share sync"). No bump, no Share sync, no `:8893` bounce. The directive's ROADMAP/BACKLOG ask (pin the `total_armor` hypothesis CLOSED-REFUTED) was already satisfied by `20532b3e` and correctly not re-done. | DONE | `e4ab8144` |
| R136 | ds-sweep-resolve-hp | DS sweep RM-101, the defensive-rune remainder R132 deliberately did not build. R132 shipped the resist DENOMINATOR feed (Aftershock 8439 / Conditioning 8429 / Unflinching 8242); RM-101 is the NUMERATOR half. Fanout 3 agents on disjoint file sets, with the orchestrator holding ALL shared seams (`ehp.py` / `hybrid.py` / `server.py` / `__init__.py`) so the slices could not collide: S1 worktree = new `_rune_health_grants.py` (Overgrowth 8451 permanent max-HP + Grasp 8437 self-side heal/permanent-HP), S2 worktree = new `_rune_hsp_amp.py` (Revitalize 8453 flat 5% Heal/Shield Power), S3 read-only = adjudicate the four remaining runes. Both build slices verifier-CONFIRMED before merge (17/17 claims, formulas re-read from raw `runesReforged.json` by the verifier itself, zero shared-seam edits). **THE DIRECTIVE'S BUILD LIST WAS WRONG AND WAS NOT FOLLOWED.** It named all seven runes including Font of Life 8463, but the repo's own RM-101 spec records 8463 as DATA-BLOCKED on an unresolved `@BaseHeal@` DDragon template var. Confirmed live this cycle: `@BaseHeal@` is present verbatim in ALL FOUR vendored snapshots (16.11.1 / 16.12.1 / 16.13.1 / 16.14.1), it is one of only 3 unresolved tokens in the whole rune file, and S3's exhaustive search found no independent vendored source (the aggregator B scrapes are a verbatim re-serve of the same DDragon payload). Built the buildable subset; the number was NOT invented. **S3 REFUTED TWO STANDING ROADMAP CLAIMS, both independently re-grepped by the orchestrator.** RM-101 said Second Wind 8444 needs a missing-health convention the EHP scorer lacks - it lacks nothing: `ehp.py:355 _MISSING_HP_SHARE_FOR_HEALS = 0.5` is a local variable at the exact call site. RM-101 said Bone Plating 8473 needs an instance-count convention that does not exist - `_passive_flat_mitigation_overrides.py:92 _ASSUMED_FLAT_DR_INSTANCES = 6.0` with discrete instance math at `:270` shipped in R9 a MONTH BEFORE the claim was written. Both are BUILDABLE-NOW and Bone Plating is the ranked next build: its instance count of 3 is stated in the rune text, so it REPLACES that registry's largest assumption instead of adding one. Guardian 8465 is CONVENTION-BLOCKED on a measured hard ceiling - `ehp.py` contains ZERO AP references, so its "+20% ability power" shield term is unrepresentable; a self-only AP-omitted subset is available at S under the shipped Irelia-W / Fizz-P omission precedent. Executor-opened correctness fix on disjoint files: `Share/CHANGELOG.md` was missing R132's 1.223.0 -> 1.224.0 entry entirely, so both it and R136's were authored. | DONE | `18aca9e3` |
| R137 | rm-100-asyncio-leak | RM-100 asyncio running-loop marker. **BOTH halves of the directive were premise-refuted on ground truth before any code was written, so the scope was re-cut rather than followed literally.** Refuted 1: the directive asked for "one unified async helper" replacing "4 near-identical `_run_coro` copies" - already shipped by `b54e315d`, `tests/_asyncio_isolation.py` is the single runner and `tests/test_asyncio_isolation_guard.py` already blocked a sixth copy; the historical count was also 5, not 4 (both ROADMAP.md and the directive carried the stale number). Refuted 2: the directive asked to "fix `tests/snapshot_panels/` to clear the asyncio marker in teardown" - there is no repo-side leak site at all, grep for asyncio across `tests/snapshot_panels/**` returns ZERO hits. The marker is armed by third-party playwright 1.59.0 (`_sync_base.py:114` and `:56` call `asyncio._set_running_loop(self._loop)` with no paired clear anywhere in the package), and - correcting a claim the repo had been repeating - it IS cleared at `PlaywrightContextManager.__exit__` via `stop_sync()` -> `_connection.py:319` `run_until_complete`, whose own `finally` restores it to `None`. Measured on Legion (py3.14 / playwright 1.59.0): marker before start `None`; inside session after `page.set_content()` `<ProactorEventLoop running=True closed=False>`; `asyncio.run()` inside session RuntimeError; marker after `cm.__exit__()` `None`; `asyncio.run()` after exit ok. **So the leak is bounded by the context manager, not permanent - it spans the session only because `pw_browser` is `scope="session"` (`tests/snapshot_panels/conftest.py:246`).** The old "never clears" phrasing mattered because it made the bug read as unfixable from the test side. Shipped instead: a BEHAVIORAL guard replacing name-matching as the primary defence - AST-scans `tests/**/test_*.py` plus every `conftest.py` and fails on `asyncio.run` / `new_event_loop` / `set_event_loop`, resolving `import asyncio as X` and `from asyncio import run` aliases so a rename cannot dodge it. `asyncio.run()` on the main thread is the actual breakage mode, so the guard pins the invariant that currently holds (measured: ZERO bare `asyncio.run(` calls remain under `tests/`, i.e. the latent bug has no live trigger today) rather than a list of two names. Three defects found and fixed DURING the slice, each proven RED-then-GREEN with a throwaway offender: the existing name guard matched only `ast.FunctionDef` so an `async def _run_coro` slipped past (now also `AsyncFunctionDef`); the verifier caught that `_guarded_paths()` scanned only the ROOT conftest, which would have missed a bare `asyncio.run()` in `tests/snapshot_panels/conftest.py` - the very fixture that arms the marker; and the orchestrator's own nested-conftest probe went red for the WRONG reason, a `SyntaxError` at line 1, revealing that `read_text(encoding="utf-8")` crashes the guard on any BOM-prefixed file on this Windows repo (both read sites now `utf-8-sig`). Docs half corrects the mechanism in `tests/_asyncio_isolation.py` (module docstring ONLY - executable region proven byte-identical by sha over the AST-stripped source), ROADMAP.md and docs/ROADMAP_HISTORY.md, plus the 4 -> 5 count. All three still read LATENT; nothing here is a shipped fix. **Deliberately NOT done, recorded rather than skipped silently:** narrowing `pw_browser` off session scope WOULD bound the leak but costs a browser launch across ~387 snapshot_panels tests - a real perf tradeoff, not a free fix, and not authorized blind; and clearing the marker from a conftest teardown is documented at `_asyncio_isolation.py:15-16` to hang playwright, which recon could NOT independently confirm, so it was treated as documented-not-verified rather than acted on. ENGINE-IMPACT NONE (no DS path, no ENGINE_VERSION, no Share mirror, no restart owed; the precommit gate independently agreed "no mirrored DS source staged"). Verifier gate CONFIRM 9/9. Full RC suite 12279 passed / 0 failed / 0 errors, 23 skipped, 359 subtests. | DONE | `bf6d8c02` |
| R138 | hexcore-offline | Refresh `docs/HEXCORE_offline.html` (the self-contained accretion-disk repo explorer): add the net-new non-test .py files added since d584e02e as DUST leaves, refresh the stats HUD off live git + DS ground truth, and add an ENGINE tooltip. ENGINE-IMPACT NONE - docs only, no DS path. | DONE | `ee0e2f62` | FINDINGS: DUST 293 -> 319 (26 net-new non-test .py since d584e02e; the 10 `Share/src/agents/daemon_slayer/*.py` hits are byte mirrors of the `agents/` hits and were excluded as duplicates, so 36 raw adds resolve to 26 unique units). The 293 literal occurred in THREE places, not the two the brief named - the `// DUST:` comment, the sr-only accessibility paragraph, and the no-JS fallback prose near the file end. HUD refreshed off live ground truth (commits 3733, HEAD 114977ee, DS 1.228.0 / patch 16.14.1 confirmed live at `:8893/health`, not read off CLAUDE.md which still said 1.226.0) plus new nodes/dust rows; the nodes row is asserted against the real parsed `len(NODES)`=141, not a literal. ENGINE tooltip added with `pointer-events:auto` scoped to that one row so the rest of the HUD stays click-through. New `tests/test_hexcore_offline_dust.py` (11 tests) pins count-vs-prose agreement, parent-id and category referential integrity against the file's own NODES/CATS, zero non-ASCII, and a `node --check` guard on the extracted script block. Verifier CONFIRM 13/13 claims.
| R139 | share-external-presentation | DIRECTOR REFILL unit (b). Read the entire `Share/` package end-to-end and raise it to external-presentation quality; credit Riot Data Dragon / CommunityDragon / Meraki explicitly; keep `tools/ds_share_sync.py --check` green. ENGINE-IMPACT NONE - docs and presentation only, no DS path touched. | DONE | `a4261c54` | FINDINGS: the directive's premise was already satisfied - the Riot / CommunityDragon / Meraki credit table has been at `Share/README.md:19-32` all along, so the shippable defect was the one beside it: the README "Changelog (recent)" list sat 5 releases stale at 1.223.0 vs live 1.228.0, and stale BY DESIGN because `_doc_anchor_rules` in `tools/ds_share_sync.py` excludes changelog history from the auto-rewrite that keeps the header anchor fresh - so `--check` read green over a five-versions-behind public doc. Docs half corrected 100-plus stale `file:line` citations plus every drifted count (172/705 -> 173/706 champs/items, 13 -> 20 snapshot JSONs, 328 -> 339 test files, RUNE_PROCS 19 -> 20, NON_DAMAGE_BLOCKS 7 -> 9) and retired one wrong REASON: 04's replay ceiling claimed a parsed `.rofl` is a strict subset of Match-V5, a justification this repo retired 2026-06-03. Prunes ZERO - every candidate is load-bearing to the sync tool. Verifier CONFIRM/CONFIRM, 6/6 spot-checks. DS 8866 passed / 2518 subtests; RC 12290 passed. |
| R140 | rm-100-asyncio-consolidation | CLEAN / REFUTED-PREMISE. Directive: consolidate the 5 near-identical local `_run_coro` / `_run_poll_loop` copies into `tests/_asyncio_isolation.py`. The premise was ALREADY SATISFIED and was refuted on ground truth BEFORE any dispatch (verify-agent-premise + audit-proposals-are-intent): the consolidation landed in R137 / cycle-1 this same loop run, and `tests/_asyncio_isolation.py` (5161 bytes, `run_coro` + `run_coro_capturing_thread`) has existed on disk since 03:23. GREP PROOF: zero local runner definitions remain anywhere under `tests/` or `agents/` - all 5 named files (test_lcu_loop_resilience:33, test_obs_frame_source:32, test_p2w1_core_f:34, test_p2w2_ds_h:57, test_coach_poll_offload_hot03) already carry `from tests._asyncio_isolation import run_coro as _run_coro`, and `tests/test_asyncio_isolation_guard.py` already fails on a sixth copy (it also pins the deeper invariant: zero bare `asyncio.run(` under `tests/`). Verified green THIS run: the guard + all 5 importers = 73 passed in 3.71s. There was nothing to build; fabricating a consolidation slice over an already-consolidated tree was the wrong action. ROOT CAUSE of the duplicate directive FIXED instead: `ROADMAP.md:20` still carried the stale parenthetical "(5 near-identical copies; consolidating them is the open follow-up)" - R137 shipped the code but never cleared the ROADMAP text, so the director's digest read a closed follow-up as open. That line now states the consolidation is CLOSED, names the shared module + the guard test, and cites `114977ee`. ENGINE-IMPACT NONE (docs-only Tier-0; no code / ENGINE_VERSION / Share / DS restart / route change; DS live 1.228.0 patch 16.14.1 == repo `agents/daemon_slayer/__init__.py`, probed this cycle, no bounce). No subagents / worktrees (nothing to build - R9 inline; the build slice was cancelled by ground truth). PART C durable steer written to `gemini_ask.txt`. | CLEAN | `789f8860` |

## EXCLUDED (live-game / operator-gated; the director MUST NOT pick these)

- DS Phase-D default-ON flag flips (apply_passive_damage, non-every-AA on_hit, per-stack assumed_stacks) - need real-game re-ranking validation.
- Live caster-stat producer for /anti-tank P3.2 activation + live survivability scorer (egg-resist / Orianna E) - need a live AbilitiesSnapshot / game.
- Champ-select brief Haiku -> deterministic flip - needs live shadow-log accrual + operator OK.
- Game-PC :8892 visual screenshot captures (machine retired post-1PC, ADR-011). C-phase / overlay visual validation uses the Playwright `ui_recon` harness + the `:8810` static preview (the Chrome `:8888` dashboard cannot be attached per R2 and is retired as an audit surface 2026-06-27; the Electron overlay is the sanctioned surface).
- Anything in the CLAUDE.md "Settled - do not re-litigate" set.
- Haiku-to-ZERO LIVE coach flips: removing/replacing a live Haiku call with the HZ-* precompute tables. Per charter 4b "do not flip blind" - needs real/replayed-game validation + operator OK. The HZ-* sessions BUILD + PERSIST + SHADOW-LOG only; Haiku stays the interim floor until validated.
- DSP/DSV default-OFF seam live default-ON flips in rank.py/burst.py + every row in docs/LIVE_GAME_GATED_SYNC.md - need a real game. The DSP* sessions ship the seam DEFAULT-OFF + offline-validate it; the executor APPENDS each new seam's live flip to docs/LIVE_GAME_GATED_SYNC.md and NEVER flips blind.
- Electron IN-GAME overlay 2026-07-05 LIVE-RECON punch-list (drag/move system, item context-menu/radial tooltip crop+position, PR enemy-spell click-to-timer, objective-gauges 1-line layout, the round-1 missing-in-game cluster rn-lead/rn-choices/rn-callouts/w-spike, the build-META-static / DMG-SURV-UTIL knob-stepper / item right-click radial bugs, the am-statspanel clarify). The operator LIVE-GATED the ENTIRE remaining punch-list in ROADMAP.md (2026-07-05 LIVE-GATED WRAP): "Do NOT ship these blind headless; root-cause is fine, but each fix needs the operator's live Ctrl+Alt+A overlay verify before it lands." The agent cannot see the Electron overlay (only ?overlay=1 headless capture, which does NOT reproduce these). Root-cause investigation is allowed; SHIPPING any of them blind is forbidden. Do NOT pick an overlay punch-list item as a directive - overlay drains route through the operator's live Ctrl+Alt+A session (docs/LIVE_GAME_GATED_SYNC.md). This is DISTINCT from dashboard Post-Game-Review UI slices (ORUN3/ORUN4), which audit headless via ui_recon + the :8810 static preview with pixel-capture OWED and ARE shippable this loop.

## Findings log (executor appends; newest first)
> Older findings entries are relocated (relocate-only, immutable) to
> docs/ORCHESTRATION_FINDINGS_ARCHIVE.md when this log outgrows the
> director-context byte budget (loop_controller PLAN_CTX_CAP). Newest ~10 stay here.

- 2026-07-20 R137 (gemini-loop cycle 7, head cb926c2e) DONE (`bf6d8c02`) - **one lesson dominates: a premise can be stale in the direction of work ALREADY DONE, not just work that is wrong.** Half this directive asked for a consolidation `b54e315d` had already shipped, and the cheap check was a `git log --grep` before dispatching a single build agent. The other half named a fix site (`tests/snapshot_panels/`) that provably contains none of the mechanism - zero asyncio hits - because the leak is third-party. Second lesson: **the repo's own docs manufactured the dead end.** "playwright never clears the marker" was wrong; `__exit__` does clear it, and once that is true the bug is a fixture-SCOPE tradeoff rather than an unfixable third-party defect. A wrong mechanism note is worse than a missing one - it forecloses the real option. Third: **two of the three guard defects were found by adversarial probing, not by reading.** The verifier surfaced the root-conftest-only scan gap, and the orchestrator's own RED probe failed for the wrong reason (a BOM `SyntaxError`), which is the only way the utf-8 vs utf-8-sig crash would have been found - a guard that goes red for the wrong reason is indistinguishable from one that works, so always read WHY it went red, never just THAT it did.
- 2026-07-19 R136-audit (gemini-loop cycle 6, head db08c374) DONE - **the directed fix was a no-op on a phantom regression, and the real defect was in the instrument that reported it.** The cycle-5 audit returned REGRESS on three claims: that R136 edited the read-only mirror `Share/src/agents/daemon_slayer` directly, that the true source `agents/daemon_slayer` was "missing from diff", and that ENGINE bumped without tests. All three are false. `git show --name-only 18aca9e3` counts 275 files: 139 under `Share/` and **129 under `agents/daemon_slayer/`**, including every file the directive ordered "moved" (`_rune_health_grants.py`, `_rune_hsp_amp.py`, `ehp.py`, `hybrid.py`, `server.py`) plus three NEW R136 tests. HEAD was provably healthy at audit time: `ds_share_sync.py --check` exit 0 (engine 1.225.0, 480 files), the R136 tests 73 passed / 435 subtests, and both `ci` and `CodSpeed` green on db08c374. Executing the directive would have been actively destructive - Agent 1's "revert manual Share/ edits" deletes mirror content that the currently-green `ds_share_sync --check` CI guard requires, converting a clean tree into a red one. **Root cause, and the durable half: the auditor was fed a head-truncated diff.** `auditor()` spent its entire 55,000-byte budget on a raw `git diff`, and git emits paths in byte order, so `Share/` (0x53) precedes `agents/` (0x61); on a commit that touches a generated mirror AND its true source, the mirror padding consumes the budget and the true source never reaches the prompt. The auditor then cannot distinguish "absent from the commit" from "past the cut" and resolves the ambiguity as a regression. Fix rebudgets the payload rather than growing it, because `GEMINI_STDIN_CAP = 60_000` is an empirically-measured cliff (silent EMPTY stdout above it) and must not be crossed: `AUDIT_DIFF_CAP = 40_000` yields 15K to a complete `--name-status` manifest (`AUDIT_MANIFEST_CAP = 12_000`) emitted BEFORE the body, carrying a total file count so manifest truncation is self-evident, and the body's cut notice now states that later-sorting paths are dropped first and that absence below is not evidence of absence. Replayed against the real commits the payload is 54,782 bytes (under cap) and names all 280 changed files. **Lesson, third cycle running in this same family: an audit verdict is a claim, not evidence, and it earns the same premise-check as a feature claim.** Cycle 3's auditor inferred a callee's semantics from an argument name; this one inferred a commit's contents from a truncated view of it. The sharper generalization is about tooling rather than reasoning: **when a tool truncates its input, absence of evidence silently becomes evidence of absence,** and any consumer downstream will confidently report the artifact of the cut as a finding. The guard is `test_truncated_diff_still_lists_late_sorting_true_source` in `tests/test_loop_audit_range.py`, which builds the exact mirror-before-source topology and fails if a changed path past the cut is not named in the prompt. Note the running controller had already loaded the old module, so the fix takes effect at the next `launch_loop.ps1`, not mid-run.
- 2026-07-19 R135 (gemini-loop cycle 4, head 20532b3e) DONE - **the sweep's own thesis was refuted by measurement, and the shippable defect was the thing found on the way past it.** The directive ordered a movespeed soft-cap seam and pre-declared ENGINE-IMPACT BUMP. Both lanes came back CLEAN on that thesis, and the reason matters more than the verdict: the soft cap is not merely unimplemented, it is UNREACHABLE at every site. Roster base MS spans 315-355 so the 220/415/490 breakpoints cannot bind on the `est_ms` path, and the DS path that CAN exceed 415 is double-gated behind a DEFAULT-OFF flag with no production caller AND an engine credit cap (`_MS_UTILITY_DPS_CAP = 0.15`) that already absorbs the error to under 0.8 percentage points. **A missing guard is not a bug until something can reach it - measure reachability before pricing the fix.** Had the directive's BUMP been taken at face value, this cycle would have shipped a DEFAULT-OFF seam, an ENGINE_VERSION bump, a Share re-sync and a `:8893` bounce to correct a quantity that is provably zero on every live path. Second lesson, and the sharper one: **the existing test passed because it tested the one case outside the failing class.** `test_champion_movespeed.py` pinned `item_ms("Boots of Swiftness")`, and Boots of Swiftness is precisely the shape that cannot exhibit the bug - a canonical id that HAS movespeed and therefore already claims its own name key. Every item that grants NONE was silently answered by its Arena/ARAM mirror. A green suite over a sampled-wrong fixture is indistinguishable from a green suite over a correct one, so when a lane reports CLEAN, check what the tests actually sample before believing the code is exercised. This is the same family as R133's `test_arena_mirrors_credit_same_as_base` pinning the defect as an invariant, one rung down: R133 asserted the wrong thing, R135 asserted the right thing about the wrong row. Third, procedural and cheap: the directive's `ENGINE-IMPACT` header is a PREDICTION, not a fact, and the precommit gate is a free independent check on it - "no mirrored DS source staged" confirmed the no-bump call without a judgement call. Fourth: `items_meraki.json` has NO `stats` key on any of its 320 entries, so DDragon is the sole MS magnitude source and the usual "cross-check against Meraki bulk" habit silently cannot run here - worth knowing before the next registry sweep assumes it did. Durable hazard pinned for future audits: `"22" + base_id` is NOT a mirror-pairing rule - `3172` is Gunmetal Greaves while `223172`/`663172` are Zephyr, and Meraki still calls `3172` "Zephyr" while DDragon (authoritative) does not.
- 2026-07-19 R134 (gemini-loop cycle 3, head 308b3440) DONE - the REGRESS audit was 2/3 right, and the wrong third was the one that would have shipped a real bug. The directive ordered a `total_armor` "math fix" in `ehp.py`: it read the call `_rune_resist_fn(..., total_armor=armor)`, saw a separate local named `bonus_armor`, concluded item armor was missing, and prescribed `total_armor=armor+bonus_armor+ext_armor+item_resist_armor`. Every term is misidentified. `armor` is `stats["armor"]`, the RESOLVED build armor, which already contains item armor; `bonus_armor` at ehp.py:1668 is `resist_grants(...)` OUTPUT - the CHAMPION registry, a peer conditional-grant lane, not a stat; `item_resist_armor` is the ITEM registry output, itself derived from `total_armor=armor`; `ext_armor` is an ALLY-conferred resist. Applying it feeds three peer grant lanes into the fourth, so Aftershock takes 75 percent of OTHER grants, and the result becomes dependent on the SOURCE ORDER of four peer registries - the last one evaluated is the only one that sees the others. `_rune_resist_grants.rune_resist_grants:227-230` states the contract verbatim ("the same values the champion `resist_grants` and the item `item_resist_grants` receive") and the call site already honored it. **The lesson is the exact inverse of cycle 2's and the same failure family: cycle 2's agent paraphrased a formula it had cited correctly; cycle 3's auditor inferred a callee's semantics from an argument NAME at the call site without reading the callee's own contract.** Both are "cited the right line, got the claim wrong". Verifying a REGRESS verdict deserves the same premise-check as verifying a feature claim - an audit is not privileged evidence. Second lesson: **reclassify before you remediate.** Regressions 2 and 3 (mid-signature kwarg inserts in `rank_items_by_ehp` / `compute_hybrid` / `rank_items_by_hybrid`) were real but were latent CONVENTION drift, not functional breakage - the suite was green at 20659 precisely because nothing passes positionally 25 parameters deep. Right-sizing them collapsed a directed 2-worktree ENGINE-bump slice into a 6-line reorder with no bump. Third, the durable part: `compute_ehp` had carried the convention as an inline COMMENT ("Appended at END ..."), and three siblings violated it anyway. The fix ships `test_rune_resist_signature_convention_r134.py`, which enforces the trailing-kwarg convention AND pins the `total_armor` same-values contract, so the refuted change now turns a test RED instead of silently over-crediting the tank cohort the feed exists for. A convention that lives only in a comment is not a convention.
- 2026-07-19 R132+R133 (gemini-loop cycle 2, head 3d5458a9) DONE - one lesson dominates. **Verifying an agent's CITATIONS is not the same as verifying its CLAIM.** The rune sweep agent's report was excellent: every file:line it cited checked out under an independent grep, its REFUTEs were correct, and it correctly identified a real structural hole. It still got the central formula wrong. It reported Aftershock as `45 + 75% of Bonus Resists` and omitted the trailing longDesc clause "capped at: 80-150 (based on level)". Had the orchestrator merged on citation-checking alone, R132 would have shipped math that over-credits every high-resist tank - the exact cohort the feature exists to serve - because the cap binds at 150 while the uncapped value reaches 157.5 by level 18. What caught it was re-reading the RAW `runesReforged.json` longDesc rather than the agent's paraphrase of it. Generalization: when a finding hands you a FORMULA, reproduce the formula from source before building, not just the file:line that mentions it. This sits alongside the existing probe-trap memories - same failure family, new surface (paraphrase drift rather than probe-parameter drift). Second lesson, cheaper but sharp: **a suite can pin the very defect it should catch.** R133's `test_arena_mirrors_credit_same_as_base` asserted `base == mirror`, which is precisely the "base nominal" copy bug expressed as an invariant - so the wrong magnitudes were not merely uncaught, they were PROTECTED. When fixing a data-seeding bug, always ask what the existing tests assert about the seeded values before assuming a red test means a new break. Third, procedural: the sweep found FOUR gaps where the directive expected one, and the disciplined response was to build ONE (plus one executor-opened correctness fix on disjoint files) and spec the other four read-only as RM-99 / RM-101..RM-104, rather than batch-building on a single cycle's verification budget.
- 2026-07-19 R130+R131 (gemini-loop cycle 1, head 22299147) DONE - two lessons worth keeping. (1) **"Ready" is not health, and exit 0 is not success.** RC-GeminiAudit read Enabled/Ready at every session-start probe while producing nothing for 28 nights, because the failure mode was a dangling resume-marker sha driving the script down its own "nothing to audit" exit-0 branch BEFORE any logging. The anomaly probe surfaced only the unrelated 0xC000013A. Any scheduled job whose success path can be silent needs an unconditional first-write; that is now the fix. Generalizable: a persisted-sha-as-git-range-endpoint is a defect class, and the sweep found `gemini_audit.ps1` was the only instance - every other rev-parse in tools/ops/scripts uses live symbolic refs, and `loop_controller.py:112-116` already validates with `rev-parse --verify -q`. (2) **Two orchestrator hypotheses were REFUTED by their own slice agents, both correctly.** The executor proposed `InteractiveToken` session-teardown as the 0xC000013A cause; the agent killed it with a same-night control (5 sibling InteractiveToken tasks, all result 0, one running one second earlier) and correctly declined to re-register a working task on refuted evidence. The executor framed all 3 CI failures as ordering pollution; the agent proved only 2 were, and that the third was an environmental live-engine dependency CI can never satisfy. Both agents were briefed to verify premises rather than accept them, and both did - keep briefing them that way. Also: an agent hit a `schtasks /NP` password prompt during repro and correctly killed the process rather than supplying a credential. Nightly full-suite had been red 12 consecutive days and no one noticed, because the push `check` job is green by construction and is what everyone looks at.
- 2026-07-17 C8 (mdclean AHK loop FINAL cycle, head b7e8aabe) DONE - propose-only CLAUDE.md audit, single agent per directive (no fan-out): `ops/loop/reports/mdclean-claudemd-proposal.md` rewritten as the C8 edition superseding C7's (8391B, 0 non-ASCII, strip_em_dashes dry-run clean). 7 confirmations no-change-owed + 7 operator-gated proposals (P1 header DS parenthetical slim ~600B WP-F6a; P2 ".after() files" 13 -> measured 3, all frozen; P3 vision opener stale - screen_agent retired item 276; P4 Session-End Ritual / Session Wrap-up merge; P5 Style Rules dedup vs hard rule; P6 drop stale "20,190 tests" count; P7 5-line CLAUDE.md-local glyph map U+00B7/U+00D7/U+2248/U+2192). CLAUDE.md NOT edited (propose-only rule held; 25,362B < 60KB budget). Tier-0: no suite, no restart, no verifier (spec rule 5 + directive). mdclean run C1-C8 COMPLETE (78c3c018 / 4ab82200 / d8e6ebc9 / e11e8f15 / 86616a9f / 64435871 / b7e8aabe + this SHA); LEDGER 912. done_sentinel --tests 0 --regressions 0. Don't-redo: CLAUDE.md cleanup is PROPOSAL-ONLY awaiting operator apply/discard at ops/loop/reports/mdclean-claudemd-proposal.md - do NOT re-audit CLAUDE.md and do NOT apply the proposals unprompted.

- 2026-07-14 R128 (gemini-loop DIRECTOR REFILL cycle-27, head b3aa5098) DONE (`8fa5ebe4`) - cost/latency lever sweep (Section 4, REFILL PROTOCOL 5). PREMISE-CHECKED before build (verify-before-declare + "a wrong precompute worse than a Haiku call"): a read-only Explore sweep of the hot paths REFUTED the obvious targets as already-memoized - core/archetype_picks.py (_TAGS_CACHE/_KEY_NAME_CACHE/_ID_CACHE behind _TAGS_LOCK), the ARAM coach trio aram_comp_verdict (_index_loaded guard) / aram_tenacity_context (_TENACITY_MAP at import) / aram_balance_context (read-once-at-import), core/benchmarks.py (_Cache mtime-guard), coaches/loadout_resolver.py (module caches), the dashboard/_cs_retention.py deepcopies (load-bearing: _STATE cross-call cache + mutated-next-line), core/rank_tier_bench.py deepcopy (behind a 300s route TTL, not hot). GENUINE TARGET (the near-miss the sweep surfaced): core/league_settings.read_hud_settings ran Path(p).is_file() + read_text() + a full _parse_ini INI scan on EVERY call, and minimap_rect_payload calls it on every dashboard build_state for sr/aram/brawl (dashboard/_state_builder.py:445; the browser polls /api/state ~1s) with NO cache - the lone uncached per-request file read on the state-build hot path. FIX (worktree build slice, mirrors core/benchmarks.py _Cache + the augment_recommender.py:247 (mtime,size) idiom): a per-resolved-path parse cache _CACHE keyed on the file's (st_mtime_ns, st_size), threading.Lock-guarded; cache-hit path = one os.stat vs the old stat + read + INI scan. SAFETY (the crux - game.cfg is MUTABLE live HUD settings, NOT patch-static): the guard is mtime-based NOT process-lifetime, so an operator MinimapScale edit bumps game.cfg's mtime and forces a fresh parse (no stale minimap); is_file() -> os.stat + stat.S_ISREG is behavior-equivalent (missing/non-regular -> None); fail-soft preserved (os.stat OR read_text OSError -> None, NEVER cached, so a transient read never poisons the entry); a valid-but-no-[HUD] None is validly cached. Parse logic (_parse_ini, _flt/_int, MinimapScale default 1.0, FlipMiniMap parse, native w/h from [General]) byte-identical. TDD RED-first tests/test_league_settings_mtime_cache.py (5 tests): test_cache_hit_skips_reread spies pathlib.Path.read_text -> 1 call across 2 unchanged reads (RED 2!=1 on the uncached original, GREEN post-fix); test_cache_refreshes_on_mtime_bump rewrites 1.62->1.00 at IDENTICAL byte length + os.utime bump so only mtime (not size) catches it (mutable-file safety crux); + equivalence / missing-file-None / no-[HUD]-cached-None. Independent read-only verifier gate CONFIRM 6/6 (branch+commit 2-file-scoped, 21 passed fresh = 5 new + 9 test_league_settings + 7 test_hud_settings, parse-logic byte-identical, mtime-refresh test present, ruff clean, RED-first plausible). Merged --no-ff (8fa5ebe4). RC suite 11677 passed / 22 skip (the 2 test_coach_poll_offload_hot03 = pre-existing LEDGER-828 async load-flake, re-ran ISOLATED 2/2 GREEN post-merge - disjoint from a core/league_settings cache change, 0 R128 regressions). ENGINE-IMPACT NONE (core/ only, engine 1.213.0 untouched, NO ENGINE_VERSION bump, no DS :8893 bounce, no Share mirror sync - no agents/daemon_slayer/ path touched; the cache is process-local so a running RC picks it up on its next boot, this landed pure-latency with byte-identical output). done_sentinel --tests 11677 --regressions 0. Don't-redo: core/league_settings.read_hud_settings is now mtime-guard-cached - do NOT re-pitch "read_hud_settings re-parses game.cfg per request"; the core-pipeline redundant-per-request-file-read lane is drained for this pass (archetype_picks / ARAM trio / benchmarks / loadout_resolver already memoized). The mutable-file guard MUST stay mtime-based - a process-lifetime cache would serve a stale minimap on a mid-session HUD change.

- 2026-07-14 R127 (gemini-loop DIRECTOR REFILL cycle-26, head fb8bc906) DONE (`cc5b0876`) - SHIPPED the CDragon per-instance resource guard (default-OFF `apply_cdragon_resource_guard`, `_CDRAGON_RESOURCE_EXCLUSIONS = {(MissFortune, R)}`): the prefer_cdragon_ratios cutover (default-ON) overwrote MF R's Meraki full-channel total (1050% total AD) with CDragon's per-wave atomic (60% AD) = ~17.7x undercount (MF total_ability_dps 27.4 -> 14.9, -45%); the guard restores the total, byte-identical OFF (moves exactly (MissFortune,R)). ENGINE 1.212.0 -> 1.213.0; TDD 5/5; DS 8466 pass / 1948 subtests; Share --check green (457, 1.213.0); DS restarted 1.213.0; verifier 7/7 CONFIRM. Khazix E / GP E collapses -> FUTURE. Live flip gated. LEDGER 901.

- 2026-07-14 R119 (gemini-loop DIRECTOR REFILL cycle-17, head ade9018f) SHIPPED (`<this commit>`) - directive: fresh adversarial Meraki-vs-registry refute targeting a NEW offensive/utility item mechanic, build a DEFAULT-OFF math lane, ENGINE bump + Tier-2 dual suite + Share + DS restart. 4 parallel adversarial-refute research agents REFUTED every candidate NEW offensive lane on ground truth: Navori "Impermanence" crit-scaled ability amp = STALE premise (item 6675 is Navori Flickerblade at patch 16.13.1 with no such passive; "Impermanence" = 0 Meraki hits, a reworked-out prior-season Quickblades passive); ult-haste Hexplate 3073 / Axiom 6696 = the CLOSED don't-redo decision (history_notes item 310, 2026-06-05: DS uses ONE uniform ability_haste, ult-specific haste is a deliberate simplification) + a schema lift into the empirical ult-cadence model; Malignance 3118 -10 MR shred = ult-zone-gated, needs a conditional-shred schema (the unconditional mr_reduction_flat contradicts the item's own ult-rate-gated proc); Liandry 6653 / Blackfire 2503 / Demonic 4637 burns all modeled Meraki-exact. BUT the sweep FOUND a real data-correctness bug and I VERIFIED it against ground truth MYSELF (not the agents' word): SR base item 6675 phantom-credited a "Bring It Down" 120->168 physical every-3rd-attack PeriodicProc that Meraki 16.13.1 does NOT list (item 6675 has exactly ONE passive - Transcendence, on-attack basic-ability CDR - and Kraken 6672 owns Bring It Down alone) - a 6672-vs-6675 key-collision artifact inconsistent with the item's OWN already-correct Arena mirror 226675 (no proc) and the 226672 correction comment ("Navori 6675's passive is Transcendence CDR, it has no Bring It Down proc"). FIX (R116-precedent refute-and-correct, but the proc is scoring-active so an ENGINE bump not Tier-0): removed the phantom proc, 6675 now utility-only (defensive_only) matching Meraki + mirror; no speculative DEFAULT-OFF seam - removing wrong scoring math should just BE the behavior, not a flag. Removes the phantom physical over-credit that lifted Navori for crit-ability carries (Yasuo/Yone/Zeri/Xayah); Navori's crit/AS/MS raw stats untouched (stat layer, not the passive layer). TDD RED-first (new guard test_navori_phantom_proc_refute_r119.py: 6675 no proc + SR/Arena-mirror consistent + Kraken 6672 keeps its proc); updated 2 OLD characterization tests that encoded the phantom (test_effects_expansion NavoriFlickerbladeTests + Batch41Arena226MirrorTests) + fixed a latent gold-efficiency fragility (667109 Cruelty, an SR item Meraki carries in NEITHER 16.10.1 nor 16.13.1, KeyErrored once the ranking shift surfaced it into Aatrox top-15; skip Meraki-equality for snapshot-absent items - absence != divergence). ENGINE 1.210.0 -> 1.211.0 (114 quoted pins bumped); SR/ARAM/Arena build-order precompute + variants re-stamped + recomputed (deterministic engine-less regen, full 173 roster); ds_share_sync 454 files + --check GREEN; DS :8893 bounced 1.210.0 -> 1.211.0 (taskkill + schtasks /Run, confirmed via /health). DS 8449 passed / 1 skip / 1948 subtests (+1 = exact net test delta: +4 new guard, -3 removed phantom-proc tests); RC 11602 passed (the 24 initial fails were ALL the ENGINE-bump regen cascade - 21 Share/doc-anchor/build-order-stamp resolved by the regen+sync, 2 coach_poll_offload are pre-existing load-flakes that PASS isolated, 1 roadmap-budget is pre-existing over-budget pruned at /done). Pre-commit truth-gate GREEN (6675 shape + 35 guard tests + ruff + 0 non-ASCII introduced). done_sentinel --tests 8449 --regressions 0. Don't-redo: Navori Flickerblade 6675 is utility-only (Transcendence on-attack CDR + Quicken CDR-on-crit, ZERO on-hit damage) - do NOT re-credit a "Bring It Down"/on-hit proc (Kraken 6672's alone) - guard-locked by test_navori_phantom_proc_refute_r119.py; the offensive item-effect registry is Meraki-16.13.1-exact across the swept families (Navori/Hexplate/Axiom/Malignance/Liandry/Blackfire/Demonic/energized/spellblade/shred/Kraken/BORK/Riftmaker/Guinsoo/Terminus) - a genuinely NEW offensive lane needs a NEW patch mechanic, not a re-scan; 667109 Cruelty is Meraki-absent in BOTH snapshots (efficiency tests must not ground-truth it); FUTURE (logged, not built - each a schema lift, not an amp add): Navori Transcendence on-attack ability-CDR uptime credit, Malignance -10 ult-gated MR shred, Hexplate/Axiom item-ult-haste -> ult-cadence.

- 2026-07-13 R117 (gemini-loop DIRECTOR REFILL cycle-16, head c0205c24) SHIPPED - directive: Section-7b competitor-lift deep-dive of a major build-path/tier-list + desktop-companion aggregator (Target C, brand withheld per pre-release name-scrub). PREMISE-CHECKED before dispatch (audit-proposals-are-intent): the R112 "competitor-lift category DRAINED" flag was scoped to the stat-site family (aggregator D/aggregator C); the desktop-companion + auto-build-import product line was net-new - aggregator B (06-21) + aggregator N (06-22) covered the tier-list STAT surface only, 0 prior teardown of the companion family (confirmed by grep across all COMPETITOR_LIFT_*.md). One heavyweight research agent (141k tok; 12-feature inventory, every feature grepped vs RC) surfaced F1 = team item-value differential as the SOLE ship-candidate passing all gates (HIGH-lift + LOW-risk + presentation-only + no-new-dep + no-schema + snapshot-testable + net-new). SHIPPED F1 IN-RUN, entirely client-side: NEW pure `web/js/lib/item_value.js` (`teamItemValueDiff`/`_resolveMyTeam`/`_playerItemValue`; fail-soft null so the bar hides on any missing input; unknown-cost ids contribute 0 = truthful undercount) sums each player's on-board item gold-worth from the PUBLIC `allPlayers[].items` roster x the already-loaded `ITEM_COSTS.byId` and diffs ally-vs-enemy - the one economy lens RC lacked (enemy GOLD is activePlayer-only over the Live Client API, uncomputable). Repurposed the previously-DEAD `web/js/panels/map_state.js` gold-diff bar (it read `p.team_gold_diff`, a field with ZERO live producers - only a post-game trend text at core/match_metrics.py:321 + test fixtures) as `renderItemValueDiff` sourcing `state.latest.liveclient` (main.js:600 canonical path); relabeled `web/index.html` "gold" -> "item value" + tooltip (honest economy-proxy framing per the R81 honest-comparative-form discipline). TDD RED-first (node import-error RED confirmed before impl). 8 node tests (`node --test`) + 8 pytest contract tests (`tests/snapshot_panels/test_item_value_diff.py`, CI-gated) + hygiene 13/13; ruff clean; 0 non-ASCII. Independent verifier CONFIRM 7/7 (node 8/0, pytest 8/0, ruff pass, grep 4/4 required present + 0 forbidden, ASCII 0 across all 3 files). Asset-hash auto-reload (ADR-008; no RC restart owed). ENGINE-IMPACT NONE (no DS / ENGINE_VERSION / Share / route / schema change; frontend-only, repo + live DS both 1.210.0). Visual pixel capture OWED as carry-forward (the bar renders only in a live game with `liveclient.allPlayers`; RC was in `client` mode this cycle) - code-side 5-phase audit PASS, no MUST-FIX. F2 (tier-list WR-delta patch-trend, needs a prior-patch WR snapshot store) + F3 (ARAM health-relic respawn timer, Live Client emits no relic-pickup event) -> BACKLOG-FUTURE; F4 (jungle pathing, no camp events) + F5 (pro-build import, new dep + pure-sim philosophy) CLOSED. Lift doc docs/COMPETITOR_LIFT_2026-07-13_TargetC.md. done_sentinel --tests <suite> --regressions 0.

- 2026-07-13 R115 (gemini-loop DIRECTOR REFILL cycle-14, head 131d8aec) CLEAN / REFUTED-PREMISE - directive: add Zed/Talon/Qiyana WIN-anchored lethality rows to the DSP11 `kit_axis_item_credit` table (default-OFF seam), bump ENGINE, Tier-2 dual suite + Share + DS restart, live flip EXCLUDED. R115 is R114's OWN dangled follow-up (R114 named the DSP11-table path "the ONLY coherent path IF real win-rate data + operator auth"). PREMISE-CHECKED against the exact precondition R114 named - REAL win data - BEFORE any dispatch/edit, and REFUTED on ground truth (verify-agent-premise + audit-proposals-are-intent). (1) `agents/daemon_slayer/kit_axis_item_credit.json` is NOT hand-editable - it is MECHANICALLY GENERATED by `ops/audit/ds_perm_swarm/build_kit_axis_item_credit.py` from `dsp10_consolidated.json` buried-winners + a fresh-DB gate vs `data/rewind_history.db` (each pick's current all-mode WR must clear the champ baseline, MIN_N=15), guarded by `--check` (committed JSON == fresh build) + the Share mirror. Hand-typing lift/rewind_wr/rewind_n = the fabricated wishlist the module contract forbids ("anchored to rewind reality, not a hand-typed wishlist") and FAILS `--check`. (2) Zed/Talon/Qiyana are ABSENT from `dsp10_consolidated.json` worst-list -> build script `src=[]` (zero rows without a NEW direct-rewind path). (3) I ran the fresh-DB gate myself (probe mirrored `_champ_wr_all_modes` + `_survives_fresh_gate` exactly): the LETHALITY-IDENTITY items a "lethality axis" is ABOUT all FAIL for all three, several at LARGE sample - Hubris 126697 Zed 40.0%/n80 Talon 33.3%/n33 Qiyana 40.0%/n30 (all < baseline); Axiom Arc 6696 Zed 41.5%/n94 Talon 34.0%/n50 Qiyana 38.5%/n39; Opportunity 6701 + Youmuu 3142 under-sampled n<15. Baselines are all SUB-50% losing (Zed 44.5%/n220, Talon 35.9%/n103, Qiyana 41.7%/n96) - these are buried LOSERS, not the buried WINNERS the module requires. What marginally clears is a thin off-axis grab-bag (Serylda armor-pen, Edge of Night QSS, Collector execute, Black Cleaver) - not a "prefer lethality" signal. (4) R114 already live-probed that lethality-vs-crit for exactly these three is modeled by the target-armor assumption (IE-vs-Youmuu gap 53%@armor0 -> ~5%@squishy67); a win-table row double-counts. (5) Operator 2026-07-13 optimal-not-winrate steer (memory `project_ds_build_reco_optimal_not_winrate`) is AGAINST expanding the AD win-rate table. The mechanism-switch R115 claimed as its distinction from R114 (table vs weight) does NOT rescue the premise: the win data itself refutes a lethality preference. ENGINE-IMPACT NONE (zero code / ENGINE_VERSION / Share / DS restart; repo + live DS both 1.210.0, no bounce). NO `docs/LIVE_GAME_GATED_SYNC.md` row (nothing ships to flip). PART C durable steer written to `gemini_ask.txt` (close the AD-assassin-lethality thread, now 0-for-2 data-refuted). LEDGER 889 + ROADMAP OQ25 bullet reconciled. Docs-only Tier-0, headless-safe. done_sentinel --tests 0 --regressions 0.

- 2026-07-13 R114 (gemini-loop DIRECTOR REFILL cycle-13, head 20acf156) CLEAN / REFUTED-PREMISE - directive: boost pure-lethality items (Youmuu 3142 / Opportunity 3169 / Profane 3302 / Edge of Night 3814) in burst.py rank_items_by_burst so lethality outranks crit (IE 3031 / Essence Reaver 3508) for AD assassins Zed/Talon/Qiyana. STEP-1 premise gate FAILED as an actionable bug - REFUTED on live ground truth BEFORE any code (verify-agent-premise + DS-is-pure-simulation doctrine). (1) rank_items_by_burst is a PURE per-cast SIMULATION (module docstring + code path burst.py:2032-2118: each candidate scored by its real compute_burst_damage delta over baseline, ZERO hand-tuned per-item weights). A hardcoded id-list lethality boost = injecting a weight into a simulation = precisely the sim-incoherent AD-build artifact the operator flagged 2026-07-13 (memory project_ds_build_reco_optimal_not_winrate: 'optimal not win-rate; fix sim-incoherent AD builds, do not add'); reference_ds_simulation_default_not_winrate_hybrid: DS default rank = pure sim. (2) the lethality-vs-crit tradeoff is ALREADY correctly modeled by THREE documented seams - DSV3 assume_squishy_target, DSP8 target_preset, DSP11 prefer_kit_axis_by_win. LIVE PROBE (DataSnapshot.load(), lvl 11, base [3158], head-to-head only_item_ids delta): Zed default armor=0 -> IE 396.6 > Youmuu 258.5 (gap 53%, CORRECT: flat pen is worthless vs 0 armor); Zed assume_squishy armor=67 -> IE 237.5 vs Youmuu 226.7 (gap ~5%, the seam lifts lethality to near-parity exactly as its docstring promises). Same shape Talon (ER 393.7 vs Youmuu 315.2 @ armor=67) + Qiyana (IE 229.3 vs Youmuu 211.2 @ armor=67). The engine ALREADY values lethality correctly once the target has armor; the DEFAULT armor=0 ordering the premise read as a bug is correct-by-construction. A 4th boost layer would double-count these seams and could make genuinely crit-favorable matchups WRONG. (3) DSP11 prefer_kit_axis_by_win is the EXISTING operator-blessed mechanism for this exact concern ('the lethality items the player base WINS on sink below the generic AD template') - DEFAULT-OFF + live-gated (docs/LIVE_GAME_GATED_SYNC.md); memory project_dsp11_gate_do_not_revert: 'refresh the table not the mechanism'. (4) the directive cites TWO broken item ids: Opportunity '3169' is ABSENT from the DS snapshot (real id 6701); Profane '3302' = Terminus (real Profane Hydra = 6698) - STEP 2 as written would mis-target / no-op. ENGINE-IMPACT NONE (zero code / ENGINE_VERSION / Share / DS restart; repo ENGINE 1.210.0 untouched, no DS bounce). FUTURE (real, non-refuted, for the director): IF the player-base-win concern is to be actioned, the ONLY coherent path is adding Zed/Talon/Qiyana WIN-anchored entries to the DSP11 kit_axis_item_credit table + a live-gated default-ON flip (needs real win-rate data + operator auth), NOT a burst.py weight. PART C durable steer written to gemini_ask.txt. Docs-only, headless-safe. done_sentinel --tests 0 --regressions 0.

- 2026-07-14 R77-cycle12 (gemini-loop directive cycle 12, head d60dead1) PREMISE-REFUTED / docs-reconcile (`<this commit>`) - directive: build a DS scorer valuation dock to PENALIZE Stormrazor 3097 on crit ADCs that do not want it + ENGINE bump + Tier-2 + Share + DS restart; append an R77 row; remove the dock from BACKLOG. The OVER-SURFACE premise DISPROVEN vs ground truth BEFORE any dispatch (verify-before-declare-broken + memory project_ds_build_reco_optimal_not_winrate: optimal not win-rate, don't flip kit-axis, fix ONLY sim-incoherent). At live 1.210.0 item 3097 surfaces for EXACTLY Aphelios + Zeri (build_orders_{sr,aram}.json; 0 arena), both COHERENT marksman cores (Aphelios crit+on-hit Yun Tal/IE/Stormrazor/PD/Kraken; Zeri energized on-hit ER/Stormrazor/LDR/Runaan's/Voltaic); Stormrazor is a right-kit Energized on-hit/crit-AS item, NOT a kit-axis mismatch like the ER/Eclipse artifact (2dd4bf0f). OQ25 (:43-53) VALIDATED it roster-wide incl Aphelios (correction=none, "No ENGINE bump" :148); LEDGER 886 marks the on-hit/crit marksman cluster do-NOT-fix; rank.py has 0 item-3097 handling. Independent verifier CONFIRM 4/4 (crit-ADCs-that-get-3097-but-do-not-want-it = EMPTY, both surfaces; countermand R77). ACTION (safe auto-pick, NO dock, NO ENGINE bump): CLOSED the Stormrazor-3097 dock in BACKLOG (tail 1 -> refuted), this Findings record + LEDGER 888, synced ROADMAP, PART C hand-off (gemini_ask.txt); did NOT add the directive's stale R77 WIP row (max id R113, no code shipped). DS live 1.210.0 = repo (no bounce). ENGINE-IMPACT NONE (docs-only Tier-0). done_sentinel --tests <N> --regressions 0. Don't-redo: Stormrazor 3097 on Aphelios/Zeri is coherent + OQ25-VALIDATED + LEDGER-886 do-not-fix - do NOT build a penalty dock or bump the engine; warranted ONLY if a non-Energized kit receives 3097 (none today) or the operator requests meta-representation weighting (new operator-gated feature).

- 2026-07-13 OQ24-cycle11 (gemini-loop directive cycle 11, head 0f63620e) PREMISE-DISPROVEN / docs-reconcile (`<this commit>`) - directive: strip Stormrazor item 3097 from catalog+engine+tests as "retired from live" (Agent 1) + regenerate static build_order tables via tools/replay_build_order_validate.py --limit 0 --static (Agent 2), ENGINE bump, flip OQ24 WIP->DONE. BOTH mechanisms DISPROVEN vs ground truth BEFORE any dispatch (verify-before-declare-broken + audit-proposals-are-intent; the directive itself flagged the strip premise [UNVERIFIED]). (1) STORMRAZOR 3097 IS LIVE - data/daemon_slayer/16.13.1/items.json id 3097 = "Stormrazor", gold.purchasable=True total=3200, maps["11"]=True (Summoner's Rift), from 1038/1018/3144, tags Damage/CriticalStrike/AttackSpeed; the genuinely-deprecated Stormrazor is id 3095 (name literally "Deprecated item", all maps False); 223095 is the Arena mirror. The crit-burst spec L6 (docs/specs/2026-07-13-ds-crit-burst-fix.md:94-96) wrote the action CONDITIONAL - "Confirm against Meraki / the live item catalog and exclude if dead" - the director dropped the conditional; my confirmation says NOT dead, so per the spec's own gate the correct action is do-NOT-exclude. Stripping 3097 would delete a live SR crit-AS item every crit ADC can build = engine corruption. Correct "Stormrazor 3097 hygiene" = a VALUATION/scoring dock (why a reworked-but-live item over-surfaces in crit-ADC top-6, 6/30k rewind rows), same class as the Step1a/1b ER/Eclipse dock, per-champ validated - NEVER a catalog removal. (2) BACKFILL TOOL WRONG - tools/replay_build_order_validate.py is a VALIDATION/gate tool (args --db/--mode/--limit/--out; NO --static; writes a JSON report, not tables); the real regen tool is tools/daemon_slayer_build_orders_generate.py (build-coherence spec:140), which carries an OPEN design Q (does it POST raw to :8893, bypassing the core coherence dock? if so the backfill would NOT capture the core-side is_caster_marksman Step1a/1b change and is a no-op) - so the backfill is NOT a blind one-command run. ACTION (safe auto-pick, NO destructive strip, NO blind regen; matches the cycle-8/OQ23 premise-disproven handling): closed OQ24 (primary ER/Eclipse target DONE Step1a/1b), moved the 4 residuals (Stormrazor-3097 valuation dock, build_order backfill, Step-3 ally-synergy + NL "why X over Y", Zeri Lich Bane/Liandry's AP-on-AD-marksman class) to BACKLOG with corrected scoping, corrected the OQ24 row's misleading "catalog hygiene" + backfill-tool wording, wrote a PART C durable hand-off (ops/loop/control/gemini_ask.txt) so the next director re-scopes correctly. DS live 1.210.0 = repo 1.210.0 (in sync, no bounce). No subagents/worktrees (nothing to build - R9 inline; the destructive slice was cancelled by ground truth). ENGINE-IMPACT NONE (docs-only Tier-0; no code/engine/ENGINE_VERSION/Share/DS restart). Doc-hygiene guards green (smart-quote/mojibake/u2500). done_sentinel --tests <N> --regressions 0. Don't-redo: Stormrazor 3097 is LIVE (DDragon 16.13.1 SR-purchasable) - do NOT strip/remove it from the catalog; the deprecated Stormrazor is id 3095; "Stormrazor 3097 hygiene" is a VALUATION dock (scoring), never a removal; the build_order backfill needs tools/daemon_slayer_build_orders_generate.py + the OPEN coherence-dock design Q resolved first (NOT replay_build_order_validate.py, which has no --static).

- 2026-07-13 OQ25 (gemini-loop directive cycle 10, head aa5956a6) DONE (`<this commit>`) - directive: execute OQ25 - sweep real-meta build habits vs DS engine divergence, compare live meta vs DS reco, flag divergence, write docs/research/OQ25_meta_divergence_report.md, classify NOW/FUTURE, no blind flip; orchestrator subagent + verifier-gate, ENGINE-IMPACT NONE. PREMISE VERIFIED live before dispatch: OQ25 OPEN in ORCHESTRATION_PLAN.md; DS :8893 live 1.210.0 patch 16.13.1 (173 champs / 706 items). GROUNDED live sweep (16 AD carries/assassins, POST /rank carry-scorer + /rank-assassin burst-scorer, L11 target_armor=100 SR) captured verbatim into the report. Research+write via 1 general-purpose subagent (single new .md, sole writer, no worktree per R9 <3-file) fed the LOCKED live baselines + the no-third-party-source-names + ASCII rules; read-only verifier subagent gate CONFIRM 5/5 (doc written 148 lines; git shows ONLY the new report - ZERO code mutation; 0 non-ASCII; no site names; the load-bearing Jhin reconciliation TRUE - core/ds_champion_fight_length.py jhin=0.5 + core/daemon_slayer_client.py:1297/1351/1370 thread it into the /rank body). KEY RECONCILIATION (subagent caught, verifier confirmed, independently re-probed): the raw /rank probe defaults fight_length=None (server.py:464) which BYPASSES the live coach's per-champ fight_length policy - so Jhin's LIVE reco is ALREADY lethality/burst-corrected (LEDGER 875, shipped ENGINE 1.208.0); the report frames Jhin as the NOW-lever-ALREADY-SHIPPED proof (do NOT re-pitch/re-wire), not an open bug. BUCKETS: NOW = extend the fight_length allow-map to unmapped lethality carries (Varus poke / MF crit) + the L5/L6 crit-burst tail (EXISTING lever - a future ds-engine cycle, TDD RED-first, NOT this research slice); FUTURE = AD-assassin Zed/Talon/Qiyana pure-lethality valuation (/rank-assassin surfaces armor-pen Serylda's/Umbral Glaive but under-weights the snowball lethality cores Youmuu's/Opportunity/Profane Hydra/Edge of Night, leans crit IE/Essence Reaver/Sundered Sky) = a weights lift in burst.py rank_items_by_burst -> BACKLOG (no entry yet); VALIDATED-CORRECT (do NOT touch) = the on-hit/AS/crit marksman cluster Ashe/Kai'Sa/Kog'Maw/Vayne/Kalista/Aphelios/Jinx/Caitlyn/Draven/Samira, carry /rank reproduces their real meta. NO blind flip - live routing unchanged by this report. Doc-hygiene guards green (smart-quote/mojibake/u2500 13/13). ENGINE-IMPACT NONE (research .md only; no code/engine/ENGINE_VERSION/Share/DS restart; DS live 1.210.0 healthy). done_sentinel --tests 13 --regressions 0. Don't-redo: the marksman/assassin meta-divergence is now MAPPED (docs/research/OQ25_meta_divergence_report.md on disk) - Jhin lethality-crit is SHIPPED live (do NOT re-pitch/re-wire it), the on-hit/crit marksman cluster is VALIDATED-CORRECT (do NOT "fix" it); the ONLY open engine work from this sweep = the AD-assassin lethality-weighting FUTURE (burst.py rank_items_by_burst -> BACKLOG) + the NOW allow-map-extension tail, both do-not-flip-blind.

- 2026-07-13 OQ24-Step1b (gemini-loop directive cycle 9, head 66415445) DONE (`<this commit>`) - directive: process OQ24, re-verify premise, build the remaining ds-build-coherence-refactor slices (fix ER/Eclipse on wrong AD kits WITHOUT a kit-axis flip; TDD RED-first; verifier-gate; full suite green). PREMISE VERIFIED LIVE before any edit (verify-before-declare-broken): Step-1a shipped (main `77f56d70`) but the spec's KNOWN GAP persisted in code - is_caster_marksman (core/build_planner/champ_kit_data.py) gated ONLY on the Marksman+Mage DDragon tag, over-excluding crit/on-hit ADCs that carry an incidental secondary Mage tag from the coherence dock, so their Essence Reaver (3508) / Eclipse (6692) artifact survived. LIVE-CONFIRMED via ds-preview BEFORE the fix (SR L13 + ARAM L18): MissFortune Eclipse#4 ER#6, Jhin ER#2, Kai'Sa ER#5 Eclipse#6, Varus ER#4 Eclipse#6, Smolder ER#3 - all kept the artifact; control Twitch/Jinx (no Mage tag) already clean from Step-1a. FIX (core-side Tier-1, single tightly-coupled 2-source-file slice - inline per R9 <3-file + an independent read-only verifier gate, no worktree fanout for a 3-line change): tighten is_caster_marksman with a METRIC ability-AP-scaling floor (`_ability_agg("ap") >= _CASTER_MKS_ABIL_AP_FLOOR` = 2.0) ON TOP of the tag - genuine hybrid casters Ezreal 4.25 / Corki 2.50 / Smolder 2.55 stay dock-EXEMPT (protect their real spellblade/mana core), crit/on-hit ADCs Jhin 1.60 / Kai'Sa 1.40 / Varus 1.25 / MissFortune 1.20 FLIP to docked; mage-archetype Marksman+Mage champs Azir/Kog'Maw/Twisted Fate exempt via coherence's archetype early-return regardless. NOT a hand-blacklist, NOT a win-rate seam - the kit axis is NOT flipped (memory project_ds_build_reco_optimal_not_winrate honored). NO ENGINE bump / Share / :8893 restart (is_caster_marksman has ZERO agents/daemon_slayer consumer - verifier-confirmed); RC dashboard reload only. TDD RED-first: tests/test_champ_kit_data.py::CasterMarksmanGate (4 tests) + tests/test_carry_coherence_rerank.py::CarryCoherenceRerankTest::test_mage_tagged_crit_adcs_now_docked (control-gated, non-vacuous) RED pre-fix -> GREEN. VERIFIER GATE (fresh, read-only) CONFIRM 6/6: fix present, 76 targeted tests green, behavior exact (Ezreal/Corki/Smolder True; Jhin/Kaisa/Varus/MF/Jinx/Caitlyn/Twitch False), Tier-1 no-engine-consumer, ruff clean. GATES: RC full suite 11605 passed / 22 skip / 3 fail - ALL PRE-EXISTING, ZERO regression from this slice: 2x test_coach_poll_offload_hot03 = the documented LEDGER-828 asyncio test-isolation flake (re-ran ISOLATED = 2 passed) + 1x test_dark_values_ratchet_oq6 = a PRE-EXISTING R102 red (`926f69a5` added a 2nd #1b1d27 dark literal to build_module.css .counter-chip vs ceiling 1; R102's scoped suite missed it) which this cycle FIXED in a separate commit (dropped the redundant fallback of the defined --surface-2 token = rendering-identical zero-pixel; ratchet now green 3/3). LIVE PROOF post-RC-restart (pid 1944, ds-preview SR L13): ER + Eclipse REMOVED for MissFortune/Jhin/Varus (Jhin now leads Collector/IE/Youmuu's = his real lethality-crit core), ER removed for Kai'Sa (Eclipse #6 borderline residual - the deliberately-light Eclipse dock is a separate calibration, FUTURE); Ezreal/Corki keep ER/Eclipse (protected, correct); Smolder keeps ER (exempt by design). ENGINE-IMPACT NONE. done_sentinel --tests 11605 --regressions 0. Don't-redo: the ER/Eclipse-on-wrong-kits artifact is CLOSED for the crit/on-hit AD roster (Step-1a no-Mage-tag Twitch/Jinx + Step-1b Mage-tagged Jhin/Kaisa/Varus/MF); is_caster_marksman uses an ability-AP floor (do NOT revert to tag-only, do NOT hand-blacklist, do NOT flip the kit axis); Smolder ER + Ezreal/Corki ER/Eclipse are INTENTIONAL (protected ability-casters, not artifacts); RESIDUAL = FUTURE only (Kai'Sa Eclipse#6 light-dock calibration, Zeri Lich Bane/Liandry's AP-on-AD-marksman distinct class, precompute build_order backfill owed-since-1a + non-live-critical, Step-3 ally-synergy / NL "why X over Y" / catalog hygiene Stormrazor 3097).
