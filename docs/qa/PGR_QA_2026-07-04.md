# PGR (Post Game Review) Page QA - Operator Review 2026-07-04

Method: operator per-page UI-QA (feedback_operator_ui_qa_method; the models are
docs/qa/CHAMP_SELECT_QA_2026-07-03.md, HOME_QA_2026-07-04.md, LOBBY_QA_2026-07-04.md).
6-mapper read-only MAP -> AskUserQuestion advocate rounds (Claude advocates,
operator rules) -> inline build (thin slice, verifier-gated) -> 5-phase fixture
audit + ui_recon proof both widths BEFORE commit.

Part of E11 (Hextech reskin across surfaces). KEY FINDING up front: PGR was
ALREADY substantially Hextech-reskinned by the s220 PGR-reframe program (S2-S5
shipped 2026-06-08) and its 4 sub-panels already passed a 5-phase UI audit on
2026-06-30 (LEDGER 702, R47). This pass is therefore a THIN hygiene slice + a
documented "reskin-complete" verdict, NOT a palette cutover.

## Surface verdict (validated 2026-07-04, router-traced)

The operator sees TWO PGR containers out-of-game, both reusing the `lm-*` CSS
(so last_match.css tokenization covers both):

- **`#view-last-match`** (web/index.html:1633) - the MAIN live PGR, auto-shown on
  game-end via `_viewSaveManual("last-match")` (main.js:637) on LCU phase
  EndOfGame/WaitingForStats/Lobby. Clobber risk LOW (post-game auto-derive is
  non-urgent -> "home"; the manual last-match sticky is cleared only by the next
  urgent champ-select). Tabs: AI-Analysis (default) / Graph / Build / Review.
- **`#view-historical-pgr`** (web/index.html:1961, historical_pgr.js) - the ARCHIVE
  PGR reached by clicking a match row on Session/History. Reuses `.lm-hero-*`
  classes, no tabs, no sub-panels (deliberate omit); own `#hpgr-*` ids, isolated
  state (no cross-talk with live PGR).

`?overlay=1` HUD hard-pins active-match (main.js:915-917) and never renders PGR.
Primary viewport = companion window 920x1280 portrait (1920 desktop secondary).

recon.py:40 root-selector for PGR was STALE (`#last-match-view`); real id is
`#view-last-match`. Fixed in this slice (needed for ui_recon crop metadata).

## Source inventory (#view-last-match, top-to-bottom)

| Section | container/class | data source | file:line |
|---|---|---|---|
| Takeaway headline | #lm-takeaway | /api/last-match quick_review | last_match.js:554 |
| Hero: identity | .lm-hero-identity | match{champion,kda,grade,mode} + enriched.win + /api/post-game-rubric | last_match.js:591 |
| Hero: stats grid (8) | .lm-hero-stats-grid | match + enriched.damage/vision/support + carry_normalized | last_match.js:1090 |
| Hero: rank-tier compare | .lm-hero-rank-compare | localStorage + _RANK_TIER_AVERAGES | last_match.js:472 |
| Build-WPA strip | #pgr-build-wpa-mount | /api/item-wpa + /api/skill-wpa | pgr_build_wpa.js (S2) |
| Loadout strip | #pgr-loadout-mount | enriched.runes / arena_augments / spells | pgr_loadout.js (S5) |
| Build tab: team-comp table + MVP/SVP | #lm-tc-table | enriched.roster[] + _rosterScores | last_match.js:688 |
| Build tab: lane-compare | #pgr-lane-compare-mount | enriched.roster[] (SR-only) | pgr_lane_compare.js (S4) |
| Graph tab: team-agg bars + timeline | #lm-chart-wrap / #lm-tl-wrap | enriched.roster/teams/timeline | last_match.js:1166 |
| AI-Analysis tab: phases + win-prob + quick-review | #lm-wpa-wrap / #pgr-winprob-mount | /api/post-game-wpa | post_game_phases.js + pgr_winprob.js (S3) |

Backend: /api/last-match (routes_last_match.py:255), /api/post-game-wpa
(routes_post_game_wpa.py:157), /api/post-game-rubric (routes_post_game_rubric.py:171),
/api/item-wpa, /api/skill-wpa, /api/dictionary/{runes,augments}. Primary DB =
data/rewind_history.db. Modes SR/ARAM/Arena branch on queueId + gameMode; phases,
win-prob, lane-compare are SR-only (fail-soft hidden for ARAM/Arena by design).

## The s220 boundary (scope guard)

s220 IS the "aggregator-G-style PGR reframe": S2 build-WPA (77c3cc3), S3 win-prob
(92c6a0f), S4 lane-compare (e6cd350b), S5 augment loadout (c162e5bd) - all SHIPPED,
functionally complete 2026-06-08 (LEDGER 285/286/287/356). The 0-100 hero score is
a separate deferred item (ROADMAP-S3). PGR is also earmarked for a FUTURE "slow
deliberate per-page UI/UX design review" (LEDGER 519).

- RESKIN-SAFE (this slice may touch): tokenize raw hex to Hextech, ASCII hygiene,
  dead-code removal, recon tooling.
- S220-OWNED / DO NOT TOUCH: pgr_*.js render logic, the rubric score, routes,
  builders_last_match.py, layout/panel-order, the by-design ARAM/Arena
  lane-compare hide.

## Findings (mapper-confirmed, my verification noted)

### Colors - the reskin crux, RESOLVED
The 4 s220 sub-panel CSS files (pgr_winprob/loadout/build_wpa/lane_compare) use
`var(--hextech-token, #hex-fallback)` form for EVERY color (e.g.
`var(--signal-good, #6ec977)`). The Hextech token ALWAYS wins at runtime; the #hex
is an inert dead fallback. So M4's "off-palette Tailwind colors" was a false alarm:
the panels are ALREADY fully Hextech-tokenized. last_match.css (1454 lines) is
likewise fully tokenized - grep `:\s*#hex` returns ONLY the 4 augment-rarity lines.

| # | Finding | file:line | Verdict |
|---|---------|-----------|---------|
| C1 | Sub-panels fully var()-tokenized (dead #hex fallbacks only) | pgr_*.css | ALREADY DONE - no action |
| C2 | Augment rarity tiers bare hex (bronze/silver/gold/prismatic) | pgr_loadout.css:119-122 + last_match.css:1451-1454 | SANCTIONED brand tint (exact lobby .lv-rank-* precedent, RC2_REDESIGN accepted-7) - KEEP |

### Fonts - already sanctioned
All 6 sub-16px font sizes carry explicit "item 182 v2.1 OPERATOR EXCEPTION"
comments (role-badge micro-labels, WPA-foot caption, MVP badge, chart-axis ticks).
Unlike lobby, there are NO un-sanctioned raw sizes to bump. No action.

### Hit-targets - non-issue
All 16 sub-44px slots are non-interactive display icons (items, portraits,
rune/aug icons). Every interactive button/tab respects --hit-min. No action.

### Dead code (D) - the actual work
| # | Finding | file:line | Verdict |
|---|---------|-----------|---------|
| D1 | `_setEnrichedBuild()` - 0 callers, pre-S3 BUILD-section renderer (final inventory + spells strip) | last_match.js:657 | CONFIRMED (grep 0 calls) - REMOVE |
| D2 | `_setDsPicks()` - 0 callers, pre-S3 DS-picks renderer | last_match.js:1135 | CONFIRMED - REMOVE |
| D3 | Clear-stub for `lm-ds-picks` + `lm-actual-build` (DOM ids no longer in index.html; dead no-ops) | last_match.js:1382-1385 | CONFIRMED orphaned - REMOVE |
| D4 | Orphaned BUILD-section CSS block (.lm-build-*, .lm-inventory, .lm-item, .lm-summoners, .lm-summ - 0 usages after D1-D3) | last_match.css:1040-1116 (~77 lines) | CONFIRMED orphaned - REMOVE (decommission-completeness) |

### Tooling
| T1 | recon.py PGR root selector stale | ops/runtime/ui_recon/recon.py:40 | FIX `#last-match-view` -> `#view-last-match` |

Sibling note: recon.py has 6 more stale `#X-view` selectors (session/history/replay/
user-builds/build-insights/settings) that the respective E11 surface sessions will
fix as each is audited (same pattern the lobby slice used for its own selector).

## Advocate rounds (rulings) - operator 2026-07-04

- **Scope: THIN hygiene slice + ship.** PGR palette is already done + R47-audited;
  a full content/density walkthrough would collide with s220 + the reserved
  LEDGER-519 design review. Ship the dead-code removal + recon fix + this verdict.
- **Augment rarity tiers: KEEP** as sanctioned brand tint (C2). No CSS touch.
- **Dead JS: REMOVE** `_setEnrichedBuild` + `_setDsPicks` (D1-D2). During build this
  expanded to the full BUILD-section decommission (D3 clear-stubs + D4 orphaned CSS)
  per feedback_decommission_completeness_sweep + the lobby D1-D5 JS+CSS precedent.
- **DEFERRED (named, not built):** backend dead passthroughs (ds_picks/weights_used/
  totals/avg_purchase_time_s, M5) - no UI impact, follow-up chip; the 6 sibling
  recon selectors (per-surface as audited); the reserved LEDGER-519 PGR design review.

## Build + verification (2026-07-04)

Inline slice (under the ~3-file worktree threshold, R9). Committed = 2 web files:
- web/js/panels/last_match.js: removed _setEnrichedBuild + _setDsPicks + the 4
  orphaned clear-stub lines; refreshed the :422 explanatory comment.
- web/css/panels/last_match.css: removed the ~77-line orphaned BUILD-section block.

Local-only (NOT committed): ops/runtime/ui_recon/recon.py PGR root selector
`#view-last-match`. ops/runtime/ is gitignored (.gitignore:114) and recon.py is a
scratch harness ("NOT committed" per its docstring); the fix persists on disk for
this + future local ui_recon runs but never enters git. Git commit = 4 ins / 142 del.

Net: pure dead-code removal, ZERO visual delta (the removed code rendered nothing -
the BUILD-section DOM was already gone from index.html in s219 v3).

- **Grep gate:** 0 remaining references to _setEnrichedBuild / _setDsPicks (except
  the doc comment) and 0 to any lm-build*/lm-inventory/lm-item/lm-summ*/lm-actual-
  build/lm-ds-picks class or id, repo-wide.
- **Syntax:** node --check last_match.js OK; py_compile recon.py OK.
- **Scoped suite (fresh): 444 passed / 0 failed** (tests/snapshot_panels/ + all
  tests/test_last_match_*.py), incl. the ASCII test, PGR/historical-PGR/tabs-reframe
  DOM snapshots. Tier-0/1 (dead-code, no schema/engine/item-effect) per R5.
- **ui_recon proof both widths (SR mock):** companion 920x1280 + desktop 1920x1080,
  0 pageerrors, 0 horizontal overflow (worstRight == viewport), rootSel resolves to
  `#view-last-match` (recon fix proven). PGR renders identically - hero band, tabs,
  all 4 sub-panels, win-prob graph, Hextech palette intact.
- **5-phase fixture audit: SHIP, no MUST-FIX** (zero visual change). STRUCTURE /
  TYPOGRAPHY (item-182 exceptions untouched) / HIT-TARGETS (unchanged) / ASCII
  (improved - removed a U+2192 arrow) / HIERARCHY all OK.
- **Verifier gate: PASS** (independent verifier subagent, fresh state). 0 orphaned
  callers (only the doc comment survives), 0 banned glyphs on added lines, 444
  passed / 0 failed on an independent re-run, recon selector `#view-last-match`
  confirmed, no out-of-scope file touched.
