# RC2 E11 Hextech Reskin - Batched Verification Sweep + Closure 2026-07-04

Closes the E11 "Hextech reskin across surfaces" stage. After the PGR and History
per-page QA passes both revealed their surfaces were ALREADY Hextech-reskin-complete
(the surfaces were reskinned incrementally during the R30 per-page design reviews +
explicit Hextech commits, not left raw), the operator chose a BATCHED verification
sweep over the remaining out-of-game surfaces instead of the full per-page UI-QA
method one-surface-per-session.

Method: 4 read-only verification mappers (one per remaining surface), each checking
5 axes - token adoption (bare hex vs var(--token)), dead JS/DOM, sub-16px fonts,
sub-44px hit-targets, prior-work boundary - and returning a COMPLETE / HAS-DELTA
verdict. Reference: web/css/tokens.css (--fs-xs=16px floor, --hit-min=42px, Hextech
palette). Live backend probed where relevant.

## Per-surface verdict

All 9 out-of-game surfaces are Hextech-reskin-complete:

| Surface | Container | CSS | Verdict | Provenance |
|---|---|---|---|---|
| home | #home-overlay / #view-home | home.css | DONE (UI-QA method) | LEDGER 768 (round-2) |
| champ-select | #view-champ-select | champ_select_view.css | DONE (UI-QA method) | LEDGER 765/766 |
| lobby | #view-lobby | header.css .lv-* | DONE (UI-QA method) | `c6670b19` LEDGER 772 |
| PGR | #view-last-match / #view-historical-pgr | last_match.css + pgr_*.css | DONE (UI-QA method) | `83418d2b` LEDGER 773 (s220 token-first + R47; BUILD dead-code purge) |
| history | #view-history | header.css .history-* | COMPLETE (verified) | R30 `d015ef34` |
| session | #view-session | header.css .session-* | COMPLETE (verified) | R30 `b1ca9931` |
| user-builds | #view-user-builds | header.css .ub-* | COMPLETE (verified) | Hextech `10b8155f` + R30 `4d91edfc` |
| build-insights | #view-build-insights | build_insights.css (own) | COMPLETE (verified) | Hextech `10a15ab0` + R30 `c1a8fa93` |
| settings | #view-settings | header.css .settings-* + panel_visibility.css | COMPLETE (verified) | R30 `eefc2140` |

For each swept surface (history/session/user-builds/build-insights/settings) the
mapper confirmed: 100% var(--token) adoption (0 genuinely-bare hex in the surface
namespace), 0 dead JS functions / 0 orphaned DOM ids, 0 sub-16px fonts, 0 sub-44px
interactive hit-targets. No reserved future review exists for any of them (only PGR
carries the reserved LEDGER-519 design review).

## Noted, not actionable (both recommend LEAVE)

- **`#1c1c2a` on `.view-section-btn:hover, .view-tab:hover`** (header.css:539). The
  ONLY genuinely-bare hex found across the whole sweep. It is SHARED chrome (every
  view's tab hover, not surface-specific), a bespoke darker hover shade near but not
  equal to --surface-3 (#1C2A3D). Tokenizing it would shift the hover color AND touch
  every view's tabs (regression surface). Treated as a sanctioned shared-chrome hover
  shade, same disposition class as the augment rarity tiers (LEDGER 773) - LEAVE.
- **`#session-start` guarded reference** (main.js ~1772). A `getElementById` +
  `if (el)` no-op for a DOM row removed during the R30 session de-redundancy. Inert
  (guard makes it a null no-op). Trivial optional cleanup, zero runtime cost - LEAVE.

## Overlay HUD (the one remaining E11 render target)

The in-game overlay HUD (`?overlay=1`) is a DISTINCT render target (hard-pins
active-match; never shows the out-of-game views). It is Hextech via its OWN lane -
the HEADLESS OVERLAY-POLISH RUN against the greenlit Hextech design
(docs/design/RC2_DESIGN.html), reconned clean 2026-06-24, and 5-phase audited at R72
(LEDGER 760, `7d099a1c`, 2026-07-03). It is live-game-gated for final in-game
eyeball, tracked in the overlay-polish lane / docs/LIVE_GAME_GATED_SYNC.md - NOT a
gap in E11's out-of-game reskin.

## Verdict

**E11 (Hextech reskin across surfaces) = DONE.** All 9 out-of-game surfaces are
reskin-complete (4 via the operator per-page UI-QA method this cycle + earlier; 5
verified complete via this batched sweep). The overlay HUD is Hextech via the
separate overlay-polish lane (live-gated eyeball owed there, not here). No code
change was required by the sweep - it is a documented completeness verdict. RC2_PLAN
E11 flipped to DONE; PROGRESS banner re-derived.
