# Home Page QA - Operator Review 2026-07-04 (IN PROGRESS)

Method: top-to-bottom operator walkthrough, Claude as advocate (per
feedback_operator_ui_qa_method; docs/qa/CHAMP_SELECT_QA_2026-07-03.md is the
model). 6-mapper read-only inventory over the Home render, then AskUserQuestion
advocate rounds, then worktree slices (verifier-gated, 5-phase audit + ui_recon
visual proof before commit).

## Surface note (validated 2026-07-04)

Home renders in the **rc-shell companion window** (mainWindow, plain
`https://legion-rc:8888`, shown OUT-OF-GAME; hidden in-game), NOT the `?overlay=1`
compact HUD (that hard-pins active-match, main.js:927-929). The retired Chrome
dashboard is gone as a standalone browser tab; the same web app now paints inside
the Electron companion window. **Primary viewport = 923x1316 portrait companion**
(config.js:23-25, reflow @media max-width:1200px); 1920x1080 desktop is secondary.
`.home-body` 2-col grid collapses to 1-col under 1200px, so out-of-game the
operator sees the narrow portrait stack. Visual proof: `ops/runtime/ui_recon/recon.py home`
renders both widths against `#home-overlay` (no overlay=1).

Files: web/index.html (#home-overlay, lines 140-421) / web/js/main.js (home block
3092-4008) / web/css/panels/home.css / dashboard/builders_home.py + route
dashboard/routes_history.py:_serve_home_summary (/api/home/summary, 20s poll).

## Source inventory (top-to-bottom, portrait companion)

| # | Element | id / file:line | Data source | Notes |
|---|---------|----------------|-------------|-------|
| H1 | Greeting (time-of-day) | #home-hero-time / main.js:3332 | client clock | "Good evening" flavor |
| H2 | Sub-text (games today + streak) | #home-hero-sub / main.js:3348 | today + streaks (match_history.db) | |
| H3 | Rank badge (tier + WR%) | #home-hero-rank / main.js:3448 | LCU live ranked | hidden when unranked |
| H4 | L20 W/L pip strip | #home-hero-rank-wl / main.js:3471 | rewind_history.db last20 | hidden if no results |
| H5 | Season WR (ranked 90d) | #home-hero-season-wr / main.js:3503 | rewind_history.db q420/440 | hidden if no ranked |
| H6 | Momentum headline (up/down/flat) | #home-hero-headline / main.js:3360 | today momentum verdict | big center text |
| H7 | Find Match CTA | #home-hero-cta / main.js:3924 | opens queue picker modal | |
| H8 | Stat chips + sparklines (KDA/CS/Gold 14d) | .home-hero-chip / main.js:3561 | match_history.db trends | sparkline needs >=2 pts |
| H9 | Hero bg motif (champ splash) | #home-hero-bg / main.js:3795 | top this_week/recent champ | |
| X1 | Icon sprite defs (static) | .home-icon-sprite / index.html:219 | static SVG | feeds hidden tiles |
| X2 | 7 quick-action tiles | .home-actions / index.html:269 | click-wired | **display:none** (RC2 cut) but DOM+wired |
| X3 | Find Match picker modal | #home-find-match-picker / index.html:313 | queue ids | hidden until CTA |
| C1 | Tonight's Pick (+Good/Bad/Ugly+Advisories) | #home-coach-pick / main.js:3640 | this_week top by avg_kda | <=2 games suppresses Good+Bad |
| C2 | Weekly Digest (by mode) | #home-weekly-digest / main.js:3743 | match_history.db 7d by mode | per-mode good/bad/ugly |
| C3 | Last Build (6 slots) | #home-coach-build / main.js:3701 | postgame_stats.db latest local | |
| B1 | Recent 5 match cards | #home-recent-list / main.js:3092 | match_history.db last 5 (non-TFT) | see pre-verified findings |
| B2 | This Week bar chart | #home-week-list / main.js:3214 | match_history.db 7d top-5 champs | |

Shared chrome (NOT home-specific, likely a separate header QA): top dual-row
header pills auto-hide off-home (main.js:1033); view-menu is the real out-of-game nav.

## A. Pre-verified findings (mapper-confirmed) - RULINGS PENDING

| # | Finding | file:line | Verdict | Ruling |
|---|---------|-----------|---------|--------|
| A1 | `_homeRenderServices` targets `#home-services-list` DELETED 2026-04-30 - silent no-op DEAD JS; backend still computes unused `services[]` (health.json + vision probe) | main.js:3403,3409 / builders_home.py:265-279 | CONFIRMED | |
| A2 | `.home-trends` CSS block dead - no `#home-trends` in HTML (trends moved into hero chips) | home.css:423-467 | CONFIRMED | |
| A3 | `.home-grid` legacy 4-col dead selector | home.css:37 | CONFIRMED | |
| A4 | 7 action tiles `display:none` but DOM-present + click-wired (ghost: restore or delete) | index.html:269 / home.css:757 | CONFIRMED | |
| A5 | Recent-5 `win` field sent per row but UNUSED - stripe is grade-based, not W/L (W/L data available) | builders_home.py:186-188 / main.js:3108 | CONFIRMED | |
| A6 | Recent-5 `mode_subtype` always null (dead passthrough, no queue_id store) | builders_home.py:165,185 | CONFIRMED | |
| A7 | Recent-5 `items` `[]` for pre-ingest rows -> hatched empty slots (most rows) | builders_home.py:184 / main.js:3162 | CONFIRMED | |
| A8 | Combo show-logic reads `trends.hidden` never set by `_homeRenderTrends` (fragile inference) | main.js:3731 | CONFIRMED | |
| A9 | KDA pill + self-spells `display:none !important` (header chrome, header.css) - separate-page scope | header.css:170,177 | CONFIRMED | out-of-scope? |

## B. Element rulings - PENDING (advocate rounds)

Round 1 (hero cluster) - 2026-07-04:

| # | Element | Ruling |
|---|---------|--------|
| H1 | Greeting line | REMOVE (fluff on a solo tool) |
| H2 | Sub-text (games today + streak) | KEEP |
| H3 | Rank badge (tier + WR) | KEEP |
| H4 | L20 W/L pip strip | KEEP |
| H5 | Season WR (ranked 90d) | REMOVE - duplicates Rank WR at coarser window. Implies `season_wr` payload + `_compute_season_wr` render becomes dead (backend cleanup in ACT). |
| H6 | Momentum headline | KEEP but GATE to >=3 games today (hide below sample) |
| H7 | Find Match CTA | KEEP (auto) |
| H8 | Stat chips | KEEP KDA + CS chips; REMOVE Gold/min chip. Implies `gold_per_min` trend may go unused (verify before stripping backend). |
| H9 | Hero bg motif | KEEP (auto) |

Round 2 (dead/hidden/modal) - 2026-07-04:

| # | Element | Ruling |
|---|---------|--------|
| X2 | 7 action tiles | RESTORE as mocked - keep hero Find Match CTA as primary; restore the other 6 (PGR/Session/History/Replay/Builds/Settings) as a 2-col tile grid; DROP the redundant Find Match tile. Portrait companion favors the tile launcher over the hidden view-menu. |
| A1 | Services dead JS + services[] payload | REMOVE both (footer health-dot is the single source) |
| A2 | `.home-trends` dead CSS | REMOVE |
| A3 | `.home-grid` dead CSS | REMOVE (grep test-guard first) |
| X3 | Find Match picker queue set | KEEP full set (mirrors real client queues) |

Round 3 (combo cluster) - 2026-07-04:

| # | Element | Ruling |
|---|---------|--------|
| C1 | Tonight's Pick | KEEP champ + reason + Good/Bad/Ugly (>=3-game gated); DROP the Advisories sub-row (duplicates footer advisory-count). Implies `_homeMirrorAlerts` + `#home-pick-section-3` removal. |
| C2 | Weekly Digest (by mode) | REMOVE - overlaps This Week + Pick. Implies `_homeRenderWeeklyDigest` + `_home_weekly_digest` backend + `#home-weekly-digest` removal. |
| C3 | Last Build (6 slots) | REMOVE - duplicates Recent-5 card#1 item strip. Implies `_home_last_build` backend + `#home-coach-build` removal. |

Round 4 (body + recent-5 data) - 2026-07-04:

| # | Element | Ruling |
|---|---------|--------|
| B1 | Recent 5 cards | KEEP; REPLACE grade side-stripe with W/L (win=green / loss=red / null=neutral); grade demoted to badge only. |
| A5 | Recent-5 `win` field | WIRE - now drives the W/L stripe (was fetched-but-unused). |
| A6/A7 | Recent-5 `mode_subtype` null / `items` empty | NO display change this pass. Treat as data-completeness: CHIP (1) items backfill for pre-ingest rows, (2) queue_id store to populate mode_subtype. |
| A8 | Combo show-logic fragility | FIX incidentally - combo now wraps ONLY Tonight's Pick (Digest + Last Build removed), so show iff pick present. |
| B2 | This Week row density | DEFERRED - blocked: Recent 5 consumes the portrait fold, This Week not yet visible to judge. |
| -- | Recent depth / 1-col stack order | DEFERRED - revisit after this slice ships + is seen on the real portrait companion render. |

## D. ACT plan (settled set - ships this pass)

FE (web/js/main.js home block + web/index.html #home-overlay + web/css/panels/home.css):
remove greeting (H1); remove Season WR (H5); gate momentum >=3 games (H6); remove
Gold/min chip (H8); restore 6 secondary tiles as 2-col grid, drop Find Match tile,
keep hero CTA (X2); remove dead `_homeRenderServices` (A1); remove `.home-trends` +
`.home-grid` dead CSS (A2/A3); W/L stripe on Recent-5 (A5/B1); drop Advisories sub-row +
`_homeMirrorAlerts` (C1); remove Weekly Digest render (C2); remove Last Build render (C3);
simplify combo show-logic (A8).

BE (dashboard/builders_home.py): remove `services[]` (A1); remove `_home_weekly_digest`
+ `weekly_digest` (C2); remove `_home_last_build` + `last_build` (C3); remove `season_wr`
render dependency IF home-only (verify `_compute_season_wr` not shared before strip).

DEFERRED to a follow-up round (after ship + visual proof): This Week row density,
Recent-5 depth, 1-col portrait stack order. CHIPS: items backfill, queue_id store.

## C. Decisions log

- 2026-07-04 MAP complete (6 mappers). Surface validated = companion window portrait.
  ADVOCATE rounds starting.
