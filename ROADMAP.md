# Amberstone - Roadmap

## WHERE WORK LIVES - this file is THE tracker

**One tracker rule (operator-directed 2026-07-26):** open work lives HERE. If another doc
claims to be a "source of truth" or "single source of truth" for open work and is not in the
table below, it is a SUBPLAN - scope it to its program or archive it. Do not start a rival list.

| doc | holds | may I put open work in it? |
|---|---|---|
| `ROADMAP.md` (this) | ALL open work, NOW / NEXT / LATER, stable `RM-NN` ids | **YES - this is the one tracker** |
| `BACKLOG.md` | aspirational tier, overflow only (ROADMAP is CI-budgeted < 80KB) | only if it does not fit here |
| `docs/LIVE_GAME_GATED_SYNC.md` | rows that need the operator IN a live game | only live-gated rows |
| `docs/DS_SWEEP_TRACKER.md` | **authoritative `RM-NN` id registry** - take ids from here, never from ROADMAP prose | no - ids only |
| `docs/LEDGER.md` | append-only per-item completions | no - history |
| `docs/ROADMAP_HISTORY.md` | relocated shipped/closed narrative | no - history |
| `WAKEUP_NOTES.md` | session continuity, auto-pruned to 3 | no - per-session |
| `docs/specs/**`, `docs/_archive/2026-07-28-research-consolidation/**` | per-item spec-first output, one per item | no - they are INDEXED by a ROADMAP row, never a rival plan |

Program subplans (scoped, NOT rival trackers): `docs/ORCHESTRATION_PLAN.md` (loop director,
machine-read each cycle), `docs/RC2_PLAN.md` (RC 2.0 program only),
`docs/OVERLAY_BUILD_MASTER_PLAN.md` (overlay build spec).

---

_NOW / NEXT / LATER buckets with stable `RM-NN` item ids (assigned 2026-07-17, mdclean C2; ids never renumber - new items take the next free id). Shipped/closed entries relocate verbatim to `docs/ROADMAP_HISTORY.md` (append-only); per-item ledger in `docs/LEDGER.md`; aspirational work in `BACKLOG.md`; live-gated drains in `docs/LIVE_GAME_GATED_SYNC.md`._

> **Size-budget relocation passes (2026-07-18, 2026-07-25, 2026-07-26, 2026-07-29, 2026-07-30, 2026-08-30, 2026-09-01).** Shipped/closed narrative and BLOCKED rows' measurement evidence move VERBATIM to the dated blocks in `docs/ROADMAP_HISTORY.md`; purely live-gated rows reroute to `docs/LIVE_GAME_GATED_SYNC.md`. Each surviving row keeps its verdict, its named prerequisite or residual, and a pointer. **No RM id has ever been deleted and no open work dropped** - every id below is still reachable, as a bullet or as a routed row in the tables. Per-pass rationale + per-bullet evidence: the same dated blocks.

> **Decommissioned 2026-06-24 (operator-directed, ADR-012): the RC<->Peer cross-Claude bridge + lessons cross-project sync were fully REMOVED - do NOT re-pitch either.** Notice relocated verbatim to `docs/ROADMAP_HISTORY.md` (2026-09-07e block).

> **Removed 2026-07-09 (operator-directed): the `budget_saver` lean / 8B-local-model fallback was fully REMOVED - do NOT re-pitch a budget-saver / lean-profile / 8B-local fallback.** Notice relocated verbatim to `docs/ROADMAP_HISTORY.md` (2026-09-07e block); see memory `project_budget_saver_removal`.

---

## NOW - active lanes

> **NEXT-5 TRIAGE (2026-08-02) relocated VERBATIM to `docs/ROADMAP_HISTORY.md`**; its two durable conclusions survive as the rows themselves (RM-122 operator-present ONLY; RM-118 seam debt + the RM-35..RM-48 GAPs as the headless-actionable pair). Pointer text archived in the 2026-09-07e block.

- **RM-384 / RM-385 / RM-386 / RM-388 ALL SHIPPED 2026-09-08** - the cross-repo inbox responder RUNNER and its tails; two rows relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (`## 2026-09-11` block). Bodies in `BACKLOG.md`; LEDGER 1364 / 1368 / 1369; measurements in `docs/RESPONDER_RUNNER_SPEC.md`. **READ that block before re-opening any of these ids** - it carries the RM-388 fence: do NOT just add a `_trim`, because `responder_metrics.jsonl` is the M1/M2/M3 evidence base, which is why the ledger ROTATES instead.
- **RM-397 / RM-399 SHIPPED 2026-09-09..10 and RM-398 (partial) / RM-400 / RM-401 / RM-402 ALL CLOSED 2026-09-10 - four rows relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block) on the doc-size budget.** Bodies in `BACKLOG.md`; LEDGER 1380 / 1383 / 1384 / 1385 / 1386. **READ that block before re-opening any of these ids** - it carries the measured verdicts and four DO-NOT-RE-PITCH fences: RM-398's own MECHANISM is REFUTED (do not re-pitch crediting subagent transcripts, nor `_negated` reuse for `CLAIM_COUNT`), RM-401 forbids a part-of-speech / noun-class / determiner narrowing of `CLAIM_PUSH`, and RM-402 forbids an idiom / word-sense exclusion list or a bash-window discriminator for `CLAIM_PUSH` plus any touch of the `_negated` window. **The RM-399 headline stays here because it is not about the tool:** specifying the sibling-name sweep found FIVE REAL sibling PROJECT NAMES in RC's own tracked prose, ALL FIVE ALREADY PUBLISHED, four redacted AT HEAD ONLY (`f6cf005bb`) with **NO history rewrite** - the honest claim is "the sweep half is ARMED with a measured escape rate above zero", never "sibling names cannot leak". The fifth is the JOINT RE-PIN row below.
- [!] **JOINT RE-PIN AWAITING RSC (relayed 2026-09-10) - the FIFTH escape, and the only one RC cannot fix alone.** It sits at `tests/test_loop_concurrency.py:492`, INSIDE the `SHARED_SHA256` byte-identical block (474-539), as a two-word name SPLIT ACROSS A COMMENT LINE WRAP - not contiguous in the raw blob, which is why every whole-token sweep ever run here called it clean. It ships as a KNOWN, NAMED, VISIBLE exception (`tools/sibling_name_sweep.py:112`, asserted REPORTED rather than suppressed - never a silent allowlist). Removing it moves bytes in a cross-repo pinned pair, so it is a JOINT act: **do NOT re-pin unilaterally, and do NOT regenerate the digests from local disk.**
- **RM-387 SHIPPED 2026-09-08; RM-403 / RM-404 / RM-405 / RM-406 / RM-407 ALL SHIPPED 2026-09-11 - six rows relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (`## 2026-09-11` block) on the doc-size budget.** Bodies in `BACKLOG.md`; LEDGER 1368 / 1388 / 1389 / 1391 / 1392 / 1393. **READ that block before re-opening any of these ids** - it carries their fences: do NOT widen `ROW_NOTE_RE` (RM-387); do NOT widen the nearest-preceding-id binding rule (RM-403); do NOT hand-escape a caret (RM-404); RM-405 is NOT licence for a general bare-`except Exception` sweep; RM-406's census cannot detect a NEW leak, is blind to SUBPROCESSES and scans TRACKED files only; and RM-407's `agents/agent3_testing/suite` is DECLARED EXCEPTED rather than wired, over a measured 1-in-3 flake. **Next free id = RM-410** (authoritative pointer in `docs/DS_SWEEP_TRACKER.md`; RM-408 and RM-409 are consumed).
- **RM-410 SHIPPED 2026-09-11** (`7ff5fc853`; LEDGER 1396; plan row R229) - the direct byte / atomicity test for `lib/ddragon/fetch._atomic_write_json`; its "no test calls that writer" premise was REFUTED before any code was written. **RM-411 FILED to `BACKLOG.md`** ("Reliability / hardening") - 7 test call sites patch a destructive stdlib primitive process-wide; the `-n 8` reason for it is refuted, do not re-file it. **The figure on the line above is SUPERSEDED - the live next-free id is RM-412**, pinned in `docs/DS_SWEEP_TRACKER.md`.
- **RM-412 SHIPPED 2026-09-12** (`48ac8986a`; LEDGER 1399; body on the OSS extraction blueprint row in `BACKLOG.md` under "Reliability / hardening") - **C1 of the OSS extraction blueprint is EXECUTED: `oss/win32_atomic_io/`, a stdlib-only src-layout package carrying the atomic-write primitives lifted out of `core/polled_json.py`.** `core/polled_json.py` is UNMODIFIED - 20-plus live importers, so the package is a SIBLING, not a replacement - and the two copies are pinned by `tests/test_oss_win32_atomic_io_drift.py`, which compares AST-normalized EXECUTABLE LOGIC and ignores docstrings/comments because the RC-specific prose was deliberately scrubbed for sharing. `PolledJsonFile` was deliberately EXCLUDED (zero production instantiations; RM-264 holds adopt-or-remove). **Four fences carried from the build:** the one real sibling-name leak was in `__pycache__`, not source, so a `git`-based publish never showed it and a `tar`/`cp -r` publish would have shipped it - and that sweep is inherently TRANSIENT, since any pytest run repopulates those files; the package first declared itself all-rights-reserved INSIDE a public Apache-2.0 tree, which is the self-contradicting-repo trap CLAUDE.md's own license gate warns about, committed against our OWN tree; `py.typed` is verified STRUCTURALLY only, never observed inside a built wheel (no `setuptools` on this interpreter); and a package-local `.gitattributes` pinning `eol=lf` is load-bearing, because `core.autocrlf=true` plus a root `.gitattributes` covering only `*.py`/`*.md` would have checked the new `.gitignore` out as CRLF in a fresh clone - green locally, red on clone. **C2 (partial) and C3 (NO) stay unexecuted; read the BACKLOG row before re-pitching either.** **Next free id = RM-413** (authoritative pointer in `docs/DS_SWEEP_TRACKER.md`).

- **SHIPPED / CLOSED pointer cluster - four lines relocated VERBATIM 2026-09-10 to `docs/ROADMAP_HISTORY.md` (2026-09-10b block), covering RM-203 / RM-208 / RM-209 / RM-211 / RM-212 / RM-214 / RM-220 / RM-250 / RM-286 / RM-287 / RM-322 .. RM-328 / RM-329 .. RM-341 / RM-342 / RM-343 / RM-344 .. RM-363 (incl. RM-345 / RM-347 / RM-348 / RM-349 / RM-350 / RM-357 / RM-359 / RM-360 / RM-361) / RM-364 / RM-365 / RM-367.** READ that block before re-filing against any of them - it carries the fences verbatim: do NOT re-derive the RM-203 denominator; the `_write_status` + `god`-spelling fence is re-stated at `BACKLOG.md:440`; RM-343's filed premise was WRONG (the defect is worktree REUSE staleness, not `git worktree add`); the RM-367 truthy-`"Unknown"` consumer-contract fence; and do NOT hand-pick LANE 10 rows while `ops/loop/queue_loop.py` runs. **Still OPEN, bodies in `BACKLOG.md`: RM-358** (the frozen `lcu/lcu_client.py:176-178` half), **RM-366** (OPERATOR-GATED on the frozen `lcu/lcu_client.py:191`), **RM-368**, **RM-369**, **RM-370 .. RM-379**, **RM-378**, **RM-380**, **RM-381**, **RM-382**, **RM-383**. Next-free id figures in those lines are SUPERSEDED - the live pin is the guarded one in `docs/DS_SWEEP_TRACKER.md`.
- **RM-291 CLOSED 2026-09-04** - row relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (`## 2026-09-11` block); body in `BACKLOG.md`, Sweep A LEDGER 1331, Sweep B LEDGER 1332.
- **RM-292 OPEN (filed 2026-08-31, lane 8 cycle 34; LANE 8 + LANE 7, Tier-1)** - four findings off the `coaches/tft_coach.py` audit (LEDGER 1300), none in the audited file; the LIVE one is a wrong `mode` discriminator in `core/feature_policy.py`. Body in `BACKLOG.md`.
- **RM-293 OPEN (filed 2026-08-31, lane 8 c35; LANE 8 + LANE 7, Tier-1)** - three findings off the `lcu/snapshot_shape.py` audit (LEDGER 1301), none fixed in-slice: no negative caching on the per-summoner lookup, an unguarded `bytes.decode()`, and an un-called re-export bypassing the module's coercion. Body in `BACKLOG.md`.
- **RM-294 OPEN (filed 2026-08-31, lane 8 cycle 36; LANE 8, Tier-1)** - one finding off the `agents/agent7_context/warm_session.py` audit (LEDGER 1302), deliberately NOT fixed in-slice: `send()` holds `self._lock` across the upstream Anthropic call, so `stats()` and `close()` - which take the same lock - can block for the full 30s request timeout, and a supervisor shutdown racing an in-flight send waits on it. Narrow the lock to the history mutation. Deferred because it changes the concurrency model of a live supervisor component and the audited defect did not require it. **Also filed here, LANE 7:** `agents/agent3_testing/suite/` is collected by NEITHER `pytest tests/` NOR `pytest agents/daemon_slayer/tests`, the only two trees CI runs (`ci.yml:130`, `:478`), so every test in that directory is UNRUN - measured, not inferred. Body in `BACKLOG.md`.

- **RM-295 OPEN (filed 2026-08-31, lane 8 cycle 37; LANE 8 + LANE 7, Tier-1)** - three findings off the `core/smoothed_rates_101qq.py` audit (LEDGER 1303), none fixed in-slice. (a) A persistently-degraded live source now KEEPS the previous snapshot instead of wiping it, but the only signal is a `log.warning` - nothing surfaces "duo-synergy has served a frozen snapshot for N hours" to `/api/health/all`, and `source()` still reports the ORIGINAL seed, which is honest about the data and silent about the staleness. (b) `coverage()` has ZERO production consumers (tests only) - a diagnostic nothing reads. (c) LANE 7: the empty-publish-over-good-cache class is generic to every live-first / static-fallback cache in `core/`; two were checked this cycle and BOTH had it, so the population is a CANDIDATE SET to sweep, not a proven defect count. Body in `BACKLOG.md`.

- **RM-297a-d OPEN (2026-08-31, lane 8 c39; LANE 8, one FROZEN-ADJUDICATED, Tier-1)** - `lcu/lcu_rune_writer.py` audit (LEDGER 1305): a 3-char prefix test that deletes USER rune pages, a 2nd rune writer in the frozen LCU client, an unbounded 1 Hz retry, two hand-rolled atomic writers. Bodies in `BACKLOG.md`.

- **RM-302 SHIPPED 2026-09-04 (LEDGER 1331 - both fallback calls bounded at 20.0 s; the p99 source the row named has n=0, so `data/coach_trace.jsonl` n=200 p99 10950 ms was used and disclosed; the bound is PER ATTEMPT so the true ceiling is 60 s); RM-301 / RM-303 OPEN (filed 2026-08-31, lane 8 cycle 43; all LANE 8, all Tier-1)** - three findings off the `tft/tft_live_analysis.py` audit (LEDGER 1309), none fixed in-slice because each is a design call, not a defect with one obvious repair. **(301)** `force_scan()` read-modify-writes the polled data file with NO lock while every other writer runs under `self._lock`. Per-writer scratch name + a logged failure landed this cycle; it was ALREADY tmp+replace at HEAD under a distinct name, so NOT 'made atomic'. Still unserialized. **(302)** neither `messages.create` call carries a timeout, so both inherit the SDK default 600s inside a 1.5s loop - vision coaching can stop for ten minutes silently. **(303)** the champion whitelist and trait scrub run on `shop_units` ONLY, so `board_units` / `bench_units` reach the panel and the prompt unfiltered, and the `hp` clamp is int/float-gated so a string `"999"` bypasses it. Bodies + acceptance in `BACKLOG.md`. **Next free id = RM-304.**

- **RM-314 SHIPPED 2026-09-04 (LEDGER 1331 - four untimed `messages.create` sites bounded with four argued values 20/15/10/10 s; the row's "12 constructions" is REFUTED, true count 17, still ZERO passing `timeout=`); RM-315 / RM-316 OPEN (2026-08-31, lane 8 c47; RM-315 LANE 7 Tier-0, others LANE 8 Tier-1)** - three findings off the `tft/tft_pbe_engine.py` audit (LEDGER 1313), none fixed in-slice: four untimed `messages.create` sites, eleven stale ROADMAP id pointers, one silent degraded-mode writer. Bodies in `BACKLOG.md`. **Next free id = RM-322** (authoritative pointer in `docs/DS_SWEEP_TRACKER.md`).

- **RM-312 SHIPPED 2026-09-11 (LEDGER 1390); RM-313 still OPEN (filed 2026-08-31, lane 8 cycle 46; LANE 8, Tier-1)** - two findings off the `lcu/champ_select_shape.py` audit (LEDGER 1312). **(312 - SHIPPED)** `dashboard/_lcu_inprocess.py` caught `Exception` and returned `None` with NO log line - the module had no logger at all - so every in-process L3 fault degraded SILENTLY to the `:8889` relay hop L3 exists to remove. The filed citation was ACCURATE, not stale. Now a throttled WARNING (log on change of fault signature, then at most once per 60s). **The contract is deliberately UNCHANGED: it still returns `None` and the not-connected path still logs nothing** - the defect was the silence, not the degrade. **`str(exc)` is never emitted** because an LCU payload carries PUUIDs and this repo is PUBLIC; only exception type, basename, lineno and func. **The DIRECT-CALLER half this row reported but did not repair is RM-405, SHIPPED 2026-09-11 (`fc0058441`, LEDGER 1391)** - `dashboard/_state_builder.py` re-swallowed the same fault one frame higher, and an `ImportError` on the lazy import is a class this row provably cannot cover. **(313 - OPEN)** `_team_picks` forwards `championId` with the LCU type unchanged, so a JSON string `"67"` is emitted as a string; s155 (`tools/lcu_agent.py:919-921`) measured that some LCU builds DO string-type these fields. Not fixed in-slice because it changes emitted value TYPES, a consumer contract change that needs its own slice. Bodies + acceptance in `BACKLOG.md`. **Next free id = RM-314.**

- **RM-310 SHIPPED 2026-09-06 (`b9ca3c3c9`, LEDGER 1354)** - eleven sibling sites stay open on the RM-310 row in `BACKLOG.md`; also closes the `rofl_archive` third of RM-251's `_atomic_write_bytes` bullet. Body archived verbatim in `docs/ROADMAP_HISTORY.md` (2026-09-07e block).

- **RM-304 / RM-305 / RM-306 / RM-307 / RM-308 OPEN (filed 2026-08-31, lane 8 cycle 44)** - five findings off the `core/event_callouts.py` audit (LEDGER 1310). The non-finite-float class was fixed in-slice; these five change panel CONTENT or need a live capture. **(304 LANE 4)** two siege lines run 15 and 13 words against an 8-word budget - the guard now SEES them (its sweep passed no event kwargs and graded 3 of 8 kinds) and grandfathers them; shortening is the UI call. **(305)** Baron shows for a 30s slice of its 20-plus-minute window. **(306)** "Elder UP now" fires with ZERO drakes taken. **(307 LIVE-GATED)** `_liveclient.py` and `decision_detector.py` resolve the same `KillerName` against different rosters; the fixture is parallel by construction so the suite cannot settle it. **(308)** duplicate inhibitor rows, identical tag and text. Bodies + acceptance in `BACKLOG.md`. **Next free id = RM-309.**

- **RM-300 OPEN (filed 2026-08-31, lane 8 cycle 42; LANE 7, Tier-1)** - **28 tracked `.py` files carry 640 non-ASCII codepoints against the CLAUDE.md 7-bit rule, and NO guard can find them.** `tools/precommit_gate.py` scans ADDED lines only, so an already-committed file is invisible to it forever; `tests/test_u2500_hygiene.py` walks an ALLOWLIST, so it cannot discover a violator. Census: U+00B7 x300, U+2550 x144, U+2192 x105, plus CJK and emoji over 34 codepoints; none exempt under `_ascii_exempt`. Body + acceptance in `BACKLOG.md`. **Next free id = RM-301.**
- **RM-299a-c OPEN (filed 2026-08-31, lane 8 cycle 41; RM-299a LANE 7, RM-299b + RM-299c LANE 8, all Tier-1)** - three findings off the `dashboard/routes_pickban.py` audit (LEDGER 1307), none fixed in-slice. (a) `tools/citation_audit.py:388` mines claim tokens ONLY from BACKTICKED spans, so a citation naming a bare symbol grades `RESOLVES / UNCHECKED` - it proves the line NUMBER is in range and checks nothing about what is on it; measured on rows 22 and 27 of `docs/RC2_QA_CONSOLIDATED.md`. (b) Nothing caps GET query work anywhere; `_handler.py:160` caps POST bodies and 20 route modules parse csv params - a CANDIDATE SET to check, not a proven defect count. (c) Two `except Exception` guards around DS imports at `:709` / `:716` can still silently kill the cleanse advisory - cycle 8 fixed the import path, not the swallow. Bodies + acceptance in `BACKLOG.md`. **Next free id = RM-300.**

- **RM-296 OPEN (filed 2026-08-31, lane 8 cycle 38; LANE 8, Tier-1)** - five findings off the `dashboard/routes_loadout.py` audit (LEDGER 1304), none fixed in-slice. (a) The `/api/lcu-cmd` edge checks the VERB and forwards every sibling key unexamined; the prose claiming the vision server revalidates was MEASURED FALSE (it is a pipe), and the real second gate is `tools/lcu_agent.py`, whose field coercion is incidental rather than contractual. (b) `/api/loadout/apply` discards the queue id, so its three pushes are structurally unpollable and a downstream rejection cannot reach the UI even in principle. (c) `/api/lcu-cmd` is outside the `_handler.py:532` control-endpoint auth list that gates four less-consequential paths - inert today (`RC_DASH_TOKEN` unset), an inconsistent posture rather than an open hole. (d) The three `push_*` flags are bare truthiness, so `{"push_runes":"false"}` pushes runes. (e) Three sequential 2s `urlopen` calls hold a request worker for up to 6s. Bodies in `BACKLOG.md`.
- **RM-317 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - `core/riot_api.py` cache keys `match:v5:<id>` and `match:v5:timeline:<id>` are prefix-ambiguous into a never-expiring table; NOT reachable today, filed because the fix is a migration of 18575 rows. Body in `BACKLOG.md`.
- **RM-318 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - the six `core/decision_detector.py` detectors are not shape-hardened against the Live Client envelope; cycle 12 made a retyped inner field VISIBLE (heartbeat `detector_errors`) but not prevented. Measured 5/6, 2/6, 2/6 and 1/6 raising across four malformed shapes. Body in `BACKLOG.md`.
- **RM-255 / RM-256 OPEN (filed 2026-08-30, lane 8 cycle 22; RM-255 LANE 8, RM-256 LANE 7-or-8, both Tier-1)** - the still-open half of the cycle-22 batch: **(255)** `lib/http/client.py`'s blocklist FAILS OPEN on both failure paths and is re-evaluated per request, on the chokepoint for every third-party fetch in the tree, with zero tests - plus `certifi` absent from both requirements files, making its `_SSL_CONTEXT` inert; **(256)** `agents/agent2_backend/ws_server.py:46` binds `0.0.0.0` with no auth on `/ingest` or `/push`, zero tests - PROBE REACHABILITY BEFORE FIXING. Bodies in `BACKLOG.md`. RM-253 (cycle 25) and RM-254 (cycle 24) are CLOSED; the original four-id row is archived verbatim in `docs/ROADMAP_HISTORY.md`.
- **RM-258 / RM-259 / RM-260 OPEN (filed 2026-08-30, lane 8 cycle 23; RM-258/259 LANE 8, RM-260 LANE 7; all Tier-1 except RM-260 Tier-0/1)** - three findings from the cycle-23 audit of `performance_tracker.py`, none of them in the audited file: **(258)** the bare-`os.replace` exposure is a repo-wide class of ~45 runtime cross-process writers, not the three RM-250 names - including `coach_integration/_coach.py:120`/`:715` (the coaching-data files every overlay polls) and `ops/rc_supervisor.py:1605`, which clears `restart_trigger.txt` bare while defining a retry helper at `:77`; **a re-count must search BOTH `os.replace` and `Path.replace`**, since the latter spelling hides ~100 further sites and is the one CLAUDE.md prescribes. Land RM-254 first so 45 callers are not converted onto a helper with a known tmp-collision bug; **(259)** a failed rating write logs `"Performance rating saved"` anyway (`app/_game_lifecycle.py:236`, FROZEN - needs an adjudicator) and leaves a stale-mtime file that `agents/supervisor.py:799-805` then copies into the next post-game summary, possibly from a different mode; **(260)** the experimental-builder hook that reads the API key is DORMANT (`mark_active()` has zero callers), so `performance_tracker.py:406` is an unreachable dead read - and the only thing keeping the key out of `logs/` on the reachable-in-principle path is `%s` formatting at `experimental_builder.py:237`, since `repr()` of the `UnicodeEncodeError` from a non-ASCII key file embeds the key. Bodies in `BACKLOG.md`. **RM-258 is the highest-leverage of the three.**
- **RM-254 CLOSED (2026-08-30, lane 8 cycle 24) + RM-261 / RM-262 / RM-263 / RM-264 OPEN** - the `core/polled_json.py` audit closed RM-254 (concurrent writers shared one destination-derived scratch file, so the repo's atomic-write CONTRACT measurably TORE its destination) plus four more defects. Open spawn: (261) ~96 modules still hand-roll the same scratch name, incl. the shared `coaches/_base_coach.py:85` `safe_write` and 11 further sites enumerated in the c30 addendum; (262) four U+2192 in a live Haiku prompt at `coaches/arena_coach.py:159-160`; (263) no `fsync` anywhere, so atomic against a reader but not `taskkill /F`; (264) `PolledJsonFile` has ZERO production instantiations. Bodies in `BACKLOG.md`; detail in `docs/LEDGER.md` 1290. **Full row body + the c30 addendum's 11-site enumeration relocated VERBATIM 2026-08-31 (lane 8 cycle 38) to `docs/ROADMAP_HISTORY.md` for the ROADMAP size budget - READ IT before re-opening any of these ids.**
- **RM-277 / RM-278 / RM-279 / RM-280 OPEN (filed 2026-08-30, lane 8 cycle 28; RM-277 LANE 8 FROZEN-ADJUDICATED, RM-278 + RM-280 LANE 8, RM-279 LANE 7; all Tier-1 except RM-279 Tier-0/1)** - the residual findings from the `core/coaching_data_lock.py` audit. **RM-199 is CLOSED** (LEDGER 1294). Bodies in `BACKLOG.md`; full row body relocated 2026-08-30 to `docs/ROADMAP_HISTORY.md` for the ROADMAP size budget.
- **RM-281 HALF-CLOSED + RM-283 OPEN; RM-284 CLOSED 2026-09-02 (LEDGER 1320 - `*.md text eol=lf`, on-disk now equals blob) (2026-08-30, lane 8 cycle 29)** - `dashboard/_idempotency.py` audit: the check-then-act on a threaded server was replaced with an atomic claim/settle/abandon, and RM-281's filed OS-divergence diagnosis was REFUTED (it is a shared process-global replay table under xdist). Bodies in `BACKLOG.md`; detail in LEDGER 1295. **Full row relocated VERBATIM 2026-08-31 (lane 8 cycle 41) to `docs/ROADMAP_HISTORY.md` for the size budget - READ IT before re-opening any of these ids.**

- **RM-253 CLOSED + RM-266 / RM-267 / RM-268 / RM-269 OPEN (2026-08-30, lane 8 cycle 25)** - `vision_server/_relay.py` audit, the untrusted-input seam feeding the coaching pipeline; seven defects fixed in-slice. Also records RM-270/RM-272 (c26) and RM-255 + RM-273..276 (c27, `lib/http/client.py` failing OPEN). Bodies in `BACKLOG.md`; detail in LEDGER 1291. **Full row relocated VERBATIM 2026-08-31 (lane 8 cycle 41) to `docs/ROADMAP_HISTORY.md` - READ IT before re-opening any of these ids.**
- **RM-233 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - three residual `core/match_db.py` weaknesses left unfixed by lane 8 cycle 16, each verified live: **(a)** the `PRAGMA journal_mode = WAL` result is never read, so a silent fallback to `delete` journalling makes the module's own concurrency claim false with no signal; **(b)** `save_match` returns `None` on BOTH success and failure, so a lost match is indistinguishable from a saved one; **(c)** a negative `limit` is UNLIMITED in SQLite - LOW, the one production caller already clamps. Body in `BACKLOG.md`. **Next free id = RM-234.**
- **RM-234 SHIPPED 2026-09-11 (LEDGER 1398); RM-235 / RM-236 / RM-237 OPEN (filed 2026-08-30, lane 8 cycle 17; LANE 8, Tier-1)** - what cycle 17 did NOT fix in `game_reader/snapshot_normalizer.py`: two per-tick `/activeplayerrunes` GETs (234 - CLOSED: one GET, `my_runes` derived from `runes_full`; the "no production reader" premise was false, `coaches/aram_coach.py:430` reads `my_runes`); process-global subresource counters with no production reader or reset (235); six further defects, FOUR wrong on real Riot data every game, headed by `quest_boots_owned` being wrong in both directions on 16.15.1 while feeding a build instruction into the live SR prompt (236); and a ~17s worst-case poll tick that trips the 12s liveness threshold, whose auto-remediation then hands a second thread the SAME `GameReader` (237). Bodies in `BACKLOG.md`.
- **RM-250 SHIPPED 2026-09-05 in its first half (LEDGER 1333), with the second half REFUTED; RM-251 OPEN (filed 2026-08-30, lane 8 cycle 21; RM-250 LANE 8, RM-251 LANE 7, Tier-1)** - what cycle 21 did NOT fix after hardening `dashboard/routes_loop_control.py`: **(250)** three SIBLING hand-rolled non-retrying atomic writers on the SAME polled control files - `ops/loop/loop_controller.py:126-129`, `ops/loop/intents.py:99-113`, `ops/loop/steer.py:96-101` - each carrying the WinError 5 exposure the route just closed, plus `core/polled_json.atomic_write_text`'s CRLF exposure for its 6 callers; **(251)** the stale `:8888` residues left by the S10 Task 9 de-registration - `dashboard/_handler.py:532`'s dead `/api/loop-control` literal in the D9 auth tuple and `dashboard/routes_auto_accept.py:18`'s trust-model prose citing it as a dashboard POST - plus three private `_atomic_write_bytes` re-rolls (`core/rofl_archive.py:815`, `lib/icons/downloader.py:29`, `tools/ddragon_mirror_refresh.py:145`) now that a public one exists. Bodies in `BACKLOG.md`. **Next free id = RM-252.**
- **RM-247 / RM-248 OPEN (filed 2026-08-30, lane 8 cycle 20; LANE 8, Tier-1)** - what cycle 20 did NOT fix after hardening `vision_server/_inference.py`: the `/vision` frame path still has no size or decompression-bomb cap and its `except Exception` FORWARDS the oversized frame instead of refusing it (247); 22 residual `except Exception` sites across `vision_server/` plus `handle_ocr` reporting `ok=True` on an empty result (248). Bodies in `BACKLOG.md`.
- **RM-242 / RM-243 / RM-244 / RM-245 OPEN (filed 2026-08-30, lane 8 cycle 19; LANE 8, Tier-1 except RM-243 Tier-2)** - what cycle 19 did NOT fix after hardening `dashboard/routes_state.py`: the systemic raw-exception-into-the-body class, 22 sites / 19 modules (242); the POST validator that logs its verdict then dispatches the bad body anyway (243); seven residual correctness defects (244); four degraded-mode paths that report success while failing (245). Bodies in `BACKLOG.md`. **Next free id = RM-249.**
- **RM-238 / RM-239 OPEN (filed 2026-08-30, lane 8 cycle 18; LANE 8, Tier-1)** - what cycle 18 did NOT fix after hardening `dashboard/routes_diag.py`: `data/decisions_log.jsonl` grows without bound and is read whole on every `/api/decisions/log` call, graded LOW on a measured 3775-byte live file (238); and the RM-134 scrubbed error envelope was built but never adopted by its callers - **25 sites across 19 route modules** still put exception text into an HTTP response body, headed by `routes_loop_control.py`, whose 6 sites reach Mission Control `:8895` only (tailnet `100.70.22.55` + loopback) - **the `:8888` half of this claim was CORRECTED 2026-08-30 by lane 8 cycle 21**: S10 Task 9 de-registered the route, `mc/routes.py:16` is its ONLY production importer, and `tests/test_mission_control_server.py:468-476` asserts `:8888` does not match `/api/loop-control` (239). Also RM-240 (both decision-resolving POSTs are unauthenticated state mutations on the `::` listener, though the gate they are missing from is itself inert until `RC_DASH_TOKEN` is set) and RM-241 (a newline-bearing `RC_VISION_TOKEN` reaches `logs/` through a urllib exception message; fix belongs in the resolver, not the callers). Bodies in `BACKLOG.md`. **Read the RM-239 acceptance before touching it** - the existing RM-134 guard is vacuous by construction and must be REPLACED, not extended.
- **RM-321 PARTIAL - 1 of 5 closed, 4 REMAIN OPEN (filed 2026-08-30; LANE 8, Tier-1)** - overriding `log_message` silently drops the stdlib's control-character escaping, and all five remaining overrides in the tree did. **`agents/_supervisor_http.py` was CLOSED 2026-08-30 by lane 8 cycle 15** (LEDGER 1281, commit `b9883446`): the injection was reproduced against the real handler over a real socket (raw `0x1b`/`0x08`/`0x7f` in the record, at INFO, on the 0.0.0.0 bind), closed by reusing `dashboard._handler._scrub_log`, and pinned by a mutation-tested regression test. Its peer half needed no fix - that override already called `address_string()`, so the original census was right and only the scrub was missing. **STILL OPEN, and one of them is the other remotely-reachable INFO logger:** `mc/handler.py` (`:8895` on the tailnet address - do NOT make `mc/` import from `dashboard/`; lift the helper instead), plus the three loopback-or-debug-only ones, `agents/daemon_slayer/server.py` (also owes a `Share/` mirror resync, so it lands Tier-2), `tools/ds_matchdb_mcp_server.py` and `vision_server/_http.py`. Log-integrity severity, backfill measured empty. `dashboard/_handler.py` was fixed in cycle 13 (LEDGER 1279). Body in `BACKLOG.md`. Next free id = RM-232.
- **RM-192 / RM-193 / RM-194 / RM-195 / RM-196 / RM-198 / RM-199 OPEN (filed 2026-08-14, lane-research refill, LEDGER 1245) - the still-open half of the RM-192..RM-202 refill; bodies + acceptance in `BACKLOG.md`.** LANE 4 (196) / LANE 7 (192-195) / LANE 8 (198/199), all Tier-1 except RM-195 Tier-0/1. **RM-197 / RM-200 / RM-201 / RM-202 CLOSED** (LEDGER 1253/1263/1264/1261, ENGINE 1.278.0); the full RM-192..RM-202 filing relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-01 block). Live next-free id **RM-322** (`docs/DS_SWEEP_TRACKER.md`).
- **RM-158 DATA HALF CLOSED 2026-08-06 (`9114100c`, LEDGER 1211); only the TABLE REGEN remains, deliberately deferred.** Corpus half DONE, writer GATED. **Three fences kept here because each cost a wrong first attempt:** the row's two hashes are correctly VALUED and wrongly LABELLED (they run from the `"scenarios":` offset, NOT whole-file); **do NOT purge the arena rows** - the first attempt did and it was wrong; and the regen is deferred on purpose, not forgotten. Full measurements in LEDGER 1211. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-15).
- **RM-125 OPEN (COMMENT HALF SWEPT 2026-07-28, R222) - only the rendered residue remains, 235 glyphs, and it is DEFERRED to RM-122 for operator-present rendered-pixel judgement, so it is NOT a headless job.** Full body + the guard/false-positive-pin enumeration relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block). **Read `docs/RM125_web_live_glyph_adjudication.md` before touching one** - it adjudicates all 235 (STRIP-candidate-deferred 152, KEEP-deliberate 52, **LOAD-BEARING 31**, where a strip breaks BEHAVIOR). Note `web/legacy_index.html` is NOT dead - `dashboard/routes_static.py:23-41` serves it at `?ui=legacy` and as the automatic fallback when `index.html` throws.

- **RM-122 OPERATOR-PRESENT UI/UX LANE - THE HEADLESS LOOP MUST NOT PICK THIS UP.** Filed 2026-07-28 (R217) out of the `random.txt` part-2 block, which is headed verbatim "session 2 (operator-present; continues 2026-07-22b). Do NOT run headless." Every item terminates in a rendered-pixel judgement, the one class an agent cannot self-adjudicate under the CLAUDE.md R3 text-first rules, so **a director scheduling this row to a headless agent violates the note's own gate.** Six-item queue in the operator's stated priority (competitor capture library -> landscape auto-fit hub -> per-page layout alternatives -> champ_select Tailwind-to-theme -> logged one-liner defects -> theme polish), each with its fences, relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06). **READ FIRST, do not re-derive:** that block, plus `WAKEUP_NOTES.md` section 2026-07-22b, `docs/qa/UI_UX_PROGRAM_QA_2026-07-22.md`, `docs/qa/SPATIAL_BRAND_SPEC_2026-07-22.md` section 5, `docs/qa/THEME_MULTIHUE_METHOD_2026-07-22.md`, `docs/qa/DESIGN_SYSTEM_MAP_2026-07-22.md`, memory `feedback_operator_ui_qa_method`. **Standing traps:** competitor shots stay LOCAL and are never committed; the champ_select shell tags stay Tailwind until DS4 lands, so a partial conversion is a defect; the `.rofl` 4-match ingest gap is RM-117, NOT a UI job; several items are UNKNOWN rather than cleared; and every page touched owes the 5-phase UI-audit ritual BEFORE its commit.

- **RM-01** [OK] **HAIKU-TO-ZERO Lane E CV substrate ADVANCING (2026-07-14, gemini-loop cycles 19+24; R121/R126).** The client-side CV vision tier + confidence-weighted Live-Client/CV fusion (`docs/NO_LLM_PRECOMPUTE_PLAN.md`, the "bigger SECOND program") has its persistence + fusion substrate BUILT and ALL DORMANT - the icon dhash atlas (227 icons), the OCR region-map atlas (25 regions) and `core/vision_fusion.py`; the module/artefact enumeration is relocated 2026-09-02 to `docs/ROADMAP_HISTORY.md`. `fuse_reads` is WIRED SHADOW-FIRST (2026-07-17, LEDGER 914 S6, `b2952b96`) - the ARAM/Arena vision tick appends a per-tick record to `data/fusion_shadow.jsonl` at the `GameVisionReader.read_tiered` chokepoint, coach output byte-identical. NEXT (live-gated): calibrate the `augment_card_*` / `minimap_fog` rects against a real Arena/ARAM frame, then a validated OCR-only flip. **Do NOT flip blind** - a wrong precompute is worse than a Haiku call. **COMPETITOR-LIFT ROTATION RETIRED:** that research category is DRAINED 4x (R100 Overlay App F / R112 aggregator D+aggregator C / R120 Overlay App F+Overlay App E / R125 Aggregator C Desktop) - do NOT re-pick it; rotate DS-sweep, Haiku-to-ZERO (Lane A/E), or a genuinely un-torn-down category instead.
  - **2026-08-02 (LEDGER 1173, `ce808f81`) - the A/B choices half was BLOCKED BY A PARSER BUG, not by missing coaching: `parse_fields` clipped every value to 220 chars, so the emit was WIRED but INERT for 3 of 4 coaches from the day it shipped; fixed by bounding per CONTENT CLASS.** Do NOT read memory `reference_coach_choices_native_emit` as evidence the values arrive. **OWED, unconfirmable headless: `both` can only leave 0 after one live ARAM on a restarted process.** Step 1 (action agreement - `wave_pct` hardcoded off at `dashboard/_deterministic_coaching.py:1336`; the noise + timing-skew hypotheses are REFUTED in `docs/ARAM_ACTION_MISMATCH_2026-08-02.md`) is an operator scope call - do NOT flip it blind. Full narrative archived verbatim in `docs/ROADMAP_HISTORY.md` (2026-09-07e block).
  - _Re-probed 2026-07-25: the accrual gate is MET (`data/fusion_shadow.jsonl` holds real-game records; the 41 garbage CV overrides were fixed at the fusion layer with a plausibility gate; the upstream half `core/vision_routing.py:118` is CLOSED, ENGINE 1.243.0). **`RC_VISION_MERGE_STRICT` is DEFAULT-OFF and MUST STAY OFF until someone re-homes `is_augment_select`** - `modes/shared_vision.py:418` aliases it AFTER the merge and it is in NO coach TIERED_FIELDS. Still shadow-only; do NOT flip. Measurements archived verbatim in `docs/ROADMAP_HISTORY.md` (2026-07-25, 2026-09-02 and 2026-09-07e blocks)._
- **RM-02 DS build-reco counter-build: the full counter-hint program is CODE-COMPLETE; the ONLY remaining work is the in-game chip eyeball.** C2 antiheal + C6 tenacity + C3 fed + the R102/R103 situational step-2 + the live `allPlayers` roster plumbing all shipped (LEDGER 908/909/914 S1+S9/916; `f8f89306`, `bd18e351`, `4ced15e4`, `18fdf4d0`; new `core/build_planner/fed_threat.py`; specs under `docs/specs/2026-07-1{6,7}-ds-c*-counter-hint-design.md`). Do NOT rebuild step-2 - the 2026-07-13 refactor spec is stale on it. **OWED (do-not-flip-blind, operator-only):** eyeball the antiheal / tenacity / fed / roster chips in a REAL game - the Electron overlay is agent-blind and a wrong hint is worse than a dark chip. Tracked as a gated row (`docs/LIVE_GAME_GATED_SYNC.md`, the RM-02 citation at the DS-plan row). Full shipped narrative + the pre-C2 history: `docs/ROADMAP_HISTORY.md` (2026-07-17 + 2026-07-18 blocks).
- **RM-03 RC 2.0 program (in flight).** Living plan `docs/RC2_PLAN.md` (9 phases / 62 stages), resume via `/RC2-Continue`; design GREENLIT Hextech (`docs/design/RC2_DESIGN.html`), League borderless @ 2560x1440. OPEN: ~~E10 ASCII+git-history rewrite (force-push, operator-gated)~~ **E10 SHIPPED 2026-09-07 (LEDGER 1361)** - executed as DELETE-AND-RECREATE rather than the planned force-push, because `refs/pull/N/head` is permanent and a rewrite never touches it; repo is PUBLIC and CI green; E12 responsiveness levers residual (= QA62 / lever L3: BUILT DARK 2026-07-20 `9eb76879` - full in-process snapshot port `lcu/snapshot_shape.py` + `dashboard/_lcu_inprocess.py` wired behind DEFAULT-OFF flag `RC_LCU_INPROCESS`; QA59/L2 already shipped as `_cached_lobby_mode`. ONLY the G1-00 live confirm owed - flag ON in a live champ-select, byte-compare `lcu.champ_select`, adjudicate the `state.lcu.config`/auto-accept-pill divergence; see `docs/LIVE_GAME_GATED_SYNC.md` G1-00 CHECK 2); E2 DS 3-game live-flip (operator-played); P5.2 served CV-flip env (`RC_LANING_CV_SERVED=1`, gated on `hz_shadow_report` agreement delta); rc-shell Electron relaunch owed (live disappear-fix/pinned); spell-fix LIVE verify owed. Shipped-stage detail: `docs/RC2_PLAN.md` + `docs/ROADMAP_HISTORY.md`.
- **RM-118 STRANDED-SEAM CLASS - the WIREABLE debt is ZERO AT BOTH TIERS (2026-08-15, LEDGER 1263/1264, ENGINE 1.278.0); re-measured 2026-09-02, `STRANDED_TODAY` 5 / `STRANDED_DEPTH1` 0.** RM-200 + RM-201 were the last two, both wired onto `compute_ehp`, so `STRANDED_DEPTH1` is empty BY WIRING, not by the guard going quiet. What stays OPEN is only the 5-seam DECLINED-BY-DESIGN depth-0 ledger (the target/caster-state arc, operator-CLOSED s232). **Take the count from `agents/daemon_slayer/tests/test_stranded_hsp_seam_r197.py::STRANDED_TODAY`, never from a sentence - every count this row has carried in prose was wrong at least once.** **This row is the stranded-seam RM-118; the mana-as-damage RM-118 further down is a DIFFERENT row under the same id - do not merge or renumber them.** Body + the FOUR DURABLES (signature-not-docstring, transport-not-flag, stale REASON, seam-under-a-different-NAME) relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06, 2026-08-15, 2026-09-02); the first two are also memory `reference_ds_route_seam_transport_vs_flag`. RM-333 (2026-09-02) adds a fifth axis: a seam under a different DEFAULT TYPE is invisible to a default-keyed checker.

- **RM-117 REPLAY ANALYSIS / COACHING CORPUS - substrate BUILT 2026-07-26 (LEDGER 1062); narrative in `docs/ROADMAP_HISTORY.md`.** 7 `core/` modules + 6 `tools/` + `RC-ReplayRosterPull` hourly, 112 tests; corpus 108 accounts / 417 `.rofl`, timeline corpus CLOSED at 3005 matches. **The `.rofl` Layer-2 fence did NOT need opening and stays CLOSED** - positions, wave state and ward coverage all have measured non-Layer-2 routes. Full module/tool enumeration relocated 2026-09-02.
  - **MEASUREMENT TRAPS - 11 of them, each a place where the obvious reading of the data is WRONG and re-deriving it costs a session. READ THEM BEFORE ANY REPLAY/CORPUS WORK.** Held VERBATIM in TWO places, which is why only this pointer lives here (2026-09-05 size-budget pass): `docs/ROADMAP_HISTORY.md` and `docs/REPLAY_T2_PARSE_CRITERIA.md`. They cover `.rofl` v2 encryption, Match-V5 position error, `assistingParticipantIds`, `monsterSubType`, ward positions, summoner-spell loadouts, the absent fetch-by-match-id route, `RC-ReplayRosterPull` busy state, `objective_participation` bias, and the B13 legal close.
  - **NEVER PROMOTE:** `kill_participation` (INERT - already team-normalised), `shutdowns_given` (measures being AHEAD - winners gave MORE in every role), `deaths_per_min` and TEAM `gold_deficit_profile` (they restate the OUTCOME).
  - **Only surviving candidate: JUNGLE `plate_share`** 0.082 vs 0.045, effect 0.49 - the one row through hygiene + effect gate + holdout + a pre-treatment confound control, and **still not causal**. Effect below ~0.3 does not survive the by-match holdout, and **NO p-value is reported anywhere in this table** - the gate is effect size plus holdout, so never read a surviving row as statistically significant.
  - **STILL OPEN / OWED:** **(ii) SHIPPED 2026-08-04 (LEDGER 1195) - `core/data_retention.py`**, four classes with four different answers; `scan` / `plan` / `render_report` / `game_information_manifest` are read-only and `apply()` is INERT (zero callers, no `--apply` on the CLI). NOTHING was deleted - `data/` is byte-identical at 10.11 GB / 2106 files. **The filed premise was WRONG in both halves:** not "past 6 GB", and the mass is NOT `.rofl` (3.72 GB is two one-off `rewind_history.db` backups - a snapshot, not a growth curve). **Operator decision still pending** on tier-1 3.72 GB / tier-2 24.6 MB / tier-3 137 MB, and **do NOT delete `data/riot_api_cache.db`** (RM-153 measured `freelist_count = 0`, it is all live payload, and its `-wal`/`-shm` sidecars must never move separately). **(iii)** T4 is attended and EXCLUSIVE (a Win32 `PrintWindow` path is UNMEASURED). **(iv) CLOSED 2026-08-04 as STALE, not false** - `MASTER` IS present in `data/rank_baselines.json` and non-degenerate; two vacuous-evidence traps recorded in `docs/ROADMAP_HISTORY.md` (2026-08-06) + LEDGER 1195. **(v)** the two forward Riot-terms conditions - prior WRITTEN APPROVAL before reselling / licensing / charging for Game Information, and TERMINATION requiring deletion of all Game Information, which `game_information_manifest` now enumerates (49 paths / 8.7 GB). The postgame narrative (`core/precomputed_replay_narrative.py` + `core/pgr_event_report.py`) is SHADOW-ONLY; no live coach path flips. Criteria triage: `docs/REPLAY_T2_PARSE_CRITERIA.md`. Specs: `docs/REPLAY_FRAME_ANALYSIS_SPEC.md`, `docs/REPLAY_T4_FRAME_CV.md`.

- **RM-106b** [OK] **OPEN AND VALIDATED - the only route that recovers ITEM progression for KIWI (a live replay with `EnableReplayApi=1` serving `:2999`, driven by `POST /replay/playback` seek-and-sample).** Full body + every measured limit relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block); the pre-existing full record is in the same file. **READ IT before acting** - it carries the fences: do NOT re-inherit the earlier 404 verdict (that probe ran flag-OFF and was wrong); the seek is EXACT only when PAUSED first and running playback silently drifts the sample; `activeplayerabilities` 400s in a replay so skill order is NOT recoverable and `eventdata` carries no `ITEM_PURCHASED`; sampled inventory is GROUND TRUTH while Match-V5 `ITEM_PURCHASED` reconstruction is ERROR-PRONE; and the archive-depth question is MOOT because Match-V5 timelines paginate arbitrarily deep with NO retention cutoff.

- **RM-124 BUILT + GATED-OFF 2026-07-29 (`efbb34d0`), awaiting one-game live cadence validation before the flip.** Pure `wave_callout()` lives beside `recall_callout` in `core/event_callouts.py`; `minion_events` (the `MinionsSpawning` anchor) is extracted in `dashboard/_liveclient.py` and threaded through `_deterministic_coaching.py` into `next_callouts(..., enable_wave=)`. **Correction to the teardown below: the callout renders on the GENERIC `web/js/panels/callouts.js` sink (any `{tag,line,eta_s,kind}` row), NOT `objective_chips.js`, and it does NOT light up the `next.js:16-83` 3-lane per-lane percentage UI - that needs per-lane push %, which is the data-blocked STATE machine.** Live emission is gated OFF: `enable_wave` defaults False and the dashboard only sets it when env `RC_WAVE_CALLOUT=1`, so `/api/state` is byte-identical today. **The one remaining task is the do-not-flip-blind live validation** (see the gated-sync doc): anchor + cadence constants (`_WAVE_INTERVAL_*`, `_CANNON_BRK_*`, `_is_cannon_wave`) are PROVISIONAL and must be checked against one real SR game's `MinionsSpawning` EventTime + observed cannon arrivals before `RC_WAVE_CALLOUT=1`. Follow-on F2 (CS efficiency curve) is unblocked once validated. Original teardown context preserved below.
  - **Pre-build filing (R220 2026-07-28) RELOCATED 2026-07-30 - the build it specified has landed; only the fences below still bind.** Full teardown text: `docs/ROADMAP_HISTORY.md` (2026-07-30 block) + `docs/COMPETITOR_LIFT_2026-07-28.md`. **(1) Do NOT hardcode a spawn time** - three sources disagree on first wave (0:30 / 1:05 / 1:30) and on the cannon breakpoint (14:00 / 15:00), and a 2025 change moved first-cannon arrival 2:05 -> 2:35; anchor on the live event's `EventTime` and validate the table against one real game before any flip. **(2) Do NOT re-pitch a live wave STATE machine** (freeze vs slow-push vs fast-push) - data-blocked three ways: `:2999` exposes no minion entities, the Overlay Platform M GEP contract gives `minionKills` counts only, and Match-V5 has no minion event type, so nothing wave-shaped is backfillable from the corpus and only a Lane-E CV route could ever see it. **(3)** The `web/js/panels/next.js:16-83` 3-lane wave readout and `wave_state_now` / `wave_control` / `wave_freezes` / `cannon_cs_summary` (`core/match_metrics.py`) have ZERO non-fixture producers and stay dark - that is (2), not a wiring gap. **(4) Tempering:** `tools/hz_shadow_report.py:196-201` EXCLUDES crash / freeze / push from the laning agreement sample, so a wave feature will NOT move the Lane A number. Follow-on F2 (CS efficiency vs a theoretical-max curve, replacing the flat `core/lead_projection.py:57` `_CS_PER_MIN_BENCHMARK_SR = 8.0`) is gated behind this clock.

### CLOSED in this lane - ids kept reachable, fences RELOCATED

Relocated VERBATIM 2026-08-06 (run-02 size-budget pass) to `docs/ROADMAP_HISTORY.md`.
Nothing there is open. **Read that block before re-opening RM-100 / RM-106 / RM-106a / RM-107 /
RM-109 / RM-111 / RM-112 / RM-115 / RM-116 / RM-120 / RM-123 / RM-126** - each carries a
do-not-re-pitch fence, not just history. RM-106b and RM-125 are still OPEN above.

## NEXT - live-game-gated / operator-gated

- **RM-204 OPEN (filed 2026-08-14) - the Arena augment play-line is BUILT, TESTED and DELIBERATELY INERT; flipping it on is the residual Haiku-to-ZERO hop and it is OPERATOR-GATED because it retires a live coach call.** Body in `BACKLOG.md` under "Draft + coach lane (Haiku-to-ZERO north star)", NOT here (byte-budget overflow rule). One-line summary: `dashboard/_deterministic_coaching.py:1481` still passes no `owned_augments=`, so `core/arena_augment_playline.py` reaches no served output. **Three things must be true before the hop lands** - there is ZERO shadow det-vs-live agreement history for this column (it has been empty since v1, so there is nothing to compare against), it is unvalidated against a real or replayed Arena game, and the display-name resolution path is untested against real vision output. **Only the PLAY half of the field is closed; the SELECT half is still Haiku-only.**

- **RM-191 OPEN (filed 2026-08-12, LEDGER 1242 residual) - `write_fatal` is a WRITE-ONLY channel with ZERO readers, which is why a 3-day outage was found by hand instead of by a monitor. OPERATOR-GATED: the fix touches the FROZEN `ops/rc_dev_runtime.py`.** Full body + the measured grep, the three compounding defects and the 2026-08-09 evidence relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block) - READ IT before acting. Fences it carries: the LEDGER 1242 rename retry explicitly does **NOT** close this; **do NOT scope it as "add a log line"** (the useful shape is a counter plus last-N ring reaching BOTH `logs/` and the health payload); and one probe is still owed on WHAT held the handle on `health.json` at the swap, because the retry masks that permanently.

- **RM-189 OPEN (filed 2026-08-11, LEDGER 1238/1239) - the Overlay Platform M submission program. The RENAME half is DONE and no longer blocks; what remains is OPERATOR-GATED.** Full body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block) - READ IT first. **STILL BLOCKING, all operator-only:** the Riot Developer Portal product registration needs the operator account login, and a PRODUCTION key needs a public website + Terms of Service + Privacy Policy, none of which exist. **B4 IS BUILT 2026-08-12 (a through e) - only B4-f remains, a DevRel question: are build/shop recommendations in or out during a live game?** Raise it with B8 (vision/OCR capture) and B9 (declare LCU use). **Framework verdict for any port: ow-electron, not Overlay Platform M Native.** **Do NOT re-open:** Tier 2 (the repo DIRECTORY rename), Tier 3 (the `RC` abbreviation), or the TFT augment-stats blocker (REFUTED). Plans + design: `docs/OVERLAY_COMPLIANCE_PLAN.md` + `docs/RENAME_SWEEP_AMBERSTONE.md` + `docs/OVERLAY_B4_DESIGN.md`.
- **RM-167 NEEDS-OPERATOR (filed 2026-08-06, LEDGER 1206) - the aram prompt-cache prefix hoist, and the 12 `cache_control` markers already in the tree are MEASURABLY INERT. "Roughly 10-15 pct of TOTAL spend" is the LOW end** (`aram_coach` alone is $40.02 / 12,413 calls / 31.2 pct of all-time spend, re-measured 2026-08-08, LEDGER 1236). Both measurement passes relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block); floor numbers also in `BACKLOG.md` (prompt-cache floor). **Still NEEDS-OPERATOR for two unchanged reasons:** the hoisted prefix still misses the model cache floor without deliberate padding, and padding plus reordering shifts Haiku positioning, so it needs a live ARAM A/B for behavioural equivalence plus a live `count_tokens`. **NOT covered by the item-286 closure**, which measured only the four augment/TFT prompts.
- **RM-168 LIVE-GATED (filed 2026-08-06, LEDGER 1206) - the log-rate sweep is CLEAN but WEAKLY POWERED for the THIRD time, because every sample so far caught an IDLE client** (the in-game multi-tab case, where the suppression list's own comment says the 2 Hz volume lives, is still UNMEASURED). Both samples relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block). **Stop re-sampling it headless - a fourth idle sample adds nothing.** Pair it with the RM-01 A/B live ARAM, and re-baseline against the post-S4-dedupe volume (LEDGER 1235 removed 21-25 pct of total log volume), never the pre-dedupe figures.
- **RM-151 OPEN (filed 2026-08-03, lane 8 / LEDGER 1180) - the postgame corpus looks like it has NEVER been written by the EOG path, only by the single-participant history fallback. LIVE-GATED.** Full body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ IT before acting on this row; it carries the measurements and the do-not-do list.
- **RM-149 OPEN (filed 2026-08-02, lane 7 / LEDGER 1174) - two operator rulings on out-of-repo cleanup, both blocking any future lane-7 pass over `%USERPROFILE%` paths.** Full body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ IT before acting on this row; it carries the measurements and the do-not-do list.
- **RM-113 w-nextbuy live-frame acceptance - 1 row still OPEN (2026-07-23).** OQ16 cadence and the `#am-next-buy` no-reflow hold both PASSED. **OPEN TRINKET:** never activated in 260 samples, and the stage gate cannot explain the silence (`stageFor` returns `mid` on the clock arm alone at `c >= 600`, `web/js/lib/next_buy_model.js:68-72`). Reduces to one unrecorded fact: whether `owned_items` still held `stealth ward`. `tools/overlay_live_frame_probe.py` now records it per sample and **flushes each row** (load-bearing - redirected stdout is block-buffered), so the next SR game closes this with no new analysis. Narrative: `docs/ROADMAP_HISTORY.md`.
### DS defensive-half sweep GAP specs (R132, 2026-07-19) - ALL CLOSED, fences RELOCATED

ALL CLOSED: **R134 CLOSED-REFUTED**, **R135 CLOSED-INERT**, **RM-99 / RM-99b / RM-101 / RM-102 / RM-103 / RM-104 / RM-105 / RM-108 / RM-114**. Read their verdicts in `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block; this fence paragraph archived verbatim in the 2026-09-07e block) before touching the DS scorer/item surface - RM-99's coefficient is 10 percent, NOT 8; RM-102 / RM-104 are PLURAL Arena mirrors.

### DS per-champion meta-valuation sweep

- **RM-81 data currency (open, tooled + measured; RESCOPED 2026-07-18).** Narrative relocated 2026-07-28 - see `docs/ROADMAP_HISTORY.md`. Open work and the measurement traps are unchanged there; nothing was closed by the relocation.

- **RM-35 .. RM-48 DS sweep GAP specs - ALL FOURTEEN RESOLVED; the row is CLOSED.** Full row + per-id verdicts relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-31). Do NOT re-open the roster.

### Programs with open kernels

- **RM-12 Haiku-elimination program - the PRIMARY north star (drive live Haiku usage to ZERO via precomputed DS lookups) is OPEN; the item-265/266 foundations are shipped.** Shipped and on disk (inventory relocated to `docs/ROADMAP_HISTORY.md`): Lane A matchup engine + `/v2/matchup`, Lane B build-order tables, Lane C/E/P deterministic generators, W3A/W3C/W3E, item-280 ARAM comp verdict. **STANDING GATE FINDING (do not re-litigate):** the matchup engine validated as a COIN-FLIP vs real outcomes (gold-at-10min 48.8%, solo-kill duel 51-52%, n in the thousands) -> NO coach was flipped off Haiku. The matchup-derived A/B chips are SECONDARY; the correct-by-construction callouts + lead are the valid Haiku-elim wins. **OPEN (operator-gated):** matchup-engine fidelity lift (item-aware + real-level; uncertain payoff, likely a modeling ceiling) OR pivot to non-prediction surfaces; overlay Lane D (Electron Phase 2); champ_select pickban-DB flip after a counter-quality validation; the champ-select brief Haiku flip after shadow-log accrual (served stays Haiku per the item-273 AST guard; the READ-ONLY gate that clause needs now exists - `tools/champ_select_shadow_report.py`, R218 `19d3e734` - so what remains is accrual, not tooling: `data/champ_select_shadow.jsonl` is empty and the report reads `state=awaiting_accrual` below MIN_SAMPLE 20). Full shipped narrative: `docs/ROADMAP_HISTORY.md` (2026-07-18 block).
- **RM-14 DS gated flip levers (dedup of the items 227-239 NEXT stacks; full records `docs/ROADMAP_HISTORY.md`).** Operator-gated, ENGINE bump where noted: flip opt-in flags default-ON after live validation (`gate_ammo` / `apply_mode_modifiers` / `aoe_targets_hit` / `apply_ability_haste` / `apply_passive_damage`; `target_hp` Cut Down / Coup de Grace gate R51 `b559eb2b` rides `docs/LIVE_GAME_GATED_SYNC.md`); auto-enable assassin keystone in /rank-assassin (operator sign-off); missile combo-clock delay IF a real per-spell cast-range source lands; static-CD consumer (needs slot->ability-name bridge + a live ability-haste model); `assume_missing_hp_heal_amp` flag (hps.py:499/:828 default OFF - the R5 heal-amp seam; the BASE ability-HPS fold-in is ALREADY live per OQ22); enchanter registry omits 5 corpus-proven winners (Dream Maker / Dawncore / Shurelya / Seraph / Luden - FUTURE); the 4 non-every-AA `on_hit` entries structured cadence + live "saner not different" re-rank; Phase D live flag-flips + the 4 different-seam exclusions + the deferred Phase 11 vision-frame collapse; the percent-of-resist "read the parsed block" lift (needs a NEW schema seam); `rune_procs` into live `rank_items_by_burst`; per_attack runes expansion (saturated); mana_sim + runes as scenario_matrix metrics; the operator sends the `Share/` handoff (notates when/to-whom). cc_tags operator-CLOSED - do not re-pitch.
  - _RM-19 / RM-20 / RM-21 are FOLDED INTO this bullet - RM-14 is the canonical index; gated rows `G2-04`, `G2-21`, `G5-05`. The fold record (RM-21's DONE note included) is archived verbatim in `docs/ROADMAP_HISTORY.md` (2026-09-07e block)._
- **RM-15 Electron shell + in-game overlay - phases 1-5 ALL SHIPPED code-side; only the un-headless tails remain.** Plan `docs/ELECTRON_OVERLAY.md`; the shell is `rc-shell/` (shipped feature list relocated to `docs/ROADMAP_HISTORY.md`). **OPEN:** (a) live-game overlay capture with ACTIVE knob interaction - operator in a real match, shell relaunch picks up the shipped slices; (b) operator packaging - `npx electron-builder` + first GitHub Release + a packaged update check against the private repo (`GH_TOKEN`), tracked as a gated row; (c) Phase 6 Pengu in-client panels, OPTIONAL. **Risk register (standing):** the overlay is DWM compositor, not DXGI capture (in-game continuous capture was retired, 1-PC ADR-011); Borderless mandatory; Vanguard-safe (only `:2999` + LCU, no memory reads/injection). Full 5-phase slice narrative: `docs/ROADMAP_HISTORY.md`. **TRAP (2026-07-30, `7cefb2e1`): the plan doc's section-5 window-state table is no longer prose** - `rc-shell/test/overlay_state_contract.test.js` PARSES it and derives its assertions from the parsed cells, so editing the doc alone can turn the suite red. `rc-shell` `npm test` is a green gate at 326/326 (`e947bcaf`).
- **RM-16 Carry-forwards, compressed (items 215/211/283/277/276; full records `docs/ROADMAP_HISTORY.md`).** **REFUTED + STRUCK 2026-07-19 - do NOT re-file these three:** item 215's *poller* clause, item 212 entirely, item 269 entirely. Each is pinned by a named test and the evidence is in `docs/ROADMAP_HISTORY.md`. Still open: **item 215** OBS DXGI match-end capture watch (lock one resolution + League Borderless while recording). **item 211** `matches.tracked_puuid` can be stale - ALWAYS re-resolve via Account-V1; Live Client rarely exposes `gameId` so `game_id=0` rides the ts-window fallback (the 7 residual orphan rows are gated row `G4-29`). **item 277/321** FUTURE: wire the Anivia egg-resist + Orianna ally-resist seams into a live survivability scorer + per-champion validation. **item 276** Zaahen real curated build; PGR S5 polish / responsive / ARAM-Arena variants + S4 @N; Build Insights skill tab. Do-not-redo: the DS engine is verified across 4 lanes (pen / on-hit / crit / AoE) - do NOT re-audit; item-286 prompt-cache restructure is CLOSED-not-viable (guard `tests/test_prompt_cache_floor_item286.py`).
- **RM-26 vision seeds - ONE thing is open: VALIDATING the anchor model, and it is blocked on an input this machine cannot produce. Needed: ONE native full-screen 21:9 or 32:9 capture** (`ingest_reference_from_path` already accepts it, so the frame is the whole remaining gap). Full body + both measured findings relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block) - READ IT before re-measuring. Fences it carries: everything else SHIPPED and must NOT be re-opened; do NOT "fix" seed reachability again (it is reachable and moves 0 of 21 crop rects); and **16:9 CANNOT discriminate between anchor classes** - at a matching aspect every anchoring yields the BYTE-IDENTICAL box, which is why the 16:9 error measured exactly 0.00x and why no 2560x1440 still can confirm or refute a single row of `REGION_ANCHORS`. Until the frame exists the model stays DEFAULT-OFF and `seed_profiles()` keeps no anchor knob.
- **RM-27 External refactor-plan triage (item 312) - triage done, two accepted items unbuilt; RM-18's whole residual rides this row.** Full body (the SHIPPED / REJECTED-do-not-re-pitch / GATED-on-approval breakdown, P1.2-P5.2 by id) relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-09-10b block) - READ IT before re-pitching any proposal from those 3 Desktop review docs. **OPEN, each its own session:** **P2.2** structured-output; **P3.2** antitank dynamic %HP (a design change - antitank is intentionally static per item 308); **the 6 UNIVERSAL_FILES template convention update**, which is also the whole residual of the old **RM-18** (its 2026-06-05 NEXT-UP block is closed and relocated; its P3.2 live-producer route-wiring + ENGINE bump ride the RM-14 Phase-D tail).
- **RM-06 Overlay redesign owed tail.** Offered, not built: a manual ward tracker; folding the enemy-spell tap-tracker into the CD ledger if the operator prefers one panel. **Do-not-redo fence (standing):** a native-HUD REPLACEMENT is impossible - Live Client exposes NO ability/summoner cooldowns, buffs, wards or XP, so the overlay AUGMENTS only and the enemy tracker stays MANUAL. The Electron live-confirm and the `ward_cue` teardown (a deliberate live-session job: live serializer + test + overlay DOM contract, never a blind headless delete) are gated rows. Detail: LEDGER 760 + `docs/ROADMAP_HISTORY.md`.
- **RM-17 build-ORDER go-live + Phase-3 UI render - code SHIPPED, only the live render review is owed.** UI B+C shipped (`2d7da9e`), `/api/build-order` serves, and the ARAM/Mayhem keystone bug is fixed + proven live (`3eb2e2d`) - shipped detail in `docs/ROADMAP_HISTORY.md`. **OPEN:** the reviewed UI session + per-page UI-audit ritual on a live champ-select (gated). CLOSED-as-stale: the `only_item_ids` threading carry-branch fix (`rank_for` already threads it, verified item 222).

### Rerouted to `docs/LIVE_GAME_GATED_SYNC.md` - table RELOCATED

The 13-row id-to-gated-row mapping (RM-05 / RM-07 / RM-08 / RM-09 / RM-10 / RM-11 / RM-13 / RM-22 / RM-23 / RM-24 / RM-25) lives in `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block; this paragraph archived verbatim in the 2026-09-07e block). **The work is drained from the gated doc, not from here.** Its do-not-redo notes travel with it: minimap colour/size/motion isolation is a validated DEAD END, replay scroll-wrap is CORRECT, and RM-13 is a standing per-game ritual which never closes.

## LATER - parked / blocked / low-value

- **RM-28** [WIP] **DEEP-AUDIT PROGRAM (2026-06-11, operator overnight directive - SUPERSEDES the per-item NEXT-UP queue while it runs).** Standing charter `docs/DEEP_AUDIT_CHARTER.md` (P0 baseline -> P8 docs); loop armed via ops/loop (max_cycles 100); live state on Desktop `RC_DEEP_AUDIT_SYNOPSIS.md`. Frozen files + history + gist + .md rewrites authorized; vanguard/CV + 2-PC/gamepc prune; BOM/encoding retro-sweep; Haiku/Sonnet-to-ZERO completion; Electron overlay to FULL (web dashboard no longer required); the director holds scope questions.
- **RM-29 Repo-audit D1 residual (own session).** The full 362-file Share-mirror de-dup / build-time-gen stays deferred (outward-facing gist + CI `--check` coupling). D1 churn-fix + D4 ratchet + D9 auth test shipped: LEDGER 490/506 + `docs/ROADMAP_HISTORY.md`. Do NOT re-pitch the D4 hot-path-narrowing revert or the D12 logger reverts as bugs (deliberate fixes).
- **RM-30 Dashboard data-wiring gaps (low value).** Open: ~88 `st-*` ADAPTATION rows have no live producer - correctly HIDDEN in-game by `_hideEmptyStatRows()` (`0e68d37d`); wiring live producers only affects the retired Chrome dashboard panel (LEDGER 573). `/api/ward-heat` permanently empty (`core.ward_events.record_ward` has no producer - Live Client emits no WARD_PLACED). Shipped parts (tonight-pick tips, cs_at_10/csd_at_15 producers, phantom active-match fix): `docs/ROADMAP_HISTORY.md`.
- **RM-31** [WIP] **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- **RM-32 DS calibration pipeline - UNBLOCKED + BUILT 2026-07-25; the residual is CORPUS, not code.** `core/ds_calibration_agreement.py` + `tools/ds_calibration_report.py` (Laplace, `MIN_BUCKET_N = 20`, DS-rank firewall pinned both directions). **The first result is instrumentation, NOT a verdict** - 50 joinable games / 462 observations cannot separate a +0.0677 shrunk delta from noise on one account. The keyless-row blocker is CLOSED (D-01b). **ARAM 5257 is provably UNRECOVERABLE and that is not a bug** (the ARAM played there is Mayhem, `KIWI` / queue 2400, which Match-V5 403s). Measurements: `docs/ROADMAP_HISTORY.md`.
- **RM-34 Standing fences (do-not-redo).** Originals in `docs/ROADMAP_HISTORY.md`; the consolidated list relocated 2026-07-28 to the same file. Read it BEFORE re-opening any fenced line.

## Fleet status at a glance

Live status is intentionally NOT duplicated here - a static table goes stale (this one had drifted to pid 1108 / ENGINE 1.63.0 / patch 16.11.1). Sources of truth:

- Processes, pids, listeners, scheduled tasks: `python tools/rc_facts.py` + `ops/runtime/health.json`
- All-node health rollup: `GET /api/health/all` (dashboard `:8888`)
- Daemon Slayer engine / patch / coverage: `GET :8860/health` + `data/daemon_slayer/current.txt`; DS feature + cc_conditional wave history lives in `agents/daemon_slayer/CHANGELOG.md` + `docs/DAEMON_SLAYER.md` (was inlined here as a ~20KB table cell - removed, not lost).
- TFT: Set 17 patch 17.3. **The CODE disagrees with that line and the disagreement is now LOUD, not silent (2026-07-30):** `tft/tft_data.py` declares Set 14, both set modules ship byte-identical `TIER_ODDS`, and `tft/tft_pbe_data.py` has no `UNITS_PER_COST` at all - so both modules carry `CONSTANTS_SET_VERIFIED = False` and a guard hard-fails if either is ever marked verified while stale. Resolving it needs a LIVE IN-CLIENT read (BACKLOG); do NOT correct these from a third-party odds table.

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Keep the `vision_token` + `mcp_token` resolvers SEPARATE** - never let one resolver fall back to the other's file.
