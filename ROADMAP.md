# Riot Commander - Roadmap

_Now + Next only. Full history in `docs/history_notes.md`. Aspirational in `BACKLOG.md`._

---

## Open items - High priority

_2026-05-21 sweep: items 119/120/121 (the 3 headless-upgrade pass entries) archived to CLAUDE.md ledger items 119/120/121 + WAKEUP_NOTES (full fidelity; not duplicated here per ROADMAP-95 "Now + Next only")._

- ✅ **item 144 - cc_conditional FOURTH + FIFTH consumer wires (coach prompt + dashboard UI) + cc_conditional wave 3 expansion + cost/latency CLEAN - SHIPPED 2026-05-22 (3 worktree agents merged + orchestrator ENGINE bump commit `9ab2435`, pushed; ENGINE 1.39.0 -> 1.40.0; non-frozen; DS :8893 restarted serves 1.40.0; RC :8888 restarted clean pid 13420 -> 10300 last_reload_ok=True - new dashboard route module triggered restart).** Operator "continue DS" -> 11th consecutive long-run using orchestrator-merge template (items 134-143). 4 worktree agents dispatched concurrent; Slice D returned CLEAN no-commit (11th consecutive CLEAN cost/latency sweep). 3 commit-bearing slices merged + green + pushed. DS suite **3623 -> 3682 / 1 skipped / 1 xfailed / 1666 subtests passed** (+59 Slice C wave 3 tests); RC suite **3108 -> 3203 / 67 subtests / 0 failed** (+95: 53 Slice A + 42 Slice B). **The cc_conditional ecosystem is now COMPLETE across 5 consumer surfaces (engine math 3/3 + coach prompt + dashboard UI).** **Slice A `f39d8c8` (merge `735d6ac`) feat(coach) cc_conditional FIRST coach prompt consumer (4th overall surface):** NEW `core/cc_conditional_impact_context.py` (~165 LOC) `cc_conditional_impact_line(enemies, mode, *, fight_window_s=6.0, cc_effectiveness_factor=0.5) -> str`. Sibling of `core/cc_blended_ehp_context.py` (item 138 Slice A `738c005`). Reads `compute_cc_pressure(name, mode, include_conditional=True)` per enemy + consumes `.conditional_cc_seconds` directly (already post-tenacity probability-weighted by engine seam at ENGINE 1.38.0). Defaults `_FIGHT_WINDOW_S=6.0` + `_CC_EFFECTIVENESS_FACTOR=0.5` match `ehp.py` exactly. Output line: `"Enemy conditional CC pressure: 8.4s probability-weighted (additive on top of unconditional CC) = 100% saturation, blended-EHP impact -50%"`. Empty when mode None/blank, enemies None/empty, or conditional_total == 0.0. ImportError fallback stub mirrors `enemy_cc_threat_context.py`. Wired into all 4 active mode coach prompts APPEND-only per cache-prefix preservation (CLAUDE.md item 121): `coaches/aram_coach.py` + `coaches/arena_coach.py` + `coaches/brawl_coach.py` 3 prompts NB/URF/OFA via shared `_cc_conditional_line` + `_cc_conditional_segment` concat + `coach_integration/_sr_prompt.py`. +53 tests in NEW `tests/test_cc_conditional_impact_context.py`. Adjacent fixture extensions in `test_ds_pick_consumption_p1l11.py` (2 fixtures) + `test_aram_tenacity_context.py` (3 sites) + `test_cc_blended_ehp_context.py` (2 CoachWireTests) + `test_enemy_cc_threat_context.py` (2 CoachWireTests). NO ENGINE math change. **Slice B `a204480` (merge `6875e49`) feat(coach) cc_conditional FIRST dashboard UI consumer (5th overall surface):** NEW `dashboard/routes_cc_conditional_pressure.py` (~260 LOC) GET `/api/cc-conditional-pressure?ally=<csv>&enemy=<csv>&mode=ARAM` returns `{ok, mode, ally_conditional_cc_s, enemy_conditional_cc_s, ratio, tier (good|warn|bad), ally_total_cc_seconds, enemy_total_cc_seconds, elapsed_ms, cached}`. Symmetric per-side aggregation via `compute_cc_pressure(roster_champion, mode, include_conditional=True).conditional_cc_seconds`. Tier bands ratio >= 1.05 -> good / 0.95-1.05 -> warn / <0.95 -> bad. 5-min TTL cache mode-aware. Fail-soft on missing/blank inputs. Registered in `dashboard/_dispatch.py`. NEW `web/js/panels/cc_conditional_pressure.js` (~180 LOC ESM) mirrors item 140's pattern; resolves numeric LCU ids via CHAMPS.byId; section signature `ccp:<count>` coalesces re-renders. Mount via `#csv-sugg-cc-conditional-pressure` chip between `#csv-sugg-cc-blended-ehp-threat` (item 140) and `#csv-sugg-pickorder`. NEW `web/css/panels/cc_conditional_pressure.css` (~135 LOC) with 3 `data-cc-cond-tier` variants. Imported via `web/css/dashboard.css`. +42 tests across NEW `test_routes_cc_conditional_pressure.py` (20) + NEW `test_cc_conditional_pressure_panel_dom.py` (22). Live curl verified end-to-end post-restart: `ally=Annie,Galio,Leona&enemy=Brand,Mordekaiser,TwistedFate&mode=ARAM` returned ok=true ratio=0.0 tier=bad ally_conditional_cc_s=0.0 enemy_conditional_cc_s=3.0 enemy_total_cc_seconds=9.0 elapsed_ms=0 (enemy team carries Brand R 1.4s + Morde R 7.0s + TF W 0.6s = 9.0s conditional CC; ally team has none = bad threat balance). NO ENGINE math change. **Slice C `6285b74` (merge `0799ec1`) feat(ds) cc_conditional wave 3 expansion (23/23 -> 28/28; +5 entries / +5 champs):** Via setdefault builder. NEW: Aatrox Q3 nth_hit 0.7 knockup 0.5s / Riven Q3 nth_hit 0.7 knockup 0.75s / Yasuo Q3 nth_hit 0.7 knockup 1.0s / Yone Q3 nth_hit 0.7 knockup 0.75s (mirror of Yasuo Q3) / Leblanc E channel_completion 0.5 root 1.5s. Uses only 10 EXISTING condition tags. REGISTRY_TOTAL_CHAMPIONS=28 / REGISTRY_TOTAL_ENTRIES=28. REJECTED wave-3 candidates documented in commit body: Renekton W + Camille E extension-shape asymmetry; carryover Aurora R / Briar Q+R / Sylas E2 / Volibear R / Renata R / Aphelios Q / Lissandra E; new screens Naafiri R / Akali R / Jhin R / Pyke R / Sett W / Pantheon Q / Galio Q / Heimerdinger / Senna W / Brand W / Nunu R / Yuumi Q. +59 tests NEW `test_cc_conditional_wave3.py` across 7 classes. `test_cc_conditional_wave2.py` 2 count-pin assertions relaxed `assertEqual -> assertGreaterEqual` for future-wave compatibility. `test_cc_conditional_forward_marker.py` 1-line allowlist addition for wave3 test file. Consumer math BYTE-IDENTICAL to 1.39.0 for default include_conditional=False callers. **Slice D CLEAN no-commit (11th consecutive cost/latency CLEAN since item 134):** Read-only investigative agent surveyed 7 levers (prompt-cache / route TTL / polling cadences / log spam / model tier / scheduled tasks / bundle size) all CLEAN. Top non-suppressed `/api/bridge` at 0.199/sec same as item 143 measurement. **Orchestrator commit `9ab2435` bumps ENGINE_VERSION 1.39.0 -> 1.40.0** in `__init__.py` with full changelog block covering both Slice A + B + C + D + syncs **32 stale ENGINE pin sites across 30 DS test files + Slice A new test + Slice C new wave3 test**. 0 merge conflicts (3 entirely disjoint surfaces). Test fix in same orchestrator commit: `tests/test_routes_cc_conditional_pressure.py::test_zero_cc_both_sides_returns_balanced_warn` swapped enemy roster Aatrox -> Caitlyn because Aatrox is no longer a "no conditional CC" pick post-wave-3 (Aatrox Q3 nth_hit conditional landed this run). **Merge order:** C first (`0799ec1`, registry data lane), A second (`735d6ac`, core + coaches), B third (`6875e49`, dashboard + frontend). Worktrees + 3 worktree branches unlocked + force-removed + 3 worktree branches deleted (local). **Verified:** DS suite **3682 passed / 1 skipped / 1 xfailed / 1666 subtests** (+59 over s143 3623 baseline). RC suite **3203 passed / 67 subtests / 0 failed**. phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart same pattern items 134-143. py_compile + ruff + ASCII clean across all touched + new files. DS :8893 restarted via Stop-Process PID 15600 + `schtasks /Run /TN RC-DaemonSlayer` -> /health returns `engine_version=1.40.0 patch=16.10.1 champions=172 items=705`. RC :8888 restarted via `restart_trigger.txt` (new route module registration) -> pid 13420 -> 10300 alive=True last_reload_ok=True mode=client. Live curl `/api/cc-conditional-pressure` verified end-to-end. Full per-slice detail in CLAUDE.md item 144. **Don't-redo:** cc_conditional ecosystem now COMPLETE - 5 consumer surfaces wired (cc_pressure FIRST engine math at 1.38.0 + compute_ehp SECOND engine math at 1.39.0 + compute_hybrid THIRD engine math at 1.39.0 + cc_conditional_impact_line FIRST coach prompt at 1.40.0 + routes_cc_conditional_pressure FIRST dashboard UI at 1.40.0). Slice A coach line is APPEND-only via `_USER_TMPL` per-call (cache-prefix preservation). Slice B route uses ratio = ally_avg / enemy_avg semantic where >1 = ally has more conditional CC to apply (good FOR US since we can follow up); <1 = enemy carries more conditional threat against us (bad). Aatrox Q3 nth_hit conditional means Aatrox is no longer "no conditional CC" for tests that check zero-conditional roster - use Garen / Caitlyn / Annie / Galio / Leona / Darius / Lulu instead. Orchestrator-merge pattern now 11 consecutive runs (items 134-144) - this is the durable template. **Carries forward:** (a) cc_conditional ecosystem CONSUMER WIRE COMPLETE (5/5 natural surfaces). Future work is LIVE-GAME VALIDATION (operator plays a real ARAM with one of the 28 registered conditional CC champs - Brand / TF / JarvanIV / Tahm / Volibear / Warwick / Viktor / Morde / Sett / Vex / Bard / Karma / Taliyah / Kennen / KSante / Ornn / Xayah / Fiora / Maokai / Pyke / Swain / Skarner / Zilean / Aatrox / Riven / Yasuo / Yone / Leblanc - to verify all 5 surfaces reflect signal correctly under real conditions). (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session. (e) Calibration midpoints operator-gated. (f) Item 133 carry (c) personal calibration STILL operator-gated. (g) obj_participation widening STILL operator-gated. (h) Future cc_conditional waves 4+ from REJECT carries operator-gated (currently 8 wave-1 + 8 wave-2 + 17+ wave-3 REJECTs queued). (i) UI/UX live-game audit ritual owed. (j) `_PER_SPELL_CC_DURATIONS` unconditional registry unchanged at 103/89 this run (no wave 8). (k) Briar frenzy + Sylas range REJECTs still need new condition tag constants (separate schema lift). Frozen-file grant NOT used this run.

- ✅ **item 143 - cc_conditional SECOND + THIRD consumer wires + cc_conditional wave 2 expansion + BACKLOG/cost-latency CLEAN - SHIPPED 2026-05-22 (3 worktree agents + orchestrator ENGINE bump `23195c3`, pushed; ENGINE 1.38.0 -> 1.39.0; non-frozen; DS :8893 restarted serves 1.39.0). DS suite 3534 -> 3623 (+89); wider RC 3108. Full detail in CLAUDE.md item 143 + ROADMAP archived header.

- ✅ **item 142 - cc_conditional FIRST consumer wire + cc_conditional wave 1 expansion + ROADMAP/BACKLOG stale-sweep wave 7 + cost/latency CLEAN - SHIPPED 2026-05-22 (3 worktree agents merged + orchestrator ENGINE bump commit `cf5f509`, pushed; ENGINE 1.37.0 -> 1.38.0; non-frozen; DS :8893 restarted serves 1.38.0; RC :8888 unchanged pid 13420).** Operator `/headless-upgrade` 6th long-run of the day. 4 worktree agents dispatched concurrent; Slice D returned CLEAN no-commit (9th consecutive CLEAN cost/latency sweep per items 134-141). 3 commit-bearing slices merged + green + pushed. DS suite **3461 -> 3534 / 1 skipped / 1 xfailed / 1666 subtests passed** (+73: 25 Slice A + 48 Slice B). **Slice A `8335ce1` (merge `098c4d8`) feat(ds) cc_conditional FIRST consumer wire via `compute_cc_pressure(include_conditional=False)` kwarg (closes item 141 carry (h)):** `agents/daemon_slayer/cc_pressure.py` gains imports from cc_conditional + 2 new CcPressureResult fields (conditional_cc_seconds + conditional_entries) + include_conditional kwarg. Default False = byte-identical for 4 existing consumers (verified 170/170 across compute_ehp / enemy_cc_threat_line / compute_hybrid / routes_cc_blended_ehp_threat). True = probability-weighted sum via get_total_conditional_cc_seconds + ARAM tenacity via effective_cc_duration (unified math seam). Forward-marker scan UPDATED: `_ALLOWED_SOURCE_FILES = {"cc_pressure.py"}` allowlist; ehp/ability_dps/engine still pinned no-import. +25 tests in NEW `test_cc_conditional_consumer_pressure.py`. **Slice B `d4253dd` (merge `03003ed`) feat(ds) cc_conditional wave 1 expansion (10/10 -> 18/18; +8 entries / +8 new champs):** Bard Q terrain 0.3 / Karma W channel 0.4 / Taliyah W channel 0.5 / Kennen E nth_hit 0.6 / KSante Q nth_hit 0.7 (coexists with KSante R wave 5) / Ornn Q debuffed 0.5 (coexists with Ornn R wave 7) / Xayah E nth_hit 0.6 / Fiora W debuffed 0.4. No new condition tags. 8 REJECT candidates documented (Aurora R / Briar Q+R / Sylas E2 / Renata R / Aphelios Q / Lissandra E / Volibear R / Swain E). +48 tests in NEW `test_cc_conditional_wave1.py`. Consumer math byte-identical (data-lane only; 8 new entries opt-in via include_conditional=True from Slice A). **Slice C `2fa01d3` docs(roadmap-backlog) stale-entry sweep wave 7 per [[feedback_backlog_path_stale_check]]:** 2 entries flipped (ROADMAP L215 Fleet status DS row + BACKLOG L13 enemy-CC consumers chain) - both bumped to reflect ENGINE 1.37.0 + 103/89 + cc_conditional forward-marker. Sweep cycle decay: wave 1=2 / wave 2=3 / wave 3=3 / wave 4=1 / wave 5=1 / wave 6=1 / wave 7=2 (slight tick up). **Slice D CLEAN no-commit:** 9th consecutive cost/latency CLEAN since item 134. Read-only investigative agent surveyed 7 levers (prompt-cache / route TTL / polling cadences / log spam / model tier / scheduled tasks / bundle size) all CLEAN. **Orchestrator commit `cf5f509`** bumps ENGINE_VERSION 1.37.0 -> 1.38.0 + full changelog + 27 stale ENGINE pin syncs across 27 DS test files. 1 merge conflict in `test_cc_conditional_forward_marker.py` (both A and B edited - resolved by combining both allowlists). DS :8893 restarted via Stop-Process PID 11108 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.38.0. CI green push `cf5f509` (run id 26305752650 in flight). Full per-slice detail in CLAUDE.md item 142. **Don't-redo:** cc_conditional.py is NO LONGER a strict forward-marker; cc_pressure is the FIRST authorized consumer at 1.38.0. Future consumers MUST update `_ALLOWED_SOURCE_FILES` in `test_cc_conditional_forward_marker.py` in same commit + explain operator gate in CLAUDE.md. Default `include_conditional=False` MUST stay byte-identical for 4 existing consumers. Conditional contribution applies ARAM tenacity through SAME `effective_cc_duration` seam as unconditional path. 18 conditional entries are operator-tunable via per-entry `probability` overrides (parallel to 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints). 8 wave-1-REJECT candidates carry forward operator-gated. Forward-marker scan now uses BOTH `_ALLOWED_TEST_FILES` AND `_ALLOWED_SOURCE_FILES` allowlists. Orchestrator-merge pattern 9 consecutive runs (items 134-142). **Carries forward:** (a) cc_blended_ehp ecosystem still COMPLETE; live-game validation pending. (b) DD Defy STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) RC-PostmortemAnalyze 2026-05-24 04:15. (e) Calibration midpoints operator-gated. (f) Item 133 carry (c) operator-gated. (g) obj_participation widening operator-gated. (h) Future cc_conditional consumers: compute_ehp + compute_hybrid threading include_conditional kwarg are NEXT natural wires. (i) UI/UX live-game audit owed. (j) Multi-wave registry-merge schema lift proven at 4 champions. (k) cc_conditional registry now 18/18; future wave 2 expansion adds more from carrying REJECT lists. (l) 15+ wave-7 REJECTs + 8 wave-1 cc_conditional REJECTs carry forward operator-gated. (m) `_PER_SPELL_CC_DURATIONS` still at 103/89 (no wave 8 this run; unconditional first-order CC approaching saturation). Frozen-file grant NOT used.

- ✅ **item 141 - _PER_SPELL_CC_DURATIONS wave 7 + cc_conditional forward-marker module + ROADMAP stale-sweep wave 6 + cost/latency CLEAN - SHIPPED 2026-05-22 (3 worktree agents merged + orchestrator ENGINE bump commit `d7f6a49`, pushed; ENGINE 1.36.0 -> 1.37.0; non-frozen; DS :8893 restarted serves 1.37.0; RC :8888 unchanged pid 13420).** Operator `/headless-upgrade` 5th long-run of the day. 4 worktree agents dispatched concurrent; Slice D returned CLEAN no-commit (8th consecutive CLEAN cost/latency sweep per items 134-140). 3 commit-bearing slices merged + green + pushed. DS suite **3365 -> 3461 / 1 skipped / 1 xfailed / 1666 subtests passed** (+96: 37 Slice A + 59 Slice B); RC suite **3183 / 67 subtests / 0 failed** (phase8_smoke flipped pass post-DS-restart). **Slice A `ae0b59e` (merge `2840cc8`) feat(ds) `_PER_SPELL_CC_DURATIONS` wave 7 (+8 entries / +7 new champs + 1 multi-wave coexistence; 95/82 -> 103/89):** Via setdefault builder. NEW: Irelia E Flawless Duet stun 0.75/0.85/0.95/1.05/1.15s / Kalista R Fate's Call knockup 1.0s / Ornn R Call of the Forge God knockup 0.5s / Shyvana R Dragon's Descent knockback 1.0s / Smolder R Mountain Breaker knockup 1.25s / Vayne E Condemn knockback 0.5s / Zac R Let's Bounce knockup 1.0s (multi-wave: Zac E was wave 2) / Volibear E Sky Splitter airborne 0.25s (introduces airborne CC kind). 15+ REJECTs documented. +37 tests in NEW `test_per_spell_cc_registry_wave7.py`. Registry: 60% of 172 SR roster. **Slice B `7233568` (merge `00d6271`) feat(ds) conditional CC axis schema lift via NEW `agents/daemon_slayer/cc_conditional.py`:** Closes item 140 carry (h). ~360 LOC FORWARD-MARKER module (mirrors s112 STAT_GRANT_CALC_KEYS empty seam). NEW `ConditionalCcEntry` frozen dataclass + 10 condition tags (COND_NTH_HIT / COND_GOLD_CARD / COND_TERRAIN / COND_CHANNEL_COMPLETION / COND_DREAM_STACK / COND_DEVOUR_TARGET / COND_TARGET_HP_BELOW / COND_TARGET_DEBUFFED / COND_DUAL_ENEMY / COND_MODE_GATED) + 10 operator-tunable midpoints + setdefault builder + `get_conditional_entries()` + `get_total_conditional_cc_seconds(apply_probability)` aggregator. **Seed of 10 canonical conditional entries from wave 4/5/6 REJECT lists:** Brand R (stun 1.5/2.0/2.5s nth_hit 0.7) / TwistedFate W (stun 1.5s gold_card 0.4) / JarvanIV E (knockup 1.0s terrain 0.5) / TahmKench R (suppression 1.0s devour 0.4) / Volibear Q (knockback 0.75s terrain 0.3) / Warwick R (suppression 1.5/1.75/2.0s channel 0.5) / Viktor W (stun 1.5s nth_hit 0.6) / Mordekaiser R (banishment 7.0s mode_gated 1.0) / Sett E (stun 1.0s dual_enemy 0.6) / Vex E (fear 1.0-1.5s debuffed 0.5). REGISTRY_TOTAL_CHAMPIONS=10 / REGISTRY_TOTAL_ENTRIES=10. NO consumer wires; forward-marker boundary pinned by NEW `test_cc_conditional_forward_marker.py` (12 tests via 4 import-signature patterns). +47 tests in NEW `test_cc_conditional.py`. **Slice C `0d0c499` (merge `8fba572`) docs(roadmap) stale-entry sweep wave 6:** 1 flip in ROADMAP.md L166 DS calibration pipeline entry reframed with live numbers (2900 matches latest 2026-05-14 via RC-RewindCatchup; Ranked Solo queue 420 specifically still has latest 2025-12-15 + 0 SR records in ds_calibration.jsonl - reframed to accurate "Blocked on SR ranked freshness"). BACKLOG sweep CLEAN. Sweep cycle decay: wave 1=2 / wave 2=3 / wave 3=3 / wave 4=1 / wave 5=1 / wave 6=1. **Slice D CLEAN no-commit:** 8th consecutive cost/latency CLEAN since item 134. **Orchestrator commit `d7f6a49`** bumps ENGINE_VERSION 1.36.0 -> 1.37.0 + full changelog + 47 stale ENGINE pin syncs across 27 DS test files + 2 post-pin-sync fixes (wave6 start_marker revert + forward-marker scan refinement). 0 merge conflicts (3 disjoint surfaces). DS :8893 restarted via Stop-Process PID 15808 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.37.0. CI green push `d7f6a49` (run id 26303835667). Full per-slice detail in CLAUDE.md item 141. **Don't-redo:** registry now 103/89 (60% SR roster); `cc_conditional.py` is FORWARD-MARKER seam at 1.37.0 (no consumer wires; production byte-identical to 1.36.0); 10 `_DEFAULT_CONDITION_PROBABILITY` midpoints operator-tunable parallel to `_MISSING_HP_SHARE_FOR_HEALS=0.5` + `_CC_EFFECTIVENESS_FACTOR=0.5`; forward-marker scan uses import-signature patterns NOT substring; multi-wave coexistence via setdefault proven at 4 champions (Lulu / Sejuani / Thresh / Zac); orchestrator pattern 8 consecutive runs (items 134-141). **Carries forward:** (a) cc_blended_ehp ecosystem still COMPLETE; live-game validation pending. (b) DD Defy STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) RC-PostmortemAnalyze 2026-05-24 04:15. (e) Calibration midpoints operator-gated. (f) Item 133 carry (c) operator-gated. (g) obj_participation widening operator-gated. (h) Conditional CC schema lift SHIPPED as forward-marker; future consumer wire is next step. (i) UI/UX live-game audit owed. (j) Multi-wave registry-merge schema lift proven at 4 champions. (k) cc_conditional 10-entry seed canonical for conditional axis. (l) 15+ wave-7 REJECTs carry forward operator-gated. Frozen-file grant NOT used.

- ✅ **item 140 - cc_blended_ehp dashboard UI consumer + _PER_SPELL_CC_DURATIONS wave 6 + registry-merge schema lift + ROADMAP/BACKLOG stale-sweep wave 5 - SHIPPED 2026-05-22 (3 worktree agents + 3 merge commits `94d1de3` `5a7d86d` `81e56b5`, pushed; ENGINE 1.35.0 -> 1.36.0; non-frozen; DS :8893 restarted serves 1.36.0; RC :8888 restarted clean pid 12864 -> 13420 last_reload_ok=True - new route module triggered restart).** Operator `/headless-upgrade` 4th long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 139 carries (a) + (j). All 3 merged + green + pushed. RC suite **3140 -> 3183 / 67 subtests / 0 failed** (+43 Slice A); DS suite **3328 -> 3365 / 1 skipped / 1 xfailed / 1666 subtests passed** (+37 Slice B). **Slice A `3b6cc4b` (merge `5a7d86d`) feat(coach) cc_blended_ehp threat-balance chip (FIRST dashboard UI consumer; closes item 139 carry (a)):** NEW `dashboard/routes_cc_blended_ehp_threat.py` (~310 LOC) GET `/api/cc-blended-ehp-threat?ally=<csv>&enemy=<csv>&mode=ARAM` returns `{ok, mode, ally_avg_cc_blended_ehp, enemy_avg_cc_blended_ehp, ratio, tier (good|warn|bad), ally_total_cc_seconds, enemy_total_cc_seconds, elapsed_ms, cached}`. Symmetric construction: ally side feeds ENEMY roster as `enemy_champions` to compute_ehp (enemy CC erodes ally EHP); enemy side feeds ALLY roster. Tier bands: ratio >= 1.05 -> good (ally CC weathers better), 0.95-1.05 -> warn (even), < 0.95 -> bad (enemy CC erodes us more). 5-min TTL cache mode-aware (ARAM/SR don't share cache slot per `_CC_EFFECTIVENESS_FACTOR` mode-invariance of the underlying compute - but DIFFERENT champ rosters can land different cache keys). Fail-soft: 400 missing/blank, 200 with ok=false reason=no_champions when both empty, 200 ratio=1.0 tier=warn when both sides 0 CC, 503 on compute_ehp import failure. NEW `web/js/panels/cc_blended_ehp_threat.js` (~195 LOC ESM) champ-select chip with `_csvRenderCcBlendedEhpThreat()` helper mirroring `_csvRenderBanSuggestToggle()`; resolves numeric LCU ids via `CHAMPS.byId` to canonical DDragon slugs. Section signature `ccbe:<ally_count>:<enemy_count>` coalesces re-renders. Mount via `#cs-cc-blended-ehp-threat` between bans grid + pickorder. Tier-tinted via `data-cc-tier` attr. NEW `web/css/panels/cc_blended_ehp_threat.css` (~145 LOC) with 3 data-cc-tier color variants (good=ok-tinted green, warn=accent/amber, bad=bad-tinted red); imported via `web/css/dashboard.css`. +43 tests in NEW `test_routes_cc_blended_ehp_threat.py` (21: RouteContractTests 8 + MathTests 6 + CacheTests 3 + FailSoftTests 2 + AsciiHygieneTests 2) + NEW `test_cc_blended_ehp_threat_panel_dom.py` (22: ChipMountTests 5 + JsConsumptionTests 10 + CssTierVariantsTests 4 + AsciiHygieneTests 3). Math verified end-to-end via live curl post-restart: `ally=Annie,Galio,Leona&enemy=Aatrox,Garen,Sett&mode=ARAM` returned ok=true ratio=1.80 tier=good ally_total_cc_seconds=0.0 enemy_total_cc_seconds=7.0 elapsed_ms=22 (Annie+Galio+Leona summed 7.0s of registered CC; Aatrox+Garen+Sett have 0 in registry; ally CC erodes enemy EHP -> good). **Slice B `cec70c2` (merge `81e56b5`) feat(ds) _PER_SPELL_CC_DURATIONS schema lift + wave 6 (90/80 -> 95/82, ENGINE 1.35.0 -> 1.36.0):** Schema lift via NEW module-level `_build_per_spell_cc_durations()` builder function uses `registry.setdefault(champ, {})[spell] = tuple` semantics enabling multi-wave augmentation of single-champion spell maps (closes item 139 carry (j)). All 90 prior-wave entries preserved byte-identical in value. Wave 6 adds **+5 entries / +2 new champs**: Lulu R Wild Growth (knockup 1.0s all 3 ranks - previously rejected wave-5 dict collision), Sejuani Q Arctic Assault (knockup ranks 0.75/0.875/1.0/1.125/1.25 - previously rejected wave-5 dict collision), Thresh E Flay (knockback 0.4s all 5 ranks - previously rejected wave-5 dict collision), Bard R Tempered Fate (stasis 2.5s all 3 ranks - NEW champ), Lillia R Lilting Lullaby (sleep 2.0s all 3 ranks - NEW champ). Rell R REJECTED (drag mechanic, not discrete pull-stun). 23 REJECTED candidates with reason recorded (wave 4+5+6 conditional CC carryovers + slows + dict-collisions for already-seeded entries). Consumer math BYTE-IDENTICAL to 1.35.0 (data lane only; the schema lift is pure construction-shape, no math change). +37 tests in NEW `test_per_spell_cc_registry_wave6.py` (~522 LOC across 7 classes: WaveSixNewEntryShapeTests 9 / WaveSixValuePinsTests 6 / WaveOneThroughFivePreservedTests 10 / RegistryGrowthTests 4 / SchemaLiftBuilderTests 5 / EngineVersionCurrentTests 1 / AsciiHygieneTests 2). Pre-existing `test_per_spell_cc_registry_seed.py` 2-line count pin update (80/90 -> 82/95). `test_per_spell_cc_registry_wave5.py::test_lulu_w_polymorph_preserved` updated from `Lulu R == ()` to `Lulu R == (1.0, 1.0, 1.0)` reflecting now-unblocked multi-wave augmentation. **+21 stale ENGINE pin syncs** across 20 DS test files (test_effects_expansion 2 hits + 19 others 1 hit each: cc_pressure / s228 / s229 / s230 / s231 / s232_saturation / ehp / ehp_cc_blended / ehp_heal_phase6 / ehp_shield_phase15 / form_index_sweep_s226 / hybrid_enemy_champions / max_priority_sweep_s227 / nested_hp_parser_s223 / per_spell_cc_phase65 / post_parser_sweep_s225 / sum_of_blocks_expansion_s215 / sum_of_blocks_expansion_s217 / unit_variants_s224). ENGINE bumped in `agents/daemon_slayer/__init__.py` with full changelog block. **Slice C `765c2c6` (merge `94d1de3`) docs(backlog) stale-entry sweep wave 5 per [[feedback_backlog_path_stale_check]]:** 1 entry flipped: BACKLOG.md L13 enemy-CC consumers `8 -> 9 shipped` (recognizing item 139 Slice A `0523511` compute_hybrid scorer as 9th overall ecosystem consumer + FIRST DS-engine scorer consumer of cc_blended_ehp). Wave 6 docs sync further bumps to **10 consumers** (item 140 Slice A dashboard UI). ROADMAP.md sweep-clean. The [[feedback_backlog_path_stale_check]] sweep cycle continues to decay (wave 1=2 flips, wave 2=3 flips, wave 3=3 flips, wave 4=1 flip, wave 5=1 flip - methodology stable). **Merge order:** C first (docs `94d1de3`), A second (`5a7d86d`, route + new panel + DOM tests), B third (`81e56b5`, ability_dps.py schema lift + wave 6 + ENGINE bump + 21 pin syncs). 0 merge conflicts (3 entirely disjoint surfaces: A=dashboard/routes_cc_blended_ehp_threat.py + web/js/panels/cc_blended_ehp_threat.js + web/css/panels/cc_blended_ehp_threat.css + champ_select.js + index.html + 2 new test files; B=ability_dps.py + __init__.py + 1 new test file + 5 wave test pin updates + 20 ENGINE pin syncs; C=BACKLOG.md only). Worktrees + 3 worktree branches unlocked + force-removed + 3 branches deleted (local + remote). **Verified:** Wider RC **3183 passed / 67 subtests / 0 failed** (+43 over s139 3140 baseline = Slice A's threat-route + DOM tests). DS suite **3365 passed / 1 skipped / 1 xfailed / 1666 subtests** (+37 over 3328 baseline). py_compile + ruff + ASCII clean on all touched + new test files. DS :8893 restarted via taskkill PID 4420 + `schtasks /Run /TN RC-DaemonSlayer` ritual -> /health returns `engine_version=1.36.0 patch=16.10.1 champions=172 items=705`. RC :8888 restarted via `restart_trigger.txt` (new route module registration) -> pid 12864 -> 13420 alive=True last_reload_ok=True mode=client. Live curl `/api/cc-blended-ehp-threat?ally=Annie,Galio,Leona&enemy=Aatrox,Garen,Sett&mode=ARAM` returned ok=true ratio=1.80 tier=good as expected. CI green push `81e56b5` 1m22s. DS audit iter sweep: NO-CHANGE at engine 1.36.0 - no new Riot patch dropped (live patch still 16.10.1; DDragon latest 16.10.1 = RC current.txt); engine remains Meraki-coherent; the audit-streak halt from item 136 (13 consecutive no-changes) inherits forward. Cost/latency lever sweep CLEAN. UI/UX live-game-gated per CLAUDE.md hard-rule. **Don't-redo:** cc_blended_ehp ecosystem is now COMPLETE - 4 consumers shipped (engine math + coach prompt + DS scorer + dashboard UI). Default ratio=1.0 tier=warn when no CC champs on either side preserves the symmetric identity contract; pinned by MathTests. Symmetric construction (ally feeds ENEMY roster, vice versa) is the load-bearing semantic; do NOT collapse to same-side feeds. Tier bands (1.05/0.95) sit next to engine's `_CC_EFFECTIVENESS_FACTOR=0.5` + `_FIGHT_WINDOW_S=6.0` calibration - retune all panel tests when changing. Mode-aware cache key (ARAM and SR don't share); chip hidden until BOTH sides have at least one resolvable champion. The schema lift via `_build_per_spell_cc_durations()` builder function + `registry.setdefault(champ, {})[spell] = tuple` is the canonical pattern - any future wave 7+ should follow the same multi-wave-friendly construction; do NOT regress to a single dict literal (would re-introduce dict-key collisions). All 4 wave-6 case-1 entries (Lulu R / Sejuani Q / Thresh E / Bard R + Lillia R as 2 new champs) now coexist with prior-wave entries on the SAME champion's spell map; `SchemaLiftBuilderTests` (5 tests) pins via a setdefault demo fixture. Wave 5's `test_lulu_w_polymorph_preserved` assertion was FLIPPED from asserting `Lulu R == ()` (deferred) to `Lulu R == (1.0, 1.0, 1.0)` (now shipped); do NOT regress. The 21 stale ENGINE pin syncs are the s243/s246 facade-split precedent. Slice A + Slice B + Slice C are entirely disjoint surfaces; merge order any permutation conflict-free (7th consecutive run using this template - items 134-140). **Carries forward:** (a) The cc_blended_ehp ecosystem has 4 consumers; future work is live-game validation (operator plays a real ARAM with one of the 82 registered CC champs to verify all 4 surfaces - engine + coach prompt + DS scorer + dashboard chip - reflect the signal correctly). (b) DD Defy heal-on-takedown STILL deferred (carries from item 130+). (c) Live ARAM/SR smoke STILL pending. (d) RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 - verify LastTaskResult=0 next session; role_grades JSON will reflect void-aware obj_participation when cron fires. (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated unchanged. (f) Item 133 carry (c) personal calibration via `data/post_game_rubric_weights.json` STILL operator-gated. (g) obj_participation widening of the 4 OVERLAPPING challenges_json fields STILL operator-gated. (h) Conditional CC axis schema lift STILL operator-gated. (i) UI/UX live-game audit ritual owed once operator plays a real game with the new dashboard threat-balance chip live. (j) Frozen-file grant authorized this run but NOT used - all 3 agents routed entirely around the frozen list; grant does NOT carry forward.

- ✅ **item 139 - compute_hybrid enemy_champions kwarg + cc_blended_ehp scoring + _PER_SPELL_CC_DURATIONS wave 5 + ROADMAP/BACKLOG stale-sweep wave 4 - SHIPPED 2026-05-22 (3 worktree agents + 3 merge commits `9f2fd6c` `a663320` `86724ea` + 1 orchestrator ENGINE bump commit `6fd05db`, pushed; ENGINE 1.34.0 -> 1.35.0; non-frozen; DS :8893 restarted serves 1.35.0; RC :8888 unchanged - all 3 slices touched DS engine + docs only, no route module edits).** Operator `/headless-upgrade` 3rd long-run of the day. RC suite **3140 / 67 subtests / 0 failed** (unchanged - DS-engine-only run); DS suite **3269 -> 3328 / 1 skipped / 1 xfailed / 1666 subtests passed** (+59: 22 Slice A + 37 Slice B). Slice A `0523511` (merge `a663320`) `compute_hybrid` in `hybrid.py:178` gains optional `enemy_champions: Iterable[str] = ()` kwarg threaded to ALL 3 internal `compute_ehp(...)` call sites. When non-empty, `hybrid_score = alpha*dps + beta*cc_blended_ehp` (POST-CC) instead of `beta*blended_ehp`. Default empty preserves byte-identical hybrid_score (item 137 identity contract). HybridResult gains `cc_blended_ehp` + `enemy_champions` fields. FIRST DS-engine SCORER consumer of `cc_blended_ehp`. +22 tests in NEW `test_hybrid_enemy_champions.py` with MultiCallSiteThreadingTests unittest.mock spy. Math verified live: Aatrox L11 SR + (Annie/Morgana/Malzahar) summed CC 7.0s clamps fraction 1.0 -> cc_blended_ehp = blended * 0.5 -> hybrid_score delta = beta * 680.99 (4 decimal). Slice B `d7af3be` (merge `86724ea`) extends `_PER_SPELL_CC_DURATIONS` from 82/73 to **90 entries / 80 champs** with +8 entries / +7 new champs: Hecarim (E+R), KSante (R), Mordekaiser (E), Urgot (E), Viego (W), Yone (R), Ziggs (W). 9 dict-key collision REJECTs (Lulu R / Quinn E / Shen E / Jax E / Jinx E / Janna Q / Sejuani Q / Rell R / Thresh E) + 19 conditional REJECTs. Consumer math BYTE-IDENTICAL to 1.34.0 (data lane only). +37 tests in NEW `test_per_spell_cc_registry_wave5.py`. Slice C `25dec0c` (merge `9f2fd6c`) 1 entry flipped: BACKLOG.md L13 enemy-CC consumers `7 -> 8 shipped` (recognizing item 138 `cc_blended_ehp_impact_line` as 8th). Sweep-rate decay: 2/3/3/1. Orchestrator commit `6fd05db` bumps ENGINE 1.34.0 -> 1.35.0 + syncs **26 stale ENGINE pin sites** across 25 DS test files + 6 source-docstring pins + 5 mislabel/docstring bumps. 0 merge conflicts (3 entirely disjoint surfaces). DS :8893 restarted via taskkill PID 13516 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.35.0 patch=16.10.1. RC :8888 unchanged. CI green push `6fd05db` 1m23s. Full per-slice detail in CLAUDE.md item 139. **Don't-redo:** compute_hybrid `enemy_champions=()` default preserves byte-identical hybrid_score; HybridResult.ehp keeps blended_ehp semantics, cc_blended_ehp is the NEW POST-CC surface alongside (do NOT collapse); `_hybrid_delta_pct` percentage normalizer keeps blended_ehp (rank-stability across enemy_champions states); the 9 wave-5 dict-key collisions need a registry schema lift (do NOT add without it); `_PER_SPELL_CC_DURATIONS` now 90/80 (do NOT add slows / conditional / wave-5 REJECT list); the 26 pin sync count is the s243/s246 facade-split precedent; orchestrator-merge pattern is now 6 consecutive runs (items 134-139). **Carries forward:** (a) `cc_blended_ehp` has 3 consumers (engine math + coach prompt + DS scorer); next surface is a DASHBOARD UI consumer operator-gated. (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) `_MISSING_HP_SHARE_FOR_HEALS` + `_CC_EFFECTIVENESS_FACTOR` calibrations operator-gated. (e) Conditional CC axis schema lift + 9 wave-5 dict-collision schema lift STILL operator-gated. (f) UI/UX live-game audit ritual owed.

- ✅ **item 138 - cc_blended_ehp coach prompt consumer + _PER_SPELL_CC_DURATIONS wave 4 + ROADMAP/BACKLOG stale-sweep - SHIPPED 2026-05-22 (3 worktree agents + 3 merge commits `b19772d` `b1992fc` `54cf688`, pushed; ENGINE 1.33.0 -> 1.34.0; non-frozen; DS :8893 restarted serves 1.34.0; RC :8888 restarted clean pid 7540 -> 12864 last_reload_ok=True - coach module edits triggered restart).** Operator `/headless-upgrade` 2nd long-run of the day authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 137 carry (a) coach-prompt half + adjacent registry wave 4 + stale-sweep. All 3 merged + green + pushed. RC suite **3097 -> 3140 / 67 subtests / 0 failed** (+43 Slice A); DS suite **3232 -> 3269 / 1 skipped / 1 xfailed / 1666 subtests passed** (+37 Slice B). Slice A `738c005` NEW `core/cc_blended_ehp_context.py` (~120 LOC) `cc_blended_ehp_impact_line(enemies, mode, *, fight_window_s=6.0, cc_effectiveness_factor=0.5) -> str` is the FIRST coach-prompt consumer of `cc_blended_ehp` (SECOND consumer overall after item 137 Slice A engine math). Sibling of `core/enemy_cc_threat_context.py` per-enemy breakdown - aggregates ACROSS all enemies into ONE summary line. Math mirrors `ehp.py:compute_ehp` exactly: `total_cc_seconds = sum(...)`; `cc_pressure_fraction = min(total/fight_window, 1.0)`; `effective_ehp_loss_pct = fraction * factor * 100`. Output: `"Enemy CC pressure: 2.5s over 6s fight = 42% saturation, blended-EHP impact -21%"`. Returns empty when mode is None/blank, enemies is None/empty, or total_cc_seconds == 0.0 (90% non-target = zero token overhead). Defaults `_FIGHT_WINDOW_S=6.0` + `_CC_EFFECTIVENESS_FACTOR=0.5` MUST match `ehp.py` exactly. Wired into all 4 active mode coach prompts APPEND-only per cache-prefix preservation. ImportError fallback stub mirrors enemy_cc_threat_context. +43 tests in NEW `test_cc_blended_ehp_context.py`. NO ENGINE bump (coach-prompt consumer, no engine math change). Slice B `6718445` extends `_PER_SPELL_CC_DURATIONS` from 67/58 to **82 entries / 73 champs** with +15 first-order CC entries across 15 new champions. 10 REJECTED candidates with reason recorded (Bard Q / Evelynn W / Hwei E / Karma W / Seraphine E / Swain E / Syndra E / TwistedFate W / Zilean Q / Volibear R) - durable across future audits. Consumer math BYTE-IDENTICAL to 1.33.0 (data lane only). +37 tests in NEW `test_per_spell_cc_registry_wave4.py`. **+21 stale ENGINE pin syncs** across DS test files (1.33.0 -> 1.34.0). Slice C `9e0220b` 3 stale entries flipped: BACKLOG.md L13 enemy-CC consumers `6 -> 7 shipped` (recognizing item 137 Slice A `99164e8` cc_blended_ehp field as 7th); reframed "remains future" sentence - the BLENDED SCORER is shipped, future surface is a LIVE UI CONSUMER. ROADMAP.md added item 137 SHIPPED entry mirroring item 136 style. ROADMAP.md L205 Fleet status DS row bumped 1.32.0 -> 1.33.0 + tests 3164 -> 3232 + wave 3 + cc_blended_ehp annotations. 0 merge conflicts (3 entirely disjoint surfaces). DS :8893 restarted via taskkill PID 6668 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.34.0 patch=16.10.1 champions=172 items=705. RC :8888 restarted via `restart_trigger.txt` (coach module edits) -> pid 7540 -> 12864 alive=True last_reload_ok=True. CI green push `54cf688` 1m22s. Full per-slice detail in CLAUDE.md item 138. **Don't-redo:** `cc_blended_ehp_impact_line` defaults MUST match `ehp.py` exactly; the line is APPENDED via `_USER_TMPL` per-call (NOT system prompt) - cache-prefix preservation; aggregates ACROSS enemies into ONE summary line (complementary to `enemy_cc_threat_line` per-enemy breakdown - do NOT collapse); ImportError fallback stub mirrors enemy_cc_threat_context (do NOT remove); `_PER_SPELL_CC_DURATIONS` now 82 entries / 73 champs of first-order CC at 16.10.1 (do NOT add slows, do NOT add the 10 REJECTED wave-4 candidates without conditional-axis schema lift). **Carries forward:** (a) `cc_blended_ehp` has 2 consumers (engine math + coach prompt); next surface is a DASHBOARD UI consumer (champ-select threat-tag panel / per-mode DS pick rankings under enemy-CC) operator-gated. (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending - operator must play a real game with one of the 73 registered CC champs. (d) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated. (e) Conditional CC axis schema lift STILL operator-gated.

- ✅ **item 137 - EHP-vs-CC blended scorer + _PER_SPELL_CC_DURATIONS wave 3 + ROADMAP/BACKLOG stale-sweep - SHIPPED 2026-05-22 (3 worktree agents + 3 merge commits `e7e8a5c` `aa78705` `64d20d9` + 1 ENGINE bump commit `665b533`, pushed; ENGINE 1.32.0 -> 1.33.0; non-frozen; DS :8893 restarted serves 1.33.0; RC :8888 unchanged - all 3 slices touched DS engine + docs only, no route module edits).** Operator `/headless-upgrade` long-run authorized with frozen-file grant + parallel orchestrator pattern. 3 worktree agents dispatched concurrent on item 136 carry (a) + adjacent registry expansion + stale-sweep. All 3 merged + green + pushed. RC suite **3096 -> 3097 / 67 subtests / 0 failed** (+1 phase8_smoke `test_live_three_profiles` flip post-DS-restart); DS suite **3164 -> 3232 / 1 skipped / 1 xfailed / 1666 subtests passed** (+68: 34 Slice A + 34 Slice B). Slice A `99164e8` `agents/daemon_slayer/ehp.py` gains NEW `_CC_EFFECTIVENESS_FACTOR = 0.5` module constant + lazy import of `compute_cc_pressure` inside `compute_ehp` body (LOAD-BEARING - avoids load-time circular: cc_pressure imports `effective_cc_duration` from ehp). EhpResult gains 3 new fields: `enemy_cc_pressure_s` + `cc_pressure_fraction` + `cc_blended_ehp`. `compute_ehp()` gains optional `enemy_champions: Iterable[str] = ()` kwarg AFTER `augments`. Math: `enemy_cc_pressure_s = sum(compute_cc_pressure(e, mode).total_cc_seconds for e in enemy_champions if e)`; `cc_pressure_fraction = min(enemy_cc_pressure_s / _FIGHT_WINDOW_S, 1.0)` where `_FIGHT_WINDOW_S=6.0`; `cc_blended_ehp = blended_ehp * (1.0 - cc_pressure_fraction * _CC_EFFECTIVENESS_FACTOR)`. Empty `enemy_champions=()` default leaves 3 new fields at 0.0 / 0.0 / equal-to-blended_ehp - identity preservation. Per-type EHP fields UNCHANGED when enemy_champions provided - CC discount lives ONLY in cc_blended_ehp. SECOND engine-math consumer of `compute_cc_pressure` (item 136 was first). +34 tests in NEW `test_ehp_cc_blended.py`. Slice B `f989522` extends `_PER_SPELL_CC_DURATIONS` from 53/44 to **67 entries / 58 champs** with +14 first-order CC entries across 14 new champions: AurelionSol R / Caitlyn W / Camille E / Diana R / Elise E / Heimerdinger E / Ivern Q / Malphite R / Pyke Q / Rell Q / Ryze W / Sion Q / Tristana R / XinZhao W. Same selection rules as waves 1+2 (first-order CC only, no slows, no conditional CC). +34 tests in NEW `test_per_spell_cc_registry_wave3.py`. Slice C `1125708` 3 stale entries flipped via grep-verification: BACKLOG.md L13 enemy-CC consumers `4 -> 6 shipped`; ROADMAP.md L203 Fleet status DS row bumped from `1.31.0 / 3120` to `1.32.0 / 3164` + cc_pressure aggregator + enemy_cc_threat_line annotations; ROADMAP.md top entry added for item 136. Orchestrator commit `665b533` bumps ENGINE 1.32.0 -> 1.33.0 + syncs **21 stale ENGINE pins** across DS test files. 0 merge conflicts (3 entirely disjoint surfaces). DS :8893 restarted via Stop-Process pid 4444 + `schtasks /Run /TN RC-DaemonSlayer` -> /health serves engine_version=1.33.0 patch=16.10.1 champions=172 items=705. Full per-slice detail in CLAUDE.md item 137. **Don't-redo:** `_CC_EFFECTIVENESS_FACTOR = 0.5` operator-tunable conservative midpoint pinned by MathTests + ContractTests; `compute_ehp(enemy_champions=())` default leaves cc_blended_ehp == blended_ehp + the 3 new fields at 0.0 (identity preservation); per-type EHP fields UNCHANGED when enemy_champions provided (CC discount lives ONLY in cc_blended_ehp); lazy import of `compute_cc_pressure` inside compute_ehp body is LOAD-BEARING (avoids load-time circular); per-enemy duplicate enumeration is INTENTIONAL; saturation contract: 5 heavy CC enemies (15s summed) clamps `cc_pressure_fraction` at 1.0 exact -> `cc_blended_ehp = blended * 0.5` floor; `_PER_SPELL_CC_DURATIONS` now 67 entries / 58 champs of first-order CC at 16.10.1 (do NOT add slows, do NOT add conditional CC without a conditional axis schema lift); the 21 stale ENGINE pin syncs are the s243/s246 facade-split precedent. **Carries forward:** (a) `cc_blended_ehp` field is now in production but no LIVE CONSUMER reads it yet in dashboard or coach prompts - future champ-select threat-tag panel or per-mode DS pick rankings under enemy-CC scenarios; operator-gated. (b) DD Defy heal-on-takedown STILL deferred. (c) Live ARAM/SR smoke STILL pending. (d) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` + `_CC_EFFECTIVENESS_FACTOR = 0.5` calibrations operator-gated. (e) Conditional CC axis schema lift (Brand R / TF W / Tahm Q / Lillia R / Volibear Q / Urgot R / Rell W / Warwick R / Viktor W / Tahm Kench R) STILL operator-gated.

- ✅ **item 136 - cc_pressure aggregator (FIRST `_PER_SPELL_CC_DURATIONS` consumer) + enemy_cc_threat_line coach prompt + ROADMAP stale-sweep - SHIPPED 2026-05-22 (3 worktree agents + 3 merge commits `5208caf` `05bbcdd` `4a45546`, pushed; ENGINE 1.31.0 -> 1.32.0; non-frozen; DS :8893 restarted serves 1.32.0; RC :8888 unchanged - non-route slice; coach modules reload prompts on next coaching tick without restart per the cached system prompt discipline).** Operator `/headless-upgrade` long-run authorized with frozen-file grant + parallel orchestrator. 3 worktree agents dispatched concurrent on item 135 carry (h) - the open seam from the registry seed. All 3 merged + green + pushed. Slice A `81cf8a3` NEW `agents/daemon_slayer/cc_pressure.py` (~240 LOC) is the FIRST aggregator consumer of the `_PER_SPELL_CC_DURATIONS` registry: NEW `CcSpellEntry` + `CcPressureResult` frozen dataclasses + `compute_cc_pressure(champion, mode="SR") -> CcPressureResult` aggregates max-rank CC durations across registered Q/W/E/R spells with per-mode aramTenacity applied through `ehp.effective_cc_duration` (unified math seam from item 122 / ENGINE 1.25.0). Returns empty result for unknown / blank / None - never raises. SR mode identity (tenacity=1.0); ARAM mode lengthens for 17 modified-tenacity champs. Spell ordering Q-W-E-R canonical. +44 tests in NEW `test_cc_pressure.py`. +15 stale ENGINE pin syncs 1.31.0 -> 1.32.0. Slice B `6aff49f` NEW `core/enemy_cc_threat_context.py` (~201 LOC) is the RC-side mirror of `aram_tenacity_context.enemy_aram_tenacity_line` - reads Slice A's `compute_cc_pressure` per enemy + mode, renders single prompt-friendly line `"Enemy CC threats: Morgana 3.0s root (Q), Malzahar 2.5s suppress (R), Annie 1.5s stun (R)"` sorted desc by `total_cc_seconds` (alphabetical tiebreak), limit=3 default. NEW `_CC_KIND_HINTS` table (53 entries matching registry breadth at patch 16.10.1). Mode-agnostic (works for SR/ARAM/KIWI/Arena/Brawl). Wired into all 4 active mode coach prompts (ARAM/Arena/Brawl/SR) APPEND-only per cache-prefix preservation (CLAUDE.md item 121). +50 tests in NEW `test_enemy_cc_threat_context.py`. Slice C `b063c9e` ROADMAP `L156` DS calibration entry reframed (stale "envisioned in ADR-007 phase 2" -> SHIPPED-clarified across `core/post_game_rubric.py` + `scripts/postmortem_analyze.py` + `core/death_patterns_loader.py` per items 124-132); ROADMAP `L203` Fleet status DS row bumped (stale ENGINE 0.90.0 / 547 items / 1953 tests -> live 1.31.0 / 705 / 3120 / 172 champs); BACKLOG.md sweep CLEAN (zero entries flipped). DS suite **3120 -> 3164 / 1 skipped / 1 xfailed / 1666 subtests** (+44 from Slice A); wider RC **3046 -> 3096 / 67 subtests / 0 failed** (+50 from Slice B). 0 merge conflicts (all 3 slices touched entirely disjoint surfaces). py_compile + ruff + ASCII clean across all 30 touched files. DS :8893 restarted via taskkill + schtasks /Run RC-DaemonSlayer -> /health serves engine_version=1.32.0. Full per-slice detail in CLAUDE.md item 136.

- ✅ **item 135 - voidMonsterKill enrichment + _PER_SPELL_CC_DURATIONS wave 2 - SHIPPED 2026-05-22 (2 merge commits `808c50b` `69422f0`, pushed; ENGINE 1.30.0 -> 1.31.0; DS :8893 restarted serves 1.31.0; non-frozen; RC :8888 restarted clean pid 12492 -> 7540 last_reload_ok=True).** 2 worktree agents in parallel on 2 disjoint slices composing on item 134 carries (g) + (h) data-side. Slice A `0eacc14` extends `core/obj_participation.py` from 7-column to 8-column by ALSO reading `voidMonsterKill` (Voidgrubs) from `participants.challenges_json` (100% blob coverage; no SQL overlap; the other 4 challenges keys overlap with existing SQL columns - DO NOT add). NEW `_challenges_objectives(blob) -> tuple[float, float]` parses JSON ONCE per row and extracts herald + void in one trip. Live curl on operator's NA1_5560540832 returned `total_score=37.28 grade=C obj_participation=0.331` (pre-Slice 0.364 because teammate got the voidgrubs; mathematically correct denominator-grew lift). +43 tests across `test_obj_participation.py` 49 -> 81, `test_routes_post_game_rubric_obj.py` 11 -> 16, `test_postmortem_analyze.py` 68 -> 74. Slice B `758756f` extends the 30-entry/24-champ `_PER_SPELL_CC_DURATIONS` seed shipped item 134 to **53 entries / 44 champs** with 23 new first-order CC entries across 20 new champions of patch 16.10 (Alistar Q+W / Amumu Q+R / Anivia Q / Braum R / Chogath Q / Fiddlesticks Q / Gnar R / Gragas E / Jhin W / Lux Q / Nami Q / Neeko E+R / Orianna R / Poppy E / Riven W / Singed E / Skarner R / Varus R / Xerath E / Zac E). Consumer math unchanged; data flows through `AbilitySpellDps.cc_duration_s` + `cc_duration_post_tenacity` for API inspection. +40 tests in NEW `test_per_spell_cc_registry_wave2.py`. +17 stale ENGINE pin syncs 1.30.0 -> 1.31.0 across DS test files. DS suite **3080 -> 3120 passed**; wider RC suite **3004 -> 3046 / 0 failed**. Full per-slice detail in CLAUDE.md item 135.

- ✅ **item 134 - herald_takedowns enrichment + _PER_SPELL_CC_DURATIONS registry seed - SHIPPED 2026-05-21 (2 merge commits, pushed; ENGINE 1.29.0 -> 1.30.0; DS :8893 restarted serves 1.30.0; non-frozen; RC :8888 restarted clean pid 5220 -> 12492 last_reload_ok=True).** 2 worktree agents in parallel on 2 disjoint slices. Slice A `d3dc4f5` extends `core/obj_participation.py` from 6-column to 7-column by reading `participants.challenges_json` Match-V5 blob + JSON-parsing per row + extracting `riftHeraldTakedowns` into both numerator + denominator (96% blob coverage; fail-soft on every degenerate shape). Live curl on operator's recent ADC match `NA1_5560540832` returned `total_score=37.61 grade=C obj_participation=0.364` (pre-Slice 0.404 because herald went to teammate; mathematically correct denominator-grew lift). +27 tests across `test_obj_participation.py` 29 -> 49, `test_routes_post_game_rubric_obj.py` 7 -> 11, `test_postmortem_analyze.py` 65 -> 68. Slice B `05f2475` populates the EMPTY `_PER_SPELL_CC_DURATIONS` seam shipped item 130 with 30 starter entries across 24 champions of first-order CC (stuns / roots / suspensions / knock-ups / charms / sleeps / fear / silence - skips slows by design). Consumer math unchanged; data flows through `AbilitySpellDps.cc_duration_s` + `cc_duration_post_tenacity` for API inspection. +52 tests in NEW `test_per_spell_cc_registry_seed.py`. +15 stale ENGINE pin syncs 1.29.0 -> 1.30.0 across DS test files. DS suite **3028 -> 3080 passed**; wider RC suite **2964 -> 3004 / 0 failed**. Full per-slice detail in CLAUDE.md item 134.
- ✅ **EHP Phase 6 healing throughput - SHIPPED 2026-05-21 (1 commit `5018e9f`, pushed; ENGINE 1.27.0 -> 1.28.0; DS :8893 restarted serves 1.28.0; non-frozen; no RC restart).** Closes the `ehp.py:23` deliberate Phase-1 omission "Healing throughput (lifesteal, Spirit Visage amp) - fits Phase 6". Three contributions feed an EHP heal pool: (a) item-passive heals via NEW `ItemHeal` dataclass + `ItemEffect.heal` (Sundered Sky 6610 / Arena 226610 Lightshield Strike 100% base AD melee / 50% ranged, one-trigger-per-fight under 10s/target CD); (b) lifesteal-derived heal `stats.lifesteal * stats.ad * stats.as * _FIGHT_WINDOW_S (6.0)` over the standard fight window (pre-mitigation approximation matching the EHP scorer's no-enemy-pen Phase-1 posture); (c) multiplicative heal amp via NEW `ItemEffect.heal_amp_pct` (Spirit Visage 3065 / Arena 223065 Boundless Vitality +25%; stacks multiplicatively per buff-system doctrine). Bloodthirster 3072 / Arena 223072 Ichorshield ships in the Phase 1.5 ItemShield pipeline (full-cap steady-state 165 L1 -> 315 L18 ANY damage type; NO `unique_passive_key="lifeline"` so it stacks alongside Sterak/Shieldbow/Maw/Hexdrinker at the engine layer). EhpResult gains heal_item_total / heal_lifesteal / heal_amp_mult / heal_total / heal_sources fields; compute_ehp folds heal_total into physical_ehp / magical_ehp / true_ehp at the top of the damage stack (heals don't discriminate by damage type, so the heal pool acts like an ANY shield). Deliberate Phase 6 omissions (deferred to Phase 6.5+): Death's Dance Defy heal-on-takedown (takedown-rate uncertain; DD 6333 + 226333 stay defensive_only), Spirit Visage amp on Phase 1.5 SHIELDS (engine ships heal-pipeline amp only; SV + lifeline-item under-credits the shield piece by ~15%), Sundered Sky 6% missing-HP additive (needs a current-HP-share assumption distinct from full-HP convention). Math verified exact: Aatrox L11 + Sundered Sky + Spirit Visage = base AD 103.875 * 1.25 amp = 129.84 heal_total; Aatrox L11 + BT alone shield_any=198.33 (165 + 2/9 * 150 at L11) + heal_lifesteal=134.67 (0.15 * 183.88 * 0.8137 * 6). +56 tests in NEW `tests/test_ehp_heal_phase6.py` across 11 classes (ItemHealSchema 9 / CollectHeals 5 / TotalHealAmp 6 / LifestealHeal 7 / BTIchorshield 8 / SunderedSkyHeal 4 / SpiritVisageAmp 5 / EhpResultHealFields 4 / DeathsDanceDeferred 3 / BTPlusLifelineStacks 2 / EngineVersionCurrent 1). +15 stale ENGINE pin syncs across 14 DS test files (1.27.0 -> 1.28.0). DS suite 2906 -> **2962 passed** / 1 skipped / 1 xfailed / 1663 subtests; wider RC suite 2780/0 (phase8_smoke flipped pass post-DS-restart). py_compile + ruff + ASCII clean on all 5 touched source files (0 non-ASCII bytes added; my new test file is 100% ASCII). DS :8893 restarted via taskkill + schtasks /Run RC-DaemonSlayer ritual; live `/health` returns engine_version=1.28.0 patch=16.10.1 champions=172 items=705. Full per-slice detail in CLAUDE.md item 129.

- ✅ **EHP Phase 1.5 shield throughput - SHIPPED 2026-05-21 (ENGINE 1.26.0 -> 1.27.0; DS :8893 restarted serves 1.27.0).** Closes the `ehp.py:21` deliberate Phase-1 omission "Shield throughput (Sterak's lifeline, Doran's Shield, Bloodthirster) - needs uptime modeling". Ships the 4 LIFELINE-style shields (single trigger per fight, value-additive to the effective-HP pool at top of damage stack): Sterak's Gage 3053 (60% bonus_hp ANY), Immortal Shieldbow 6673 (400 L1-L8 -> 700 L18 ANY ranged x0.80), Maw of Malmortius 3156 (200 + 150% bonus_ad MAGICAL ranged x0.75), Hexdrinker 3155 (110 L1-L8 -> 280 L18 MAGICAL ranged x0.75). All values from Meraki 16.10.1. NEW `ItemShield` dataclass + `ItemEffect.shield` field; `EhpResult` gains 5 shield_* fields; `compute_ehp` folds totals into physical_ehp / magical_ehp / true_ehp at top of damage stack. Bloodthirster ichor-shield + Doran's Shield block-per-source DEFERRED to Phase 6. +46 tests; +15 stale ENGINE pin syncs; DS suite 2860 -> 2906 passed; wider RC 2780 unchanged; py_compile + ruff + ASCII clean. Full per-slice detail in CLAUDE.md item 128.

- ✅ **Per-enemy ARAM tenacity wire - SHIPPED 2026-05-21 (non-engine; non-frozen; RC restart owed).** Symmetric extension of item 126: aramTenacity is the multiplier on ANY CC on that champion, whether enemy CC on operator (item 126) OR operator's CC on enemy (this slice). NEW `enemy_aram_tenacity_line(enemies, mode) -> str` in `core/aram_tenacity_context.py` renders a single line `"Enemy ARAM tenacity (your CC on them lasts longer): Talon 1.20x, Zed 1.20x"` listing only modified enemies, sorted desc-mult then alpha. Wired into `coaches/aram_coach.py` `_USER_TMPL` next to `{aram_tenacity}` (self) - both lines empty in the 90% non-ARAM case so prompt size is unchanged. `tests/test_ds_pick_consumption_p1l11.py` fixture +1 line. +18 tests; RC suite 2762 -> 2780 passed; py_compile + ruff + ASCII clean. Full per-slice detail in CLAUDE.md item 127.

- ✅ **EHP enemy-CC coach consumer (ARAM tenacity) - SHIPPED 2026-05-21 (non-engine; non-frozen; RC restart owed).** Closes the ENGINE 1.25.0 BACKLOG carry "Future EHP enemy-CC model for `aram_tenacity_mult`". DS audit ceiling at engine 1.26.0 (streak 13 halt); no Meraki drift to action - pivoted to first live consumer of the tenacity helper. NEW `core/aram_tenacity_context.py` reads `data/daemon_slayer/<patch>/champions.json` once at module-load + maps 17 ARAM-modified champs (Akali/Belveth/Ekko/Evelynn/Katarina/Kayn/Khazix/Lucian/Nunu/Pyke/Qiyana/Quinn/Rengar/Talon/Zed at 1.20x; Elise/Fizz at 1.10x). `aram_tenacity_line(champion, mode) -> str` renders a single prompt-friendly line using `agents.daemon_slayer.ehp.effective_cc_duration` for the worked example; returns empty when mode != ARAM/KIWI, champion unknown, or multiplier == 1.0 (90% of calls). Wired into `coaches/aram_coach.py` `_USER_TMPL` between `Enemy keystones` and `DS top items` - per-call injection via state["champion"] + state["game_mode"]; the cached system prompt is unchanged so prompt-cache discipline preserved (per item 121 / `reference_coach_choices_native_emit`). +29 tests in NEW `tests/test_aram_tenacity_context.py` (LoadTenacityMapTests 5 / GetTenacityMultTests 4 / AramTenacityLineTests 10 / FailSoftTests 6 / AramCoachWireTests 4). Pre-existing `tests/test_ds_pick_consumption_p1l11.py` fixture extended (+1 line `aram_tenacity=""`). Full RC suite 2733 -> **2762 passed / 0 failed** (+29); DS suite 2860 unchanged; py_compile + ruff + ASCII clean on all 4 touched files. **Don't-redo:** the line is APPENDED via `_USER_TMPL` not system prompt - per-call injection by design (champion changes per match; module-import caching of a per-champion fact would be wrong); do NOT migrate to a system-prompt suffix. The map at module-load is the right cache (patch changes infrequently; RC restart picks up new patch via `data/daemon_slayer/current.txt`). The 17-champ set is patch-data-driven via `champions.json` -> `lolmath.aram_modifiers.aramTenacity`; do NOT hardcode the list. Worked example always uses 1.0s as base (multiplier directly readable). Only ARAM coach is wired - Brawl/Arena/SR coaches do NOT have aramTenacity in their modes; do NOT extend to those. EhpResult / fight-sim / per-spell CC consumers are SEPARATE seams (still future) - the `effective_cc_duration` helper at `agents/daemon_slayer/ehp.py:77` is intact and reusable.

- ✅ **Postmortem follow-up 4-slice drain - SHIPPED 2026-05-21 (4 commits `50c83ac` `6d22e2e` `3cfea90` + 2 merges, pushed; non-engine; non-frozen; RC restarted clean pid 5972 -> 2640; `RC-PostmortemAnalyze` scheduled task LIVE NextRun 2026-05-24T04:15).** Closes item 124 carry-forward (a)/(b)/(d) + item 70 PGR S2-S5 stale-doc sweep. Operator AskUserQuestion-picked "all 4 in parallel" from a 4-option fork; 2 parallel worktree subagents (B + C) + 2 inline slices (A + D). **(B) 2 new death patterns** `50c83ac`: `small_skirmish` (assists in {1,2}; fills gap between caught_4plus and solo_1v1_loss; assist-band elif) + `midgame_collapse` (8:00-15:00; independent if-block). +10 tests. NEW top-3 after live regen: solo_pickoff (59%) / **small_skirmish (49%)** / caught_4plus (44%) - small_skirmish DISPLACED rapid_repeat. **(C) /api/personal-context route + frontend panel** `6d22e2e`: NEW `dashboard/routes_personal_context.py` (60s mtime cache; ok=false reason=no_data with HTTP 200 on missing JSON) + 3-card stack inside Right Now panel body (label + count chip + rate% chip + truncated description). `core/death_patterns_loader.py` extended `top_patterns()` with rate/confidence + NEW `report_summary()`. +31 tests. **(A) Cadence task LIVE** `3cfea90`: NEW `ops/install_RC_PostmortemAnalyze.ps1` registers `RC-PostmortemAnalyze` weekly Sundays 04:15 (15 min after RC-RewindCatchup 04:00 - LOAD-BEARING offset) + NEW `ops/run_postmortem_with_restart.ps1` wrapper logs + writes `restart_trigger.txt` only on clean exit. Task REGISTERED LIVE this session. **(D) Stale docs sweep** `3cfea90`: ROADMAP line 70 PGR aggregator-G-style reframe flipped 🟡 -> ✅ (S2-S5 all shipped per CLAUDE.md items 115-118); other open entries verified legitimately operator-gated. Full RC suite 2692 -> **2733 passed / 0 failed** (+41); CI green; live `/api/personal-context` returns top-3. Full per-slice detail in CLAUDE.md item 125. **Don't-redo:** assist-band/time-band classifier shape pinned (small_skirmish elif + midgame_collapse if); route ok=false uses HTTP 200 not 404; cache key is MTIME not TTL; loader is single-source-of-truth; cadence task 04:15 offset is load-bearing; the 04:24 first run on 2026-05-24 is the live test - verify LastTaskResult=0 + `logs/postmortem_analyze.2026-05-24.log`.

- ✅ **ADR-007 phase 2 postmortem-analyze - SHIPPED 2026-05-21 (1 commit, pushed; non-engine; non-frozen; RC restarted clean pid=5972 last_reload_ok=True).** Closes the ADR-007 s169 (2026-05-11) deferred phase. NEW `scripts/postmortem_analyze.py` mines `data/rewind_history.db` CHAMPION_KILL events for the tracked PUUIDs (current `rO-...#Trist` + stale `v8H...#Vayne`), classifies each death into 6 patterns (caught_4plus / solo_1v1_loss / early_pre_3min / solo_pickoff / late_throw / rapid_repeat), aggregates counts + Laplace-smoothed rates via `core/smoothed_rates.py`, writes top-3 to `data/coaching/death_patterns.json` (gitignored - personal data). Live: **28,236 deaths / 2,838 matches** -> top-3 = solo_pickoff (16595, 59%) / caught_4plus (12345, 44%) / rapid_repeat (6165, 22%). NEW `core/death_patterns_loader.py` fail-soft reader + `personal_context_block()` formatter; appended to ALL 6 active coach system prompts (aram `_SYSTEM` + arena `_SYSTEM_PROMPT` + brawl NB/URF/OFA + SR `SR_SYSTEM_PROMPT`) as PERSONAL CONTEXT below the OUTPUT FORMAT Choices line, cache-prefix-preserving (append after the closing triple-quote so the cached prefix stays byte-identical between RC sessions). `ARAM_SYSTEM_PROMPT` inside `_sr_prompt.py` deliberately left untouched (legacy path; test-pinned via `test_choices_not_added_to_aram_prompt_in_same_file`). +30 tests (24 analyzer + 6 loader); full RC suite 2662 -> 2692 passed; 87 existing coach-choices-emit tests still green; py_compile + ruff + ASCII clean across 8 touched files. `data/coaching/death_patterns.json` gitignored. The JSON is module-load-time cached - regenerate via `py scripts/postmortem_analyze.py` then RC restart to pick up changes in coach prompts. **Don't-redo:** the 6 patterns are operator-tuned at this fixture sample (changing `caught_4plus` from `assists >= 2` to `assists >= 3` is intentional - `>= 1` was 92% of all deaths so non-informative; the test `test_caught_and_solo_1v1_are_mutually_exclusive` pins the threshold). The personal_context block is APPENDED after the closing triple-quote of each prompt (cache-prefix preservation per item 121 / `reference_coach_choices_native_emit`), NEVER inserted mid-block; do NOT refactor to f-string substitution. `ARAM_SYSTEM_PROMPT` in `_sr_prompt.py` is legacy and test-pinned not-modified (`test_choices_not_added_to_aram_prompt_in_same_file` guards the slice boundary); the active ARAM coach is `coaches/aram_coach.py`. Cadence: run the analyzer manually or schedule a weekly cron (the catchup task at Sundays 04:00 keeps the DB fresh; postmortem can ride the same schedule).

- ✅ **BACKLOG drain 2026-05-21: aram_tenacity_mult EHP wire + Dead Man's Plate stacks-schema lift - SHIPPED (`e9bf6b3` + `ce563c4`, pushed; ENGINE 1.24.0 -> 1.26.0).** Two BACKLOG carries closed. (1) ENGINE 1.25.0 - EhpResult surfaces `aram_tenacity_mult` (17 ARAM champs non-1.0); NEW `effective_cc_duration(base_cc_s, tenacity_mult)` helper in `agents/daemon_slayer/ehp.py` is the seam for any future fight-sim / coach-prompt / EHP-vs-CC consumer; blended_ehp math UNCHANGED (CC duration vs HP pool is a separate axis). (2) ENGINE 1.26.0 - NEW `PeriodicProc.stack_ramp_seconds` field is the family extension for the stack-accumulation -> discharge pattern; Dead Man's 3742 + Arena 223742 re-encoded with Shipwrecker = lambda c: 40 + c.base_ad PHYSICAL every_n_attacks=1 stack_ramp_seconds=3.57 (replaces the iter-16 defensive_only deferral). Sustained model gates at max(ramp, attack_period); burst path skips ramp-gated procs. Math verified exact: Aatrox L11 vs 100-armor delta = (40 + 103.875)/3.57 * 0.5 = 20.15 DPS. Also closes 4 stale BACKLOG entries: aramAbilityHaste SHIPPED note + augment formula evaluator SHIPPED note + item-AH "queued" (already shipped `f273acb`) + ward-heat frontend "queued" (already shipped `f4938af`). +37 DS tests / +1 RC test; ruff + py_compile + ASCII-clean. DS :8893 restarted twice via taskkill + schtasks /Run RC-DaemonSlayer; live /health serves 1.26.0.

- ✅ **s220 PGR S5 follow-ups: participant-name join + lm-build-pending dead-id cleanup - SHIPPED 2026-05-20 (1 commit, pushed; non-engine; non-frozen-files-only; dashboard restarted once for the route module edit; ADR-008 asset-hash auto-bumps cover JS/CSS).** Closes the two carries-forward from item 117 / s220 S5 (operator-authorized via NEXT_SESSION_QUEUE in WAKEUP_NOTES). **(1) Participant-name join.** `dashboard/routes_replay_events.py` gains a NEW `_participant_meta(conn, match_id) -> dict[int, (summoner_name, champion_name)]` helper that joins the `participants` table; each event dict gains 6 fields - `actor_name` / `actor_champion` / `victim_name` / `victim_champion` / `assists_names: list[str|None]` / `assists_champs: list[str|None]`. Empty strings normalize to None for null-parity with actor/victim (audit refinement (a)). Module docstring updated with the new shape. Cache key gains a `_CACHE_SCHEMA = "v2-participant-join"` sentinel so stale pre-join entries evict on first hit instead of waiting out the 5-min TTL (audit refinement (e)). **Frontend `web/js/panels/replay_events.js`** refactored: NEW `_portraitUrl(champ)` mirroring `cd_ledger.js:74-79` alphanum-strip (load-bearing for Kai'Sa/Cho'Gath punctuation) + `CHAMPS` import for version; `_actorText`/`_victimText` -> `_actorChip`/`_victimChip` returning HTML with portrait `<img>` + name text via NEW `_chipHtml(pid, champ, name, prefix)` helper. Three-tier fallback chain: champion portrait + champ-text -> summoner_name only -> P<id> (kept as ultimate fallback for event-mode esoterica where actor_name is null). Portrait `<img onerror="this.style.display='none'">` collapses cleanly on a 404. `_actorText`/`_victimText` (bare-string returners) are gone. CSS row grid widened 60/80 -> 110/130 to fit champion names; NEW `.replay-events-portrait` (18px square + flex layout) + `.replay-events-chip-text` (ellipsis overflow); `.replay-events-victim { text-align: right }` -> `justify-content: flex-end` to honor the new flex container. **(2) `lm-build-pending` dead-id cleanup.** Single-line edit at `web/js/panels/last_match.js:1132`: forEach loop in `_setEmptyState` no longer references the dead id (was a no-op `getElementById` return null fall-through; only `lm-tc-pending` + `lm-chart-pending` + `lm-tl-pending` exist in index.html). The two guarded `if (pending) ...` references in `_setEnrichedBuild` are LEFT ALONE - safe dead code per the operator's "Single-line cleanup" scope. **Parallel audit (S5 pattern continued):** general-purpose subagent dispatched in BACKGROUND with the design intent + just-edited code while the implementation was in-flight. 5 findings returned during the test cycle, 3 actionable refinements applied in the SAME commit: (a) null-parity for assists_names/champs; (b) DDragon portrait icon via cd_ledger pattern; (e) cache schema sentinel for stale-eviction. Findings (c) GPL cleanroom + (d) payload-size were verified safe - no action needed. **+12 tests across 3 files** (5 backend + 6 DOM + 1 dead-id guard): `tests/test_routes_replay_events.py` - test_champion_kill_carries_participant_names_and_champs + test_assist_names_use_null_parity_for_unknown_pid + test_non_actor_event_has_null_names + test_cache_key_carries_schema_sentinel; `tests/test_replay_events_panel_dom.py` - ParticipantJoinConsumptionTests (5: CHAMPS import + portraitUrl alphanum strip + new-field consumption + chip helpers + P-id fallback) + CssGridWidenedForNamesTests (1); `tests/test_last_match_tabs_reframe_dom.py` - test_set_empty_state_does_not_reference_dead_id. **Verified:** py_compile + ruff + ASCII clean across all 7 touched files; full RC suite **2401 passed / 67 subtests / 0 failed** (+12 over post-S5 2389 baseline); RC :8888 restarted via `restart_trigger.txt` to reload the route module pid=13016 alive=True last_reload_ok=True; live curl `GET /api/replay/events?match_id=NA1_4683461559` returned events with both names populated (e.g. CHAMPION_KILL t=140 actor=Sona/Forniami -> Lucian/Eisstrahl assists_names=['shaodw']) confirming the join works on older matches with summoner_name data; live curl `?match_id=NA1_5560540832` (operator's most-recent) returned 211 events with `actor_champion=Jax` + `victim_champion=Teemo` chains - summoner_name is blank for new matches in the DB (rewind_history.db schema), the null fallback handles cleanly so the chip renders the champion-only path. ADR-008 asset-hash auto-bumps cover JS/CSS. No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** the participant join is FORWARD-COMPATIBLE - summoner_name is sometimes blank in the rewind_history.db schema (newer matches), the chip's three-tier fallback renders champion only in that case + falls back to P<id> if both are absent; do NOT pretend the field is always populated. The portrait `_portraitUrl` alphanum strip is LOAD-BEARING for punctuation in display names (Kai'Sa, Cho'Gath) - do NOT remove. The `_CACHE_SCHEMA = "v2-participant-join"` sentinel is the durable mechanism for breaking-shape cache evictions - bump the sentinel string in the SAME commit as any future event-dict shape change (NOT the TTL). The 3-tier chip fallback (champ-portrait+text -> name only -> P<id>) is intentional - do NOT collapse to just champ-portrait (event-mode esoterica may have null champion). The CSS grid widening 60/80 -> 110/130 is pinned by `CssGridWidenedForNamesTests::test_css_actor_victim_columns_widened` - do NOT shrink back. `_setEnrichedBuild`'s two `if (pending) ...` lm-build-pending guards are LEFT ALONE per operator's single-line scope; they no-op safely (`getElementById` returns null); do NOT fold them into a wider sweep without explicit operator approval. **Carries forward:** S5 OBS video overlay remains operator-deferred (its own ADR + dependency decision per ADR-009 "Watch for"). Future operator-gated polish: (a) portrait-only chips (no text) for ultra-compact ribbon - currently champ portrait + name text; (b) hover-tooltip click-to-deep-link to participant detail view in a future PGR drilldown. All item 117/116/115 carries-forward unchanged.

- ✅ **s220 PGR S5 Replay scaffold + Match-V5 timeline events ribbon - SHIPPED 2026-05-20 (1 commit `7870124`, pushed; non-engine; non-frozen-files-only; dashboard restarted once to register new route module; ADR-008 asset-hash auto-bumps cover the JS/CSS).** Stage S5 of the s220 aggregator-G-style Post Game Review reframe - the FINAL S2-S5 stage. The existing Replay view (per-frame scrubber over `rewind_history.db.timeline_frames`) gains a sidecar Match-V5 timeline **event ribbon** under the scrubber: chronological list of discrete state transitions (CHAMPION_KILL / BUILDING_KILL / ELITE_MONSTER_KILL / TURRET_PLATE_DESTROYED by default; ITEM_PURCHASED / SKILL_LEVEL_UP / LEVEL_UP / WARD_PLACED / WARD_KILL when operator toggles include filters). Default ribbon for a 35-min SR match = ~80-160 events; full opt-in ~800-1200. **Backend NEW `dashboard/routes_replay_events.py`:** GET `/api/replay/events?match_id=<id>[&include=items,skills,wards,all]` reads `timeline_events` table, normalizes to `{clock_s, type, team, actor, victim, assists, subtype, lane, pos}`, server-side filter on event_type keeps payloads 5-15 KB, 5min TTL cache keyed on `(match_id, include_set)`, fail-soft 400/404/503 mirror `routes_post_game_wpa`. **Frontend NEW `web/js/panels/replay_events.js` + CSS:** vertical scrolling list with team-tinted left borders (ally=ok/green, enemy=bad/red) + kind-tinted labels (kill=red, structure=warn, objective=gold, ward=ok, item=accent, skill=dim). Filter checkboxes persist via `localStorage[rc-replay-events-include]`; re-fetch on toggle (cache hit on the new tuple). **PGR "Review ->" button routing fix:** pre-S5 wire routed to `#review` which is NOT in `VIEW_IDS` (it has `"replay"`); the unknown id silently fell back to "home" via `applyView`'s gate at `main.js:575`. S5 routes to `#replay` + renames sessionStorage key `rc-review-focus-match` -> `rc-replay-focus-match` for consistency. `_replayViewRefresh` consumes + clears the focus id on load so a later manual click isn't shadowed. **Pre-existing CSS bug fix exposed by S5:** `header.css:613` had `body[data-view="replay"] main { display: none }` intended to hide the top-level dashboard `<main>` when Replay view is active. The selector unintentionally also hid the nested `<main class="replay-main-pane">` inside view-replay - which meant the entire Replay scrubber + grid + new events ribbon were invisible. Pre-S5 nobody clicked into Replay long enough to notice the empty right pane. Surgical un-hide: added `body[data-view="replay"] main.replay-main-pane { display: flex }` (the class-targeted rule wins specificity); the nested scaffold renders while the top-level main stays hidden. **NEW `docs/adr/ADR-009-replay-events-cleanroom.md`** captures the design intent + the **league_record (GPLv3) cleanroom boundary**: methodology reference for pairing this event sidecar with a future OBS video overlay; do NOT vendor league_record source (GPL incompatible). The sidecar shape is also Match-V5 timeline's natural shape, so no implementation borrowed. `.rofl` parsing was confirmed dead-end 2026-05-17 (only aggregate `PlayerStatistics`, no packet stream); do not re-pitch. **Parallel-audit pattern (operator's "edits and audits in parallel" instruction):** architecture-review subagent dispatched in BACKGROUND with the design intent while the route was being written. Audit returned 4 refinements that landed in this commit: (1) keep `#replay` not `#review` (renamed sessionStorage key for consistency); (2) server-side event-type filter; (3) league_record reference belongs in `docs/adr/` not just a module comment; (4) no pre-split of `routes_coach.py` - new sibling module is the right call. **+39 tests across 2 NEW files** (17 backend in `test_routes_replay_events.py` + 22 frontend DOM in `test_replay_events_panel_dom.py`): StrongEventsTests / IncludeFiltersTests / FailureContractTests / CacheTests / CleanroomDocPresenceTests backend; EventRibbonMountTests / PgrReviewButtonRoutingTests / ReplayViewAutoSelectTests / ReplayEventsPanelJsTests / CleanroomBoundaryTests / AsciiHygieneTests frontend. Tests pin default strong-event set + clock order + actor/victim/assists/pos + building destroyer-not-owner flip + include-set widening + fail-soft contract + cache key + ADR existence + module-docstring ADR reference + #replay routing + sessionStorage key rename + legacy-id absence + cleanroom GPL/no-vendor boundary + ASCII hygiene. **Verified:** py_compile + ruff + ASCII clean across all 12 touched files; full RC suite **2389 passed / 67 subtests / 0 failed** (+39 over post-S4 2350 baseline); live curl `/api/replay/events?match_id=NA1_5439050124` returned ok=true count=100 elapsed_ms=1 cached=false on cold + cached=true on second hit; live Playwright @ `https://127.0.0.1:8888/#replay` on operator's most recent match NA1_5560540832 (Kai'Sa, May 14) showed the full Replay scaffold + event ribbon: 211 events strong-only, team-tinted left borders, kind-tinted labels (Kill red, Plate warn, Grubs HORDE gold, Dragon HEXTECH_DRAGON gold, etc). No DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** Match-V5 timeline events from `rewind_history.db.timeline_events` is the CANONICAL event source - do NOT re-pitch `.rofl` parsing (confirmed dead-end 2026-05-17). `league_record` (GPLv3) is methodology-only - do NOT vendor its source (`CleanroomBoundaryTests` guards + ADR-009 documents). BUILDING_KILL.team_id is FLIPPED to destroyer-side server-side - the events table stores the OWNER that lost the building; the ribbon paints by destroyer (`StrongEventsTests::test_building_kill_team_is_destroyer_not_owner` pins). PGR Review button now routes to `#replay` (canonical VIEW_IDS member) NOT `#review` (never built); `PgrReviewButtonRoutingTests::test_button_routes_to_hash_replay` + `test_session_storage_key_uses_replay_namespace` guard. The header.css un-hide `body[data-view="replay"] main.replay-main-pane { display: flex }` is LOAD-BEARING - the broad `display: none` rule above WILL re-hide the scaffold without it; do NOT remove or refactor the un-hide without the broader selector cleanup. Server-side event-type filter (default strong-only) is deliberate - WARD_PLACED (284k) + ITEM_PURCHASED (528k) would drown the kill/objective signal; `?include=...` opts in selectively. Audit-in-parallel pattern (subagent in background with design intent + just-edited code) is durable - matches operator's instruction directly. **Carries forward:** S2-S5 of the s220 aggregator-g reframe are now ALL SHIPPED. Future operator-gated slices: (a) actual OBS video overlay keyed to LCU game-lifecycle (its own ADR + dependency decision per ADR-009 "Watch for"); (b) richer participant join in events (currently `P1..P10` chips; a join against `participants.summoner_name` + `champion_name` would surface real names but bloat the payload - operator-gated). (c) The `lm-build-pending` dead-id reference in `_setEmptyState` (flagged by S4 audit, S4 unchanged, still out of scope - separate cleanup). Item 116 + 115 carries unchanged. Frozen-file grant NOT used this session.

- ✅ **s220 PGR S4 tabs reframe (4 -> 3: Build / Graph / AI Analysis) - SHIPPED 2026-05-20 (1 commit `d199f30`, pushed; non-engine; non-frozen-files-only; no RC restart - JS/CSS asset-hash auto-bump via ADR-008; index.html picks up on next page nav).** Stage S4 of the s220 aggregator-G-style Post Game Review reframe. Tab strip flipped 4 -> 3 per the aggregator G pattern: **Build** = was Comp (rosters with full per-player items + summoners + augments + MVP/SVP card); **Graph** = was Chart (team-aggregate bars) merged with the visuals half of Timeline (per-minute gold/XP/CS differential sparklines + objective ribbon); **AI Analysis** = was Insights (3-column Quick Review) merged with the heuristic-insights half of Timeline (WPA "Phases that mattered"). **Review** nav button stays - it routes to a deep-review page, not a sibling panel. Legacy localStorage migration via NEW `_migrateLegacyTab(saved)` helper called from `wireLastMatchOnce`: `comp -> build` / `chart -> graph` / `timeline -> graph` (visuals home) / `insights -> ai-analysis`; migrated value written back to localStorage in a one-shot so subsequent loads see the new id verbatim. NEW `_VALID_TABS = ["build", "graph", "ai-analysis"]` allowlist gates `_activateTab` against malformed values. **Same-commit audit fixes** (UI-audit subagent dispatched in parallel with the implementation per operator's "edits and audits in parallel" instruction; audit returned 1 minor + 2 doc nits + pre-existing CSS gap I caught visually): (1) Graph panel was rendering a stray "Timeline unavailable" pending banner under the Chart bars whenever LCU enrichment landed but Match-V5 timeline didn't (event modes, pre-key imports) - pre-S4 those lived in separate tabs, merged into Graph it read as a partial-failure banner; replaced both pending blocks with a single shared placeholder ("Graph pending - team-aggregate bars populate from LCU enrichment; per-minute sparklines + objective ribbon load when the Riot Match-V5 timeline lands."). (2) Pre-existing CSS gap: `.lm-wpa-wrap` had `display: flex` without a matching `[hidden]` selector, so `renderPhases({ ok: false })` set `wrap.hidden = true` but the "Phases that mattered" head still rendered - pre-S4 this was masked because the WPA lived inside the (hidden) Timeline tab; S4 exposed it at the top of AI Analysis. Added `.lm-wpa-wrap[hidden] { display: none; }` to mirror the existing `.lm-tl-wrap[hidden]` pattern. (3) Stale doc comments in `last_match.js:wireLastMatchOnce` (referenced pre-S4 "Comp / Chart / Review") + `post_game_phases.js` module header (referenced "Timeline tab"). NEW `tests/test_last_match_tabs_reframe_dom.py` +15 grep-based DOM-contract tests across 5 classes: TabStripTests (4) pin the 3 canonical tab ids + Build-default-active + legacy-id absence + Review-nav-button kept; PanelMountTests (4) pin Build hosts roster + Graph merges chart+timeline visuals + AI Analysis hosts WPA + WPA-above-Quick-Review ordering; LegacyMigrationTests (5) pin `_VALID_TABS` constant + `_migrateLegacyTab` function + each legacy-id case + writeback + legacy-array absence; CssNoLegacySelectorsTests (1) guard against legacy `data-tab-panel="comp|chart|timeline|insights"` selectors; AsciiHygieneTests (1) scan JS for em/en-dashes/smart quotes. **Verified:** py_compile + ruff + ASCII clean across all 5 touched files (`web/index.html` + `web/js/panels/last_match.js` + `web/js/panels/post_game_phases.js` + `web/css/panels/last_match.css` + `tests/test_last_match_tabs_reframe_dom.py`); full RC suite **2350 passed / 67 subtests / 0 failed** (+15 over post-S3 2335 baseline); live Playwright verify @ `https://127.0.0.1:8888/#last-match` on operator's ARAM Vel'Koz 4/9/25 across all 3 tabs - **Build** = MVP cards + rosters render correctly with hero **48 BAD** + SVP **bliv 64 OK** + MVP **GenieInTheLamp 77 GOOD** persisting; **Graph** = team-aggregate bars (Kills 33 vs 54, Deaths 54 vs 33, etc.) with NO stray Timeline-unavailable banner; **AI Analysis** = Quick Review 3-col (What went right / What went wrong / What you fail most at) with WPA hidden cleanly when no Match-V5 timeline for this event mode. ADR-008 asset-hash auto-bumps cover JS/CSS; index.html picks up on next page nav. No RC restart, no DS restart. No ENGINE bump. No frozen-file edits. **Don't-redo:** the 3-tab reframe is final - Build/Graph/AI Analysis are the canonical ids; do NOT re-add Comp/Chart/Timeline/Insights (`TabStripTests::test_legacy_tab_ids_removed` guards). `_migrateLegacyTab` writeback is INTENTIONAL one-shot (operator only pays the migration cost once per saved value); do NOT remove the writeback. `_VALID_TABS` allowlist gates `_activateTab` against malformed values; do NOT relax. WPA was ABOVE Quick Review in AI Analysis (heuristic-insights ordering); `PanelMountTests::test_ai_analysis_hosts_wpa_then_quick_review` pins ordering. The `.lm-wpa-wrap[hidden]` rule is load-bearing (without it the head bleeds through `display: flex`); do NOT remove. Single shared Graph placeholder replaces 2 pre-S4 placeholders; the lm-tl-pending id lookup in `_setTimeline` no-ops cleanly when the element is absent. **Carries forward:** S5 (Replay page scaffold + Match-V5 timeline + league_record GPLv3 cleanroom reference) still operator-gated; the s220 reframe chain continues stage-by-stage with the per-page UI-audit ritual between sessions. Pre-existing `lm-build-pending` dead-id reference in `_setEmptyState` is flagged (audit) but NOT touched here (out of scope for S4 - separate cleanup). Frozen-file grant NOT used this session.

- ✅ **s220 PGR S3 aggregator G score+MVP polish - SHIPPED 2026-05-20 (1 commit `92c6a0f`, pushed; non-engine; non-frozen-files-only; no RC restart - JS/CSS asset-hash auto-bump via ADR-008; index.html picks up on next page nav).** Stage S3 of the s220 aggregator-G-style Post Game Review reframe (s2 structure largely landed prior; carries-forward in WAKEUP). Three aggregator-G-pattern affordances added, all sourcing from the existing `_rosterScores` 100-point heuristic in `web/js/panels/last_match.js` so the three numbers stay mutually consistent. **(1) Hero match-score block** - NEW `#lm-hero-score` mounted as the 6th column of `.lm-hero-identity` carrying a big numeric 0-100 + tier label (Bad/OK/Good/Excellent). NEW `_setHeroScore(m, enriched)` + `_scoreTier(score)` helpers; tier-tinted via `data-tier` attribute (red/purple/green/gold). Hidden until LCU enrichment lands. **(2) Per-side MVP/SVP cards** - NEW `#lm-tc-ally-mvp` + `#lm-tc-enemy-mvp` above each `.lm-tc-list` (between `.lm-tc-group-head` and the list). Portrait + inline crown SVG (NOT emoji - ASCII hard rule) + name + numeric score. NEW `_renderMvpCard(cardId, top, sm)` helper. `data-kind="mvp"` = winning-side top (gold-tinted purple bg, gold left-border); `data-kind="svp"` = losing-side top (accent-tinted). `rankSide` closure inside `_setTeamComp` extended to return `{meta, top}` so the card can pull the top-scoring row by participant_id. **(3) Per-row score chip** - the existing `.lm-tc-score` cell becomes a flex column stacking the MVP/SVP/#rank badge text over the integer 0-100 numeric score (was tooltip-only). Row grid columns 46 -> 56 (SR + augment-mode templates both bumped so ally/enemy align). Decimal + factors stay in the hover tooltip. NEW `tests/test_last_match_score_card_dom.py` +26 grep-based DOM-contract tests mirroring `test_draft_elo_panel_dom.py` pattern: HeroScoreBlock / MvpCard / PerRowScoreChip / EmptyStateReset / AsciiHygiene; the `MvpCardTests::test_js_mvp_card_uses_roster_scores` guard catches future divergent-scorer drift, `AsciiHygieneTests` catches Unicode glyph regressions (BAD dict built via chr() so the test file stays ASCII-clean against its own scan), grid-template-columns pinned in tests so the score-column width doesn't shrink back. **Verified:** py_compile + ruff + ASCII clean across all 4 touched files (`web/index.html` + `web/js/panels/last_match.js` + `web/css/panels/last_match.css` + `tests/test_last_match_score_card_dom.py`); full RC suite **2335 passed / 67 subtests / 0 failed** (+26 over s113 2309); live-verified via Playwright @ `https://127.0.0.1:8888/#last-match` on operator's ARAM Vel'Koz 4/9/25 last match - hero **48 BAD** (red), ally SVP **bliv 64 OK** (accent), enemy MVP **GenieInTheLamp 77 GOOD** (green), all 10 per-row chips populated with correct badge+numeric stack. No DS restart, no RC restart needed. No ENGINE bump (non-engine, frontend-only). No frozen-file edits. **Don't-redo:** the three S3 affordances (hero score / MVP card / per-row chip) all source from the SAME `_rosterScores` heuristic - do NOT introduce a divergent scorer in any of the three sites; the `test_js_mvp_card_uses_roster_scores` guard catches that drift. Crown is INLINE SVG (path `M2 14 L4 4 L8 9 L12 2 L16 9 L20 4 L22 14 Z`) NOT an emoji - the ASCII hard rule forbids Unicode glyphs in authored content; do NOT swap to U+1F451 or unicode stars. Tier thresholds (80/65/50) are calibrated against the 100-point `_rosterScores` scale + frozen in `test_js_score_tier_buckets` - if `_rosterScores` weights ever change, retune these thresholds + the test together so the visual tiers stay calibrated. Row grid 46 -> 56 also covers the 3-digit edge case (score=100); do NOT shrink back. `rankSide` now returns `{meta, top}` (was just `meta`) - downstream code that pattern-matches must use `.meta` / `.top`; the closure stays local to `_setTeamComp`. **Carries forward:** S4 (tabs reframe Comp/Chart/Timeline/Insights -> AI Analysis/Graph/Build) + S5 (Replay page scaffold + Match-V5 timeline + league_record GPLv3 cleanroom reference) still operator-gated; the s220 reframe chain continues stage-by-stage with the per-page UI-audit ritual between sessions. Item 114 + 113 carries unchanged. Frozen-file grant NOT used this session.

- ✅ **Fleet OAuth Max migration finished + Legion ON/OFF desktop shortcuts - SHIPPED 2026-05-20 (1 commit `cc9401b`, pushed; non-engine; no frozen-file edits; no RC restart needed).** Closes the 2026-05-20 OAuth fleet migration that item 100 had landed ASPIRATIONALLY but only Legion actually flipped. Triggered by operator's billing console showing $ on api.anthropic.com fluctuation from user_id fingerprint `182f0ecf0bc77e27788c33ddce44ac20c00097c662611fa5bf83e8b1f8bc43ac` (matched Peer machine). Two parallel bridge tasks dispatched (Peer + Game-PC) for full 6-step OAuth migrations. **Peer result:** was process-scope sk-ant-api03 (no User/Machine reg leak; the injection vector was bridge daemon `subprocess.run` inheriting `os.environ` wholesale + operator-planted HKLM `ANTHsssROPIC_API_KEY` canary), no OAuth credentials, no oauthAccount; operator ran `claude /login` mid-session enrolling under accountUuid 4eb3a75e (matches Legion); Peer daemon patched env.pop, sha256 63c5bbce. **Game-PC result:** TWO leak vectors that s100 missed - HKLM Machine `ANTHROPIC_API_KEY` (len 108) + `C:/RC-Agent/.claude/settings.local.json` env block; OAuth was already enrolled (sk-ant-oat01-Rpf) but CLI preferred raw API on conflict per the warning text. Both vectors removed, daemon patched (sha256 9ff5d8fd), old pid 4792 -> 16416. **Legion repo patches:** `tools/legion_bridge_daemon.py` + `tools/gamepc_bridge_daemon.py` ALSO missing env.pop (item 100 only patched `bridge_watcher_actions.py`; the daemons are a different codepath); patched both with `_env = os.environ.copy(); _env.pop("ANTHROPIC_API_KEY"); env=_env` + comment. Patching `tools/gamepc_bridge_daemon.py` ALSO keeps RC ON re-downloads from clobbering Game-PC's local patch on next boot (Game-PC's daemon is pulled from Legion's `/agent/` endpoint). **Legion ON/OFF shortcuts:** NEW `tools/legion_on.ps1` (starts all 6 RC-* AtLogon/AtStartup tasks + DS port-listen guard at :8893 + Claude Desktop singleton launch) + `tools/legion_off.ps1` (Stop-ScheduledTask + taskkill /F /PID per CLAUDE.md hard rule "never Stop-Process"). Both idempotent (IgnoreNew refusal on scheduled tasks, port-check on DS, singleton on Claude Desktop). Desktop shortcuts created via WScript.Shell COM: `Legion ON.lnk` + `Legion OFF.lnk`, both UAC-clean (operator runs as Administrator). Old `Claude RC.lnk` backed up at `Claude RC.lnk.bak`. **NEW memory `feedback_oauth_flip_injection_vectors.md`** documents all 3 non-obvious injection vectors (bridge daemon subprocess + HKLM Machine + .claude/settings.local.json) + verification ritual + auth-conflict shape so the next OAuth flip on a new machine knows where to look. **Don't-redo:** OAuth fleet is now ACTUALLY 100% (was 33% per item 100); the 4 env.pop files fleet-wide are bridge_watcher.py + bridge_watcher_actions.py (item 100, Legion repo) + legion_bridge_daemon.py + gamepc_bridge_daemon.py (this session). Peer-side `C:\Peer-Bridge\peer_bridge_daemon.py` (sha256 63c5bbce) + Game-PC `C:/RC-Agent/gamepc_bridge_daemon.py` (sha256 9ff5d8fd) live OUTSIDE this repo - the Legion-side copies are the source-of-truth for next `RC ON` propagation. Legion's RC-BridgeDaemon (running pid 472) holds pre-patch code in memory; next Legion OFF+ON cycle picks up the patch. Peer recommendation for self-PID-lock on legion_bridge_daemon deferred (Legion daemon already has `_lock_held`/`_set_lock` lines 188-194 - functionally equivalent). **Carries forward:** (a) Operator pending on Peer: restart `\Peer-BridgeDaemon` schtask + optional kill of :8899 web_dashboard orphans pids 19824/21972 (vestigial from 2026-05-05, zero Anthropic calls). (b) Next session **s220 aggregator-G-style Post Game Review reframe** (UI; operator-decided single-match richer layout + RC-heuristic 0-100 score; staged S2-S5). (c) Verify $ drop on `mcp__anthropic-usage__query_usage` 2026-05-21+ once Max absorbs full day across all 3 nodes. (d) All item 113 carries unchanged.

- ✅ **3-agent parallel BACKLOG NOW lane drain wave 2 - SHIPPED 2026-05-20 (6 commits + 3 merges `e0eabb7`..`89e645e`, pushed; ENGINE 1.22.0 -> 1.23.0; no frozen edits; DS :8893 restarted serves 1.23.0; RC :8888 restarted for new draft-elo route module pid=7692 last_reload_ok=True).** Operator "start what is left in backlog and roadmap in parallel" -> 3 worktree agents concurrent on disjoint NOW-lane items, all merged + tested green + pushed. **Agent A `4e5d818` (aramAbilityHaste + aramTenacity engine CONSUMPTION)** - closes the consumption side of item 109 iter 17 exposure. NEW helpers in `agents/daemon_slayer/ability_dps.py:800-861` `_effective_ability_cd(base_cd, total_haste) = base_cd / (1 + total_haste/100)` (Riot canonical mirroring s110 summoner CD ledger) + `_total_ability_haste()` folds `aram_ability_haste` from scaled stats. `AbilitySpellDps` gains `base_cooldown` + `total_ability_haste`; `AbilityDpsResult` gains `aram_ability_haste` + `aram_tenacity_mult` (forward-marker for future EHP enemy-CC model). `cooldown` field now POST-haste effective (SR + zero-aramAH ARAM identity-preserved -> 42 existing test files unaffected). +26 tests + 13 stale ENGINE pin syncs; ENGINE 1.22.0 -> 1.23.0. **Agent B `e0eabb7` (HURTS-THEM/HELPS-US frontend toggle)** - closes item 109 carry-forward (a). NEW `web/js/panels/ban_suggest_toggle.js` + CSS + 25 snapshot tests mirroring `test_augment_reco_panel_dom.py`. Wires to `dashboard/routes_ban_suggest.py` (item 109 backend). Mode persists `localStorage[rc-cs-bansugg-mode]` default `"hurts_them"`; `data-bs-active` attr-driven CSS flip (red HURTS / green HELPS); per-row sample-density `n=solo/pair` chip with band tier. **Agent C `ea9b88f` (per-row contribution hover strip)** - closes item 109 carry-forward (b). NEW `Contribution` dataclass + `top_contributions()` sibling aggregator in `core/draft_elo.py:141-238` (NOT inside `team_score`). Route gained `?breakdown=1` query param adding `top_contributions: [{kind, a, b, delta, n}, ...]` field (max 3); cache stores full payload + strips at serve. Frontend uses PURE CSS `:hover` (zero JS handlers, zero click-to-pin); `HoverOnlyContractTests` guard prevents regressions. +31 tests. Live curl `GET /api/draft-elo?...&breakdown=1` returned top-3 contributions with sample densities. **Merges:** disjoint files, no conflicts. DS **2803 passed** (+26 over 2777); wider RC **2309 passed** (+56 over 2253); py_compile + ruff + ASCII clean (touched files clean; pre-existing carryover in unchanged code untouched). DS :8893 restarted (pid 4800 -> new); failing `test_live_three_profiles` flipped pass post-restart. RC :8888 restarted (new dashboard route module triggered restart_trigger.txt). Worktrees + branches cleaned up. **Don't-redo:** all 3 NOW items SHIPPED + live-verified; `cooldown` field semantics now POST-haste effective cd - back-compat preserved through identity at total_haste=0; `aram_tenacity_mult` is FORWARD-MARKER (no consumer built, future EHP enemy-CC owns); `base_ah` parameter is a SEAM for future item-AH lane (currently 0 - awaits `_effects_types.ItemEffect.ability_haste_flat` field); ban-suggest toggle persists locally per-machine via localStorage; per-row hover is PURE CSS - do NOT relax `HoverOnlyContractTests` without operator approval; `top_contributions` cache strip-at-serve pattern is deliberate. **Carries forward:** (a) DS item-AH lane queued (touches `_effects_types.py` + `_effects_data.py` + extractor pass). (b) `aram_tenacity_mult` consumer (future EHP enemy-CC model). (c) `STAT_GRANT_CALC_KEYS` seam unchanged (Arena S2 26.09 trigger). (d) post-game-phases live-game smoke pending. (e) Arena S2 patch 26.09 schema break ahead. (f) item 110 Peer bridge_watcher zombie + Game-PC vision triad SKIPPED + DDragon 5/21 03:30 cron unchanged. (g) ruff/type-hint pass STILL operator-gated.

- ✅ **3-agent parallel BACKLOG NOW lane drain - SHIPPED 2026-05-20 (4 commits `e0eec7e`..`21657eb` merge `21657eb`, pushed; ENGINE 1.21.0 -> 1.22.0; no frozen edits; DS :8893 restarted serves 1.22.0).** Operator "start what is next on backlog in parallel" -> 3 worktree agents concurrent on the BACKLOG NOW lane: (A) cdragon `mFormulaParts` typed-part formula evaluator `c965167` + `583a3bc` - NEW `agents/daemon_slayer/augment_formula_eval.py` interprets 4 typed parts (`NamedDataValueCalculationPart` / `StatByNamedDataValueCalculationPart` / `StatByCoefficientCalculationPart` / `NumberCalculationPart`) + `mMultiplier` + sum composition; wired into `compute_augment_stats` with `_AUGMENT_STAT_OVERLAYS` fallback; `STAT_GRANT_CALC_KEYS` displacement seam INTENTIONALLY empty at cdragon 16.10.1 (all 82 unique calc keys are damage/heal/shield/conversion, no stat grants - production identical today); +47 tests; ENGINE 1.21.0 -> 1.22.0 + 13 test pin syncs. (B) LoLytics WPA framework PGR S2 `a250416` - NEW `core/post_game_score.py` pure-Python LR via batch gradient descent (no sklearn/numpy dep) + `data/post_game_wpa_model.json` trained checkpoint (200 SR matches, 800 samples, weights gold_diff +1.32 / xp_diff +0.65) + NEW `dashboard/routes_post_game_wpa.py` GET `/api/post-game-wpa?match_id=<id>` + NEW `web/js/panels/post_game_phases.js` stacked card list mounted in last-match view + dispatch registration; +68 tests; live `GET /api/post-game-wpa?match_id=NA1_5439050124` ok=true event_count=86 elapsed_ms=2 top-3 CHAMPION_KILL phases t=60/65/73s WPA +2.3% each. (C) Hextech-drake LCU wire `e0eec7e` - extended `dashboard/_state_cooldowns.py` with NEW `_hextech_drakes_by_team()` helper watching `gameData.events.Events` for DragonKill+DragonType="Hextech" + KillerName-to-team via allPlayers roster + per-participant `hextech_drakes` field feeds s110 `58d1e87` haste-formula (`HEXTECH_DRAKE_AH_PER_STACK=5`); +10 tests. **Merge `21657eb`:** no conflicts (3 agents touched disjoint files). DS suite 2777 passed (+47); wider RC 2253 passed (+86). DS :8893 restarted (taskkill + relaunch start_daemon_slayer.py per `reference_ds_server_not_supervisor_watched`); /health -> engine_version=1.22.0. **Don't-redo:** all 3 BACKLOG NOW items SHIPPED; cdragon evaluator's empty seam INTENTIONAL (do NOT manually populate, Arena patches populate it); pure-Python LR no numpy dep on purpose; new `summs.summoner_haste`/`ult.ability_haste`/`hextech_drakes` keys forward-compatible additions; _AUGMENT_STAT_OVERLAYS registry KEPT as today-fallback (do NOT delete). **Carries forward:** seam populates when Arena S2 patch 26.09 schema break lands; aramAH/Tenacity engine consumption still queued for DS cooldown math (not PGR-side); WPA model retraining cadence operator-gated; post-game-phases live-game smoke pending; bridge/vision/DDragon item 110 carryforward unchanged.

- ✅ **Fleet bridge daemon symmetry + autonomous cadence verified - SHIPPED 2026-05-20 (3 commits `f40e8fb`..`0e7cb8e`, pushed; non-engine; 1 frozen edit operator-approved).** User-driven scoped session. **`f40e8fb` fix(gamepc):** `tools/gamepc_boot.ps1` section 6 repointed from `AppData\Roaming\Claude\claude-code\<ver>\claude.exe` (which IS the CLI native installer, comment was wrong) to Squirrel-installed Claude Desktop at `AppData\Local\AnthropicClaude\claude.exe` (Product=Claude v1.8089.1); CLI fallback (sec 6c) removed per operator decision. Also unset `ANTHROPIC_API_KEY` at **Machine** scope on Game-PC (User was already empty) - bridge_watcher subprocess env.pop'd it per fleet item 100 so OAuth path unaffected. **`cb74e00` feat(bridge):** legion_bridge_daemon + frozen push-notif fix + honest gauge naming. NEW `tools/legion_bridge_daemon.py` (TARGET=legion, mirror of gamepc/peer pattern) + `ops/install_RC_LegionBridgeDaemon.ps1` registers `RC-BridgeDaemon` (AtLogOn Highest restart=3/1min ETL=0). **Frozen edit** (operator-approved): `tools/bridge_watcher.py:256` push-notif spawn now uses `_actions._resolve_claude_executable()` instead of bare `"claude"` - fixes fleet-wide push-notif WinError 2 (subprocess.run can't resolve .cmd npm shim on Windows). **Bridge field split** `dashboard/routes_state.py`: `bridge.age_s` -> `bridge.gamepc_result_age_s` (honest naming - gauge ONLY tracked kind=result+source=gamepc); ADDED `bridge.legion_daemon` sub-block (status, pid, invocations_since_boot, last_check_age_s); `web/js/main.js:6229,6232` consumer renamed. **`0e7cb8e` fix(bridge-daemons):** CREATE_NO_WINDOW on all 3 daemon's `subprocess.run` call - conhost window flashed on each cadence because pythonw parent + `.cmd` shim child requires cmd.exe-hosted batch; STARTUPINFO alone insufficient for `.cmd`. **Peer bridge daemon was NEVER INSTALLED on Peer** - rc_facts startup's "peer bridge daemon: watcher=alive" was just the bridge_watcher heartbeat publisher, not a real daemon process. Operator pulled `peer_bridge_daemon.py` from Legion's `/agent/` allowlist (already in `_AGENT_ALLOWED`) to `C:\Peer-Bridge\peer_bridge_daemon.py`; registered scheduled task `Peer-BridgeDaemon` (pid 24772 -> 29720 post-CREATE_NO_WINDOW pull). **End-to-end verified:** 4 autonomous test rounds. Round 4 sent hash-reporting probes to gamepc + peer, both replied autonomously: gamepc sha256=18196dfbecff1539 hostname=WIN-T1GK0AC0JNF pid=8584; peer sha256=f075f314dd6a7e0c hostname=DESKTOP-CTBMA25 pid=29720; both MATCH Legion repo source of truth. Fleet symmetry proven; RC-VerifyBridgeRoundtrip-Once LastResult flipped 0xC000013A -> 0x00000000 (PASS latency 44.35s); bridge.gamepc_result_age_s gauge cleared 79961s -> 45s status red -> green. **Don't-redo:** (1) gamepc shortcut points at Squirrel Claude Desktop - do NOT repoint to `claude-code\<ver>\claude.exe` (CLI); (2) Legion has its own bridge daemon - do NOT pitch `/loop /process-bridge-tasks`; (3) `bridge.age_s` field GONE from /api/health/all, consumers use `bridge.gamepc_result_age_s`; (4) Peer daemon at `C:\Peer-Bridge\` (no repo clone on Peer); patching Legion source requires `/agent/` re-pull on Peer (one-liner pattern durable); (5) `bridge_watcher.py:256` frozen edit shipped, do NOT revert; (6) ANTHROPIC_API_KEY unset at Machine scope on Game-PC. **Carries forward:** (a) Peer bridge_watcher zombie - process alive, internal poll loop frozen at 2026-05-06 (14 days stale); daemon covers task autofire so non-blocking, but watcher zombie worth a separate look on Peer. (b) Game-PC vision screen-agent triad still SKIPPED per gamepc_boot.ps1 sec 4 isolation test 2026-05-16 (Vanguard BSOD investigation). (c) RC-DDragonMirrorRefresh next 5/21 03:30 cron is the live test of item 110's `943d2e0` retry-with-backoff.

- ✅ **Housekeeping batch + cdragon calculations enrichment phase-1 + ddragon retry-fix + summoner CD haste-formula - SHIPPED 2026-05-20 (7 commits `1a8e0bf`..`58d1e87`, pushed; no ENGINE bump; non-engine; no frozen-file edits).** User-driven scoped session. **Branch+worktree cleanup:** 4 local + 1 remote stale branches deleted; locked worktree `agent-a7da71cb6830f84fc` (107MB) unlocked+removed. **Claude RC.lnk repointed** to launch Claude Desktop (MS Store AUMID `Claude_pzs8sxrjxfjjc!Claude` via `explorer.exe shell:AppsFolder\...`) instead of CLI launcher; smoke-launched pid 1368. `tools/claude-rc.ps1` deleted (`3571e1b`). **4 dated 2026-05-02 `docs io RC peer/` artifacts archived** to `docs/_archive/2026-05-02-rc-peer/` per convention; 2 left in place because of frozen-file refs (`main.py` for BRIDGE_MONITOR, `sync-all-md.md` for PHASE1_LESSON_SCHEMA). **`943d2e0` fix(ddragon):** retry-with-backoff on transient HTTP errors - root cause of RC-DDragonMirrorRefresh exit 2 was 1/6767 assets timing out per cron (profileicon/5668.png). `_http()` retries URLError + HTTPError 429/500/502/503/504 with 1s+2s backoffs (max 2 retries); HTTPError 4xx not retried. +4 tests; live-verified: prior run exit 2 -> re-run exit 0. **`4808f64` chore(gitignore):** `_scratch/` (~1MB session-debug logs). **`8f7a71b` feat(ds):** cdragon `calculations` formula defs lifted into `Augment.from_record()` (`+calculations: dict` field + `+calculation_keys` property + `__post_init__` None->{} default); upstream extractor already captured them, was being trimmed at the Augment boundary. 83/220 augments carry non-empty calculations at cdragon 16.10.1. Dataclass-only; NO ENGINE bump; no DS restart. +3 tests. **`94fe721` + `8d1c28e` docs(backlog):** 125 -> 105 lines, 0 strikethrough remaining; dropped 14 shipped entries + the stale `hps_split_aram_heal_shield_mults` entry (actually shipped iter 17 `816ab4a`); rewrote `aramAbilityHaste`/`aramTenacity` entry to reflect actual state (exposure shipped, CONSUMPTION still owed). **`58d1e87` feat(coach):** haste-formula cycle math for summoner cooldown ledger. Replaced additive-percent CDR in `core/summoner_cooldowns.py` with Riot's canonical `eff_cd = base / (1 + h/100)` (additive model double-counted at high stacks). New constants: `COSMIC_INSIGHT_SSH=18` / `LUCIDITY_SSH=18` / `LUCIDITY_AH=12` / `MAGICAL_FOOTWEAR_SSH=10` / `HEXTECH_DRAKE_AH_PER_STACK=5`. New helpers `_summoner_haste(runes, items)` / `_ability_haste(items, hextech_drakes=0)`; old `_cdr_summs`/`_cdr_ult`/cdr-fraction `_effective_cd` deleted. Participant dict +`hextech_drakes:int` (default 0). Output +`summs.summoner_haste`+`ult.ability_haste` for panel tooltips. 14 -> 29 tests (+4 Hextech Drake +3 haste-totals shape +1 negative-haste extreme). Flash @ Cosmic+Boots pin: 216s -> 220.59s (correct compression). **Verified:** wider RC suite 2096 passed + 67 subtests; py_compile + ruff + ASCII clean. No DS restart. No frozen-file edits. **Don't-redo:** all 7 commits real + pushed; 2 dated files deliberately left in place (frozen-file refs); cdragon calculations is HALF SHIPPED - the formula-evaluator that consumes `mFormulaParts` typed-parts to displace `_AUGMENT_STAT_OVERLAYS` is the NEXT slice and will ENGINE-bump. **Carries forward:** (a) `aramAbilityHaste`+`aramTenacity` engine CONSUMPTION still queued (requires baseline ability-haste model in DS engine first - currently NONE per `ability_dps.py:49`). (b) cdragon `calculations` formula evaluator. (c) CD ledger Live Client wire for Hextech drake count from `gameData.events.Events` (DragonKill DragonType=Hextech). (d) all item 109 carries (HURTS-THEM/HELPS-US frontend, LoLytics WPA, Arena S2 patch 26.09, ward-heat liveclient wire, shaper UI, live-game UX validation).

- ✅ **Second headless run UX waves 1+2 + research wave + DS audit iter 17-19 - SHIPPED 2026-05-20 (12 commits `f4938af`..`9f356a3`, pushed; ENGINE_VERSION 1.17.0 -> 1.21.0; CI 12/12 green after one same-run ruff-F541 fix `cb73757`).** Full ledger at CLAUDE.md item 109; living synopsis at `C:\Users\Administrator\Desktop\RC_HEADLESS_SYNOPSIS_2026-05-20.md`. **UX wave 1 closed `f4938af`:** ward-heat frontend strip + producer wire to `core/liveclient_cache` via NEW listener API (item-108 carry-forward (a)). **UX wave 2 shipped `ab3f553`+`00051f3`:** Elo log-odds team-vs-team draft aggregator + frontend chip with sample-density n= indicator (`core/draft_elo.py` pure math + `core/draft_elo_db.py` rewind queries + `dashboard/routes_draft_elo.py` + `web/js/panels/draft_elo.js`). **HURTS-THEM/HELPS-US ban-suggest `6e7b7e5`:** `dashboard/routes_ban_suggest.py` returns BOTH ratings per candidate (the "genuine market gap" research-wave finding). **Multi-agent research wave** (5 parallel slice agents); triage LOCKED in BACKLOG.md. **DS audit iter 17-19 (3 ENGINE bumps):** iter 17 `816ab4a` hps_split_aram_heal_shield_mults (item-108 carry-forward (a)); iter 17 `6bba097` aramAbilityHaste + aramTenacity exposure (item-108 carry-forward (b)); iter 18 `74d2ba6` Lich Bane Spellblade AP 50% -> 40% per Meraki; iter 19 `9f356a3` Nashor's Tooth AP 20% -> 15% per Meraki; iter 20-26 NO-CHANGE (Stridebreaker/Liandry/Luden/Hollow Radiance/Sunfire/Statikk-RFC/Profane Hydra verified clean). **/headless-upgrade skill `b007783`:** 11-section autonomous-run skill (`tools/headless-upgrade.md`). **Verified:** DS **2730 passed / 1 xfailed / 1663 subtests** (+20 over 2710); RC **2167 passed** (+50 over 2117); CI 12/12 green; DS :8893 serves 1.21.0 live; RC :8888 alive. **Don't-redo:** all ENGINE iterations are Meraki 16.10.1 corrections; `mode_multiplier` @property is backward-compat alias for heal_mult; AH/Tenacity exposure-only is intentional; research-wave CLOSED list authoritative (MobaHinted dead, Arena win-rates Riot-banned, etc); frozen-file grant was honored minimally and does NOT carry forward. **Carries forward:** HURTS-THEM/HELPS-US frontend deferred; LoLytics WPA framework -> PGR S2; cdragon-arena dataValues+calculations enrichment; Arena S2 Augment Level-Up patch 26.09; live-game UX validation pending.

- ✅ **Overnight autonomous run END-TO-END (Phases 1-3) - SHIPPED 2026-05-19/20 (31 commits `021c6c4`..`98e059c`, pushed; ENGINE_VERSION 1.9.0 -> 1.17.0; CI 31/31 green; no frozen-file edits despite operator's blanket grant).** Single overnight run executed end-to-end in caveman ultra. Full off-repo synopsis at `C:\Users\Administrator\Desktop\RC_OVERNIGHT_SYNOPSIS_2026-05-20.md`; full ledger entry at CLAUDE.md item 108. **DS audit 18 iterations, 8 real bugs (8 ENGINE bumps):** iter 1 `3013500` multi-source %-pen multiplicative compose (1.9.0 -> 1.9.1); iter 2 `08c6e03` lethality V14.1 1:1 flat pen, removed legacy level-scale (1.9.1 -> 1.10.0); iter 3 `0ff658a` hydra_cleave unique-passive family (1.10.0 -> 1.10.1); iter 7 `54c40a8` BotRK Mist's Edge melee 8% -> 9% (1.10.1 -> 1.10.2); iter 8 `a9a0bb9` Immolate family + Titanic Cleave Meraki drift + 8 Arena mirrors (1.10.2 -> 1.11.0); iter 10 `6fac6cc` The Collector lethality drift fixed (1.11.0 -> 1.12.0); iter 11 `884ef06` Statikk Shiv Electrospark 110/3s -> 180/10s (1.12.0 -> 1.13.0); iter 12 `0c550b5` Guinsoo's Wrath flat 30 magic on-hit was missing + family audit (1.13.0 -> 1.14.0); iter 14 `a4ddcb8` Arena mirror audit sync 226676 Collector + 223124 Guinsoo (1.14.0 -> 1.15.0); iter 15 `2c38c2d` Hextech Alternator 75 -> 65 + Voltaic Firmament flat 100 phys (1.15.0 -> 1.16.0); iter 16 `1f1a8d6` Sundered Sky calibrated to `70 + 80% total_AD` + Dead Man's `defensive_only` flip with stacks-schema deferral (1.16.0 -> 1.17.0). Iter 4-6 + 9 + 13 verifier-clean; iter 17-18 NO-CHANGE with 2 structural drifts queued. 14 stale ENGINE pins synced `3e5c5b0`. **5 UX backends shipped, 4 with frontends:** `0d04829` champ-select threat tags `/api/personal-vs` (lifetime SR-only WR vs hovered enemy, 60s TTL cache, bands red/amber/green/unknown) - frontend wired same commit; `c7698d9` `/api/damage-mix` per-enemy AD/AP/true breakdown, frontend `de4a2c0` 4-slice SVG donut in Active Match build pane; `ca07e9f` `/api/spike-curve` team power-curve ally-vs-enemy DPS+EHP across levels 1/6/11/16/18, frontend `18589ab` 40px sparkline panel above BUILD; `97c29f5` + `7cdc681` per-enemy summoner CD ledger backend (`core/summoner_cooldowns.py`) wired into `/api/state`, frontend `8c962f6` right-rail panel; `ccfe1c8` + `19a33ef` ward-heat backend + producer (`core/ward_producer.py` infers from allPlayers inventory delta) - frontend DEFERRED to operator. **Shaper backend primitive `c6327d8`:** `core/shaper.py` `ShaperState` + `apply_shaper` pure function + 57 property tests; gated on 100% DS coverage. **pytest_guard docs-skip fix `89d4eb4`:** empty/unknown payload now correctly SKIPS pytest + 22 gate tests. **Active Match step 5 sub-unit `7767126`:** dead fleet view JS + CSS stripped; remaining step-5 count flipped 5 -> 3. **ASCII retro-sweep `021c6c4`:** 5 files purged of stray glyphs. **Verified:** DS tests **2710 passed / 1 xfailed / 1663 subtests**; wider RC ~2087 / 0 failed; CI **31/31 green**; py_compile + ruff + ASCII clean across every commit; DS :8893 serves engine_version 1.17.0 live; RC :8888 alive. **Don't-redo:** all 8 ENGINE iterations real Meraki/V14.1 patch-grounded - do NOT revert; iter-1 multiplicative %-pen compose engine-authoritative, do NOT re-pitch additive; iter-2 lethality 1:1 flat-pen is post-V14.1 doctrine; iter-3 hydra_cleave family extension stops Titanic + Stridebreaker double-proc; iter-16 Dead Man's defensive_only flip is intentional safer state; the 4 UX frontends wire-complete; shaper primitive intentionally pure-function gated on 100% DS coverage; fleet view removal final; pytest_guard fix validated mid-run; no-frozen-file constraint honored despite blanket grant. **Carries forward:** iter-17 `hps_split_aram_heal_shield_mults` + `aramAbilityHaste`/`aramTenacity` queued; iter-15 Dead Man's stacks-schema deferral; ward-heat frontend operator-gated; shaper UI blocked on DS 100% coverage; champ-select / live game UX validation pending; queued `ruff --unsafe-fixes` + type-hint pass STILL operator-gated.

- ✅ **Cross-Claude lessons Phase 4 - SHIPPED 2026-05-19 (`77e3437`, pushed).** BACKLOG Cross-Claude infrastructure item drained. 3 sub-units in one slice: (a) `core/lessons_ack_watcher.py` polls `/api/bridge?kind=result` over HTTPS shuttle, merges in_reply_to-matched ack body into `lessons_sent.jsonl` atomically (fail-soft fetch); (b) `core/lessons_confidence.py` reads both ledgers, computes per-(peer, mem_type) buckets via `core/smoothed_rates.laplace_rate`+`shrink` (composes on CLAUDE.md #90 shared primitive); (c) `core/lessons_revert.py` `apply_with_revert(lesson_id, commit_sha, tests)` runs pytest, on fail+sha does `git revert --no-edit` + follow-up `auto_reverted=True` ack. Surfaces: `tools/lessons_status.py` (JSON + `--plain` tabular) + new `GET /api/lessons/status?refresh=0|1` route (`dashboard/routes_lessons.py` registered in `_dispatch.py`). +60 tests across 4 new files. Wider RC suite **1771 / 61 subtests / 0 failed** (s105 1705 baseline + 66). py_compile + ruff + ASCII clean. Live-verified: `/api/lessons/status?refresh=0` 200 with 2 real buckets (peer:reference 1/1 took, peer:unknown 4 discarded). `docs/OPERATIONS.md` "Cross-Claude lessons (Phase 4 surface)" section added; BACKLOG entry flipped. **Don't-redo:** all modules + surface + tests + docs done + behavior-proven + live-verified. The "auto-revert is NOT a daemon, Claude-invoked safety net" framing is deliberate (vision section 5 + section 6 lean-conservative constraint) - do NOT re-pitch a daemon-fired auto-apply. mem_type-from-path derivation is the agreed schema-recovery path (no jsonl row migration). HTTPS shuttle stays the single-source-of-truth boundary (same s105/s245 lesson).

- ✅ **DS per-level stat-growth correctness fix - SHIPPED 2026-05-19 (`fd80bf3` + docs-sync `17c782e`, pushed; ENGINE_VERSION 1.4.0->1.5.0).** Found cross-checking DS math against lolmath `@lolmath/calc`. `agents/daemon_slayer/stats.py` scaled champion per-level base stats LINEARLY (`base + perlevel*(level-1)`); Riot's in-game growth is QUADRATIC: `base + perlevel*(level-1)*(0.7025 + 0.0175*(level-1))`. The curves coincide ONLY at level 1 (mult 0) and level 18 (mult exactly 17.0); every per-level base stat (hp/mp/regen/armor/mr/ad) was over-stated for levels 2-17 - a real correctness bug, cross-checked vs the canonical Riot/League-wiki formula + lolmath's growth(). Focused TDD session in an isolated worktree (removed; branch `fix/ds-stat-growth` FF-merged to main). New `stats.growth_multiplier()` + `stats.scaled()`; the 8 `CHAMPION_SCALING_RULES` repointed off the deleted `linear`; `attack_speed_scaling` (already correct Riot AS math) + `clamp_level` + structure untouched; single application point `engine._scale_champion_base` (no double-scaling); the per-level ability lambdas in `_effects_data.py` are intentionally linear ability scalings and were correctly left alone. Engine hand-proven correct BEFORE any rebaseline (16 stat curves + DPS/EHP pipeline traces match the formula exactly; Garen/Lux HP match known in-game values); then exactly 6 DS tests rebaselined to corrected output - 4 genuine mid-level drift pins + 2 pre-existing fragile exact-float assertions made tolerant. ENGINE_VERSION 1.4.0 -> 1.5.0 + 14 version pins. Adjacent: corrected the stale/backwards pen-pipeline comment in `ability_dps.py:1142` to match the actual `effects.effective_target_armor` order (flat-reduction -> %-reduction -> %-pen -> flat-pen); no pen code changed. **Verified:** DS 2283/748/0, wider RC 1457/0; py_compile+ruff+ASCII clean. Production :8893 restarted via the RC-DaemonSlayer scheduled task -> serves engine_version 1.5.0; live: Garen L1=690, L11=1549.95 (in-game 1550), L18=2356.0000 (endpoint unchanged). Living docs synced `17c782e`. **Don't-redo:** fixed + hand-proven; do NOT touch AS scaling (correct) or re-pin the 2 float-noise tests (tolerant assertions are right); the 6 test rebaselines are the intended consequence. **Flagged (not blocking, operator):** DAEMON_SLAYER champ-coverage prose ("196 entries / 125 champions", "73%") + BRIEF "2,851 matches" deliberately left as prior-batch state (not affected by this fix; a correct recompute belongs to a DS-batch session's own docs-sync - the 4 override registries use a nested `_meta`/`default`/overrides schema); pre-existing broken ref `docs/_archive/CHANGELOG.md` (3 cites) + `.claude/commands` vs `tools/` sync-all-md.md drift surfaced, not auto-fixed; local branch `fix/ds-stat-growth` retained (merged+pushed, delete anytime).

- ✅ **s246 - effects.py engine-core facade split - SHIPPED 2026-05-18 (`34da8d1`, pushed).** The **final AUTONOMOUS_AUDIT 4.C item** (audit #6, the last oversized engine-core module) - operator-chosen from the s245-NEXT fork with review available; `effects.py` is NOT frozen (the CLAUDE.md frozen-list is explicit and does not contain it). **The entire AUTONOMOUS_AUDIT s5 menu is now exhausted:** 4.A dashboard.js quarantine, 4.B `unique_passive_key` on all 6 rankers (`aaa9c5a`), 4.C builders.py / adaptation_hint.py (s242) / supervisor.py (s243) / **effects.py (this)**, 4.D view-router cross-link (`5843c93`), 4.E `test_builders_enrich.py` - all done. Same byte-verbatim facade pattern proven in s242/s243. **Split (acyclic `_effects_types <- _effects_data <- effects`):** `_effects_types.py` (392 LOC - schema types + damage-type constants `CallContext`/`DamageFn`/`PeriodicProc`/`ItemEffect`/`PHYSICAL`/`MAGICAL`/`TRUE`/`_DAMAGE_TYPES`, original lines 37-419 verbatim, zero deps); `_effects_data.py` (4947 LOC - the 547-entry `ITEM_EFFECTS` table, original lines 422-5347 verbatim, imports the 6 type/constant names its literals use); `effects.py` (5692 -> 397 LOC - re-export facade: original docstring + the 14 effect-aggregation logic fns verbatim + full public-surface re-export; original stdlib import block kept verbatim so `effects.<dataclass|Callable|Iterable|Union>` still resolve, the s243 attribute lesson). All 14 importers unchanged. **Design (don't re-litigate):** the types MUST be a third module (data <-> facade would be circular); the 14 functions stay in the facade (they ARE the logic - "leave effects.py pure logic"); NO `__all__` added (the original had none - adding one would change `import *` semantics); classes' `__module__` is now `_effects_types` (intentional, no consumer asserts it / no pickling - same call as s243 moving `_QuietHandler`); the non-ASCII box-drawing chars in data-table comments are preserved **verbatim** (byte-relocation, not authoring - the no-em-dash rule governs authored content and that purge was a separate operator-gated pass). **Equivalence proof:** AST self-check - all 23 substantive top-level nodes (4 constants, 4 type defs, the full `ITEM_EFFECTS` table, all 14 functions) byte-identical to the pre-split original + facade docstring identical; full `tests/` + `agents/daemon_slayer/tests/` = **3727 passed / 778 subtests / 0 failed**, exactly the pre-split baseline (the DS value-pin suite is the definitive behavior-equivalence proof). ruff + py_compile + archmap + state_schema clean; pre-commit green. **No ENGINE_VERSION bump / no DS :8893 restart** - a no-op source relocation, the running engine's math is identical (audit #5 + ROADMAP-85 precedent for behavior-neutral work). **Don't-redo:** split done + behavior-proven; do NOT re-pitch a data-format change / re-merge / `__all__`. **NEXT:** AUTONOMOUS_AUDIT fully closed. Remaining pending work is operator-gated, not autonomous-safe: the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual) and the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232).

- ✅ **s245 - #6 local DS + match-DB MCP server - SHIPPED 2026-05-18 (`f585933`, pushed).** AUTONOMOUS_AUDIT s5 product opportunity #6 (operator-directed from the s244-NEXT autonomous-safe fork, over the operator-gated effects.py engine split / the live-game-gated s220 PGR reframe). The DS engine (:8893) and `data/match_history.db` were RC-internal infra reachable only from inside the running dashboard process; a local agent doing build research / calibration / post-game analysis had no first-class way to query them. New `tools/ds_matchdb_mcp_server.py` mirrors the proven `tools/gamepc_mcp_server.py` pattern EXACTLY (stdlib `ThreadingHTTPServer` + JSON-RPC 2.0 over `POST /mcp`, Bearer auth, `GET /health`, `--show-token`, connect-probe port preflight, **zero new deps**) + `tools/start_ds_matchdb_mcp.py` boot launcher mirroring `tools/start_daemon_slayer.py` (cwd pin, port preflight, logs/+ProgramData fallback, `serve_forever()->int` contract). **8 focused tools:** 4 DS (`ds_health`, `ds_rank_items`, `ds_build_order`, `ds_archetype_for` - wrap `core.daemon_slayer_client.rank_for_primary_archetype` / `core.build_order.plan_build_order` / `core.archetype_picks.get_archetype_for`) + 4 match-DB (`match_recent`, `match_mode_stats`, `match_tft_comps`, `match_tft_streak` - wrap `core.match_db.MatchDB` read queries via a lazy WAL-safe process-wide singleton). **Design (don't re-litigate):** (1) port **8894**, **HOST 127.0.0.1** - localhost-only because it proxies the *local* DS engine + reads the *local* DB (no cross-machine need, unlike gamepc's `0.0.0.0` LAN exposure); Bearer auth kept anyway for defense-in-depth + same token chain (`RC_MCP_TOKEN` -> `tools/mcp_token.txt` -> `tools/vision_token.txt` -> dev fallback) so ONE token covers both RC MCP servers. (2) **Fail-soft, never load-bearing** (the `daemon_slayer_client` opt-in-infra contract): engine-down + match-DB-missing both return a graceful `{error,...}` dict, never raise. (3) **Archetype auto-resolve** - blank `archetype` -> `get_archetype_for(champion)["primary"]` (the same smart default `dispatch_for_coach` uses; live-proven: Vayne's persisted `mage` override routed `ds_rank_items` -> `ds.ability`). (4) Read-only w.r.t. RC state (never writes the match DB). **Verified:** 36 new tests in `tests/test_ds_matchdb_mcp_server.py` (pure tool fns w/ deps monkeypatched + protocol handlers + a real ephemeral-port server for the auth/JSON-RPC envelope contract + a TOOLS_SCHEMA<->TOOL_FUNCS parity guard + ASCII-only guard); full `tests/` **1457 / 30 subtests / 0** (s244 baseline 1421 + 36); `ruff` + `py_compile` clean; pure ASCII. TDD caught a real bug pre-commit (`if str(i).strip()` turned `None` -> the string `"None"`; fixed to the client's `if i` idiom). **Live-proven end-to-end via the production launcher** against the live DS engine (:8893 `engine_version=1.4.0` patch 16.10.1) + real `match_history.db`: initialize/tools-list/all-8-tools 200, `ds_build_order(Vayne)` ran 4 sequential live engine calls -> a real unique-passive-safe ordered build, `match_mode_stats(ARAM)` returned 20-game aggregates; ad-hoc test instance taskkilled clean (never `Stop-Process`). No ENGINE bump / no DS restart / no RC restart (new standalone tool; non-engine; ROADMAP-85 precedent). docs/OPERATIONS.md gained the scheduled-task row + a `## Local DS + match-DB MCP (:8894)` section. **Don't-redo:** server+launcher+tests+docs done + behavior-proven; the localhost-only + zero-dep + gamepc-mirror architecture is deliberate - do NOT re-pitch a FastMCP/SDK rewrite or LAN exposure. **OPERATOR-GATED (not blocking, flagged):** (a) the `RC-DS-MatchDB-MCP` scheduled task is documented but NOT created - it is a new always-on listener; register only when a local Claude is actually pointed at it. (b) `.mcp.json` client wiring is NOT done - editing it changes a live Claude session's own tool surface (operator's call when/if to consume it). **NEXT (AUTONOMOUS_AUDIT s5):** the only remaining 4.C is `effects.py` (5692) engine-core = reviewed-only-never-autonomous (operator-gated). Other autonomous-safe work largely exhausted across s237-s245; the big pending non-engine item is the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual). Optional data: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232).

- ✅ **s244 - #4 Arena/Mayhem augment recommender FOREGROUNDED - SHIPPED 2026-05-18 (`d7f6033`, pushed).** AUTONOMOUS_AUDIT s5 product opportunity #4 (operator-directed; chose over #6 local MCP / effects.py). The data-driven recommender (`core/augment_recommender.py` + `augment_external_source.py`, #88) shipped+tested but its ONLY surface was the invisible `#augments-pill` `title` hover tooltip - useless mid-game on a glance-only secondary monitor. Now a prominent `#ib-aug-reco-block` INSIDE `#item-build` surfacing the causal "why" (own WR vs external Mayhem prior, synergy, confidence, sample size) - the explanation Overlay App Z6/Aggregator B/Overlay App E structurally cannot give for Arena augments (a niche they all skip). New `web/js/panels/augment_reco.js` (presence-gated, sig-idempotent) + `web/css/panels/augment_reco.css` + dashboard.css @import + index.html block + main.js wire after `renderItemBuild`; `coaches/arena_coach.py` `_augment_recommendation` gained per-row `"syn"` (additive). **Design (don't re-litigate):** toggled build-section inside #item-build mirroring `#ib-ds-block` - NOT a new grid panel (zero layout risk; matches the established no-reflow pattern that drove augments to the header pill in 2026-04-23); `p.aug_reco` presence IS the mode gate (module decoupled from mode authority); `#augments-pill` tooltip left unchanged (no regression). **Verified:** full `tests/` **1421 / 30 subtests / 0** (s243 baseline 1405 + 16 new: 14 DOM-wiring guards + 2 Playwright snapshot tests arena+Mayhem); snapshot suite 13/13; node+ruff+py_compile clean. Asset hash flipped LIVE to `1819f07ef8` (ADR-008 auto-reload - no RC restart), confirmed via Game-PC DISPLAY5 capture (clean Home load, footer shows new hash). No ENGINE bump / no DS restart (non-engine, ROADMAP-85 precedent). **Don't-redo:** foregrounding shipped + behavior-proven; the inside-#item-build toggled-block decision is deliberate - do NOT re-pitch a separate grid panel; session-start "RC-Phase3-Supervisor failing" was a FALSE POSITIVE (pid 15068 = s243-verified healthy; `0x800710E0` = task-terminated residue of s243's End+Run). **NEXT:** (a) operator-gated - live Arena/Mayhem augment-select render proven only by snapshot fixtures (no game ran; LCU offline); validates on next Arena/Mayhem game. (b) `arena_coach` `syn` activates on next coach reload (JS degrades gracefully meanwhile). (c) Remaining autonomous-safe: #6 local DS+matchDB MCP, or effects.py split (operator-gated engine session).

- ✅ **s243 - agents/supervisor.py 2312->966 LOC behavior-preserving facade split + freeze-conflict RESOLVED - SHIPPED 2026-05-19 (`ca30daa`, pushed).** AUTONOMOUS_AUDIT 4.C. **Freeze first (the operator-gated blocker):** `agents/supervisor.py` is **NOT frozen** - CLAUDE.md hard-list has `ops/rc_supervisor.py` (the *main* supervisor), not this (Phase-3); s238's "FROZEN" label + the audit's "extract `_Phase3Watcher`+`CircuitBreaker`" target both conflate the frozen sibling (those 2 classes live at `ops/rc_supervisor.py:210`/`:340`, NOT here); edited routinely w/o frozen-auth s168/s171.8/s173/s218/s236. Persisted to ROADMAP + memory `reference_two_supervisors.md` so it stops recurring. **Split (s237/s242 byte-verbatim facade pattern):** facade 2312->966 LOC; `_QuietHandler`/`_WebServer`/`start_web_server` -> `agents/_supervisor_http.py` (925), ephemeral claude dispatch -> `agents/_supervisor_ephemeral.py` (250), constants+leaf-helpers+`log` -> `agents/_supervisor_common.py` (364). `Supervisor`+`_run`/`main` stay in the facade BY DESIGN - tests patch `agents.supervisor.{IDLE_ANALYZE_SEC,WARM_UI_*}` + reassign `sup_mod.{_PROJECT_ROOT,LOG_ROOT}`, all read by Supervisor methods (the s242 monkeypatch-contract lesson); full surface re-exported via `__all__`; F401 ignored project-wide so the facade keeps its original import block verbatim (makes `agents.supervisor.shutil`/`.subprocess` resolve for `test_supervisor_stub`). Acyclic `_common <- _ephemeral <- _http <- facade`. Extraction = AST-span line-slice + self-check proving all 48 top-level code nodes byte-identical before write (throwaway script deleted). 10 structural-pin tests (`test_round26/29/32/33/34/35/36/37/38/42`) repointed to the handler's new home (intent + behavioral tests unchanged). **Verified (equivalence = unchanged tests):** full `tests/` **1405 / 30 subtests / 0 failed** (exact s242 baseline) + agent3 **356 / 0**; `ruff check .` + compile-all clean. **Phase-3 elevated bar CLEARED:** s236 mtime-watcher auto-restarted Phase-3 into the new code as predicted; controlled End+Run -> pid 15068, `status.json.phase3.last_state=healthy`, `:8890`/`:8891` listening, `GET /api/env` 200 (proves moved `_QuietHandler` <-> facade `Supervisor` wiring live). No ENGINE bump / no DS restart (non-engine, behavior-neutral; ROADMAP-85 / s237 / s242 precedent). **Don't-redo:** the split is done + behavior-proven; `agents.supervisor` is a facade, callers import unchanged; `Supervisor` MUST stay in the facade (monkeypatch contract) - do NOT move it to a sibling; the freeze question is CLOSED, do not re-litigate (only `ops/rc_supervisor.py` is frozen). **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C `effects.py` (5692) engine-core reviewed-only-never-autonomous. Autonomous-safe: product opportunity #4 (augment recommender foregrounding - `core/augment_recommender.py` ships, needs marketing-pillar UI) or #6 (local MCP exposing Daemon Slayer + match DB :8893). Optional data: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232).
- ✅ **lobby "change mode" FIXED + champ-select 920→2400 label sweep + 2 patch-structural changes (`c3a1e23`, pushed; CLAUDE #89). Repo-side complete; live verification operator-gated.** Operator: lobby switch works for standard matchmade queues (Swiftplay/Draft/Ranked/ARAM-Classic/TFT/Double-Up) but **NOT Practice Tool, ARAM Mayhem, Arena**. Recon done (don't re-grep cold) - root causes per mode: Practice Tool needs `lobby.create_practice_tool` (only the Home picker `main.js:2614-2629` special-cases it; lobby-view menu `main.js:3713-3723` is generic-`change_queue_type`-only); ARAM Mayhem lobby path is stale on queue **920** (should be **2400** - champ-select was fixed in the keystone item below, lobby create/switch was not; `gamepc_lcu_agent.py:229`); Arena (1700) likely needs a bespoke Cherry lobby-create payload, not generic `change_queue_type`. **Same session, patch-structural:** Brawl removed from rotation (strip lobby-side residue; champ-select already done s214); Arena **2v8→3x6** (6 teams × 3 - update champ-select sub-team render + tips + agent Arena handling). Needs a LIVE client to verify (lobby creation can't be proven in sim). Don't touch the working standard-queue modes. + cross-family unique-passive no-double rule** (commits `1a20424` `18139a5` `240f709` `da4f334` `81af51e`; **0 engine changes, ENGINE stays 1.3.0, no DS restart**). Closed "always the same items, not match-specific" + the hard rule "never recommend two items sharing a unique passive". Root cause: every `rank_items_by_*` does *greedy single-item marginal* scoring vs a fixed owned-item set + returns a flat top-N; the list itself can contain Trinity Force **and** Essence Reaver (both `spellblade`). Fix = pure orchestration: new `core/build_order.py` `plan_build_order()` iterates `rank_for_primary_archetype` once per slot, appending each pick to `item_ids` so later slots re-rank vs the accumulated build + real enemy context (kills "always the same"), forcing `filter_shared_uniques=True` so the engine's own `collect_effects`/`current_unique_keys` dedup excludes every same-family candidate for all later slots - **engine-authoritative, planner carries NO family map (s173 anti-drift safe)**. **Live-proven vs real DS :8893**: flat bruiser ranking of {Trinity Force, Lich Bane, Essence Reaver} returns all 3 (the bug); planner stops at exactly 1. Opt-in wired into `coach_integration/archetype_dispatch.py` (`with_build_order=False` default - per-tick path stays 1 call) + read-only `POST /api/build-order` (`dashboard/routes_state` + `BuildOrderRequest` schema + dispatch pin). Finding: `effects.py` has **6** unique-passive families not 3 (`spellblade`/`lifeline`/`immolate` + single-item `fiendhunter_barrage`/`hellfire_char`/`innervating_fill`) - family-agnostic planner covers all 6 + any future one for free; machine-guarded (`tests/test_build_order_no_double_guard.py`, derives the set from `ITEM_EFFECTS`, ≥6 tripwire). Full `tests/` **1141 passed / 0 failed** (one self-introduced dispatch-pin regression caught by the full-suite run + fixed in-run, `81af51e`); ruff+py_compile clean. Plan + cascade-6 Phase-3 UI design menu at `Desktop/BUILD_ORDER_PLAN_2026-05-17.md`.
- 🟡 **#89 live verification - operator-gated (not provable offline).** Repo-side shipped `c3a1e23`. At Game-PC w/ live client: confirm Practice Tool / ARAM Mayhem (now sends queue 2400) / Arena lobby creation from BOTH pickers; if any still fails the LCU error is now surfaced (`_lvQueueChangeError`) - capture it for the bespoke Arena/Mayhem create-payload (recon: must come from live capture; deliberately not fabricated). Confirm Arena champ-select renders 6×3 (allies 3 cells / central me+2 / 5 enemy trio cards). Agent name-map (cosmetic queue label + Brawl drop) needs a Game-PC `C:\RC-Agent\` redeploy (bridge dispatch) - the core Mayhem fix is JS, served from Legion, no redeploy. Precondition: confirm League WindowMode in-client (prior fullscreen-lockup note). `dashboard.js:5055` has the same dead `_replayQueueLabel` 920 bug - confirmed-dead legacy, left alone.
- 🟡 **build-ORDER go-live + Phase-3 UI render** - **UI B+C SHIPPED `2d7da9e`** (collapsible champ-select "Build Order" card + in-game `#ds-pill` next-2 glance; headless-verified node --check + 38 snapshot/view-router; go-live was already-serving from the morning RC restart, ADR-008 auto-served). **Live champ-select visual + per-page UI-audit ritual UNBLOCKED** - the ARAM/Mayhem KNOWN-BUG keystone is ✅ fixed + proven live (`3eb2e2d`, see ✅ entry below); the B-card will render in any (now-correct) ARAM/Mayhem/Arena champ-select. Original scope was operator-gated, NOT done blind overnight. (a) `/api/build-order` serves on the next RC reload; (b) the reviewed UI session renders the ordered build per the desktop plan §6b 3-option menu (recommend: vertical "Build Order" card + `#ds-pill` glance; reject horizontal-cram) - needs the per-page UI-audit ritual + a live game. Plus a small carry-branch fix: `only_item_ids` is silently ignored by `rank_for` (dps); only the 5 non-dps scorers thread it (needs a DS restart).

- ✅ **KEYSTONE - champ-select wrong for ARAM-Mayhem: FIXED + PROVEN LIVE** (2026-05-17, commit `3eb2e2d`; surrender-slice `a5405ee`). **Root cause was NOT phase-timing/retention/router** (the hand-off's leading hypothesis) - it was a flat **queue-ID gap**: ARAM Mayhem reports `queueId=2400` (KIWI, documented s220 from real match data), but `core/queue_modes.QUEUE_ID_TO_MODE_KEY` only mapped `920` (Poro King - the codebase had conflated 920↔2400) and the agent's `is_aram = queue_id in (450,920)` likewise omitted 2400 → `is_aram=False` → JS `_csvDetectMode` fell to "sr", bench/quick-swap never rendered; `_preflip_mode_from_lcu(2400)→None` → `mode_key` stuck at "client". The hand-off's "`phase:InProgress` already" was the *symptom of the old broken state*, **not** Mayhem actually reporting InProgress - disproven live (`cs_debug.raw_phase=ChampSelect`), so the phase-driven view-router was correct and deliberately left unchanged. Fix: add `2400→aram` to `core/queue_modes`; agent uses a single `_ARAM_QUEUE_IDS={450,720,920,2400}` mirror (anti-drift parity test `tests/phase_b_champ_select` guards it); new `dashboard/_cs_retention.py` holds the last champ_select across the genuinely-fast no-draft flip (robustness layer - also fixes the plain-ARAM/Arena transient-loss half of the operator symptom); agent `cs_debug` breadcrumb (raw gameflow-phase + `/lol-champ-select/v1/session` shape) so the open question was answerable from Legion with no screen capture; `tests/conftest.py` autouse fixture isolates the new process-global cache. **1159 tests pass**, ruff+py_compile clean. **Proven live in a real Mayhem champ-select:** `queue_id=2400`, `is_aram=True`, `mode_key=aram`, `champ_select` fully populated (`bench=[420,38,27,12,887,238,83]`), `cs_debug.raw_phase=ChampSelect`. Deployed: RC reloaded (pid 12668, `last_reload_ok=true`); Game-PC `gamepc_lcu_agent.py` redeployed via bridge (`task-08722043fb4c`, ~45s round-trip → new agent pid 8464). Load-bearing detail: Mayhem's champ-select session omits `gameData.queue` (all-None, s154 behavior) - the agent's `/lol-gameflow/v1/session` fallback recovers `2400`; that fallback is essential and confirmed working. **Residual: none functional** - only the operator's own visual reassurance of the rendered bench on a future Mayhem champ-select (the data path is proven; no capture pursued - in-game capture is the Vanguard-BSOD surface).
- ✅ **s237 - dashboard/builders.py payload-boundary split + `_enrich_from_lcu` coverage** (2026-05-18, `2d1993c`, pushed). AUTONOMOUS_AUDIT spec 4.C (lowest-risk slice) + 4.E, paired so the split is provably behavior-preserving. **4.E first:** `tests/test_builders_enrich.py` - 31 characterization tests for the untrusted-LCU parser `_enrich_from_lcu` (guard-clause bails return `{}`; full parse shape; regression anchors = `firstDargon` LCU-typo key + `cs=minions+neutral` + `heal_plus_shield` sum + 1-indexed `playerAugment` + single `is_me`; SR/Mayhem/Arena fixtures; odd-field coercion) - green on the PRE-split baseline. **4.C:** `builders.py` 1428→288 LOC; home/LCU-enrich/last-match clusters extracted byte-verbatim via an audit-gated atomic-splice (boundary sentinels; script deleted) into `builders_home.py`/`builders_lcu_enrich.py`/`builders_last_match.py`; builders.py keeps session/history/loadouts/diagnostics + **re-exports all 21 public names** so every caller (and web_dashboard's re-bind) is unchanged - no circular import, no `builders_common` needed. Verified: py_compile (8 mods) + ruff clean; full `tests/` **1314 passed / 0 failed**; facade smoke (correct `__module__`); RC reloaded clean (pid 11020, reload_ok); `/api/home/summary` `/api/last-match` `/api/history` `/api/diagnostics` all 200 live. **Don't-redo: the split is done + behavior-proven; `dashboard.builders` is a facade, callers import unchanged.** **NEXT (AUTONOMOUS_AUDIT s5):** product-level opportunity #2 (per-user pick-ban) or #3 (on-demand VLM coach), operator-directed. Remaining 4.C: `adaptation_hint.py` (1602) autonomous-OK; `agents/supervisor.py` (2312) FROZEN→needs approval; `effects.py` (5692) engine-core reviewed-only-never-autonomous.
- ✅ **s240 - on-demand VLM coach (SCREEN READ) - SHIPPED 2026-05-18 (`306f5f3`, pushed).** AUTONOMOUS_AUDIT s5 competitive opportunity #3 (operator-directed; full vertical slice w/ dedicated pill). Operator clicks "SCREEN READ" -> `POST /api/command{screen_read}` -> RC does ONE Sonnet pass over the frame it ALREADY holds in the `:8889` relay -> a single tactical coach one-liner on a dedicated RIGHT NOW pill. New `dashboard/_screen_read.py` (`ScreenReadVision(GameVisionReader)` + `run_screen_read` + `trigger_screen_read`: sync pending marker, daemon-thread Sonnet pass, in-flight dedupe); `routes_state` command case; `_state_builder` stamps `state.screen_read` (dedicated field, NOT `coach.immediate` -> not clobbered by the next coach tick). Frontend mirrors the s184.1 chip (`web/js/panels/screen_read.js` + button/pill in RIGHT NOW panel + 3 `/api/state` callsites + `.rn-sr-*` CSS). **Key low-risk finding:** RC does the whole pass server-side via `modes.shared_vision._capture_screen()` reading the relay buffer the s221-safe bettercam agent already fills - **NO new capture (does NOT touch the Vanguard-BSOD surface)**, **NO new Sonnet/cost code** (subclasses the audited `GameVisionReader` -> moon_proxy fallback + `cost_tracker` cap + token-bucket apply verbatim, s173 anti-drift). ADR-006 compliant (vision/LiveClient self-coaching only; prompt = strategic judgement, never input automation). TDD red->green - caught + fixed a real `{}`-is-falsy bug (`if not raw:` -> `if raw is None:`). 30 new tests; full `tests/` **1405 / 0 failed** (s239 1375 + 30); ruff+py_compile+node-check clean. RC reloaded (pid 9824). **Live-verified off-game:** command -> trigger -> worker -> graceful `no_fresh_frame` -> `/api/state.screen_read`. `data/screen_read.json` gitignored (runtime). No ENGINE bump / no DS restart (non-engine, ROADMAP-85 precedent). **OPERATOR-GATED:** the live Sonnet-on-real-frame pass + visual pill only fire in a live game (relay fed only mid-game). Next live game: click SCREEN READ, confirm a sane ADR-006-toned one-liner renders (ok status) + does NOT overwrite the coach `immediate`. **Don't-redo:** full slice + pipeline done + live-proven off-game; the "RC server-side from relay buffer, no MCP, no BSOD surface" architecture is established - don't re-investigate. **NEXT:** autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split; or product opportunity #4 (augment recommender foregrounding) / #6 (local DS+matchDB MCP). DS conditional arc operator-CLOSED (s232).
- ✅ **s239 - per-user pick-ban contextual read + champ-select foreground - SHIPPED 2026-05-18 (`c0cf54f`, pushed).** AUTONOMOUS_AUDIT s5 item 4 = competitive opportunity #2 (operator-directed; items 1-3 done: Phase 4d `aaa9c5a` / dashboard.js quarantine s236 / builders split s237). Operator chose full scope ("Contextual read + foreground"). New read-only `GET /api/champ-select/personal-record?champ=&allies=&enemies=&role=&queue=` in `dashboard/routes_pickban.py` (not frozen; additive): the mood queries RECOMMEND, this REPORTS the operator's actual lifetime record vs the champions on the board - WR on hovered champ (all-roles headline + at-role qualifier), WR with each locked ally, WR vs each locked enemy. The per-user differentiator no cohort-averaged SaaS can produce. `champ_select.js` fetches it (same 60s dedupe as the mood recs) + prepends a titled "YOUR RECORD" headline ABOVE the mood rows so the per-user model is the first/strongest thing in the (otherwise header-hidden, buried) pick-ban card = the foregrounding. New `.csv-pr-*` CSS (lavender accent, WR-tinted chips, >=13px floors); row-2 grid floor 354->460 to budget the headline (only binds on short viewports). Assumptions stated (hard rule): A1 vs-enemy = opposing-team any-lane (`_query_bans` stays lane-strict for recs by design); A2 with-ally independent of operator's champ; A4 displayed wr_pct RAW (s238 invariant - reports, not ranks); A5 0-game entries returned; A7 ALL-TIME (no recency window) so real data despite `rewind_history.db` staleness. TDD red->green; full `tests/` **1375 / 0 failed** (s238 1344 + 31 new: 16 backend incl. e2e + 15 frontend wiring/ASCII guards); ruff+py_compile+node-check clean. RC reloaded (pid 11424, reload_ok). **Live-verified on :8888 with REAL data:** Vayne 45%/47g all-roles, 18%/11g at BOT, 44%/27g w/ Thresh, 56%/18g vs Darius, 50ms. No ENGINE bump / no DS restart (non-engine, ROADMAP-85 precedent). **OPERATOR-GATED (not blocking):** champ-select **visual** UI-audit ritual - headline only renders in a live SR draft (panel CSS-hidden in ARAM/Arena/Mayhem); next live SR champ-select, eyeball headline + confirm the 3 mood rows aren't crushed, then run the per-page UI-audit subagent. **Don't-redo:** backend + foreground wire done + live-proven; assumptions operator-aligned (full scope chosen) - don't re-litigate A1/A2. **NEXT (AUTONOMOUS_AUDIT s5):** opportunity #3 (on-demand VLM coach - operator-gated Game-PC capture) OR autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split. 101.qq.com CDN seed still the only outstanding pick/ban data enrichment.
- ✅ **s242 - adaptation_hint.py 1602 LOC split (4.C) + view_router cross-link (4.D) - SHIPPED 2026-05-18 (`35566e5` + `5843c93`, pushed).** Recovered an interrupted s241 /done (operator /clear mid-ritual; only the WAKEUP rotation was uncommitted -> `35998e9`; feature+archival already pushed). Then the two named autonomous-safe NEXT items. **4.C `coaches/adaptation_hint.py`**: 1602-line monolith -> 7 modules (the s237 builders.py facade pattern): new `_adaptation_common.py` (shared DB/mode/threshold internals + `_db` + `_start_of_today_iso`) + 6 concern siblings (`_champion` coach hot-path / `_aggregates` / `_session` / `_temporal` / `_digest` / `_cli`); `adaptation_hint.py` is now a pure facade re-exporting the full 33-name surface + `__main__` guard. **Load-bearing design:** ~17 test files monkeypatch `coaches.adaptation_hint.DB_DIR` - `_db()` resolves the dir off the facade at call time via `sys.modules.get` (no import, no circular risk, never raises) so the contract holds with ZERO test edits. Extraction was an AST-span slice + a self-check proving all 25 def/assign nodes byte-identical before write (throwaway script deleted). **4.D (audit #11)**: 3-line cross-link comment at `web/js/main.js:_viewAutoDerive` -> its `dashboard/view_router_state.py` pytest mirror (the Python side already had the reciprocal header); zero runtime change. **Verified (equivalence = unchanged tests):** 162/162 adaptation_hint+test_round* (incl. every DB_DIR patch); full `tests/` **1405 / 30 subtests / 0 failed**; ruff+py_compile clean (8 files); CLI works through the facade; RC reloaded clean (pid 3184->13328, reload_ok). No ENGINE bump / no DS restart (non-engine, behavior-neutral; ROADMAP-85 / s237 / s238 precedent). **Don't-redo:** the split is done + behavior-proven; `coaches.adaptation_hint` is a facade, callers import unchanged; the `_db` sys.modules indirection is load-bearing - do NOT inline it back. **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C: `agents/supervisor.py` (2312) ✅ SPLIT SHIPPED s243 (`ca30daa`) - was RESOLVED NOT frozen; CLAUDE.md hard-list has `ops/rc_supervisor.py`, NOT `agents/supervisor.py`; s238's "FROZEN" label + the audit 4.C "extract _Phase3Watcher+CircuitBreaker" target both conflate the frozen sibling (those 2 classes are `ops/rc_supervisor.py:210`/`:340`, NOT in agents/supervisor.py); this file is edited routinely w/o frozen-authorization (s168/s171.8/s173/s218/s236). Split is autonomous-eligible via the s237/s242 byte-verbatim facade pattern over its REAL surface (`_QuietHandler`/`_WebServer` + `EphemeralStub*` excs + the ~780-LOC `Supervisor` god-class), with an elevated Phase-3 verify bar (Phase-3 has no restart_trigger; s236 `2329ebf` mtime-watcher auto-restarts it on the split -> confirmed post-split: Phase-3 live-restarted healthy, GET /api/env 200 (see the ✅ s243 entry at the top of this list for the full record).
- ✅ **s232 - DS Phase 5.9.32: conditional pure-data vein SATURATION PROOF - autonomous loop concluded** (2026-05-16, **no ENGINE change, stays 1.3.0**). Operator "Continue ds". s231 flagged the vein approaching exhaustion; rather than churn low-value no-op conversions (the explicit s223-227 "don't churn low-value registry work" lesson + the no-padding principle), ran a definitive saturation scan, surfaced the finding via `AskUserQuestion`, **operator chose "Accept loop complete."** Built `tools/ds_execute_prefilter.py` (kept, durable) to catch the Kindred-E archetype the s231 constant-k scanner structurally misses (amp concentrated in an HP coefficient, not a uniform k×). **Result: zero unaddressed clean execute candidates.** `target_full_hp` vein - the clean ones were Evelynn R + KogMaw R (s231) + Kindred E (s228); the only other true `target_missing_hp_pct` pairs are already engine-default-correct (Jinx R block 0 holds the 25-35% max coeff - confirms s217, verified live) or deliberate R-entangled skips (Varus W blocks 5/6 - s225). `target_no_setup` vein - the one real fix was Fiddlesticks Q (s230); everything else is already correct plain-int, conditional conversion = pure Part-1 no-op (zero ranking value until a Part-2 live-advisory surface consumes the downgrade - not built; s229 reframed per-tick B-2 as a mis-feature for item-ranking). All other discrete-pair hits are `target_max_hp_pct` (NOT the execute semantic - flat fraction of the health bar, not HP-gated) and already mapped-correctly. **This is the s227 situation for the conditional vein: autonomous pure-data DS work is mined out.** Shipped (NO registry/engine change): the 2 durable per-patch saturation tools + new `test_conditional_block_index_s232_saturation.py` (8 tests) including the s223-style machine-checked `MissingHpExecuteVeinSaturationGuard.test_no_unaccounted_clean_execute_pair` (enumerates every same-shape monotone-execute-coeff pair across all 171 champs; a future Meraki re-extract introducing a new clean candidate trips it = signal to revisit) + vocab-stays-2 + all-10-shipped-conditionals-intact + registry-still-125 + an ENGINE-unchanged pin (deliberately NOT bumped - docs/tooling/guard only). No DS restart, no stale-pin migrations. DS suite 2247→**2255**; ruff+py_compile clean. **DECIDED 2026-05-16 (operator) - DS conditional arc CLOSED, not continued:** Part-2 live target-state plumbing is **shelved permanently**. Rationale (the s229 finding, operator-confirmed): the conditional schema's value for the only DS surface that exists (item-build ranking) is **already fully delivered by Part-1** - `"default"` is the correct strategic model; live per-tick resolution would only be valid as a *separate live-advisory product surface*, which is **out of scope / not wanted**. DrMundo E `caster_low_hp` vocab extension **declined** - single instance, fails the s227/s229 5+-uses bar, and moot with Part-2 shelved. The 10 shipped conditionals stay as latent correctness scaffolding (the s232 saturation guard trips if a future Meraki re-extract introduces a new clean candidate). **No further autonomous OR Part-2 DS conditional-engine work - do not re-pitch Part-2.** **Next session = the s220 Post Game Review aggregator-G-style reframe** (see the 🟡 "Post Game Review aggregator-G-style reframe" item below for full scope; non-engine UI work, not blocked on the Riot key).
- ✅ **s231 - DS Phase 5.9.31: conditional seed-expansion - EXECUTE family: Evelynn R + KogMaw R** (2026-05-16, ENGINE 1.2.0→**1.3.0**). Operator "Continue ds" - continued the s230 NEXT bucket (more debuff-state-family conditional amps; **NO vocab change** - s227 "don't over-build", both reuse the existing `target_full_hp` term, extending the s228 Kindred E execute pattern). Built `tools/ds_cond_pair_prefilter.py` (kept, reusable per-patch) - scans all 171 champions for the discrete amped/un-amped pair signature (same-shape blocks, B_hi = constant k×B_lo). 280 hits but most are channel/multi-hit totals already correctly plain-int mapped (the s193/s198 pattern, NOT target-state). The untapped vein = **the EXECUTE family** (clean discrete 2× vs sub-threshold-HP, currently plain-int to the execute block). **2 conversions, both provable Part-1 no-op** (default == prior int 1, byte-identical), verified per-rank vs Meraki 16.10.1 + live A/B :8893. **Evelynn R "Last Caress"** int `1` → `{default:1, target_full_hp:0}` - block 1 = EXACTLY **2.4×** block 0 (base 125→300, ap 75→180), the bonus-vs-sub-30%-max-HP execute; TWO discrete Meraki blocks (clean pair), NOT a continuous in-block missing-HP coefficient (the precise distinction that rejected Bel'Veth R). s228 sibling Q `{default:5,target_no_setup:0}` preserved through the dict-merge. Live A/B /burst lvl11 80/30/2000: registry **1220.34** == forced{R:1} == cond-dict (no-op); forced{R:0} **951.11** (load-bearing). **KogMaw R "Living Artillery"** int `1` → `{default:1, target_full_hp:0}` - block 1 = EXACTLY **2.0×** block 0 (base/bonus_ad/ap all 2×), the double-vs-low-HP execute; registry **640.68** == forced{R:1}; forced{R:0} **532.99**. default == s204 (Evelynn) / s196 (KogMaw) int. Registry stays **125 champions** (both converted in-place). `_meta` appended via surgical str.replace. New `test_conditional_block_index_s231.py` (15 tests) + 11 ENGINE pin bumps + 9 stale-pin migrations. **Caught a latent s230 stale pin:** `ServerRouteSourceTests.test_ability_dps_champion_source` (a *live-server* test) had asserted the pre-s230 Cassi E int shape - s230 only re-ran phase8_smoke post-restart, not the full suite, so it was silently stale since s230; now migrated to the s230 conditional shape (lesson: live-server tests must re-run against the *restarted* server). **Veigar R stays the deferred non-converted exemplar** (also a clean 2.0× execute, but the canonical stable-int test fixture - s229 _meta said pick non-fixture executes; s231 did exactly that). **Akali R/R2 not touched** (s192 token-variant double-count guard). DS suite 2232→**2247**; wider RC **1107**; ruff+py_compile clean; DS restarted (taskkill 3724 → pythonw relaunch) → `/health` **1.3.0**. **NEXT:** the execute vein is largely tapped (remaining clean executes scarce - Veigar deferred-fixture, others already plain-int mapped-correctly). The autonomous conditional-seed-expansion vein is approaching exhaustion like the s223-227 pure-data sweep did; remaining `target_no_setup` candidates are mostly already-correct plain ints (conversion ROI now low - only adds value if a future Part-2 live-advisory surface consumes the downgrade). Likely flag to operator: high-ROI remaining DS work is Part-2 (live target-state plumbing - architectural, wants sign-off). DrMundo E still blocked on a `caster_low_hp` vocab decision. s220 aggregator G Post-Game-Review reframe remains the big pending UI item.
- ✅ **s230 - DS Phase 5.9.30: conditional seed-expansion - Fiddlesticks Q + Cassiopeia E** (2026-05-16, ENGINE 1.1.0→**1.2.0**). Operator "continue ds" - continued the s229 NEXT bucket (pure-data conditional seed-expansion on the stable 2-term vocab; **NO vocab change** - s227 "don't over-build", both entries reuse `target_no_setup`). **Rigor bar first:** verified every s229-named candidate against the live Meraki 16.10.1 `champion_abilities.json` (new `tools/ds_cond_inspect.py`, kept - reusable per-patch). **Lux CLOSED as a phantom carry-forward** - Lux P is `parse_status=no_damage` (zero damage_blocks), Q/E/R each single-block; no discrete amped/un-amped pair exists anywhere in its kit, it never met the discrete-pair rigor bar (the recurring "Lux Illumination" mention was recollection, not reality). **Aatrox W stays deferred** - "chain-landed" is a positional escaped/not-escaped condition, NOT an operator-applied target-state setup. **2 clean entries shipped: 1 NEW key + 1 conversion.** **Fiddlesticks Q "Terrify"** `{default:[2,3], target_no_setup:[0,1]}` - the FIRST registry conditional whose branches are `list[int]` (combines the s207 sum-of-blocks list schema with the s228 conditional schema; **zero engine change** - `_normalize_block_index_value` already recursively normalizes each branch, only rejects a *nested* dict). Filtered damage blocks (raw[0]/raw[3] duration, dropped by the `attribute_kind=='damage'` filter): fidx2 = EXACTLY 2.0× fidx0, fidx3 = EXACTLY 2.0× fidx1 - the textbook Terrify-double-vs-feared discrete pair. `default`=[2,3] feared/amped sum (Fiddle's kit revolves around fear - operator-commits canonical), `target_no_setup`=[0,1] un-amped. A **real correctness fix**: pre-s230 the engine scored Q at filtered-block-0 ONLY (un-amped %HP, missing the un-amped base AND the fear-amp). Live A/B :8893 (lvl11 80/30/2000): Q raw/cast **80→240 (+200%)**, Qdps 3.31→9.93, total 22.998→29.618; exact-2× `registry 240 == 2.0 × no_setup 120` confirmed live. **Cassiopeia E "Twin Fang"** int `1` → `{default:1, target_no_setup:0}` - provable Part-1 no-op (default==prior int 1, byte-identical; registry raw 168 == forced E:1 == 168, forced E:0 raw 100 downgrade load-bearing). **RESOLVES the s229 "Cassi E irregular 18-element Meraki array - defer until investigated" carry-forward:** the array is block 1's base scaling, rank-indexed by `_evaluate_block` exactly as since s191; the conditional conversion is orthogonal (Part-1 always resolves to default=1). Registry stays **125 champions** (Fiddlesticks gains Q alongside R/W; Cassi E converted in-place). `_meta` appended via surgical str.replace. New `test_conditional_block_index_s230.py` (24 tests) + 10 ENGINE pin bumps + cross-registry stale-pin migrations: 4 `test_block_index_overrides` Cassi-E shape pins → conditional; s228/s229 `test_cassiopeia_E_still_plain_int` → s230 evolution guards (shape + Part-1 numeric equivalence); s223 byte-stable + s225 swept-keys + s207 int-exemplar migrated. **`test_max_priority_sweep_s227.test_fiddlesticks_w_first_beats_default` MIGRATED, not "fixed":** s230's correct Fiddle Q boost (Q under-counted at s227) flips the lvl-11 numeric A/B so Q-first now numerically beats W-first (~0.85, no longer >1.10) - **precisely the s227 lesson** (max_priority is meta-curated, NOT numeric-sweepable; the W-E-Q entry is still real-meta-correct). Test now guards the *decision* (registry entry + resolution), not the stale inequality. DS suite 2210→**2232**; wider RC **1107**; ruff+py_compile clean; DS restarted (taskkill 14372 → pythonw relaunch, not supervisor-watched) → `/health` **1.2.0**. **NEXT (self-continuing loop):** more debuff-state-family conditional amps (each needs fresh Meraki verification - the s229/s230 lesson: ROADMAP/_meta recollection mis-names mechanics ~half the time). DrMundo E still blocked on a `caster_low_hp` vocab decision (caster-missing-HP, NOT target - flag for operator). Aatrox W needs the positional question answered architecturally, not pure-data. s220 aggregator G Post-Game-Review reframe remains the big pending UI item.
- ✅ **s229 - DS Phase 5.9.29: conditional seed-expansion + vocab generalization** (2026-05-16, ENGINE 1.0.0→**1.1.0**). Continued s228. **Pivot:** investigated the signed-off literal "B-2" (per-tick liveclient HP/CC → ranking call) and found it a **mis-feature for the only DS surface that exists** (item-build recommendation) - per-tick enemy HP would flicker build recs; the operator-commits default (Part 1) is the *correct* strategic model, so the schema's value is already delivered. `target_current_hp_pct` is already plumbed end-to-end; `target_no_cc` has no liveclient signal. Surfaced via `AskUserQuestion`; **operator chose "pivot → pure-data conditional seed-expansion"** (the s207→s215/s217 cadence). **Vocab generalized** `target_no_cc` → **`target_no_setup`**: the expansion candidates (Anivia *chill*, Brand *ablaze*) share s228's identical modeling semantic regardless of debuff type - 5+ concrete uses → the honest general term (vocab stays 2: `{target_full_hp, target_no_setup}`, not over-built). Validator now rejects the old key; s228's Zoe E + Evelynn Q migrated. **3 conversions of shipped unconditional entries** - all provably Part-1 no-op, verified per-rank vs Meraki 16.10.1, live-A/B on :8893: **Morgana W** `{default:3,target_full_hp:2}` (block 3 = **2.7×** block 2, the <50%-max-HP amp; s195's "vs rooted" was imprecise - the Q-root is the operator's setup; 459.00==forced{W:3}), **Anivia E** `{default:1,target_no_setup:0}` (block 1 = **2.0×** block 0 vs Chilled; 300.00==forced{E:1}), **Brand W** `{default:1,target_no_setup:0}` (block 1 = **1.25×** block 0 vs ablaze; 318.75==forced{W:1}). **Documented skips:** Veigar R (clean target_full_hp execute but the canonical stable-int test fixture - converting cascades fixture repoints; defer), Bel'Veth R (continuous in-block missing-HP, not a discrete amped/un-amped block pair - already correctly modeled), Cho'Gath R (single block), Cassiopeia E (irregular Meraki array), Brand R / Aatrox W. Registry stays **125 champions** (in-place). New `test_conditional_block_index_s229.py` (~30 tests) + global `target_no_cc`→`target_no_setup` rename (33 refs / 4 files) + 12 `test_block_index_overrides` stale pins migrated (conditional-shape + Part-1 equivalence) + 10 ENGINE pin bumps. DS suite 2188→**2210**; wider RC **1107**; ruff+py_compile clean; DS restarted → `/health` **1.1.0**. **NEXT:** more conditional seed-expansion on the 2-term vocab (Lux Illumination, Aatrox W chain-landed [resolve positional-vs-CC first], Fiddle Q fear-state, debuff-state family) - each needs Meraki block verification + the discrete-pair check. **DrMundo E is caster-missing-HP, not target** → needs a `caster_low_hp` vocab term, flag for operator. Literal B-2 live-plumbing **shelved as a mis-feature** (would need a separate live-advisory surface). s220 aggregator G Post-Game-Review reframe remains the big pending UI item.
- ✅ **s228 - DS Phase 5.9.28: conditional-target-state block_index schema lift - Part 1** (2026-05-16, ENGINE 0.99.0→**1.0.0**). The s225/s226/s227 hand-offs flagged pure-data registry work as **exhausted** (5 iterations, all 4 override registries swept) and the conditional-target-state schema lift as the only remaining DS vein - architectural, reserved for sign-off. Surfaced via `AskUserQuestion`; **operator chose option B** (the multi-session lift). This is **Part 1**, scoped exactly like s207's `int→int|list[int]` lift (schema + flagship seeds first; bulk seeding + live plumbing are follow-ups). **Schema:** `block_index` value widens `int | list[int]` → ALSO `dict[str, int|list[int]]` mapping a target-state condition → block; `"default"` (required) = the operator-commits/canonical-amped branch (s191 model); other keys = positive live-target-state descriptors selecting a **downgrade**. **Closed vocab** `_BLOCK_INDEX_CONDITIONS = {target_full_hp, target_no_cc}` (mirrors `_BLOCK_STRATEGIES`); unknown key **raises**. **Part-1 resolver:** `_select_blocks` resolves any conditional dict to `"default"` **unconditionally** - byte-identical to the equivalent int/list (**provably zero regression**); live predicate evaluation is **Part 2**. Server `_parse_block_index` accepts well-formed conditional bodies + skips malformed defensively (untrusted-body no-500; engine validator = hard enforcer for the trusted registry). **3 flagship seeds - all CONVERSIONS of shipped unconditional entries** (Part-1 provably no-op): **Zoe E** `{default:2,target_no_cc:0}` (live A/B 140.00 reg==forced{E:2}, 2× block-0 70.00), **Evelynn Q** `{default:5,target_no_cc:0}` (345.00==forced{Q:5}, 7.7× block-0; R:1 preserved), **Kindred E** `{default:1,target_full_hp:0}` (80.00 reg==forced{E:1}==forced{E:0} - honest full-HP no-op; the 7.5/5% missing-HP delta is Part-2's signal). Caller conditional dict via wire proven. Registry stays **125 champions** (in-place conversions). New `test_conditional_block_index_s228.py` (~52 tests) + `test_block_index_overrides` shape-pins migrated (incl. `test_every_value_*`→`_int_list_or_conditional`, mirrors s207) + 5 s223-s226 backward-compat pins updated to assert new shape + Part-1 equivalence (s223 KindredE class's 4 semantic tests pass UNCHANGED) + 9 ENGINE pin bumps. DS suite 2136→**2188**; wider RC **1107**; ruff+py_compile clean; DS restarted (taskkill 13452 → relaunch) → `/health` **1.0.0**. **NEXT (option B continues):** **Part 2** - thread real liveclient target HP%/CC into the ranking call (`coach_integration/archetype_dispatch.py` + state-builder + `/api/ds-preview`; `target_full_hp` has a clean liveclient signal, `target_no_cc` stays commits-default until a CC source exists). Plus a fast pure-data conditional seed-expansion batch (s207→s215/s217 pattern): Lux Illumination / Aatrox W chain-landed / Fiddle Q fear-state / more sleep/charm/mark amps. **DrMundo E is caster-missing-HP, not target** - needs a `caster_low_hp` vocab extension, flag for operator. s220 aggregator G Post-Game-Review reframe remains the big pending UI item.
- ✅ **s227 - DS Phase 5.9.27: max_priority audit + combo_sequence assessment (last two override registries)** (2026-05-16, ENGINE 0.98.0→0.99.0). Iteration 5 of the self-paced DS loop - applied the proven pre-filter methodology to the final two override registries. New `tools/ds_max_priority_prefilter.py` A/B'd all 6 (Q,W,E) max orderings vs the Q-W-E default for every champion. **Key finding: the numeric level-11 optimum is NOT the real in-game max order** (it flags Azir W-first, contradicting the universal Q-max; Ziggs/Ryze/Taliyah numerically reorder but really max Q which the default already does) - so `max_priority` + `combo_sequence` are **play-pattern registries, NOT numeric-sweepable pure-data batches** (bounds future autonomous work - don't auto-expand these from a pre-filter). After filtering the 59 raw flags to ds.ability/ds.burst-archetype champions AND cross-checking established meta, **3 universally-known non-default orders the registry genuinely missed**: **Brand W-E-Q** (Pillar of Flame - Brand's primary dmg+waveclear, maxed W-first for years; the original s185 _meta wrongly listed Brand as a 'default is fine' example - corrected; live A/B **+18.7%**), **Talon W-Q-E** (Rake - canonical Talon max-first; **+12.0%**), **Fiddlesticks W-E-Q** (Bountiful Harvest drain - standard jungle max; **+16.4%**). The sibling **combo_sequence** registry was assessed in the same iteration and found **ADEQUATE** for its stated reset/shadow/chain-cast purpose - the genuinely-unmapped assassin-archetype champs (Shaco/Ekko; Fizz+Katarina are already curated) have no such mechanic the default `Q-W-E-AA-R-AA` misses → a valid negative result, **no additions**. max_priority registry 12→**15 champions**. `test_max_priority_sweep_s227.py` (13 tests incl. the Azir over-flag guard + combo-adequacy negative-result guard + Fiddlesticks-carries-3-registries composition) + 8 ENGINE pin bumps. DS suite 2123→**2136**; wider RC **1107**; DS restarted → `/health` 0.99.0. **All four override registries (block_index / form_index / max_priority / combo_sequence) now swept across 5 iterations (s223-227)** - the remaining DS frontier is the conditional-target-state schema lift (architectural; the common blocker for Heimer R-upgraded / Fiddle fear-state / Skarner boulder / LeBlanc Mimic - wants operator sign-off).
- ✅ **s226 - DS Phase 5.9.26: first form_index coverage sweep since s205** (2026-05-16, ENGINE 0.97.0→0.98.0). Iteration 4 of the self-paced DS loop, pivoting the proven pre-filter methodology from the now-exhausted `block_index` space to the far-less-scrutinized `champion_form_index.json` (which form of a multi-form ability the engine evaluates; default form 0). New `tools/ds_form_index_prefilter.py` ran ground-truth A/B (forced form 0 vs each later form, full-HP + 40%-HP) over all **16 multi-DAMAGE-form (champion, key) pairs not yet in the registry** → 9 flagged, **3 clean ADDs**: **Swain R=1** (form 0 'Demonic Ascension' = 7.5-17.5 drain-channel per-tick; form 1 'Demonflare' = 150-350 + 50% AP recast nuke - Swain's ult payoff, same shape as the existing AurelionSol R=1; live A/B **12.5→250, 20×**), **Briar W=1** (form 0 'Blood Frenzy' has **zero damage blocks** - it is the AS/MS frenzy-buff cast; form 1 'Snack Attack' is the entire W damage incl. the s224-migrated 9% missing-HP; live A/B 72→129.5), **Evelynn E=1** (form 1 'Empowered Whiplash' = Eve's canonical Demon-Shade-opened combo E, 1.33× form 0 + 4% vs 3% target max HP; composes orthogonally with Evelynn's s204 block_index `{R:1,Q:5}`; live A/B 120→160). **Skipped with rationale** (registry _meta): Heimerdinger W/E (form 1 is the R-UPGRADED one-shot - R-gated; modeling W/E as upgraded over-attributes R's empower, same principle as Varus W block 2 vs R-entangled block 4 in s225 → conditional bucket), Gnar Q/E (contextual mini↔mega transform, uncontrollable Rage timing, form 0 mini is dominant-uptime), RekSai Q (contextual burrow-dance; form 0 Queen's Wrath is the sustained-combat Q), Skarner Q (form 1 Upheaval is boulder-resource-gated). form_index registry 9→**12 champions**. `test_form_index_sweep_s226.py` (10 tests incl. Briar form-0-null-damage proof + form/block_index orthogonal-composition check) + 8 ENGINE pin bumps. DS suite 2113→**2123**; wider RC **1107**; DS restarted → `/health` 0.98.0. **block_index AND form_index coverage spaces now both swept** - next DS frontier = combo_sequence/max_priority registry audits or the conditional-target-state schema lift (architectural, wants operator sign-off).
- ✅ **s225 - DS Phase 5.9.25: post-parser-fix block_index sweep - Varus W** (2026-05-16, ENGINE 0.96.0→0.97.0). Iteration 3 of the self-paced DS loop. Re-ran `tools/ds_unmapped_key_prefilter.py` over the **post-s224 snapshot** (champions whose candidate blocks evaluated ~0 pre-migration now show real ratios) → closes the iteration-2 Varus-W follow-up. **One clean ADD: Varus W=2** - "Blighted Quiver" block 2 `Bonus Magic Damage at Max Stacks` (9-15% target max HP = **exactly 3× block 1's per-Blight-stack** value, verified per-rank) is the canonical 3-stack detonation (Varus's standard combo: W-passive stacks via AAs/Q, then Q/R detonates). The engine defaulted to block 0 `Bonus Magic Damage` (the trivial 6-30 + 35% AP passive on-hit), scoring Varus W at **~7% of reality** (live A/B 18→240 raw, **13.3×**). Pattern D resource-state amp - operator fully self-controls the 3-stack build (precedent Twitch E 6-stack s198 / Renekton full Fury s197); block 2 chosen over block 4 (`Maximum…at Max Stacks` = 1.5× block 2) because block 4 entangles Varus R's Blight amplification and W must score R-independent (R is its own ability_dps key). Varus → `{Q:1, W:2}`. **Examined + deliberately skipped**: Fiddlesticks Q (s224's current-HP fix already made its engine-default block 0 correct; the "Increased" block is a fear-sequence conditional → conditional-schema bucket), Fizz W (block 0 literally "Total Magic Damage"), LeBlanc R (s197 Mimic data-gap - mimic-Q/E "Total" near-identical; canonical block not unambiguous → wants operator sign-off, not an autonomous skip-reversal). `test_post_parser_sweep_s225.py` (9 tests incl. the 3×-per-stack mechanic proof) + 6 ENGINE pin bumps + `test_block_index_overrides` Varus shape-pin update. DS suite 2104→**2113**; wider RC **1107**; DS restarted → `/health` 0.97.0. **Unmapped-key + uncovered-champion block_index spaces are now both exhausted** - next DS frontier is the conditional-target-state schema lift (architectural, wants operator sign-off) or other registry audits.
- ✅ **s224 - DS Phase 5.9.24: unmapped-key pass on covered champs - Bel'Veth R + _UNIT_TO_FIELD variant family** (2026-05-16, ENGINE 0.95.0→0.96.0). Iteration 2 of the self-paced DS loop. New `tools/ds_unmapped_key_prefilter.py` ran the s223 ground-truth A/B over every not-yet-mapped ability key of all **125 covered champions** (both full-HP and 40%-HP passes to catch execute blocks) → tight **7-item shortlist**, of which 2 are documented skips (Corki R = s194 every-4th-missile; DrMundo Q = s203 conditional-higher-vs-full-HP) and 3 were flags on already-mapped siblings. **One clean block_index ADD: Bel'Veth R=1** - "Endless Banquet" block 1 (`True Damage` 150-250 + 100% AP + 25% missing-HP) is the canonical recast nuke; the engine defaulted to block 0 (`Bonus True Damage` 6-10, the per-takedown bonus), a **56× under-count** (live A/B: 8→450 raw). This *corrects* s223's over-conservative "Bel'Veth R = no entry, already parsed" call - field-parsed ≠ engine-block-selected. Bel'Veth → `{E:2, R:1}`; the s223 test was updated to a s223→s224 evolution guard. **Part 2 (s223-sibling parser fix)**: the pre-filter exposed that `_UNIT_TO_FIELD` was missing a whole **Meraki text-drift health-unit family** - double-space `"%  of target's current health"`, `"the target's"` forms of maximum/missing health, and caster-max pronoun/name forms (`"% of his/her maximum health"`, `"% of Braum's/Zac's maximum health"`). 8 keys added; the audit-gated deterministic migration (`tools/migrate_abilities_unit_variants_s224.py`, same contract as s223, idempotent) promoted **exactly 32 modifiers across 13 champions** (Ambessa Q · Braum Q · Briar W · Fiddlesticks Q · Gnar E · Gwen Q·R · Maokai Q · Sejuani W · Skarner E · TahmKench R · Trundle R · Varus W · Zac Q) whose %HP component was silently dropped - **Trundle R and Fiddlesticks Q scored 0 entirely pre-s224** (live A/B: Trundle R 0→500, Fiddle Q 0→72). Skipped non-clean: TahmKench Q / Pantheon W `% AP per 100 bonus health` (hybrid), AurelionSol Q malformed Stardust string. `test_unit_variants_s224.py` (18 tests) + 1 shape-pin update. DS suite 2086→**2104**; wider RC **1107**; DS restarted → `/health` 0.96.0. Unmapped-key space now near-exhausted; surfaced **Varus W as a future block_index candidate** (its Blight maxHP is now parsed but the engine defaults W to block 0).
- ✅ **s223 - DS Phase 5.9.23: nested %HP parser fix + provable registry saturation** (2026-05-16, ENGINE 0.94.0→0.95.0). **Parallel-batch finding**: a 5-way parallel research-agent scan of all **47 not-yet-covered champions** (every multi-damage-block ability independently enumerated) returned **zero** clean single-int/list block_index candidates - the s191→s217 registry is *provably saturated*; the residue is single-block kits / block-0-is-already-the-single-target-max (Caitlyn Q · Ezreal R · Jinx R · Orianna Q · Yone W·R · Zeri Q·E · Zed Q) / card-choice (TwistedFate W) / turret-pet (Zyra · Yorick) / upstream data gaps (Aphelios `no_damage` · Shyvana E malformed Meraki base). Future block_index growth needs the conditional-target-state schema lift or upstream Meraki fixes, **not** more uncovered-champion scanning. **Pivot ship**: the real remaining debt was the long-deferred (s199→s217) nested-missing-HP parser bucket - root-caused to Meraki encoding %-target-health scalings behind nested conditional `(+ ...)` parentheticals that `_normalize_modifiers` left in `unparsed_modifiers`. New `tools/daemon_slayer_abilities_extract._canonicalize_unit()` strips them innermost-first (handles K'Sante's doubly-nested + Kindred E's recursively-nested forms); a deterministic **zero-re-fetch** in-place migration (`tools/migrate_abilities_nested_hp_s223.py`, hard audit-gate) promoted **exactly 22** modifiers across **10 champions** (Amumu W · Cho'Gath E×2 · Elise Q×2 both forms · Evelynn E×2 · K'Sante W×4 · Kindred W+E×2 · Kled W · Sett Q×2 · Shen Q×4 · Zac W) whose entire %HP damage component had been silently dropped since Phase 4a. Live A/B (vs 2000 HP): Cho'Gath E +250%, Zac W +200%, K'Sante W +152%, Amumu W +300%, Sett Q +40% raw. One new block_index entry: **Kindred E=1** (enhanced-execute 7.5% vs 5% missing-HP; monotone ≥ block 0, exact no-op at full HP, registry=125 champs). Kayle E / Bel'Veth R re-examined - already had `target_missing_hp_pct` parsed (s217 hand-off mischaracterized them); no entry. New `tools/ds_block_scanner.py` (filtered-index ground-truth A/B helper). DS suite 2060→**2086**; wider RC **1107**; DS server restarted → `/health` 0.95.0.
- ✅ **s222 - `/sync-all-md` skill + repo-wide md congruence pass** (2026-05-16, `014eef0` + docs). New reusable doc-sync skill (tracked canonical `tools/sync-all-md.md`; `.claude/commands/` mirror gitignored per the `done.md` pattern): canonical-facts-once reconciliation across every LIVING `.md`, locked-s207 README refresh, cross-ref-integrity scan, orphan/deprecation flagging, history-protected (WAKEUP / `_archive` / ADRs never rewritten); `--dry-run` / `commit` / `readme` args + report banner. Pass applied: ENGINE_VERSION 0.92.0→0.94.0, DS tests→2060, block_index coverage→193/124 (72%, operator-confirmed denominator), rewind→2851, README 2,022→2,060 + two-thirds→three-quarters, BRIEF 922/1426/0.69.0/2,846→canonical (BOM preserved). Dead `docs/_archive/CHANGELOG.md` (cited 3×, never existed) repointed → `docs/history_notes.md`. DAEMON_SLAYER.md Phase-3 "three implemented" → all six wired (s174-s182). 3 orphan root docs quarantined → `docs/_archive/2026-05-16-doc-sync/`. Memory index staleness flagged for `/consolidate-memory`.
- ✅ **s221 - Game-PC game-end BSOD root-cause + view-router fix + lobby settings** (2026-05-16, `a92e9e2` / `e9d921c` / `5dafe32` + docs). **BSOD**: bugcheck `0x50 PAGE_FAULT_IN_NONPAGED_AREA` ~30-50s after every match (first 2026-05-09, deterministic constant fault offset) root-caused to `tools/gamepc_screen_agent.py` PIL `ImageGrab.grab(all_screens=True)` BitBlt across the virtual desktop (spanning the Parsec + Duet **virtual display adapters**) faulting a display driver at the fullscreen-game→desktop mode switch. Replaced with **bettercam DXGI Desktop Duplication** on the single real Intel adapter (out0 game / out1 Duet; BGRA→numpy RGB, no cv2; graceful access-loss camera-rebuild); deps in `pythoncore-3.14-64`; deployed, 3 stream agents live. `tools/gamepc_boot.ps1` `tasksOwn` marker → 3 RC-ScreenAgent-* ONLOGON tasks sole launcher (no boot double-primary race; refresh loop kept). RC-ScreenAgent-* tasks repointed `py`→`pythoncore-3.14-64\pythonw.exe`+workdir (0x80070002→0x41301; Game-PC OS state, not repo). **View-router**: Home Find-Match pinned `#lobby`; `_viewResolveAndApply` skipped its stale-manual clear on ANY hash (`!hashView`) → frozen on lobby through champ-select+game; fix clears a stale *game-state* hash (non-game-state stays sticky). **Lobby settings**: new PRE-GAME LOBBY Settings card - Auto Accept (agent CONFIG source-of-truth, bidirectional, persistent) + Party default (localStorage pref, synced, no auto-apply). **Recent-5 items**: `_lcu_build_items` from `raw_data.lcu_match_detail` + DDragon mirror icon path. Memory `feedback_gamepc_screen_capture_bsod.md`. Operator to confirm post-real-match (no BSOD; auto-switch; toggle persist/sync).
- ✅ **s220 - Post Game Review: Item C/E/Settings + polish, aggregator G reframe pivot** (2026-05-16, `920c9a3`). Item C rich DDragon item tooltips on Comp tab (mirrors champ_select s213). Item E phase 1 Timeline tab (per-min gold/XP/CS diff + objective ribbon) - LCU exposes **no `/timeline` (404)** so source pivoted to server-side **Match-V5** via `core.riot_api.get_match_timeline` in `dashboard/builders._attach_match_timeline` (immutable-cached; degrades to placeholder while Riot key **403**). Settings "POST GAME REVIEW" card (rank-tier canonical home + baseline knob `?baseline=` clamped 5-50). Polish: tabs Comp/Chart/Timeline/Insights/Review, Chart contrast, hero separators, **section 3 rebuilt 4-col mirroring section 2** (selector over KDA+KP% pair), per-side roster score+MVP/SVP, CS↔summoner swap, L##→##, +1/+3 font bumps, uniform 17px gap. `tools/gamepc_lcu_agent.py` edited then **fully reverted** (no LCU timeline - don't re-add). 17 tests `tests/test_last_match_timeline.py`. Phase 2 interactive minimap deferred.
- ✅ **Post Game Review aggregator-G-style reframe S2-S5 - SHIPPED 2026-05-20 (4 stage commits + S5 follow-up; non-engine; non-frozen-files-only).** Stages of the s220 aggregator-G-style PGR reframe. S1 surrender tag `a5405ee`; S2 low-fork polish `23e36d3` (Sustain rename + hero KDA tabular + champ-name 2-line clamp + per-player augments column + cherry_augments dictionary route; 12 tests); S3 score+MVP polish `92c6a0f` (hero match-score 0-100 + per-side MVP/SVP cards + per-row chip + design tokens; 26 tests); S4 tabs reframe 4 -> 3 `d199f30` (Build/Graph/AI Analysis with legacy-tab migration + WPA above Quick Review; 15 tests); S5 Replay scaffold + Match-V5 timeline events ribbon `7870124` (`dashboard/routes_replay_events.py` + frontend ribbon + ADR-009 league_record cleanroom boundary; 39 tests); S5 follow-up participant-name join + lm-build-pending dead-id cleanup (12 tests). Full per-stage detail in CLAUDE.md items 115-118. **NOT blocked on the Riot key** (key valid + in-scope; event-mode Match-V5 403s are Riot policy not key fault). **Carry-forward (operator-gated):** per-page UI-audit ritual on the next live PGR open.
- ✅ **s219 - Post Game Review page build + LCU ingest pipeline + Phase 3 supervisor watch** (2026-05-15/16, 16 commits `8228164` → `6c0728e`). Multi-session marathon (~6 hours). **Pre-work**: salvaged PR #3 (Boots + Spellblade tuples in `_ITEM_CLASS_PEERS`, 11 tests, GitHub clean); folded Phase 3 supervisor into main RC supervisor's watch (`_Phase3Watcher` class in `ops/rc_supervisor.py` - frozen-file authorized edit, 200 lines additive, 15 unit tests, `status.json.phase3` block). **Post Game Review backend**: new `/api/last-match` + `/api/last-match/ingest` endpoints; `dashboard/builders._enrich_from_lcu` parses LCU `/lol-match-history/v1/games/{gameId}` into roster (10 players × items/runes/summoners/damage/augments) + teams (objectives/bans) + operator stats (vision/heal+shield/full damage breakdown); raw_data stash (no normalized schema per operator). **LCU agent**: `tools/gamepc_lcu_agent.py` edge-fires `/api/last-match/ingest` POST on `EndOfGame` phase transition; crash-recovery via `C:\RC-Agent\agent_state.json` persisted `last_game_id_ingested` + `_recover_missed_ingest()` one-shot on boot (covers Game-PC crash / agent offline at end-of-game). **Frontend iterations (10+ visual passes)**: rename "Last Match" → "Post Game Review" everywhere display-facing (internal view-id `last-match` preserved for test stability); hero row → 3 sections (identity cluster left, 2-row match stats middle, 2-row rank-tier compare right with Iron→Challenger dropdown + localStorage persistence); inline Vision/CS/Tanked/KP%/Damage/CS-min/Heal+Shield stats; tabbed panel (Comp / Chart / Review) replaces TEAM COMPOSITION title - Chart tab renders ally-vs-enemy aggregate bars (Kills/Deaths/Damage/Gold/Vision/Towers + SR-only Dragons/Barons/Heralds); team-comp roster row uses fully fixed-width columns (40/144/40/84/70/60/1fr) so ally + enemy panels share identical column widths; CS column right-aligned + vertically synced. **Quick Review heuristics**: SR + ARAM mode-aware team-level signals (lost first blood/tower, dragon control diff, soul/baron giveaway, tower diff, gold deficit, kill deficit) + chronic-fail proxies (deaths-above-median, grade slump). **Cosmetic**: 3 font bumps over the session (Sec 1 hero from 24/21/36 → 28/25/44 on champ/KDA/grade); top header pills (CS/Vision/etc.) hidden on this view via CSS; meta strip trimmed to mode · "X ago" · baseline-count; "Open Deep Review →" CTA moved from page-bottom button into Quick Review section title. **Bonus**: BACKLOG.md gained 4 research items (coachless.gg / DDragon mirror auto-refresh / Pengu.lol MCP / Pengu.lol Discord); DDragon mirror at `web/data/ddragon/16.8.1` cloned to `16.10.1` (12 MB, gitignored). Tests: 27 view_router_state + 6 game-mode + 4 adversarial panel snapshots all green (snapshot test required CSS-override patch to honor s219's main-hide rule on `/#last-match`). CI green after fixing 3 inherited ruff errors (F541 × 2, UP022 × 1). **Carried to next session** (operator's "/done for /clear then C+E+settings"): rich item tooltips (LoL-content via existing `lol_descriptions.js`), LCU timeline / interactive minimap port, Settings page (rank-tier persistence + baseline-count knob), 3rd font bump (still owed), Section 3 typography bump + KDA/KP swap, Insights/Review tab rename, Chart tab contrast fix, hero section dividers, augment frontend rendering (backend already ships data), grade-icon redesign (UI agent review needed), tab color-tying (UI agent review needed).

- ✅ **s218 - Home page redesign + foundation overhaul** (2026-05-15, `4518ed9`). Operator-driven multi-pass UI session capped with "this page is done now". **Foundation**: pin RC menu width at 230px static across views; +1px typography bump globally (414 declarations across 14 panel CSS files via script, 4 RC-menu rules preserved); body zoom default 1.33 → 1.0; fixed dev.js/main.js localStorage key mismatch (`rc-zoom` vs `rc-body-zoom`). **Dev/Sim Preview removal**: drop `#view-dev` section + `#sim-banner`, delete `web/js/sim.js` + `web/css/panels/dev.css` + `dashboard/routes_dev.py`, archive `data/sim/*` + `data/sim_states.json` → `docs/_archive/2026-05-15-dev-sim-removal/`, scrub orphan refs across 8+ files. **Home view content**: 7-tile quick actions (Find Match lavender-accented as primary) + Home-unique queue picker modal (centered fixed-pos + backdrop dimmer; pick fires `change_queue_type` LCU then routes to lobby); Tonight's Pick 3-section restructure (champion intro / Good-Bad-Ugly placeholder rows / Streak + Advisories sub-headers wired to `_HOME.streaks` + `#advisory-count`); Recent 5 (spell "X Minutes", CS + CS/min chip, 6-slot item strip placeholder, card click → History deep-link with pulse-highlight); This Week (KDA breakdown "1.8 22/10/18", AVG CS per game, grade-tint demoted to 3px left-border accent, list-style not card-style); backend `builders.py` extends `/api/home/summary` recent[] + this_week[] with cs/items/kda fields; hero headline drops absolute-threshold up/down classifier (always `.flat` until comparison baseline); Tonight's Pick champion name clickable → History filtered. **Footer + tooltip**: 44 → 36px height, text-faint → text-dim color, `wrapSixWords` preserves \n boundaries so multi-section tooltips render per-row. **Cleanup**: drop orphan `.home-pick-btn*` CSS + `wireAlertRow` calls + view-dev selectors. Mid-session ran independent UI audit subagent - operator picked 6 of 7 findings to apply. Tests: 27 view-router-state + 59 in focused sweep green. RC hard-restarted twice via `taskkill + restart.bat` (`restart_trigger.txt` mechanism didn't consume). Tagged-for-removal placeholders stay until backend catches up (post-match Good/Bad/Ugly analyzer + items[] ingest). Heartbeat `♥ -` in header top-right flagged - WS envelope not re-arming after RC restart; supervisor broadcast loop investigation deferred to next session.

- ✅ **s217 - DS Phase 5.9.22 sum-of-blocks data batch** (2026-05-15, `e529a40` + `2ca9717`). Second pure-data sum-of-blocks batch following s215; closes s215's carry-forward queue under the operator-commits framing. **2 entries:** Taliyah.E = [0, 2] NEW key (initial shard-impact pass-through 60-240 + 60% AP + Total Maximum Detonation aggregate 62.5-262.5 + 75% AP - operator commits to landing spell on target + target stepping through Unraveled Earth terrain; reverses s215's "likely no-op" framing since block 0 IS a distinct damage source from block 2 detonation aggregate); DrMundo.W = [1, 2] LIFT from s193's `{W: 1}` (full 4-second Heart Zapper drain channel block 1 Total Magic 80-320 + recast detonation burst block 2 Magic 20-80 - same in-batch lift pattern as s215's Thresh.E lift). Zero engine code changes; s207's schema lift handles both. ENGINE_VERSION 0.93.0 → 0.94.0. Registry stays at 124 champions; total (champion, key) entries 192 → 193. **Live A/B headlines on :8893 (/ability-dps lvl 11 vs 80 armor / 30 MR / 2000 HP via HTTP probe):** Taliyah E per-spell raw 60 → 122.50 (**+104%**, sum 60+62.5 exact); DrMundo W per-spell raw 200 → 250 (**+25%**, sum 200+50 exact). Tests: 19 new in `test_sum_of_blocks_expansion_s217.py` (RegistrySeedTests 3 / AbilityDpsTests 6 / BackwardCompatTests 9 / EngineVersionTests 1); 3 ENGINE_VERSION pin bumps; 3 existing assertions in `test_block_index_overrides.py` updated (DrMundo + Taliyah shape pins + `test_drmundo_W_routes_to_sum_of_blocks` rename replacing int-only `_delta_check`). DS suite 2041 → 2060 (+19); wider RC `tests/` 953 green post-DS-restart; full project discovery 3013 green. DS server restarted via `Start-Process pythonw` - listener pid 19128; /health confirms engine_version 0.94.0, patch 16.10.1, 172 champions, 705 items. **Investigation outcomes:** Confirmed Jinx R deferral correct (block 0 IS canonical primary-target max at max-distance + missing-HP; blocks 2-3 are secondary AOE); Kindred E / Kayle E / Belveth R remain in nested-missing-HP parser bucket. **Sum-of-blocks bucket queue now exhausted under current operator-commit framing.** Carried forward: (a) Conditional target-state schema lift (6+ candidates queued: Lux Illumination, DrMundo E missing-HP, Renekton Q conditional Fury model, Zed shadow Q empowered, Aphelios weapon-form conditionals - schema lift `block_index: int | list[int] | dict[str, int]`); (b) Nested missing-HP parser bucket (Kindred E / Kayle E / Belveth R - upstream Phase 4a parser work); (c) Per-form block_index (Heimerdinger.W form 1 Upgrade!!! 20-rocket model - schema lift, per-form dict); (d) Aphelios Q forms (Meraki bulk parses all 6 weapon-form variants as no_damage - upstream data-quality fix). **Live ARAM Mayhem test still pending** post-/clear (carried s214→s215→s216→s217).
- ✅ **s214 v2 - Retire Brawl mode from champ-select view** (2026-05-15, `85fc157`). Brawl removed from League rotation; champ-select branches stripped from `_csvDetectMode` + `_csvDsModeFor` + modeLabel + buildsTitle chains; ally/enemy renderers no longer test `mode === "brawl"`; 5 CSS data-cs-mode=brawl rules dropped; `flow_03d_brawl_select.json` deleted; comments updated. Net diff +44 / −135 across 5 files. **Carried forward**: legacy `coaches/brawl_coach.py` + brawl detection across 6 dashboard files left as deadcode (separate mechanical cleanup pass).
- ✅ **s215 - Build chooser auto-generator + DS Phase 5.9.21 sum-of-blocks data batch** (2026-05-15, `b395032` + `77d716c`). **Part A (loadouts):** new `tools/champion_loadout_autogen.py` emits up to 3 algorithmic build variants per (champion, mode) into `data/champion_loadouts.json` via `rank_for_primary_archetype` per archetype scorer. Hand-curated wins: each (champion, mode) counts curated variants with that mode in `modes[]`; only fills to 3 with auto entries when count < 3. Stable keys `auto-<mode>-<slot>-<archetype>` so reruns refresh in place without bloat. Runes mirror `_CSV_EXPERIMENTAL_RUNES` from `champ_select.js` (canonical archetype → keystone+trees consensus); summoners archetype-keyed for SR (carries Flash+Heal, bruisers/tanks Flash+TP, mages/assassins Flash+Ignite, enchanters Flash+Exhaust) + mode-fixed for ARAM (Flash+Mark) / Arena (Flash+Heal); items are top-6 from DS engine at level 11 mid-game. Full 172-champ run: 13.7s, **1017 auto entries added** (332 SR + 169 ARAM + 516 Arena), 0 unfilled. File grows 6k → 36k lines (920 KB). 35 new tests in `tests/test_champion_loadout_autogen.py` via DS-stub mocks (key shape, summoner mapping, archetype triplet resolver, merge policy, atomic write sorting). Vayne (3+3 curated → only Arena gets auto), Lulu (`support-enchanter` modes=[sr,aram] covers both → 2 SR auto + 0 ARAM auto + 3 Arena auto), Veigar (1 SR + 2 ARAM curated → 2 SR auto + 1 ARAM auto + 3 Arena auto) spot-verified preserved. **Part B (DS Phase 5.9.21):** first pure-data batch consuming s207's sum-of-blocks schema lift. 4 entries: Thresh E=[1,2] (Maximum Bonus Magic at full Souls 0+90-210%tAD + canonical Magic Damage 75-255+70%AP - lifts s203's single-int {E:2}, **first int→list lift since s207 seed**), Sona Q=[0,1] (active Magic 50-190+40%AP + Power Chord empowered AA bonus 10-30 base; routes to ds.hps by default), Kalista E=[0,1,1,1,1] (base Rend 5-45+70%tAD+20%AP + 4× additional stacks = 5-stack model, same as Heimerdinger W rocket-count s207), Malzahar R=[0,2] (Total Magic suppression channel 125-275+80%AP + Total target-max-HP% bonus 10-20% from full Nether Grasp duration). Registry 121 → 124 champions. Zero engine code changes - s191/s192 walker logic + s207's list handler in `_select_blocks` handle all entries transparently. ENGINE_VERSION 0.92.0 → 0.93.0. DS server restarted live to pid pinned at /health=0.93.0. **Live A/B headlines on :8893 (/ability-dps lvl 11 vs 80 armor / 30 MR / 2000 HP):** Malzahar R per-spell raw 200→500 (**+150.0%**, +9.8% total - 110s CD dilutes per-second contribution), Kalista E per-spell raw 67.65→167.25 (**+147.2%**, +36.6% total - E is big chunk of Kalista's ability_dps), Thresh E per-spell raw 75→145.20 (**+93.6%**, +9.9% total - E was small fraction of 7.21), Sona Q per-spell raw 190→220 (**+15.8%**, +9.8% total - Power Chord block has unparsed `% of Sona's AP` Phase 4a parser can't extract yet). All 4 verified per-rank against Meraki 16.10.1 snapshot. Tests: 19 new in `agents/daemon_slayer/tests/test_sum_of_blocks_expansion_s215.py` (registry shape 4 + arithmetic parity sum==sum-of-singletons 4 + strict-greater A/B guards 4 + backward-compat for s207 seed entries 4 + Thresh int→list lift assertion 1 + ENGINE_VERSION pin 1 + unmapped-champion guard 1) + 4 prior-batch tests updated for new Thresh shape (test_known_champion_overrides + test_thresh_E_routes_to_block_2 + test_pre_s204_thresh_unchanged + test_pre_s205_thresh_unchanged) + 2 ENGINE_VERSION pin bumps in test_effects_expansion. DS suite 2022 → 2041 (+19); wider RC 1024 → 1064 (+40 = 35 new autogen + 5 noise). Pushed `064e9f3..77d716c main -> main`. **Carried forward:** (a) Live ARAM Mayhem test scheduled by operator post-/clear (now also validates 3-variant build chooser pushing correct runes/items/spells to LCU + Phase 5.9.21 lifts riding through `/api/ds-preview`). (b) DS Phase 5.9.22+ sum-of-blocks candidates remaining: Taliyah E (block 2 'Total Maximum Detonation' already aggregates - likely no-op), Jinx R secondary AOE (not single-target), Kindred E / Kayle E / Belveth R execute curve (nested missing-HP parser bucket - Phase 4a parser limitation, separate schema lift). (c) Loadout autogen calibration - operator may want to flip some `default_per_mode` pointers post-Mayhem if a curated default loses to the auto-primary on live ARAM rounds. No code work - pure JSON edit; autogen preserves operator's manual default choices on re-run.
- 🟡 **Live ARAM Mayhem test (carried s214 + s215)** - operator scheduled post-/clear. Authorized to monitor + fix inconsistencies live without permission. Validates: build-chooser pushes correctly to LCU runes/items/spells (now 3-variant + Experimental row), archetype-mismatch nudge surfacing on first non-trivial item buy, comp-aware row-3 tip rendering for MAYHEM role label, Phase 5.9.21 Thresh/Sona/Kalista/Malzahar ability_dps lifts riding through DS preview.
- ✅ **s214 - Champ-select s213 carry-forward + UI audit batch** (2026-05-15, `f31bc1b`). Closed the 11-item s213 carry-forward list in one batch + ran a UI audit subagent + iterated on live operator follow-ups. **UI cleanup:** dropped CURRENT BANS title row from Suggestions; removed per-cell countdown timers + ticker interval from allies/enemies; My Pick portrait re-laid as 3-col grid `[1fr | auto icon | 1fr]` so icon sits at exact panel center with state/name flanks not influencing it; build-chooser keystone strip pinned to 130px width with a 2-row left-aligned name slot (always reserves 2 lines for uniform vertical rhythm across all 4 rows); hover hit-areas expanded on champion-name cells + build-item icons; unified keystone tooltip (bare `title` removed so wrapper's `data-tt-html` rich tooltip fires on either icon or name); champ-select grid column-gap tightened 12→4px; **zero scrollbars policy** - `.csv-card-body` overflow auto→hidden. **Wiring:** archetype-button click invalidates per-archetype DS cache + passes `archetype` to `/api/ds-preview` so experimental build row re-fetches with the new scorer; pick-click safety gate retightened to honor `cs.active_round.type === "ban"` rather than coarse `cs.phase`. **Pick & Ban backend (`routes_pickban.py`):** query helpers refactored to return `list[dict]` with `exclude_ids` + `top` params for cascade filtering; new `allies=` query param for synergy mood (joint-WR-with-locked-allies SQL when ally ids are provided; falls back to recent-form proxy otherwise); response gains `performance_picks` list alongside legacy single `performance` dict for back-compat. **Pick & Ban frontend:** COMFORT keeps legacy perf/mastery/meta layout; LIMIT/NEW/SYNERGY render 3 same-mood cascade-filtered rows; client-side exclude set folds ally + enemy bans + locked picks + hover intents; reason text clamped to 3 lines via CSS; pick rows `flex: 1 1 0` for uniform height. **Suggestion tips comp-aware:** new `_csvCompAwareTip()` reads `championTags` for locked allies + visible enemies, derives 6-class counts, role-conditionally swaps row-3 tip (BOT/SUP/TOP/JNG/MID/ARAM-Mayhem). **Audit follow-ups:** pick-order tips 44px min-height for uniform rhythm; enemy 100%-confidence pills dimmed 0.55 to free eye-line attention budget for is-med/is-low rows. **BACKLOG:** Interactive Item Shaper (post-DS-100%) recorded - 3 modifiers DAMAGE/SURVIVABILITY/UTILITY nudge active archetype scorer weights mid-game; resets per match. **Tests:** 5 new s214 cases in `TestS214CascadeAndMultiPick` covering top-N + exclude_ids + synergy ally_ids + parse_csv_ints. 22 pickban tests pass (was 17); 1018 wider RC pass; 11 panel snapshots green; view-router 25 green. Net diff +947 / −280 across 6 files. **Live ARAM Mayhem test pending** - operator scheduled post-/clear; authorized to monitor + fix live without permission.
- ✅ **s209-s213 - Champ-select full view redesign** (2026-05-15, `f1ca81e`). Iterative ~14-round overhaul replacing the s208 retire-the-overlay scaffold with a production-grade draft surface. Grid reshape (Enemies row 1 + new Suggestions row 2 right); My Pick header dropped + portrait/name/LOCKED inline to free a 4th build chooser row; **loading view retired entirely** (GameStart routes direct to active-match). **Build chooser**: 3 curated + Experimental auto-row, 2-row card with colored badge labels (cyan/red/violet/amber), 42px items, stacked summoner spells; click pushes runes+items+summoners via new `override_runes`/`override_items` payload fields on `/api/loadout/apply`. **Mood toggle wired**: Comfort/Limit/New/Synergy branches the performance query in `routes_pickban.py` + drives champion→counters lookup (`data/meta/champion_counters.json`) so bans flip with the recommended pick. **Suggestions panel** (new, row 2 right, phase-aware): 4 ban suggestions during ban phase (from `data/meta/global_top_bans.json`), 2×5 banned grid post-ban-phase. **Adaptive summoners**: new `/api/champ-select/adaptive-summoners` swaps Heal→Cleanse (CC≥4/10) or Heal→Barrier (burst≥6/10) for ADC-style roles. **Rich tooltips**: new `web/js/lib/lol_descriptions.js` + `/api/dictionary/items`+`/api/dictionary/runes` endpoints, cleaned LoL HTML into the existing `data-tt-html` app-tooltip surface - item icons show name+gold+stats+passive/active; keystones show name+tree+shortDesc. **Enemies panel**: lock 🔒 removed, "(guess)" replaced with 2-piece tag chips (CC/BURST/AD/AP × TANK/BRUISER/ASSASSIN/SUPPORT/ADC/MAGE/FLEX from `/api/dictionary/champion-tags`) + role-confidence pill (100%/85%/50%). **Allies panel**: self-row position pip gold→indigo to match the `.me` cell border. **/api/mains icons fix**: switched 404'ing `/data/ddragon/...` paths to `/icons/champions/<slug>.png`. **`IMPLEMENTED_SCORERS` flipped to all 6** (s174-s181 archetype plan was 100% but the flag stayed at 3). **UI audit ritual run mid-session**: subagent dispatched against the fullscreened dashboard; 5 must-fix closed + 2 consider closed + 1 explicitly kept (6-button grid). Net diff +2181 / −1368 across 26 files. 11/11 panel snapshots + 27/27 view-router tests green. **Carried forward to next session:** drop "CURRENT BANS" title row, keystone vertical alignment fix, experimental row updates on archetype-change (add archetype to DS cache key), increase hover hit-area, Pick & Ban filter constraints (hide already-banned/picked/unavailable in mode pool), LIMIT/NEW rows 2+3 follow row 1's heuristic, SYNERGY needs team-comp aware lookup, remove countdown timers (allies+enemies), validate ranked-vs-normal-draft ban timing, move LOCKED state centered below portrait, tips text incorporates role+ally-comp+enemy-comp. **Future feature (defer until DS 100% champion coverage):** interactive Item Shaper with 3 modifiers (DAMAGE/SURVIVABILITY/UTILITY) - operator's idea.
- ✅ **s208 - Retire legacy `#cs-overlay` champ-select page** (2026-05-14, `2eff521`). Operator complained the OLD champ-select page flashed first when CS opened (since s164 the new full-page `view-champ-select` view became default-on but the floating `#cs-overlay` overlay was still rendered unconditionally on every LCU envelope). Retired the entire old surface: `<div id="cs-overlay">` block + `renderChampSelectPanel()` (~280 LOC) + `_fetchDsPreview()` + `champSelectViewEnabled()` gate + force-Flash-Snowball override + ARAM team-comp analyzer + cs-lock/reroll button wiring (~624 LOC total in `champ_select.js`) + entire `web/css/panels/champ_select.css` stylesheet (365 LOC). Routing collapsed in both `web/js/main.js` and `dashboard/view_router_state.py` - `phase === "ChampSelect"` now always routes to `champ-select` view, no opt-out fallback. **Bonus fix:** removing the force-Flash-Snowball override closes operator's "meta rune and spell choices not forwarding" complaint - variant summoners now push to LCU on every build pick (previously the toggle was clobbering them with hardcoded `set_summoners {d:4, f:32}`). Tests: removed 2 `ChampSelectViewGateTests` opt-out cases + replaced with positive `ChampSelectViewTests.test_cs_phase_routes_to_champ_select`. 911 RC + 25 view-router + 11 panel snapshots green. RC restarted (pid 7484, `last_reload_ok=true`); asset hash flipped to `5208321bea`; `curl /` returns 0 cs-overlay, 1 view-champ-select. Net diff −1137 lines across 9 files (1 deleted). **Carried forward (spawn_task chip):** orphan helper cleanup in `champ_select.js` - `_csOnChampionOrModeChange`, `_csRenderBuildList`, `_csOnBuildRowClick`, `_csApplyLoadout`, `_csMarkSelectedRow`, plus entire `_srDraft*` cluster all have zero callers. Left in place because `_csDiffItemIds` has a downstream caller in `item_build.js:327` that doesn't import it (pre-existing latent ReferenceError bug); chain-delete blocked on fixing that.
- ✅ **TFT 17.3 patch** - shipped 0e9617b (2026-05-08). Morgana 4g, Anima/Stargazer reworks, Primordian AVOID, AP comps buffed.
- ✅ **Riot API key policy reversal** - ADR-006 shipped (s145, 2026-05-09). Personal-tier key permitted for full-team champ-select context + post-game review. Live in-game advisory stays LCU/LiveClient-only per Riot ToS. Implementation tickets: FU02 + FU04 (next).
- ✅ **FU04 - Riot Personal-tier API key application** - submitted + approved same-day on 2026-05-09 (App ID 834837, well inside the documented 2-6 week window). f1c8b10. Bundle preserved at `Desktop/FU04-Application-Evidence/`; key staged in `API-Key-Riot.txt` (gitignored on Legion only).
- ✅ **FU02 - `core/riot_api.py` + champ-select team-context fan-out** - shipped s148 (`dfa13f0`). Personal-tier key resolver, dual token bucket (20/s + 100/120s + 429 cooldown), SQLite cache (immutable + 5-min TTL), six endpoint wrappers (Account-V1, Match-V5 ids/detail/timeline, League-V4, Mastery-V4), priority-1/priority-2 fan-out via daemon thread with swappable dispatcher, progressive reveal, backend ranked-name-blanking. `/metrics` exposes `rc_riot_api_calls_total{endpoint,outcome}`. 63 new tests, 565 total. **Last mile remaining:** Game-PC `tools/gamepc_lcu_agent.py` doesn't yet POST to `/api/team-context/refresh` on `ChampSelect` transition - manual roster posts work; LCU agent extension is independent.
- ✅ **FU01 - minimap-locate** - shipped s168 (2026-05-11). New `agents/_minimap_bbox.py` resolver + `agents/supervisor.py:597` rewire. 3-path resolution: HTTP `?bbox=` override → `data/vision_regions.json` `_minimap_<mode>` key (NEW persisted layer, underscore-prefixed so `core/vision_tesseract._regions()` filters it out of OCR namespace) → hardcoded 1920×1080 fallback. Bbox shape validated (4 ints + `r>l` + `b>t`); malformed entries silently fall back. 19 new tests under `tests/fu01_minimap/`. Behavior byte-identical until calibration entries are written - see WAKEUP_NOTES s168 for the calibration recipe. Phase 3 supervisor restart pending.
- ✅ **LCU agent → team-context refresh wiring** - shipped s149 (`e7b5af1`). Game-PC `tools/gamepc_lcu_agent.py` POSTs roster (with PUUIDs) to `https://192.168.8.230:8888/api/team-context/refresh` on ChampSelect entry + on lock/swap. Bridge-secret resolver (env → `bridge_secret.txt` → `local_paths.json`); LCU champion-id→name cache; edge-trigger w/ 3s repost rate-limit. 30 new tests, 595 total. Game-PC deployed live (RC-LCU PID 16080); resolver self-test confirmed. Live verification awaits next CS pop.
- ✅ **DS surfacing - header pill + ARAM Build-row fallback + match-record wire** - shipped s152 (`091d18c`). Glanceable `#ds-pill` in header row 2 (info-blue, ◆ glyph, mode-gated), Next panel ARAM/Brawl `Build` row falls back to `DS: <name> +Ndps` when coach hasn't emitted `item_extra`/`objective` (coach copy still wins), and `performance_tracker.save_rating` folds the engine's last DS pick set into `matches.raw_data["daemon_slayer_picks"]` so calibration analysis loses the JSONL ⨝ on (champion, mode, ~ts). 8 new tests, 624 total. Smoketested live: pill rendering `Stormrazor +54dps` from real coach state.
- ✅ **Mode-flicker chain + DS resolver + LCU agent fixes** - shipped s153-s158 (2026-05-10). s153 (`96bf4ee`) + s157 (`0e3d87a`) close the SR-lobby pre-flip flicker by mirroring into both HTTP `/api/state` and WS push paths via shared `resolve_mode_key` + `apply_preflip_mirror` helpers in `dashboard/_state_builder.py`. s154 (`3a3bf58`) Game-PC LCU agent falls back to `/lol-gameflow/v1/session.gameData.queue.id` when CS-session omits `gameData` during BAN_PICK. s155 (`5c39b9b`) `lock_pick` race-tolerance + UI failure feedback. s156 (`6c4a940`) DS server display-name reverse map (Kai'Sa→Kaisa, Wukong→MonkeyKing, Renata Glasc→Renata, full apostrophe family) + new `resolve_inventory` dropping trinkets/consumables/elixirs from `/rank` current_item_ids; SR coach uses it. s158 (`bd0c88e`) `?dbg=1` mode/view transition log + floating debug overlay. 15 new tests; live-verified `daemon_slayer_picks=5` mid-Kai'Sa.
- ✅ **Pre-Game Lobby v2 redesign + handleChampSelect bug fix** - shipped s162 (`5c019f9`, 2026-05-10). Phase 3 step 1 of the 14-fixture game-flow build. Rebuilt QUEUE / Mains (YOUR + PARTY tabs) / Party / "My Top 8" panels with unified 15px typography, color palette per member (`data/sim/flow_01_lobby_solo.json`). `handleChampSelect` ReferenceError chain closed (cross-module calls were silently failing in SSE try/catch → lobby never re-rendered post-refresh); orchestration moved to new `handleLcuEnvelope` wrapper in `main.js` (4 call sites). Phase 1 + 2 prep: removed 5 live menu views (Loadouts/Diagnostics/Coach Calls/Bridge Pending/Fleet), slimmed dev page, sim DEV banner → corner pill, archived 46 prior fixtures, EventSource stub in sim mode. Doc baseline: Edge / 1920×1280 → Chrome windowed / 1920×1080.
- ✅ **Pre-Game Lobby v3 polish + conflict UI + AVG/Match grade** - shipped s163 (`e316291`, 2026-05-10). Phase 3 step 1 LOCKED for both solo + multi-member states (placeholder-driven 5-slot renderer; no separate flow_02 fixture pass needed). Visual-hierarchy audit subagent ran mid-session; 5 must-fix items shipped. Self-row mirror (operator's lane prefs col 2 + DB-assessed role pip col 3); Primary-lane CONFLICT detection (red outline + diagonal CONFLICT overlay when self clashes; orange when two non-self members clash; cleared on resolution); AVG 5 grade letter (S/A/B/C/D, KP%-style 5-tier color band) - Gold dropped, Vision moved up; Mains Overall 2x2 grid; League captain-icon-crown PNG (CommunityDragon mirror) + Phosphor copy SVG; LVL ### : Unranked fallback in MY TOP 8; JGL→JNG / SUPP→SUP role normalizer; font floors 9→13px; drop shadow on tooltips/mode menu; page fits 1080-viewport without scrollbar. 913 ins / 181 del across 7 files.
- ✅ **Phase 3 step 2 + step 3 - Champ Select view scaffold + Pick&Ban panel + trade popup** - shipped s164 (`c0e6043`, 2026-05-10). Step 2 (`flow_02_lobby_with_others.json`) was a renaming pass; step 3 built the new top-level `view-champ-select` page from scratch. 3-col grid (Allies + Pick&Ban left, My Pick + Builds center, Enemies right); auto-promotes on `?cs=1` + `phase=ChampSelect`. Pick&Ban Recommendations panel: 3 source rows (perf/mastery/meta) + 4-button mood toggle (Comfort/Limit/New/Comp Synergy) + quick-select with lock-after-click (gated to skip BAN_PICK so user can't accidentally ban their own pick). Trade popup: SWAP/TRADE header + CHAMPION/Nth-PICK/ROLE buttons appended to `<html>` to bypass body's `zoom:1.33` with matching `transform:scale`. Enemy "(guess)" tag + cyan timer (no "s" suffix) + 2px round-border (red ban / gold pick). LED-dot animation explored + removed (didn't render reliably). 1986 ins / 4 del across 10 files (3 new: flow_02 fixture, flow_03 fixture, champ_select_view.css).
- ✅ **Phase B - LCU agent handlers + champ-select state population** - shipped s166 (2026-05-10). 5 new command handlers in `tools/gamepc_lcu_agent.py` (set_ban_intent / set_pick_intent / set_augment_intent stub / request_position_swap / request_pick_order_swap) + new champ-select state fields (active_round / is_brawl / position_swaps / pick_order_swaps / arena_teams / augments scaffold) + per-player summoners on `_team_picks`. 30 new tests under `tests/phase_b_champ_select/` (665 total green). set_augment_intent returns explicit "unsupported" until `/lol-cherry/v1/*` endpoint can be confirmed against a live Arena lobby.
- ✅ **Phase 3 step 4 - Loading Screen view scaffold** - shipped s166 (2026-05-10). New top-level `view-loading` with mode-conditional team panels (sr/aram/arena/brawl), `?ld=1` opt-in, `phase=GameStart` auto-promote. Briefing card removed before commit per operator (defer copy iteration to a later session). New files: `web/js/panels/loading.js`, `web/css/panels/loading_view.css`, `data/sim/flow_04_loading_screen.json`. Backend population paths for the new view land via the Phase B `_team_picks` summoners change above.
- ✅ **Phase 3 step 3 follow-ups - central panel + enemies redesign for ALL game modes** - shipped s165 (`91a42e1`, 2026-05-10). New `_csvDetectMode(cs)` returns `sr|aram|arena|brawl` from queue_id + is_aram/is_brawl flags; `section.dataset.csMode` stamped on render; CSS branches via `[data-cs-mode]`. SR: 3-variant Build Chooser populated (was empty placeholder). ARAM: 5-cell horizontal Bench (`bench_swap` wired) + ARAM Build Chooser. Arena: 2-cell allies, central pane swaps to Duo+Augments (3 slots silver/gold/prismatic + options w/ `set_augment_intent`), enemies = 3 sub-team cards. Brawl: 5v5 no roles/bans, Brawl Build Chooser. Pick&Ban + Allies panels untouched (LOCKED per operator). 3 new fixtures (flow_03b/c/d). Phase B follow-ups merged into the LCU agent ticket: handlers for `set_augment_intent` + populate `cs.arena_teams`/`cs.augments`/`cs.is_brawl`; real build variants from `/api/loadout/list`.
- 🟡 **Phase 3 fixture flow (steps 5-14)** - remaining canonical fixtures: champ-select 1st/5th/locked variants, active match @ 0/6/12/20/30/40 min, game summary (NEW view), last match. **PAUSED per operator 2026-05-10** while backend work catches up. Step 4 (Loading Screen) shipped in s166. Per `feedback_phase3_fixture_ritual.md` - each fixture begins with visual-hierarchy audit subagent, ends with `/done` + `/clear`.
- 🟡 **Active Match view (step 5)** - steps 1-4 shipped. Step 4 shipped s171 (`73b66ec`, 2026-05-12): static SR/ARAM map base from `/data/ddragon/16.8.1/img/map/*` + champion-dot canvas overlay polled from `/api/vision-state` at 500ms + MIA badges (`missing_for_s > 12`) + JG-gank warning band (enemy MIA > 20s in ENEMY_JUNGLE/RIVER) + view-gated polling. **Remaining (3):** zen-lock in-game + RIGHT NOW fold + bridge-pending -> dev-panel button. **Fleet view removal SHIPPED 2026-05-19** (overnight headless): dead JS poll of `/api/health/peer` (~158 LOC in `web/js/main.js`) + ~99 LOC fleet CSS in `bridge_pending.css` + 3 `body[data-view="fleet"]` selectors in `header.css` stripped. Backend `/api/health/peer` + `/api/health/all` rollup kept (other consumers). Snapshot tests 13/13 green.
- ✅ **ADR-007 event-coach pivot - phase 1** - shipped s169 (2026-05-11). Architectural pivot from continuous narration to event-driven decision forks. `core/decision_detector.py` extended with 2 new detectors (`detect_jungler_gank_likely`: enemy JG missing 20s+ outside own jungle; `detect_throwing_lead`: 2 self-deaths within 45s post-8min) + tightened `detect_low_hp_backable` (40%/45s → 25%/90s thresholds). Heartbeat counter (file-backed at `data/decisions_heartbeat.json`) + `GET /api/decisions/heartbeat` + new `POST /api/decisions/respond_active` (keybind-friendly endpoint that maps `choice_index 0|1` → first pending decision's options). Dashboard `#trigger-pill` in header row 2 (green/amber/grey alive indicator + per-match eval counter). Game-PC keybind listener (`tools/gamepc_keybind_listener.py`, Numpad 1/2/0; install-only). 25 new tests; 719 total. ADR at `docs/adr/ADR-007-event-coach-pivot.md`.
- ✅ **ADR-007 phase 2 - postmortem from rewind_history.db** - SHIPPED 2026-05-21 (see top SHIPPED entry above). 6 pattern classifiers + Laplace-smoothed top-3 -> appended to 6 active coach system prompts as PERSONAL CONTEXT. Live: 28,236 deaths / 2,838 matches; top-3 = solo_pickoff / caught_4plus / rapid_repeat.
- 🟡 **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- ✅ **DS per-level DPS curve helper** - shipped s167 (`1522b90`, 2026-05-11). `compute_dps_curve()` + `DpsCurvePoint` + `DPS_CURVE_LEVELS=(1,6,11,16,18)` in `agents/daemon_slayer/dps.py`. Pure additive (no engine math change); ENGINE_VERSION 0.60.0 → 0.61.0. 12 new tests (35 total in test_dps; 949 total in DS suite). Unlocks coach hints like "Lulu peaks at lvl 6".
- ✅ **rewind_history.db catchup** - shipped s167 (`1522b90`, 2026-05-11). `scripts/rewind_catchup.py` hydrates Match-V5 detail + timeline into the existing 5-table schema. `core/riot_api.get_recent_matches` extended with `start`/`startTime`/`endTime`/`queue`/`type` params. Auto-rotates PUUID via Account-V1 when DB PUUID is stale. Idempotent + resumable via `data/rewind_catchup.state.json`. Live: 2846 → 2851 matches (operator's recent activity sparse - 5 games since Dec 2025).
- ✅ **LCU mastery wired into LCU agent** - shipped s167 (`1522b90`, 2026-05-11). `tools/gamepc_lcu_agent.py` adds `_resolve_local_summoner_id` + `_maybe_refresh_mastery` (5-min TTL); surfaced at `state["lcu"]["mastery"]` on Lobby/CS/InGame phases. 10 new tests (`tests/phase_b_champ_select/test_lcu_mastery.py`). **Last mile:** Game-PC redeploy of `tools/gamepc_lcu_agent.py` needed before mastery field appears live.
- ✅ **API surface audit** - shipped s167 (`1522b90`, 2026-05-11). `scripts/audit_api_surface.py` greps source for endpoint literals; `docs/API_SURFACE_AUDIT.md` rendered. Counts: 89 distinct internal `/api/*` (322 callsites), 70 LCU `/lol-*` (215 callsites), 6 Riot Web (7 callsites), 2 LiveClient (13 callsites). Cross-reference list for future endpoint plumbing.
- ✅ **Synthetic match pruning** - shipped s167 (`1522b90`, 2026-05-11). `scripts/prune_synthetic_matches.py` removed 56 dev-test rows (41 "Dark Star Vertical" + 15 empty-champion zero-duration) from `data/match_history.db`. `data/ds_calibration.jsonl` clean. Backup: `data/match_history.db.bak-2026-05-10-prune-synthetic`.
- ✅ **LCU wiring punch list - pre-game lobby + champ-select + DS active match** - shipped s170 (`85461e2`, 2026-05-11). Six of seven items closed: (#1) `state.lcu.lobby.members[]` + `local_member` + `is_leader` + `party_type` + `queue_name` + `search_state` forwarder in `tools/gamepc_lcu_agent.py`, with is_self via summoner-id match + name enrichment via `/lol-summoner/v1/summoners/{sid}`, 31 tests; (#2) DS enemy-stats helper (`coach_integration/enemy_stats.py`, 27 tests); (#3) Active Match per-tick DS rerank with CommunityDragon icons; (#4) `GET /api/champ-select/pickban-recs` route (`dashboard/routes_pickban.py`) querying `rewind_history.db`, 17 tests, 33ms live response; (#5) RC-LCU scheduled-task action path fix via bridge dispatch (Game-PC Claude resolved); (#7) Top 8 dummy purge. 788 total green (was 719).
- 🟡 **set_augment_intent Cherry endpoint discovery** (s170 punch list item #6) - Game-PC `tools/gamepc_lcu_agent.py:777` returns `augment_intent_unsupported` stub. Endpoint discovery requires a live Arena lobby - dispatch via `bridge_cli.py task --target gamepc` with the lockfile-auth path-enumeration pattern from s168 when next Arena queue pops. **2026-05-17 UPDATE (live Mayhem/KIWI, rigorous AT-picker-open probe - `Game-PC C:\RC-Agent\rc_augment_probe.py`):** the capture-free **API path is RESOLVED-NEGATIVE** - no `/lol-cherry/*` or `/lol-game-augments/*` route (all 404 mid-game, picker open), no `:2999` augment field (the "augment" substring is i18n tooltip noise). So augment data **must come from screen OCR**, not an API. Companion finding: on-demand `mcp__gamepc__capture_monitor` **survived a full Mayhem match incl. match-end with no BSOD (1 match - promising, NOT yet confirmed; needs ≥1-2 more)** - distinct mechanism from the disabled continuous screen-agent. Next augment step = confirm MCP-capture safety over 1-2 more matches, then build a picker-frame→OCR path (Arena/CHERRY queue-1700 still a small separate API re-check). Detail + caveats in memory `feedback_gamepc_screen_capture_bsod.md`.
- ✅ **s171 LCU + DS + UX bug crawl** - 8 commits 2026-05-12 (`73b66ec`→`d12a330`). (a) Restored `lcuCmd`/`lcuPollResult` to `main.js` module scope - 22 callsites had silently thrown ReferenceError since the s133 Phase 3.1 ESM split (38ac760); all lobby buttons (Find Match / Cancel / queue-change / party toggle / lane prefs / invite / promote / kick) now work end-to-end. (b) **6 new lobby LCU handlers** (set_party_type / set_position_prefs / invite_player / create_practice_tool / promote_leader / kick_member) + **5 P&B commands added to allowlist** (set_pick_intent / set_ban_intent / request_position_swap / request_pick_order_swap / set_augment_intent) - all silently 400-rejected since s166. (c) **Active-match view** opt-in→opt-out, `coach.immediate` renders as RIGHT NOW, freshness guard between games, **step 4 map overlay** (see active-match item). (d) **Enemy-aware DS ranking** - new `core/enemy_aware_stats.py` reads liveclient `allPlayers[i].items[]`, computes target_armor/_mr/_max_hp via ddragon_items.json stat lookup, passes to `rank_for()`. (e) **Defensive-pick ranker** - new `core/defensive_picks.py` classifies enemy threat profile (`_KNOWN_BURSTERS` + DDragon info.attack/magic/defense) + 22-item curated catalog (Maw/Sterak/GA/Plated/Randuin/etc.); DEFENSE row in BUILD pane when `burst≥5 OR ad≥7 OR ap≥7`. (f) **`/api/top8` + `/api/mains` backend** (`dashboard/routes_lobby_aux.py`) - Top 8 server-side persistence (was localStorage, origin-dependent) + top-N main champs from `rewind_history.db.participants` + LCU mastery overlay. (g) **`my_completed` from `sess.actions[][]`** - was reading non-existent `myTeam[i].completed`, always False; now derived from local cell's pick action. **`local_cell`** also surfaced so P&B role-resolver works. (h) **championPickIntent hover fallback** + **auto_accept default flipped True→False**. (i) **Post-CS view-routing sticky guard** (`_VIEW.gameStarted`) rides through transient phase=null/Lobby during CS→loading→game flip. (j) **`champSelectViewEnabled` opt-in→opt-out** (s171.7) so ARAM Mayhem operator sees the new view without `?cs=1`.
- ✅ **s171.8 view-router + cache-bust unification** - 6 commits 2026-05-12 (`eba274b`→`876fd01`). (a) **4 ruff fixes** including F601 dup `local_cell` key in `gamepc_lcu_agent.py` silently overwriting defensive coercion. (b) **FU01 audit-6 ship**: `parse_http_override` helper validates `?bbox=` against r>l/b>t/coord-range; 6 new tests; gitignored `top8_list.json` + `decisions_heartbeat.json`. (c) **CI Node.js 24 bump** (`checkout@v4→v6` + `setup-python@v5→v6`). (d) **Loading-view sticky-guard inference** + dodge clear in `web/js/main.js:_viewAutoDerive` (`3e3b14e`). (e) **Phase 3 mode-detector overlay** in `agents/agent2_backend/file_ingest._compute_effective_mode` - LCU phase fills in when Legion can't see Game-PC lockfile. (f) **Unified asset-hash + auto-reload** - `/api/ui-version` defers to `compute_asset_hash` (`876fd01`); both walk root + `web/{js,css}/panels/*` + `web/js/lib/*`. Closes a s164→s171.7 cache staleness where operator's browser served pre-s171.7 `champ_select.js` (opt-in gate) for ~10 days, making the menu work but real CS render legacy lobby. ADR-008 staged for overnight brief. (g) **View-router test backfill** - `dashboard/view_router_state.py` (test-only Python mirror of `_viewAutoDerive`) + `tests/test_view_router_state.py` (27 tests covering sticky-guard inference, dodge clears, transient null, post-game clears, view-gate fallbacks). Shipped via overnight brief (`a43981b`).
- ✅ **Build-variant persistence (champ-select → in-game)** - shipped s171.8 (`1aba0da`). `_csvBuildVariantsFor` merges DS engine row + user-saved variants from `/api/loadout/list` (parallel cache); click in `_csvWireBuildVariants` persists to `localStorage.rc-ingame-build-<champion>` - same key `item_build.js:_ibSavedChoice` already reads. Pre-selects saved row on re-render. New cyan "saved" tag distinguishes user-curated from DS engine output. Live verification awaits next champ-select.
- ✅ **s174 - Phase 1 Tank EHP scorer** (2026-05-12). New `agents/daemon_slayer/ehp.py` sibling of `dps.py`: `compute_ehp()` + `EhpResult` + `rank_items_by_ehp()` + `EhpRankResult`. Closed-form math - physical/magical/true EHP components weighted by caller-supplied `enemy_ad_share` / `enemy_ap_share` (remainder = true). ARAM `aramDamageTaken` modifier folded in. New `/ehp` + `/rank-tank` server routes + `rank_tank_for()` / `ehp_for()` client helpers. `core/defensive_picks.recommend_defensive_items_via_ehp()` ships the Option B integration - curated `_DEFENSIVE_ITEMS` as `only_item_ids` whitelist on the ranker; falls back to heuristic when engine down. ENGINE_VERSION 0.62.0 → 0.63.0. Tests: 67 new (44 ehp + 19 rank_tank + 4 server) → 1022 DS suite (was 955); wider RC 839 green. Live-validated: Malphite vs 90/10 AD → Randuin's > Dead Man's > Sunfire; Nasus vs 30/70 AP → Kaenic Rookern > Warmog's > Jak'Sho. First of six archetype scorers per `NEXT_SESSION_PLAN_2026-05-12_ARCHETYPE_EXPANSION.md` (Phase 2 = Bruiser hybrid, Phase 3 = CS picker UI, Phase 4 = Mage ability DPS, Phase 5 = Assassin burst, Phase 6 = Enchanter HPS). Phase 1 deferrals to 1.5: shield throughput, healing throughput, caster-side enemy pen.
- ✅ **s175 - Phase 2 Bruiser hybrid scorer** (2026-05-12). Second of the six archetype scorers. New `agents/daemon_slayer/hybrid.py` composes `compute_dps()` + `compute_ehp()` into `hybrid_score = α·dps + β·ehp`: `compute_hybrid()` + `HybridResult` for the scorer, `rank_items_by_hybrid()` + `HybridRankedItem` + `HybridRankResult` for the ranker. Per-champion (α,β) overrides ship in new `agents/daemon_slayer/archetype_weights.json` - 20 bruiser entries (Jarvan IV 0.55/0.45, Darius 0.65/0.35, Riven 0.70/0.30, Mordekaiser 0.50/0.50, ... full list in CLAUDE.md item 32) + default fallback (0.50, 0.50). Ranker sort key is normalized percentage delta (`α · dps_delta/baseline_dps + β · ehp_delta/baseline_ehp`) so weights stay intuitive across the ~10× DPS/EHP magnitude gap. New `/hybrid` + `/rank-bruiser` server routes + optional `alpha`/`beta` override params + `rank_bruiser_for()` / `hybrid_for()` client helpers + `BruiserRankedItem` dataclass. ENGINE_VERSION 0.63.0 → 0.64.0. Live-verified on Legion :8893: Jarvan IV α=0.55 picks Trinity Force first; Nasus with override α=0.2/β=0.8 surfaces Heartsteel + Warmog's in top 5. Tests: 44 new (40 hybrid + 4 server HybridRouteTests) → 1066 DS suite (was 1022); wider RC 839 green. **Next: Phase 3 = CS scorer-picker UI** (single session, pure UI, exposes 6-archetype dropdown in champ-select + in-game switch).
- ✅ **s176 - Phase 3 CS archetype-picker UI + dispatcher** (2026-05-12). UI lift unlocking operator-driven scorer selection. New `core/archetype_picks.py` owns the data layer (DDragon-tag → archetype mapping, per-champion override persistence in `data/cs_archetype_picks.json`, `get_archetype_for()` merger). New `dashboard/routes_archetype.py` exposes `GET/POST /api/cs-archetype-pick`. New `rank_for_primary_archetype()` dispatcher in `core/daemon_slayer_client.py` routes carry → ds.dps, bruiser → ds.hybrid, tank → ds.ehp; mage/assassin/enchanter fall through to ds.dps with `fell_back=True` until Phases 4-6 ship dedicated scorers. New 6-button 3×2 picker grid in `web/js/panels/champ_select.js` My Pick card; clicks save to `localStorage.rc-cs-archetype-<champion>` + POST to persist. Unimplemented scorers grayed but still clickable. **Deferred to coach-wiring session:** state-builder stamp (needs champion-id → name resolver), coach integration (`coaches/aram_coach.py` / `arena_coach.py` / `brawl_coach.py` / `coach_integration/_coach.py` still call `rank_for()` directly), first-purchase-mismatch soft-nudge, in-game switch tab, secondary-scorer refresh on item-complete events. Tests: 63 new (33 archetype_picks + 15 routes_archetype + 15 dispatcher) → 1968 wider RC suite (was 1905). Live: `curl /api/cs-archetype-pick?champion=Aatrox` returns DDragon default; POST persists; clear rolls back.
- ✅ **s184.1 - Archetype-mismatch chip renderer** (2026-05-13, `912efe1`). Closes s184's carried-forward item (a). New `web/js/panels/archetype_nudge_chip.js` (~95 LOC) reads `state.archetype_nudge` from the SSE/HTTP-fallback/LCU-poller paths and renders a yellow warn-style chip in header-row-2 next to `#ds-pill`. Phase-gated to `"fired"`; `pending`/`no_mismatch`/`dismissed` all collapse. X-button POSTs `/api/archetype-nudge/dismiss {champion}`. CSS in `web/css/panels/map_state.css` mirrors `.ds-pill` mode-gating exactly (collapsed in client/tft). 3 callsites in `web/js/main.js` matching `renderTeamContext(st)` count. Unified asset-hash (s171.8 ADR-008) made the live restart unnecessary - pid 1360 picked up the new files within seconds. Tests: 15 new in `tests/test_archetype_nudge_chip_dom.py` (grep-based wiring regression guard - index.html / panel JS / main.js wiring / CSS) → 1013 wider RC suite (was 1009 in s184 wrap; +4 net). Live-verified end-to-end (5 surfaces + dismiss endpoint round-trip + Game-PC monitor 0 screenshot). **Carried forward:** (a) live-game chip lifecycle validation; (b) `_TOP_N_THRESHOLD` retune once real games yield fired nudges; (c) calibration analysis additive (`nudge_history` rows to `core/ds_calibration`).
- ✅ **s184 - First-purchase archetype-mismatch soft-nudge** (2026-05-13). Closes the s176 Phase 3 deferral. New `core/archetype_mismatch.py` (~270 LOC) owns the evaluator: `compute_nudge_payload(coach, lc, lcu_snapshot, cs_archetype_pick)` is called once per /api/state and returns the `state.archetype_nudge` payload. Mismatch detection is dispatcher-driven (no hardcoded item→archetype affinity table) - calls `rank_for_primary_archetype(champion, primary, top=15)` once per (champion, game_session) pair and stamps "fired" when the operator's first non-trivial completed item isn't in the top-15 ids. Item filter denylist covers boots/Doran's/trinkets/consumables/SR-starters/jungle-pets (~28 IDs); operator's other 99% of items qualify as signal. Per-game-session dedup via liveclient's `gameData.gameId` (with synthetic `champion@start-floor` fallback); module-level threadsafe cache, restart-wipes. Default DDragon-tag picks skip evaluation (`source="default"` → no nudge) - only fires when operator explicitly picked or accepted a prior nudge. Three states: `pending` (no first item yet), `no_mismatch` (item in top-15, decision cached), `fired` (mismatch). Engine-down returns empty payload + leaves cache untouched so the next poll retries when DS comes back. `dashboard/_liveclient.py` extended with `owned_item_ids` (parallel int-id list to `owned_items` names) + `game_id` siblings. `dashboard/_state_builder.py` stamps `state.archetype_nudge` next to `state.cs_archetype_pick`. Two new routes in `dashboard/routes_archetype.py`: `GET /api/archetype-nudge` (diagnostic snapshot) + `POST /api/archetype-nudge/dismiss {champion}` (operator clicked the chip's X - idempotent; flips phase to "dismissed"). `dashboard/api_schema.py` gains `ArchetypeNudgeDismissRequest` + `ArchetypeNudgePayload` models. ENGINE_VERSION 0.69.0 unchanged - no DS engine math touched. Tests: 54 new (40 in `test_archetype_mismatch.py` covering item-filter denylist + session-token + engine-mode mapper + 12 nudge-evaluator cases (happy / dedup / engine-down / default-source-skipped / pending / re-eval on new session) + dismiss + NudgeResult shape + 5 dispatcher-boundary cases; 5 in `test_state_builder_archetype_nudge.py` covering build_state stamp + empty + pending passthrough + exception-resilience; 9 in `test_routes_archetype_nudge.py` covering GET snapshot + POST happy/400/empty paths + route-registration smoke) → 1009 wider RC (was 955); DS suite unchanged 1426. Live-verified end-to-end on Legion :8888 after `echo restart > restart_trigger.txt`: `GET /api/archetype-nudge` returns `{"ok": true, "state": {}}`; `POST /api/archetype-nudge/dismiss` handles happy + 400 + idempotent paths; `/api/state.archetype_nudge` present (empty since no game). Dashboard JS chip renderer **deferred to s184.1** - backend-first ship matches s174-s183 cadence; operator can verify via raw `/api/state` until the chip lands.
- ✅ **s183 - Dashboard JS scorer-aware unit rendering** (2026-05-13). Follow-up to s182's coach-dispatch wire-in. New `web/js/lib/scorer_units.js` (~35 LOC) single source of truth on the JS side, mirroring the Python `_UNIT_SUFFIX` table in `coach_integration/archetype_dispatch.py:52-59`: exports `scorerUnit(scorer)` + `formatDsDelta(row)`. 9 callsites across 5 panel files (`item_build.js` `#ds-pill` + `#ib-ds-block`, `champ_select.js` `_fetchDsPreview` + `_csvBuildVariantsFor`, `next.js` Build-row, `active_match.js` `_dsIcon` tooltip) migrated from inline `+${Math.round(r.delta_dps)}dps` to the helper - pill now flips to "+25hps" for enchanter / "+1690ehp" for tank / "+8%" for hybrid bruiser. Backend `_serve_ds_preview_post` stamps `"scorer": scorer` on every row of the response array (closes the gap for callers that cache rows, not the response envelope - CS Build Chooser `_CSV_DS_CACHE`). `dashboard/api_schema.py` schema models updated (`DsPreviewItem.scorer`, `DsPreviewResponse.scorer` + `.archetype`, `DsPreviewRequest.archetype` - soft-validators, fields optional). JSDoc typedefs in `web/js/lib/state_schema.js` extended. ENGINE_VERSION 0.69.0 unchanged. Tests: 7 new (mock dispatcher boundary, stub HTTP handler, covers carry/tank/enchanter/hybrid per-row stamp + archetype override propagation + empty-ranked + engine-down 503) → 955 wider RC (was 948); DS suite unchanged 1426; panel snapshot suite 11 green. `dashboard.js` noted dead (no `<script>` reference in `web/index.html`). Pre-s182 supervisor backward-compat preserved: `scorerUnit()` falls back to "dps" when `row.scorer` is absent. **Carried forward:** (a) RC supervisor restart still pending from s182 (operator-driven `restart_trigger.txt`); (b) DS server :8893 was down at session start, unrelated to s183.
- ✅ **DS engine data refresh 16.9.1 → 16.10.1** (2026-05-13, `da67555`). Re-extract via `daemon_slayer_extract.py` + `daemon_slayer_abilities_extract.py`; copied hand-curated `enchanter_items.json` forward. Notable item math impact: Doran's Bow 6→8 AD, Doran's Helm 110→140 HP, Gluttonous Greaves 650g→700g + 1%/6→0.6%/10 stacks. Arena augments 219→220. DS server restarted; tests 1426 / wider RC 1013 green.
- ✅ **s185 - Phase 4d per-champion max_priority overrides** (2026-05-13, `d0c604b`). New `agents/daemon_slayer/champion_max_priority.json` (12 entries) + loader in `ability_dps.py` + result-type `max_priority_source` field. Both mage and assassin scorers resolve via registry when caller doesn't supply. Cassi lvl 9 baseline +40%. ENGINE 0.69.0 → 0.70.0. DS suite 1426 → 1465.
- ✅ **s186 - Phase 5.5 per-champion combo_sequence overrides** (2026-05-13, `e8a9a1e`). New `agents/daemon_slayer/champion_combo_sequences.json` (15 entries - all canonical assassins + Briar). Zed Q-W-E-R-Q2-AA (shadow), Yone Q1→Q2→Q3 chain, Akali R-recast, Leblanc R-mimic-Q. Loader in `burst.py`; `BurstResult.combo_sequence_source` field. ENGINE 0.70.0 → 0.71.0. Zed baseline +24% (333.89 → 413.33). DS suite 1465 → 1499.
- ✅ **s187 - Phase 4e per-(champion, key) form_index overrides** (2026-05-13, `b48ff97`). New `agents/daemon_slayer/champion_form_index.json` (5 entries: Nidalee/Elise/Jayce/Hwei/LeeSin). Caller dict merges with registry per-key. Hwei DPS drops 11.95 → 0.02 when forced to form 0 (his form-0 "Subject:" stances have zero damage blocks). ENGINE 0.71.0 → 0.72.0. DS suite 1499 → 1534.
- ✅ **s188 - Phase 5.6 per-attack on-hit AA procs in burst** (2026-05-13, `9953c23`). New `dps._per_attack_proc_damage()` + `DpsResult.per_attack_on_hit_damage` field. `compute_burst_damage` reads it so each AA token includes Wit's End / BotRK / Statikk contributions. Akali +Wit's End AA jumps 105.6 → 187.5; Zed +BotRK AA jumps 53.9 → 165.0. ENGINE 0.72.0 → 0.73.0. DS suite 1534 → 1545. **Spellblade gap left open** (Spellblade items are `every_n_seconds` not `every_n_attacks` so skipped by the helper - closed in s189).
- ✅ **s189 - Phase 5.7 Spellblade-in-burst** (2026-05-13, `6aa573b`). Closes the s188 Spellblade deferral. New `dps._spellblade_per_proc_damage()` finds the build's `unique_passive_key=="spellblade"` item (8 SR + 6 Arena mirrors + Sheen + Bloodsong, deduped first-seen-wins) and evaluates the per-proc damage through the standard pipeline. `DpsResult` gains `spellblade_per_proc_damage` + `spellblade_item_name`. `burst.py` combo walker tracks `spellblade_armed: bool` - any ability token arms, AA token consumes; AA branch adds the per-proc damage to `final_damage`. `BurstResult` gains `spellblade_procs` + `spellblade_damage` + `spellblade_item_name`. Live: Akali +TF 787.9 → 1111.0 burst (2 procs +211.1); Zed +Divine Sunderer 413.3 → 654.0 burst (1 proc +134.0). `/rank-assassin` shift on Zed: Essence Reaver #5 → #2, Trinity Force enters top 10 at #7. ENGINE 0.73.0 → 0.74.0. Tests: 32 new in `test_spellblade_burst.py` → DS suite 1545 → 1577; wider RC 1013 green.
- ✅ **s190 - Phase 5.8 Sundered Sky Lightshield Strike in burst** (2026-05-13). Closes s189 carry-forward (a). New `dps._lightshield_strike_per_proc_damage()` filters by `proc.name == "Lightshield Strike"` (Sundered Sky has no `unique_passive_key`). Same arm-consume model as s189 Spellblade but capped at 1 proc per combo via `lightshield_procs_fired == 0` re-arming gate (Sundered Sky's 8s real CD vs 2-3s burst window). Builds with both Sundered Sky + Trinity Force land BOTH procs on the same AA (independent state machines). `DpsResult` gains `lightshield_strike_per_proc_damage` + `lightshield_strike_item_name`. `BurstResult` gains `lightshield_strike_procs` + `lightshield_strike_damage` + `lightshield_strike_item_name`. Live: Aatrox +Sundered Sky 293.7 → 519.9 burst (LS=1× +133); +TF + Sundered Sky 838.7 burst = exactly Sundered Sky alone + TF alone (clean additive). Q-AA-W-AA combo: SS fires once (cap honored even with 2 eligible AAs); TF fires twice. ENGINE 0.74.0 → 0.75.0. Tests: 28 new in `test_lightshield_strike_burst.py` → DS suite 1577 → 1605; wider RC 1013 green.
- ✅ **s203 - Phase 5.9.16 block_index expansion** (2026-05-14). 12 new entries (5 new champions: Blitzcrank, Gwen, Kled, LeeSin, Thresh + 5 key extensions: Diana R, Jax R, Kennen W, Smolder E, Vladimir Q). Kled contributes 3 keys for 7 entries on new champs. Registry 110 → 115 champions, 161 → 173 entries. ENGINE_VERSION 0.87.0 → 0.88.0. **Six sub-patterns:** A multi-hit single-target totals (Diana R full channel / Gwen R 9-needle 9× / Kled Q 3-stage 3× / Kled E 2-strike 2× / Vladimir Q Crimson Rush 1.85×); B resource-state amp Smolder E max-stack 5× (reverts s198/s202 'Meraki Minimum label' skip); C active-cast vs passive-zap split (Blitzcrank R / Kennen W / Jax R - engine pre-s203 was scoring the passive zap as the R/W cast value); D max-charge Kled R 3×; E missing-HP layered on s187 form_index - LeeSin Q form 1 max-missing-HP (**first NET-damage layering of block_index on form_index**); F **empty-block-0 fix** - Thresh E (first instance of routing past a 0-damage block 0 with only unparsed soul scaling). **Live A/B headlines:** Gwen R **+218.4%** (2.76 → 8.80), Kled all-3-combined **+134.4%** (7.36 → 17.26), Vladimir Q **+53.8%** (28.09 → 43.19), LeeSin Q **+46.4%** (9.99 → 14.62), Blitzcrank R **+29.4%** (4.60 → 5.96), Thresh E **+11.8%** (6.45 → 7.21), Kennen W **+7.6%**, Smolder E **+6.7%**, Diana R **+5.3%**, Jax R **+1.1%**. Tests: 25 new in `Phase599_16ExpansionTests` class (12 _delta_check + 6 multi-key resolved-shape + 4 math sanity + 3 backward-compat) + 10 explicit assertions in `test_known_champion_overrides` + 2 stale-assertion conversions to `.get()` subset (Vladimir/Smolder) + 2 ENGINE_VERSION pins → DS suite 1894 → 1919 (+25); wider RC 1024 green post-DS-restart. Nineteenth consecutive override / proc-shape ship (s185-s203); twelfth pure-data batch. Cumulative coverage 173 entries / 115 champions (67% of 171-roster touched). **Carried forward:** all s202 carry-forwards remain (sum-of-blocks bucket now 8+ candidates: Thresh E + Taliyah E + Sona Q + Camille W + Katarina R + Malphite W + Malzahar E/R + Kalista E + Jinx R; nested missing-HP parser: Kindred E + Kayle E + Belveth R; form_index registry seeds needed for Riven + Qiyana before next form-conditional block_index batch).
- ✅ **s202 - Phase 5.9.15 block_index expansion** (2026-05-14, `723fb1c`). 18 new entries (6 new champs: Gangplank/Gnar/KSante/RekSai/Vayne/Yunara + 12 key extensions on Zoe/Akshan/AurelionSol/Nasus/Poppy/Renekton/Rumble/Smolder/Viktor/Yuumi). Registry 104 → 110 champs, 143 → 161 entries. ENGINE 0.86.0 → 0.87.0. Five sub-patterns: A multi-hit totals (KSante R / Vayne E / Yunara Q / Zoe W / Viktor E); B channel/duration (Gangplank R / AurelionSol Q / Nasus R / Renekton R / Rumble R / Yuumi R / Poppy E); C max-charge/distance (Akshan R / Smolder R); D resource-state (RekSai E / Smolder Q); E wall-stun/charge (Gnar R / Rumble Q). Reverts 4 prior skips. Headlines: AurelionSol Q +351.6% / Renekton R +68.8% / Smolder Q +51.6% / Nasus R +48.4%. Tests: 28 new → DS 1894.
- ✅ **s201 - Phase 5.9.14 block_index expansion** (2026-05-14, `9365f5c`). 16 new entries (13 new champs: Fizz/Galio/Garen/Graves/Janna/Jhin/Kennen/Taliyah/Teemo/Viego/Xerath/Yasuo/Ziggs). Registry 91 → 104 champs, 127 → 143 entries. ENGINE 0.85.0 → 0.86.0. Four sub-patterns: A multi-hit totals; B fully-charged amps; C channel/duration; D resource/positional. Reverts 4 prior skips. Headlines: Taliyah Q +138.2% / Graves Q +104.5% / Xerath R +77.0%. Tests: 24 new → DS 1866 / wider RC 913.
- ✅ **s200 - Phase 5.9.13 rescue batch block_index expansion** (2026-05-14, `8f837c0`). 7 new entries closing 4 previously-deferred mechanics by re-evaluating skip-list with improved understanding: Ambessa Q/W (s196/s197/s198 'form swap' was actually Drain-stack resource amp like Renekton Fury); Anivia R (s195 'channel ambiguous' was Empowered phase amp like Belveth E max-charge); Lillia Q (s199 'uncertain' was Q + Dream Dust AA combo like Sett Q); Nilah Q (s199 'uncertain 2-stack' was max-stack empowered AA like Twitch E / Tristana E). Plus Ambessa E (Lacerate slash+thrust 2×) and Poppy Q (Hammer Shock out+return 2×). Pattern A multi-hit totals (Ambessa E, Lillia Q, Poppy Q); Pattern B resource-state amps (Ambessa Q, Ambessa W, Nilah Q); Pattern C channel commit (Anivia R Empowered). Registry 90 → 91 champions (Ambessa new), 120 → 127 entries. ENGINE_VERSION 0.84.0 → 0.85.0. **Live A/B headlines (/ability-dps lvl 11 vs 80/30/2000):** Nilah Q +82.0%, Poppy Q +75.7%, Ambessa Q +37.1%, Lillia Q +19.4%, Anivia R +10.5%. Tests: 17 new in `Phase599_13ExpansionTests` class (7 _delta_check + 5 multi-key resolved-shape + 2 math sanity + 3 backward-compat) + 6 explicit assertions in `test_known_champion_overrides` + 2 updated (Anivia resolved now `{Q:2, E:1, R:1}`) + 2 ENGINE_VERSION pins → DS suite 1825 → 1842 (+17); wider RC 913 green. Ninth pure-data batch; sixteenth consecutive override / proc-shape ship (s185-s200). **Carried forward:** all s199 carry-forwards remain (token-variant for multi-stage Q chains, form-swap schema, sequence-state schema, conditional target-state schema lift).
- ✅ **s199 - Phase 5.9.12 block_index expansion + filtered-index discovery** (2026-05-14, `4ee733c`). 17 new entries (6 new champions: Ashe/Shaco/Shen/Swain/Tristana/Xayah + 11 key extensions on Aatrox/Fiddlesticks/Karthus/Nautilus/Nunu/Samira/Sejuani/Talon/Udyr/Viktor/Zac). Registry 84 → 90 champions, 103 → 120 entries. ENGINE_VERSION 0.83.0 → 0.84.0. Six sub-patterns: A multi-hit totals (Ashe Q Ranger's Focus 5×, Nunu E 3-snowball, Samira W 2-rotation, Shen Q 3-AA empowered, Swain Q 5-bolt, Viktor Q + AA, Xayah Q out+return, Zac Q both arms); B positional/sweet-spot amps (Aatrox Q1 sweet-spot, Shaco E backstab, Talon Q champion crit); C resource-state (Tristana E max-stack, Udyr Q Awakened); D channel total (Karthus E per-second); E direct-hit primary (Sejuani R, Nautilus R); F execute amp (Fiddlesticks W low-HP). **Filtering note discovery:** `_select_blocks` filters `attribute_kind != "damage"` blocks BEFORE indexing - registry value is the FILTERED damage-block index, not raw block. Shen Q (raw block 0 'Slow' non-damage) corrected from Q=2 → Q=1; 4 others (Ashe/Karthus/Shaco/Nautilus) accidentally landed correct filtered idx via `clamp out-of-range to last` semantics. Documented in registry `_meta`. **Live A/B headlines:** Shen Q +113.9%, Swain Q +63.0%, Xayah Q +62.5%, Aatrox Q +56.1%, Udyr Q +44.6%, Viktor Q +41.9%, Tristana E +31.7%. Tests: 31 new in `Phase599_12ExpansionTests` class (17 _delta_check + 6 multi-key resolved-shape + 3 math sanity + 4 backward-compat) + 17 explicit assertions in `test_known_champion_overrides` + 1 updated Talon assertion + 1 relaxed `test_pre_s198_aatrox_unchanged` assertion + 2 ENGINE_VERSION pins → DS suite 1794 → 1825 (+31); wider RC 913 green. Eighth pure-data batch; fifteenth consecutive override ship.
- ✅ **s195 - Phase 5.9.8 multi-hit/charge/recast block_index expansion** (2026-05-14, `a4e4046`). Pure-data continuation closing the next clean class of single-static-block_index candidates beyond s193 channels (s194 calibration follow-up). 13 new (champion, key) entries across 4 sub-patterns: **(A) Multi-hit single-target totals** (operator focuses all hits on one target): Ahri W=2 (Fox-Fire 3-bolt), Kaisa Q=2 (Icathian Rain focus), Lulu Q=3 (Glitterlance both passes), Sivir Q=2 (Boomerang out+back), Talon W=2 (Rake), Talon R=2 (Shadow Assault unstealth chain), Velkoz W=2 (Void Rift), Ekko Q=3 (Timewinder). **(B) Fully-charged amps**: Varus Q=1, Zoe Q=1 (long-distance post-E), Vladimir E=1 (2-charge). **(C) Recast amps**: Camille Q=2 (Precision Protocol 2nd cast). **(D) CC-conditional duration totals**: Morgana W=3 (Tormented Shadow vs rooted). Registry: 25 → 35 entries. Zero code changes - s191/s192 walker logic (token-canonical + base-key fallback) handles all new entries transparently. ENGINE_VERSION 0.79.0 → 0.80.0. **A/B impact (/ability-dps at lvl 11 vs 80/30/2000):** Morgana W +158.5%, Zoe Q +135.8%, Sivir Q +85%, Ekko Q +70%, Camille Q +69%, Talon W +65%, Kaisa Q +48%, Varus Q +34%, Lulu Q +33%, Vladimir E +24%, Velkoz W +15%, Ahri W +12%, Talon R +4%. Tests: 19 new in `Phase598ExpansionTests` class (13 per-entry _delta_check + 3 multi-key resolved-shape + 1 Ahri W numeric sanity matching block 0 + 2× block 1 + 2 backward-compat) + 11 explicit assertions in `test_known_champion_overrides` + 2 version-pin bumps → DS suite 1689 → 1708; wider RC `tests/` 913 green. **Regression checks pass:** s194 Corki entry preserved; s193 Singed/Anivia preserved; s192 Akali R/R2 token-variant intact; s191 BackwardCompatTests green. **Architectural pattern continues:** eleventh consecutive override registry; fourth pure-data batch (s191 seed, s193 channels, s194 calibration, s195 multi-hit/charge/recast). **Carried forward:** conditional block_index based on target state (Zoe sleep + Lux mark + DrMundo E missing-HP - 3+ candidates accumulated, schema lift ready); sum-of-blocks (DrMundo W detonation, single candidate); conditional resource-state (Corki R Big One, Renekton Q Fury, Aatrox Q chain stage).
- ✅ **s194 - Phase 5.9.7 calibration-follow-up block_index expansion** (2026-05-14, `9a2504d`). Closes s193 carry-forward list: Corki {W:1, E:1} (Valkyrie trail + Gatling Gun channel), Hecarim {W:1, E:1} (Spirit of Dread aura + Devastating Charge max-charge), Jayce {Q:1, W:1} (Shock Blast through Acceleration Gate + Lightning Field hammer aura), Rell {R:1} (Magnet Storm full channel), DrMundo {W:1} (Heart Zapper drain channel). Pure data batch - no resolver/walker code changes. **Jayce Q is the first block_index that layers on a prior form_index override** (s187 set Jayce.Q form_index=1 cannon-form; s194 now sets block_index=1 within that form). Resolvers compose at runtime without any new code. Skipped: Corki R Big One (charge-based every-4th-missile, not per-cast amp - requires conditional resource-state modeling). ENGINE_VERSION 0.78.0 → 0.79.0. Registry: 20 → 25 entries. Tests: 13 new in new `CalibrationFollowUpExpansionTests` class → DS suite 1689; wider RC 1024 green. **A/B impact on /ability-dps at lvl 11 vs 80/30/2000:** Jayce W +31.9%, Corki W +24.9%, Hecarim W +23.2%, DrMundo W +18.1%, Jayce Q +16.2%, Corki E +7.7%, Rell R +4.8%, Hecarim E +1.9% (diluted by Q/W dominance in total). **Regression checks pass:** Singed Q s193 entry preserved; Cassi /ability-dps 39.48 unchanged; Akali /burst 941.71 unchanged; unmapped Zed (BackwardCompatTests) byte-identical. **Carried forward:** conditional block_index based on target state (Zoe sleep / Lux mark / DrMundo E missing-HP / Renekton Q Fury - schema lift, defer until 3+ accumulate); sum-of-blocks block_index (DrMundo W detonation - schema lift `int | list[int]`); conditional resource-state block_index (Corki R Big One); + all s193 carry-forwards.
- ✅ **s193 - Phase 5.9.6 channeled-ability block_index expansion** (2026-05-14). Pure data batch - no resolver/walker/server code changes. Scanning `champion_abilities.json` for "Total"/"Maximum"/"Increased" attribute names surfaced ~30 candidates beyond s191/s192; this batch picks 8 of the cleanest "per-tick → total" wins for channeled abilities plus an Anivia Q extension. Registry expands from 12 → 20 entries: Alistar E=1 (Trample total), AurelionSol E=1 (Singularity total), Fiddlesticks R=1 (Crowstorm channel total), MissFortune E=1 (Make It Rain duration), Samira R=1 (Inferno Trigger spray total), Singed Q=1 (Poison Trail full duration), Velkoz R=1 (Disintegration Ray max), Syndra R=2 (max sphere stacks); Anivia extended from `{"E": 1}` to `{"Q": 2, "E": 1}` (block 2 = initial pass + detonation total). ENGINE_VERSION 0.77.0 → 0.78.0. Tests: 11 new in new `ChanneledAbilityExpansionTests` class (registry-shape assertion + delta-check per new entry + Anivia regression guard + Singed Q numeric-match math sanity) + 9 new explicit assertions in `test_known_champion_overrides` → DS suite 1665 → 1676; wider RC 1013 green. **A/B impact (/ability-dps at lvl 11 vs 80/30/2000):** Singed Q +182% (4.06 → 11.46 adps); Fiddlesticks R +125% (5.24 → 11.81); Anivia Q+E +105% (9.66 → 19.80); AurelionSol E +65%; Velkoz R +23%; MissFortune E +17%; Syndra R +10%. Channel-class abilities were systematically under-counted by 20-180% pre-s193; the Meraki schema was correct, the engine just needed to know which block represented realistic per-cast value. /rank-mage top 5 for Singed shows AP items climb dominantly (Rabadon's #1 +11.77) reflecting Poison Trail's 85% AP block-1 scaling; Fiddle /rank-mage shows similar AP dominance via Crowstorm's 250% AP at rank 3. **Regression checks pass:** Cassi /ability-dps 39.48 unchanged (s191 E:1 preserved); Akali /burst 941.71 unchanged (s192 R/R2 logic preserved). **Carried forward to Phase 5.9.7:** Corki E/W + Hecarim W + Jayce W + Rell R + DrMundo W (same per-tick → total pattern, +5-20% adps each, deferred this batch as lower-impact); + all s192 carry-forwards (conditional block_index, inter-spell amps, etc.).
- ✅ **s192 - Phase 5.9.5 token-variant block_index for Akali R** (2026-05-14). Closes s191 carry-forward (a). Akali R has 3 Meraki blocks - block 0 base + bonus-AD scaling (R1), block 1 minimum-HP R2, block 2 maximum-HP R2 via missing-HP curve. Pre-s192 the burst walker only knew base keys so setting `{"Akali": {"R": 2}}` globally would over-count R1. Extended the walker to do TOKEN-CANONICAL lookup first (`block_overrides.get("R2")`), falling back to BASE-KEY lookup (`block_overrides.get("R")`). API surface stayed `dict[str, int]`; valid-key set extended from `{Q,W,E,R}` to also include `{Q2,W2,E2,R2}`. Forward-compatible - future per-token entries drop in without code changes. Registry gains `"Akali": {"R": 0, "R2": 2}`. ENGINE_VERSION 0.76.0 → 0.77.0. Tests: 7 new in new `AkaliTokenVariantTests` class + extended valid-key assertion → DS suite 1658 → 1665; wider RC 1013 green. Live A/B on :8893: Akali burst 787.9 (forced R+R2:0) vs **941.7 (registry)** vs 1095.6 (naive R:2 only - over-counts R1) - registry +19.5% delta sits exactly halfway, confirming token-variant model captures the realistic mid-point. /rank-assassin top: **Lich Bane #1 +396.54** (Spellblade × doubled R2 max-execute × 90% AP scaling). `compute_ability_dps` unaffected - R2 entry is invisible to the mage scorer's Q/W/E/R-only iteration. **Carried forward to Phase 5.9.6:** conditional block_index based on target state (Zoe sleep amp, Lux Illumination, DrMundo E missing-HP threshold, Renekton Q Fury - substantial schema lift; defer until 3+ candidates accumulate).
- ✅ **s191 - Phase 5.9 per-(champion, key) damage block_index overrides** (2026-05-14). Closes a long-standing systematic under-scoring: 248 multi-block (champion, key, form) tuples in `champion_abilities.json` carry block ≥1 with amped/empowered/max/total damage; pre-s191 `block_strategy="first"` locked every caller to block 0. New `agents/daemon_slayer/champion_block_index.json` seed registry (12 entries across 11 champions): Cassiopeia E=1 (Total Enhanced vs poisoned), Veigar R=1 (Maximum vs executed), Anivia E=1 (Enhanced vs chilled), Diana W=2 (all 3 Pale Cascade orbs), Brand W=1 + R=1, Evelynn R=1 (Empowered sub-30% HP), Aurora Q=2, Belveth E=2, Karma W=1, Vex R=2, Ahri Q=1. Loader + resolver in `ability_dps.py` mirror the Phase 4e form_index pattern (`_BLOCK_INDEX_PATH` + singleton-cached `_load_block_index_table` + `get_block_index_for` + `_resolve_block_index_overrides` + `reset_block_index_cache`). `_select_blocks` extended with new `"indexed"` strategy + `block_index: int = 0` param (clamps negative→0, out-of-range→last). `compute_ability_dps` / `rank_items_by_ability_dps` / `compute_burst_damage` / `rank_items_by_burst` all gain `block_index_overrides: Optional[dict[str, int]] = None` arg + per-spell loop switches to `"indexed"` strategy per-key when override present (others honor global `block_strategy`). All 4 result types gain `block_index_source: str` + `block_index_resolved: dict[str, int]` surfaced in `to_dict()`. Four server routes (`/ability-dps`, `/rank-mage`, `/burst`, `/rank-assassin`) accept new `block_index` JSON dict body field via shared `_parse_block_index` decoder. ENGINE_VERSION 0.75.0 → 0.76.0. Tests: 53 new in `test_block_index_overrides.py` (9 classes covering registry shape, loader cache, resolver, indexed strategy direct, compute integration, ranker propagation, to_dict, backward-compat invariants, server-route surfacing) → DS suite 1605 → 1658; wider RC 1013 green. Live-validated `/ability-dps` + `/burst` + `/rank-mage` + `/rank-assassin`: Cassi /ability-dps 39.48 vs forced-block-0 27.66 (+43%); Veigar /burst 807.01 vs forced-R0 614.70 (+31%); Anivia /ability-dps 14.86 vs forced-block-0 9.66 (+54%); Veigar /rank-assassin Lich Bane climbs to #1 (+391.15 - Spellblade amplification on now-doubled R block1). **Akali R deliberately skipped** (would double-count R1/R2 - needs per-token-variant overrides, separate feature); DrMundo E + Renekton Q + AurelionSol R + Caitlyn Q deferred per inline rationale in registry `_meta`. Backward-compat verified via `BackwardCompatTests` - unmapped champions see byte-identical pre-s191 output.
- ✅ **s182 - Coach archetype dispatch wire-in** (2026-05-13). Cross-phase coach-integration follow-up to the archetype-expansion plan. New `coach_integration/archetype_dispatch.py` exposes `dispatch_for_coach()` + `CoachDispatchResult` + `display_label()` - resolves archetype via `core.archetype_picks.get_archetype_for`, calls `rank_for_primary_archetype()`, returns scorer-aware `picks_str` ("dps"/"ehp"/"%"/"adps"/"burst"/"hps" suffix) + `display_rows` (legacy `daemon_slayer_picks` shape preserved + new `delta`/`scorer` fields). All 4 mode coaches (SR/ARAM/Arena/Brawl) swap their `rank_for()` calls - DS picks match the archetype (Lulu → ds.hps; Malphite → ds.ehp; Veigar → ds.ability; Zed → ds.burst; Jarvan IV → ds.hybrid; Caitlyn → ds.dps default). `dashboard/routes_state._serve_ds_preview_post` routes through dispatcher with optional `archetype` payload override; response gains `scorer` + `archetype` siblings. `dashboard/_state_builder.build_state()` stamps `state.cs_archetype_pick` derived from `_active_champion()` priority (liveclient → coach → LCU CS local pick). ENGINE_VERSION 0.69.0 unchanged (no DS engine math changed). Tests: 33 new (19 dispatch helper + 14 state-builder) → 948 wider RC (was 915); DS unchanged 1426. Live-validated end-to-end: 4 archetypes route correctly through the helper. `NEXT_SESSION_PLAN_2026-05-12_ARCHETYPE_EXPANSION.md` archived to `docs/_archive/`. **Carried forward:** (a) live RC supervisor needs restart to pick up changes; (b) dashboard JS `#ds-pill` + active-match panel still render "+Ndps" for non-DPS scorers (numerically right, label drift - follow-up); (c) DDragon-tag → archetype mapping covers Fighter/Mage/Assassin/Marksman/Tank/Support but champions like Yasuo/Jax/Wukong with multiple tags get tags[0] as primary - operator override via picker UI takes precedence.
- ✅ **s181 - Phase 6 enchanter healing throughput scorer + `/rank-enchanter` + dispatcher** (2026-05-13). Sixth and final archetype scorer per the archetype-expansion plan. New `agents/daemon_slayer/hps.py` (~625 LOC) sibling of `ehp.py`: `compute_hps()` + `HpsResult` + `HpsItemContribution` for the evaluator; `rank_items_by_hps()` + `HpsRankedItem` + `HpsRankResult` for the ranker; `EnchanterFormulasSnapshot` + `EnchanterItemFormula` for the curated formula loader (singleton pattern: `load_default_formulas()` + `reset_formulas_cache()`). New hand-curated `data/daemon_slayer/16.9.1/enchanter_items.json` registry covering 9 enchanter items: Moonstone Renewer (6617, 30% chain amp), Redemption (3107, AoE heal 150→350 / +10% H&S power), Mikael's Blessing (3222, single-target heal 100→250 + cleanse / +12% H&S power), Echoes of Helia (6620, Soul Siphon ~50 HP × 0.4/s + 15% AP scaling), Ardent Censer (3504, 15 ally_buff_credit + 10% H&S power), Staff of Flowing Water (6616, 12 ally_buff_credit + 10% H&S power), Locket of the Iron Solari (3190, AoE shield 290→360 / 90s CD), Imperial Mandate (4005, 6 ally_buff_credit damage proc), Knight's Vow (3109, 10 ally_buff_credit ally tank-share). Total throughput formula: (healing_raw + shielding_raw) × product(1 + amp_pct) × mode_mult + sum(ally_buff_credit). ARAM `aramShieldsHealing` modifier applied when present on the champion. Operator override `targets_per_proc_override` retunes the "average teammate" assumption (Arena 2v2 → override=1). Two new server routes (`/hps`, `/rank-enchanter`) with shared `_opt_targets_override` decoder; ranker's `enchanter_only` boolean (default True) restricts candidate pool to the curated registry + Arena/ARAM mode mirrors by name match. `core/daemon_slayer_client.py` gains `EnchanterRankedItem` + `rank_enchanter_for()` + `hps_for()` client helpers; dispatcher's `enchanter` branch now routes to `ds.hps` (returns `scorer="hps"`, `fell_back=False`). ENGINE_VERSION 0.68.0 → 0.69.0. Tests: 80 new in DS suite (46 in `test_hps.py` covering formula loader + scoring + amp pipeline + mode multiplier + targets override + edge cases + server route; 34 in `test_rank_enchanter.py` covering ranker basics + scoring + filter pipeline + validation + serialization + 8 server route cases) + 5 net in `test_archetype_dispatcher.py` (new `EnchanterRoutingTests` class with 5 tests replacing the old single fallback assertion + 1 new EngineDownTests case) → 1426 DS suite (was 1346); wider RC 915 green (was 899). Live-validated `/rank-enchanter` on Legion :8893: Soraka lvl 11 naked → baseline 0.0 HPS, top 8 = Helia +25.14 / Ardent +15.00 / Staff +12.00 / Locket +11.04 / Knight's Vow +10.00 / Redemption +7.36 / Mandate +6.00 / Mikael's +3.76; with Redemption+Mikael's built (baseline 12.17 HPS), Moonstone surfaces at +3.05 - confirms amp compounding (1.10×1.12×1.30 product math). **All 6 archetype scorers fully wired** through `rank_for_primary_archetype()` (carry/bruiser/tank/mage/assassin/enchanter); archetype-expansion plan complete. **Carried forward to Phase 6.5:** real ally-state plumbing, champion-spell healing throughput, heal_shield_power amp on champion abilities, coach integration (no coach reads `state.cs_archetype_pick.primary` yet - same as Phases 1/2/3/4/5 omission).
- ✅ **s180 - Phase 5 assassin burst-window scorer + `/rank-assassin` + dispatcher** (2026-05-13). Fifth of the 6 archetype scorers per the archetype-expansion plan. New `agents/daemon_slayer/burst.py` (~750 LOC) sibling of `ability_dps.py`: `compute_burst_damage()` + `BurstResult` + `ComboCast` + `rank_items_by_burst()` + `BurstRankedItem` + `BurstRankResult`. Per-combo evaluator walks a caller-supplied `combo_sequence` (default `("Q","W","E","AA","R","AA")`; tokens `AA`/`P`/`Q`/`W`/`E`/`R`/`Q2`-`R2` for repeats at same rank), fires each spell once at level-resolved rank, sums damage blocks via shared `_evaluate_block`/`_select_blocks` from `ability_dps`, applies the full Phase 4b amp pipeline (Rabadon, Liandry, Demonic Embrace, Abyssal Mask magic-only, Riftmaker HP→AP, Mejai's stacked AP) plus mitigation pipeline (lethality + flat pen + % pen for PHYSICAL; flat + % magic pen for MAGIC; TRUE bypass). Auto-attack hits contribute build's per-hit `avg_attack_dmg` from `compute_dps` (post-armor + mode, no on-hit periodic procs - Phase 5.5 deferral). Two new server routes (`/burst`, `/rank-assassin`) with shared `_parse_combo_sequence` decoder accepting list / dash-string / comma-string forms. `core/daemon_slayer_client.py` gains `AssassinRankedItem` + `rank_assassin_for()` + `burst_for()` client helpers; dispatcher's `assassin` branch now routes to `ds.burst`. ENGINE_VERSION 0.67.0 → 0.68.0. Tests: 89 new in DS suite (51 in `test_burst.py` + 38 in `test_rank_assassin.py`) + 4 net in `test_archetype_dispatcher.py` (new `AssassinRoutingTests` class with 5 tests replacing the old fallback assertion) → 1346 DS suite (was 1257); wider RC 899 green. Live-validated `/rank-assassin` on Legion :8893: Zed lvl 11 vs 80 armor / 30 MR → baseline 333.9 burst (primary_scaling=AD), top 8 = IE +250.2 / BT +213.3 / Umbral +205.6 / LDR +204.6 / Serylda's +203.6 / Bastionbreaker +202.0 / Axiom Arc +191.0 / Youmuu's +191.0 - lethality + armor-pen dominate as expected. **5 of 6 archetype scorers fully wired** through `rank_for_primary_archetype()` (carry/bruiser/tank/mage/assassin); only enchanter still falls back to ds.dps pending Phase 6. Phase 6 closed in s181.
- ✅ **s179 - Phase 4c mage ability DPS ranker + `/rank-mage` + dispatcher** (2026-05-12). Third of the 3-session Phase 4 lift; this slot consumes Phase 4b's `compute_ability_dps()` evaluator and ships the per-archetype ranker so the dispatcher's mage branch routes to a real scorer instead of falling back to ds.dps. New `rank_items_by_ability_dps()` + `AbilityDpsRankedItem` + `AbilityDpsRankResult` dataclasses appended to `agents/daemon_slayer/ability_dps.py` (~250 LOC of additions) - mirrors `rank_items_by_hybrid` / `rank_items_by_ehp` shape (same `_filter_candidates` pipeline + `delta` / `efficiency` sort keys + Arena trinket strip + dead-unique dedup), but each candidate is scored by total-ability-DPS gain over the baseline. New `/rank-mage` server route in `agents/daemon_slayer/server.py`; shared `_parse_max_priority` / `_parse_form_index` helpers extracted so `/ability-dps` and `/rank-mage` both parse operator-supplied priority/form overrides identically (list / comma-string / compact "QWE"). New `MageRankedItem` + `rank_mage_for()` + `ability_dps_for()` client helpers in `core/daemon_slayer_client.py`. **Dispatcher mage branch wired:** `rank_for_primary_archetype()` now routes mage → `rank_mage_for()` (returns `scorer="ability"`, `fell_back=False`); assassin + enchanter still fall back to ds.dps pending Phases 5-6. ENGINE_VERSION 0.66.0 → 0.67.0. Tests: 40 new (36 in new `test_rank_mage.py` covering basics + scoring + filter-pipeline parity + validation + serialization + mode/amp flow + 8 server route cases; 4 net new in `test_archetype_dispatcher.py` - new `MageRoutingTests` class with 5 tests replaces the old single mage-fallback assertion) → 1257 DS suite (was 1221); wider RC 906 green. Live-validated `/rank-mage` on Legion :8893: Veigar lvl 11 naked vs 30 MR → baseline 25.07 ability DPS, primary_scaling=AP, top 5 = Rabadon's +13.58 / Shadowflame +13.26 / Mejai's +11.65 / Stormsurge +11.44 / Void Staff +10.96; BotRK absent from top-3. **Phase 4 complete** - mage no longer falls back in the dispatcher; carry/bruiser/tank/mage all map to real scorers. **Phase 5 (Assassin burst-window) starts next.**
- ✅ **s178 - Phase 4b mage ability DPS evaluator** (2026-05-12). Second of the 3-session Phase 4 lift. New `agents/daemon_slayer/ability_dps.py` (~600 LOC) sibling of `dps.py`: `compute_ability_dps()` + `AbilityDpsResult` + `AbilitySpellDps` per-spell breakdown + `AbilityContext` resolved stats. Per-spell evaluator at rank determined by canonical Q-first / W-second / E-third max-priority table (`rank_at_level()`; configurable via `max_priority` param); R unlocks at lvl 6/11/16. Sums the first damage block's per-rank scaling from the Phase 4a `DamageBlock` schema - base + total/bonus AD + AP + caster max/bonus HP + target max/missing/current/bonus HP + target armor + caster bonus armor/MR + caster max MP - then applies mode multiplier (`aramDamageDealt` for ARAM), build-wide damage_amp + giant_slayer + target_bonus_hp_amp + magic_amp on magic-typed spells (ported from `compute_dps`), AP cross-derivations (Riftmaker HP→AP, Mejai's stacked AP, Rabadon's ap_amp, Demonic Embrace hp_ap_amp), then mitigation per damage type (PHYSICAL→target_armor curve, MAGIC→target_mr curve, TRUE→bypass, MIXED→half-half). Per-spell DPS = post-mitigation damage × measured casts/sec. New `data/daemon_slayer/spell_cast_rates.json` (172 champs × Q/W/E/R × SR/ARAM/ARENA/global, 669 buckets kept ≥ 5-sample threshold) derived by new `scripts/build_spell_cast_rates.py` from 2851 rewind matches. `ult_rates.py` extended with `get_spell_casts_per_sec(champion, key, mode)`; legacy `get_ult_casts_per_sec` preserved for Malignance Hatefog. New `/ability-dps` POST/GET route on `:8893`. Single-block + `form_index=0` defaults - multi-form (Aphelios) + multi-block (Aatrox Q chain) callers can override per-key. ENGINE_VERSION 0.65.0 → 0.66.0. Tests: 70 new (8 cast_rates + 13 rank_at_level + AbilityContext + mitigation + block-eval + primary-scaling + 16 compute_ability_dps + 5 server route) → 1221 DS suite (was 1151); wider RC 902 green. Live-validated: Veigar lvl 11 naked SR vs 30 MR → 25.07 total DPS; +Rabadon (130 AP @ ×1.30 amp = 169 effective AP) → 38.65 DPS (54% lift); Aatrox Q (PHYSICAL) vs 100 armor → exactly 50% mitigation factor applied.
- ✅ **s177 - Phase 4a champion ability ingest** (2026-05-12). First of the 3-session Phase 4 (Mage ability DPS scorer) lift. New `tools/daemon_slayer_abilities_extract.py` (~360 LOC) fetches Meraki Analytics bulk champions endpoint + normalizes each ability form into typed `damage_blocks` keyed by attribute, with per-rank scaling fields (`base`, `total_ad_pct`, `bonus_ad_pct`, `ap_pct`, `caster_max_hp_pct`, `caster_bonus_hp_pct`, `target_max_hp_pct`, `target_missing_hp_pct`, `target_current_hp_pct`, `target_bonus_hp_pct`, `target_armor_pct`, `bonus_armor_pct`, `bonus_mr_pct`, `caster_max_mp_pct`). Snapshot at `data/daemon_slayer/<patch>/champion_abilities.json` versioned alongside `champions.json` / `items.json` / `items_meraki.json`. New `agents/daemon_slayer/abilities.py` (~280 LOC) loader exposes `AbilitiesSnapshot.load()` + `AbilityForm` + `DamageBlock` dataclasses; module-level `load_default()` singleton mirrors `ult_rates.py`'s lazy-cache pattern. Multi-form abilities preserved (Aphelios 6× Q/P, Jayce/Elise/Karma/LeeSin/Nidalee/Sylas 2× per affected key). Coverage: 171/172 champions (Meraki bulk lags Zaahen), 927 ability forms / 98.4% ok / 99.3% parsed (target 80%) - 4 unparsed niche aggregates documented (Illaoi spirit reflection, Ryze legacy R, Trundle ult HP-drain, MonkeyKing clone-output scalar). ENGINE_VERSION 0.64.0 → 0.65.0. **Phase 4b lift starts next:** `compute_ability_dps()` evaluator + cast rate from `ult_rates.py` extended to P/Q/W/E + mana-economy denominator. Phase 4c wires `rank_items_by_ability_dps()` + `/rank-mage` route. Tests: 85 new (NormalizeDamageTypeTests / IsAoeTests / ValuesTupleTests / NormalizeCooldownOrCostTests / ClassifyAttributeTests / NormalizeModifiersTests / BuildDamageBlockTests / BuildFormTests / SnapshotLoadTests / AatroxQTests / VeigarQTests / EzrealQTests / EzrealRTests / MultiFormTests / DamageBlockTests / AbilityFormTests / CoverageThresholdTests / SingletonCacheTests / MissingSnapshotTests) → 1151 DS suite (was 1066).
- 🚫 **FU03 - clipboard helper** - permanently superseded by FU04 same-day approval (Personal keys don't expire; daily-renewal helper unnecessary).
- ✅ **rewind_history.db ongoing freshness - SHIPPED 2026-05-19.** Initial catchup landed s167 (now 2851 rows; newest 2025-12-16). New `RC-RewindCatchup` weekly task (Sundays 04:00) via `ops/install_RC_RewindCatchup.ps1` runs `scripts/rewind_catchup.py` (idempotent + resumable; PUUID rotation auto-resolved via Account-V1). Cadence is weekly not hourly because operator's play cadence is sparse (~5 games / 5 months 2026-05); hourly would be ~168 no-op runs/week. ExecutionTimeLimit 20 min. Live-registered + `--dry-run` proved current state ("No new matches. DB is up to date.").
- **Vision regions calibration** - tune `data/vision_regions.json` bboxes. Blocked on live game for calibration frame.
- **DS calibration pipeline** - analyzes operator's actual DS pick outcomes vs game results to tune item-curve weights. Blocked on SR ranked freshness: `rewind_history.db` now has 2900 matches (latest 2026-05-14 via RC-RewindCatchup weekly cron item 101) but Ranked Solo (queue 420) latest is still 2025-12-15 + only 0 SR records in `data/ds_calibration.jsonl` per item 125 probe. Normal Draft (queue 400) has 11 SR matches in last 14 days but the calibration delta wants ranked specifically. Waits on ~20+ ranked-game samples. (Note: this is the SEPARATE DS-calibration concern. The ADR-007 phase 2 postmortem-from-rewind death-pattern analyzer `scripts/postmortem_analyze.py` SHIPPED 2026-05-21 per CLAUDE.md item 124 + cadence task LIVE per item 125; that pipeline is the personal-context death-pattern lane, not this DS-calibration delta lane.)
- **Bridge Watcher acceptance-criteria** - need 50+ real-traffic auto-action samples (currently synthetic only). Watch `auto_ok_since_boot` vs `auto_err_since_boot` on RC heartbeat.
- ✅ **gamepc_boot.ps1 hardening** - DONE 2026-05-18 (`4da19b0`, Audit7 H-01). Root cause was config not start-sequence: both tasks shipped with `ExecutionTimeLimit=PT72H` (72h force-kill of infinite daemons) + logon-only trigger. Fixed live (Set-ScheduledTask: `PT0S` + 10-min self-heal repetition + `StartWhenAvailable`) AND in `gamepc_boot.ps1` step 5b (every-boot ensure-block mirroring RC-BridgeDaemon; served live via Legion `/agent/`, no redeploy). Detection/escalation half also shipped (`68ff0c7`): `/api/health/all` graded peer status + rollup flip + Phase-3 watchdog files a deduped Agent-1 task.

## Open items - Medium priority

- **Auto-ops verb expansion** - once Phase 3 auto-action success rate clears 95%, add: `tail .* log`, `restart agent .*`, `verify .*` to Legion `auto_ops_verbs`.
- **Game-PC + Peer auto-action lanes** - watchers installed but `--enable-auto-action-lanes` is OFF. Enable when success rate is proven.
- ✅ **RC-DaemonSlayer task context - RESOLVED 2026-05-17 (`ef30b6f`, code fix, kept SYSTEM+startup).** Root cause: `tools/start_daemon_slayer.py:_log_startup` wrote only to `logs/` and did `except OSError: pass`, so under the SYSTEM-context task a boot-time write failure vanished. Rejected the proposed ops change (SYSTEM→Administrator + startup→LogonTrigger) - DS server is **not** supervisor-watched, so a logon trigger would leave it down on an unattended reboot (availability regression to buy traceability). Instead made logging resilient: canonical `logs/` → ProgramData fallback (`_FALLBACK_LOG_FILE`, always SYSTEM-writable) → best-effort stderr echo (`sys.stderr` may be unusable under pythonw - swallowed); `_log_startup` now returns the `Path` written or `None` and never raises. TDD'd (`tests/phase7_polish/test_start_daemon_slayer_logging.py`, 8 tests + 2 helper tests; full phase7_polish 43 green; ruff clean). Live-smoked: launcher's port-bound guard line landed in the real log **and** stderr. No scheduled-task change; no reboot needed.
- ✅ **DS review + augments - bonus-AD-zeroing augment: VERIFIED not an RC bug (2026-05-17 - do not re-investigate).** External report (different League project): an Arena augment ("Adapt") zeroes bonus AD → Maw shield / Endless Hunger haste / Death's Dance heal floor out; reporter hedged it might be a modeling-accuracy nit. **Confirmed against `effects.py`:** RC models all three as `defensive_only` / **non-DPS** - Maw `3156` (lifeline magic shield, no DPS), Death's Dance `6333` (bleed, no DPS proc), Endless Hunger `2517`+`222517` (Famine bonus-AD→haste + Feast omnivamp explicitly tagged *"utility; no DPS contribution"*). They contribute **zero** to DPS ranking regardless of bonus AD, so a bonus-AD-zeroing augment cannot make DS over- or under-rank them - there is no bonus-AD-scaled credit to distort. The one lifeline-family item RC *does* credit with DPS, Sterak's Gage `3053` "Claws that Catch", is correctly keyed off **base AD** (`bonus_ad_pct_base_ad=0.45`), not bonus AD - also immune. EHP path likewise gives no credit: shield throughput is an explicit Phase-1.5 deferral in `ehp.py` (lifeline family handled only as a dead-unique dedup, never bonus-AD-scaled EHP). **The other project's diagnosis does not lift to RC.** The real, *separate* gap is the already-tracked Phase-1.5 shield/heal throughput + P1b Arena-augment `as_pct` overlay (CLAUDE #26c) - RC currently gives no Arena pick-credit for these defensive items at all, a deliberate modeling boundary, not a bonus-AD over-rank bug.

- ✅ **Mayhem/Arena augment recommender - SHIPPED 2026-05-18 (`10aa944`, pushed).** (Scoped 2026-05-17; detail retained below.) Turn the already-proven augment-OCR path (CLAUDE.md item 86 - OCR reads *which* augments are offered, 2/2 live Mayhem matches) into a data-driven *pick* recommendation. Algorithm lifted (re-implemented, no-license repo) from `ReformedDoge/Mayhem-Doctor` `src/ui/augments.js`: Laplace-smoothed per-augment WR + pairwise co-occurrence lift with `n/(n+5)` shrinkage + greedy synergy given already-picked augments. Data substrate exists: offered augments at pick-time = `state["augment_choices"]` (vision OCR, already wired into `coaches/arena_coach.py` ~L380); historical outcomes = `match_history.db` ingest → `enriched.roster[].augments` (augment IDs, per-player, win-derivable - `dashboard/builders.py` ~L684/722). Second lift: cache the static asset `/lol-game-data/assets/v1/cherry-augments.json` for authoritative id→name/icon/**rarity** (`kSilver/kGold/kPrismatic`) - richer than RC's `arena_augments.json`. Cold-start **decided 2026-05-17 - Option B (external-seed):** bootstrap from external Mayhem augment-WR, blend toward own ingested history via `w=n_own/(n_own+K)`. **External source validated + locked 2026-05-17:** Overlay App E/iesdev `aram_mayhem_augments` JSON API (`data.v2.iesdev.com/api/v1/query_objects/prod/lol/aram_mayhem_augments` - unauth, daily, true Mayhem, keyed by Riot augment id = 1:1 with `cherry-augments.json`; per-augment marginal WR only, so own-history still owns pairwise/synergy). Full scope: `Desktop/MAYHEM_AUGMENT_RECOMMENDER_PLAN_2026-05-17.md`. Non-goals (do NOT re-pitch): LCU augment API (confirmed dead, item 86), continuous DXGI screen-agent (BSOD-confirmed). **Cross-link 2026-05-17 (`open item research.txt` triage):** the Laplace + `n/(n+K)` smoothing here is now the **shared smoothed-rate primitive** (new item below) - build once, also consumed by pick/ban synergy + the s220 PGR score; algorithm cross-ref `Maelian25/lol-draft-prediction` (no license → reference). Complementary data substrate found: `aggregator K` per-champ **ARAM Mayhem** WR+runes+items (70K games @16.10, scrape-parse, no API). **SHIPPED `10aa944`:** `core/augment_external_source.py` + `core/augment_recommender.py` + `coaches/arena_coach.py` (parallel to Haiku, persists on LLM outage) + `web/js/panels/item_build.js` (augments-pill tooltip); 33 tests, ruff clean, RC pid 7632. Task-1 gate: 13 augment matches ALL Mayhem KIWI/2400, 0 Arena, n_own≈0-2 → external prior dominates by design. **Known/expected (don't "fix"):** Overlay App E `arena_augments` sibling = 0 usable rows → graceful neutral degrade (Mayhem §4-locked primary; 0 Arena own-data). **NEXT (operator-gated, not provable offline):** live Mayhem augment-select cross-check (OCR→rank vs pick made).

- ✅ **Shared smoothed-rate primitive - SHIPPED 2026-05-18 (`3f4902d`, pushed; s238). (decided 2026-05-17 - `open item research.txt` triage; operator-locked; detail retained below.)** ONE module computing a smoothed empirical rate over RC's own match DB: Laplace `(wins+α)/(games+2α)` per-key WR + Beta-smoothed pairwise co-occurrence + `n/(n+K)` shrinkage / external-seed blend. Three consumers, build once: (a) the #88 Mayhem/Arena augment recommender above (already scoped - Mayhem-Doctor is this same family); (b) **pick/ban synergy** - the panel today is only a recent-form proxy; this gives true smoothed duo/counter WR over `rewind_history.db`; (c) the s220 PGR/Deep-Review **0-100 score** - `noaboa07/Morello` `badges.ts`/`match-insights.ts` (**MIT, vendor-safe**) is the reference impl to fold in. Algorithm reference for the matrices: `Maelian25/lol-draft-prediction` (**no license → reimplement clean, don't vendor**). Build it in its own `/clear`'d session; Task-1 reuses the #88 augment item's data-audit gate (count `match_history.db` augment/participant rows by mode) - **#88 augment side now satisfies this (`10aa944`): 13 Mayhem-only matches, n_own≈0-2; `core/augment_recommender.py` already implements the Laplace `(w+α)/(g+2α)` + `n/(n+K)` family - the concrete impl to generalize into this shared module.** Sequencing: primitive first, then the three consumers thread off it. **SHIPPED `3f4902d` (s238):** `core/smoothed_rates.py` (laplace_rate / shrink / blend; 25 unit tests) + `core/augment_recommender.py` refactored onto it **provably byte-identical** (existing `tests/test_augment_recommender.py` unchanged + green = equivalence proof; every caller passes alpha=1.0 so the laplace zero-guard is unreachable, shrink/blend copied with the exact original guard semantics) + consumer (b) **pick/ban synergy** wired (`routes_pickban` synergy mood now ranks by smoothed WR not raw wins/games; displayed `wr_pct` stays the RAW observed rate; the `HAVING games>=2` floor is kept; comfort/limit/new untouched - out of the locked "pick/ban synergy" scope; +5 s238 characterization tests; verified live on the real `rewind_history.db` ally-path). Consumer (c) the s220 PGR 0-100 score is **ROADMAP-S3 (not built yet)** - the API is shaped to serve it but there is no consumer to wire; fold the Morello reference in when S3 ships. Full `tests/` 1344 / 0 failed. No ENGINE bump / no DS restart (behavior-preserving + non-engine consumer, ROADMAP-85 precedent). The optional 101.qq.com CDN seed (next item) is the only outstanding pick/ban-synergy data enrichment.
- 🔵 **101.qq.com duo-synergy capture - one-off Game-PC step (decided 2026-05-17).** Tencent-**official** CN double-rank synergy/counter data (`https://101.qq.com/#/hero-rank-double?tier=200`) - no Western equivalent. Delivery confirmed as static JSON on the `game.gtimg.cn/images/lol/...` CDN, but the exact `hero-rank-double` payload URL resists blind probing → needs ONE live browser Network-tab capture on Game-PC (champ-select-safe; never in-game). Output = the resolved URL + payload shape, recorded as an optional external synergy seed for the shared-primitive's pick/ban consumer. Pure research/data step; no code until the URL is captured.

## Future-Proofing Plan (retired 2026-05-09 - all 7 phases ✅)

Retired to `docs/_archive/RC_FUTUREPROOFING_PLAN_retired_2026-05-09.md` after all phases shipped.

| Phase | Status |
|---|---|
| 1 - Knowledge architecture | ✅ Done (fc1361b - s125+s126) |
| 2.1 - champion_profiles split | ✅ Done (s127 - 902→29 LOC + 168 JSONs) |
| 5 - CI + smoke harness | ✅ Done (d21f533 - 343→380 tests, ruff 0 violations, CI gate live) |
| 4 - Contracts/schemas | ✅ Done (791e2db - s143 dispatch soft-warn validator + 26 tests; closes s129 4.1+4.3 partial) |
| 3 - Frontend modules | ✅ Done (s137 - 3.1+3.2+3.3; 6-fixture Playwright snapshots, WS-stub isolation) |
| 2.3 - coach_integration split | ✅ Done (s138 - 1225 LOC → _profiles/\_sr\_prompt/\_coach pkg, 386 tests) |
| 6 - Bridge consolidation | ✅ Done (s144 - `tools/bridge_cli.py` + 7 shims, BridgeMetrics namespace in `core/prom_metrics.py`, 42 tests; state consolidation + 5 watcher daemons explicitly out of scope) |
| 2.2 - game_reader split | ✅ Done (ea7589e - s139, 1474 LOC → mixin pkg, 386 tests) |
| 2.4 - vision server split | ✅ Done (9cf262a - s140, 710 LOC → shim + vision_server/ pkg, 386 tests) |
| 7 - Process polish | ✅ Done (s141 48d11be + s142, phase-marker normalization + archmap journal + WAKEUP_NOTES auto-prune + Conventional Commits commit-msg hook, 412 tests) |

---

## Fleet status at a glance

| Layer | State |
|---|---|
| RC supervisor + main | Stable (pid=1108 at last check) |
| Web dashboard `:8888` | All endpoints green |
| Vision relay `:8889` | Stale-frame rejection live; auth via rotated token |
| MCP server `:8892` (Game-PC) | HTTP 200 |
| Bridge Watcher (Legion) | pid=15004 cadence=active; 24h ring live |
| Game-PC bridge daemon | watcher=alive queue=0 |
| Peer bridge daemon | watcher=alive queue=0 |
| Coaches | All 4 modes DS-before-Haiku ✅ |
| Daemon Slayer `:8893` | ENGINE 1.42.0 · 705 items · 3881 tests (1666 subtests / 1 skipped / 1 xfailed) · patch 16.10.1 · 172 champions · all 6 archetype scorers wired (s174-s181) · coach archetype-dispatch wire-in (s182) · **per-champion override registries**: max_priority (s185), combo_sequence (s186), form_index (s187 + s204 Riven seed + s205 Qiyana/AurelionSol/Renekton seed - 9 champions / 13 entries), **block_index (s191-s205 - 185 entries across 118 champions)** · **per-attack on-hit AA procs in burst** (s188) · **Spellblade-in-burst** (s189) · **Sundered Sky Lightshield Strike in burst** (s190) · **EHP Phase 1.5 shield throughput** (item 128 ENGINE 1.27.0) · **EHP Phase 6 healing throughput** (item 129 ENGINE 1.28.0) · **EHP Phase 6.5** (item 130 ENGINE 1.29.0: per-spell CC duration seam + SV amp on Phase 1.5 shields + Sundered Sky missing-HP heal) · **_PER_SPELL_CC_DURATIONS registry** (item 134 ENGINE 1.30.0 seed 30/24; item 135 ENGINE 1.31.0 wave 2 +23/+20; item 137 ENGINE 1.33.0 wave 3 +14/+14; item 138 ENGINE 1.34.0 wave 4 +15/+15; item 139 ENGINE 1.35.0 wave 5 +8/+7; item 140 ENGINE 1.36.0 wave 6 +5/+2 + registry-merge schema lift via `_build_per_spell_cc_durations()` builder fn enabling multi-wave augmentation; item 141 ENGINE 1.37.0 wave 7 +8/+7 = **103 entries / 89 champs** of first-order CC at patch 16.10.1 = 60% of 172 SR roster) · **`compute_cc_pressure` aggregator** (item 136 ENGINE 1.32.0 Slice A `81cf8a3` - FIRST aggregator consumer of `_PER_SPELL_CC_DURATIONS`; `agents/daemon_slayer/cc_pressure.py`) · **`enemy_cc_threat_line` coach prompt** (item 136 Slice B `6aff49f` - 4-mode coach consumer; `core/enemy_cc_threat_context.py`) · **`cc_blended_ehp` EHP-vs-CC blended scorer** (item 137 ENGINE 1.33.0 Slice A `99164e8` - 2nd engine-math consumer of `compute_cc_pressure`; `EhpResult.cc_blended_ehp` via `compute_ehp(enemy_champions=...)`; `_CC_EFFECTIVENESS_FACTOR = 0.5` operator-tunable) · **`cc_blended_ehp_impact_line` coach prompt** (item 138 Slice A `738c005` - 2nd coach prompt consumer wired into all 4 modes; `core/cc_blended_ehp_context.py`) · **`compute_hybrid` cc_blended_ehp scorer** (item 139 ENGINE 1.35.0 Slice A `0523511` - FIRST DS-engine SCORER consumer of cc_blended_ehp; `hybrid.py:compute_hybrid(enemy_champions=...)` threads through to 3 internal compute_ehp call sites; HybridResult.cc_blended_ehp + .enemy_champions fields) · **`cc_blended_ehp` dashboard UI consumer** (item 140 Slice A `3b6cc4b` - FIRST dashboard UI consumer of cc_blended_ehp; `dashboard/routes_cc_blended_ehp_threat.py` GET `/api/cc-blended-ehp-threat?ally=...&enemy=...&mode=...` returns ally vs enemy cc_blended_ehp ratio + tier; champ-select chip `web/js/panels/cc_blended_ehp_threat.js` mounted between bans + pickorder; the cc_blended_ehp ecosystem is now COMPLETE with 4 consumers wired: engine math + coach prompt + DS scorer + dashboard UI) · **`cc_conditional.py` forward-marker module** (item 141 ENGINE 1.37.0 Slice B `7233568` - NEW `agents/daemon_slayer/cc_conditional.py` ~360 LOC shipping the schema + machinery + 10-entry seed but NO consumer wires; FORWARD-MARKER seam mirroring s112 `STAT_GRANT_CALC_KEYS` empty seam pattern; production behavior byte-identical to 1.36.0; `ConditionalCcEntry` frozen dataclass + 10 condition tags (`COND_NTH_HIT` / `COND_GOLD_CARD` / `COND_TERRAIN` / `COND_CHANNEL_COMPLETION` / `COND_DREAM_STACK` / `COND_DEVOUR_TARGET` / `COND_TARGET_HP_BELOW` / `COND_TARGET_DEBUFFED` / `COND_DUAL_ENEMY` / `COND_MODE_GATED`) + 10 operator-tunable `_DEFAULT_CONDITION_PROBABILITY` midpoints + `get_total_conditional_cc_seconds(apply_probability)` aggregator; 10 canonical conditional entries from wave 4/5/6 REJECT lists - Brand R / TwistedFate W / JarvanIV E / TahmKench R / Volibear Q / Warwick R / Viktor W / Mordekaiser R / Sett E / Vex E) · **cc_conditional FIRST consumer wire** (item 142 ENGINE 1.38.0 Slice A `8335ce1` - `cc_pressure.compute_cc_pressure(include_conditional=False)` kwarg + 2 new CcPressureResult fields conditional_cc_seconds + conditional_entries; default False = byte-identical for 4 existing consumers; forward-marker scan updated to allowlist cc_pressure.py as FIRST authorized consumer) · **cc_conditional wave 1 expansion** (item 142 Slice B `d4253dd` - 10/10 -> 18/18; +8 entries / +8 new champs Bard Q / Karma W / Taliyah W / Kennen E / KSante Q / Ornn Q / Xayah E / Fiora W; no new condition tags) · **cc_conditional SECOND consumer wire** (item 143 ENGINE 1.39.0 Slice A - `compute_ehp(include_conditional=False)` kwarg threads through to `compute_cc_pressure(include_conditional=...)`; INDIRECT wire so forward-marker `_ALLOWED_SOURCE_FILES` stays at `{"cc_pressure.py"}`; default False = byte-identical for all 4 existing cc_blended_ehp consumers) · **cc_conditional THIRD consumer wire** (item 143 Slice B - `compute_hybrid(include_conditional=False)` kwarg threads through ALL 3 internal compute_ehp call sites via `**_ehp_kwargs` gating; HybridResult.cc_blended_ehp reflects conditional contribution automatically when opted in) · **cc_conditional wave 2 expansion** (item 143 Slice C - 18/18 -> 23/23; +5 entries / +5 new champs Maokai Q terrain / Pyke E channel_completion / Swain E channel_completion / Skarner Q nth_hit / Zilean Q nth_hit; uses only 10 existing condition tags) · **cc_conditional FOURTH consumer wire (FIRST coach prompt)** (item 144 ENGINE 1.40.0 Slice A `f39d8c8` - NEW `core/cc_conditional_impact_context.py` `cc_conditional_impact_line()` sibling of `cc_blended_ehp_impact_line`; reads `compute_cc_pressure(include_conditional=True).conditional_cc_seconds` per enemy; wired into all 4 active mode coach prompts APPEND-only) · **cc_conditional FIFTH consumer wire (FIRST dashboard UI)** (item 144 Slice B `a204480` - NEW `dashboard/routes_cc_conditional_pressure.py` GET `/api/cc-conditional-pressure?ally=...&enemy=...&mode=...` returns ally vs enemy conditional_cc_seconds + ratio + tier; champ-select chip mounted between cc_blended_ehp_threat + pickorder; tier-tinted via `data-cc-cond-tier`) · **cc_conditional wave 3 expansion** (item 144 Slice C - 23/23 -> 28/28; +5 entries / +5 new champs Aatrox Q3 nth_hit / Riven Q3 nth_hit / Yasuo Q3 nth_hit / Yone Q3 nth_hit / Leblanc E channel_completion; uses only 10 existing condition tags; cc_conditional ecosystem now COMPLETE across 5 consumer surfaces) · **cc_conditional JSON override loader** (item 145 ENGINE 1.41.0 Slice A `ebeb8c9` - operator personal calibration mirror of item 131 post_game_rubric pattern; NEW `_OVERRIDES_PATH = Path("data") / "cc_conditional_calibration.json"` (gitignored) + `_load_overrides()` fail-soft + `_apply_default_probability_overrides()` + `_apply_per_entry_overrides()` + module-load step + inline `_p()` helper threading per-entry overrides into ALL 33 ConditionalCcEntry constructions; schema `{default_condition_probability: {tag: float}, per_entry_probability: {Champion:Spell: float}}` both keys optional; out-of-range [0,1] DROPPED not clamped; bool defense LOAD-BEARING per item 131; +46 tests) · **cc_conditional wave 4 expansion** (item 145 Slice B `01cc0d5` - 28/28 -> 32/33; +5 entries / +4 new champs + 1 multi-wave coexistence Nunu R channel_completion / Yuumi Q channel_completion / Pantheon Q channel_completion / Aatrox W debuffed_target (FIRST multi-wave coexistence with Aatrox Q wave 3) / Briar Q terrain; uses only 10 existing condition tags; +63 tests) · **cc_conditional wave 5 expansion** (item 146 ENGINE 1.42.0 Slice A `fbb1354` - 33/32 -> 35/32; +2 entries / +0 net-new champs both multi-wave coexistence Briar E channel_completion (coexists with Briar Q wave 4) / TahmKench Q nth_hit (coexists with TahmKench R wave 1); uses only 10 existing condition tags; +52 tests; multi-wave coexistence count now 4 cc_conditional champions) · **_PER_SPELL_CC_DURATIONS wave 8 expansion** (item 146 Slice B `4ab11ba` - 103/89 -> 106/89; +3 entries / +0 net-new champs via multi-wave coexistence Lissandra W root / Maokai W root / Rakan R charm; multi-wave coexistence now proven at 7 unconditional champions; +38 tests; unconditional first-order CC at 16.10.1 saturated for net-new - future waves coexist on already-registered champs OR need new schema) |
| TFT | Set 17 patch 17.3 live (0e9617b) |

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Don't break the cross-Claude bridge** - `vision_token` + `mcp_token` are SEPARATE; never let one resolver fall back to the other's file.
