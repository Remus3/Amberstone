# Riot Commander - Roadmap

_Now + Next only. Full history in `docs/history_notes.md`. Aspirational in `BACKLOG.md`._

---

## Open items - High priority

_Shipped/closed entries are archived to `docs/ROADMAP_HISTORY.md` (append-only). Only open/in-flight work stays below._

**>> NEXT-UP for the loop (priority order, set 2026-06-05; the Gemini director picks ONE bounded item/cycle):**
1. **cdragon extractor off-by-one fix [item 311 NEXT]** - highest-value tracked tail; UNBLOCKS the item-17 flip. `tools/daemon_slayer_cdragon_ratio_extract.py::resolve_calc_block`: trim the leading rank-0 entry (len>=6) + fallback-guard the explosion + fractional-base blocks (TDD); regenerate sidecar; re-run WITH the flip ON; validate genuine drifts per-champ; THEN flip `prefer_cdragon_ratios` default-on + bump ENGINE + restart DS. Do this BEFORE re-attempting the flip (item 311 proved a blind flip ships a broken engine).
2. NEW daily upstream content-drift poll (item below).
3. NEW phone monitor / loop remote-control (item below).
4. **P3.2 schema-lift SHIPPED 2026-06-05 (item 315** - antitank `ap_ratio`/`ad_ratio` + optional `stats` injection, seeded Gwen P + KogMaw W, byte-identical live so NO ENGINE bump; the live-producer route wiring + ENGINE bump are the Phase-D tail). The 6 UNIVERSAL_FILES convention update remains. **P2.2 COMPLETE 2026-06-05 (item 313 swap + item 314 tail).**
Stale `Desktop\todo.md` (DS source-layering) was FULLY SHIPPED (Win1=item295, Win2=item296) + deleted 2026-06-05 - do NOT redo. Loop relaunch: raise `max_cycles` in `ops/loop/config.json` (it stopped at "max_cycles 1 reached") then run `ops/loop/launch_loop.ps1` (or the Desktop "RC Headless" launcher).

- 🔵 **NEW (queued 2026-06-05) - daily upstream content-drift poll.** A daily (or 2x/day) scheduled detector probing DDragon `versions.json` + Meraki `patchLastChanged` + the CDragon patch segment that ALERTS / auto-triggers a refresh on a CONTENT change (not fetch-time). Builds on existing infra: `tools/ddragon_mirror_refresh.py` (RC-DDragonMirrorRefresh) + RC-PatchRefresh + the item-296 Meraki content-freshness guard (`_meraki_content_patch`) + `tools/ds_windup_offset_compare.py`. Scope: a small `tools/upstream_drift_check.py` + a daily scheduled task writing a drift sentinel + an optional bridge note. Additive, low risk. Recommended.
- 🔵 **NEW (queued 2026-06-05) - phone monitoring / loop remote-control.** MONITORING works TODAY: the RC dashboard (https://legion-rc:8888) is on Tailscale -> install Tailscale on the phone to view live state from anywhere. ADD a small loop-status surface on the dashboard (reads `ops/loop/control/claude.done` + `cycle.txt` + `controller.log` tail + last git commit) for at-a-glance loop progress on mobile. CONTROL (remote commands) = use claude.ai/code remote agents (the native path) rather than bespoke phone-control; a minimal stopgap is a Tailscale-only control page writing `ops/loop/control/STOP` or a directive override. Recommended order: monitor-panel first, control later.

- ✅ **item 315 (2026-06-05; DS engine SCHEMA LIFT but NO ENGINE bump - byte-identical live, ENGINE stays 1.118.0; DS NOT restarted; non-frozen) - P3.2: antitank.py dynamic %HP scaling via optional injected ResolvedStats.** Operator-accepted design change (item 312 had flagged antitank as intentionally STATIC per item 308). One upfront AskUserQuestion surfaced the domain tension - pure %max-HP (Vayne W / Fiora P, the dominant MAX_HP kind) does NOT scale with caster AP/AD in-game; only a subset (Kog'Maw W cap, Gwen P bilinear) carries a real caster-stat term - and forked the model: operator chose the **per-row coefficient schema lift** over a blanket multiplier or a full quantitative rebuild. `AntiTankEntry` gains optional `ap_ratio` / `ad_ratio` (default 0.0, appended at END so positional construction is safe); `compute_antitank` / `_mechanism_value` take an optional `stats` (a `ResolvedStats` or any `.get("ap"/"ad")` mapping); NEW `_effective_magnitude(entry, stats)` = `base + ap*ap_ratio + ad*ad_ratio` when a seeded row meets injected stats, else base. SEEDED Gwen P (ap_ratio 0.0005) + KogMaw W (ap_ratio 0.0004); the AD path is proven via a synthetic test entry (no champion mis-seeded with a fake AD term). PURELY ADDITIVE: `stats=None` (the default, and what the `/anti-tank` route passes) is byte-identical to item 308, as is any zero-ratio row even WITH stats injected -> NO ENGINE bump, NO live-output change, NO downstream caller change (tight blast radius per the operator). The scored `AntiTankSourceEntry` / `AntiTankResult` shapes are unchanged (the effective magnitude flows into the existing `magnitude` / `value`). TDD characterization-FIRST: NEW `agents/daemon_slayer/tests/test_antitank_p3_2.py` (+24 tests - schema defaults, byte-identical static path, the additive guarantee that 12 un-seeded champs are identical under ap=800/ad=800, seeded Gwen/KogMaw math at pinned AP, synthetic ap/ad/conditional math, real-ResolvedStats==dict parity, exactly-two-rows-seeded). Live math verified: Gwen 0.85 static -> 0.95 @200AP / 0.85 @999AD (AP-only correct); KogMaw 0.926 -> 0.966 @200AP; Vayne 0.95 byte-identical @800AP/800AD. DS-dir suite 6644 passed / 1 skip / 1 xfail / 1936 subtests; root `tests/` 5032 passed / 1 skip / 85 subtests (unchanged); ruff clean; Share re-synced 315 files --check 0; ASCII-only. Relocated item 308 verbatim to docs/ROADMAP_HISTORY.md before adding this (80KB budget). **Don't-redo:** the blanket-multiplier model is REJECTED on domain grounds (it scales pure %max-HP that ignores caster stats); ratios live ONLY on `AntiTankEntry` (registry), not on the scored source. **NEXT (Phase D, gated):** wire a live caster-stat producer into the `/anti-tank` route (resolve a ResolvedStats from the POST body / live build) to ACTIVATE scaling, THEN bump ENGINE + restart DS; seed the remaining caster-stat-scaled %HP rows (the per-row scan is open). (full record: docs/LEDGER.md item 315)

- ✅ **item 314 (2026-06-05; non-engine, non-frozen; DS NOT restarted; RC restart-deferred) - P2.2 TAIL: the 4 coaches' native-emit choices decode unified onto a shared validated Pydantic model.** Follow-on to item 313, COMPLETING P2.2. The native-emit `choices` JSON decode (json.loads -> list, [] on malformed/non-list/empty) was duplicated verbatim in all 4 coaches (aram/arena/brawl `_run_coach` + SR `coach_integration/_coach.py::_write_fields`); extracted to NEW `core/coach_output.py` (`CoachOutput` pydantic v2 model + `decode_choices(raw) -> list`), the 4 sites now call `decode_choices(fields.get("choices"))`. The two TEXT extractors (base `parse_fields` + SR `_parse_response`) were deliberately NOT merged - they diverge by design (key remapping, multi-line join, different markdown regex, no 220-cap, no positional fallback) and are golden-mastered / item244-pinned. +`tests/test_coach_output_p2_2.py` (11 + 11 subtests) PARITY golden master (decode_choices == old inline block byte-for-byte); the 4 emit tests' ParserPassthroughTests source guards updated to the new wiring + behavioral checks. FULL `tests/` 5032 passed / 0 failed; ruff clean; item 313's 38 golden masters stayed green. Relocated item 307 verbatim to ROADMAP_HISTORY (80KB budget). **Don't-redo:** do NOT merge the two extractors (diverge by design); `decode_choices` is the single home for the choices decode (raw list; downstream `core.coach_choices.parse_choices` cleans). **NEXT:** P3.2 + the 6 UNIVERSAL_FILES update remain queued. (full record: docs/LEDGER.md item 314)

- ✅ **item 313 (2026-06-05; non-engine, non-frozen; DS NOT restarted; RC restart-deferred) - P2.2 structured-output / Pydantic v2 hardening of the coach LLM-parse seam.** Characterization-FIRST (operator: rigid tests before swapping the parse logic so the live coach pipelines do not break). +38 golden-master tests across 2 new files pinning `coaches/_base_coach.py::parse_fields`/`parse_field` (25 - lowercase-key contract, markdown strip, 220-trunc, positional-fallback threshold + whole-line footgun, choices-JSON passthrough, parse_field-vs-parse_fields asymmetry) + the `core/coach_choices.py` wire dict (13, shape-agnostic via to_dict/to_jsonable). Then swapped `CoachChoice` `@dataclass(frozen=True)` -> `@pydantic.dataclasses.dataclass(frozen=True)` (operator-chosen over a literal BaseModel AFTER I surfaced the blast radius: a BaseModel breaks 5 existing tests + to_dict; the pydantic dataclass stays a TRUE stdlib dataclass so `dataclasses.fields`/`asdict` + the 4 emit tests + `test_frozen_dataclass` keep working = 0 existing-test edits, wire byte-identical, + construction-time validation). Targeted 212 passed; FULL `tests/` 5020 passed / 1 skip / 74 subtests; ASCII-only. Relocated item 294 + the DS source-layering plan verbatim to `docs/ROADMAP_HISTORY.md` (item 312 had skipped the relocate-on-add 80KB-budget convention). **NEXT (deferred):** migrate the base-coach parse callers + SR `coach_integration/_coach.py::_parse_response` onto a shared validated model (golden masters guard it); P3.2 antitank dynamic %HP is the queued sibling. (full record: docs/LEDGER.md item 313)

- 🟡 **item 312 (2026-06-05; non-engine, non-frozen; DS NOT restarted) - external refactor-plan TRIAGE + P4.2 shipped.** Operator handed 3 Desktop review docs (`Claude_Refactor_Plan.md` + `Riot_Commander_Review.md` + `Riot_Commander_Deep_Dive.md`) - external AI 5-phase audit, kept OUT of the repo per the outreach rule. Triaged each proposal vs hard rules / frozen list / settled ADRs (proposals are INTENT, verified vs live code). **SHIPPED P4.2:** `RC_TESSERACT_CMD` env override in `core/vision_tesseract.py` (portability for a non-default Tesseract install; +5 CI-safe tests injecting a fake pytesseract; byte-identical when env unset; ruff clean). **REJECTED:** P3.1 amp-DRY (PREMISE FALSE - already centralized in `effects.py`, both dps.py + burst.py import it); P5.1 drop-em-dash-ban (the settled PS-ParseFile hard rule). **GATED on approval:** P1.2/P1.3/P4.1 (frozen `game_snapshot.py`/`_loop.py`/`_game_lifecycle.py`); P2.1 FastAPI rewrite (undoes ADR-008 + Vanguard-safe stdlib posture); P5.2 SQLite ledger (already solved via LEDGER relocation + 60KB budget). **ACCEPTED, own session:** P2.2 structured-output (touches live coach parse + partly redundant), P3.2 antitank dynamic %HP (a design change, antitank is intentionally static per item 308). Also cleaned `Desktop\UNIVERSAL_FILES\` to the 6 originals (19 item-310 scratch -> gitignored `_scratch/uf_dump_item310/`; `BRIEF.md` -> Desktop, out of repo). **NEXT (operator-gated):** P2.2 + P3.2 focused sessions; update the 6 UNIVERSAL_FILES templates to current conventions; frozen-file + big-rewrite phases need explicit go. Loop note: the Gemini director reads repo-only (never the Desktop), picks ONE bounded item/cycle - pin any accepted item in ROADMAP NOW to route it through the loop. (full record: docs/LEDGER.md item 312)

- ✅ **item 309 (2026-06-05; ENGINE 1.118.0; DS restarted taskkill pid 5740 + Start-ScheduledTask RC-DaemonSlayer -> 1.118.0 live-verified /health + GET/POST /extended-duel; headless-upgrade autonomous run) - DS EXTENDED-DUELING / 1v1 sustained-fight scorer, the SEVENTEENTH scored axis, built by a 10-channel roster fan-out.** Operator: "start the next DS schema lift with 10 multi-agents fan out different channels and exhaust it then /done for /clear" (17th use, 10th explicit multi-agent fan-out after items 294/297/298/299/300/301/302/303/304). Anti-tank (item 308) was EXHAUSTED; an upfront AskUserQuestion forked the axis -> operator chose **extended-dueling/1v1** (the longest-standing runner-up across items 297-308) over pick-potential/engage-threat or disengage/self-peel. The axis that owns the PROLONGED-1v1 / attrition dimension: how well a champion's OWN KIT wins a long duel PAST the burst window (the burst scorer measures front-loaded damage and the DPS scorer measures raw output vs a fixed dummy; neither answers whether the kit WINS the long fight - this is the COMPOSITE who-out-ramps / re-cycles / out-heals / out-lasts a single opponent). NEW `agents/daemon_slayer/extendedduel.py` (`ExtendedDuelEntry` per-(champion,source,kind) registry + `_EXTENDEDDUEL_KIND_WEIGHT` 4-tier [RAMP 1.0 / RESET 0.85 / DUELHEAL 0.75 / ENDURE 0.6] + `_EXTENDEDDUEL_CADENCE_MULT` 3-tier [SUSTAINED 1.0 / PERIODIC 0.8 / BURST 0.65] + `_EXTENDEDDUEL_CONDITIONAL_PROB=0.5` + `compute_extendedduel` -> `ExtendedDuelResult`). Each mechanism carries a 0..1 magnitude; `duel_score`=sum(kind_weight*cadence_mult*magnitude), conditional at 0.5; `top_kind` label + `ramps` flag (any RAMP mechanism - the champion gets STRONGER the longer the fight runs, so do not commit to a long 1v1 against her). A slot may carry TWO kinds (Aatrox P = RAMP + DUELHEAL; Nasus R = RAMP + DUELHEAL; Renekton fury = RESET + ENDURE). NEW `/extended-duel` route (POST+GET). PURELY ADDITIVE -> every existing route byte-identical. SELECTIVE but BROAD (most fighters/skirmishers carry an attrition tool, unlike sparse anti-tank). 171-champ Workflow fan-out (10 classify + 10 completeness critics, Sonnet, schema-validated, 1.05M tok / 247 tool uses / 584s) -> 283 entries / 111 champs / 81 conditional / 63 ramping / kinds RAMP 81 ENDURE 76 DUELHEAL 75 RESET 51 / cadence SUSTAINED 97 PERIODIC 95 BURST 91; I wrote engine + tests from the structured output (PROBLEMS=0). + `tools/ds_extendedduel_build.py` (reusable validate/inject generator) + `extendedduel_registry_notes.json` provenance sidecar (Share-excluded - added to the ds_share_sync exclude list, did NOT leak). Live: MasterYi 2.74 / Tryndamere 2.38 / Jax 2.12 / Aatrox 2.11 / Yone 1.66 / Fiora 1.65 / Nasus 1.60 / Renekton 1.57 / Warwick 1.55 / Vladimir 1.52 (top duelists) vs Leblanc/Veigar/Lux/Xerath/Janna/Ziggs/Zoe/Sona/Milio 0.0 (burst/artillery/utility floor); Fiora/Katarina/Cassiopeia top_kind RESET (ramps False). +32 tests; DS suite 6611 passed; tests/ ENGINE+Share-anchor subset green post-restart; ruff clean; 65 source files ENGINE pin-synced; Share 313 --check 0; CHANGELOG +1.118.0; relocated item 304 verbatim to docs/ROADMAP_HISTORY.md before adding this (80KB budget). **NEXT (Phase D):** an auto-pairing consumer crediting extended-duel into a live matchup / "do not take this lane into a long 1v1" verdict; tune the kind weights + cadence mults after a live re-rank. The extended-dueling/1v1 axis is EXHAUSTED across the roster. (full record: docs/LEDGER.md item 309)

- 🟡 **DS ability-ratio re-source mechanism SHIPPED 2026-06-03 (additive; engine cutover staged) - `tools/daemon_slayer_cdragon_ratio_extract.py`.** Directly addresses the line-below NEXT ("re-source the balance-sensitive ability RATIOS from DDragon/CDragon to cure the frozen Meraki `latest`"). DDragon ruled out (tooltip-text only, no coefficients); CDragon character-bin `mSpellCalculations` carry `{coeff, stat}` and resolve ~75-80% of damage blocks mechanically. NEW standalone resolver + sidecar (`cdragon_ability_ratios.json`) + drift report (`cdragon_ratio_drift.json` vs frozen Meraki) + 35 offline tests; engine UNTOUCHED (suite stays green). 4-champ probe: 19/26 blocks drifted vs Meraki 25.15 - material staleness confirmed. Plan + staged cutover: `docs/DS_ABILITY_RATIO_RESOURCE_PLAN.md`. **DEFAULT-OFF ENGINE SEAM SHIPPED 2026-06-05 (commit `4b782538`):** `AbilitiesSnapshot.load(prefer_cdragon_ratios=False, cdragon_root=None)` reads the `cdragon_ability_ratios.json` sidecar and prefers its `resolution=="mechanical"` blocks per-field on each primary form, with Meraki fall-back for any field/block the resolver could not resolve (or a missing sidecar). Byte-identical when OFF; +6 tests (`test_cdragon_ratio_preference.py`). Block count never changes. **NEXT:** generate the live `cdragon_ability_ratios.json` for 16.11.1 -> review the full `--drift` map -> re-pin gold/golden tests from the reviewed diff -> flip default-on + bump ENGINE + restart DS. Hard tail (conditional / buff-counter / by-level / target-HP) stays Meraki until a calc-graph resolver phase 2.

- [open, from item 283] **NEXT (gated/FUTURE, headless run 2026-06-03):** antiheal/grievous callout (competitor #2) SHIPPED item 285 (`4acf063`, `core/heal_threat.py`, deterministic Haiku-free; in-game visual capture OWED); cost prompt-restructure for aram_aug_select / tft_live_analysis CLOSED-not-viable item 286 (static portions ~62/669/51 tok are far below the 2048-tok Haiku cache floor -> caching inert; tft primary path is FROZEN single-string moon_proxy; guard `tests/test_prompt_cache_floor_item286.py`; do NOT re-attempt); inhibitor-callout in-game VISUAL capture OWED (fires only when an inhibitor is down in a live SR game); champ-select brief Haiku flip stays gated on shadow-log accrual. DS engine verified-correct across 4 lanes (pen/on-hit/crit/AoE) - do NOT re-audit. (full record: docs/LEDGER.md item 283)
- [open, from item 277] **NEXT (gated):** Anivia P / Orianna E DS resist seams (headless) ; live-game-gated (champ-select brief flip, vision self-heal validation, Phase D flag-flips, Arena PGR capture) ; PGR @N gold@10/cs@10 (needs per-participant timeline frames). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 276] **NEXT (operator-gated):** VISUAL CAPTURE PGR S3/S4 + Build Insights skill tab (?ui_mock, Legion Windows-MCP) ; PGR S5 (polish/responsive/ARAM-Arena variants) + S4 @N ; vision self-heal LIVE in-game validation ; gamepc archival dedicated slice ; Zaahen real curated build ; champ-select brief flip ; DS Phase D / Anivia P / Orianna E. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 275] **NEXT (operator-gated):** PGR S2 polish / S4-S5 (lane comparison) ; skill-WPA frontend chip / Build Insights skill-WPA tab ; champ-select brief flip after shadow-log validation ; DS Phase D flag-flips / resist exclusions (Anivia P / Orianna E). (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 273] **NEXT (operator-gated):** Build Insights view visual capture (Game-PC MCP down) + optional spells/runes-WPA extension ; champ-select brief flip after live shadow-log validation ; Phase D flag-flips (item 268/272) ; DS resist exclusions (Anivia P / Orianna E) ; gamepc archival (item 269 L8). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 269] **NEXT (operator-gated):** L6 capture ; L7 overlay launch + Phase 4+ ; Phase D live flag-flips (item 268) ; Arena auto-flavor "Mage" mislabel sweep (L4 carry) ; gamepc archival cleanup slice (L8 carry). (full record: docs/ROADMAP_HISTORY.md)
- 🟡 **Haiku-elimination program (item 265 FOUNDATIONS + item 266 WAVE 3 shipped 2026-06-02; ENGINE 1.94.0).** PRIMARY north star = drive live Haiku to ZERO via precomputed DS lookups. DONE: Lane A 1v1 matchup engine (`agents/daemon_slayer/matchup.py` `compute_matchup` + `/v2/matchup`) ; Lane B build-order tables (`data/daemon_slayer/16.11.1/build_orders_{sr,aram,arena}.json`, 516/516 cells) ; Lane C/E/P deterministic generators (`core/laning_verdicts.py` / `core/event_callouts.py` / `core/lead_projection.py`). **WAVE 3 SHIPPED (item 266, `cb46386`, no ENGINE bump):** W3A wired deterministic choices(first)+callouts+lead into `dashboard/_state_builder.py` via NEW `dashboard/_deterministic_coaching.py` (TTL-cached 3s, fail-soft, 2 new /api/state keys callouts+lead_projection) ; W3B+trade replay-validation harness (`tools/replay_matchup_validate.py`, gold + solo-kill duel ground truths) ; W3C DE-BIASED Pick&Ban targets DB (`data/daemon_slayer/16.11.1/pickban_targets.json` + `core/pickban_targets.py`) ; W3E rendered callouts+lead in RIGHT NOW (`web/js/panels/callouts.js`). **GATE FINDING:** the matchup engine validated as a COIN-FLIP vs real outcomes (gold-at-10min 48.8%, solo-kill duel 51-52% NOT significant, n thousands) -> NO coach flipped off Haiku (section 4b: do not flip blind). The matchup-derived A/B chips are SECONDARY; the correct-by-construction callouts + lead are the valid Haiku-elim wins. **NEXT (operator-gated):** matchup-engine fidelity lift (item-aware + real-level; uncertain payoff, likely a modeling ceiling) OR pivot to non-prediction surfaces (callouts/lead/build-orders already shipped) ; overlay Lane D (Electron Phase 2) ; champ_select pickban-DB flip after a counter-quality validation ; W3E callouts/lead VISUAL capture OWED (Game-PC MCP down post-1PC). **item 280 SHIPPED (2026-06-02, `77c3647`/`d68ceff`; headless):** NEW `core/aram_comp_verdict.py` deterministic ARAM swap/variant/stay engine (correct-by-construction from champions.json range/damage/frontline facts; NOT prediction) retires the `aram_team_analyzer` Haiku call (deterministic-first, Haiku fallback only for <3-champ teams); + reused its `compute_factors` to add a shadow-only enemy-itemization hint (armor/MR) to the champ-select brief substrate (`dashboard/_champ_select_deterministic.py`, served stays Haiku per the item-273 AST guard). NEXT still gated: flip the brief after shadow-log data accrues; wire the comp-verdict to a live ARAM bench surface.
- 🟡 **Live UI watch (item 243/244 NEXT, ongoing per-game).** 2026-06-01 ranked-SR Caitlyn tick verified item-211 ingest (row 1805 game_id=5572690426 detail=True, no misattribution) + item-243 live-metrics (`live_5572690426` 15 rows) + build chooser (3 clean paths) + DS-vs-enemy-comp (0 junk) all healthy; caught + addressed 3 backlog flags in item 244 (`25b0be3`: F1 champ-select archetype resolver, F2 coach `**` markdown strip, F3 retracted-as-not-a-bug). **2026-06-02 ranked-SR Ashe + ARAM-Mayhem Kai'Sa watch:** dashboard ACTIVE MATCH == `/api/state` every tick (Seraphine-ally-aware + drake-timed + CC-saturation-94% coaching), build chooser populates SR + ARAM (the Kai'Sa-ARAM "not populated" report was NOT a global bug). On-demand Legion capture (Windows-MCP Screenshot; gamepc MCP :8892 down post-1PC) x2 mid-game = **clean**. Continue ticking champ-select + in-game vs `/api/state` each ranked game; fix in-session where ADR-008-auto-served.
- 🟡 **Dashboard data-wiring gaps (item 281 review, Class B deferred 2026-06-03).** Surfaces wired in the UI but with no live producer: (1) ADAPTATION stats-view ~90 `st-*` rows (`web/js/panels/right_now.js renderStats` via setv) - source keys (cs_at_10/csd_at_15/gd_at_15/apm/reaction_time/tilt_meter/wall_collision_deaths/...) exist ONLY in post-game `core/match_metrics.py` + `core/aftergame_summary.py`, never in the live coach dict -> wall of "-" in-game; (2) `/api/ward-heat` permanently empty (`core.ward_events.record_ward` has no producer - Live Client emits no WARD_PLACED); (3) Home "Tonight's Pick" Good/Bad/Ugly hardcoded dummy (`web/index.html` data-dummy-data="tonights-pick-tips" + TODO s218, pending last-match-analyzer wire-up). P0 phantom active-match + P1 minimap-crop 502 spam already FIXED item 281 (`f6a1c52`). (full record: docs/LEDGER.md item 281)
- [open, from DS gaps roadmap sections 2+3] **NEXT (gated):** the default-ON `apply_passive_damage` flip + the 4 non-every-AA `on_hit` entries need the structured cadence + a live "saner not different" re-rank. (full record: docs/ROADMAP_HISTORY.md)
- [open, from DS unlabeled] **NEXT (operator-gated):** Phase D live flag-flips (each re-ranks, needs a real game) + the 4 different-seam exclusions + the deferred Phase 11 vision-frame collapse. (full record: docs/ROADMAP_HISTORY.md)
- [open, from DS GAP] **NEXT (operator-gated):** Phase D live flag-flips; a "read the parsed block" resist-value lift (the value-not-in-text set) + a percent-of-resist mode (Poppy/Rell) both need a NEW schema seam. (full record: docs/ROADMAP_HISTORY.md)

_2026-05-21 sweep: items 119/120/121 (the 3 headless-upgrade pass entries) archived to CLAUDE.md ledger items 119/120/121 + WAKEUP_NOTES (full fidelity; not duplicated here per ROADMAP-95 "Now + Next only")._

- 🟡 **DS-completion (next LIVE session) + Share review-gist + champ-select SR UI redesign (2026-05-31, commit e7bca41; CI green).** **Phase A(A1+A2)+B SHIPPED 2026-06-01 (item 241; commits `205a9b2`/`5d24191`/`8eb901a`/`3775c75`; pushed `9c02493..092f0c8`; CI green run 26731968996; ENGINE 1.75.0 unchanged): __init__ 4627->19-line changelog reloc to CHANGELOG.md, ability_dps.py 2792->1270 3-seam split (_per_spell_cc/_registries/_rank_mage, all re-exported), 4 stale docstrings + dead cli fn, Share re-sync 239->242. A3 SHIPPED 2026-06-01 (item 245, commit `81a8c97`; cc_conditional registry -> co-located `cc_conditional_registry.json` + thin JSON loaders, 4911->938 lines, byte-identical sha 8d2073f0, NO ENGINE bump; builders archived verbatim to `CC_CONDITIONAL_NOTES.md`, excluded from Share like CHANGELOG.md; +`tools/ds_cc_conditional_to_json.py` generator/drift-guard). Phase C1 SHIPPED 2026-06-01 (item 246, commit `24ea243`; ENGINE 1.76.0 -> 1.77.0; DS restarted 1.77.0): staged-amp block-index routing - Hwei Q f2 routes to "Maximum Damage" under `apply_ability_amps` (default byte-identical; live :8893 proof 160->560); Sion Q already resolved by `champion_block_index.json {Q:2}` (both STAGED entries removed). Phase C3 SHIPPED 2026-06-01 (item 247, commit `04dbcde`; ENGINE 1.77.0 -> 1.78.0; DS restarted): 6 of 8 exotic passives authored default-OFF in `_passive_damage_overrides.py` (Aatrox/JarvanIV/Zed/Caitlyn/Ekko/Gangplank P; +`_step_per_level` 3-tier step + float|tuple scaling + target_current_hp_pct); Gwen P (bilinear AP-on-HP) + Kai'Sa P (per-stack) STAGED own-slice. Phase C2 SHIPPED 2026-06-01 (item 247, commit `092a549`; ENGINE 1.78.0 -> 1.79.0; DS restarted 1.79.0): AA-empower seam in compute_dps (`apply_ability_amps` + `_aa_amp_multiplier` scales base-AA only, route-reachable via /dps, FORWARD-MARKER inert until Phase D authors per-champ values). Bilinear schema lift SHIPPED 2026-06-01 (item 248; ENGINE 1.79.0 -> 1.80.0; DS restarted 1.80.0): NEW `DamageBlock.bilinear_terms` (flat `factor * ctx[a] * ctx[b]` product in `_evaluate_block`, default-OFF byte-identical) + `_per_100` helper unblocks the AP-scaled %-of-HP form; SEEDED Gwen P + Aurora P + Lillia P + Renata P (all on target max HP) default-OFF; 5 bilinear-on-HP candidates STAYED STAGED (Kai'Sa per-stack / Brand+Ekko W conditional / Karma W f1+Viego P heal). Per-stack schema lift SHIPPED 2026-06-01 (item 249; ENGINE 1.80.0 -> 1.81.0; DS restarted 1.81.0): NEW `PerStackTerm` + `PassiveDamageEntry.per_stack`/`assumed_stacks`; `to_damage_block` FOLDS `field + per_stack.field * assumed_stacks` (no abilities.py/ability_dps.py change, default-OFF byte-identical); SEEDED Kai'Sa P + Darius P + Twitch P + UPGRADE Orianna P (the 15-form "per stack" scan EXHAUSTED: only these 4 are per-stack TARGET damage, the other 11 are stat steroids / stack-gain). HEAL registry SHIPPED 2026-06-01 (item 250; ENGINE 1.81.0 -> 1.82.0; DS restarted 1.82.0; commit `8d92c7a`; CI green run 26769846946): NEW `_passive_heal_overrides.py` (bilinear AP/AD-on-HP self-heals feeding `compute_ability_hps` NOT `ability_dps`) + NEW `apply_passive_heal` flag + `bilinear_ctx` eval path in `ability_hps._eval_heal_shield_block`; SEEDED Viego P + Karma W f1 + Kayn R (all target/caster-MISSING-HP scaled -> default `compute_ability_hps` byte-identical even with the flag on; surface under `resolve_target_relative`); EXHAUSTED scan: bilinear self-heal set is exactly these 3, Fiora P (flat heal) + Vladimir Q (existing heal block) documented exclusions. LINEAR HEAL registry SHIPPED 2026-06-01 (item 251; ENGINE 1.82.0 -> 1.83.0; DS restarted 1.83.0): 16 LINEAR effects-text self-heals into the same `_passive_heal_overrides` module (14 P-slot Ahri/Alistar/Aurora/Cho'Gath/Evelynn/Fiora/Gragas/Lillia/Maokai/Rek'Sai/Swain/Trundle/Xin Zhao/Yuumi + 2 SPELL-slot Rakan Q/Talon Q); one new schema bit `level_scaled` (a SPELL-slot "based on level" heal indexes its 18-element per-level tuple by champion LEVEL not spell rank, threaded via `ability_hps._eval_heal_shield_block`); 84-candidate scan EXHAUSTED (vamp / resource-restore / revive / grey-health / bespoke-product/charge/crit/form-gate documented exclusions); default-OFF byte-identical (unlike the bilinear seeds, the flat/caster-stat terms resolve non-zero under the flag without an HP assumption). AurelionSol W cross-spell seam SHIPPED (item 257, commit `c47f04b9`, ENGINE 1.87.0 -> 1.88.0; `_CROSS_SPELL_AMP_OVERRIDES` consumed at `ability_dps.py:1111`, default-OFF byte-identical) and the conditional-gate damage slices SHIPPED (item 255, commit `db52d355`, ENGINE 1.86.0 -> 1.87.0; Brand P + Ekko W in `_passive_damage_overrides.py`, default-OFF byte-identical). Only Phase D (live flag-flips + tune the 4 per-stack assumed_stacks) remains - operator-gated, NOT headless (needs a real game; section 4b do-not-flip-blind). See WAKEUP_NOTES + the CLAUDE item-255/257 entries.** SECRET gist of `Share/` auto-syncs on every Share/-touching commit (`tools/gist_share_sync.py` + Legion-local `.git/hooks/post-commit`) - the lolmath external-handoff link. `docs/DS_GAP_COMPLETION_PLAN.md` = the next LIVE session's engine work (3 refactors -> stale-doc fix -> Sion Q/Hwei Q f2 block-index amps + AA-seam-OFF + 8 exotic passives-OFF -> live flag-flips -> Share re-sync). Champ-select SR redesign shipped in 2 audited rounds (0 MUST-FIX); UI **part-3** (rune nested panel + per-category LCU push + persistence) SHIPPED 2026-06-01 (item 259, merge `ee33f20` + audit fix `fc0363e`; 5-phase UI audit ALL PASS, MUST-FIX none; non-engine, RC not restarted; spec `docs/CHAMP_SELECT_UI_SPEC.md` [DEFERRED] -> SHIPPED) - the live LCU push at a REAL champ-select is the only OWED piece (operator was mode=client). C2 AA-empower authored SHIPPED 2026-06-01 (item 258, FF `43c3d67`; ENGINE 1.88.0 -> 1.89.0; DS restarted 1.89.0; only Fiora E maps to a base-AA damage mult, the other 4 documented not-fit; default-OFF byte-identical; live saner-not-different re-rank owed = Phase D). Open sign-off: #7 7th-item slot (engine-dependent), #8 CC-cards scroll-reach in the packed My-Pick column.
- [open, from item 239] **NEXT (operator-gated):** live-game validation to flip the opt-in flags default-on; AA-empowerment amp seam in compute_dps; Gap2 exotic candidates (% HP / crit / per-stack / dot); refactor recs (ability_dps split, cc_conditional registry externalization, __init__ changelog relocation). Operator sends the `Share/` handoff (gated; notates when/to-whom). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 237] **NEXT (all operator-gated, NOT headless):** live-game validation to flip the now-route-reachable opt-in flags default-on; assassin keystone auto-enable in /rank-assassin (product decision); patch-refresh (16.11.1 current). Marginal lifts (Riot-formula-constant naming / /tenacity diagnostic / scenario invariant) REJECTED as low-value. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 234] **NEXT (operator-gated / data-blocked):** flip the 4 opt-in flags (gate_ammo/apply_mode_modifiers/aoe_targets_hit/apply_ability_haste) after LIVE-GAME validation; auto-enable assassin keystone (operator sign-off); missile combo-clock delay IF a real per-spell cast-RANGE source lands; more per_attack runes (saturated). cc_tags operator-CLOSED. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 233] **NEXT (operator-gated):** static-CD consumer needs a live ability-haste model; flip the 3 default-on flips after live validation; missile combo-clock delay IF a real per-spell cast-range source lands; auto-enable assassin keystone; more per_attack runes. cc_tags operator-CLOSED. 5 of 6 buckets wired + static-CD accessor-staged. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 232] **NEXT (operator-gated, ENGINE bump each):** 2 owed buckets missile (missile_speed + geometry -> travel-time) + static-CD (wiki static, needs name-bridge + haste model); flip apply_mode_modifiers/gate_ammo/aoe_targets_hit defaults-on after live validation; thread role from attackrange into burst rune calls; auto-enable assassin keystone; target_hp gate for Cut Down/Coup de Grace. cc_tags operator-CLOSED. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 231] **NEXT (operator-gated, ENGINE bump each):** remaining 5 owed sidecar buckets (missile needs skillshot-range source; static-CD needs slot->ability-name bridge; geometry no hitbox consumer; recharge time-step ledger; mode_modifiers Arena re-rank validate + urf/ofa/usb/nb inactive; cc_tags BLOCKED = cc_conditional operator-CLOSED); flip gate_ammo default-on after live validation; auto-enable assassin keystone in /rank-assassin default (item-230 carry, operator sign-off). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 230] **NEXT (operator-gated, ENGINE bump each):** item 225's 6 data sidecar buckets still owed engine wiring (the last item-229-NEXT carry); flip the live `/rank-assassin` to auto-pass an assassin keystone by default (re-rank validation vs real builds - operator sign-off); expand per_attack to other on-hit runes if DDragon exposes them. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 229] **NEXT (operator-gated, ENGINE bump each):** wire `rune_procs` into LIVE `rank_items_by_burst` /rank-assassin (re-ranks assassin builds - validate); mana_sim + runes as scenario_matrix metrics; per_attack runes (Lethal Tempo / Fleet Footwork / Hail of Blades) not yet modeled; item 225's 6 data sidecar buckets still owed wiring. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 227] **NEXT (operator-gated, ENGINE bump):** wire `ability_hps` v2 into the live enchanter `ds.hps` scorer (re-ranks enchanter builds - deferred from blind overnight because ability_hps is a documented lower-bound + it is a scoring-math change that must be validated vs real enchanter builds; substrate now ready); compose the 5 substrate modules into a unified V2 fight-report; expand rune_procs coverage + wire rune procs into burst/combo; expand mana_sim into a scenario_matrix metric. (full record: docs/ROADMAP_HISTORY.md)

- 🟡 **item 214 - Electron shell + in-game overlay** - PLAN SHIPPED `82ecfba` (`docs/ELECTRON_OVERLAY.md`). Move RC off the 2nd-display Chrome view to a stabilized single-screen Electron product, 3 render surfaces: (A) companion/pop-out window dockable beside the client, (B) in-game transparent always-on-top overlay (hotkey toggle, click-through <-> interactive) for mid-match HUD + on-the-fly DS/coach interactivity, (C) optional Pengu in-client panels. Topology-agnostic (`RC_ORIGIN` config; thin-client-now decoupled from 1-PC consolidation per origin-follows-load: relative fetches + `location.hostname` WS). 6-phase roadmap in the doc. **Phase 1 companion shell SHIPPED 2026-05-30 (item 223, merge `9f45984`):** NEW `rc-shell/` Electron project (config/store PURE + node:test 41/41 + main.js frameless window loading `RC_ORIGIN` default https://legion-rc:8888 + cert-trust scoped to RC origin host; npm install OK electron 33.4.11; Vanguard-safe DWM window no capture/memory/injection). VISUAL LAUNCH owed (`npm start`, operator-gated headless). **Next = Phase 2 in-game overlay** (DWM compositor transparent always-on-top + hotkey toggle; Borderless mandatory; hide on resolution-swap edges for capture reliability) - operator opens that scope. Risk-register: overlay is DWM compositor not DXGI capture (in-game continuous capture was retired, 1-PC ADR-011); Borderless mandatory; Vanguard-safe (only :2999 + LCU, no memory reads/injection).

_2026-05-23 sweep: shipped items 94-149 (May 19-22 sessions covering CLAUDE.md item numbering 94-149 = s220 PGR S2-S5 + DS audit iters + cc_blended_ehp ecosystem + cc_conditional ecosystem + obj_participation 9-col + Fleet OAuth + headless-upgrade runs) archived to docs/history_notes.md per session-workflow last-3 rule. Older shipped entries (s218-s246) kept inline below for AUTONOMOUS_AUDIT chain context._

- [open, from item 215] **Carries forward:** (a) LIVE VERIFY owed - champ-select rune/item/summoner push + in-game :2999 (operator not in client). (b) poller relay-first: if RC-LiveClientRelay dies a :8889 404 short-circuits "no game" - watch or refactor poller to prefer direct local :2999. (c) OBS DXGI match-end capture watch (lock one resolution + League Borderless while recording). (d) Game-PC cross-Claude bridge KEPT. (e) deferred: in-process vision collapse, archive gamepc_*.py. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 212] **Carries forward:** (a) minor runes (`minor_primary`/`minor_secondary`) SAVED on the user-builds page but NOT pushed to LCU (`format_for_display` + `build_perk_ids` ignore them -> default minors) - natural NEXT user-builds-page item. (b) Visual capture of rendered chooser row OWED (operator mode=client). (c) `_csvMaybePushBuildsToLCU` 4-set cap can crowd a user build out of the in-game dropdown; click-apply unaffected. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 211] **Carries forward:** (a) 15 orphan rows still have no clean target (RC was down / agent offline). Recover per-row via `tools/recover_match_via_match_v5.py --row-id <orphan>`, OR add `--clear-orphans` flag to backfill tool. (b) matches.tracked_puuid stale for older rows (Riot PUUID rotation); always re-resolve via Account-V1 for Match-V5 recovery. (c) Live Client `/allgamedata` rarely exposes gameId so performance_tracker writes game_id=0 most games; the ts-window fallback covers that. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 210] **Carries forward:** (a) 4 divergent RC page-name templates still ship in source (dashboard/routes_loadout.py:120 + lcu/lcu_client.py:401 + loadout_resolver.py:351 + agent default); filter catches all so no functional impact, but a hygiene pass could unify to single `"RC: "` prefix. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 209b] **Carries forward:** event-driven captures must avoid phases that coincide with the resolution swap (1920x1080 <-> 1440p), where a bound DXGI grab is unreliable. The discrete-event mental model ("only fires once at game end") is wrong - the resolution swap co-fires with the bound DXGI surface, so gate on a phase that settles after the swap. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 209a] **Carries forward:** none - both spawn sites locked by CI drift guard. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 208] **Carries forward:** (a) `tools/champion_loadout_align.py` scorer fix - add ranged-ADC penalty for TF/Bastionbreaker/Heartsteel/Umbral in the `carry` archetype path so next patch regen does NOT REPRODUCE the pollution and clobber these hand-fixes. Fix lives in `core/build_order.py::_score_item_for_archetype` "carry" branch. (b) Sweep `data/champion_loadouts.json` for non-coverage-gap ADCs still carrying TF + Bastionbreaker on `_archetype="carry"` paths (Sivir/Tristana/Xayah/Ashe/Draven/Lucian/Miss Fortune/Senna/Samira/Kog'Maw/Aphelios/Nilah etc.). (c) Pantheon Sup Roam path INTENTIONALLY keeps Umbral Glaive (ward-clear support tool); do NOT re-pitch as pollution. (full record: docs/ROADMAP_HISTORY.md)

- [open, from UI scale v2.1] **Carries forward:** (a) Live UI capture OWED for pages #11/12/13 Active Match SR/ARAM/Arena at next in-game window (operator out-of-game during item 184 = capture deferred). (b) Page #3 Replay flex-allocation edge CLOSED 2026-05-30 (item 222 verify): item 185 already retuned `.replay-events-list` max-height 480 -> 320 (`replay_events.css:34`) + `.replay-grid-wrap` min-height 360 (`primitives.css:33`); the "max-height: 480 saturates" carry was stale. (c) Active Match stale-detection (when liveclient.champ=null + coaching_data.mtime > N min, render should show empty "waiting for game start" state instead of stale snapshot) - **SHIPPED item 281 (`f6a1c52`):** the view-router now gates active-match on a live game (liveclient non-empty) in both `web/js/main.js _viewAutoDerive` + the Python mirror, and `build_state` honors `coach.cleared_at`, so an idle dashboard shows home/lobby instead of a stale phantom snapshot. (d) The 21 operator-exceptions across 5 EXCLUSIVE Active Match CSS files (active_match/draft_elo/spike_curve/ward_heat/cd_ledger) carry rationale in inline CSS comments (density / tooltip-micro / chart-axis); do NOT bump without operator approval. right_now.css + next.css remain SHARED (ripple to all views) + UNTOUCHED. No bulk sweep authorized for the 583 hardcoded font-size declarations across 29 CSS files - swept incrementally per-page; saturation now reached. (full record: docs/ROADMAP_HISTORY.md)

- [open, from s246] **NEXT:** AUTONOMOUS_AUDIT fully closed. Remaining pending work is operator-gated, not autonomous-safe: the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual) and the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s245] **NEXT (AUTONOMOUS_AUDIT s5):** the only remaining 4.C is `effects.py` (5692) engine-core = reviewed-only-never-autonomous (operator-gated). Other autonomous-safe work largely exhausted across s237-s245; the big pending non-engine item is the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual). Optional data: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s244] **NEXT:** (a) operator-gated - live Arena/Mayhem augment-select render proven only by snapshot fixtures (no game ran; LCU offline); validates on next Arena/Mayhem game. (b) `arena_coach` `syn` activates on next coach reload (JS degrades gracefully meanwhile). (c) Remaining autonomous-safe: #6 local DS+matchDB MCP, or effects.py split (operator-gated engine session). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s243] **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C `effects.py` (5692) engine-core reviewed-only-never-autonomous. Autonomous-safe: product opportunity #4 (augment recommender foregrounding - `core/augment_recommender.py` ships, needs marketing-pillar UI) or #6 (local MCP exposing Daemon Slayer + match DB :8893). Optional data: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)
- [open, from #89] **Carry-forward:** Arena 6x3 champ-select VISUAL capture (3 ally cells + central me+2 + 5 enemy trio cards per c3a1e23 _csvArenaPaneHtml refactor) still owed - needs operator to commit a real Arena queue + accept + CS entry. **Don't-redo:** the canonical Arena queue is 1750 (1700/1710 legacy aliases only); Practice Tool body MUST carry explicit queueId:3140; live LCU catalog at /lol-game-queues/v1/queues is the authoritative queue-ID source. (full record: docs/ROADMAP_HISTORY.md)
- 🟡 **build-ORDER go-live + Phase-3 UI render** - **UI B+C SHIPPED `2d7da9e`** (collapsible champ-select "Build Order" card + in-game `#ds-pill` next-2 glance; headless-verified node --check + 38 snapshot/view-router; go-live was already-serving from the morning RC restart, ADR-008 auto-served). **Live champ-select visual + per-page UI-audit ritual UNBLOCKED** - the ARAM/Mayhem KNOWN-BUG keystone is ✅ fixed + proven live (`3eb2e2d`, see ✅ entry below); the B-card will render in any (now-correct) ARAM/Mayhem/Arena champ-select. Original scope was operator-gated, NOT done blind overnight. (a) `/api/build-order` serves on the next RC reload; (b) the reviewed UI session renders the ordered build per the desktop plan §6b 3-option menu (recommend: vertical "Build Order" card + `#ds-pill` glance; reject horizontal-cram) - needs the per-page UI-audit ritual + a live game. Plus a small carry-branch fix: `only_item_ids` threading - CLOSED 2026-05-30 (item 222 verify): STALE, `rank_for` already threads `only_item_ids` to the dps scorer (`archetype_dispatch.py:269` -> `rank_items` -> `_filter_candidates` only_ids gate); the "needs a DS restart" was deployed by a later ENGINE bump.

- [open, from KEYSTONE] **Residual: none functional** - only the operator's own visual reassurance of the rendered bench on a future Mayhem champ-select (the data path is proven; no capture pursued - in-game continuous capture was retired, 1-PC ADR-011). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s237] **NEXT (AUTONOMOUS_AUDIT s5):** product-level opportunity #2 (per-user pick-ban) or #3 (on-demand VLM coach), operator-directed. Remaining 4.C: `adaptation_hint.py` (1602) autonomous-OK; `agents/supervisor.py` (2312) FROZEN→needs approval; `effects.py` (5692) engine-core reviewed-only-never-autonomous. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s240] **NEXT:** autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split; or product opportunity #4 (augment recommender foregrounding) / #6 (local DS+matchDB MCP). DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s239] **NEXT (AUTONOMOUS_AUDIT s5):** opportunity #3 (on-demand VLM coach - operator-gated Game-PC capture) OR autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split. 101.qq.com CDN seed still the only outstanding pick/ban data enrichment. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s242] **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C: `agents/supervisor.py` (2312) ✅ SPLIT SHIPPED s243 (`ca30daa`) - was RESOLVED NOT frozen; CLAUDE.md hard-list has `ops/rc_supervisor.py`, NOT `agents/supervisor.py`; s238's "FROZEN" label + the audit 4.C "extract _Phase3Watcher+CircuitBreaker" target both conflate the frozen sibling (those 2 classes are `ops/rc_supervisor.py:210`/`:340`, NOT in agents/supervisor.py); this file is edited routinely w/o frozen-authorization (s168/s171.8/s173/s218/s236). Split is autonomous-eligible via the s237/s242 byte-verbatim facade pattern over its REAL surface (`_QuietHandler`/`_WebServer` + `EphemeralStub*` excs + the ~780-LOC `Supervisor` god-class), with an elevated Phase-3 verify bar (Phase-3 has no restart_trigger; s236 `2329ebf` mtime-watcher auto-restarts it on the split -> confirmed post-split: Phase-3 live-restarted healthy, GET /api/env 200 (see the ✅ s243 entry at the top of this list for the full record). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s231] **NEXT:** the execute vein is largely tapped (remaining clean executes scarce - Veigar deferred-fixture, others already plain-int mapped-correctly). The autonomous conditional-seed-expansion vein is approaching exhaustion like the s223-227 pure-data sweep did; remaining `target_no_setup` candidates are mostly already-correct plain ints (conversion ROI now low - only adds value if a future Part-2 live-advisory surface consumes the downgrade). Likely flag to operator: high-ROI remaining DS work is Part-2 (live target-state plumbing - architectural, wants sign-off). DrMundo E still blocked on a `caster_low_hp` vocab decision. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s230] **NEXT (self-continuing loop):** more debuff-state-family conditional amps (each needs fresh Meraki verification - the s229/s230 lesson: ROADMAP/_meta recollection mis-names mechanics ~half the time). DrMundo E still blocked on a `caster_low_hp` vocab decision (caster-missing-HP, NOT target - flag for operator). Aatrox W needs the positional question answered architecturally, not pure-data. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s229] **NEXT:** more conditional seed-expansion on the 2-term vocab (Lux Illumination, Aatrox W chain-landed [resolve positional-vs-CC first], Fiddle Q fear-state, debuff-state family) - each needs Meraki block verification + the discrete-pair check. **DrMundo E is caster-missing-HP, not target** → needs a `caster_low_hp` vocab term, flag for operator. Literal B-2 live-plumbing **shelved as a mis-feature** (would need a separate live-advisory surface). s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s228] **NEXT (option B continues):** **Part 2** - thread real liveclient target HP%/CC into the ranking call (`coach_integration/archetype_dispatch.py` + state-builder + `/api/ds-preview`; `target_full_hp` has a clean liveclient signal, `target_no_cc` stays commits-default until a CC source exists). Plus a fast pure-data conditional seed-expansion batch (s207→s215/s217 pattern): Lux Illumination / Aatrox W chain-landed / Fiddle Q fear-state / more sleep/charm/mark amps. **DrMundo E is caster-missing-HP, not target** - needs a `caster_low_hp` vocab extension, flag for operator. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from Post Game Review aggregator G] **Carry-forward (operator-gated):** per-page UI-audit ritual on the next live PGR open. (full record: docs/ROADMAP_HISTORY.md)

- 🟡 **Live ARAM Mayhem test (carried s214 + s215)** - operator scheduled post-/clear. Authorized to monitor + fix inconsistencies live without permission. Validates: build-chooser pushes correctly to LCU runes/items/spells (now 3-variant + Experimental row), archetype-mismatch nudge surfacing on first non-trivial item buy, comp-aware row-3 tip rendering for MAYHEM role label, Phase 5.9.21 Thresh/Sona/Kalista/Malzahar ability_dps lifts riding through DS preview.
- 🟡 **Phase 3 fixture flow (steps 5-14)** - remaining canonical fixtures: champ-select 1st/5th/locked variants, active match @ 0/6/12/20/30/40 min, game summary (NEW view), last match. **PAUSED per operator 2026-05-10** while backend work catches up. Step 4 (Loading Screen) shipped in s166. Per `feedback_phase3_fixture_ritual.md` - each fixture begins with visual-hierarchy audit subagent, ends with `/done` + `/clear`.
- 🟡 **Active Match view (step 5)** - steps 1-4 shipped. Step 4 shipped s171 (`73b66ec`, 2026-05-12): static SR/ARAM map base from `/data/ddragon/16.8.1/img/map/*` + champion-dot canvas overlay polled from `/api/vision-state` at 500ms + MIA badges (`missing_for_s > 12`) + JG-gank warning band (enemy MIA > 20s in ENEMY_JUNGLE/RIVER) + view-gated polling. **Remaining (3):** zen-lock in-game + RIGHT NOW fold + bridge-pending -> dev-panel button. **Fleet view removal SHIPPED 2026-05-19** (overnight headless): dead JS poll of `/api/health/peer` (~158 LOC in `web/js/main.js`) + ~99 LOC fleet CSS in `bridge_pending.css` + 3 `body[data-view="fleet"]` selectors in `header.css` stripped. Backend `/api/health/peer` + `/api/health/all` rollup kept (other consumers). Snapshot tests 13/13 green.
- 🟡 **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- 🟡 **set_augment_intent Cherry endpoint discovery** (s170 punch list item #6) - Game-PC `tools/gamepc_lcu_agent.py:1179-1228` now implements a 4-endpoint PATCH chain handler (item 187 Slice E scaffold + item 188 Slice C `0a4ee4d` wire). Endpoint priority chain (first 2xx wins): (1) PATCH `/lol-cherry-game-intra-event/v1/augment-select` primary guess + (2) PATCH `/lol-cherry/v1/augment-select` namespace fallback + (3) PATCH `/lol-cherry-summoner/v1/augments` summoner-scope fallback + (4) POST `/lol-cherry-game-intra-event/v1/augment-select` method fallback. Body shape `{augmentId, slotIndex}` for Arena 4-augment progression rounds 1-4. All 4 failing returns a full error envelope with each attempt's status so live verification surfaces the working endpoint. **Live verification + Game-PC redeploy OWED** at next Arena 1750 champ-select augment phase via HTTP-pull dance per memory `reference_gamepc_http_server_redeploy` (Legion `py -m http.server 8765 --bind 0.0.0.0 --directory tools` + Game-PC `Invoke-WebRequest` to `.new` + sha256-verify + taskkill old pid + atomic Move-Item + pythonw relaunch) + then `RC_LIVE_ARENA=1 RC_LIVE_AUG_ID=<id> py -m pytest tests/test_set_augment_intent_handler.py::LiveAugmentSelectIntegrationTests` to log which of the 4 endpoints the live LCU exposes (shrinks attempts tuple to verified surface). Recipe + rationale: `docs/CHERRY_AUGMENT_SCAFFOLD_NOTES.md`. **Historical** (s170 -> s171 -> 2026-05-17): augment-select was first stubbed `augment_intent_unsupported` until s171 added the command to the allowlist; a live Mayhem/KIWI probe at 2026-05-17 (`Game-PC C:\RC-Agent\rc_augment_probe.py`) found neither `/lol-cherry/*` nor `/lol-game-augments/*` exposed mid-Mayhem (all 404; KIWI is event-mode, not Cherry/1750) + no `:2999` augment field (the "augment" substring is i18n tooltip noise) - the negative result on KIWI does NOT preclude the 1750/CHERRY surfaces the 4-PATCH chain now targets. Companion finding: on-demand `mcp__gamepc__capture_monitor` survived a full Mayhem match incl. match-end cleanly (1 match - distinct mechanism from the retired continuous screen-agent). OCR fallback remains a parking-lot if all 4 PATCH endpoints fail in live Arena 1750.
- 🚫 **FU03 - clipboard helper** - permanently superseded by FU04 same-day approval (Personal keys don't expire; daily-renewal helper unnecessary).
- **Vision regions calibration** - tune `data/vision_regions.json` bboxes. Blocked on live game for calibration frame.
- **DS calibration pipeline** - analyzes operator's actual DS pick outcomes vs game results to tune item-curve weights. Blocked on SR ranked freshness: `rewind_history.db` now has 2900 matches (latest 2026-05-14 via RC-RewindCatchup weekly cron item 101) but Ranked Solo (queue 420) latest is still 2025-12-15 + only 0 SR records in `data/ds_calibration.jsonl` per item 125 probe. Normal Draft (queue 400) has 11 SR matches in last 14 days but the calibration delta wants ranked specifically. Waits on ~20+ ranked-game samples. (Note: this is the SEPARATE DS-calibration concern. The ADR-007 phase 2 postmortem-from-rewind death-pattern analyzer `scripts/postmortem_analyze.py` SHIPPED 2026-05-21 per CLAUDE.md item 124 + cadence task LIVE per item 125; that pipeline is the personal-context death-pattern lane, not this DS-calibration delta lane.)
- **Bridge Watcher acceptance-criteria** - need 50+ real-traffic auto-action samples (currently synthetic only). Watch `auto_ok_since_boot` vs `auto_err_since_boot` on RC heartbeat.

## Open items - Medium priority

- 🟡 **DS 6-bucket engine wiring** (item 225 DATA shipped `e2db0c2`, 2026-05-30) - the 3 sidecars at `data/daemon_slayer/16.11.1/` (wiki_stats `mode_modifiers` + `cdragon_spell_stats.json` ammo/missile/cc-tags/geometry + `wiki_ability_stats.json` static-CD/recharge/CC-flags) are DATA-ONLY; NO engine consumes them yet. Each is its own future batch (ENGINE bump + DS restart + DPS validation): charge/ammo cadence in combo.py/dps.py, URF/OFA/USB/NB mode-mults applied like ARAM, per-ability missile speed for kited-DPS, static-CD flag to stop Haste over-credit, typed-CC for cc_conditional classification. **Operator-gated - do NOT auto-wire.** CC-duration-seconds is a confirmed CEILING (structured on no source); do NOT re-research (`docs/DS_DATA_SOURCE_SWEEP_2026-05-30.md`).
- **Auto-ops verb expansion** - once Phase 3 auto-action success rate clears 95%, add: `tail .* log`, `restart agent .*`, `verify .*` to Legion `auto_ops_verbs`.
- **Game-PC + Peer auto-action lanes** - watchers installed but `--enable-auto-action-lanes` is OFF. Enable when success rate is proven.
- 🟡 **CI Watchdog - auto-fix red CI via headless claude** (item 204 `adb2e96`, 2026-05-26) - scope `docs/CI_WATCHDOG_PLAN.md` ONLY this session. Poller every 2 min on Legion; on red main, invokes `claude -p` headless on dedicated worktree at `C:\RC-CIWatchdog\` restricted to ruff/lint/py_compile/import fixes; opens `ci-fix/<run-id>` PR; escalates after 2 strikes via ESCALATION.md + bridge. Implementation BLOCKED on operator answering 3 open questions (PR auto-merge policy / bridge escalation envelope shape / stale-fix cancellation on new commits). TDD-first per new CLAUDE.md rule.
- [open, from LCU phase] OWED on Game-PC (`tools/gamepc_phase_watcher_install.ps1`; prereq `py -m pip install bettercam websocket-client`). `aeff4a6` follow-up: added both files to `dashboard/routes_static.py:_AGENT_ALLOWED` after Game-PC reported 404; also revived RC-Supervisor (DEAD since 2026-05-24 per side-finding). (full record: docs/ROADMAP_HISTORY.md)

- [open, from Mayhem/Arena augment recommender] **NEXT (operator-gated, not provable offline):** live Mayhem augment-select cross-check (OCR→rank vs pick made). (full record: docs/ROADMAP_HISTORY.md)

## Fleet status at a glance

| Layer | State |
|---|---|
| RC supervisor + main | Stable (pid=1108 at last check) |
| Web dashboard `:8888` | All endpoints green |
| Vision relay `:8889` | Stale-frame rejection live; auth via rotated token |
| MCP server `:8892` (Game-PC) | HTTP 200 |
| Bridge Watcher (Legion) | pid=15004 cadence=active; 24h ring live |
| Game-PC bridge daemon | watcher=alive queue=0 |
| Peer bridge daemon | watcher=alive queue=0 |
| Coaches | All 4 modes DS-before-Haiku ✅ |
| Daemon Slayer `:8893` | ENGINE 1.63.0 · 705 items · 4894 tests (1781 subtests / 1 skipped / 1 xfailed) · patch 16.11.1 · 172 champions · all 6 archetype scorers wired (s174-s181) · coach archetype-dispatch wire-in (s182) · **per-champion override registries**: max_priority (s185), combo_sequence (s186), form_index (s187 + s204 Riven seed + s205 Qiyana/AurelionSol/Renekton seed - 9 champions / 13 entries), **block_index (s191-s205 - 185 entries across 118 champions)** · **per-attack on-hit AA procs in burst** (s188) · **Spellblade-in-burst** (s189) · **Sundered Sky Lightshield Strike in burst** (s190) · **EHP Phase 1.5 shield throughput** (item 128 ENGINE 1.27.0) · **EHP Phase 6 healing throughput** (item 129 ENGINE 1.28.0) · **EHP Phase 6.5** (item 130 ENGINE 1.29.0: per-spell CC duration seam + SV amp on Phase 1.5 shields + Sundered Sky missing-HP heal) · **_PER_SPELL_CC_DURATIONS registry** (item 134 ENGINE 1.30.0 seed 30/24; item 135 ENGINE 1.31.0 wave 2 +23/+20; item 137 ENGINE 1.33.0 wave 3 +14/+14; item 138 ENGINE 1.34.0 wave 4 +15/+15; item 139 ENGINE 1.35.0 wave 5 +8/+7; item 140 ENGINE 1.36.0 wave 6 +5/+2 + registry-merge schema lift via `_build_per_spell_cc_durations()` builder fn enabling multi-wave augmentation; item 141 ENGINE 1.37.0 wave 7 +8/+7 = **103 entries / 89 champs** of first-order CC at patch 16.10.1 = 60% of 172 SR roster) · **`compute_cc_pressure` aggregator** (item 136 ENGINE 1.32.0 Slice A `81cf8a3` - FIRST aggregator consumer of `_PER_SPELL_CC_DURATIONS`; `agents/daemon_slayer/cc_pressure.py`) · **`enemy_cc_threat_line` coach prompt** (item 136 Slice B `6aff49f` - 4-mode coach consumer; `core/enemy_cc_threat_context.py`) · **`cc_blended_ehp` EHP-vs-CC blended scorer** (item 137 ENGINE 1.33.0 Slice A `99164e8` - 2nd engine-math consumer of `compute_cc_pressure`; `EhpResult.cc_blended_ehp` via `compute_ehp(enemy_champions=...)`; `_CC_EFFECTIVENESS_FACTOR = 0.5` operator-tunable) · **`cc_blended_ehp_impact_line` coach prompt** (item 138 Slice A `738c005` - 2nd coach prompt consumer wired into all 4 modes; `core/cc_blended_ehp_context.py`) · **`compute_hybrid` cc_blended_ehp scorer** (item 139 ENGINE 1.35.0 Slice A `0523511` - FIRST DS-engine SCORER consumer of cc_blended_ehp; `hybrid.py:compute_hybrid(enemy_champions=...)` threads through to 3 internal compute_ehp call sites; HybridResult.cc_blended_ehp + .enemy_champions fields) · **`cc_blended_ehp` dashboard UI consumer** (item 140 Slice A `3b6cc4b` - FIRST dashboard UI consumer of cc_blended_ehp; `dashboard/routes_cc_blended_ehp_threat.py` GET `/api/cc-blended-ehp-threat?ally=...&enemy=...&mode=...` returns ally vs enemy cc_blended_ehp ratio + tier; champ-select chip `web/js/panels/cc_blended_ehp_threat.js` mounted between bans + pickorder; the cc_blended_ehp ecosystem is now COMPLETE with 4 consumers wired: engine math + coach prompt + DS scorer + dashboard UI) · **`cc_conditional.py` forward-marker module** (item 141 ENGINE 1.37.0 Slice B `7233568` - NEW `agents/daemon_slayer/cc_conditional.py` ~360 LOC shipping the schema + machinery + 10-entry seed but NO consumer wires; FORWARD-MARKER seam mirroring s112 `STAT_GRANT_CALC_KEYS` empty seam pattern; production behavior byte-identical to 1.36.0; `ConditionalCcEntry` frozen dataclass + 10 condition tags (`COND_NTH_HIT` / `COND_GOLD_CARD` / `COND_TERRAIN` / `COND_CHANNEL_COMPLETION` / `COND_DREAM_STACK` / `COND_DEVOUR_TARGET` / `COND_TARGET_HP_BELOW` / `COND_TARGET_DEBUFFED` / `COND_DUAL_ENEMY` / `COND_MODE_GATED`) + 10 operator-tunable `_DEFAULT_CONDITION_PROBABILITY` midpoints + `get_total_conditional_cc_seconds(apply_probability)` aggregator; 10 canonical conditional entries from wave 4/5/6 REJECT lists - Brand R / TwistedFate W / JarvanIV E / TahmKench R / Volibear Q / Warwick R / Viktor W / Mordekaiser R / Sett E / Vex E) · **cc_conditional FIRST consumer wire** (item 142 ENGINE 1.38.0 Slice A `8335ce1` - `cc_pressure.compute_cc_pressure(include_conditional=False)` kwarg + 2 new CcPressureResult fields conditional_cc_seconds + conditional_entries; default False = byte-identical for 4 existing consumers; forward-marker scan updated to allowlist cc_pressure.py as FIRST authorized consumer) · **cc_conditional wave 1 expansion** (item 142 Slice B `d4253dd` - 10/10 -> 18/18; +8 entries / +8 new champs Bard Q / Karma W / Taliyah W / Kennen E / KSante Q / Ornn Q / Xayah E / Fiora W; no new condition tags) · **cc_conditional SECOND consumer wire** (item 143 ENGINE 1.39.0 Slice A - `compute_ehp(include_conditional=False)` kwarg threads through to `compute_cc_pressure(include_conditional=...)`; INDIRECT wire so forward-marker `_ALLOWED_SOURCE_FILES` stays at `{"cc_pressure.py"}`; default False = byte-identical for all 4 existing cc_blended_ehp consumers) · **cc_conditional THIRD consumer wire** (item 143 Slice B - `compute_hybrid(include_conditional=False)` kwarg threads through ALL 3 internal compute_ehp call sites via `**_ehp_kwargs` gating; HybridResult.cc_blended_ehp reflects conditional contribution automatically when opted in) · **cc_conditional wave 2 expansion** (item 143 Slice C - 18/18 -> 23/23; +5 entries / +5 new champs Maokai Q terrain / Pyke E channel_completion / Swain E channel_completion / Skarner Q nth_hit / Zilean Q nth_hit; uses only 10 existing condition tags) · **cc_conditional FOURTH consumer wire (FIRST coach prompt)** (item 144 ENGINE 1.40.0 Slice A `f39d8c8` - NEW `core/cc_conditional_impact_context.py` `cc_conditional_impact_line()` sibling of `cc_blended_ehp_impact_line`; reads `compute_cc_pressure(include_conditional=True).conditional_cc_seconds` per enemy; wired into all 4 active mode coach prompts APPEND-only) · **cc_conditional FIFTH consumer wire (FIRST dashboard UI)** (item 144 Slice B `a204480` - NEW `dashboard/routes_cc_conditional_pressure.py` GET `/api/cc-conditional-pressure?ally=...&enemy=...&mode=...` returns ally vs enemy conditional_cc_seconds + ratio + tier; champ-select chip mounted between cc_blended_ehp_threat + pickorder; tier-tinted via `data-cc-cond-tier`) · **cc_conditional wave 3 expansion** (item 144 Slice C - 23/23 -> 28/28; +5 entries / +5 new champs Aatrox Q3 nth_hit / Riven Q3 nth_hit / Yasuo Q3 nth_hit / Yone Q3 nth_hit / Leblanc E channel_completion; uses only 10 existing condition tags; cc_conditional ecosystem now COMPLETE across 5 consumer surfaces) · **cc_conditional JSON override loader** (item 145 ENGINE 1.41.0 Slice A `ebeb8c9` - operator personal calibration mirror of item 131 post_game_rubric pattern; NEW `_OVERRIDES_PATH = Path("data") / "cc_conditional_calibration.json"` (gitignored) + `_load_overrides()` fail-soft + `_apply_default_probability_overrides()` + `_apply_per_entry_overrides()` + module-load step + inline `_p()` helper threading per-entry overrides into ALL 33 ConditionalCcEntry constructions; schema `{default_condition_probability: {tag: float}, per_entry_probability: {Champion:Spell: float}}` both keys optional; out-of-range [0,1] DROPPED not clamped; bool defense LOAD-BEARING per item 131; +46 tests) · **cc_conditional wave 4 expansion** (item 145 Slice B `01cc0d5` - 28/28 -> 32/33; +5 entries / +4 new champs + 1 multi-wave coexistence Nunu R channel_completion / Yuumi Q channel_completion / Pantheon Q channel_completion / Aatrox W debuffed_target (FIRST multi-wave coexistence with Aatrox Q wave 3) / Briar Q terrain; uses only 10 existing condition tags; +63 tests) · **cc_conditional wave 5 expansion** (item 146 ENGINE 1.42.0 Slice A `fbb1354` - 33/32 -> 35/32; +2 entries / +0 net-new champs both multi-wave coexistence Briar E channel_completion (coexists with Briar Q wave 4) / TahmKench Q nth_hit (coexists with TahmKench R wave 1); uses only 10 existing condition tags; +52 tests; multi-wave coexistence count now 4 cc_conditional champions) · **_PER_SPELL_CC_DURATIONS wave 8 expansion** (item 146 Slice B `4ab11ba` - 103/89 -> 106/89; +3 entries / +0 net-new champs via multi-wave coexistence Lissandra W root / Maokai W root / Rakan R charm; multi-wave coexistence now proven at 7 unconditional champions; +38 tests; unconditional first-order CC at 16.10.1 saturated for net-new - future waves coexist on already-registered champs OR need new schema) · **cc_conditional wave 6 expansion** (item 147 ENGINE 1.43.0 Slice A `1dca288` - 35/32 -> 36/32; +1 entry / +0 net-new champs multi-wave coexistence Brand Q target_debuffed 1.25s (coexists with Brand R wave 1); uses only 10 existing condition tags; +44 tests; multi-wave coexistence count now 4 cc_conditional champions Aatrox Q+W, Brand Q+R, Briar Q+E, TahmKench R+Q) · **_PER_SPELL_CC_DURATIONS wave 9 expansion** (item 147 Slice B `3c73c3b` - 106/89 -> 108/89; +2 entries / +0 net-new champs both multi-wave coexistence Chogath W silence + Malzahar Q silence; silence joins first-order CC scope per same precedent as wave 6 stasis; +37 tests; multi-wave coexistence count now 9 unconditional champions; confirms item 146 saturation prediction - audit walked all 226 unregistered spells of 89 registered champs, only 2 of 6 candidates surfaced were genuine unconditional first-order CC) · **cc_conditional wave 8 expansion** (item 151 ENGINE 1.45.0 `656c4f5` - 36/32 -> 39/35; +3 entries / +3 net-new champs Morgana R Soul Shackles channel_completion + Seraphine E Beat Drop target_debuffed + Evelynn W Allure target_debuffed; closes prior-wave REJECT carries from items 138/146/147; uses ONLY existing 12 condition tags - no new tag constants needed; +25 tests; +32 stale ENGINE pin syncs; consumer math BYTE-IDENTICAL to 1.44.0 for default include_conditional=False; Meraki parse-strip STILL BINDS for OTHER candidates - wave 8 closes ONLY candidates verifiable WITHOUT schema lift) · **cc_conditional wave 7 tag schema lift** (item 148 ENGINE 1.44.0 `2547836` - 2 NEW condition tag constants `COND_FRENZY_STATE` + `COND_RANGE_GATED` as FORWARD-MARKER seam mirroring s112 STAT_GRANT_CALC_KEYS pattern; closes item 147 carry (l) Briar frenzy + Sylas range REJECTs; both tags registered in `_DEFAULT_CONDITION_PROBABILITY` with calibrated midpoint 0.4; NO new registry entries this wave (36/32 unchanged) - schema lift unblocks future entries without separate engine bump; Meraki 16.10.1 re-verify found Sylas E2 stuns regardless of cast range (item 142 REJECT note incorrect) + Briar W parse-strip data lacks leveling detail to verify frenzy-empowered Q/R; future entries populate when Meraki-verifiable mechanic surfaces (e.g. Renekton W Fury / Aatrox post-R passive / Volibear R passive form); +29 tests NEW `test_cc_conditional_wave7_tags.py` across 7 classes; +32 stale ENGINE pin syncs across 31 DS test files; consumer math BYTE-IDENTICAL to 1.43.0 for all 5 consumer surfaces) · **cc_conditional wave 9 + Meraki extractor schema lift** (item 153 ENGINE 1.46.0 `051e606` - `tools/daemon_slayer_abilities_extract.py` schema-lifted to capture `effects_descriptions: list[str]` per form record (purely additive, +33% file size); re-extracted `data/daemon_slayer/16.10.1/champion_abilities.json` at 1.46.0; 39/35 -> 42/38; +3 entries / +3 net-new champs Renekton W Ruthless Predator frenzy_state (FIRST consumer of wave 7 forward-marker COND_FRENZY_STATE tag) + Hwei E form_index=1 Grim Visage Disable + Neeko E Empowered Root; 4 schema-lift-verified REJECTS Karma W form 1 (registry needs same-spell-slot lift) + Aatrox R (post-R fear minion-only per description) + Volibear R (Disable Duration turret-only) + Briar W (frenzy self-buff only); +58 tests `test_cc_conditional_wave9.py` 36 + `test_abilities_extract_descriptions.py` 22; consumer math BYTE-IDENTICAL to 1.45.0 for default include_conditional=False; closes the Meraki parse-strip standing constraint from items 147-152) · **cc_conditional wave 10 same-spell-slot sidecar registry schema lift** (item 154 ENGINE 1.47.0 `20b3afa` - parallel sidecar registry `_PER_SPELL_CC_CONDITIONAL_FORMS[champion][(spell,form_index)]` preserves backward compat with primary registry; `ConditionalCcEntry.form_index` optional; 42/38 -> 44/38 primary + 2 sidecar; Karma W form_index=1 R-empowered Renewal root frenzy_state (SECOND consumer of COND_FRENZY_STATE tag) + Hwei E form_index=2 Gaze of the Abyss root channel_completion; +56 tests; consumer math BYTE-IDENTICAL to 1.46.0 for default include_conditional=False) · **cc_conditional wave 11 expansion** (item 156 ENGINE 1.48.0 `5ec3d65` - 44/38 -> 46/40 (43 primary + 3 sidecar); +2 entries / +2 net-new champs Sion R Unstoppable Onslaught channel_completion + Gnar W form_index=1 Wallop frenzy_state sidecar Mega-form-gated (THIRD consumer of COND_FRENZY_STATE); +46 tests; 14 REJECT verdicts confirmed via effects_descriptions; 2 deferred wave 12+ candidates Jayce E cast_time + Singed E Mega-Adhesive overlap root; consumer math BYTE-IDENTICAL to 1.47.0 for default include_conditional=False) · **cc_conditional wave 12 expansion** (item 170 ENGINE 1.49.0 `cb834a6` - 46/40 -> 49/43 (45 primary + 4 sidecar); +3 entries / +3 net-new champs Singed E Mega-Adhesive overlap root target_debuffed (closes item 156 wave 11 schema-strip oversight - Meraki damage_blocks DOES expose per-rank Root Duration) + Alistar E Trample 5-stack stun nth_hit + Sylas E form_index=1 Abduct stun channel_completion sidecar (first Sylas first-order CC registration); +58 tests; per-tag consumer counts COND_NTH_HIT +1 / COND_TARGET_DEBUFFED +1 / COND_CHANNEL_COMPLETION +1; consumer math BYTE-IDENTICAL to 1.48.0 for default include_conditional=False; 2 REJECTS Jayce E cast_time STILL schema-blocked + Maokai R distance-gated double-counts unconditional registry) · **cc_conditional wave 13 expansion** (item 171 ENGINE 1.50.0 `8611ff3` - 49/43 -> 56/48 (51 primary + 5 sidecar); +6 primary entries / +1 sidecar / +5 net-new champs Sejuani E Permafrost 4-Frost-stack stun nth_hit + Renata Q Handshake recast bystander stun channel_completion + Shaco R Hallucinate clone-death box fear channel_completion + Fizz R Chum the Waters lure-on-champion knockup target_debuffed + Warwick E Primal Howl recast fear channel_completion (joins wave 0 Warwick R) + TahmKench W Abyssal Dive emerge stun channel_completion (FIRST 3-slot cc_conditional champ - W + Q wave 5 + R wave 0) + Aphelios Q form_index=3 Gravitum Binding Eclipse expunge root target_debuffed sidecar; +109 tests; per-tag consumer counts COND_NTH_HIT +1 / COND_TARGET_DEBUFFED +2 / COND_CHANNEL_COMPLETION +4; consumer math BYTE-IDENTICAL to 1.49.0 for default include_conditional=False) · **cc_conditional wave 14 + Meraki extractor cast_time schema lift** (item 172 ENGINE 1.51.0 `b5b0ec3` - `tools/daemon_slayer_abilities_extract.py` schema-lifted to capture `cast_time: float | None` per form via NEW `_normalize_cast_time(raw)` helper; re-extracted patch 16.10.1 (171 champs / 927 forms / 99.0% ok_rate); 56/48 -> 57/49 (51 primary + 6 sidecar); +1 sidecar entry Jayce E form_index=0 Thundering Blow cast-time root channel_completion 0.25s FIRST Jayce first-order CC registration anywhere (closes item 170 wave 12 REJECT carry); +43 tests; REJECT Maokai R distance-gated (would double-count existing unconditional Maokai R entry - same-spell-slot coexistence machinery STILL operator-gated) + LeeSin R 0.25s unconditional + KSante R self-buff immunity; consumer math BYTE-IDENTICAL to 1.50.0 for default include_conditional=False) · **cc_conditional wave 15 expansion** (item 173 ENGINE 1.52.0 `2129e6c` - 57/49 -> 60/51 (53 primary + 7 sidecar); +2 primary entries / +1 sidecar / +3 net-new champs Zac Q Stretching Strikes 2nd-strike different-target root nth_hit (first Zac first-order CC anywhere) + Ornn E Searing Charge terrain-collision stun (FIRST consumer of COND_TERRAIN forward-marker tag since wave 7; multi-wave coexistence with wave 4 Ornn Q) + Rell W form_index=0 Ferromancy Crash Down stun channel_completion sidecar after 0.625s leap cast_time; +79 tests; per-tag consumer growth COND_NTH_HIT +1 / COND_TERRAIN +1 (first consumer) / COND_CHANNEL_COMPLETION +1; consumer math BYTE-IDENTICAL to 1.51.0 for default include_conditional=False) · **cc_conditional wave 16 expansion** (item 174 ENGINE 1.53.0 `45955fc` - 60/51 -> 64/54 (57 primary + 7 sidecar); +4 primary entries / +3 net-new champs Garen Q Decisive Strike empowered-AA silence nth_hit (FIRST Garen first-order CC anywhere) + Syndra E Scatter the Weak Dark-Sphere knockback stun target_debuffed (FIRST Syndra first-order CC anywhere) + Udyr E Blazing Stampede empowered-AA pounce stun nth_hit (FIRST Udyr first-order CC anywhere) + KSante W Path Maker recast channel_completion stun multi-wave coexistence with wave 1 KSante Q; +84 tests; per-tag consumer growth COND_NTH_HIT +2 / COND_TARGET_DEBUFFED +1 / COND_CHANNEL_COMPLETION +1; consumer math BYTE-IDENTICAL to 1.52.0 for default include_conditional=False) · **cc_conditional wave 17 expansion** (item 175 ENGINE 1.54.0 `3fa56a1` - 64/54 -> 65/54 (58 primary + 7 sidecar); +1 primary entry / +0 net-new champs / +1 condition tag schema lift Taliyah E Unweaver's Wall traversal knockup nth_hit 0.75s multi-wave coexistence with Taliyah W wave 1 (Taliyah becomes 2-slot cc_conditional champion); NEW COND_TRAVERSE tag constant + midpoint 0.3 in _DEFAULT_CONDITION_PROBABILITY (13th condition tag); +60 tests; per-tag consumer growth COND_TRAVERSE +1 (FIRST consumer of new tag); consumer math BYTE-IDENTICAL to 1.53.0 for default include_conditional=False) · **cc_conditional wave 18 FULL schema lift** (item 176 ENGINE 1.55.0 `c066a8a` - 65/54 -> 67/54 (60 primary + 7 sidecar); NEW `coexists_with_unconditional: bool = False` field on ConditionalCcEntry + consumer-side MAX-rule semantics in `compute_cc_pressure()` (when True, conditional entry coexists with same-spell-slot unconditional entry in `_PER_SPELL_CC_DURATIONS`; consumer credits MAX per slot - never double-sums); +2 primary entries / +0 net-new champs Maokai R Nature's Grasp distance-gated root COND_RANGE_GATED 0.4 coexists_with_unconditional=True (FIRST consumer of COND_RANGE_GATED forward-marker tag since wave 7 schema lift; Maokai becomes 2-slot cc_conditional champion Q wave 2 + R wave 18) + Briar R Certain Death Hematomania impact fear COND_TARGET_DEBUFFED 0.5 (SECOND 3-slot cc_conditional champion after TahmKench wave 13 - Q wave 4 + E wave 5 + R wave 18; FIRST Briar R first-order CC anywhere); +32 tests; per-tag consumer growth COND_RANGE_GATED +1 (FIRST consumer closes wave 7 forward-marker contract) + COND_TARGET_DEBUFFED +1; consumer math BYTE-IDENTICAL to 1.54.0 for default include_conditional=False) · **cc_conditional wave 19 Meraki extractor state-tracking schema lift** (item 177 ENGINE 1.56.0 `487981f` - `tools/daemon_slayer_abilities_extract.py` schema-lifted to capture `parent_resource: str | None` per spell form via NEW `_build_form(parent_resource=...)` signature threaded from champion-level `payload["resource"]` (FURY / BLOOD_WELL / FRENZY / RAGE / HEAT / ENERGY / GRIT / MANA); re-extracted 16.10.1 (171 champs / 927 forms / 99.0% ok_rate); 67/54 unchanged (0 net-new entries); ALL 6 brief-named wave-7 schema-lift carries verified ALREADY-SHIPPED via prior waves (Renekton W wave 9 frenzy_state / Karma W form 1 wave 10 sidecar / Hwei E form 2 wave 10 / Neeko E wave 1) + 3 REJECT-verified (Aatrox post-R minion-only / Volibear R turret-only / Briar W self-buff); schema lift ships as forward-marker infrastructure for future COND_FRENZY_STATE consumers beyond Renekton W + Karma W + Gnar W; +30 tests; consumer math BYTE-IDENTICAL to 1.55.0 for default include_conditional=False) · **cc_conditional wave 20 + Meraki extractor notes schema lift** (item 188 ENGINE 1.58.0 `7530002` - `tools/daemon_slayer_abilities_extract.py` schema-lifted to capture `notes: str | None` per spell form via NEW `_build_form(notes=...)` signature (purely additive parallel to cast_time/effects_descriptions/parent_resource); re-extracted 16.10.1 (171 champs / 927 forms / 98.8% notes coverage); 67/54 -> 68/55 (61 primary + 7 sidecar); +1 entry / +1 net-new champion Vayne; SHIP Vayne E Condemn terrain-collision stun 1.5s primary COND_TERRAIN 0.3 coexists_with_unconditional=True (SECOND consumer of COND_TERRAIN after Ornn E wave 15; coexists with 0.5s knockback in `_PER_SPELL_CC_DURATIONS`; FIRST Vayne first-order CC anywhere); 3 schema-lift-verified REJECTS Kindred E (slow 1s + missing-HP crit damage only no hard CC) + Diana P (pure AS bonus no CC) + Vayne P (pure MS bonus no CC); +37 tests `test_cc_conditional_wave20.py`; consumer math BYTE-IDENTICAL to 1.57.0 for default include_conditional=False) · **cc_conditional wave 21 expansion** (item 189 ENGINE 1.59.0 `4d60420` - cc_conditional re-audit of prior wave REJECT carries against item 188 `notes` field schema lift; 68/55 -> 70/56 (62 primary + 7 sidecar); +2 primary entries / +1 net-new champion - Xin Zhao becomes 2-slot champion (Q + R landing in same wave); SHIP Xin Zhao Q Three Talon Strike 3rd-attack knockup primary COND_NTH_HIT 0.7 0.75s flat all 5 ranks (FIRST Xin Zhao Q first-order CC anywhere; COND_NTH_HIT docstring's own canonical motivating example - waves 0-20 never added it) + Xin Zhao R Crescent Sweep target-NOT-Challenged stun primary COND_TARGET_DEBUFFED 0.5 0.75s flat all 3 ranks INVERSE-gated semantic (SECOND INVERSE-gated COND_TARGET_DEBUFFED after Briar R wave 18; corrects wave 15 REJECT carry; FIRST Xin Zhao R first-order CC anywhere); 38 REJECT verdicts (~11 unconditional candidates belonging in `_PER_SPELL_CC_DURATIONS`; rest false-positive regex hits / self-CC / cross-references); +51 tests `test_cc_conditional_wave21.py`; consumer math BYTE-IDENTICAL to 1.58.0 for default include_conditional=False) · **cc_conditional wave 22 expansion** (item 199 ENGINE 1.60.0 `c16c3a2` - 70/56 -> 71/56 (62 primary + 8 sidecar); +1 sidecar entry / +0 net-new champs Rell W form_index=1 Ferromancy: Mount Up empowered-AA stun COND_CHANNEL_COMPLETION 0.4 0.6s flat all 5 W ranks 2-cast cycle form 0 -> form 1 within 3.5s -> empowered-AA charge-delivery; FOURTH same-spell-slot multi-form-coexistence sidecar after Karma W 0+1 / Hwei E 0+1+2 / Sylas E 0+1; coexists with wave 15 Rell W form 0 sidecar on same (Rell, W) slot via form_index discriminator; mechanic evidence Meraki cast_time=0.25 form 1 vs cast_time=0.625 form 0; +45 tests `test_cc_conditional_wave22.py`; consumer math BYTE-IDENTICAL to 1.59.0 for default include_conditional=False) · **cc_conditional wave 23 expansion** (item 202 ENGINE 1.61.0 `084cc50` - 71/56 -> 72/57 (63 primary + 8 sidecar); +1 primary entry / +1 net-new champion Hecarim R Onslaught of Shadows distance-gated fear 1.5s primary COND_RANGE_GATED 0.4 coexists_with_unconditional=True (SECOND consumer of wave 7 forward-marker COND_RANGE_GATED tag after Maokai R wave 18; coexists with unconditional Hecarim R 1.0s baseline; FIRST Hecarim cc_conditional entry anywhere); 6 REJECT verdicts via audit subagent (Jhin W / Anivia Q vague duration / Heimerdinger E spatial / Lillia R COND_DREAM_STACK empty / Lissandra W+R / Jinx E unconditional); +27 tests `test_cc_conditional_wave23.py`; consumer math BYTE-IDENTICAL to 1.60.0 for default include_conditional=False) |
| TFT | Set 17 patch 17.3 live (0e9617b) |

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Don't break the cross-Claude bridge** - `vision_token` + `mcp_token` are SEPARATE; never let one resolver fall back to the other's file.
