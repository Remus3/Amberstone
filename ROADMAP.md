# Riot Commander - Roadmap

_Now + Next only. Full history in `docs/history_notes.md`. Aspirational in `BACKLOG.md`._

---

## Open items - High priority

_Shipped/closed entries are archived to `docs/ROADMAP_HISTORY.md` (append-only). Only open/in-flight work stays below._

- ✅ **FULL HEXTECH UI REDESIGN COMPLETE (2026-06-22, gemini-directed, 17 commits, CI green).** All 12 dashboard pages + the in-game overlay reskinned onto a new shared Hextech design-system foundation (`web/css/hextech.css` + base/tokens palette cutover). Dashboard UN-RETIRED (reverses the 2026-06-21 retirement). Plan + per-page status + deferred lifts: `docs/RC2_REDESIGN_PLAN.md`; ledger item 589. Final gate 228 tests green; per-page captures in `tests/snapshot_panels/screenshots/`.
- ✅ **Redesign FEATURE-LIFT tail COMPLETE (2026-06-22, gemini-directed, 6 commits, CI green; ledger item 590).** Shipped: champ-select counter-pick hero (`4d0172b6`) + ally AD/AP meter (`66375d63`, new /api/champ-select/team-damage-mix); home last-20 W/L strip + WR (`c3bf040d`, last20 in /api/home/summary); PGR score-decomposition 5-bar (`a461980b`; carry-metrics were already shipped s219); history season WR + client-side filters (`c0ce9faf`); controllable ready-check auto-accept (`8e972a14`, new core/auto_accept_pref + /api/lcu/auto-accept + operator-authorized minimal frozen gate, unified into the existing lobby toggle). DROPPED: PGR @15 (no backing data - deferred to a data epic, gemini ruling). Detail: `docs/RC2_REDESIGN_PLAN.md`.
- 🟡 **Redesign tail residuals (minor).** OWED: live in-game overlay capture (eye-line + S0 pulse) - no live game during the lift session. Not-built: lobby last-session recap (low value - the data already renders in the Session view via /api/session/summary; gemini deprioritized it).

- 🔴 **HEADLESS OVERLAY-POLISH RUN ARMED (operator directive 2026-06-21, gemini+ahk loop). TOP PRIORITY THIS RUN - route EVERY cycle here until the in-game Electron overlay UI matches the agreed finished design: minimalistic, out of the way, glance-first.** Source specs: `docs/research/RC2_OVERLAY_CONDENSATION_SPEC.md` (S0/S1 condensation + per-element glance floors) + GREENLIT Hextech `docs/design/RC2_DESIGN.html` + `docs/ELECTRON_OVERLAY.md` sec 6/7. Scope = `web/css/overlay.css` + the overlay renderers in `web/js/panels/*` + `web/js/overlay_*.js` + `rc-shell/` surface gating; Tier-1 frontend (overlay.css NOT frozen, asset-hash auto-reload per ADR-008). Each cycle: ONE bounded overlay-polish slice, root-cause-first, the 3b 5-phase fixture audit RUN on the legion-rc origin (NOT 127.0.0.1, which masks mixed-content bugs) with every MUST-FIX resolved in-slice, and `tests/snapshot_panels/test_overlay_view.py` updated on any panelset-contract change. **Verification cadence (operator 2026-06-21):** a live practice match is available THROUGHOUT this run - the operator keeps restarting matches over ~2 hours - so do live in-game overlay capture verification EACH cycle NOW (Playwright `?overlay=1` on the legion-rc origin against live `/api/state`, plus the `:8889` in-game frame when a game is up), not a one-time post-30min gate. A live game may briefly be absent between restarts; if so, verify against the live backend and note the in-game frame as OWED for that cycle, do not block.
  - **Concrete queue (newest-first):** (1) FIGHT MODEL pane `#am-pane-ovds` DEFAULT-subset clip - **RESOLVED / already shipped** (verified 2026-06-21 headless continue 6). The doctrine widget-field model (commit `7bd7d6a3`) already gates `w-ovds` to the `build` panelset ONLY: overlay.css section 4d hides it in default/coach/threat (L418/421/437) and shows it only in build (L451), and the pane is a `fit-content` `.ovx-widget` (no height cap, inner `overflow:visible`) so it auto-sizes and no longer clips. 25 Playwright snapshot tests render-assert `#am-pane-ovds`=none in default + shown in build. This cycle corrected only the now-stale `overlay_ds_controls.css` comment (it said "base subset + the build panelset" / "section 4b"; reality is build-ONLY / "section 4d"). Do NOT re-chase the 662/1305 clip - that was the pre-widget-field grid behavior. (2) CALL focal hierarchy SHIPPED item 559 (commit a4d6e6ba) - do NOT redo. (3) L1 minimap-anchored objective timers - DEFERRED item 566 (spec marks L1 out-of-scope l40-42; Live Client :2999 has no coords so ZOI/anchoring is ungroundable; ETAs already ship as w-callouts). (4) L3 live spike cue - DONE item 565 (shipped as w-spike). (5) sweep vs the condensation spec - DONE item 566 (overlay impl confirmed clean; palette-token + doc-drift fixes only). **Don't-redo:** the WS mixed-content + backgroundThrottling + SR-PRACTICETOOL + ds-503 + ASCII-arrow fixes are DONE (items 557/558/559) - do NOT re-litigate.
  - **SHIPPED 2026-06-22 (overlay-polish slice; commit `41c6ae50`, Tier-1 frontend, 27/27 overlay tests green; ledger item 591): the band-color CHANNEL restored on the PRIMARY `w-call` ACTION line (doctrine section 5/6).** A read-only Plan gap-analysis vs `docs/research/RC2_OVERLAY_CONDENSATION_SPEC.md` + `docs/OVERLAY_DOCTRINE.md` narrowly REFUTED the standing "DRAINED" claim: the spec->doctrine dock-retirement (item 564) moved S0 from `#rn-action` to `w-call` but dropped the band-color binding (it lived on `right_now.css .action-urgent`, now `#rn-action` = `display:none`), so the overlay ACTION line rendered flat WHITE for every coach band (`overlay.css:298-303`) - urgent/good read identically, and the `overlay.css:570` combat-shed comment ("the band color carry the read") described behavior that did not exist. `web/js/panels/active_match.js` now stamps `data-call-band` from `classifyAction` (overlay-shell-gated -> 1920 dashboard byte-identical) + prepends an ASCII-safe `String.fromCharCode` band glyph (U+26A0/2713/25BA); `web/css/overlay.css` keys a glyph color + 3px color-bar left border off the band (urgent=red reserved pop-out / fight=gold / good=green), verb stays white (doctrine 164). RED-first `test_overlay_call_action_band_channel` (band==classifyAction(verb) WIRING + token colors + white verb). Live-verified on the **legion-rc:8888 origin** (band=good, green glyph+bar, white verb) - vs the LIVE backend, not just the fixture. **The overlay-polish lane is now genuinely DRAINED again** - this was the single remaining buildable (d) slice; the rest is already-shipped / operator-gated-flip / Phase-4-deferred (per the gap analysis). VISUAL OWED: live in-game capture (no game this cycle, mode=client / liveclient null).
  - **SHIPPED 2026-06-22 (headless continue 22; commit `e0f0ffac`, Tier-1 frontend competitor-lift, DIRECTOR REFILL R20):** the overlay-polish lane stays DRAINED, so the Gemini director picked an off-lane **Section-7b competitor deep-dive lift of Aggregator N** (`docs/COMPETITOR_LIFT_2026-06-22_AGGREGATOR_N.md`, F1-F7 6-point checklist). Headline F1 SHIPPED IN-RUN: **ARAM per-champion balance-adjustment grid panel.** RC already loads Riot's full 7-field ARAM modifier grid (aramDamageDealt/Taken/Healing/Shielding/Tenacity/AbilityHaste/AttackSpeed) into `data/daemon_slayer/<patch>/champions.json` but consumed only dealt+taken, and ONLY as a coach-prompt line (`core/aram_balance_context.py`) - ZERO web panel rendered the numbers. New read-side surface: `balance_grid_for`/`balance_grid_map` accessors + `GET /api/aram-balance` (`dashboard/routes_aram_balance.py`, 134 non-neutral champs at 16.12.1) + mode-gated `web/js/panels/aram_balance.js` grid (self from `coach.champion` always; ally/enemy from `liveclient.allPlayers`; signed green/red deltas; AH additive). Presentation-only over existing local data - NO new dependency, NO Claude/Riot call, NO DS schema lift, NO ENGINE_VERSION bump (held 1.151.0). RED-first `tests/test_aram_balance_grid.py` (13 cases); route live-proven (200 ok, Aatrox +5%); 1 worktree build agent + verifier CONFIRM. Closed-negative respected (Aggregator N Arena augment WINRATE is Riot-policy-forbidden; pick-rate only). VISUAL OWED: the populated ARAM-mode capture (panel is mode-gated; this headless cycle cannot drive ui_mock - preview won't attach to the live RC-owned `:8888`, computer-use needs an interactive approval). F2-F7 deferred (already-have / forbidden-source / new-domain).
  - **SHIPPED 2026-06-22 (headless continue 21; commit `ee673c1d`, DS schema-surface ds-sweep, byte-identical, DIRECTOR REFILL R19):** the overlay-polish lane stays DRAINED, so the Gemini director picked the off-lane **R19 DS schema lift - survivability per-rank PERCENT damage reduction**: NEW forward-marker accessor `DataSnapshot.spell_damage_reduction_pct(champ_id, slot)` surfacing the percent damage-taken reduction from `champion_abilities.json` defensive modifier blocks (the `modifier_blocks` taxonomy already classified them `defensive_self` but no accessor exposed the numeric %). Lazy + frozen-safe; the pure-% units filter excludes flat reductions (Amumu E, Leona W) + per-stat scaling sub-modifiers. 8 champs at 16.12.1 (Alistar R 55/65/75, Galio W magic+physical, Garen/Gragas/MasterYi W, Belveth/Braum/Warwick E). **ENGINE_VERSION HELD at 1.151.0 (NOT bumped) - gemini director ruling B (synchronous gemini_ask) reconciled the directive's "bump" instruction with its "byte-identical forward-marker" framing: no consumer = byte-identical output, so the item-339/343 no-bump convention holds (avoids ~70 version-pin edits).** Share mirror re-synced (--check green, 373) + DS :8893 restarted (loads the code; /health stays 1.151.0). RED-first `test_spell_damage_reduction_pct.py` 14 cases (13 red first); DS 7511 + RC 9378 passed (the 12 RC fails all pre-existing, 0 regressions). A survivability/EHP consumer reading the % is the FUTURE lift; live default-ON wiring EXCLUDED.
  - **SHIPPED 2026-06-22 (headless continue 19; commit `5f308036`, Tier-2 ds-sweep, DIRECTOR REFILL R17):** the overlay-polish lane stays DRAINED, so the Gemini director picked the off-lane **R17 DS schema lift - anti-tank level-ramp %max-HP endpoints** (`AntiTankEntry.ramp_lo`/`ramp_hi` END-appended default 0.0 + optional `compute_antitank(level=)`; mirrors the P3.2 ap/ad default-OFF seam). The hand-tuned magnitude encodes late-game (max-ramp) reliability; a ramp-seeded row scales by `lerp(ramp_lo,ramp_hi,(level-1)/17)/ramp_hi` when a level is injected - `level=None` (route default) AND `level=18` byte-identical, every un-ramped row byte-identical at any level. Seeded 10 verified MAX_HP champion-level ramps from the 16.12.1 source_quotes (Aatrox 4:8, Brand 8:12, KSante 1:2, Mordekaiser 1:5, Ornn 10:18, Renata 1:2, Skarner 5:9, Urgot 2:6, Zed 6:10, Zeri 1:11; ground truth is 10 not the directive's ~16 estimate - the rest is rank-scaled/flat). ENGINE 1.150.0 -> 1.151.0, DS :8893 bounced, Share re-synced same commit; RED-first `test_antitank_ramp_r17.py` (25); DS 7497 + antitank cluster 94 green; RC 9367 (the 6 remaining pre-existing, reproduced on base `b00c4dc7`). verifier CONFIRM. Live default-ON flip EXCLUDED -> `docs/LIVE_GAME_GATED_SYNC.md`.
  - **SHIPPED 2026-06-21 (headless continue 11; commit `17128058`, CI green; Tier-1 pure-JS, ZERO live render delta):** spec section-9 **Q2 lethal-incoming S0 predicate WIRED** - the priority-100 EMERGENCY cue had a complete consumer (`overlay_priority.js` rates `lethal`=100 / tier emergency) but NO producer, so the S0 emergency tier was structurally suppressed. `signalFromState` now DERIVES it from the coach's own band + `hp_pct`: `_isLethalAtFight(band, hpPct)` fires iff band in {fight,urgent} AND `0 < hp_pct <= 25` (LETHAL_HP_PCT, 0-100 scale). Conservative by construction (a dead/missing-HP dropout or a safe low-HP recall can NEVER false-fire the reserved red pop-out, A2); explicit `lethal_incoming` passthrough preserved. SHADOW-only (the live flip stays operator-gated in `docs/LIVE_GAME_GATED_SYNC.md`; `body[data-fight]` provably unchanged since `isFightCue` was already true in both combat bands -> no electron relaunch). +8 TDD, 37/37 pass, render-contract 25/25, verifier-gated SHIP. NO backend/engine/DS/Share/ENGINE_VERSION/schema change. **NEXT:** the lethal cue is READY for the operator-gated shadow->authoritative S0 flip; remaining overlay-vs-spec deltas are Phase-4-deferred (magnitude-as-bar) or already shipped.
  - **SHIPPED 2026-06-21 (headless continue; commits `cfdc9f22`/`97d781ec`/`72509d5c`/`ada17002`/`fc438c24`/`dd9aae68`, all CI green; live-verified over a real SR game across 3 rc-shell relaunches):** the doctrine widget-field is now test-pinned + polished. (a) rewrote the 10 retired-dock snapshot tests to the widget-field model + aligned `overlay.css` panel-set presets to the doctrine DELTA model (coach core persists; build=+w-build+w-ovds-w-threat, threat=+w-threat-w-build-w-choices) + fixed the `#rn-choices` id-mount specificity leak (chips leaked into build/threat); (b) CSS polish: OBJECTIVE 2-line clamp, hex-notch corner brackets, combat declutter; (c) rc-shell DURABLE widget-layout mirror (`rc-shell-state.json` `overlay.widgetLayout`) + **Alt+Shift+R** field-reset hotkey (preload+main IPC, pure `overlay_state` helpers, +13 node tests); (d) NEW cues - **w-spike** = queue #4 L3 live spike cue (`spike_cue.js`, ult-level 6/11/16 cross from `liveclient.level`, transient one-shot) + **w-trinket** (`ward_cue` registered as a movable widget); (e) CALL tiering re-keyed off `data-call-line` (the rows are CONDITIONAL - live OBJECTIVE was the 2nd child) with `!important` over `_line()` inline styles -> labels drop, cyan/white/faint tiers + the clamp all hold live. **NEXT in this lane:** queue #3 L1 minimap-anchored objective timers; the rc-shell widget-layout IPC live round-trip (drag->persist->relaunch) is wired + unit-tested; the live Electron round-trip is now DONE (item 566).
  - **SHIPPED 2026-06-21 (headless continue 2; commit `ae09ab27`, CI green):** (a) LIVE-VERIFIED the widget-layout durable-mirror round-trip over a real SR game - seeded `overlay.widgetLayout` in `<userData>/rc-shell-state.json` -> relaunch RESTORES the widget position; cleared -> section-4 defaults (== reset); the persist helpers preserve widgetLayout+settings across the boot bounds-save. The pointer-DRAG + Alt+Shift+A/R HOTKEY stay node-test-covered (synthesized hotkeys leak to League; physical presses work - memory `reference_overlay_live_verify_technique`). (b) Declutter SWEEP (queue #5): overlay impl confirmed substantially clean post-565; shipped only a palette-token of the 2 white literals (`--ovx-text`, byte-identical) + doctrine `w-ovds`/neutral-text rows + the ROADMAP spec-path fix. (c) L1 minimap (queue #3) DEFERRED - out-of-scope per spec + no Live-Client coords. **NEXT:** overlay is doctrine-faithful + clean; only minor w-* default-position tuning remains.

- 🟡 **RC 2.0 PROGRAM IN FLIGHT (operator overnight directive 2026-06-19/20).** Overlay/UX + coaching + responsiveness + hygiene reframe. Living plan + live % + E-stage table in `docs/RC2_PLAN.md` (9 phases / 62 stages); resume with the `/RC2-Continue` command. Design GREENLIT = Hextech (`docs/design/RC2_DESIGN.html`); League borderless @ 2560x1440. SHIPPED per-stage detail is in `docs/RC2_PLAN.md` + relocated to `docs/ROADMAP_HISTORY.md` (2026-06-20). OPEN: E10 ASCII+git-history rewrite (force-push), E11 Hextech reskin, E12 responsiveness levers (L4 shipped via P6.3; L1/L2 via P6.2 - residual "etc." only), E7 bench+LCU-pooling (frozen), E2 DS 3-game live-flip (operator-played); P5.2 served CV-flip env (`RC_LANING_CV_SERVED=1`, gated on `hz_shadow_report` agreement delta); rc-shell Electron relaunch owed for the live disappear-fix/pinned; spell-fix LIVE verify owed.

- 🔵 **HEADLESS SWARM RUN ARMED 2026-06-17 (gemini+AHK loop, ~unbounded cycles).** Operator directive: drive ALL open items to completion across many `**/done`+`**/clear`+continue cycles. Centerpiece = DS permutation swarm (`docs/DS_PERMUTATION_SWARM_PLAN.md`): maximize DS logic across self/ally/enemy runes + summoner spells + ally/enemy teams; BUCKET not enumerate; each seam DEFAULT-OFF + Meraki/rewind-WIN anchored + ENGINE bump + DS restart + Share sync. Adapt to lolmath >= parity (DSP9). Implement findings to smallest benefit. Sessions DSP1-10 + HZU1 + LGS1 + OPEN1/2 in `docs/ORCHESTRATION_PLAN.md` (director PRIMARY source). Every default-OFF live flip -> `docs/LIVE_GAME_GATED_SYNC.md` (consolidated; operator runs all-in-sync at live games). Operator-gated forks -> gemini PART C recommended path, never block. Launch `ops/loop/launch_loop.ps1 -Mode live`; abort = drop `ops/loop/control/STOP`.
  - **Swarm progress (newest first):** RF1-RF6 + DSP1-DSP11 + OPEN1/2 + LGS1 + HZU1 all DONE (round-2 refill queue DRAINED); full detail in `docs/LEDGER.md` 464-487 + `docs/ROADMAP_HISTORY.md` (2026-06-20) + the `docs/ORCHESTRATION_PLAN.md` Findings log. NEXT: director picks top-down (expect NO_WORK or a new operator refill).

- 🟡 **Repo-audit (`docs/_archive/2026-06-17-repo-audit/repo-audit.md`) follow-up tail (2026-06-17, Antigravity batch verified+shipped item 490, commits `a13d7496` + `d16200b3`).** D2/D3/D5/D6/D8/D10/D13/D14 SHIPPED + ground-truth verified; the frozen `app/_game_lifecycle.py` mode->coach map extraction (D5) was operator-ratified into `core/coach_registry.py`. **OPEN:** **D1** churn-fix SHIPPED 2026-06-19 (item 506: `--precommit` gate kills the per-commit MANIFEST re-stamp + spurious gist upload); full 362-file mirror de-dup / build-time-gen still deferred (outward-facing gist + CI `--check` coupling, own session); **D4** blind-except ratchet SHIPPED 2026-06-19 (item 506: BLE001 + 954 app-code noqa baseline; engine/Share/frozen exempt); **D9** control-endpoint auth ✅ test SHIPPED (8ccbaa9c, `tests/test_control_endpoint_auth.py` +7: 200/401/open-when-unset/non-control-ungated; handler unchanged); **D11** 405 `assert`->`if/raise` = n/a (item 506; no 405 in repo). Do NOT re-pitch the D4 hot-path-narrowing revert or the D12 logger reverts as bugs - they were deliberate fixes. Detail: `docs/LEDGER.md` item 490.

- 🟡 **COMPREHENSIVE PER-CHAMPION DS CROSS-EVAL (operator directive 2026-06-16, NEXT SESSION).** Evaluate the DS engine's build recommendations for EVERY champion INDIVIDUALLY (never grouped/batched), crossing varying builds x enemy compositions (AD/AP/true, tank/squishy, CC) x runes (self + enemy + ally); cover each champion as the operator's own pick, not only as an enemy. Multi-agent fan-out, ONE agent per champion; if multi-session, headless Gemini+AHK `**/done` + `**/clear` loop until the whole roster is done. Outcome-anchored on rewind_history.db WIN data, NOT eyeballed rankings. SEED: `ops/audit/DS_BRUISER_DAMAGE_TYPE_2026-06-16.md` (the bruiser scorer IS damage-aware; the visible top-N is DPS-tier-dominated; this is a SCORER-OUTPUT audit, NOT registry coverage - do not dismiss as "DS registries saturated"). KICKOFF GATE before fanning out ~170 agents: lock the deliverable shape + validation anchor + rune data source with Gemini. Any weight/scorer change = Tier-2 (ENGINE bump + dual suite + Share mirror). Memory `project_ds_comprehensive_cross_eval`; see LEDGER item 462. **TIER-2 REPORT-FIRST gate SHIPPED item 495 (`ae47d0eb`, `ops/audit/ds_cross_eval/TIER2_REPORT.md`):** nomination B (kit-aware DPS) was the strongest + actionable. **B1 melee-applicability gate SHIPPED item 496 (PM4, `<this commit>`, ENGINE 1.140.0 -> 1.141.0, DS :8893 restarted, Share 366):** DEFAULT-OFF `apply_melee_aa_gate` seam - a `PeriodicProc.ranged_only` proc (Runaan's Hurricane bolts) contributes 0 on a melee auto (attackrange < `dps.MELEE_RANGE_CEILING`=350); byte-identical OFF; +9 TDD; live `rank.py`/`server.py` flip stays validation-gated -> `docs/LIVE_GAME_GATED_SYNC.md`. **F2 gold-aware top SHIPPED item 497 (PM5, `<this commit>`, ENGINE 1.141.0 -> 1.142.0, DS :8893 restarted -> 1.142.0, Share 367):** DEFAULT-OFF `cost_ceiling` param on the shared `_filter_candidates` (+ threaded through `rank_items`/`rank_items_by_ehp`/`rank_items_by_hybrid`) drops any candidate whose `gold.total` STRICTLY exceeds the ceiling, so the 6000g ARAM/Arena mega-item Void Immolation (223069, maps 12+30 - a genuine base id, NOT the alias quirk) that `sort_by="delta"` floats to rank-1 is excluded; byte-identical OFF (+12 char tests, difference-of-differences); reproduced live (Garen bruiser ARAM rank-1 = 223069). Live `rank.py`/`server.py` flip stays validation-gated -> `docs/LIVE_GAME_GATED_SYNC.md`. **NEXT (this lane):** B2 kit-agnostic AD-axis residual is LARGELY COVERED by the shipped DSP11 `prefer_kit_axis_by_win` (Ezreal Trinity un-strip + kit-axis float) + DSP2 `exempt_offclass_by_win` - only a per-champion live re-rank validation remains; A ARAM-override stays default-OFF (only Shaco a clean CONFIRM, an operator off-meta product call). The bounded ship-or-close Tier-2 nominations (B1/F2) are now DONE; the lane tail is live-validation-gated.

- 🟡 **DEEP-AUDIT PROGRAM (2026-06-11, operator overnight directive - SUPERSEDES the per-item NEXT-UP queue while it runs).** Standing charter `docs/DEEP_AUDIT_CHARTER.md` (P0 baseline -> P8 docs); loop armed via ops/loop (max_cycles 100); live state on Desktop `RC_DEEP_AUDIT_SYNOPSIS.md`. Frozen files + history + gist + .md rewrites authorized; vanguard/CV + 2-PC/gamepc prune; BOM/encoding retro-sweep; Haiku/Sonnet-to-ZERO completion; Electron overlay to FULL (web dashboard no longer required); gemini holds scope questions.
  - **2026-06-15 plan REFILL + relaunch:** OVL1/2 + DSV1/2/3/4 + UIX1/2/3 (items 429-437) all DONE - the A1-UIX3 orchestration set drained; full detail in `docs/LEDGER.md` 429-437 + `docs/ROADMAP_HISTORY.md` (2026-06-20). NEXT: director refills `docs/ORCHESTRATION_PLAN.md` or emits NO_WORK.
  - **2026-06-22 DS scorer-valuation residual tail CLOSED (item 592, DSV5).** The P6-G5 residual ("AP-DoT-vs-burst EHP-gating" + "kill-state/carry_share weighting") was VERIFY-THE-PREMISE'd against ground truth + a rewind WIN-anchored measurement. Kill-state half = CLOSE-as-covered (the DSV2 `assume_takedown` seam is dormant in every live path - no caller passes True - and winning supports build Hubris/Collector 0.1%, so there is nothing to suppress). AP-DoT half = genuine but REFRAMED: DSV1 already values the burn; the gap was the comp-BLIND `enemy_stats.max_hp`, so DSV5 ships a DEFAULT-OFF comp-conditioned max-HP seam (`RC_COMP_HP_LEAN`) that tilts the live ranking toward DoT vs 2+-tank comps (rewind: +12.0pt DoT, burst halves). Tier-1 (no ENGINE bump / Share sync - the scorer was already shipped). Live default-ON flip is operator-gated -> `docs/LIVE_GAME_GATED_SYNC.md`. **Don't-redo: do NOT re-pitch a kill-state/carry_share scorer (dormant seam, no live false-positive) or a new AP-DoT scorer (DSV1 covers valuation; DSV5 covers the target).**

**>> NEXT-UP for the loop (priority order, set 2026-06-05; the Gemini director picks ONE bounded item/cycle):**
1. **cdragon Phase-2 hard tail - CLOSED 2026-06-07 (item 351; scope + by-level drift-check, NO engine change).** prefer_cdragon_ratios default-ON cutover (items 317/319/320, ENGINE 1.120.0) stays COMPLETE - do NOT re-flip / re-extract / re-bump (shipped detail in docs/ROADMAP_HISTORY.md 2026-06-06). The 577 Meraki-fallback ratio blocks were censused + bucketed: 292 emission-guard tooltip aggregates (must stay fallback by design) + ~174 need live target/stack/branch state (buff-counter 26 / conditional 1 / unknown-stat / resource / CDR) = CLOSED-as-Meraki, same class as the operator-CLOSED item-232 conditional-target-state arc + 111 by-level (per-champ-level arrays). The by-level bucket was drift-checked vs frozen Meraki: ALIGNED, not stale - cdragon indexes by champ-level 1..18 while Meraki indexes by spell-rank 1..5 (different axes of the same growth), the blocks are overwhelmingly auxiliary (caps / minion+monster mods / heal+shield / durations / modifiers whose primary damage is already a sibling MECHANICAL calc the engine reads), and where a by-level calc names a real primary-damage quantity it agrees with Meraki (<=~7%). Re-sourcing would need a NEW champ-level schema axis for near-zero gain = churn -> by-level DEFERRED to BACKLOG. **Don't-redo: do NOT re-pitch a calc-graph resolver for this tail; the only remaining cdragon growth needs a new extractor key, not a resolver.**
2. ✅ SHIPPED - daily upstream content-drift poll (commit d3fcc070, 2026-06-06; RC-UpstreamDriftCheck live, verified 2026-06-07). See below.
3. SHIPPED - loop-status monitor (item 346) + control (item 349) both LIVE 2026-06-07. See below.
4. **P3.2 schema-lift SHIPPED 2026-06-05 (item 315** - antitank `ap_ratio`/`ad_ratio` + optional `stats` injection, seeded Gwen P + KogMaw W, byte-identical live so NO ENGINE bump; the live-producer route wiring + ENGINE bump are the Phase-D tail). The 6 UNIVERSAL_FILES convention update remains. **P2.2 FULLY COMPLETE (item 313 swap + item 314 tail + item 322 parse-caller migration 2026-06-06 - all 4 coaches + SR now build `choices` via `CoachOutput.from_fields`).**
Stale `Desktop\todo.md` (DS source-layering) was FULLY SHIPPED (Win1=item295, Win2=item296) + deleted 2026-06-05 - do NOT redo. Loop relaunch: raise `max_cycles` in `ops/loop/config.json` (it stopped at "max_cycles 1 reached") then run `ops/loop/launch_loop.ps1` (or the Desktop "RC Headless" launcher).

**Orchestration fanout run (`docs/ORCHESTRATION_PLAN.md`, the director's per-cycle source):** A1 DS-Profile panel (item 325) + A2 axes batch 2 (item 326, 8 axes) + A2b DS-Matchup card (item 327, GET /api/ds-matchup) + A3 DS-coach shadow-log (item 328, anti-tank + scaling power-curve hints, SHADOW-only) + B1 deterministic-coaching validation shadow-log (item 329) + C1 champ-select ARAM/Arena UI audit (item 330, SHIP-READY + reproducible Playwright snapshot coverage `tests/snapshot_panels/test_champ_select_view.py`) + C2 active-match UI audit (item 331) + C3 post-game-review UI audit (item 332) + D1 DS relative-score bar (item 333, verify-premise) + D2 draft tool L (the community fork) Elo log-odds aggregator (item 334, verify-premise, already-shipped 2026-05-20 `21bf98ef`) DONE; E1 (item 335) + E2/E3/F1 (item 348, 2026-06-07) DONE - orchestration fanout A1-F1 fully CLOSED, director NO_WORK. **NEXT (gated, A3 tail):** SURFACE the DS-coach hints into coach context after shadow-log (`data/ds_coach_hints_shadow.jsonl`) accrues + validates on real games (do-not-flip-blind; champ-select Haiku-elim pattern).

- 🟡 **P6 LOLMATH BUILD-ENGINE PARITY (operator-appended 2026-06-15; routes through the deep-audit loop).** Operator handed the Desktop `LOLMATH_VS_DS_SWEEP.md` + reproducer folder for review + implementation; both copied in-repo (`ops/audit/LOLMATH_VS_DS_SWEEP.md` + `ops/audit/lolmath_ds_sweep/`), work-map `ops/audit/P6_LOLMATH_PARITY.md`. all-champion lolmath (26.12 == 16.12.1) vs DS `build_orders_sr.json` mixed sweep. **G1 SHIPPED 2026-06-15 (item 421, commit `1d7da921`; ENGINE 1.121.0 -> 1.122.0; DS :8893 restarted; Share re-synced): DS built the wrong damage axis vs the kit `damage_distribution`.** Root cause = `core/archetype_picks` resolved the scorer archetype from the DDragon CLASS tag (role), ignoring the kit axis; fix re-bases the DEFAULT archetype onto `champions.json lolmath.damage_distribution` (below operator picks). 18 default-source champs re-based (11 sweep-named incl Gwen/Teemo/Rumble/Diana + Pyke AP->AD, + 7 AP assassins the sweep missed: Akali/Ekko/Evelynn/Fizz/Kassadin/Katarina/LeBlanc). All 9 build_orders tables regenerated (flat+HZ-B1+HZ-B2 x sr/aram/arena). NOT flipped (kit axis sides with DS, or axis-neutral): 6 tank-archetype AP kits (Amumu/Galio/Nunu/Singed/TahmKench/Blitzcrank -> BACKLOG role-reassignment), XinZhao+Taric (lolmath quirks). `core/archetype_mismatch.py` was a red herring (UI nudge, not axis). **G4 SHIPPED 2026-06-15 (item 423, commit `a58a1f10`; ENGINE 1.122.0 -> 1.123.0; DS :8893 restarted -> 1.123.0; Share re-synced 336 files): boots-pool refresh to the 16.12.1 SR-only tier-3 upgraded boots.** `core/build_order._select_boots` now upgrades the resolved tier-2 family to its tier-3 form on SR (map 11) via the new `_BOOTS_SR_UPGRADE` map (each DDragon `into` verified map11-only); ARAM (map 12) + Arena (map 30) keep tier-2 (no tier-3 there); assassin default moved off the out-of-store Mobility Boots (3117) to Ionian -> Crimson Lucidity. No engine MATH change (ranker byte-identical, never imports core/build_order); all 9 tables regenerated, 9/9 verified (SR tier-3, ARAM/Arena tier-2, 0 Mobility). ✅ G4-Arena-tail SHIPPED 2026-06-18 (item 499, commit `c258c4ab`; ENGINE 1.143.0 -> 1.144.0; DS :8893 restarted -> 1.144.0; Share 367): the deferred "Arena should use the 22xxxx mirror (3xxx are map30=False)" - `_select_boots` now remaps Arena/CHERRY tier-2 boots to their `22`-prefixed map30-legal mirror (`_BOOTS_ARENA_MIRROR`, each verified maps.30=True); 3 Arena tables regenerated boots-only (510/684/342), SR/ARAM byte-identical; ARAM 3xxx kept (map12-legal). NOT live-gated (data fix); only an Arena augment-phase boot-render visual is OWED. **G5 CLOSED 2026-06-15 (item 424; NO pool gap, premise falsified; Tier-0 diagnosis, no ENGINE bump / no regen / no restart):** root-caused via durable probes (`ops/audit/lolmath_ds_sweep/g5_pool_probe.py` + `g5_live_rank_probe.py`, verified live at ENGINE 1.123.0). DS's candidate pool is `rank._filter_candidates` over ALL 706 items (purchasable + terminal + map-legal + budget, no per-archetype whitelist) - REAL SR pool gaps = 0; every lolmath-favored item resolves to a canonical map11 id and appears IN the live ranking, just below top-6 (Talon: Umbral #7 / Hubris #13 / Profane #33; Lux: Liandry's #23; Jhin: Collector #12). The probe v1 false-flagged 42 by indexing the 6-digit "22.." Arena ALIAS ids (map11=False, memory `reference_items_index_alias_ids`); canonical short ids are in-pool. DS recommends only ~60 distinct items / 172 champs x 4 comps - the breadth gap is SCORER VALUATION (item passives needing kill-state: Hubris/Collector/Death's Dance; AP DoT burn vs single-rotation `ability` model: Liandry's/Blackfire; lethality-vs-sustained tradeoff in `burst`), NOT pool membership (lethality pen itself is modeled, `effects.py:326`). Re-routed to the G3/G6 design track. **G2 CLOSED 2026-06-15 (item 425; low-overlap re-measure, NOT a separate bug; Tier-0 diagnosis, no ENGINE bump / no regen / no restart):** durable probe `ops/audit/lolmath_ds_sweep/g2_remeasure_probe.py` re-paired lolmath-vs-DS at live 1.123.0 (post-G1/G4): `BEFORE(1.120.0) G1=20 G2=59` -> `AFTER G1=8 G2=60 ok=101 nodata=3`. G1 20->8, all 8 residuals by-design + per-champ verified (Lulu operator pick source=user_cs never-touched archetype_picks.py:121; Amumu/Blitzcrank/Galio/Singed/TahmKench tank axis-neutral :124; Taric/XinZhao DS-correct lolmath-quirk) = ZERO regressions from item 421. G2 held ~flat (axis fix pulled the AP-damage champs OUT of G1 but matching SPECIFIC items needs the design tracks; G4 boots don't move the metric - overlap subtracts BOOTS by design). G2 residual = 3 buckets all downstream of routed tracks (per-champ build dumps): A role-item-vs-lolmath-glass-cannon (Janna/Sona/Alistar... DS utility/heal/shield, lolmath raw AP) -> G6/by-design; B AD scorer-valuation (Aatrox/Fiora/Nasus/Darius/Jhin lethality+kill-state passives Hubris/Collector/Endless Hunger/Death's Dance) -> G5-residual; C AP DoT-valuation (Anivia/Swain/Lissandra Liandry's/Blackfire burn vs DS burst) -> G5-residual. G2 is NOT a fixable bug (premise confirmed). **REMAINING:** G3 runes / G6 cost-model / G7 comp-harness + the G5 scorer-valuation residual = Gemini-consult first (G6 likely by-design CLOSED). G1/G2/G4/G5 are the bounded-slice set and are all DONE/CLOSED - the P6 tail is now design-level (Gemini-consult), no bounded slice remains. Director picks ONE bounded slice/cycle, root-cause-first.
- 🟡 **Haiku-to-ZERO HZ-* fanout (reseed 2026-06-08, source `docs/ORCHESTRATION_PLAN.md`).** Charter 4b PRIMARY north star, refilled after the A1-F1 fanout closed. **HZ-A1 DONE (item 352, commit 85b13b7c):** NEW `core/laning_scenario_precompute.py` precomputes laning `trade`/`all_in`/`back_off`/`even` verdicts over (my_champ x enemy x level-band x mana-state x cd-state) from the SHIPPED `compute_matchup` engine -> atomic versioned JSON `data/daemon_slayer/laning_scenarios/<patch>/`; generator + fail-soft mtime-cached reader + CLI; itemless 10-champ SR seed (1600 cells); BUILD + PERSIST + READ only, NO live coach flip (do-not-flip-blind, needs real/replayed-game validation; Haiku stays interim floor). **HZ-A2 DONE (item 353, commit dc5e6293):** v2 `economy` block (recall/back-timing + power-spike-ETA, gold-income + item-completion driven via `core/lead_projection.py` new spike-ladder/income primitives) threaded onto every HZ-A1 cell; SR seed regenerated (1600 cells, recall_now 940 / back_soon 440 / hold 220); BUILD + PERSIST only. **HZ-B1 DONE (item 362, commit 3a129071):** NEW `core/build_order_precompute.py` precomputes build orders per (champ x mode x enemy-comp-archetype: frontline_heavy/burst_heavy/poke/mixed, each re-parameterizing the shipped `plan_build_order` levers) -> atomic versioned JSON `data/daemon_slayer/build_orders/<patch>/` (NEW subdir, the item-265/266 damage-profile table untouched); generator + fail-soft reader + CLI; 10-champ SR seed x 4 archetypes (40 cells); BUILD + PERSIST only. **HZ-B2 DONE (item 364, commit 8d8bc311):** sibling `core/build_order_variants.py` emits anti_tank vs anti_squishy build VARIANTS per champ (the two durability extremes), each modulated by the DS A3 anti-tank score (a kit that shreds gets a softer synthetic wall) -> `data/daemon_slayer/build_orders/<patch>/build_order_variants_<mode>.json` (10-champ SR seed x 2 = 20 cells); BUILD + PERSIST only. **HZ-C1 DONE (item 366, commit 3581afed):** NEW `core/precomputed_laning_coach.py` turns ONE HZ-A laning cell into two grounded A/B `CoachChoice` (combat verdict + economy/recall), pure static read (no Haiku, no :8893); `core/hz_choice_shadow.py` shadow-logs alongside live Haiku, wired in `_state_builder`; SHADOW only, NO flip. **HZ-C2 DONE (item 367, commit 5627053b):** NEW `core/precomputed_build_coach.py` classifies enemy-comp durability (`aram_comp_verdict.compute_factors` frontline_count) -> anti_tank/anti_squishy BUILD A/B from the HZ-B2 table; `core/hz_build_shadow.py` shadow-logs. **HZ-C report + native-capture DONE (items 368/369, commits 1d116194/1f702711):** `tools/hz_shadow_report.py` = the coverage+distribution flip-readiness gate; shadow records now carry `native_action`+`native_choices` (what Haiku said) for precompute-vs-Haiku validation. **OPEN (director picks top-down):** HZ-C SEED-EXPANSION DONE (item 370, 2026-06-09; operator-gated, 3 forks: full 171-champ SR + slim+compact commit + trim-to-~55-60MB). Regenerated full 171x171 SR: laning `laning_scenarios_sr.json` 350892 cells / 65.6MB (slim+compact schema v2 -> **v3**: dropped sequence/my_can_full_combo/manaless/economy.spike_eta_s + compact writer + GEN_BANDS L2/L6/L11 omitting the L16 late-lane band, reader fail-softs lvl>=14) + HZ-B1 build_orders_sr 684 + HZ-B2 variants 342; non-seed champs now covered; full RC suite 5611 passed; ENGINE 1.120.0 untouched, no Share, no flip (still SHADOW). **SR precompute REGENERATED at live patch 16.12.1 (item 377, commit `cc35d5ed`)** - resolves the patch-drift that had zeroed shadow coverage (16.11.1 seed vs 16.12.1 live; `hz_shadow_report` 0/512 laning + 0/475 build): all 3 SR tables rebuilt from the full 172-champ roster (laning 355008 cells / 66MB LFS + build_orders 688 + variants 344), read path live-verified (Ahri vs Darius L6 -> back_off / "Recall now"); the 0-coverage was HISTORICAL (covered baked at log time), accrues covered=true on new games now. **NEXT (gated):** accrue real-game HZ shadow on the expanded seed -> precompute-vs-Haiku agreement analysis (item-369 native capture) -> THEN the live coach FLIP (do-not-flip-blind); ARAM/Arena = a `--mode all` offline expand (no code change); HZ-D1 overlay ROUTE SHIPPED item 378; panelset rendering + visual launch item 379; drag region + overlay ACTIVE drag item 380; overlay position/panelset persistence + ACTIVE glow indicator item 381; Phase 4 in-overlay DS controls + Phase 5 drift-guard smoke item 382; Phase 5 stabilization tail (updater channels + crash isolation) item 383 - **HZ-D1 DONE** (see the item-214 line - remaining = live-game capture + operator packaging/Release; Phase 6 Pengu optional). **HZ-D4 charter sweep DONE (item 386, 2026-06-10, merges `d446ea80`+`b54d040e`): 7-lever cost sweep 7/7 CLEAN + the HZ shadow pipeline ROOT-CAUSE-FIXED** - all 837+800 pre-2026-06-10 shadow rows were junk (idle-tick stale-coach replays, enemy=null on 100 percent, + pytest Champ0 pollution into the real data/ jsonls); live `lc.get("champion")` gate in both shadow_log_precomputed_* + hermetic autouse SHADOW_PATH redirect in tests/conftest.py + polluted logs rotated to _scratch/. **Full-roster ARAM tables shipped** (every native capture is mode=aram): laning_scenarios_aram 65.75MB LFS + build_orders_aram 688 + variants_aram 344 at 16.12.1 (gen CLIs default to the 10-champ seed - pass `--champions <172-CSV>`, CANONICAL ids not display names - display names silently skip 6783 pairs, item 388). **Full-roster ARENA tables shipped (item 388, `fb66722f`):** laning_scenarios_arena 355008 cells 66.4MB LFS + build_orders_arena 688 + variants_arena 344 - HZ shadow now armed on ALL THREE modes (sr 377 / aram 386 / arena 388); read paths live-verified. **Agreement metric shipped (item-369 tail):** `tools/hz_shadow_report.py` v2 classify_verdict/record_agreement/summarize_agreement - the flip-readiness gate now computes precompute-vs-Haiku agreement once covered+native rows accrue from real ARAM games. **Metric DE-BIASED (item 574, 2026-06-21, commit `79a7906a`):** native "recall" actions are an ECONOMY decision, not a laning-combat verdict, and the precompute combat-A can never be recall (its recall signal is choice B: "Recall now"/"Back soon"). Scoring native-recall against combat-A was a category error counting ~1794 guaranteed mismatches in the laning denominator - the honest laning agreement is 54.6% (1133/2076), not 29.3%. Native-recall ticks now route to a new `economy` sub-block {native_recall, precompute_also_recall} (the "scored against the economy block" read); remaining laning mismatches are now GENUINE combat disagreements. Tier-1 tooling, no engine/Share; TDD +6/3-updated, verifier-gated. The flip itself stays operator-gated (do-not-flip-blind). **Genuine mismatches DIAGNOSED (item 575, 2026-06-21, commit `367297b6`):** `tools/hz_mismatch_diagnose.py` buckets each genuine laning mismatch by log-time net_swing -> the over-caution is MOSTLY CALIBRATION near the hold/trade boundary (368 ticks: hold->all_in/even->trade/hold->trade), with ONE structural MODEL-ERROR pocket (`back_off->trade` n=138 median -0.30 = the enemy `sequence_b=_FULL_COMBO` over-kill, `core/laning_scenario_precompute.py:355`); the largest class `back_off->hold` (n=351) is per-pair ambiguous. NOT Haiku noise. Operator-gated NEXT levers: a realistic partial-enemy-combo model (the principled MODEL-ERROR fix, NOT a threshold tune) + ground-truth (rewind-db / live side-by-side) before any `_BACK_OFF`/`_HOLD_LOW` nudge (anti-circularity). Report: `ops/audit/HZ_MISMATCH_DIAGNOSE.md`. **Ground-truth cross-ref instrument SHIPPED (item 576, 2026-06-21, commit `fc520407`):** `tools/hz_mismatch_diagnose.py --rewind` adds an SR-only matchup-aggregate cross-ref over `rewind_history.db` (reuses the `replay_matchup_validate` primitives) - the anti-circularity check that validates the CALIBRATION classes against REAL lane outcomes (gold@10min + solo-kill differential, my-perspective) BEFORE any threshold nudge; ARAM/client excluded+counted (no lanes); a conservative agree/disagree/insufficient_data rule with a low-sample guard; agreement REPORTED-ONLY (never fed into a threshold). Tier-1, schema v1->v2 tool-local, default-OFF flag, fail-soft, +19 hermetic tests; no engine/Share/ENGINE/table change. Coverage is currently SPARSE (operator DB is mostly self-games -> most classes read insufficient_data); richer rewind SR coverage per matchup is what makes it actionable. The flip itself stays operator-gated (do-not-flip-blind).
- 🟡 **item 312 (2026-06-05; non-engine, non-frozen; DS NOT restarted) - external refactor-plan TRIAGE + P4.2 shipped.** Operator handed 3 Desktop review docs (`Claude_Refactor_Plan.md` + `Riot_Commander_Review.md` + `Riot_Commander_Deep_Dive.md`) - external AI 5-phase audit, kept OUT of the repo per the outreach rule. Triaged each proposal vs hard rules / frozen list / settled ADRs (proposals are INTENT, verified vs live code). **SHIPPED P4.2:** `RC_TESSERACT_CMD` env override in `core/vision_tesseract.py` (portability for a non-default Tesseract install; +5 CI-safe tests injecting a fake pytesseract; byte-identical when env unset; ruff clean). **REJECTED:** P3.1 amp-DRY (PREMISE FALSE - already centralized in `effects.py`, both dps.py + burst.py import it); P5.1 drop-em-dash-ban (the settled PS-ParseFile hard rule). **GATED on approval:** P1.2/P1.3/P4.1 (frozen `game_snapshot.py`/`_loop.py`/`_game_lifecycle.py`); P2.1 FastAPI rewrite (undoes ADR-008 + Vanguard-safe stdlib posture); P5.2 SQLite ledger (already solved via LEDGER relocation + 60KB budget). **ACCEPTED, own session:** P2.2 structured-output (touches live coach parse + partly redundant), P3.2 antitank dynamic %HP (a design change, antitank is intentionally static per item 308). Also cleaned `Desktop\UNIVERSAL_FILES\` to the 6 originals (19 item-310 scratch -> gitignored `_scratch/uf_dump_item310/`; `BRIEF.md` -> Desktop, out of repo). **NEXT (operator-gated):** P2.2 + P3.2 focused sessions; update the 6 UNIVERSAL_FILES templates to current conventions; frozen-file + big-rewrite phases need explicit go. Loop note: the Gemini director reads repo-only (never the Desktop), picks ONE bounded item/cycle - pin any accepted item in ROADMAP NOW to route it through the loop. (full record: docs/LEDGER.md item 312)

- [open, from item 283] **NEXT (gated/FUTURE, headless run 2026-06-03):** antiheal/grievous callout (competitor #2) SHIPPED item 285 (`4acf063`, `core/heal_threat.py`, deterministic Haiku-free; in-game visual capture OWED); cost prompt-restructure for aram_aug_select / tft_live_analysis CLOSED-not-viable item 286 (static portions ~62/669/51 tok are far below the 2048-tok Haiku cache floor -> caching inert; tft primary path is FROZEN single-string moon_proxy; guard `tests/test_prompt_cache_floor_item286.py`; do NOT re-attempt); inhibitor-callout in-game VISUAL capture OWED (fires only when an inhibitor is down in a live SR game); champ-select brief Haiku flip stays gated on shadow-log accrual. DS engine verified-correct across 4 lanes (pen/on-hit/crit/AoE) - do NOT re-audit. (full record: docs/LEDGER.md item 283)
- [open, from item 277] **NEXT (gated):** Anivia P egg-resist seam CUTOVER SHIPPED item 321 (2026-06-06, ENGINE 1.119.0 -> 1.120.0 - `apply_egg_resist` default-ON; Orianna E ally-resist needs NO flip - consumer-driven via `external_resist_armor/mr`, already active, item-316 regression green; remaining FUTURE = wire both seams into a live survivability scorer + per-champion validation) ; live-game-gated (champ-select brief flip, vision self-heal validation, Phase D flag-flips, Arena PGR capture) ; PGR @N gold@10/cs@10 (needs per-participant timeline frames). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 276] **NEXT (operator-gated):** VISUAL CAPTURE PGR S3/S4 + Build Insights skill tab (?ui_mock, Legion Windows-MCP) ; PGR S5 (polish/responsive/ARAM-Arena variants) + S4 @N ; vision self-heal LIVE in-game validation ; relocated-agent rename/Game-PC teardown DONE 2026-06-20 ; Zaahen real curated build ; champ-select brief flip ; DS Phase D / Anivia P / Orianna E. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 275] **NEXT (operator-gated):** PGR S2 polish / S4-S5 (lane comparison) ; skill-WPA frontend chip / Build Insights skill-WPA tab ; champ-select brief flip after shadow-log validation ; DS Phase D flag-flips / resist exclusions (Anivia P / Orianna E). (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 273] **NEXT (operator-gated):** Build Insights view visual capture (Legion-local) + optional spells/runes-WPA extension ; champ-select brief flip after live shadow-log validation ; Phase D flag-flips (item 268/272) ; DS resist exclusions (Anivia P / Orianna E) ; relocated-agent rename DONE 2026-06-20 (item 269 L8). (full record: docs/ROADMAP_HISTORY.md)
- 🟡 **ZOI minimap overlay program (item 567, 2026-06-21).** The Zone-of-Influence minimap overlay, grounded on a NO-API coordinate source: League `game.cfg` `[HUD]` MinimapScale/FlipMiniMap geometry (NOT the coordinate-less Live Client :2999). **FOUNDATION + SLICES 1+2+3 DONE:** (a) `w-mmrect` settings-driven click-through gold-outline box (`94b31bad`); (b) slice-1 fixes `f93f5f01` - the box now RENDERS over a live game (threaded `minimap_rect` into client `state.latest` at all 3 poll sites; the ui_mock test had masked the gap) and FRAMES the minimap (overlay window sized to full display BOUNDS not the taskbar-excluded work area -> ovscale 1.33; re-assert bounds after topmost -> winH 1440 so the bottom is reachable; outline 1px/0.55 -> 2px/0.85 gold); live-verified box vs minimap teal frame within ~1px; (c) slice-2 `d6a853f2` - `core/minimap_blob_detect.py` pure-numpy team-color blob detection (saturation discriminates icon vs terrain tint; 8-connected flood-fill centroids; `crop_minimap` maps design-px rect onto the ~1280-wide frame by fraction) -> `/api/state.minimap_dots` (mode + in-game gated, TTL-cached, fail-soft); 11 tests, live 32 dots in ~1.6ms. **SLICE 3 DONE** (`163b76ee`): low-opacity team-colored ZOI bubbles + a weighted-bisector demarcation render inside the box (canvas; PER-TEAM offscreen compositing caps each team at alpha 0.25, EMA-smoothed, click-through pointer-events:none), one bubble per dot, radius/weight scaled by RC per-team strength (dot presence + MY ult spikes 6/11/16 + game_time), and a deterministic map-control callout feeds the coach (`det.callouts`, no Haiku). Honest scope: strength is PRESENCE-grounded - the Live Client exposes no enemy level/alive/gold; `blue=ally/red=enemy` = League default colors. Live-verified via CDP (29 bubbles painting over the real minimap, click-through confirmed, 99.7% pixels <= 0.25); UI-audit SHIP-CLEAN. **NEXT (future, no owner; PARKED 2026-06-21 cont7 - LOW value + regression risk, do NOT build blind):** champion-only isolation (template matching) vs the presence detector - `core/minimap_blob_detect.py:16-19` author note says "total team presence is the right input anyway" for a ZOI map-control signal, and a pure-numpy template match on a noisy ~312px crop would REPLACE the live-verified presence detector (slices 1-3); two Gemini passes said do not attempt without operator sign-off. Richer strength if an enemy-data source appears. (full record: docs/LEDGER.md items 567 + 570 + 571 + 573)
- **RC architecture self-map (DONE 2026-06-21, item 568).** Built HEXCORE (`docs/HEXCORE.html`) - an interactive 3D hextech knowledge-graph viewer of RC/DS's own architecture: the DS engine reactor core, the per-mode coaches, the `GET_ROUTES` / `_dispatch.dispatch_get` route binding, the web HTTP fetch->endpoint data flow, and the real git co-change coupling. The corrected, ground-truth-verified self-map; design ideas sourced from Gemini. (full record: docs/LEDGER.md item 568)
- [open, from item 269] **NEXT (operator-gated):** L6 capture ; L7 overlay launch + Phase 4+ ; Phase D live flag-flips (item 268) ; Arena auto-flavor "Mage" mislabel sweep (L4 carry) ; relocated-agent rename/Game-PC teardown DONE 2026-06-20 (L8 carry). (full record: docs/ROADMAP_HISTORY.md)
- 🟡 **Haiku-elimination program (item 265 FOUNDATIONS + item 266 WAVE 3 shipped 2026-06-02; ENGINE 1.94.0).** PRIMARY north star = drive live Haiku to ZERO via precomputed DS lookups. DONE: Lane A 1v1 matchup engine (`agents/daemon_slayer/matchup.py` `compute_matchup` + `/v2/matchup`) ; Lane B build-order tables (`data/daemon_slayer/16.11.1/build_orders_{sr,aram,arena}.json`, 516/516 cells) ; Lane C/E/P deterministic generators (`core/laning_verdicts.py` / `core/event_callouts.py` / `core/lead_projection.py`). **WAVE 3 SHIPPED (item 266, `cb46386`, no ENGINE bump):** W3A wired deterministic choices(first)+callouts+lead into `dashboard/_state_builder.py` via NEW `dashboard/_deterministic_coaching.py` (TTL-cached 3s, fail-soft, 2 new /api/state keys callouts+lead_projection) ; W3B+trade replay-validation harness (`tools/replay_matchup_validate.py`, gold + solo-kill duel ground truths) ; W3C DE-BIASED Pick&Ban targets DB (`data/daemon_slayer/16.11.1/pickban_targets.json` + `core/pickban_targets.py`) ; W3E rendered callouts+lead in RIGHT NOW (`web/js/panels/callouts.js`). **GATE FINDING:** the matchup engine validated as a COIN-FLIP vs real outcomes (gold-at-10min 48.8%, solo-kill duel 51-52% NOT significant, n thousands) -> NO coach flipped off Haiku (section 4b: do not flip blind). The matchup-derived A/B chips are SECONDARY; the correct-by-construction callouts + lead are the valid Haiku-elim wins. **NEXT (operator-gated):** matchup-engine fidelity lift (item-aware + real-level; uncertain payoff, likely a modeling ceiling) OR pivot to non-prediction surfaces (callouts/lead/build-orders already shipped) ; overlay Lane D (Electron Phase 2) ; champ_select pickban-DB flip after a counter-quality validation ; W3E callouts/lead VISUAL capture OWED (Legion-local capture). **item 280 SHIPPED (2026-06-02, `77c3647`/`d68ceff`; headless):** NEW `core/aram_comp_verdict.py` deterministic ARAM swap/variant/stay engine (correct-by-construction from champions.json range/damage/frontline facts; NOT prediction) retires the `aram_team_analyzer` Haiku call (deterministic-first, Haiku fallback only for <3-champ teams); + reused its `compute_factors` to add a shadow-only enemy-itemization hint (armor/MR) to the champ-select brief substrate (`dashboard/_champ_select_deterministic.py`, served stays Haiku per the item-273 AST guard). NEXT still gated: flip the brief after shadow-log data accrues. Comp-verdict bench surface + /api/aram-comp-verdict route SHIPPED on main (`9d2e85ea`/`df78b090`, 2026-06-06; item-378 branch triage confirmed the 3 orphan worktree branches patch-equivalent + pruned).
- 🟡 **Live UI watch (item 243/244 NEXT, ongoing per-game).** 2026-06-01 ranked-SR Caitlyn tick verified item-211 ingest (row 1805 game_id=5572690426 detail=True, no misattribution) + item-243 live-metrics (`live_5572690426` 15 rows) + build chooser (3 clean paths) + DS-vs-enemy-comp (0 junk) all healthy; caught + addressed 3 backlog flags in item 244 (`25b0be3`: F1 champ-select archetype resolver, F2 coach `**` markdown strip, F3 retracted-as-not-a-bug). **2026-06-02 ranked-SR Ashe + ARAM-Mayhem Kai'Sa watch:** dashboard ACTIVE MATCH == `/api/state` every tick (Seraphine-ally-aware + drake-timed + CC-saturation-94% coaching), build chooser populates SR + ARAM (the Kai'Sa-ARAM "not populated" report was NOT a global bug). On-demand Legion capture (Windows-MCP Screenshot) x2 mid-game = **clean**. Continue ticking champ-select + in-game vs `/api/state` each ranked game; fix in-session where ADR-008-auto-served.
- 🟡 **Dashboard data-wiring gaps (item 281 review, Class B deferred 2026-06-03).** Surfaces wired in the UI but with no live producer: (1) ADAPTATION stats-view ~90 `st-*` rows (`web/js/panels/right_now.js renderStats` via setv) - source keys (cs_at_10/csd_at_15/gd_at_15/apm/reaction_time/tilt_meter/wall_collision_deaths/...) exist ONLY in post-game `core/match_metrics.py` + `core/aftergame_summary.py`, never in the live coach dict -> wall of "-" in-game; (2) `/api/ward-heat` permanently empty (`core.ward_events.record_ward` has no producer - Live Client emits no WARD_PLACED); (3) ✅ SHIPPED item 281-tips (2026-06-06, merge df64eb75): Home "Tonight's Pick" Good/Bad/Ugly now wired to deterministic tips from the this_week aggregate (`dashboard/builders_home.py::_home_pick_tips` -> `tonight_pick.tips` = avg-KDA headline / deaths-per-game cost / sample-or-CS risk; `data-dummy-data` removed; `web/js/main.js` renders with the "-" empty-state fallback; +4 tests `tests/test_home_tonight_pick_tips.py`). Gap (1) PARTIAL: cs_at_10/csd_at_15 live producers SHIPPED on main (`7e8e47fa`, 2026-06-06, `dashboard/_adaptation_latch.py`; item-378 triage pruned the patch-equivalent orphan branch); the remaining ~88 st-* rows + (2) ward-heat producer still open (NB 2026-06-21 cont7: the in-game "wall of dashes" SYMPTOM is already resolved - commit `94f1e07b` `_hideEmptyStatRows()` hides every no-live-producer row + collapses empty group headers, so the ~88 unwired rows are correctly hidden mid-game; wiring more live producers only affects the RETIRED Chrome dashboard panel = low value, see LEDGER 573). P0 phantom active-match + P1 minimap-crop 502 spam already FIXED item 281 (`f6a1c52`). (full record: docs/LEDGER.md item 281)
- [open, from DS gaps roadmap sections 2+3] **NEXT (gated):** the default-ON `apply_passive_damage` flip + the 4 non-every-AA `on_hit` entries need the structured cadence + a live "saner not different" re-rank. (full record: docs/ROADMAP_HISTORY.md)
- [open, from DS unlabeled] **NEXT (operator-gated):** Phase D live flag-flips (each re-ranks, needs a real game) + the 4 different-seam exclusions + the deferred Phase 11 vision-frame collapse. (full record: docs/ROADMAP_HISTORY.md)
- [open, from DS GAP] **NEXT (operator-gated):** Phase D live flag-flips; a "read the parsed block" resist-value lift (the value-not-in-text set) + a percent-of-resist mode (Poppy/Rell) both need a NEW schema seam. (full record: docs/ROADMAP_HISTORY.md)

_2026-05-21 sweep: items 119/120/121 (the 3 headless-upgrade pass entries) archived to CLAUDE.md ledger items 119/120/121 + WAKEUP_NOTES (full fidelity; not duplicated here per ROADMAP-95 "Now + Next only")._

- DS Phase A/B/C completion epic (items 241-259, 2026-06-01) + champ-select SR UI redesign SHIPPED - full detail relocated to docs/ROADMAP_HISTORY.md 2026-06-19. Residual: Phase D live flag-flips (operator-gated; see EXCLUDED + CLAUDE item-255/257); champ-select sign-offs #7 (7th-item slot, engine-dependent) + #8 (CC-cards scroll-reach).
- [open, from item 239] **NEXT (operator-gated):** live-game validation to flip the opt-in flags default-on; AA-empowerment amp seam in compute_dps; Gap2 exotic candidates (% HP / crit / per-stack / dot); refactor recs (ability_dps split, cc_conditional registry externalization, __init__ changelog relocation). Operator sends the `Share/` handoff (gated; notates when/to-whom). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 237] **NEXT (all operator-gated, NOT headless):** live-game validation to flip the now-route-reachable opt-in flags default-on; assassin keystone auto-enable in /rank-assassin (product decision); patch-refresh (16.11.1 current). Marginal lifts (Riot-formula-constant naming / /tenacity diagnostic / scenario invariant) REJECTED as low-value. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 234] **NEXT (operator-gated / data-blocked):** flip the 4 opt-in flags (gate_ammo/apply_mode_modifiers/aoe_targets_hit/apply_ability_haste) after LIVE-GAME validation; auto-enable assassin keystone (operator sign-off); missile combo-clock delay IF a real per-spell cast-RANGE source lands; more per_attack runes (saturated). cc_tags operator-CLOSED. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 233] **NEXT (operator-gated):** static-CD consumer needs a live ability-haste model; flip the 3 default-on flips after live validation; missile combo-clock delay IF a real per-spell cast-range source lands; auto-enable assassin keystone; more per_attack runes. cc_tags operator-CLOSED. 5 of 6 buckets wired + static-CD accessor-staged. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 232] **NEXT (operator-gated, ENGINE bump each):** 2 owed buckets missile (missile_speed + geometry -> travel-time) + static-CD (wiki static, needs name-bridge + haste model); flip apply_mode_modifiers/gate_ammo/aoe_targets_hit defaults-on after live validation; thread role from attackrange into burst rune calls; auto-enable assassin keystone; target_hp gate for Cut Down/Coup de Grace. cc_tags operator-CLOSED. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 231] **NEXT (operator-gated, ENGINE bump each):** remaining 5 owed sidecar buckets (missile needs skillshot-range source; static-CD needs slot->ability-name bridge; geometry no hitbox consumer; recharge time-step ledger; mode_modifiers Arena re-rank validate + urf/ofa/usb/nb inactive; cc_tags BLOCKED = cc_conditional operator-CLOSED); flip gate_ammo default-on after live validation; auto-enable assassin keystone in /rank-assassin default (item-230 carry, operator sign-off). (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 230] **NEXT (operator-gated, ENGINE bump each):** item 225's 6 data sidecar buckets still owed engine wiring (the last item-229-NEXT carry); flip the live `/rank-assassin` to auto-pass an assassin keystone by default (re-rank validation vs real builds - operator sign-off); expand per_attack to other on-hit runes if DDragon exposes them. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 229] **NEXT (operator-gated, ENGINE bump each):** wire `rune_procs` into LIVE `rank_items_by_burst` /rank-assassin (re-ranks assassin builds - validate); mana_sim + runes as scenario_matrix metrics; per_attack runes (Lethal Tempo / Fleet Footwork / Hail of Blades) not yet modeled; item 225's 6 data sidecar buckets still owed wiring. (full record: docs/ROADMAP_HISTORY.md)
- [open, from item 227] **NEXT (operator-gated, ENGINE bump):** wire `ability_hps` v2 into the live enchanter `ds.hps` scorer (re-ranks enchanter builds - deferred from blind overnight because ability_hps is a documented lower-bound + it is a scoring-math change that must be validated vs real enchanter builds; substrate now ready); compose the 5 substrate modules into a unified V2 fight-report; expand rune_procs coverage + wire rune procs into burst/combo; expand mana_sim into a scenario_matrix metric. (full record: docs/ROADMAP_HISTORY.md)

- 🟡 **item 214 - Electron shell + in-game overlay** - PLAN SHIPPED `82ecfba` (`docs/ELECTRON_OVERLAY.md`). Move RC off the 2nd-display Chrome view to a stabilized single-screen Electron product, 3 render surfaces: (A) companion/pop-out window dockable beside the client, (B) in-game transparent always-on-top overlay (hotkey toggle, click-through <-> interactive) for mid-match HUD + on-the-fly DS/coach interactivity, (C) optional Pengu in-client panels. Topology-agnostic (`RC_ORIGIN` config; thin-client-now decoupled from 1-PC consolidation per origin-follows-load: relative fetches + `location.hostname` WS). 6-phase roadmap in the doc. **Phase 1 companion shell SHIPPED 2026-05-30 (item 223, merge `9f45984`):** NEW `rc-shell/` Electron project (config/store PURE + node:test 41/41 + main.js frameless window loading `RC_ORIGIN` default https://legion-rc:8888 + cert-trust scoped to RC origin host; npm install OK electron 33.4.11; Vanguard-safe DWM window no capture/memory/injection). **Phases 2+3 shell-side SHIPPED (item 269 L7, `f786516b`):** transparent always-on-top overlay window + Alt+Shift+O/A hotkeys + /api/state surface-switch poll. **HZ-D1 frontend overlay route + Phase 4 partial SHIPPED 2026-06-09 (item 378, commits `b58a1550`/`47b9e5d6`/`0522ceab`):** `?overlay=1` -> `body[data-shell=overlay]` compact 460px right-dock (CALL+BUILD+lead/choices/callouts; `web/css/overlay.css` + `web/js/overlay_pulse.js` change-pulse; 5-phase UI audit, 4 MUST-FIX resolved in-slice; 6 snapshot tests) + rc-shell ACTIVE 20s auto-revert / poll backoff 2-15s / Alt+Shift+C panel-set cycle (`panelset=` URL contract; node 77/77). **Item-378 tail SHIPPED 2026-06-10 (item 379, commits `be8a033e`/`74ba5d31`):** dashboard-side `panelset=NAME` subset rendering (coach/build/threat via `body[data-panelset]` + overlay.css 4b; threat set = CDS ledger with a game-distance typography lift; audited, 10/10 snapshot tests) + the dark-mounts fix (chip task_8a4ebe04: /api/state top-level coach/lead_projection/callouts now stamped onto state.latest at all 3 ingest sites - the lead/choices/callouts mounts were dark since W3E) + Electron visual launch (node_modules drift repaired via npm install; companion window live-verified, shell left running). **HZ-D1 slice 1 SHIPPED 2026-06-10 (item 380, merge `0b2eea62`):** companion drag region - NEW `rc-shell/src/drag_region.js` pure module injected on did-finish-load (insertCSS + executeJavaScript; preload stays empty, sandbox preloads cannot require local modules); overlay strip inert while click-through, grabbable when ACTIVE = overlay user-movable (Phase 4 control); node 85/85; running shell needs relaunch to pick it up. **HZ-D1 slice 2 SHIPPED 2026-06-10 (item 381, merge `8116c6c2`):** overlay position/panelset persistence (`rc-shell-state.json` gains `overlay: {x, y, panelSet}` via pure `overlayStateFrom`/`resolveOverlayBounds`/`mergeOverlayPatch`; saved coords clamped on relaunch, right-edge dock default; panel set survives restarts) + Phase 4 ACTIVE edge-glow indicator (NEW `rc-shell/src/active_indicator.js`, shell-injected, `pointer-events:none`, tracks Alt+Shift+A flips + the 20s auto-revert via `applyClickThrough`); node 109/109. **HZ-D1 slice 3 SHIPPED 2026-06-10 (item 382, merges `85c54ba3`+`141c1333`, audit-fix `bc8c94d3`):** Phase 4 in-overlay DS controls - NEW `#am-pane-ovds` FIGHT MODEL pane (`web/js/panels/overlay_ds_controls.js`; overlay-only, base+build panelsets) over the existing GET /api/ds-knobs, 4 knobs + top-5 re-ranked rows = build reorder; proof live (armor 300 re-ranks); item-NAME->id bridge + cascade-leak MUST-FIX in-slice; Phase 5 starter `tests/test_overlay_route_smoke.py` PANEL_SETS drift guard. **HZ-D1 slice 4 = Phase 5 stabilization tail SHIPPED 2026-06-10 (item 383, merges `96cf5241`+`544cff59`, integration `33dc9b3a`): HZ-D1 DONE.** Stable/dev update channels (`rc-shell/src/update_channel.js` + electron-updater ^6.3.9 lazy-required, autoInstallOnAppQuit never-mid-session, Shell-menu radios, `electron-builder.yml` github publish) + crash isolation (`rc-shell/src/crash_guard.js` per-window rolling restart budget 3/60s, killed/clean-exit never resurrect, give-up hides instead of strobing; `attachCrashGuard` on both windows, unresponsive logs only); package.json 0.4.0, node 145/145. Electron phases 1-5 ALL shipped code-side. REMAINING: live-game overlay capture w/ ACTIVE knob interaction (operator in a real match; shell relaunch picks up slices 1-4) ; operator packaging (`npx electron-builder`) + first GitHub Release + packaged update check (private repo GH_TOKEN) ; Phase 6 Pengu optional. Risk-register: overlay is DWM compositor not DXGI capture (in-game continuous capture was retired, 1-PC ADR-011); Borderless mandatory; Vanguard-safe (only :2999 + LCU, no memory reads/injection).

_2026-05-23 sweep: shipped items 94-149 (May 19-22 sessions covering CLAUDE.md item numbering 94-149 = s220 PGR S2-S5 + DS audit iters + cc_blended_ehp ecosystem + cc_conditional ecosystem + obj_participation 9-col + Fleet OAuth + headless-upgrade runs) archived to docs/history_notes.md per session-workflow last-3 rule. Older shipped entries (s218-s246) kept inline below for AUTONOMOUS_AUDIT chain context._

- [open, from item 215] **Carries forward:** (a) LIVE VERIFY owed - champ-select rune/item/summoner push + in-game :2999 (operator not in client). (b) poller relay-first: if RC-LiveClientRelay dies a :8889 404 short-circuits "no game" - watch or refactor poller to prefer direct local :2999. (c) OBS DXGI match-end capture watch (lock one resolution + League Borderless while recording). (d) Game-PC cross-Claude bridge SEVERED 2026-06-20 (ADR-011 retirement). (e) relocated-agent rename DONE 2026-06-20; full in-process vision collapse still deferred. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 212] **Carries forward:** (a) STALE-SHIPPED (verified 2026-06-10, item 388 round): `build_perk_ids` HAS honored `minor_primary`/`minor_secondary` since item 212 itself (lcu/lcu_rune_writer.py:144-150 + `test_build_perk_ids_honors_user_minors`) - do NOT re-pitch. (b) Visual capture of rendered chooser row OWED (operator mode=client). (c) `_csvMaybePushBuildsToLCU` 4-set cap can crowd a user build out of the in-game dropdown; click-apply unaffected. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 211] **Carries forward:** (a) recovery round 2026-06-10 (item 388): 5 MOVEs applied + 3 rows Match-V5 re-stamped; orphans 12 -> **7 residual** (1640/1633/1519/1518/1517/1506/1487) = an off-by-one LCU misattribution CHAIN (row's lcu_champ = next row's db_champ; 5 gids never ingested; 2 event-mode-excluded). Recover tool searches by the row's wrong db champion so chain rows refuse cleanly - **`--trust-lcu CHAMP` + `--match-id` flags SHIPPED (item 394, 2026-06-10**; match by LCU-truth champ or explicit Match-V5 id, puuid participation verified; 9 tests). NEXT = operator picks per-row: run the flag against the 7 residual (needs the per-row lcu_champ list + live RC + Riot key) OR clear-as-junk. (b) matches.tracked_puuid stale for older rows (Riot PUUID rotation); always re-resolve via Account-V1 for Match-V5 recovery. (c) Live Client `/allgamedata` rarely exposes gameId so performance_tracker writes game_id=0 most games; the ts-window fallback covers that. (full record: docs/ROADMAP_HISTORY.md)

- [closed by OPEN1 / item 480, 2026-06-17] item 210 page-name unify: VERIFIED already converged - all 3 non-frozen producers emit the canonical `"RC: "` (loadout_resolver / routes_loadout / routes_sr_draft / agent default / RuneWriter.PAGE_PREFIX). The lone divergence is the FROZEN `lcu/lcu_client.py` `"RC - "` (harmless - the agent wipe filter reclaims it). Guard: `tests/test_rc_page_name_prefix_unified.py`. (full record: docs/LEDGER.md item 480)

- [open, from item 209b] **Carries forward:** event-driven captures must avoid phases that coincide with the resolution swap (1920x1080 <-> 1440p), where a bound DXGI grab is unreliable. The discrete-event mental model ("only fires once at game end") is wrong - the resolution swap co-fires with the bound DXGI surface, so gate on a phase that settles after the swap. (full record: docs/ROADMAP_HISTORY.md)

- [open, from item 209a] **Carries forward:** none - both spawn sites locked by CI drift guard. (full record: docs/ROADMAP_HISTORY.md)

- [open, from UI scale v2.1] **Carries forward:** (a) Live UI capture OWED for pages #11/12/13 Active Match SR/ARAM/Arena at next in-game window (operator out-of-game during item 184 = capture deferred). (b) Page #3 Replay flex-allocation edge CLOSED 2026-05-30 (item 222 verify): item 185 already retuned `.replay-events-list` max-height 480 -> 320 (`replay_events.css:34`) + `.replay-grid-wrap` min-height 360 (`primitives.css:33`); the "max-height: 480 saturates" carry was stale. (c) Active Match stale-detection (when liveclient.champ=null + coaching_data.mtime > N min, render should show empty "waiting for game start" state instead of stale snapshot) - **SHIPPED item 281 (`f6a1c52`):** the view-router now gates active-match on a live game (liveclient non-empty) in both `web/js/main.js _viewAutoDerive` + the Python mirror, and `build_state` honors `coach.cleared_at`, so an idle dashboard shows home/lobby instead of a stale phantom snapshot. (d) The 21 operator-exceptions across 5 EXCLUSIVE Active Match CSS files (active_match/draft_elo/spike_curve/ward_heat/cd_ledger) carry rationale in inline CSS comments (density / tooltip-micro / chart-axis); do NOT bump without operator approval. right_now.css + next.css remain SHARED (ripple to all views) + UNTOUCHED. No bulk sweep authorized for the 583 hardcoded font-size declarations across 29 CSS files - swept incrementally per-page; saturation now reached. (full record: docs/ROADMAP_HISTORY.md)

- [open, from s246] **NEXT:** AUTONOMOUS_AUDIT fully closed. Remaining pending work is operator-gated, not autonomous-safe: the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual) and the one-off 101.qq.com CDN capture (now Legion-local; DONE + live-wired item 277). DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s245] **NEXT (AUTONOMOUS_AUDIT s5):** the only remaining 4.C is `effects.py` (5692) engine-core = reviewed-only-never-autonomous (operator-gated). Other autonomous-safe work largely exhausted across s237-s245; the big pending non-engine item is the s220 aggregator-G-style Post Game Review reframe (UI; needs a live standard-queue game + the per-page UI-audit ritual). Optional data: the one-off 101.qq.com CDN capture (now Legion-local; DONE + live-wired item 277). DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s244] **NEXT:** (a) operator-gated - live Arena/Mayhem augment-select render proven only by snapshot fixtures (no game ran; LCU offline); validates on next Arena/Mayhem game. (b) `arena_coach` `syn` activates on next coach reload (JS degrades gracefully meanwhile). (c) Remaining autonomous-safe: #6 local DS+matchDB MCP, or effects.py split (operator-gated engine session). (full record: docs/ROADMAP_HISTORY.md)

- [open, from s243] **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C `effects.py` (5692) engine-core reviewed-only-never-autonomous. Autonomous-safe: product opportunity #4 (augment recommender foregrounding - `core/augment_recommender.py` ships, needs marketing-pillar UI) or #6 (local MCP exposing Daemon Slayer + match DB :8893). Optional data: the one-off 101.qq.com CDN capture (now Legion-local; DONE + live-wired item 277). DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)
- [open, from #89] **Carry-forward:** Arena 6x3 champ-select VISUAL capture (3 ally cells + central me+2 + 5 enemy trio cards per c3a1e23 _csvArenaPaneHtml refactor) still owed - needs operator to commit a real Arena queue + accept + CS entry. **Don't-redo:** the canonical Arena queue is 1750 (1700/1710 legacy aliases only); Practice Tool body MUST carry explicit queueId:3140; live LCU catalog at /lol-game-queues/v1/queues is the authoritative queue-ID source. (full record: docs/ROADMAP_HISTORY.md)
- 🟡 **build-ORDER go-live + Phase-3 UI render** - **UI B+C SHIPPED `2d7da9e`** (collapsible champ-select "Build Order" card + in-game `#ds-pill` next-2 glance; headless-verified node --check + 38 snapshot/view-router; go-live was already-serving from the morning RC restart, ADR-008 auto-served). **Live champ-select visual + per-page UI-audit ritual UNBLOCKED** - the ARAM/Mayhem KNOWN-BUG keystone is ✅ fixed + proven live (`3eb2e2d`, see ✅ entry below); the B-card will render in any (now-correct) ARAM/Mayhem/Arena champ-select. Original scope was operator-gated, NOT done blind overnight. (a) `/api/build-order` serves on the next RC reload; (b) the reviewed UI session renders the ordered build per the desktop plan §6b 3-option menu (recommend: vertical "Build Order" card + `#ds-pill` glance; reject horizontal-cram) - needs the per-page UI-audit ritual + a live game. Plus a small carry-branch fix: `only_item_ids` threading - CLOSED 2026-05-30 (item 222 verify): STALE, `rank_for` already threads `only_item_ids` to the dps scorer (`archetype_dispatch.py:269` -> `rank_items` -> `_filter_candidates` only_ids gate); the "needs a DS restart" was deployed by a later ENGINE bump.

- [open, from KEYSTONE] **Residual: none functional** - only the operator's own visual reassurance of the rendered bench on a future Mayhem champ-select (the data path is proven; no capture pursued - in-game continuous capture was retired, 1-PC ADR-011). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s237] **NEXT (AUTONOMOUS_AUDIT s5):** product-level opportunity #2 (per-user pick-ban) or #3 (on-demand VLM coach), operator-directed. Remaining 4.C: `adaptation_hint.py` (1602) autonomous-OK; `agents/supervisor.py` (2312) FROZEN→needs approval; `effects.py` (5692) engine-core reviewed-only-never-autonomous. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s240] **NEXT:** autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split; or product opportunity #4 (augment recommender foregrounding) / #6 (local DS+matchDB MCP). DS conditional arc operator-CLOSED (s232). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s239] **NEXT (AUTONOMOUS_AUDIT s5):** opportunity #3 (on-demand VLM coach - operator-gated Legion capture) OR autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split. 101.qq.com CDN seed DONE + live-wired (item 277). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s242] **NEXT (AUTONOMOUS_AUDIT s5):** remaining 4.C: `agents/supervisor.py` (2312) ✅ SPLIT SHIPPED s243 (`ca30daa`) - was RESOLVED NOT frozen; CLAUDE.md hard-list has `ops/rc_supervisor.py`, NOT `agents/supervisor.py`; s238's "FROZEN" label + the audit 4.C "extract _Phase3Watcher+CircuitBreaker" target both conflate the frozen sibling (those 2 classes are `ops/rc_supervisor.py:210`/`:340`, NOT in agents/supervisor.py); this file is edited routinely w/o frozen-authorization (s168/s171.8/s173/s218/s236). Split is autonomous-eligible via the s237/s242 byte-verbatim facade pattern over its REAL surface (`_QuietHandler`/`_WebServer` + `EphemeralStub*` excs + the ~780-LOC `Supervisor` god-class), with an elevated Phase-3 verify bar (Phase-3 has no restart_trigger; s236 `2329ebf` mtime-watcher auto-restarts it on the split -> confirmed post-split: Phase-3 live-restarted healthy, GET /api/env 200 (see the ✅ s243 entry at the top of this list for the full record). (full record: docs/ROADMAP_HISTORY.md)
- [open, from s231] **NEXT:** the execute vein is largely tapped (remaining clean executes scarce - Veigar deferred-fixture, others already plain-int mapped-correctly). The autonomous conditional-seed-expansion vein is approaching exhaustion like the s223-227 pure-data sweep did; remaining `target_no_setup` candidates are mostly already-correct plain ints (conversion ROI now low - only adds value if a future Part-2 live-advisory surface consumes the downgrade). Likely flag to operator: high-ROI remaining DS work is Part-2 (live target-state plumbing - architectural, wants sign-off). DrMundo E still blocked on a `caster_low_hp` vocab decision. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s230] **NEXT (self-continuing loop):** more debuff-state-family conditional amps (each needs fresh Meraki verification - the s229/s230 lesson: ROADMAP/_meta recollection mis-names mechanics ~half the time). DrMundo E still blocked on a `caster_low_hp` vocab decision (caster-missing-HP, NOT target - flag for operator). Aatrox W needs the positional question answered architecturally, not pure-data. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s229] **NEXT:** more conditional seed-expansion on the 2-term vocab (Lux Illumination, Aatrox W chain-landed [resolve positional-vs-CC first], Fiddle Q fear-state, debuff-state family) - each needs Meraki block verification + the discrete-pair check. **DrMundo E is caster-missing-HP, not target** → needs a `caster_low_hp` vocab term, flag for operator. Literal B-2 live-plumbing **shelved as a mis-feature** (would need a separate live-advisory surface). s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from s228] **NEXT (option B continues):** **Part 2** - thread real liveclient target HP%/CC into the ranking call (`coach_integration/archetype_dispatch.py` + state-builder + `/api/ds-preview`; `target_full_hp` has a clean liveclient signal, `target_no_cc` stays commits-default until a CC source exists). Plus a fast pure-data conditional seed-expansion batch (s207→s215/s217 pattern): Lux Illumination / Aatrox W chain-landed / Fiddle Q fear-state / more sleep/charm/mark amps. **DrMundo E is caster-missing-HP, not target** - needs a `caster_low_hp` vocab extension, flag for operator. s220 aggregator G Post-Game-Review reframe remains the big pending UI item. (full record: docs/ROADMAP_HISTORY.md)
- [open, from Post Game Review aggregator G] **Carry-forward (operator-gated):** per-page UI-audit ritual on the next live PGR open. (full record: docs/ROADMAP_HISTORY.md)

- 🟡 **Live ARAM Mayhem test (carried s214 + s215)** - operator scheduled post-/clear. Authorized to monitor + fix inconsistencies live without permission. Validates: build-chooser pushes correctly to LCU runes/items/spells (now 3-variant + Experimental row), archetype-mismatch nudge surfacing on first non-trivial item buy, comp-aware row-3 tip rendering for MAYHEM role label, Phase 5.9.21 Thresh/Sona/Kalista/Malzahar ability_dps lifts riding through DS preview.
- **[CLOSED 2026-06-20 per operator]** Phase 3 fixture flow (steps 5-14) - the new Electron overlays get their own audit passes; dashboard re-audit deprioritized (operator to revisit later, not deferred). Original (was PAUSED 2026-05-10): canonical fixtures champ-select/active-match/game-summary/last-match. Step 4 (Loading Screen) shipped in s166. Per `feedback_phase3_fixture_ritual.md` - each fixture begins with visual-hierarchy audit subagent, ends with `/done` + `/clear`.
- **[CLOSED 2026-06-20 per operator]** Active Match view (step 5) - same call as Phase 3 fixture flow (Electron overlays self-audit; dashboard re-audit deprioritized, revisit later). Original: steps 1-4 shipped. Step 4 shipped s171 (`73b66ec`, 2026-05-12): static SR/ARAM map base from `/data/ddragon/16.8.1/img/map/*` + champion-dot canvas overlay polled from `/api/vision-state` at 500ms + MIA badges (`missing_for_s > 12`) + JG-gank warning band (enemy MIA > 20s in ENEMY_JUNGLE/RIVER) + view-gated polling. **Remaining (3):** zen-lock in-game + RIGHT NOW fold + bridge-pending -> dev-panel button. **Fleet view removal SHIPPED 2026-05-19** (overnight headless): dead JS poll of `/api/health/peer` (~158 LOC in `web/js/main.js`) + ~99 LOC fleet CSS in `bridge_pending.css` + 3 `body[data-view="fleet"]` selectors in `header.css` stripped. Backend `/api/health/peer` + `/api/health/all` rollup kept (other consumers). Snapshot tests 13/13 green.
- 🟡 **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- 🟡 **set_augment_intent Cherry endpoint discovery** (s170 punch list item #6) - `tools/lcu_agent.py:1179-1228` (Legion-local) now implements a 4-endpoint PATCH chain handler (item 187 Slice E scaffold + item 188 Slice C `0a4ee4d` wire). Endpoint priority chain (first 2xx wins): (1) PATCH `/lol-cherry-game-intra-event/v1/augment-select` primary guess + (2) PATCH `/lol-cherry/v1/augment-select` namespace fallback + (3) PATCH `/lol-cherry-summoner/v1/augments` summoner-scope fallback + (4) POST `/lol-cherry-game-intra-event/v1/augment-select` method fallback. Body shape `{augmentId, slotIndex}` for Arena 4-augment progression rounds 1-4. All 4 failing returns a full error envelope with each attempt's status so live verification surfaces the working endpoint. **Live verification OWED** at next Arena 1750 champ-select augment phase (Legion-local now, so no cross-machine redeploy dance - edit `tools/lcu_agent.py` in place + restart the RC-LCUAgent task) + then `RC_LIVE_ARENA=1 RC_LIVE_AUG_ID=<id> py -m pytest tests/test_set_augment_intent_handler.py::LiveAugmentSelectIntegrationTests` to log which of the 4 endpoints the live LCU exposes (shrinks attempts tuple to verified surface). Recipe + rationale: `docs/CHERRY_AUGMENT_SCAFFOLD_NOTES.md`. **Historical** (s170 -> s171 -> 2026-05-17): augment-select was first stubbed `augment_intent_unsupported` until s171 added the command to the allowlist; a live Mayhem/KIWI probe at 2026-05-17 (`C:\RC-Agent\rc_augment_probe.py`) found neither `/lol-cherry/*` nor `/lol-game-augments/*` exposed mid-Mayhem (all 404; KIWI is event-mode, not Cherry/1750) + no `:2999` augment field (the "augment" substring is i18n tooltip noise) - the negative result on KIWI does NOT preclude the 1750/CHERRY surfaces the 4-PATCH chain now targets. Companion finding: on-demand screen capture survived a full Mayhem match incl. match-end cleanly (1 match - distinct mechanism from the retired continuous screen-agent). OCR fallback remains a parking-lot if all 4 PATCH endpoints fail in live Arena 1750.
- 🚫 **FU03 - clipboard helper** - permanently superseded by FU04 same-day approval (Personal keys don't expire; daily-renewal helper unnecessary).
- **Vision regions calibration** - tune `data/vision_regions.json` bboxes. Blocked on live game for calibration frame.
- **DS calibration pipeline** - analyzes operator's actual DS pick outcomes vs game results to tune item-curve weights. Blocked on SR ranked freshness: `rewind_history.db` now has 2900 matches (latest 2026-05-14 via RC-RewindCatchup weekly cron item 101) but Ranked Solo (queue 420) latest is still 2025-12-15 + only 0 SR records in `data/ds_calibration.jsonl` per item 125 probe. Normal Draft (queue 400) has 11 SR matches in last 14 days but the calibration delta wants ranked specifically. Waits on ~20+ ranked-game samples. (Note: this is the SEPARATE DS-calibration concern. The ADR-007 phase 2 postmortem-from-rewind death-pattern analyzer `scripts/postmortem_analyze.py` SHIPPED 2026-05-21 per CLAUDE.md item 124 + cadence task LIVE per item 125; that pipeline is the personal-context death-pattern lane, not this DS-calibration delta lane.)
- **Bridge Watcher acceptance-criteria** - need 50+ real-traffic auto-action samples (currently synthetic only). Watch `auto_ok_since_boot` vs `auto_err_since_boot` on RC heartbeat.

## Open items - Medium priority

- ✅ [STALE-SHIPPED, corrected 2026-06-10 item 388 round] **DS 6-bucket engine wiring** - this entry had drifted: 5 of 6 buckets WERE wired items 231-233 (ammo->gate_ammo, geometry->aoe_targets_hit, recharge ledger, mode_modifiers, missile) + item 234 added apply_ability_haste; static-CD = honest no-consumer; cc_tags BLOCKED (cc_conditional operator-CLOSED). See the BACKLOG "DS sidecar bucket wiring (item 225 carry)" entry which was always current. Remaining = the live-validation default-ON flips (operator-gated, listed under High priority). CC-duration-seconds ceiling note stands; do NOT re-research.
- **Auto-ops verb expansion** - once Phase 3 auto-action success rate clears 95%, add: `tail .* log`, `restart agent .*`, `verify .*` to Legion `auto_ops_verbs`.
- **Peer auto-action lanes** - watcher installed but `--enable-auto-action-lanes` is OFF. Enable when success rate is proven.
- 🟡 **CI Watchdog - auto-fix red CI via headless claude** (item 204 `adb2e96`, 2026-05-26) - scope `docs/CI_WATCHDOG_PLAN.md` ONLY this session. Poller every 2 min on Legion; on red main, invokes `claude -p` headless on dedicated worktree at `C:\RC-CIWatchdog\` restricted to ruff/lint/py_compile/import fixes; opens `ci-fix/<run-id>` PR; escalates after 2 strikes via ESCALATION.md + bridge. **UNBLOCKED 2026-06-18 (operator answered the 3 gating Qs):** (1) PR auto-merge policy = AUTO-MERGE green ci-fix PRs (self-heal red main unattended, matches overnight-headless posture); (2) bridge escalation envelope = reuse the EXISTING `core.bridge.send` schema (no bespoke envelope); (3) stale-fix on new commits = CANCEL + restart on newest HEAD (never merge a fix built against stale code). **BUILT 2026-06-18 (commit `391191be`, item 204; 19 passing tests) - NOT armed:** poller skeleton + red-CI detector + monotonic sentinel + 2-strike escalate + frozen-guard + `core.bridge.send` reuse all shipped (`tools/ci_watchdog.py`, `tools/ci_watchdog_fix.md`, `ops/RC-CIWatchdog.xml`, `tests/test_ci_watchdog.py`). REMAINING (operator-gated, do-not-flip-blind): the live `claude -p` dispatch wiring (currently a documented stub) + the schtasks ARM + the `C:\RC-CIWatchdog\` worktree - an unattended auto-merge-to-main loop must dry-run a live red-main first (CI_WATCHDOG_PLAN.md step 5). Verified shipped by the item-509 swarm.
- [closed by Game-PC retirement 2026-06-20] LCU phase-watcher deploy: `phase_watcher.py` (formerly `gamepc_phase_watcher.py`) is retired with the 1-PC consolidation (ADR-011); the `phase_watcher_install.ps1` deploy is moot. `aeff4a6` historical follow-up added both files to `dashboard/routes_static.py:_AGENT_ALLOWED` + revived RC-Supervisor. (full record: docs/ROADMAP_HISTORY.md)

- [open, from Mayhem/Arena augment recommender] **NEXT (operator-gated, not provable offline):** live Mayhem augment-select cross-check (OCR→rank vs pick made). (full record: docs/ROADMAP_HISTORY.md)

## Fleet status at a glance

Live status is intentionally NOT duplicated here - a static table goes stale (this one had drifted to pid 1108 / ENGINE 1.63.0 / patch 16.11.1). Sources of truth:

- Processes, pids, listeners, scheduled tasks: `python tools/rc_facts.py` + `ops/runtime/health.json`
- All-node health rollup: `GET /api/health/all` (dashboard `:8888`)
- Daemon Slayer engine / patch / coverage: `GET :8893/health` + `data/daemon_slayer/current.txt` (ENGINE 1.121.0 / patch 16.12.1 as of 2026-06-14); DS feature + cc_conditional wave history lives in `agents/daemon_slayer/CHANGELOG.md` + `docs/DAEMON_SLAYER.md` (was inlined here as a ~20KB table cell - removed, not lost).
- TFT: Set 17 patch 17.3

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Don't break the cross-Claude bridge** - `vision_token` + `mcp_token` are SEPARATE; never let one resolver fall back to the other's file.
