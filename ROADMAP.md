# Riot Commander - Roadmap

## WHERE WORK LIVES - this file is THE tracker

**One tracker rule (operator-directed 2026-07-26):** open work lives HERE. If another doc
claims to be a "source of truth" or "single source of truth" for open work and is not in the
table below, it is a SUBPLAN - scope it to its program or archive it. Do not start a rival list.

| doc | holds | may I put open work in it? |
|---|---|---|
| `ROADMAP.md` (this) | ALL open work, NOW / NEXT / LATER, stable `RM-NN` ids | **YES - this is the one tracker** |
| `BACKLOG.md` | aspirational tier, overflow only (ROADMAP is CI-budgeted < 80KB) | only if it does not fit here |
| `docs/LIVE_GAME_GATED_SYNC.md` | rows that need the operator IN a live game | only live-gated rows |
| `docs/DS_SWEEP_TRACKER.md` | **authoritative `RM-NN` id registry** - take ids from here, never from ROADMAP prose | no - ids only |
| `docs/LEDGER.md` | append-only per-item completions | no - history |
| `docs/ROADMAP_HISTORY.md` | relocated shipped/closed narrative | no - history |
| `WAKEUP_NOTES.md` | session continuity, auto-pruned to 3 | no - per-session |
| `docs/specs/**`, `docs/_archive/2026-07-28-research-consolidation/**` | per-item spec-first output, one per item | no - they are INDEXED by a ROADMAP row, never a rival plan |

Program subplans (scoped, NOT rival trackers): `docs/ORCHESTRATION_PLAN.md` (loop director,
machine-read each cycle), `docs/RC2_PLAN.md` (RC 2.0 program only),
`docs/OVERLAY_BUILD_MASTER_PLAN.md` (overlay build spec).

---

_NOW / NEXT / LATER buckets with stable `RM-NN` item ids (assigned 2026-07-17, mdclean C2; ids never renumber - new items take the next free id). Shipped/closed entries relocate verbatim to `docs/ROADMAP_HISTORY.md` (append-only); per-item ledger in `docs/LEDGER.md`; aspirational work in `BACKLOG.md`; live-gated drains in `docs/LIVE_GAME_GATED_SYNC.md`._

> **Pruned 2026-07-18 (per-item adjudication pass, 85125 -> under the 80KB `tests/test_doc_size_budget.py` budget).** Every NEXT bullet was decided against ground truth (LEDGER, git, files on disk, the live `:8893`), NOT against its own prose. Shipped research narrative relocated verbatim to `docs/ROADMAP_HISTORY.md`; purely live-gated rows rerouted to `docs/LIVE_GAME_GATED_SYNC.md` (which already carried most of them). No RM id was deleted and no open work was dropped - every id below is still reachable, either as a bullet or as a routed row in the tables. Rationale + evidence per bullet: the 2026-07-18 block in `docs/ROADMAP_HISTORY.md`.

> **Relocated 2026-07-25 (operator-directed cleanup, 79443 -> 71.6KB against the 80KB budget).** Every SHIPPED row's narrative and every BLOCKED row's measurement evidence moved verbatim to the `2026-07-25 (cleanup pass 2)` block in `docs/ROADMAP_HISTORY.md`; each row keeps its verdict, its named prerequisite or residual, and a pointer. Nothing deleted, no RM id dropped, and the DS per-champion meta-valuation sweep section (PROBE HAZARD text) was left untouched.

> **Decommissioned 2026-06-24 (operator-directed, ADR-012):** the RC<->Peer cross-Claude bridge + the lessons cross-project sync subsystem were fully removed (daemons + scheduled tasks + ~67 modules/routes/tests + docs + memory). No cross-Claude messaging remains; Peer is now only the separate private-project machine. Do NOT re-pitch a bridge or lessons-sync.

> **Removed 2026-07-09 (operator-directed):** the `budget_saver` lean / 8B-local-model fallback (the `ops/budget_saver/` dir + the `RC-BudgetSaverProxy` / `RC-BudgetSaverWatchdog` scheduled tasks + the local LiteLLM/Ollama proxy + the `CLAUDE.md.full` swap-backup + the BUDGET_SAVER and LLM-vault specs) was fully removed. It produced low-quality autonomous work that got redone properly by the full model. Do NOT re-pitch a budget-saver / lean-profile / 8B-local fallback. See `docs/LEDGER.md` + memory `project_budget_saver_removal`.

---

## NOW - active lanes

- **RM-120 CLOSED 2026-07-28** - loop controller relaunched (pid 9380, run_id `eadf15e3`). The row's premise was wrong: the loop was DEAD, not running stale code. Narrative + the durable stale-lock-vs-dead lesson: `docs/ROADMAP_HISTORY.md` (2026-07-28 block).

- **RM-125 OPEN (COMMENT HALF SWEPT 2026-07-28, R222) - only the rendered residue remains, and it is DEFERRED to RM-122.** The comment half is done and machine-guarded: `tools/web_ascii_sweep.py` is the non-`ast` comment tokeniser for JS / CSS / HTML this row asked for, `tests/test_web_comment_lines_ascii.py` is the whole-tree guard, and `tests/test_web_ascii_sweep.py` carries the false-positive pins (a glyph inside a JS string / template / regex, a CSS `content:` value and an HTML text node must classify LIVE and never be swept). Residue **3153 -> 261 tool-reported**, and of that only **235 actually RENDER** - the other 26 sit in `web/legacy_index.html`'s inline `<style>`/`<script>` comments, which `_scan_html` leaves alone by design. **The remaining 235 are NOT a headless job**: `docs/RM125_web_live_glyph_adjudication.md` adjudicates every one (STRIP-candidate-deferred 152, KEEP-deliberate-UI-glyph 52, **LOAD-BEARING 31**) and routes the whole set to RM-122 for operator-present rendered-pixel judgement. Read that doc before touching a single one - 31 of them are load-bearing, meaning a strip breaks BEHAVIOR and not just looks: `web/js/lib/items_index.js:62` holds `U+2192` as an alternation branch in the build-string split regex, and `web/js/panels/right_now.js:563-566` is a producer/matcher coupling where the emitted glyphs ARE the char class the next line's regex tests. Also note `web/legacy_index.html` is NOT dead - `dashboard/routes_static.py:23-41` serves it at `?ui=legacy` and as the automatic fallback when `index.html` throws.
- **RM-125 ORIGINAL FILING - historical, RELOCATED 2026-07-29.** Full verbatim filing (the 3153-character non-ASCII census across 31 web files, split 2884 comment / 270 live-string) in `docs/ROADMAP_HISTORY.md`.

- **RM-126 DONE** (filed R223, fixed R224 2026-07-28, `65beb575`) - **RELOCATED 2026-07-29.** Full narrative (the two-latch drag fix and why returning the STORED closure is load-bearing) in `docs/ROADMAP_HISTORY.md`.

- **RM-121** 🟡 **OPERATOR DESKTOP QUEUE - items 1-3 DONE, two files left.** Five-file chain on the Legion desktop, run IN ORDER, each finished AND implemented before the next. **Exact filenames, operator-confirmed 2026-07-28 - the original note carried two typos, do not go hunting for the misspelled names:** `research coverage.txt` -> `research ocr cv.txt` -> `replay continue.txt` (note said "reply") -> `random.txt` + `roadmap work.txt` (note said "roadmap continue.txt"; `RM continue.txt` ALSO exists on the desktop and is NOT this item). Operator's model: multi-agent parallel orchestrated, looped, self-adjudicating at the QA step. **DONE:** item 1 (`ce37f4de`, `3550de16`) -> `docs/research/RESEARCH_CONSOLIDATED_2026-07-28.md`; item 2 (`5442955c`, `c441deef`) -> `docs/research/CV_STACK_2026-07-28.md`; item 3 all four sub-items (LEDGER 1091/1092/1100). **REMAINING: `random.txt`, then `roadmap work.txt`.** **The durable rule from items 1-3, worth more than the outputs:** a filed row's QA verdict is the deliverable, not its extraction - 44 pct of what the archived research docs called open had already shipped, and item 3's "6 xdist failures" did not exist at all because the command that produced them never terminated. A row marked STILL OPEN without a cited empty search, or a failure list from a run that printed no summary, manufactures work. Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-28).

- **RM-122 OPERATOR-PRESENT UI/UX LANE - THE HEADLESS LOOP MUST NOT PICK THIS UP.** Filed 2026-07-28 (R217) out of the `random.txt` part-2 block, which is headed verbatim "session 2 (operator-present; continues 2026-07-22b). Do NOT run headless." R217 was itself a headless cycle, so it RECORDED this queue and executed none of it; **a director scheduling this row to a headless agent violates the note's own gate** - every item terminates in a rendered-pixel judgement, the one class an agent cannot self-adjudicate under the CLAUDE.md R3 text-first rules. Queue in the operator's stated priority: **(1)** per-panel competitor capture library - 8 apps installed and logged in on Legion, full-window .NET `CopyFromScreen` per window rect into `C:\Users\Administrator\Documents\RC_Competitor_Research\`; **shots stay LOCAL, never committed**. **(2)** build the landscape auto-fit hub and RETIRE the 920x1280 portrait (the spatial-brand implementation, memory `project_out_of_game_spatial_brand`). **(3)** 2-3 layout alternatives per page, Home first. **(4)** `web/css/panels/champ_select_view.css` Tailwind-to-theme conversion, ruling DS4, the last off-theme page - **the champ_select shell tags stay Tailwind until DS4 lands**, so a partial conversion is a defect, not progress. **(5)** the logged one-liner defects. **(6)** optional theme deployment-map polish. **READ FIRST, do not re-derive:** `WAKEUP_NOTES.md` section 2026-07-22b, `docs/qa/UI_UX_PROGRAM_QA_2026-07-22.md`, `docs/qa/SPATIAL_BRAND_SPEC_2026-07-22.md` section 5, `docs/qa/THEME_MULTIHUE_METHOD_2026-07-22.md`, `docs/qa/DESIGN_SYSTEM_MAP_2026-07-22.md`, memory `feedback_operator_ui_qa_method`. **Item 5 is PARTLY DONE ALREADY - do NOT re-fix** the `core/performance_tracker.py:37-39` em-dash escapes or the `data/ratings/*.json` backfill behind them; both are closed, evidence in the RM-121 item-4 record. **Still real:** the footer "Legion-PC" at **`web/index.html:2328`** - the filed 2251 is a DRIFTED line number - plus a U+00B7 in the same span; and the note's bare `champ_select_view.css` path resolves to `web/css/panels/champ_select_view.css`. **NOT probed this cycle, so UNKNOWN rather than cleared** (do not let a later summary read them green): Home nav-grid gaps + loading-forever, PGR double-grade / phantom button / provenance, Replay header clip, Settings voice picker. **Trap from the note: the `.rofl` backfill left 4 net-new matches un-inserted - an INGEST gap under RM-117, NOT a UI job**, and it must not be pulled into this lane. Every page touched owes the 5-phase UI-audit ritual BEFORE its commit. **ID NOTE:** RM-122 taken because RM-121 was the highest ROADMAP id and `docs/DS_SWEEP_TRACKER.md` tops out at RM-119; tracker registration is owed by whoever picks this up (same precedent as RM-120 / RM-121).

- **RM-01** 🟢 **HAIKU-TO-ZERO Lane E CV substrate ADVANCING (2026-07-14, gemini-loop cycles 19+24; R121/R126).** The client-side CV vision tier + confidence-weighted Live-Client/CV fusion (docs/NO_LLM_PRECOMPUTE_PLAN.md, the "bigger SECOND program") now has its persistence + fusion substrate BUILT, all dormant: R121 the icon dhash atlas (core/vision_atlas_precompute.py -> data/daemon_slayer/vision_atlas_manifest.json, 227 icons); R126 the OCR region-map atlas (core/vision_region_atlas.py + data/daemon_slayer/vision_region_atlas.json, 25 regions = 21 calibrated HUD rects + the augment/fog API-gap slots) AND the LiveClient/CV partial-read fusion (core/vision_fusion.py - LC-exact-1.0 vs CV-heuristic-0.7, api_gap CV-authoritative, 0.6 stale-override, never-raises). NEXT (live-gated): calibrate the augment_card_*/minimap_fog rects against a real Arena/ARAM frame, then a validated OCR-only flip. fuse_reads is now WIRED shadow-first (2026-07-17, LEDGER 914 S6, `1e0ecfa6`): the ARAM/Arena vision tick appends a per-tick fusion record to data/fusion_shadow.jsonl at the GameVisionReader.read_tiered chokepoint, served coach output byte-identical - accrue real-game fusion_shadow before the flip. **Do NOT flip blind** - a wrong precompute is worse than a Haiku call. **COMPETITOR-LIFT ROTATION RETIRED:** the competitor-lift/overlay/companion/stat-site research category is DRAINED 4x (R100 Overlay App F / R112 aggregator D+aggregator C / R120 Overlay App F+Overlay App E / R125 Aggregator C Desktop) - the director should NOT re-pick it; rotate DS-sweep, Haiku-to-ZERO (Lane A/E), or a genuinely un-torn-down category instead.
  - _Re-probed 2026-07-25 - the 2026-07-18 "file does not exist" note is STALE. `data/fusion_shadow.jsonl` holds **230 real-game records**, so the accrual gate is MET, and triage found **41 of 41 CV overrides in the ledger's entire history were garbage** (all `gold`, all the literal value `1`). FIXED at the fusion layer with a plausibility gate. **The upstream half `core/vision_routing.py:118` is now CLOSED too (2026-07-25, ENGINE 1.243.0)** - the Sonnet-escalation merge is scoped to the requested field list behind `RC_VISION_MERGE_STRICT`, **DEFAULT-OFF and it must stay off until someone re-homes `is_augment_select`**: `modes/shared_vision.py:418` aliases it to `augment_select` AFTER the merge and it is in NO coach's `TIERED_FIELDS`, so a default-ON scope would silently regress the live-verified ARAM Mayhem augment-select path. Replay over the 232-record corpus: 104 records (44.8 pct) drop keys, all TFT-shaped. Unmeasured before any flip: the same scope shrinks `_log_fusion_shadow`'s own `cv_reads` payload. Still shadow-only; do NOT flip. Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-25)._
- **RM-02 DS build-reco counter-build: the full counter-hint program is CODE-COMPLETE; the ONLY remaining work is the in-game chip eyeball.** C2 antiheal + C6 tenacity + C3 fed + the R102/R103 situational step-2 + the live `allPlayers` roster plumbing all shipped (LEDGER 908/909/914 S1+S9/916; `e9487a78`, `1e3cab6c`, `566e472b`, `318e7e3a`; new `core/build_planner/fed_threat.py`; specs under `docs/specs/2026-07-1{6,7}-ds-c*-counter-hint-design.md`). Do NOT rebuild step-2 - the 2026-07-13 refactor spec is stale on it. **OWED (do-not-flip-blind, operator-only):** eyeball the antiheal / tenacity / fed / roster chips in a REAL game - the Electron overlay is agent-blind and a wrong hint is worse than a dark chip. Tracked as a gated row (`docs/LIVE_GAME_GATED_SYNC.md`, the RM-02 citation at the DS-plan row). Full shipped narrative + the pre-C2 history: `docs/ROADMAP_HISTORY.md` (2026-07-17 + 2026-07-18 blocks).
- **RM-03 RC 2.0 program (in flight).** Living plan `docs/RC2_PLAN.md` (9 phases / 62 stages), resume via `/RC2-Continue`; design GREENLIT Hextech (`docs/design/RC2_DESIGN.html`), League borderless @ 2560x1440. OPEN: E10 ASCII+git-history rewrite (force-push, operator-gated); E12 responsiveness levers residual (= QA62 / lever L3: BUILT DARK 2026-07-20 `875aa355` - full in-process snapshot port `lcu/snapshot_shape.py` + `dashboard/_lcu_inprocess.py` wired behind DEFAULT-OFF flag `RC_LCU_INPROCESS`; QA59/L2 already shipped as `_cached_lobby_mode`. ONLY the G1-00 live confirm owed - flag ON in a live champ-select, byte-compare `lcu.champ_select`, adjudicate the `state.lcu.config`/auto-accept-pill divergence; see `docs/LIVE_GAME_GATED_SYNC.md` G1-00 CHECK 2); E2 DS 3-game live-flip (operator-played); P5.2 served CV-flip env (`RC_LANING_CV_SERVED=1`, gated on `hz_shadow_report` agreement delta); rc-shell Electron relaunch owed (live disappear-fix/pinned); spell-fix LIVE verify owed. Shipped-stage detail: `docs/RC2_PLAN.md` + `docs/ROADMAP_HISTORY.md`.
- **RM-116 CLOSED 2026-07-26 (ENGINE 1.256.0, R194)** - three vamp/sustain follow-ons resolved (sustain score_by SHIPPED, Sundered Sky overheal REFUTED, `assume_cleave_lifesteal` SHIPPED by ID SUFFIX). **Do NOT re-commission a base-magnitude / mirror-parity vamp sweep - closed three times over (R181 + R193 + R194).** Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-26 R194) + `agents/daemon_slayer/CHANGELOG.md` 1.256.0.
- **RM-118 DS STRANDED-SEAM CLASS (2026-07-27, ENGINE 1.261.0, R197).** `assume_hsp_amp` was accepted by the engine and parsed by NO route, while both RM-115 reachability guards read GREEN because they enumerate the keys `server.py` already parses - **circular by construction.** The new guard derives its universe from the ENGINE via `inspect.signature`. Class: 195 seam-parameter occurrences / 74 distinct names; 23 route-facing stranded, 1 FIXED, 22 in a commented debt ledger that can only shrink. **A THIRD stranded shape is on record and NOT fixed: parsed but never forwarded** - `assume_item_crit_dr` / `assume_item_aa_dr` / `assume_item_enemy_as_slow` (`server.py:832/836/840`). **OPEN:** those three; the 22 ledgered seams; and `assume_hsp_amp` still cannot be expressed by `rank_items_by_ehp` / `compute_hybrid` / `rank_items_by_hybrid`, which is where item choice is actually decided - **the route+client wire made it EXPRESSIBLE, not LIVE**, and `ehp_for` / `sustain_for` have zero non-test callers. Do not let a later summary read this as a live-path win. Narrative: `docs/ORCHESTRATION_PLAN.md` R197 + `agents/daemon_slayer/CHANGELOG.md` 1.261.0.
  - **RM-118 UPDATE 2026-07-29 (ENGINE 1.264.0): two of the three open shapes are now closed or corrected.** (1) **The "THIRD stranded shape" sub-claim was STALE AT FILING** - `assume_item_crit_dr` / `assume_item_aa_dr` / `assume_item_enemy_as_slow` were wired into `_route_rank_tank` -> `rank_items_by_ehp` on 2026-07-25 (`e6b7b238`, ENGINE 1.250.0) via the `assumed_share_kwargs` tri-state block (`server.py` now ~843-853, forwarded at ~899), TWO DAYS BEFORE the R197 filing said they were not fixed. Verified on disk this session; nothing to build. (2) **The `assume_hsp_amp` EHP/tank-RANKER half is CLOSED** - threaded through all three gates (`rank_items_by_ehp` + `POST /rank-tank` + `core.daemon_slayer_client.rank_tank_for`), DEFAULT-OFF and byte-identical OFF, INERT unless a self-shield item (Sterak's 3053 / Shieldbow 6673 / Maw 3156) is in the baseline or a candidate build (the R194 per-candidate shape, not the RM-115 uniform-multiplier inert shape). Pinned by `agents/daemon_slayer/tests/test_rank_ehp_hsp_amp_rm118.py` (11 tests). **STILL OPEN:** the HYBRID ranker half (`compute_hybrid` / `rank_items_by_hybrid`, a separate module - not wired here); the 22 ledgered seams in `test_stranded_hsp_seam_r197.py::STRANDED_TODAY`. Narrative: `agents/daemon_slayer/CHANGELOG.md` 1.264.0.

- **RM-115 CLOSED 2026-07-26 (ENGINE 1.254.0)** - DS seam reachability, per-route debt 101 -> 4; the 4 remaining are DECLINED BY DESIGN (no consumer). Two durable lessons kept in history: a DS seam has THREE gates (engine kwarg, route parse, `core/daemon_slayer_client.py`), and NON-PREFIXED transports are invisible to both guards. Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-26 block).

- **RM-117 REPLAY ANALYSIS / COACHING CORPUS - substrate BUILT 2026-07-26 (LEDGER 1062); narrative relocated to `docs/ROADMAP_HISTORY.md` 2026-07-28.** Shipped: `core/{replay_roster,replay_analysis,replay_seek,replay_camera,event_patterns,corpus_hygiene,cohort_baseline}.py`, `tools/{replay_roster_pull,ladder_role_scout,timeline_ingest,mine_event_patterns,build_cohort_baseline,build_rank_baselines}.py`, `RC-ReplayRosterPull` hourly, 112 tests; corpus 108 accounts / 417 `.rofl` plus a timeline corpus CLOSED at 3005 matches. **The `.rofl` Layer-2 fence did NOT need opening and stays CLOSED** - positions, wave state and ward coverage all have measured non-Layer-2 routes.
  - **MEASUREMENT TRAPS - carried forward deliberately, because each one is a place where the obvious reading of the data is WRONG and re-deriving it costs a session:**
    - **The v2 container has NO encryption**, so the fence's CRYPTO rationale is void while its CHURN rationale stands and has worsened. Do not cite crypto as the reason.
    - **Match-V5 60 s positions carry ~2000 units of error.** `FAR_UNITS` / `SIGNAL_DECAY` in `core/replay_analysis.py` are historical-calibration constants ONLY - they are **NOT facts about junglers** and must never be quoted as behavioural findings.
    - **`RC-ReplayRosterPull` Disabled with a NON-EMPTY `busy` is CORRECT, not a stuck watchdog.** See memory `reference_replay_chain_watch_busy_gate`.
    - **No fetch-by-match-id route exists** - never plan a `.rofl` backfill. History comes from timelines, which paginate arbitrarily deep with no retention cutoff.
    - **Summoner spells are LOADOUT ONLY**; buff intervals and buff sharing are unrecoverable.
    - **Ward map positions carry a systematic uncalibrated offset** - the marker is a HEALTH BAR above the ward, not the ward.
    - **`assistingParticipantIds` carries ENEMY participants** (3167 of 25450 elite kills, 12.44 pct, re-measured 2026-07-28 over the FULL 3005-timeline corpus - an earlier 400-timeline subset read 12.63 pct) and **`monsterSubType` is DRAGON-only** (present on 10833/10833 DRAGON, 0 on HORDE 8864 / BARON_NASHOR 3034 / RIFTHERALD 2719 - discriminate on `monsterType`). Both in `docs/REPLAY_T2_PARSE_CRITERIA.md`.
    - **The `objective_participation` survivorship bias DEFLATES, it does not inflate** (sign corrected 2026-07-28 with the per-role measurement inline in `docs/REPLAY_T2_PARSE_CRITERIA.md`). The row is still REFUTED (LEDGER 1064) - do not re-promote it.
    - **B13 legal is CLOSED (2026-07-26)** - operator-granted, and a read of Riot's own terms AGREES. The **"one (1) developer account" limit is about DEVELOPER accounts, NOT tracked player accounts**, so the 108-account roster never collided with it - do not re-audit the roster against that clause.
  - **NEVER PROMOTE:** `kill_participation` (INERT - already team-normalised), `shutdowns_given` (measures being AHEAD - winners gave MORE in every role), `deaths_per_min` and TEAM `gold_deficit_profile` (they restate the OUTCOME).
  - **Only surviving candidate: JUNGLE `plate_share`** 0.082 vs 0.045, effect 0.49 - the one row through hygiene + effect gate + holdout + a pre-treatment confound control, and **still not causal**. Effect below ~0.3 does not survive the by-match holdout, and **NO p-value is reported anywhere in this table** - the gate is effect size plus holdout, so never read a surviving row as statistically significant.
  - **STILL OPEN / OWED:** (ii) no retention policy on a corpus past 6 GB growing hourly; (iii) T4 is attended and EXCLUSIVE (a Win32 `PrintWindow` path is UNMEASURED); (iv) MASTER cohort absent from `data/rank_baselines.json`; (v) the two forward Riot-terms conditions - prior WRITTEN APPROVAL before reselling/licensing or charging for Game Information, and TERMINATION requiring deletion of all Game Information, which the retention policy has to answer to. The postgame narrative (`core/precomputed_replay_narrative.py` + `core/pgr_event_report.py`) is SHADOW-ONLY; no live coach path flips. Criteria triage: `docs/REPLAY_T2_PARSE_CRITERIA.md`. Specs: `docs/REPLAY_FRAME_ANALYSIS_SPEC.md`, `docs/REPLAY_T4_FRAME_CV.md`.

- **RM-100 CLOSED 2026-07-20** - nightly critic + nightly full-suite repaired. The asyncio running-loop-marker leak is an ACCEPTED TRADEOFF - do not re-pitch a fix. One watch-item survives closure (NOT a re-open): the RC-GeminiAudit `0xC000013A` last-run result is UNPROVEN. Narrative: `docs/ROADMAP_HISTORY.md`.

- **RM-106 SHIPPED 2026-07-19** - the sanctioned Match-V5 `/replays` pull, live-proven on a 15-minute schedule. **Bodies are GZIP-framed; 404 = Riot no longer retains the file (expected); `/replays` needs the PRODUCT key.** ROTATION ANSWERED 2026-07-26 - do not re-open; **trap: a dead account reports `rotated=False` forever.** Narrative: `docs/ROADMAP_HISTORY.md`.
  - **RM-106a CLOSED 2026-07-19** - event-mode timelines ARE obtainable over the local LCU, with a permanently reduced event set (no item or skill order) and a hard 20-entry cap, so **this route can NEVER backfill.** Narrative: `docs/ROADMAP_HISTORY.md`.
  - **RM-107 SHIPPED** (`556662a7`). Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-19 RM-107 block).
  - **RM-106b** 🟢 **OPEN AND VALIDATED - the only route that recovers ITEM progression for KIWI.** With `EnableReplayApi=1` in `game.cfg`, a live replay serves `:2999/liveclientdata/allgamedata` (10 players with `items` / `level` / `runes` / `scores`), and `POST /replay/playback {"time":N}` seeks exactly - so per-timestamp item state is reconstructable by diffing `allPlayers[].items` across seeks. **Do NOT re-inherit the earlier 404 verdict** - that probe ran flag-OFF and was wrong. Measured limits: `activeplayerabilities` 400s in a replay (skill order NOT recoverable) and `eventdata` carries no `ITEM_PURCHASED` (items come from SAMPLING). The "forward-capture only" bound is CONDITIONALLY FALSE - an old `.rofl` paired with a matching old client backfills over the same route; the limit is archive depth plus locally-kept clients, not Riot retention. **SEEK-AND-SAMPLE IS NOW FULLY CHARACTERISED (2026-07-26, RM-117 / LEDGER 1062):** the seek is EXACT only when PAUSED first (`{"paused":true,"time":t}`; running playback silently drifts the sample), and sampled inventory is GROUND TRUTH - all 7 final item slots matched the match blob exactly, level 16==16. Reconstructing inventory from Match-V5 `ITEM_PURCHASED` instead is ERROR-PRONE (it must model component consumption on upgrade, starting trinkets and `ITEM_UNDO`), so this lane is not merely an event-mode fallback - it is the only source reporting the inventory the game itself had. **The archive-depth question is now MOOT for analysis:** Match-V5 timelines paginate arbitrarily deep with NO retention cutoff (a 2026-03-31 patch-16.6 match still returns 22 frames), so history comes from timelines, not from replaying old `.rofl` against kept clients. Full record: `docs/ROADMAP_HISTORY.md`.

- **RM-109 SHIPPED + CLOSED 2026-07-20** (`533d70fb`) - played-with-pro corpus, 26 matches by a LOCAL SQL join at zero Riot API calls. Roster expansion, the post-2025-09 gap and the blank-name risk are all operator-CLOSED. Narrative: `docs/ROADMAP_HISTORY.md` + LEDGER 972.
- **RM-111 SHIPPED + CLOSED 2026-07-20** (`core/aram_item_interaction.py`) - **descriptive only, firewalled from DS rank and pinned by test; `MIN_BUCKET_N=15` is a hard drop.** Narrative: `docs/ROADMAP_HISTORY.md`.

- **RM-112 CLOSED 2026-07-23** - `Share/` runs its own suite clean standalone. Narrative: `docs/ROADMAP_HISTORY.md`.

- **RM-123 DONE 2026-07-29 (ENGINE 1.262.0 -> 1.263.0, commit `8f67f810`, LEDGER 1109).** Reconciled to ONE canonical split (`_melee_ranged.attackrange_is_ranged`, ranged iff base attackrange >= 350) across all SEVEN classifier sites (not the two filed: also `coaches/loadout_resolver`, `tools/hotfix_ranged_only_melee_loadouts`, and the gate test - the dual suite caught the cross-package siblings). 173-roster scan proved the only in-band champs are Rakan 300 / Lillia 325 (both melee, were wrongly ranged under 250) and Urgot 350 (ranged, was wrongly melee under burst strict `>`). Full dual suite green; no build/loadout backfill needed.

- **RM-119 CLOSED 2026-07-28 (R217-U1, `9e7b70d1`)** - push CI gates the WHOLE tree, both halves. Narrative: `docs/ROADMAP_HISTORY.md`.
  - **The skip audit behind it is relocated to `docs/ROADMAP_HISTORY.md` (R219 size-budget pass, 2026-07-28) - the numbers, not the fence:** 162 real skip control points (127 RC + 35 DS), classed **A 78 legitimate-capability / B 75 masking / C 9 ambiguous**, where B means the test skips because the thing under test is ABSENT and therefore reports green by not running. B1 (precompute seed tables) / B3 (tautological mount scaffolds) plus the `SkipTest`-caught-by-`except Exception` relabelling bug are FIXED; **B2 (19 DS live-route sites), B4 (~22 data-contradicts-premise) and B5 (~22 gating on TRACKED, always-present files) remain OPEN.** Two traps for a re-run, both in the history entry: the obvious pattern set misses `self.skipTest(...)` and under-counts by a third, and 90 of the 108 runtime skips gate on two env vars set NOWHERE in the repo. `tests/test_skip_condition_hygiene.py` is the live guard and it is not advisory - it caught a net-new B5 skip during R219.

- **RM-124 BUILT + GATED-OFF 2026-07-29 (`266c1fac`), awaiting one-game live cadence validation before the flip.** Pure `wave_callout()` lives beside `recall_callout` in `core/event_callouts.py`; `minion_events` (the `MinionsSpawning` anchor) is extracted in `dashboard/_liveclient.py` and threaded through `_deterministic_coaching.py` into `next_callouts(..., enable_wave=)`. **Correction to the teardown below: the callout renders on the GENERIC `web/js/panels/callouts.js` sink (any `{tag,line,eta_s,kind}` row), NOT `objective_chips.js`, and it does NOT light up the `next.js:16-83` 3-lane per-lane percentage UI - that needs per-lane push %, which is the data-blocked STATE machine.** Live emission is gated OFF: `enable_wave` defaults False and the dashboard only sets it when env `RC_WAVE_CALLOUT=1`, so `/api/state` is byte-identical today. **The one remaining task is the do-not-flip-blind live validation** (see the gated-sync doc): anchor + cadence constants (`_WAVE_INTERVAL_*`, `_CANNON_BRK_*`, `_is_cannon_wave`) are PROVISIONAL and must be checked against one real SR game's `MinionsSpawning` EventTime + observed cannon arrivals before `RC_WAVE_CALLOUT=1`. Follow-on F2 (CS efficiency curve) is unblocked once validated. Original teardown context preserved below.
  - **(pre-build filing, R220 2026-07-28)** the deterministic wave / cannon clock, and the dead wave UI it would light up. Full teardown: `docs/COMPETITOR_LIFT_2026-07-28.md`. **The enabling fact, verified this run:** the Live Client DOES emit `MinionsSpawning` on the top-level `events` block - `dashboard/_state_cooldowns.py:18` names it and `:20-21` then passes an EMPTY event list downstream, and a repo-wide grep for `MinionsSpawning` returns that one comment and nothing else. The anchor is free and has never been read. Build = a 4th event extract beside `dashboard/_liveclient.py:302-313` `inhib_events` (respect the `:298-302` trap: `events` is TOP-LEVEL on allgamedata, NOT under `gameData`) plus a pure `wave_callout()` beside `core/event_callouts.py:414` `recall_callout`, joining the kind union at `:807` and mode-gated like `_RECALL_MODES` (`:148`); it reaches `/api/state` with no route change via `dashboard/_deterministic_coaching.py:50-51` and renders on `objective_chips.js`. The algorithm already exists in a different registry - `core/decision_detector.py:158-176` `_next_objective_spawn` - so this is that function with a piecewise cadence. **Tier-1, no new data source, no Riot key, no Claude, no schema lift, NO ENGINE bump.** **Do NOT hardcode a spawn time:** three sources disagree on first wave (0:30 / 1:05 / 1:30) and on the cannon-cadence breakpoint (14:00 / 15:00), and a 2025 change moved first-cannon arrival 2:05 -> 2:35 - anchor on the live event's `EventTime` and validate the cadence table against one real game before any flip. **The payoff is bigger than one chip: `web/js/panels/next.js:16-83` renders a complete 3-lane FREEZE/TRADE/CRASH/DISENGAGE wave readout whose only writers repo-wide are test fixtures** (`scripts/rebuild_sim_fixtures.py:166,191,229`), and `wave_state_now` / `wave_control` / `wave_freezes` / `cannon_cs_summary` (`core/match_metrics.py:248,253,256,244`) are four more labelled STATS rows with zero producers. **Do NOT re-pitch a live wave STATE machine** (freeze vs slow-push vs fast-push): data-blocked three ways - `:2999` exposes no minion entities, the Overlay Platform M GEP contract gives `minionKills` counts only, and Match-V5 has no minion event type (`scripts/rewind_scraper.py:311-341`), so nothing wave-shaped is backfillable from the 2966-match corpus and only a Lane-E CV route could ever see it. Follow-on F2 (CS efficiency vs a theoretical-max curve, replacing the flat `core/lead_projection.py:57` `_CS_PER_MIN_BENCHMARK_SR = 8.0`) is gated behind this clock. **Tempering note so nobody oversells it:** `tools/hz_shadow_report.py:196-201` `_NON_LANING_ACTION_MARKERS` EXCLUDES crash / freeze / push from the laning agreement sample, so a wave feature will NOT move the Lane A agreement number.

## NEXT - live-game-gated / operator-gated

- **RM-113 w-nextbuy live-frame acceptance - 1 row still OPEN (2026-07-23).** OQ16 cadence and the `#am-next-buy` no-reflow hold both PASSED. **OPEN TRINKET:** never activated in 260 samples, and the stage gate cannot explain the silence (`stageFor` returns `mid` on the clock arm alone at `c >= 600`, `web/js/lib/next_buy_model.js:68-72`). Reduces to one unrecorded fact: whether `owned_items` still held `stealth ward`. `tools/overlay_live_frame_probe.py` now records it per sample and **flushes each row** (load-bearing - redirected stdout is block-buffered), so the next SR game closes this with no new analysis. Narrative: `docs/ROADMAP_HISTORY.md`.
- **RM-114 CLOSED 2026-07-25** - the DS NEXT BUY fallback is SHIPPED and DEFAULT-ON (`core/next_buy_fallback.py`, kill switch `RC_NEXTBUY_DS_FALLBACK=0`), 173/173 champions. **Do NOT re-open.** Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-25 block).

### DS defensive-half sweep GAP specs (R132, 2026-07-19 - spec'd read-only, all UNBUILT)

Five verified gaps left over from the R132 defensive sweep. All were confirmed against live
code + raw `data/daemon_slayer/16.14.1/items.json` by the orchestrator, not just asserted by a
sweep agent. Ordered best-first. The rune RESIST half shipped as R132 (`e6a84734`) and the
three wrong mirror magnitudes shipped as R133 (`ad16ba65`); everything below is what was
deliberately NOT built in that cycle.

> **CLOSED-REFUTED 2026-07-19 (R134, `534ab3ef`) - do NOT re-pitch the `total_armor=armor+bonus_armor+ext_armor+item_resist_armor` prescription.** Every added term is misidentified, and applying it would feed three peer grant lanes into the fourth and make the result depend on the SOURCE ORDER of four peer registries. `_rune_resist_grants.py:227-230` states the real contract; `ehp.py:1663-1664` carries the invariant in code; machine-guarded by `agents/daemon_slayer/tests/test_rune_resist_signature_convention_r134.py`. Term-by-term evidence + the measured Aftershock blow-up: `docs/ROADMAP_HISTORY.md` (2026-07-25 block + 2026-07-26 size-budget pass 4).

> **CLOSED-INERT 2026-07-19 (R135, `e4ab8144`) - do NOT re-pitch the movespeed soft cap.** The League MS piecewise is real, correctly stated, and unreachable at every live site, so it is not a gap. Full evidence (the three call sites, the measured bounds, and the exact conditions that would re-open it) is relocated verbatim to `docs/ROADMAP_HISTORY.md` (2026-07-24 block).

- **RM-99 SHIPPED 2026-07-19 (R137, ENGINE 1.226.0) - DEFAULT-OFF `assume_item_health_stacks`.** Heartsteel 3084 + Arena mirror 223084 permanent-max-HP half, folded into the three main per-type EHP numerators and route-surfaced on `/ehp`, `/rank-tank`, `/hybrid`, `/rank-bruiser`. **The coefficient is 10 percent, not the spec's 8 - do NOT re-inherit the 8 percent.** Narrative (including the three spec errors it corrected): `docs/ROADMAP_HISTORY.md` (2026-07-19 block); record: the ENGINE 1.226.0 entry in `agents/daemon_slayer/CHANGELOG.md`.
  - ~~**RM-99b the Heartsteel DAMAGE half's cadence is wrong**~~ **SHIPPED 2026-07-25, ENGINE 1.245.0.** REAL, and the filed 8.5714x was EXACT. **SR 3084 corrected to 30.0 directly in the data table, DEFAULT-ON (operator call); Arena 223084 HELD at 3.5** - unsourced on its own feed per doctrine B, so `apply_heartsteel_cadence_fix` survives rescoped to the mirror only. Sibling sweep: 0 mirror cadence disagreements across 23 SR/Arena proc pairs. Magnitudes + the regen deltas: `docs/ROADMAP_HISTORY.md` (2026-07-25 block).
    ~~**NEW residual, OPEN: `3131` Sword of the Divine 15.0s vs a 90s active cooldown**~~ **SHIPPED, row was STALE (found 2026-07-26).** `_effects_data.py` already carries `every_n_seconds=90.0` on the Divine Judgment proc, guarded by `agents/daemon_slayer/tests/test_sword_of_divine_cadence.py`; landed in `3ad4d075` (ENGINE 1.246.0). The item is Nexus Blitz only, so the fix was correctness, not a live re-rank. Original filing text: `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).
- **RM-101 CLOSED** - every buildable rune covered (Bone Plating 8473, Second Wind, etc.). Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-28 relocation block).
- **RM-102 SHIPPED 2026-07-19** - Warmog's Arena mirror 443083 was credited a passive it does not have. Do NOT re-credit it. Narrative: `docs/ROADMAP_HISTORY.md`.
- **RM-103 SHIPPED 2026-07-19** - Unending Despair 2502 + Arena mirror 222502 Anguish SELF-heal. Narrative: `docs/ROADMAP_HISTORY.md`.
- **RM-104 SHIPPED 2026-07-19** - four Arena mirrors credited ZERO shield EHP, not one. **The count is four - do not re-file it as a single-item fix.** Narrative: `docs/ROADMAP_HISTORY.md`.

- **RM-105 SHIPPED 2026-07-19** - `effective_ehp_with_sustain` omitted the entire PERMANENT-HP family. Narrative: `docs/ROADMAP_HISTORY.md`.

- **RM-108 SHIPPED 2026-07-19 - unresolved-template-token detector. Do NOT rebuild it.** Narrative: `docs/ROADMAP_HISTORY.md`.

### DS per-champion meta-valuation sweep

- **RM-04 DS sweep: ROSTER CLOSED 173/173 - the open work is now SHAPE-BUILD, not scanning.** Canonical tracker `docs/DS_SWEEP_TRACKER.md` (Resolved 173/173: **GAP 135 / REFUTE 35 / FENCED 3**, closed 2026-07-18 batch32). **Do NOT re-open the roster or re-scan for uncovered champions** - further growth needs a schema lift, not another roster pass. Per-champion verdicts live in the tracker's `## Resolved verdicts` section + 101 `project_ds_sweep_<champ>_*` memory files; method in memory `feedback_ds_sweep_meta_valuation_research`. The batch-by-batch running narrative that used to live here is relocated verbatim to `docs/ROADMAP_HISTORY.md` (2026-07-18 block) - it was a duplicate of the tracker, and its "resolved 169 / remaining 4, next up = Ziggs" line was stale against the closed roster.
  - **PROBE HAZARD (measured, load-bearing - re-read before any DS probe).** `POST /rank` is the CARRY scorer (`ds.dps`) and silently IGNORES `enemy_ad_share` / `enemy_ap_share`; probing a mage there returns an AD head (Stormrazor #1) that looks healthy and is meaningless. Use the routed endpoints (`POST /rank-mage`, `/rank-enchanter`, ...) or `core.daemon_slayer_client.rank_for_primary_archetype(champ, '<archetype>', level=16, item_ids=[], mode='SR', top=200)`. Routed rows carry `delta`, NOT `delta_dps`, under key `"ranked"`. `mode="CLASSIC"` is NOT in `MODE_MAP_ID` so it disables every mode-gated deny - always probe `mode="SR"`. **Probe at `top=200`, never `top=40`** (real pools are carry 111 / bruiser-tank 143 / mage 144; a `top=40` read caused a retracted cross-axis-pool-partition claim). **ALWAYS pass explicit TARGET STATS - the route defaults are `target_armor=0.0 / target_mr=0.0 / target_max_hp=0.0 / target_bonus_hp=0.0`, and a zero-HP target nullifies every %max-HP effect** (BotRK reads #1 vs the sweep's standard `armor 100 / mr 60 / hp 2500 / bonus 1200` tanky target and **#52** against the zero default - a ~50-place swing that manufactured a false "RM-39 does not reproduce" retraction on 2026-07-18, caught and reverted the same session). Sweep-standard targets: tanky `100/60/2500/1200`, squishy `30/30/1900/800`. **Body key is `items`, NOT `item_ids`** (`server.py:794`), and there is **no `/rank-hybrid`** route - bruisers are served by `/rank-bruiser` though the module is `ds.hybrid`. **Check `gold.purchasable` / `inStore` before calling an absence an omission** - that check correctly spared Ryze's Seraph's Embrace, Taliyah's transform targets and Seraphine's Dream Maker, and it is why the "Opportunity is missing from every pool" filing was WRONG (removed from SR in 26.09; do not re-file).
  - **THE HEADLINE IS A MEASUREMENT, NOT A SHAPE.** The mage head is EXACTLY invariant across seven mages (Liandry's #1 / Blackfire #2 / Mejai's #6 for all seven; Luden's #13 for six) and **BotRK is #1 on 11 of 11 AD-routed champions**. That mechanically explains the standing "Luden's Echo appears ZERO times" fact - #13 invariantly can never be a first legendary. Replicated at scale (32 champions, 4 scorers, 28 pairs each, top-8 SET overlap: `ds.ability` 8.00/8 perfectly invariant, `ds.ehp` 7.57/8, `ds.hybrid` 6.04/8, `ds.dps` 4.54/8, and `ds.dps` BIFURCATES rather than varying continuously). Roster-wide the shipped build orders are archetype-TEMPLATED: **53 distinct orders for 173 champions**, 140/173 (81%) sharing an order, largest cohort 28; first legendary BotRK 64 / Liandry's 47 / Randuin's 28 / Echoes 11 = 150/173 (87%) on four items (supersedes the older 62/53/24/16 figures). **The BotRK lead is not uniformly WRONG, it is uniformly UNCONDITIONAL** (correct for Warwick 76.95% and Yone 91.8%; wrong for Viego, Zeri and Urgot for three DIFFERENT mechanical reasons) - so any remedy must be a DISCRIMINATOR, not a demotion.
  - **CHEAPEST ACTIONABLE FINDING, and it is NOT an objective change:** the carry pool is a fixed 111 items byte-identical across seven marksmen and categorically excludes Black Cleaver / Spear of Shojin / Bloodsong / Stridebreaker / Sterak's Gage while admitting Eclipse - removing Senna's 26.7% opener and Smolder's entire 2nd-and-3rd core BEFORE scoring runs. A filter-list edit, not L2 work.
  - **RM-86 L1 SHIPPED (LEDGER 940, ENGINE 1.218.0)** - `agents/daemon_slayer/kit_conversion.py` + a default-OFF `kit_conversion_strength` lever on 4 rankers (carry / assassin / mage / tank), byte-identical at 0.0 (proven full-roster). **Three fences survive, do NOT relearn them:** deriving the conversion vector from `damage_blocks` is IMPOSSIBLE (no attack-speed / crit / on-hit / DoT key exists in any of the 1709 blocks across 171 champions and the loader drops `effects_descriptions`), so the vector must be a prose-seeded curated registry per the `_passive_damage_overrides.py` precedent; THREE section-8 anchors are UNREACHABLE by L1 by construction (Olaf Stridebreaker and Pantheon Black Cleaver / Heartsteel are **L2 objective-coverage work**); and FIVE kits (Pantheon, Rek'Sai, Rengar, Olaf, Riven) must not be credited at face value - **a BOOLEAN gate is decisively refuted; the conversion vector must be CONTINUOUS.** `hybrid` / `hps` / `onhit` deliberately did NOT get the lever. Full narrative: `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).
  - **L2-for-hps is REFUTED - do NOT build it** (LEDGER 940, spec section 10): forcing `apply_ability_hsp_amp` ON leaves all 8 probed enchanters byte-identical to each other, so the finding is RC-2 pool membership, not RC-1. Spec section 3's claims about `hps.py` are BOTH factually false (`hps.py:648` imports the amp; `hps.py:679` adds `ability_hps_total` unconditionally). Full text: `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).
  - **OPEN SHAPE BACKLOG (the actual NEXT work).** Spec'd, unbuilt, all Tier-2 + operator-gated. Full per-id narrative + every measurement behind the verdicts: `docs/ROADMAP_HISTORY.md` (the 2026-07-25 "RM-79..RM-98 shape backlog" block, the 2026-07-26 size-budget pass 3 block, and the 2026-07-26 size-budget pass 4 block). **STILL OPEN:** **RM-79** Locke (DATA-COVERAGE, not valuation - Locke + Zaahen are absent from Meraki's 171-champ bulk map so every kit-dependent scorer falls through to `ds.dps`; the ability-data half was **NOT blocked upstream - it was blocked by an RC-CONTROLLED roster cap**, de-capped 2026-07-25 by A-26/B1 behind a DEFAULT-OFF `--full-roster`. Meraki alone is genuinely dead for them; the wiki and CDragon are not). **RM-83** Naafiri. **RM-85** Nasus (RC-2 pool). **RM-87** intra-pool `ds.ehp` weighting (Ornn/Rammus - resists pay twice while a self-EHP objective counts them once). **RM-88** Pyke (a throughput objective cannot price a discontinuous execute threshold plus a gold-and-reset economy). **RM-89** Quinn (FIRST champion carrying BOTH RC-1 and RC-2 - proves L1 is necessary and provably INSUFFICIENT). **RM-92** non-output item value - population AUDITED 2026-07-18, **do NOT re-run: 21 -> 17 REAL / 2 ARTIFACT / 2 POOL-CANDIDACY**, and the old canonical instance is RETRACTED (Soraka Moonstone "#10 of 10" is an `item_ids=[]` PROBE ARTIFACT). Term A SHIPPED for the ally-facing slice (`score_by="team_blended"` on `ds.ehp`, DEFAULT-OFF); the live default-ON flip is operator-gated. Residual = the ABILITY-HASTE class, **SIZED 2026-07-18, verdict DEFER - do NOT re-size and do NOT open a third id** (already spec'd twice as RM-39 + RM-43). **REOPENED 2026-07-29 by operator decision, then RE-CLOSED the same day by the gating experiment.** The DEFER judged a RATE-based term; a reviewed competitor export showed an AUTHORED per-spell coefficient design (`ahGeneral/ahQ/ahW/ahE/ahR` per champion) that disqualifier 1 does not reach. **The mandated gating experiment ran (audit `ops/audit/RM39_RM43_haste_gating_2026-07-29.md`) and both candidate designs are MOOT.** Since RM-39's L0 test, L1 shipped (ENGINE 1.222.0), so with `apply_ad_axis_ability_damage` ON the first-order ability path is now REACHABLE (amplifying `compute_ability_dps` x1e6 moves Aatrox + Ambessa hard: item 3143 climbs to #1). But driving the haste-derived cooldown to ~0 with the flag ON is BYTE-IDENTICAL - Aatrox/Ambessa spells score on the `measured>0` whole-game cast-rate branch, so the `theoretical=1/cooldown` path is never entered and ability-HASTE stays inert no matter how it is authored (A or B). No third id, no re-size, no build. Class CLOSED. Full pro/con QA of the two options (authored ~865 values and unfalsifiable, vs a derived rule that is falsifiable through `tools/ds_cross_eval/` but risks re-deriving inertness off the contaminated measured-rate input) is in memory `project_ds_ability_haste_measured_inert`.
  - **CLOSED sub-entries - fences only; full verbatim text in `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).** ~~**RM-80** Master Yi mis-ROUTED~~ **REFUTED + CLOSED 2026-07-25 - do NOT re-open** (`ds.onhit` is the on-hit-**AP** scorer and Yi is pure AD; sibling sweep 0 moved). **RM-82** Mordekaiser - TERM 2 SHIPPED (DEFAULT-OFF `apply_passive_aura_damage`), TERM 1 slow-credit CLOSED BLOCKED, the row's headline and the A-07 block premise BOTH REFUTED; **Rylai's does NOT move**. ~~**RM-90** support-cohort build-order collapse~~ **S1+S2 SHIPPED at 1.248.0; S3 CLOSED BLOCKED-UNFALSIFIABLE - do NOT file a fifth ally-grant row** (population is **1, not 4**, and the ally lane's ceiling is an order of magnitude under the per-slot self-EHP deficit). ~~**RM-91** HP-as-damage~~ **CLOSED 2026-07-26, BOTH HALVES** (ENGINE 1.258.0 `apply_health_damage_coupling` + 1.259.0 `apply_item_caster_hp_proc`, both DEFAULT-OFF, sort-key only, `/rank-tank` only) - **do NOT re-file the health axis or re-scan for caster-HP items**; the population is machine-derived and pinned in BOTH directions, so a "missing item" claim must first fail the census guard test. ~~**RM-93** support-quest item candidacy~~ **REFUTED + CLOSED 2026-07-25 - do NOT re-open.** ~~**RM-94** snowball stacks pinned at FULL value~~ **FIXED 2026-07-25, ENGINE 1.243.0** (Mejai's `bonus_ap_stacked` 125.0 -> 25.0); **do NOT apply RM-94 reasoning to Dark Seal 1082**, which shares the passive but is modelled 0.0 and UNDER-states. **RM-95** ability-data COVERAGE - 95a FIXED 2026-07-25, the 95b/B2 half adjudicated 2026-07-26: **do NOT build the B2 promoter** (the residual shipped at **1, not 3**, ENGINE 1.257.0 DEFAULT-OFF `apply_wiki_form_damage`; Jayce W and Mel W are NOT data gaps and both refutations are pinned as tests). **RM-98** cast-rate TIME-BASE mismatch - **ADJUDICATED 2026-07-19, BUILT 2026-07-26 (ENGINE 1.244.0, DEFAULT-OFF `apply_cast_rate_propensity_prior`) - do NOT chase a denominator replacement**; `hybrid.py` ONLY, the other five consumers are documented at the seam and deliberately NOT wired. RM-84 Morgana is FIXED via Slice C `core/ds_support_route_overrides.json` (Renata Glasc + Zilean deliberately HELD as unrepresentable by any current scorer).
  - **RM-97 pet / summon damage - PROBED ON DISK 2026-07-28 (R225), spec HOLDS, still SPEC-ONLY / OPEN.** The DAMAGE numerator credits no persistent pet: 7 pet-bearing forms carry ZERO `attribute_kind == "damage"` blocks (Zyra P Garden of Thorns + W Rampant Growth, Heimerdinger Q **both** turret forms, Ivern R Daisy!, Yorick P Shepherd of Souls, Yorick R Eulogy of the Isles whose one block is the `("Mist Walkers","other")` COUNT). **Two corrections to the filed one-liner:** (a) "prices them at exactly ZERO" is numerator-only - `objdamage.py:79-83` SUMMON_DPS 0.55 and `zonecontrol.py:73-77` SUMMON 0.60 already credit the same champion list on non-damage axes; (b) 3 summon forms ARE credited (Malzahar W Void Swarm, Annie R summon burst, Elise W Volatile Spiderling), so the gap is **no PERSISTENT-ENTITY uptime model**, not "no summon ever scores". Measured Zyra L11 / SR / armor 30 / MR 30 / no items: W dps 0.0 (rank 2, unlocked - the zero is the block filter), Q 9.147609147609147, E 1.2968849332485697, R 1.0865999671969822; the P row is ABSENT not zero (`SPELL_KEYS` at `ability_dps.py:209` excludes it). Pinned by `agents/daemon_slayer/tests/test_pet_summon_damage_uncredited_rm97.py` (12 tests / 27 subtests) - a RED there means a pet-damage lane landed and the pin must be updated deliberately, not loosened. A fix is still a SCHEMA LIFT (persistent-entity damage form with uptime + attack cadence), not a scorer tweak; no engine math shipped, ENGINE stays 1.262.0.
  - **ID ALLOCATION: champion GAP specs run through RM-98; the next free id is RM-119** (RM-96 Zilean + RM-97 Zyra assigned in batch32). **The old "next free GAP id is RM-99" reading was STALE** - RM-99 and RM-101..RM-104 were allocated 2026-07-19 to the R132 defensive-half ITEM / RUNE axis (see `docs/DS_SWEEP_TRACKER.md:44-48`), RM-99 shipped as `assume_item_health_stacks`, and ids RM-105..RM-118 are in use. The tracker (`docs/DS_SWEEP_TRACKER.md`) is the authoritative id registry - ids never renumber.
  - **RC-2 follow-on SHIPPED (LEDGER 941, ENGINE 1.219.0):** Dawncore 6621 + Whispering Circlet 2526 registered in the enchanter formulas registry; **does NOT fix enchanter invariance (a test pins that it does not).** **CROSS-MODE GOTCHA (guarded, LEDGER 951):** the registry is BASE-id keyed and the 22xxxx Arena mirrors resolve through it, so an SR-only edit can silently move Arena build orders - do NOT re-file a map-30 defect from item NAMES; run `tests/test_build_order_map_legality.py`. Full narrative: `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).
  - **A-27b Golden Spatula Arena deny - CLOSED 2026-07-25 (ENGINE 1.243.0).** `224403` (map 30 Arena) is now denied alongside `4403` (map 21) and `443064` 'Talisman Of Ascension'; Meraki `rank: DISTRIBUTED` was REJECTED as a generic pollution test (the whole legit Arena prismatic pool is DISTRIBUTED). **LESSON: sweep joke/meme items by ID SUFFIX across every map, never by name.** Full narrative: `docs/ROADMAP_HISTORY.md` (2026-07-26 size-budget pass 4).
  - **RM-118 mana-as-damage on the tank route - SPEC'D UNBUILT, filed 2026-07-26. Population is exactly 1.** Same defect class as RM-87 (resist axis, SHIPPED) and RM-91 (health axis): `agents/daemon_slayer/ehp.py` is the ONLY scorer with zero references to the ability model (`dps` 9 refs, `burst` 13, `hybrid` 16, `hps` 1, `ehp` **0**), so on `/rank-tank` every "caster stat converts into damage" axis is credited nowhere. **Full census of the 1709 damage blocks at 16.14.1** - health `caster_max_hp_pct`/`caster_bonus_hp_pct` 30 blocks / 14 champions (RM-91); resist `caster_armor_pct`/`bonus_armor_pct`/`bonus_mr_pct` 8 / 5 (RM-87, shipped); **mana `caster_max_mp_pct`/`caster_bonus_mp_pct` 8 / 3**; **movespeed `caster_bonus_ms_pct` 1 / 1**. **The only genuine instance is Blitzcrank** - R Static Field `caster_max_mp_pct` 2.0 per rank, Tank-primary so he routes to `/rank-tank`, and he actually builds mana (Winter's Approach / Fimbulwinter), so the payment is real and uncredited. **Kassadin and Ryze are NOT defects and must not be seeded** - Kassadin is Assassin-primary (`ds.burst`) and Ryze is Mage-primary (`ds.ability`), and both scorers consume `AbilityContext`, which already carries `caster_max_mp` + `caster_bonus_mp` derived at `ability_dps.py:299-300`. All four axes are wired in the ability model (`_registries.py:381-394`); the gap is purely WHICH SCORER READS IT. Build as the third instance of the same lever: mirror `_resist_damage_coupling.py` / `_health_damage_coupling.py` - DEFAULT-OFF flag pair, percent-free baseline pool, sort-key only, `/rank-tank` only, byte-identical control. **UNRESOLVED, do not state either way without probing:** Janna W Zephyr scales `caster_bonus_ms_pct` 30.0 and she is Support-primary, so she routes to `/rank-enchanter` (throughput, `hps.py`), which carries only a token ability-model reference - whether her movespeed term is priced there is UNMEASURED. **Also noted:** `caster_mr_pct` has an `AbilityContext` field (R49) but ZERO blocks in the data - wired but empty. **ID NOTE:** filed as RM-118, NOT RM-99 - RM-99 was allocated 2026-07-19 to the R132 defensive-half item/rune axis and shipped as `assume_item_health_stacks`.
- **RM-81 data currency (open, tooled + measured; RESCOPED 2026-07-18).** Narrative relocated 2026-07-28 - see `docs/ROADMAP_HISTORY.md`. Open work and the measurement traps are unchanged there; nothing was closed by the relocation.

- **RM-35 .. RM-48 DS sweep GAP specs (14 champions, all spec'd read-only, all UNBUILT).** These were the first 14 GAPs of the sweep and each carried a full research narrative here. **The narrative is relocated verbatim to `docs/ROADMAP_HISTORY.md` (2026-07-18 block)** - it was a partial, inconsistent duplicate: `docs/DS_SWEEP_TRACKER.md` carries **RM-35 through RM-98** (64 ids) while ROADMAP carried verbose prose for only these 14, and each champion's authoritative record is its own memory file (5.3-12.0 KB each, richer than the ROADMAP copy). No Tier-2 build has landed for any of them; `kit_conversion.py` L1 is default-OFF and explicitly cannot reach several of these anchors (spec section 10). Every one stays OPEN, operator-gated, and each needs the standard tail: RED-first tests + named byte-identical controls + ENGINE bump + Share mirror + DS `:8893` restart + live eyeball.

  | id | champion / scorer | the defect | fix class |
  |---|---|---|---|
  | RM-35 .. RM-48 | 14 champions | **table relocated 2026-07-25 to `docs/ROADMAP_HISTORY.md`** (size budget) | see below |

  The verbatim 14-row table and the RM-40 / RM-45 / RM-47 blocked-prerequisite note now
  live in `docs/ROADMAP_HISTORY.md` (2026-07-25 block). Authoritative record is
  `docs/DS_SWEEP_TRACKER.md` (RM-35..RM-98) plus the per-champion memory files.
  **Status changes made 2026-07-25 (this session):** RM-44 Amumu is CLOSED - its stated
  prerequisite (champion-sensitivity in `ds.ehp`) is REFUTED, see the NOW lane. RM-48 Azir
  is CLOSED BLOCKED-UNFALSIFIABLE - nine mages with and without pets return Liandry's #1 /
  Nashor's #16-#19 at identical params.

### Programs with open kernels

- **RM-12 Haiku-elimination program - the PRIMARY north star (drive live Haiku usage to ZERO via precomputed DS lookups) is OPEN; the item-265/266 foundations are shipped.** Shipped and on disk: Lane A `agents/daemon_slayer/matchup.py` + `/v2/matchup`; Lane B build-order tables (`data/daemon_slayer/<patch>/build_orders_{sr,aram,arena}.json`); Lane C/E/P deterministic generators (`core/laning_verdicts.py` / `core/event_callouts.py` / `core/lead_projection.py`); W3A `dashboard/_deterministic_coaching.py`; W3C `core/pickban_targets.py`; W3E `web/js/panels/callouts.js`; item-280 `core/aram_comp_verdict.py` + `/api/aram-comp-verdict`. **STANDING GATE FINDING (do not re-litigate):** the matchup engine validated as a COIN-FLIP vs real outcomes (gold-at-10min 48.8%, solo-kill duel 51-52%, n in the thousands) -> NO coach was flipped off Haiku. The matchup-derived A/B chips are SECONDARY; the correct-by-construction callouts + lead are the valid Haiku-elim wins. **OPEN (operator-gated):** matchup-engine fidelity lift (item-aware + real-level; uncertain payoff, likely a modeling ceiling) OR pivot to non-prediction surfaces; overlay Lane D (Electron Phase 2); champ_select pickban-DB flip after a counter-quality validation; the champ-select brief Haiku flip after shadow-log accrual (served stays Haiku per the item-273 AST guard; the READ-ONLY gate that clause needs now exists - `tools/champ_select_shadow_report.py`, R218 `bb746286` - so what remains is accrual, not tooling: `data/champ_select_shadow.jsonl` is empty and the report reads `state=awaiting_accrual` below MIN_SAMPLE 20). Full shipped narrative: `docs/ROADMAP_HISTORY.md` (2026-07-18 block).
- **RM-14 DS gated flip levers (dedup of the items 227-239 NEXT stacks; full records `docs/ROADMAP_HISTORY.md`).** Operator-gated, ENGINE bump where noted: flip opt-in flags default-ON after live validation (`gate_ammo` / `apply_mode_modifiers` / `aoe_targets_hit` / `apply_ability_haste` / `apply_passive_damage`; `target_hp` Cut Down / Coup de Grace gate R51 `d0b14962` rides `docs/LIVE_GAME_GATED_SYNC.md`); auto-enable assassin keystone in /rank-assassin (operator sign-off); missile combo-clock delay IF a real per-spell cast-range source lands; static-CD consumer (needs slot->ability-name bridge + a live ability-haste model); `assume_missing_hp_heal_amp` flag (hps.py:499/:828 default OFF - the R5 heal-amp seam; the BASE ability-HPS fold-in is ALREADY live per OQ22); enchanter registry omits 5 corpus-proven winners (Dream Maker / Dawncore / Shurelya / Seraph / Luden - FUTURE); the 4 non-every-AA `on_hit` entries structured cadence + live "saner not different" re-rank; Phase D live flag-flips + the 4 different-seam exclusions + the deferred Phase 11 vision-frame collapse; the percent-of-resist "read the parsed block" lift (needs a NEW schema seam); `rune_procs` into live `rank_items_by_burst`; per_attack runes expansion (saturated); mana_sim + runes as scenario_matrix metrics; the operator sends the `Share/` handoff (notates when/to-whom). cc_tags operator-CLOSED - do not re-pitch.
  - _RM-19 / RM-20 / RM-21 were verbatim restatements of the clauses above (`apply_passive_damage` + the 4 non-every-AA `on_hit`; Phase-D flag-flips + the 4 different-seam exclusions + Phase 11; Phase-D + the read-the-parsed-block resist lift). They are FOLDED INTO this bullet - RM-14 is the canonical index. Gated rows: `G2-04`, `G2-21`, `G5-05`. RM-21's own DONE note (the percent-of-resist Poppy/Rell seam, item 268 + R45 ENGINE 1.159.0) is shipped and relocated._
- **RM-15 Electron shell + in-game overlay - phases 1-5 ALL SHIPPED code-side; only the un-headless tails remain.** Plan `docs/ELECTRON_OVERLAY.md`; the shell is `rc-shell/` (companion window, transparent always-on-top overlay, Alt+Shift+O/A/C hotkeys, `?overlay=1` compact right-dock, `panelset=` subset rendering, in-overlay FIGHT MODEL DS controls, drag region, overlay position/panelset persistence, ACTIVE edge-glow, stable/dev update channels, per-window crash guard). **OPEN:** (a) live-game overlay capture with ACTIVE knob interaction - operator in a real match, shell relaunch picks up the shipped slices; (b) operator packaging - `npx electron-builder` + first GitHub Release + a packaged update check against the private repo (`GH_TOKEN`), tracked as a gated row; (c) Phase 6 Pengu in-client panels, OPTIONAL. **Risk register (standing):** the overlay is DWM compositor, not DXGI capture (in-game continuous capture was retired, 1-PC ADR-011); Borderless mandatory; Vanguard-safe (only `:2999` + LCU, no memory reads/injection). Full 5-phase slice narrative: `docs/ROADMAP_HISTORY.md` (2026-07-18 block).
- **RM-16 Carry-forwards, compressed (items 215/211/283/277/276; full records `docs/ROADMAP_HISTORY.md`).** **REFUTED + STRUCK 2026-07-19 - do NOT re-file these three:** item 215's *poller* clause (the relay CANNOT emit a 404 for a running local game; pinned by `tests/test_liveclient_self_heal_1pc.py`), item 212 entirely (RC only ever evicts its OWN `RC-`-prefixed sets, so the 4-set cap is a self-limit; pinned by `tests/test_lcu_item_sets_wipe_stale.py:166`), and item 269 entirely (closed by item 273 Slice A; a full-roster sweep found ZERO `flavor=mage` labels). Evidence for all three: `docs/ROADMAP_HISTORY.md` (2026-07-19 session-2 block + 2026-07-26 size-budget pass 4). Still open: **item 215** OBS DXGI match-end capture watch (lock one resolution + League Borderless while recording). **item 211** `matches.tracked_puuid` can be stale - ALWAYS re-resolve via Account-V1; Live Client rarely exposes `gameId` so `game_id=0` rides the ts-window fallback (the 7 residual orphan rows are gated row `G4-29`). **item 277/321** FUTURE: wire the Anivia egg-resist + Orianna ally-resist seams into a live survivability scorer + per-champion validation. **item 276** Zaahen real curated build; PGR S5 polish / responsive / ARAM-Arena variants + S4 @N; Build Insights skill tab. Do-not-redo: the DS engine is verified across 4 lanes (pen / on-hit / crit / AoE) - do NOT re-audit; item-286 prompt-cache restructure is CLOSED-not-viable (guard `tests/test_prompt_cache_floor_item286.py`).
- **RM-26 Vision regions calibration - open non-gated tails.** The 2560x1440 / ultrawide seed GENERATOR is SHIPPED (2026-07-20, `1e3a186c`): `SEED_BASES` + `seed_profiles()` in `core/vision_profiles.py`, writing through the existing atomic `save_profile`; 1920x1080 is excluded and additivity is test-enforced. TWO LIMITS, both documented in the docstring and both still open: the seeds are NOT WIRED (`load_profile` keys on the exact `config_key` including HUD toggles, not the bare resolution the seeds use, so an ultrawide first run STILL falls through to `legacy_seed` at unscaled 1080p - closing that is a live-path change), and proportional scaling stretches element WIDTHS where League edge-anchors HUD elements at roughly fixed pixel size, so the 16:9 2560x1440 seed is materially more accurate than the 21:9 / 32:9 ones. `data/vision_profiles/` is gitignored, so the generator is the deliverable and seeds are per-machine. Colorblind/gamma OCR correction is default-neutral and byte-identical until a real frame tunes it. Lane E CV matcher is sound at the pristine ceiling (LEDGER 866, `tools/vision_atlas_validate.py` -> `data/vision_atlas_accuracy.md`; champions 173/173, 0 genuine mis-id, smallest distinct margin 0.299) - in-game accuracy + the per-champ/objective/item live wiring stay gated. **Foundation don't-redo:** `core/vision_template_match.py` is a DEFAULT-OFF generic `match_icon` atlas (R96 `406ac0e3`), DISTINCT from the minimap-DOT-scoped `core/minimap_identity.py` - do not conflate them. The per-HUD bbox TUNING of `data/vision_regions.json`, the colorblind frame and the per-field OCR-only flip are gated rows.
- **RM-27 External refactor-plan triage (item 312) - triage done, two accepted items unbuilt.** Operator handed 3 Desktop review docs (kept OUT of the repo per the outreach rule); each proposal was triaged against hard rules / frozen list / settled ADRs, proposals treated as INTENT and verified vs live code. SHIPPED: P4.2 `RC_TESSERACT_CMD` env override in `core/vision_tesseract.py`. REJECTED, do not re-pitch: P3.1 amp-DRY (PREMISE FALSE - already centralized in `effects.py`); P5.1 drop-the-em-dash-ban (the settled PS-ParseFile hard rule). GATED on explicit approval: P1.2 / P1.3 / P4.1 (frozen files), P2.1 FastAPI rewrite (undoes ADR-008 + the Vanguard-safe stdlib posture), P5.2 SQLite ledger (already solved by the LEDGER relocation + budget). **OPEN, each its own session:** **P2.2** structured-output (touches the live coach parse, partly redundant); **P3.2** antitank dynamic %HP (a design change - antitank is intentionally static per item 308); **the 6 UNIVERSAL_FILES template convention update** (this is also the whole residual of the old RM-18, whose 2026-06-05 NEXT-UP block is closed and relocated; its P3.2 live-producer route-wiring + ENGINE bump ride the RM-14 Phase-D tail). Loop note: the Gemini director reads repo-only and picks ONE bounded item per cycle - pin any accepted item in NOW to route it through the loop.
- **RM-06 Overlay redesign owed tail.** Offered, not built: a manual ward tracker; folding the enemy-spell tap-tracker into the CD ledger if the operator prefers one panel. **Do-not-redo fence (standing):** a native-HUD REPLACEMENT is impossible - Live Client exposes NO ability/summoner cooldowns, buffs, wards or XP, so the overlay AUGMENTS only and the enemy tracker stays MANUAL. The Electron live-confirm and the `ward_cue` teardown (a deliberate live-session job: live serializer + test + overlay DOM contract, never a blind headless delete) are gated rows. Detail: LEDGER 760 + `docs/ROADMAP_HISTORY.md`.
- **RM-17 build-ORDER go-live + Phase-3 UI render - code SHIPPED, only the live render review is owed.** UI B+C shipped (`2d7da9e`): collapsible champ-select "Build Order" card + in-game `#ds-pill` next-2 glance; `/api/build-order` serves; go-live was already-serving via ADR-008 auto-serve. The ARAM/Mayhem keystone bug that blocked the champ-select visual is fixed and proven live (`3eb2e2d`), so the B-card renders in any ARAM/Mayhem/Arena champ-select. **OPEN:** the reviewed UI session + per-page UI-audit ritual on a live champ-select (gated). CLOSED-as-stale: the `only_item_ids` threading carry-branch fix - `rank_for` already threads it to the dps scorer (`archetype_dispatch.py:269` -> `rank_items` -> `_filter_candidates`), verified item 222.

### Rerouted to `docs/LIVE_GAME_GATED_SYNC.md`

These items were purely "owed live capture" or "flip after a real game". The gated doc (125 open rows across 7 gates, reorganized by gate 2026-07-18; G2-43 added 2026-07-24 by RM-41) already carried each of them - the ROADMAP copy was a duplicate. Ids are retained here so nothing becomes unreachable; the work itself is drained from the gated doc, not from here.

| id | item | where it lives now |
|---|---|---|
| RM-05 | Overlay live-gated punch-list (round-1 MISSING-IN-GAME cluster + round-2 drag/move + enemy-spell chip + objective gauges) - operator Ctrl+Alt+A verify each, do NOT ship blind headless | `G2-28` / `G2-32` / `G2-33` / `G2-34` / `G2-35` (+ the `RM-05` disposition notes: build META row stays STATIC; DMG/SURV/UTIL knob steppers dead; item right-click 5-choice radial dead; `am-statspanel` intent CLARIFY with the operator before acting). CONFIRMED-OK: `am-mmrect` ZOI markings DO show in-game |
| RM-07 | L4 capability-gap live tail - SR-game validation with the chip ON (`RC_CAPGAP_SURFACE` default-ON since LEDGER 765) | the `SOURCE: RM-07` row - "the ONLY remaining gate is the in-game EYEBALL of the chip CONTENT vs a real enemy comp". Do-not-redo: both tail axes are already IN `core/ds_capability_gap.py` (zone_control gate :189, objective_damage gate :226). Open non-gated tail: the advisory DS-assumed-vs-actual divergence line + the shard seed-back into DS `base_stats` (a product call). E1 TFT deterministic twin untouched |
| RM-08 | Per-page UI/UX review live tail - the E.1 ACTIVE knob physical-press round-trip | the ACTIVE knob-press row (bundles the ROADMAP-19 E.1 round-trip; SOURCE LEDGER 598). **Partly stale as written: the `RC_COMP_HP_LEAN` live EYEBALL is already DONE (2026-06-23, R24)** - only the default-ON flip AUTHORIZATION remains, and that is its own gated row. Respect: replay scroll-wrap is correct (do NOT "fix"); the operator-locked `last_match.css` sub-floors are fenced in RM-34. Recon harness `ops/runtime/ui_recon/` |
| RM-09 | HZ shadow -> agreement -> flip chain (do-not-flip-blind): accrue real-game HZ shadow, re-measure precompute-vs-Haiku agreement on the item-614 corrected tables, THEN the live coach flip | `G7-01` (Lane-A laning-agreement flip gate, HOLD) + the `RC_LANING_CV_SERVED=1` >=70%-agreement rows. Arena tail: the shadow-report tool + flip-readiness read after real Arena games (R76, LEDGER 763). v4 full-roster DATA regen deferred to `BACKLOG.md`. Don't-redo anchors (agreement de-bias 574, mismatch diagnose 575/576, enemy-combo model fix 614) in `docs/ROADMAP_HISTORY.md` |
| RM-10 | Orchestration A3 tail - SURFACE the DS-coach hints into coach context only after `data/ds_coach_hints_shadow.jsonl` accrues + validates on real games | the `SOURCE: RM-10` row. The A1-F1 fanout is CLOSED (director NO_WORK) |
| RM-11 | ZOI district + identity live tail - verify macro callouts + district vector + MIA rings + OBS round-trip in a practice SR game before flipping `obs.frame_source` / roster-wiring ON | the two `RM-11` rows. Minimap IDENTITY is already DEFAULT-ON + live-verified (`fe37c534`, 2026-07-08). Do-not-redo: per-champion isolation via color/size/motion is a validated dead-end - presence detection + the native-res grab are the foundation (memory `project_zoi_minimap_reality`) |
| RM-13 | Live UI watch - tick champ-select + in-game dashboard vs `/api/state` each ranked game, fix in-session where ADR-008 auto-serves | `G4-15`, tagged **"Standing per-game, never one-shot - it does not close."** That is a ritual, not a roadmap item |
| RM-22 | Post Game Review aggregator G reframe - per-page UI-audit ritual on the next live PGR open | `G4-27` + the `F4 PGR UI-audit` batch reference |
| RM-23 | Live ARAM Mayhem residue - the Phase 5.9.21 Thresh/Sona/Kalista/Malzahar `ability_dps` lifts riding through DS preview, unverified in a live Mayhem game | the `ability_dps` consumer rows. The item-243/244 live watches already covered build-chooser LCU push + archetype-mismatch nudge + comp-aware row-3 tip |
| RM-24 | `set_augment_intent` Cherry endpoint discovery - `tools/lcu_agent.py` ships a 4-endpoint PATCH chain for the Arena 1750 augment-select phase (first 2xx wins); live verification owed at the next Arena augment phase | the `SOURCE: ROADMAP.md:113 / RM-24` row. Recipe + full historical record: `docs/CHERRY_AUGMENT_SCAFFOLD_NOTES.md` |
| RM-25 | Mayhem/Arena augment recommender live cross-check - OCR->rank vs the pick actually made | the two `RM-25` rows, incl. the **drainable-today workaround: hold an augment ~25s so a vision tick lands**. The reco-pipeline field-bug is FIXED (`67519018`, LEDGER 867) |

## LATER - parked / blocked / low-value

- **RM-28** 🟡 **DEEP-AUDIT PROGRAM (2026-06-11, operator overnight directive - SUPERSEDES the per-item NEXT-UP queue while it runs).** Standing charter `docs/DEEP_AUDIT_CHARTER.md` (P0 baseline -> P8 docs); loop armed via ops/loop (max_cycles 100); live state on Desktop `RC_DEEP_AUDIT_SYNOPSIS.md`. Frozen files + history + gist + .md rewrites authorized; vanguard/CV + 2-PC/gamepc prune; BOM/encoding retro-sweep; Haiku/Sonnet-to-ZERO completion; Electron overlay to FULL (web dashboard no longer required); gemini holds scope questions.
- **RM-29 Repo-audit D1 residual (own session).** The full 362-file Share-mirror de-dup / build-time-gen stays deferred (outward-facing gist + CI `--check` coupling). D1 churn-fix + D4 ratchet + D9 auth test shipped: LEDGER 490/506 + `docs/ROADMAP_HISTORY.md`. Do NOT re-pitch the D4 hot-path-narrowing revert or the D12 logger reverts as bugs (deliberate fixes).
- **RM-30 Dashboard data-wiring gaps (low value).** Open: ~88 `st-*` ADAPTATION rows have no live producer - correctly HIDDEN in-game by `_hideEmptyStatRows()` (`94f1e07b`); wiring live producers only affects the retired Chrome dashboard panel (LEDGER 573). `/api/ward-heat` permanently empty (`core.ward_events.record_ward` has no producer - Live Client emits no WARD_PLACED). Shipped parts (tonight-pick tips, cs_at_10/csd_at_15 producers, phantom active-match fix): `docs/ROADMAP_HISTORY.md`.
- **RM-31** 🟡 **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- **RM-32 DS calibration pipeline - UNBLOCKED + BUILT 2026-07-25; the residual is CORPUS, not code.** `core/ds_calibration_agreement.py` + `tools/ds_calibration_report.py` (Laplace, `MIN_BUCKET_N = 20`, DS-rank firewall pinned in both directions). **The first result is instrumentation, NOT a verdict** - 50 joinable games / 462 observations cannot separate a +0.0677 shrunk delta from noise on one account. The keyless-row blocker is CLOSED (D-01b: the producer mints through the existing `core/live_metrics.match_key()` seam, 572 of 6109 rows backfilled). **ARAM 5257 is provably UNRECOVERABLE and that is not a bug** - the ARAM played there is Mayhem (`KIWI` / queue 2400), which Match-V5 403s. Measurements + the two corrected counts: `docs/ROADMAP_HISTORY.md` (2026-07-25 blocks).
- **RM-33** **Auto-ops verb expansion** - once Phase 3 auto-action success rate clears 95%, add: `tail .* log`, `restart agent .*`, `verify .*` to Legion `auto_ops_verbs`.
- **RM-34 Standing fences (do-not-redo).** Originals in `docs/ROADMAP_HISTORY.md`; the consolidated list relocated 2026-07-28 to the same file. Read it BEFORE re-opening any fenced line.

## Fleet status at a glance

Live status is intentionally NOT duplicated here - a static table goes stale (this one had drifted to pid 1108 / ENGINE 1.63.0 / patch 16.11.1). Sources of truth:

- Processes, pids, listeners, scheduled tasks: `python tools/rc_facts.py` + `ops/runtime/health.json`
- All-node health rollup: `GET /api/health/all` (dashboard `:8888`)
- Daemon Slayer engine / patch / coverage: `GET :8893/health` + `data/daemon_slayer/current.txt`; DS feature + cc_conditional wave history lives in `agents/daemon_slayer/CHANGELOG.md` + `docs/DAEMON_SLAYER.md` (was inlined here as a ~20KB table cell - removed, not lost).
- TFT: Set 17 patch 17.3

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Keep the `vision_token` + `mcp_token` resolvers SEPARATE** - never let one resolver fall back to the other's file.
