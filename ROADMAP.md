# Amberstone - Roadmap

## WHERE WORK LIVES - this file is THE tracker

**One tracker rule (operator-directed 2026-07-26):** open work lives HERE. If another doc
claims to be a "source of truth" or "single source of truth" for open work and is not in the
table below, it is a SUBPLAN - scope it to its program or archive it. Do not start a rival list.

| doc | holds | may I put open work in it? |
|---|---|---|
| `ROADMAP.md` (this) | ALL open work, NOW / NEXT / LATER, stable `RM-NN` ids | **YES - this is the one tracker** |
| `BACKLOG.md` | aspirational tier, overflow only (ROADMAP is CI-budgeted < 80KB) | only if it does not fit here |
| `docs/LIVE_GAME_GATED_SYNC.md` | rows that need the operator IN a live game | only live-gated rows |
| `docs/DS_SWEEP_TRACKER.md` | **authoritative `RM-NN` id registry** - take ids from here, never from ROADMAP prose | no - ids only |
| `docs/LEDGER.md` | append-only per-item completions | no - history |
| `docs/ROADMAP_HISTORY.md` | relocated shipped/closed narrative | no - history |
| `WAKEUP_NOTES.md` | session continuity, auto-pruned to 3 | no - per-session |
| `docs/specs/**`, `docs/_archive/2026-07-28-research-consolidation/**` | per-item spec-first output, one per item | no - they are INDEXED by a ROADMAP row, never a rival plan |

Program subplans (scoped, NOT rival trackers): `docs/ORCHESTRATION_PLAN.md` (loop director,
machine-read each cycle), `docs/RC2_PLAN.md` (RC 2.0 program only),
`docs/OVERLAY_BUILD_MASTER_PLAN.md` (overlay build spec).

---

_NOW / NEXT / LATER buckets with stable `RM-NN` item ids (assigned 2026-07-17, mdclean C2; ids never renumber - new items take the next free id). Shipped/closed entries relocate verbatim to `docs/ROADMAP_HISTORY.md` (append-only); per-item ledger in `docs/LEDGER.md`; aspirational work in `BACKLOG.md`; live-gated drains in `docs/LIVE_GAME_GATED_SYNC.md`._

> **Size-budget relocation passes (2026-07-18, 2026-07-25, 2026-07-26, 2026-07-29, 2026-07-30, 2026-08-30).** Shipped/closed narrative and BLOCKED rows' measurement evidence move VERBATIM to the dated blocks in `docs/ROADMAP_HISTORY.md`; purely live-gated rows reroute to `docs/LIVE_GAME_GATED_SYNC.md`. Each surviving row keeps its verdict, its named prerequisite or residual, and a pointer. **No RM id has ever been deleted and no open work dropped** - every id below is still reachable, as a bullet or as a routed row in the tables. Per-pass rationale + per-bullet evidence: the same dated blocks.

> **Decommissioned 2026-06-24 (operator-directed, ADR-012):** the RC<->Peer cross-Claude bridge + the lessons cross-project sync subsystem were fully removed (daemons + scheduled tasks + ~67 modules/routes/tests + docs + memory). No cross-Claude messaging remains; Peer is now only the separate private-project machine. Do NOT re-pitch a bridge or lessons-sync.

> **Removed 2026-07-09 (operator-directed):** the `budget_saver` lean / 8B-local-model fallback (the `ops/budget_saver/` dir + the `RC-BudgetSaverProxy` / `RC-BudgetSaverWatchdog` scheduled tasks + the local LiteLLM/Ollama proxy + the `CLAUDE.md.full` swap-backup + the BUDGET_SAVER and LLM-vault specs) was fully removed. It produced low-quality autonomous work that got redone properly by the full model. Do NOT re-pitch a budget-saver / lean-profile / 8B-local fallback. See `docs/LEDGER.md` + memory `project_budget_saver_removal`.

---

## NOW - active lanes

> **NEXT-5 TRIAGE (2026-08-02) relocated VERBATIM to `docs/ROADMAP_HISTORY.md`** - it was a point-in-time read of which rows were headless-actionable, and three of the rows it triaged have since closed. Its two durable conclusions survive as the rows themselves: RM-122 is operator-present ONLY, and RM-118's seam debt plus the RM-35..RM-48 DS sweep GAPs are the genuinely headless-actionable pair, each its own Tier-2 session.

- **RM-203 OPEN (filed 2026-08-14) - the DS coverage denominator `547/547` is STALE in two docs and NO denominator reproduces it; the fix belongs in a DS BATCH, not a general sync.** Body in `BACKLOG.md` under "Daemon Slayer scorer calibration", NOT here - this file was at ~91 pct of its byte budget when the row was filed, so it took the overflow tier under this file's own rule. One-line summary: `docs/DAEMON_SLAYER.md:10` and `docs/DS_COMPLETENESS_GAP.md:32,33,261` both assert `547/547`, but re-measured at 16.15.1 the three candidate denominators are **ITEM_EFFECTS 548, DDragon `gold.purchasable` 544, DDragon total 706** - it is two stale numbers colliding, not one drifted count. **Do NOT "fix" it by flipping 547 to 703/705**: the `docs/history_notes.md:16625` fence forbidding that is about catalog-vs-purchasable and does NOT bless 547 against patch drift.

- **RM-208 OPEN (filed 2026-08-15; LANE 6, Tier-1)** - DS doc route-list guard is regex-blind to the 2 `/v2/*` routes of 34 and checks one direction only. Body in `BACKLOG.md`.
- **RM-209 OPEN (filed 2026-08-15; LANE 4, Tier-1)** - 7 CSS tokens referenced by 8 panel stylesheets are defined nowhere, so those panels never theme. Body in `BACKLOG.md`.
- **RM-210 OPEN (filed 2026-08-15; LANE 7, Tier-0)** - `docs/ARCHITECTURE.md:287` calls the DS banner drift-guarded for counts no guard pins; its test count is stale by 27. Body in `BACKLOG.md`.
- **RM-211 CLOSED + RM-286 / RM-287 OPEN (2026-08-30, lane 8 cycle 30; RM-286 + RM-287 LANE 8, Tier-1)** - the audit of `tft/tft_coach_engine.py`, the LIVE TFT coach. **Closed RM-211** (`_write_status` wrote the polled artifact non-atomically while its PBE twin had carried the atomic fix since 2026-04-27) and found a sharper defect above it: the module's own system prompt asks the model for `God pick:` every round while `FIELD_MAP` knew only `carousel`, so the Set 17 headline advice was **concatenated into the `items` string** and `god_pick` - the key `core/coaching_payload.py:168`, `coaches/tft_coach.py:147` and `tests/snapshot_panels/fixtures/tft.json` all read - was never written. Eight further defects fixed in-slice (a provably DEAD empty-fields guard, an inert `self._timeout` leaving the Anthropic call untimed inside a held lock, a silent no-API-key path, a fail-OPEN spend gate, unvalidated config, and a str/bool HP disagreement between two paths reading one file). 21 tests, 20/20 mutations killed. **New rows:** (286) `tft/tft_pbe_engine.py` carries the inert-timeout and unvalidated-config halves of the same audit - its `_write_status` and its `god` field spelling are CORRECT and fenced, do not touch them; (287) `coaches/_base_coach.py:88` `safe_write`, the shared writer for EVERY coach, uses `write_text`, so every coaching artifact on disk carries CRLF - measured 11 pairs in the live `data/tft_coaching_data.json`. Bodies in `BACKLOG.md`. **Next free id = RM-288.**
- **RM-212 OPEN (filed 2026-08-15; LANE 7, Tier-1)** - `rc-shell/package.json:9` names its 16 test files by hand and nothing guards the list against the directory. Body in `BACKLOG.md`.
- **RM-214 OPEN (filed 2026-08-15; LANE 8, Tier-1)** - the vision-server `/monitor` path-disclosure guard skips itself once the file it probes for exists in CWD. Body in `BACKLOG.md`.
- **RM-291 OPEN (filed 2026-08-31; LANE 8, Tier-1)** - two sibling sweeps off the `core/augment_external_source.py` audit (LEDGER 1292), filed as CANDIDATE POPULATIONS, not defect counts: `UnicodeDecodeError`-escaping `read_text` handlers, and bare numeric coercion of payload fields. Body in `BACKLOG.md`. **Next free id = RM-292.**
- **RM-221 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - `core/riot_api.py` cache keys `match:v5:<id>` and `match:v5:timeline:<id>` are prefix-ambiguous into a never-expiring table; NOT reachable today, filed because the fix is a migration of 18575 rows. Body in `BACKLOG.md`.
- **RM-222 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - the six `core/decision_detector.py` detectors are not shape-hardened against the Live Client envelope; cycle 12 made a retyped inner field VISIBLE (heartbeat `detector_errors`) but not prevented. Measured 5/6, 2/6, 2/6 and 1/6 raising across four malformed shapes. Body in `BACKLOG.md`.
- **RM-255 / RM-256 OPEN (filed 2026-08-30, lane 8 cycle 22; RM-255 LANE 8, RM-256 LANE 7-or-8, both Tier-1)** - the still-open half of the cycle-22 batch: **(255)** `lib/http/client.py`'s blocklist FAILS OPEN on both failure paths and is re-evaluated per request, on the chokepoint for every third-party fetch in the tree, with zero tests - plus `certifi` absent from both requirements files, making its `_SSL_CONTEXT` inert; **(256)** `agents/agent2_backend/ws_server.py:46` binds `0.0.0.0` with no auth on `/ingest` or `/push`, zero tests - PROBE REACHABILITY BEFORE FIXING. Bodies in `BACKLOG.md`. RM-253 (cycle 25) and RM-254 (cycle 24) are CLOSED; the original four-id row is archived verbatim in `docs/ROADMAP_HISTORY.md`.
- **RM-258 / RM-259 / RM-260 OPEN (filed 2026-08-30, lane 8 cycle 23; RM-258/259 LANE 8, RM-260 LANE 7; all Tier-1 except RM-260 Tier-0/1)** - three findings from the cycle-23 audit of `performance_tracker.py`, none of them in the audited file: **(258)** the bare-`os.replace` exposure is a repo-wide class of ~45 runtime cross-process writers, not the three RM-250 names - including `coach_integration/_coach.py:120`/`:715` (the coaching-data files every overlay polls) and `ops/rc_supervisor.py:1605`, which clears `restart_trigger.txt` bare while defining a retry helper at `:77`; **a re-count must search BOTH `os.replace` and `Path.replace`**, since the latter spelling hides ~100 further sites and is the one CLAUDE.md prescribes. Land RM-254 first so 45 callers are not converted onto a helper with a known tmp-collision bug; **(259)** a failed rating write logs `"Performance rating saved"` anyway (`app/_game_lifecycle.py:236`, FROZEN - needs an adjudicator) and leaves a stale-mtime file that `agents/supervisor.py:799-805` then copies into the next post-game summary, possibly from a different mode; **(260)** the experimental-builder hook that reads the API key is DORMANT (`mark_active()` has zero callers), so `performance_tracker.py:406` is an unreachable dead read - and the only thing keeping the key out of `logs/` on the reachable-in-principle path is `%s` formatting at `experimental_builder.py:237`, since `repr()` of the `UnicodeEncodeError` from a non-ASCII key file embeds the key. Bodies in `BACKLOG.md`. **RM-258 is the highest-leverage of the three.**
- **RM-254 CLOSED + RM-261 / RM-262 / RM-263 / RM-264 OPEN (2026-08-30, lane 8 cycle 24; RM-261 LANE 7 sweep with a LANE 8 runtime subset, RM-262 LANE 4/7, RM-263 + RM-264 LANE 8)** - the cycle-24 audit of `core/polled_json.py`, the repo's atomic-write CONTRACT, **closed RM-254**: the scratch filename came from the destination alone, so concurrent writers to one file opened the SAME scratch file and the destination was measurably TORN - from the module whose docstring promises "never a partial write". `data/force_scan.json` has FOUR writers, one in its own process. Fixed with a per-writer `<name>.<pid>.<token>.tmp` plus cleanup on every failure path. Four more defects fixed with it: `read_json_dict` raised `UnicodeDecodeError` despite promising the default (three consumers had already widened their handlers rather than fix the resolver), it and `PolledJsonFile` both shallow-copied the caller's `default`, and an exhausted retry orphaned the scratch file. **New rows:** (261) 96 modules still hand-roll the same destination-derived scratch name - same writer set as RM-258, convert once and satisfy both; (262) four U+2192 arrows in a live Haiku prompt string at `coaches/arena_coach.py:159-160`, pre-existing; (263) no `fsync` anywhere, so atomic against a reader but not against `taskkill /F`; (264) `PolledJsonFile` has ZERO production instantiations - adopt or remove. Bodies in `BACKLOG.md`; detail in `docs/LEDGER.md` 1283. **Next free id = RM-265.** **ADDENDUM 2026-08-30 (lane 8 cycle 30):** the highest-traffic member of the RM-261 scratch-name class is `coaches/_base_coach.py:85` `safe_write` - the shared writer every coach calls - which derives its scratch from the destination and is serialized only by an IN-PROCESS lock that cannot reach the other processes writing these files. Eleven further destination-derived sites enumerated this run: `core/coaching_timestamps.py:65`, `core/feature_policy.py:440`, `core/hot_reload.py:127` + `:202`, `core/safe_coach_output.py:54`, `coaches/experimental_builder.py:121` + `:349`, `tft/placement_aggregator.py:113`, `tft/tft_live_analysis.py:452`, `tft/tft_pbe_engine.py:495`.
- **RM-277 / RM-278 / RM-279 / RM-280 OPEN (filed 2026-08-30, lane 8 cycle 28; RM-277 LANE 8 FROZEN-ADJUDICATED, RM-278 + RM-280 LANE 8, RM-279 LANE 7; all Tier-1 except RM-279 Tier-0/1)** - the residual findings from the `core/coaching_data_lock.py` audit. **RM-199 is CLOSED** (LEDGER 1287). Bodies in `BACKLOG.md`; full row body relocated 2026-08-30 to `docs/ROADMAP_HISTORY.md` for the ROADMAP size budget.
- **RM-281 HALF-CLOSED + RM-283 / RM-284 OPEN (2026-08-30, lane 8 cycle 29; RM-283 LANE 4-or-8, RM-284 LANE 7)** - audit of `dashboard/_idempotency.py`, the replay table gating every side-effecting operator intent on the Mission Control plane. **Closed RM-281's loop-control half and REFUTED its diagnosis:** that row called it an OS divergence; it is not. The table is a process-global `OrderedDict` (900 s TTL) with no suite-wide isolation, and `test_session_intents.py:43` shares a key with `test_loop_control_contention.py:125`, so when xdist puts both on one worker the second REPLAYS the first's answer and never acts - hence `0 matching attempts`. Reproduces on WINDOWS in 1.8 s. Fixed in `tests/conftest.py`, not by `skipif`; the right per-file fixture had existed at `test_loop_control_idempotency.py:56` since S2, which is why the class stayed open (3 keys, 4 files). **The audit finding is bigger:** `_apply_idempotent` was a check-then-act on a `ThreadingHTTPServer` (`dashboard/server.py:54`), so two same-key requests both acted - the one shape the module exists to stop. Replaced with an atomic `claim` / `settle` / `abandon`; a waiter blocks on the owner's reservation (2 s) and inherits its answer, a non-200 abandons - wire contract unchanged. **New rows:** (283) `web/mc/mc.js:51` maps EVERY 503 to "auth not configured", so a lane-launch failure reads as an auth fault; (284) this file is at its CI budget and CRLF on checkout blows it - relocate shipped rows. Bodies in `BACKLOG.md`; detail in LEDGER 1288. **Next id RM-285.**

- **RM-253 CLOSED + RM-266 / RM-267 / RM-268 / RM-269 OPEN (2026-08-30, lane 8 cycle 25; RM-266 / RM-268 / RM-269 LANE 8, RM-267 LANE 7-or-8, all Tier-1)** - the audit of `vision_server/_relay.py`, the untrusted-input seam feeding the coaching pipeline. **Closed RM-253**; six more defects fixed in-slice, the sharpest being that a truthy non-object body was cached with a FRESH `ts`, SUPPRESSING the 1-PC :2999 self-heal that exists for when the relay agent dies. **New rows:** (266) `_http.py` hand-rolls the lookup `lcu_get_result` exists for, and that function has zero callers; (267) `/stats` is the only un-gated GET on :8889 - probe the status page before gating; (268) `/lcu-cmd` queues an unvalidated body, the one shape hole unreachable from inside `_relay.py`; (269) a backward wall-clock step makes a stale snapshot read as FRESH at three sites across two modules. Bodies in `BACKLOG.md`; detail in `docs/LEDGER.md` 1284. **RM-270 CLOSED c26; RM-272 filed. c27 CLOSED RM-255** - `lib/http/client.py` FAILED OPEN when its blocklist went missing, and a trailing dot, `file://` or a redirect each walked past it. Filed RM-273/274/275/276. **Next id RM-277.**
- **RM-233 OPEN (filed 2026-08-30; LANE 8, Tier-1)** - three residual `core/match_db.py` weaknesses left unfixed by lane 8 cycle 16, each verified live: **(a)** the `PRAGMA journal_mode = WAL` result is never read, so a silent fallback to `delete` journalling makes the module's own concurrency claim false with no signal; **(b)** `save_match` returns `None` on BOTH success and failure, so a lost match is indistinguishable from a saved one; **(c)** a negative `limit` is UNLIMITED in SQLite - LOW, the one production caller already clamps. Body in `BACKLOG.md`. **Next free id = RM-234.**
- **RM-234 / RM-235 / RM-236 / RM-237 OPEN (filed 2026-08-30, lane 8 cycle 17; LANE 8, Tier-1)** - what cycle 17 did NOT fix in `game_reader/snapshot_normalizer.py`: two per-tick `/activeplayerrunes` GETs for fields no production code reads (234); process-global subresource counters with no production reader or reset (235); six further defects, FOUR wrong on real Riot data every game, headed by `quest_boots_owned` being wrong in both directions on 16.15.1 while feeding a build instruction into the live SR prompt (236); and a ~17s worst-case poll tick that trips the 12s liveness threshold, whose auto-remediation then hands a second thread the SAME `GameReader` (237). Bodies in `BACKLOG.md`.
- **RM-250 / RM-251 OPEN (filed 2026-08-30, lane 8 cycle 21; RM-250 LANE 8, RM-251 LANE 7, Tier-1)** - what cycle 21 did NOT fix after hardening `dashboard/routes_loop_control.py`: **(250)** three SIBLING hand-rolled non-retrying atomic writers on the SAME polled control files - `ops/loop/loop_controller.py:126-129`, `ops/loop/intents.py:99-113`, `ops/loop/steer.py:96-101` - each carrying the WinError 5 exposure the route just closed, plus `core/polled_json.atomic_write_text`'s CRLF exposure for its 6 callers; **(251)** the stale `:8888` residues left by the S10 Task 9 de-registration - `dashboard/_handler.py:532`'s dead `/api/loop-control` literal in the D9 auth tuple and `dashboard/routes_auto_accept.py:18`'s trust-model prose citing it as a dashboard POST - plus three private `_atomic_write_bytes` re-rolls (`core/rofl_archive.py:815`, `lib/icons/downloader.py:29`, `tools/ddragon_mirror_refresh.py:145`) now that a public one exists. Bodies in `BACKLOG.md`. **Next free id = RM-252.**
- **RM-247 / RM-248 OPEN (filed 2026-08-30, lane 8 cycle 20; LANE 8, Tier-1)** - what cycle 20 did NOT fix after hardening `vision_server/_inference.py`: the `/vision` frame path still has no size or decompression-bomb cap and its `except Exception` FORWARDS the oversized frame instead of refusing it (247); 22 residual `except Exception` sites across `vision_server/` plus `handle_ocr` reporting `ok=True` on an empty result (248). Bodies in `BACKLOG.md`.
- **RM-242 / RM-243 / RM-244 / RM-245 OPEN (filed 2026-08-30, lane 8 cycle 19; LANE 8, Tier-1 except RM-243 Tier-2)** - what cycle 19 did NOT fix after hardening `dashboard/routes_state.py`: the systemic raw-exception-into-the-body class, 22 sites / 19 modules (242); the POST validator that logs its verdict then dispatches the bad body anyway (243); seven residual correctness defects (244); four degraded-mode paths that report success while failing (245). Bodies in `BACKLOG.md`. **Next free id = RM-249.**
- **RM-238 / RM-239 OPEN (filed 2026-08-30, lane 8 cycle 18; LANE 8, Tier-1)** - what cycle 18 did NOT fix after hardening `dashboard/routes_diag.py`: `data/decisions_log.jsonl` grows without bound and is read whole on every `/api/decisions/log` call, graded LOW on a measured 3775-byte live file (238); and the RM-134 scrubbed error envelope was built but never adopted by its callers - **25 sites across 19 route modules** still put exception text into an HTTP response body, headed by `routes_loop_control.py`, whose 6 sites reach Mission Control `:8895` only (tailnet `100.70.22.55` + loopback) - **the `:8888` half of this claim was CORRECTED 2026-08-30 by lane 8 cycle 21**: S10 Task 9 de-registered the route, `mc/routes.py:16` is its ONLY production importer, and `tests/test_mission_control_server.py:468-476` asserts `:8888` does not match `/api/loop-control` (239). Also RM-240 (both decision-resolving POSTs are unauthenticated state mutations on the `::` listener, though the gate they are missing from is itself inert until `RC_DASH_TOKEN` is set) and RM-241 (a newline-bearing `RC_VISION_TOKEN` reaches `logs/` through a urllib exception message; fix belongs in the resolver, not the callers). Bodies in `BACKLOG.md`. **Read the RM-239 acceptance before touching it** - the existing RM-134 guard is vacuous by construction and must be REPLACED, not extended.
- **RM-228 PARTIAL - 1 of 5 closed, 4 REMAIN OPEN (filed 2026-08-30; LANE 8, Tier-1)** - overriding `log_message` silently drops the stdlib's control-character escaping, and all five remaining overrides in the tree did. **`agents/_supervisor_http.py` was CLOSED 2026-08-30 by lane 8 cycle 15** (LEDGER 1274, commit `f736e14d`): the injection was reproduced against the real handler over a real socket (raw `0x1b`/`0x08`/`0x7f` in the record, at INFO, on the 0.0.0.0 bind), closed by reusing `dashboard._handler._scrub_log`, and pinned by a mutation-tested regression test. Its peer half needed no fix - that override already called `address_string()`, so the original census was right and only the scrub was missing. **STILL OPEN, and one of them is the other remotely-reachable INFO logger:** `mc/handler.py` (`:8895` on the tailnet address - do NOT make `mc/` import from `dashboard/`; lift the helper instead), plus the three loopback-or-debug-only ones, `agents/daemon_slayer/server.py` (also owes a `Share/` mirror resync, so it lands Tier-2), `tools/ds_matchdb_mcp_server.py` and `vision_server/_http.py`. Log-integrity severity, backfill measured empty. `dashboard/_handler.py` was fixed in cycle 13 (LEDGER 1272). Body in `BACKLOG.md`. Next free id = RM-232.
- **RM-192..RM-202 FILED 2026-08-14 (lane-research refill, LEDGER 1245) - 11 open rows, all bodies in `BACKLOG.md`, NOT here.** This file was at 89.8 pct of its byte budget when they were filed, so they went to the overflow tier under its own rule, beside their siblings RM-135 / RM-160 / RM-161. Split: **RM-200 / RM-201 / RM-202** under "Daemon Slayer scorer calibration" (lane 6; RM-200 + RM-201 are Tier-2 and bump `ENGINE_VERSION` + the Share mirror), **RM-192..RM-199** under "Reliability / hardening" (lanes 4 / 7 / 8, all Tier-0/1). Headlines: **RM-202** the stranded-seam guard is green while BLIND to two live seams - **CLOSED 2026-08-15, LEDGER 1261**; a depth-1 tier with its own `STRANDED_DEPTH1` ledger now sees both, and closing it needed two blind spots the row did not name (function-local imports, `**splat` forwarding); **RM-192** the `docs/DS_SWEEP_TRACKER.md` id pointer was 57 ids stale and is corrected to RM-203, guard still owed; **RM-197** `core/prompt_sanitize.py` has three EXECUTED bypasses - **CLOSED 2026-08-15, LEDGER 1253**. **The whole sanitizer arc is closed: RM-197 + RM-205 + RM-206 + RM-202 + RM-207, LEDGER 1253-1262.** **UPDATE 2026-08-15: RM-200 / RM-201 / RM-203 ALSO CLOSED (LEDGER 1263-1265, ENGINE 1.278.0); `STRANDED_DEPTH1` is now EMPTY, by wiring. RM-208..RM-214 filed, RM-215 filed then REFUTED by its author. Bodies in `BACKLOG.md`. Next free id = RM-216.**
- **RM-159 CLOSED-BY-MEASUREMENT 2026-08-04 - DO NOT make `tools/stop_claim_gate.py` negation-aware.** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-158 DATA HALF CLOSED 2026-08-06 (`a712d1f5`, LEDGER 1211); only the TABLE REGEN remains, deliberately deferred.** Corpus half DONE, writer GATED. **Three fences kept here because each cost a wrong first attempt:** the row's two hashes are correctly VALUED and wrongly LABELLED (they run from the `"scenarios":` offset, NOT whole-file); **do NOT purge the arena rows** - the first attempt did and it was wrong; and the regen is deferred on purpose, not forgotten. Full measurements in LEDGER 1211. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-15).
- **RM-155 RETIRED 2026-08-06 via ADR-013 (`a45dcb9e`, LEDGER 1219)** - the laning verdict is BELOW THE NOISE FLOOR (bias-corrected MI is NEGATIVE), and the decider was the learnability control, not the bad score: a per-(role, champion) prior beats a 200 MB corpus. The native-corpus path is DEFINITIONALLY closed - Riot emits no `team_position` for ARAM or Arena. **NOT deleted: the `laning_scenarios` tables, the shadow logging, `laning_trigger` (live), `RC_LANING_CV_SERVED`.** Full body relocated to `docs/ROADMAP_HISTORY.md` (2026-08-30). **Read `docs/adr/ADR-013-laning-verdict-flip-retired.md` before re-opening.**
- **RM-188 REFUTED 2026-08-08 THE SAME DAY IT WAS FILED - `pytest tests` does NOT restart the live RC, and neither does a DS bounce. DO NOT RE-FILE THIS.** Body + the original filing relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-14 size-budget pass) - READ THEM before re-opening this id. Three controlled arms all came back clean; the real restart path is `restart_trigger.txt` (`ops/rc_supervisor.py:1595-1607`), NOT the suite. The one unexplained residual is a low-frequency restart around repo-mutating operations - watch `status.json.last_restart_reason` across a MERGE, do not re-run the suite.
- **RM-174 CLOSED 2026-08-07 (`79a6a574`, LEDGER 1223) - the overlay flake is LOAD-DEPENDENT and the FILED MECHANISM WAS REFUTED (not a read-before-paint race).** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 10) - READ IT before re-opening. Two fences that must not be lost: **do NOT 'fix' this class by polling** (once the class is stripped the pane never hides, so a wait burns its full timeout and fails SLOWER, 5.01 s confirmed) - hide through the store-backed seam instead; and the **RESIDUAL CLASS is OPEN and UNGUARDED - any test that mutates a layout-owned class or style out-of-band inherits the same 91-107 ms debounce fuse.**
- **RM-175 DECIDED 2026-08-07 via ADR-014 (`8b41a25b`, LEDGER 1224) - the ARAM tables are known-wrong and deliberately NOT regenerated.** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 10). **Read `docs/adr/ADR-014-aram-laning-table-regen-declined.md` before re-opening** - the equal-base premise behind the constant error is load-bearing (ARENA's base is 3.0, so its ratios are NOT constant), the filing was TRANSPOSED on which bands move, and if a regen ever happens the coherent unit is ALL THREE MODES AT ONE PATCH.
- **RM-170 CLOSED 2026-08-06 (`c2f2b6a7`, LEDGER 1215) - the three unguarded trees are green, and BOTH of this row's filed claims were WRONG.** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 11) - READ THEM before re-opening this id; they carry the suite-scope rule and the do-NOT-revert on the cdragon AST import guards.
- **RM-171 SHIPPED 2026-08-06 (`274bdb27`, LEDGER 1214) - `tools/citation_audit.py` is the ONLY citation checker in the repo; extend it, never write a second.** Body + all three probe traps + the RESOLVES-IS-NOT-CORRECT finding + the open `director_prompt.md:65` action relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-14 size-budget pass) - READ THEM before re-running a citation audit; a naive re-run reports garbage.
- **RM-164 FULLY CLOSED 2026-08-08 - BOTH halves shipped; the consumer wiring landed at merge `9752cdfc` (LEDGER 1233) and the "operator-gated" framing is HISTORY, not a live gate.** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 11) - READ THEM before re-opening this id. Provenance half (below) is unchanged.
- **RM-164 PROVENANCE HALF SHIPPED 2026-08-06 (`b44a48e5`, LEDGER 1209) - CLOSED; its consumer-wiring gate was lifted and shipped 2026-08-08 (row above).** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 10) - READ IT before extending the provenance stamp; it carries the three traps (`KNOWN_ARCHETYPES` must include `onhit`; validation is deliberately ASYMMETRIC; `plan_build_order` is an injectable seam whose stand-ins may lack the attribute), the deliberate non-stamping of patch dirs 16.11.1-16.14.1, and the **PRE-EXISTING `tools/ds_feed_index.py --check` drift on the three 16.15.1 FLAT build_orders files - do NOT misattribute it.**
- **RM-150 CLOSED 2026-08-04 (LEDGER 1190) - `:8889` binds loopback and its 5xx bodies say `internal error`.** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-144 SHIPPED 2026-08-02 (LEDGER 1165) - vision was NEVER dead.** Full row relocated VERBATIM to `docs/ROADMAP_HISTORY.md`. Do NOT re-probe `:8889` over `https://` (it is plain HTTP, token-gated on `X-RC-Token`) and do NOT re-file a stale `screen_read` as a vision outage - it is an operator-click-only dwell field. Memories `reference_vision_token_canonical` + `reference_screen_read_sticky_bydesign`.
- **RM-142 SHIPPED + CLOSED 2026-08-02 (`3214d8f5`, ENGINE 1.268.0 -> 1.269.0)** - `ops/audit/P6_LOLMATH_PARITY.md` is DRAINED; read its DRAIN STATUS table before touching any slice. Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-125 OPEN (COMMENT HALF SWEPT 2026-07-28, R222) - only the rendered residue remains, and it is DEFERRED to RM-122.** The comment half is done and machine-guarded (`tools/web_ascii_sweep.py` is the non-`ast` comment tokeniser for JS / CSS / HTML; `tests/test_web_comment_lines_ascii.py` is the whole-tree guard; `tests/test_web_ascii_sweep.py` carries the false-positive pins - a glyph inside a JS string / template / regex, a CSS `content:` value and an HTML text node must classify LIVE and never be swept). Residue **3153 -> 261 tool-reported**, of which only **235 actually RENDER** (the other 26 sit in `web/legacy_index.html` inline blocks that `_scan_html` leaves alone by design). **The remaining 235 are NOT a headless job:** `docs/RM125_web_live_glyph_adjudication.md` adjudicates every one (STRIP-candidate-deferred 152, KEEP-deliberate 52, **LOAD-BEARING 31**) and routes the set to RM-122 for operator-present rendered-pixel judgement. **Read that doc before touching one** - 31 are load-bearing, meaning a strip breaks BEHAVIOR: `web/js/lib/items_index.js:62` holds `U+2192` as an alternation branch in the build-string split regex, and `web/js/panels/right_now.js:563-566` is a producer/matcher coupling where the emitted glyphs ARE the char class the next line's regex tests. Note `web/legacy_index.html` is NOT dead - `dashboard/routes_static.py:23-41` serves it at `?ui=legacy` and as the automatic fallback when `index.html` throws.
- **RM-121 CLOSED 2026-07-28 (R217, LEDGER 1094)** - the five-file operator desktop queue is DRAINED, there is no file 5, do NOT re-pick it. It read "two files left" for three days after LEDGER 1094 closed it (the stale-ROADMAP class LEDGER 1093 named); caught 2026-08-01. Narrative: `docs/ROADMAP_HISTORY.md`. The `random.txt` part-2 UI/UX block is NOT this row and lives on as **RM-122**.

- **RM-127 CLOSED 2026-08-01 - CCR LINK-INGEST, PHASES 1-7 ALL SHIPPED, ZERO RESIDUE** (LEDGER 1108 / 1145 / 1147 / 1149 / 1150 / 1151 / 1152 / 1155; full narrative relocated VERBATIM to `docs/ROADMAP_HISTORY.md`, do not re-derive it here). 146 links triaged to 4 ADOPT / 1 HOLD / 1 DROP; Phase 5 collapsed the four adoptions into ONE build; both remaining operator calls are MADE. **Do NOT re-run any pass or re-score the roster.** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block); fences live there.

- **RM-129 CLOSED 2026-08-01 - DS PORT-BLOCK MIGRATION SHIPPED** (`2872c1a7`). DS binds **8860** (engine) + **8861** (match-DB MCP) in its own 8860-8879 block; 8893/8894 retired. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-03) - read it before touching a port. Fences that must not be lost: **DO NOT renumber 8895** (Mission Control); a residual 8893 in a provenance or append-only history file is CORRECT, not rot; and never size a port move with a bare `grep -c` (the filed "~186 places" was substring noise - LEDGER 1146 + `core/ports.py`).

- **RM-122 OPERATOR-PRESENT UI/UX LANE - THE HEADLESS LOOP MUST NOT PICK THIS UP.** Filed 2026-07-28 (R217) out of the `random.txt` part-2 block, which is headed verbatim "session 2 (operator-present; continues 2026-07-22b). Do NOT run headless." Every item terminates in a rendered-pixel judgement, the one class an agent cannot self-adjudicate under the CLAUDE.md R3 text-first rules, so **a director scheduling this row to a headless agent violates the note's own gate.** Six-item queue in the operator's stated priority (competitor capture library -> landscape auto-fit hub -> per-page layout alternatives -> champ_select Tailwind-to-theme -> logged one-liner defects -> theme polish), each with its fences, relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06). **READ FIRST, do not re-derive:** that block, plus `WAKEUP_NOTES.md` section 2026-07-22b, `docs/qa/UI_UX_PROGRAM_QA_2026-07-22.md`, `docs/qa/SPATIAL_BRAND_SPEC_2026-07-22.md` section 5, `docs/qa/THEME_MULTIHUE_METHOD_2026-07-22.md`, `docs/qa/DESIGN_SYSTEM_MAP_2026-07-22.md`, memory `feedback_operator_ui_qa_method`. **Standing traps:** competitor shots stay LOCAL and are never committed; the champ_select shell tags stay Tailwind until DS4 lands, so a partial conversion is a defect; the `.rofl` 4-match ingest gap is RM-117, NOT a UI job; several items are UNKNOWN rather than cleared; and every page touched owes the 5-phase UI-audit ritual BEFORE its commit.

- **RM-01** [OK] **HAIKU-TO-ZERO Lane E CV substrate ADVANCING (2026-07-14, gemini-loop cycles 19+24; R121/R126).** The client-side CV vision tier + confidence-weighted Live-Client/CV fusion (docs/NO_LLM_PRECOMPUTE_PLAN.md, the "bigger SECOND program") now has its persistence + fusion substrate BUILT, all dormant: R121 the icon dhash atlas (core/vision_atlas_precompute.py -> data/daemon_slayer/vision_atlas_manifest.json, 227 icons); R126 the OCR region-map atlas (core/vision_region_atlas.py + data/daemon_slayer/vision_region_atlas.json, 25 regions = 21 calibrated HUD rects + the augment/fog API-gap slots) AND the LiveClient/CV partial-read fusion (core/vision_fusion.py - LC-exact-1.0 vs CV-heuristic-0.7, api_gap CV-authoritative, 0.6 stale-override, never-raises). NEXT (live-gated): calibrate the augment_card_*/minimap_fog rects against a real Arena/ARAM frame, then a validated OCR-only flip. fuse_reads is now WIRED shadow-first (2026-07-17, LEDGER 914 S6, `1e0ecfa6`): the ARAM/Arena vision tick appends a per-tick fusion record to data/fusion_shadow.jsonl at the GameVisionReader.read_tiered chokepoint, served coach output byte-identical - accrue real-game fusion_shadow before the flip. **Do NOT flip blind** - a wrong precompute is worse than a Haiku call. **COMPETITOR-LIFT ROTATION RETIRED:** the competitor-lift/overlay/companion/stat-site research category is DRAINED 4x (R100 Overlay App F / R112 aggregator D+aggregator C / R120 Overlay App F+Overlay App E / R125 Aggregator C Desktop) - the director should NOT re-pick it; rotate DS-sweep, Haiku-to-ZERO (Lane A/E), or a genuinely un-torn-down category instead.
  - **2026-08-02 (LEDGER 1173, `bed019b0`) - the A/B choices half of this lane was BLOCKED BY A PARSER BUG, not by missing coaching.** `coaches/_base_coach.parse_fields` clipped every value to 220 chars and a 2-entry `choices` array is ~315, so the LIVE side had emitted `choices` **zero** times across the entire ARAM shadow log AND the entire Arena log - `both = 0`, `label agree 0/0`, i.e. the flip was never validatable on its primary surface. SR alone was clean (own uncapped parser). Fixed by bounding per CONTENT CLASS (prose 220 / structured 2000, both still enforced). **Do NOT read memory `reference_coach_choices_native_emit` as evidence the values arrive** - it was corrected in place; the emit was WIRED but INERT for 3 of 4 coaches from the day it shipped. **OWED and unconfirmable headless: `both` can only leave 0 after one live ARAM on a restarted process.** Action agreement is a separate matter and is NOT noise - 98.6 pct of mismatches are exactly one ladder tier apart and the rule's only one-tier operator (`wave_pct`) is hardcoded off at `dashboard/_deterministic_coaching.py:1336` while Haiku IS given it; the noise + timing-skew hypotheses are REFUTED by measurement (`docs/ARAM_ACTION_MISMATCH_2026-08-02.md`). Step 0 instrumentation shipped; Step 1 changes served coaching, needs a live game, and would add a Sonnet vision dependency to the deterministic action - an operator scope call, do NOT flip it blind._
  - _Re-probed 2026-07-25 - the 2026-07-18 "file does not exist" note is STALE. `data/fusion_shadow.jsonl` holds **230 real-game records**, so the accrual gate is MET, and triage found **41 of 41 CV overrides in the ledger's entire history were garbage** (all `gold`, all the literal value `1`). FIXED at the fusion layer with a plausibility gate. **The upstream half `core/vision_routing.py:118` is now CLOSED too (2026-07-25, ENGINE 1.243.0)** - the Sonnet-escalation merge is scoped to the requested field list behind `RC_VISION_MERGE_STRICT`, **DEFAULT-OFF and it must stay off until someone re-homes `is_augment_select`**: `modes/shared_vision.py:418` aliases it to `augment_select` AFTER the merge and it is in NO coach's `TIERED_FIELDS`, so a default-ON scope would silently regress the live-verified ARAM Mayhem augment-select path. Replay over the 232-record corpus: 104 records (44.8 pct) drop keys, all TFT-shaped. Unmeasured before any flip: the same scope shrinks `_log_fusion_shadow`'s own `cv_reads` payload. Still shadow-only; do NOT flip. Narrative: `docs/ROADMAP_HISTORY.md` (2026-07-25)._
- **RM-02 DS build-reco counter-build: the full counter-hint program is CODE-COMPLETE; the ONLY remaining work is the in-game chip eyeball.** C2 antiheal + C6 tenacity + C3 fed + the R102/R103 situational step-2 + the live `allPlayers` roster plumbing all shipped (LEDGER 908/909/914 S1+S9/916; `e9487a78`, `1e3cab6c`, `566e472b`, `318e7e3a`; new `core/build_planner/fed_threat.py`; specs under `docs/specs/2026-07-1{6,7}-ds-c*-counter-hint-design.md`). Do NOT rebuild step-2 - the 2026-07-13 refactor spec is stale on it. **OWED (do-not-flip-blind, operator-only):** eyeball the antiheal / tenacity / fed / roster chips in a REAL game - the Electron overlay is agent-blind and a wrong hint is worse than a dark chip. Tracked as a gated row (`docs/LIVE_GAME_GATED_SYNC.md`, the RM-02 citation at the DS-plan row). Full shipped narrative + the pre-C2 history: `docs/ROADMAP_HISTORY.md` (2026-07-17 + 2026-07-18 blocks).
- **RM-03 RC 2.0 program (in flight).** Living plan `docs/RC2_PLAN.md` (9 phases / 62 stages), resume via `/RC2-Continue`; design GREENLIT Hextech (`docs/design/RC2_DESIGN.html`), League borderless @ 2560x1440. OPEN: E10 ASCII+git-history rewrite (force-push, operator-gated); E12 responsiveness levers residual (= QA62 / lever L3: BUILT DARK 2026-07-20 `875aa355` - full in-process snapshot port `lcu/snapshot_shape.py` + `dashboard/_lcu_inprocess.py` wired behind DEFAULT-OFF flag `RC_LCU_INPROCESS`; QA59/L2 already shipped as `_cached_lobby_mode`. ONLY the G1-00 live confirm owed - flag ON in a live champ-select, byte-compare `lcu.champ_select`, adjudicate the `state.lcu.config`/auto-accept-pill divergence; see `docs/LIVE_GAME_GATED_SYNC.md` G1-00 CHECK 2); E2 DS 3-game live-flip (operator-played); P5.2 served CV-flip env (`RC_LANING_CV_SERVED=1`, gated on `hz_shadow_report` agreement delta); rc-shell Electron relaunch owed (live disappear-fix/pinned); spell-fix LIVE verify owed. Shipped-stage detail: `docs/RC2_PLAN.md` + `docs/ROADMAP_HISTORY.md`.
- **RM-118 STRANDED-SEAM CLASS - the WIREABLE debt is now ZERO AT BOTH TIERS (2026-08-15, LEDGER 1263/1264, ENGINE 1.278.0).** RM-200 + RM-201 were the last two, both wired onto `compute_ehp`, which PROMOTED them into the depth-0 universe, so `STRANDED_DEPTH1` is empty BY WIRING rather than by the guard going quiet. What stays OPEN is only the 5-seam DECLINED-BY-DESIGN depth-0 ledger - the target/caster-state arc, operator-CLOSED s232, `assume_lifeline_shield` among them (shield-NAMED but caster-state class). **Take the count from `agents/daemon_slayer/tests/test_stranded_hsp_seam_r197.py::STRANDED_TODAY`, never from a sentence - every count this row has carried in prose was wrong at least once.** FOUR DURABLES, all measured: route ownership comes from `inspect.signature`, never a sibling docstring (wrong twice in OPPOSITE directions in one run); check the TRANSPORT, not just the flag, since a flag-only seam is settable, guard-green and arithmetically INERT; a ledger REASON goes stale in a way the count does not; and a seam wired under a DIFFERENT NAME is invisible to a name-keyed checker (RM-207). **This row is the stranded-seam RM-118; the mana-as-damage RM-118 further down is a DIFFERENT row under the same id - do not merge or renumber them.** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06, again 2026-08-15).

- **RM-117 REPLAY ANALYSIS / COACHING CORPUS - substrate BUILT 2026-07-26 (LEDGER 1062); narrative relocated to `docs/ROADMAP_HISTORY.md` 2026-07-28.** Shipped: `core/{replay_roster,replay_analysis,replay_seek,replay_camera,event_patterns,corpus_hygiene,cohort_baseline}.py`, `tools/{replay_roster_pull,ladder_role_scout,timeline_ingest,mine_event_patterns,build_cohort_baseline,build_rank_baselines}.py`, `RC-ReplayRosterPull` hourly, 112 tests; corpus 108 accounts / 417 `.rofl` plus a timeline corpus CLOSED at 3005 matches. **The `.rofl` Layer-2 fence did NOT need opening and stays CLOSED** - positions, wave state and ward coverage all have measured non-Layer-2 routes.
  - **MEASUREMENT TRAPS - relocated VERBATIM 2026-08-06 (run-02 size-budget pass) to `docs/ROADMAP_HISTORY.md`; also in `docs/REPLAY_T2_PARSE_CRITERIA.md`. READ THEM BEFORE ANY REPLAY/CORPUS WORK** - each is a place where the obvious reading of the data is WRONG and re-deriving it costs a session. Headlines, so none is lost from this file: the v2 container has NO encryption (the fence's CHURN rationale stands, the CRYPTO one is void); Match-V5 60 s positions carry ~2000 units of error and `FAR_UNITS` / `SIGNAL_DECAY` are calibration constants, NOT facts about junglers; `assistingParticipantIds` carries ENEMY participants (12.44 pct of 25450 elite kills); `monsterSubType` is DRAGON-only, so discriminate on `monsterType`; ward map positions are the HEALTH BAR above the ward, not the ward; summoner spells are LOADOUT ONLY; no fetch-by-match-id route exists, so never plan a `.rofl` backfill; a Disabled `RC-ReplayRosterPull` with a non-empty `busy` is CORRECT; the `objective_participation` bias DEFLATES; and B13 legal is CLOSED, the "one (1) developer account" limit being about DEVELOPER accounts, not tracked players.
  - **NEVER PROMOTE:** `kill_participation` (INERT - already team-normalised), `shutdowns_given` (measures being AHEAD - winners gave MORE in every role), `deaths_per_min` and TEAM `gold_deficit_profile` (they restate the OUTCOME).
  - **Only surviving candidate: JUNGLE `plate_share`** 0.082 vs 0.045, effect 0.49 - the one row through hygiene + effect gate + holdout + a pre-treatment confound control, and **still not causal**. Effect below ~0.3 does not survive the by-match holdout, and **NO p-value is reported anywhere in this table** - the gate is effect size plus holdout, so never read a surviving row as statistically significant.
  - **STILL OPEN / OWED:** **(ii) SHIPPED 2026-08-04 (LEDGER 1195) - `core/data_retention.py`**, four classes with four different answers; `scan` / `plan` / `render_report` / `game_information_manifest` are read-only and `apply()` is INERT (zero callers, no `--apply` on the CLI). NOTHING was deleted - `data/` is byte-identical at 10.11 GB / 2106 files. **The filed premise was WRONG in both halves:** not "past 6 GB", and the mass is NOT `.rofl` (3.72 GB is two one-off `rewind_history.db` backups - a snapshot, not a growth curve). **Operator decision still pending** on tier-1 3.72 GB / tier-2 24.6 MB / tier-3 137 MB, and **do NOT delete `data/riot_api_cache.db`** (RM-153 measured `freelist_count = 0`, it is all live payload, and its `-wal`/`-shm` sidecars must never move separately). **(iii)** T4 is attended and EXCLUSIVE (a Win32 `PrintWindow` path is UNMEASURED). **(iv) CLOSED 2026-08-04 as STALE, not false** - `MASTER` IS present in `data/rank_baselines.json` and non-degenerate; two vacuous-evidence traps recorded in `docs/ROADMAP_HISTORY.md` (2026-08-06) + LEDGER 1195. **(v)** the two forward Riot-terms conditions - prior WRITTEN APPROVAL before reselling / licensing / charging for Game Information, and TERMINATION requiring deletion of all Game Information, which `game_information_manifest` now enumerates (49 paths / 8.7 GB). The postgame narrative (`core/precomputed_replay_narrative.py` + `core/pgr_event_report.py`) is SHADOW-ONLY; no live coach path flips. Criteria triage: `docs/REPLAY_T2_PARSE_CRITERIA.md`. Specs: `docs/REPLAY_FRAME_ANALYSIS_SPEC.md`, `docs/REPLAY_T4_FRAME_CV.md`.

- **RM-106b** [OK] **OPEN AND VALIDATED - the only route that recovers ITEM progression for KIWI.** With `EnableReplayApi=1` in `game.cfg`, a live replay serves `:2999/liveclientdata/allgamedata` (10 players with `items` / `level` / `runes` / `scores`), and `POST /replay/playback {"time":N}` seeks exactly - so per-timestamp item state is reconstructable by diffing `allPlayers[].items` across seeks. **Do NOT re-inherit the earlier 404 verdict** - that probe ran flag-OFF and was wrong. Measured limits: `activeplayerabilities` 400s in a replay (skill order NOT recoverable) and `eventdata` carries no `ITEM_PURCHASED` (items come from SAMPLING). The "forward-capture only" bound is CONDITIONALLY FALSE - an old `.rofl` paired with a matching old client backfills over the same route; the limit is archive depth plus locally-kept clients, not Riot retention. **SEEK-AND-SAMPLE IS NOW FULLY CHARACTERISED (2026-07-26, RM-117 / LEDGER 1062):** the seek is EXACT only when PAUSED first (`{"paused":true,"time":t}`; running playback silently drifts the sample), and sampled inventory is GROUND TRUTH - all 7 final item slots matched the match blob exactly, level 16==16. Reconstructing inventory from Match-V5 `ITEM_PURCHASED` instead is ERROR-PRONE (it must model component consumption on upgrade, starting trinkets and `ITEM_UNDO`), so this lane is not merely an event-mode fallback - it is the only source reporting the inventory the game itself had. **The archive-depth question is now MOOT for analysis:** Match-V5 timelines paginate arbitrarily deep with NO retention cutoff (a 2026-03-31 patch-16.6 match still returns 22 frames), so history comes from timelines, not from replaying old `.rofl` against kept clients. Full record: `docs/ROADMAP_HISTORY.md`.

- **RM-119 CLOSED 2026-07-28 (R217-U1, `9e7b70d1`)** - push CI gates the WHOLE tree, both halves. Narrative: `docs/ROADMAP_HISTORY.md`.
  - **Skip audit (R219, 2026-07-28) - FULLY DRAINED 2026-08-06; nothing in this class is open. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08) - read it there before re-opening any skip-masking question.**

- **RM-124 BUILT + GATED-OFF 2026-07-29 (`266c1fac`), awaiting one-game live cadence validation before the flip.** Pure `wave_callout()` lives beside `recall_callout` in `core/event_callouts.py`; `minion_events` (the `MinionsSpawning` anchor) is extracted in `dashboard/_liveclient.py` and threaded through `_deterministic_coaching.py` into `next_callouts(..., enable_wave=)`. **Correction to the teardown below: the callout renders on the GENERIC `web/js/panels/callouts.js` sink (any `{tag,line,eta_s,kind}` row), NOT `objective_chips.js`, and it does NOT light up the `next.js:16-83` 3-lane per-lane percentage UI - that needs per-lane push %, which is the data-blocked STATE machine.** Live emission is gated OFF: `enable_wave` defaults False and the dashboard only sets it when env `RC_WAVE_CALLOUT=1`, so `/api/state` is byte-identical today. **The one remaining task is the do-not-flip-blind live validation** (see the gated-sync doc): anchor + cadence constants (`_WAVE_INTERVAL_*`, `_CANNON_BRK_*`, `_is_cannon_wave`) are PROVISIONAL and must be checked against one real SR game's `MinionsSpawning` EventTime + observed cannon arrivals before `RC_WAVE_CALLOUT=1`. Follow-on F2 (CS efficiency curve) is unblocked once validated. Original teardown context preserved below.
  - **Pre-build filing (R220 2026-07-28) RELOCATED 2026-07-30 - the build it specified has landed; only the fences below still bind.** Full teardown text: `docs/ROADMAP_HISTORY.md` (2026-07-30 block) + `docs/COMPETITOR_LIFT_2026-07-28.md`. **(1) Do NOT hardcode a spawn time** - three sources disagree on first wave (0:30 / 1:05 / 1:30) and on the cannon breakpoint (14:00 / 15:00), and a 2025 change moved first-cannon arrival 2:05 -> 2:35; anchor on the live event's `EventTime` and validate the table against one real game before any flip. **(2) Do NOT re-pitch a live wave STATE machine** (freeze vs slow-push vs fast-push) - data-blocked three ways: `:2999` exposes no minion entities, the Overlay Platform M GEP contract gives `minionKills` counts only, and Match-V5 has no minion event type, so nothing wave-shaped is backfillable from the corpus and only a Lane-E CV route could ever see it. **(3)** The `web/js/panels/next.js:16-83` 3-lane wave readout and `wave_state_now` / `wave_control` / `wave_freezes` / `cannon_cs_summary` (`core/match_metrics.py`) have ZERO non-fixture producers and stay dark - that is (2), not a wiring gap. **(4) Tempering:** `tools/hz_shadow_report.py:196-201` EXCLUDES crash / freeze / push from the laning agreement sample, so a wave feature will NOT move the Lane A number. Follow-on F2 (CS efficiency vs a theoretical-max curve, replacing the flat `core/lead_projection.py:57` `_CS_PER_MIN_BENCHMARK_SR = 8.0`) is gated behind this clock.

### CLOSED in this lane - ids kept reachable, fences RELOCATED

Relocated VERBATIM 2026-08-06 (run-02 size-budget pass) to `docs/ROADMAP_HISTORY.md`.
Nothing there is open. **Read that block before re-opening RM-100 / RM-106 / RM-106a / RM-107 /
RM-109 / RM-111 / RM-112 / RM-115 / RM-116 / RM-120 / RM-123 / RM-126** - each carries a
do-not-re-pitch fence, not just history. RM-106b and RM-125 are still OPEN above.

- **RM-190 CLOSED 2026-08-12 (LEDGER 1241, `f2e162e3`) - operator chose a1: the 16.16.1 DDragon mirror is committed, item 3175 flat magic pen 18 -> 20 carried into the DS registry, ENGINE 1.277.0 -> 1.277.1, DS left PINNED to data patch 16.15.1.** Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-12 size-budget pass) - READ IT before re-opening this id; it carries the two measured corrections and the traps. **Fences that must survive the relocation:** `:8860` reporting patch **16.15.1** at engine 1.277.1 is **CORRECT, not drift** - do NOT file the version gap as a defect, and do NOT bump the DS per-patch snapshot casually (21 files / ~12 MB, needs a Meraki extract, and never `--force` - the `latest` endpoint is mutable). `core.build_order_precompute --static` does **NOT** restamp `build_order_variants_*.json`; that needs the separate `core.build_order_variants` run. `tools/ds_share_sync.py` deliberately does **NOT** author release history - `Share/CHANGELOG.md` + `Share/README.md` need hand-written entries even after the sync reports clean.
- RM-190 original filing body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-12 size-budget pass) - it carries the full measurement trail and the two option shapes; READ IT before re-opening this id.

## NEXT - live-game-gated / operator-gated

- **RM-204 OPEN (filed 2026-08-14) - the Arena augment play-line is BUILT, TESTED and DELIBERATELY INERT; flipping it on is the residual Haiku-to-ZERO hop and it is OPERATOR-GATED because it retires a live coach call.** Body in `BACKLOG.md` under "Draft + coach lane (Haiku-to-ZERO north star)", NOT here (byte-budget overflow rule). One-line summary: `dashboard/_deterministic_coaching.py:1481` still passes no `owned_augments=`, so `core/arena_augment_playline.py` reaches no served output. **Three things must be true before the hop lands** - there is ZERO shadow det-vs-live agreement history for this column (it has been empty since v1, so there is nothing to compare against), it is unvalidated against a real or replayed Arena game, and the display-name resolution path is untested against real vision output. **Only the PLAY half of the field is closed; the SELECT half is still Haiku-only.**

- **RM-191 OPEN (filed 2026-08-12, LEDGER 1242 residual) - `write_fatal` is a WRITE-ONLY channel with ZERO readers, which is why a 3-day outage was found by hand instead of by a monitor. OPERATOR-GATED: the fix touches the FROZEN `ops/rc_dev_runtime.py`.** Measured this session, not inferred: `grep -rn last_fatal` over the whole tree returns **only its own writes** (`ops/rc_dev_runtime.py:152` / `:251` / `:252`) plus a row in `ops/audit/p0_inventory_full.csv`, which is a file-inventory listing and not a consumer. Nothing reads `ops/runtime/last_fatal.txt` - not the supervisor, not the dashboard, not `/api/health/all`, not a test - and `health.json` carries **no `fatal` key**, so the marker never reaches the API either. Three compounding defects, all in `write_fatal` (`ops/rc_dev_runtime.py:250-255`): (1) it uses `write_text` to a **single-slot** file, so each occurrence **overwrites the last** and there is no frequency record; (2) it never reaches `logs/YYYY-MM-DD.log`, so the one place a human or a watchdog would look has nothing; (3) its own `except Exception: pass` means a failure to record the failure is itself silent. **The evidence this row is about:** the 2026-08-09 outage (`WAKEUP_NOTES.md`) was diagnosed only when someone opened that file on 2026-08-12 - the `PermissionError [WinError 5]` had been sitting there for three days. The rename retry shipped (LEDGER 1242, `603a8fd2`) and stops the common cause, but it explicitly does **NOT** close this: a persistent fault still raises into `write_fatal`, and that path is still unobservable. **Do NOT scope this as "add a log line".** The useful shape is a counter plus last-N ring reaching BOTH `logs/` and the health payload, so a stale-health condition is visible in `/api/health/all` while the process is still alive - which was the actual failure mode (the health file froze while the loop kept running; the supervisor being dead separately is why nothing restarted it). **Also unanswered and worth one probe before designing:** nothing identified WHAT held the handle on `health.json` at the swap - a reader, Defender, or the indexer - and the retry masks that permanently, so a diagnostic that records the holder is worth more than a bigger retry budget.

- **RM-189 OPEN (filed 2026-08-11, LEDGER 1238/1239) - the Overlay Platform M submission program. The RENAME half is DONE and no longer blocks; what remains is OPERATOR-GATED.** Riot's third-party rules cost RC four banned surfaces (enemy summoner-spell tracker, summoner+ultimate cooldown ledger, power-spike cue, `/api/cooldown-watch`), all removed with INVERTED guard tests, and the CC threat cell was rebuilt compliant (`cc_threat_cell`, CC duration not cooldowns). The product is now **Amberstone** (engine stays Daemon Slayer) across packaging, appId, brand, manifest, User-Agent and the GitHub repo `Remus3/amberstone`. **STILL BLOCKING, all operator-only:** the Riot Developer Portal product registration needs the operator's account login, and a PRODUCTION key needs a public website + Terms of Service + Privacy Policy, none of which exist. **B4 IS BUILT 2026-08-12 (a through e; `f0501048` / `543f738c` / `12fc506c` / `149468f9` / `66914b81`) - only B4-f remains and it is a DevRel question.** Producer-side suppression of every coach imperative while a game is live plus the top-level `callouts` / `lead_projection` feeds, a client-side gate, a silent voice path, `game_id` + `game_run_id` on the shadow record, and the post-game DECISION BRANCHES card over `/api/branch-review`. Design + the three findings that reshaped it: `docs/OVERLAY_B4_DESIGN.md`. **B4-f is the open question: are build/shop recommendations in or out during a live game?** Raise it with B8 (vision/OCR capture) and B9 (declare LCU use). **Also corrected this session: B3 was recorded DONE while its artefact was still live** - see plan section 6c2, and do not re-close a row by deleting the component named after the rule. **Framework verdict for any port: ow-electron, not Overlay Platform M Native.** **Do NOT re-open:** Tier 2 (the repo DIRECTORY rename - recommended defer indefinitely, no outsider sees it), Tier 3 (the `RC` abbreviation - initials are not a trademark), or the TFT augment-stats blocker (REFUTED - RC displays none of the three, and the augment win-rate source is Arena, which the TFT-scoped rule does not reach). Plans: `docs/OVERLAY_COMPLIANCE_PLAN.md` + `docs/RENAME_SWEEP_AMBERSTONE.md`.
- **RM-167 NEEDS-OPERATOR (filed 2026-08-06, LEDGER 1206) - the aram prompt-cache prefix hoist, worth roughly 10-15 pct of TOTAL spend, and the 12 `cache_control` markers already in the tree are MEASURABLY INERT.** Across 51 real ledger days, 38,319 calls and 62.1M input tokens, `cache_in = 126` and `cache_write = 126` (**0.0002 pct**), because every marked prefix sits below its model's cache floor - aram `_SYSTEM` static tail 6177 chars (~1670 tok vs the 2048 Haiku floor), arena 3082, vision 616 vs 1024. Worse, `coaches/aram_coach.py:920` interpolates all 5 placeholders into the FIRST 145 chars of `_SYSTEM`, so the marked prefix mutates every tick. A hoist still misses the floor and needs deliberate padding, and moving instructions shifts Haiku positioning, so it needs a live ARAM A/B for behavioural equivalence plus a live `count_tokens` (offline estimates 1670-3089 straddle the floor). **NOT covered by the item-286 closure**, which measured only the four augment/TFT prompts. Measurements: `BACKLOG.md` (prompt-cache floor). **RE-MEASURED 2026-08-08 on the POST-REPAIR ledger (LEDGER 1236) - the conclusion is unchanged and the SIZING is not: 56,166 calls / 62,375,169 tokens_in, `cache_in = 138`, `cache_write = 138`, 0.000221 pct. And the prize is BIGGER than this row says - `aram_coach` alone is $40.02 across 12,413 calls, 31.2 pct of all-time spend, so "roughly 10-15 pct of TOTAL spend" is the LOW end.** Still NEEDS-OPERATOR for the same two unchanged reasons: the hoisted prefix still misses the floor without deliberate padding, and padding plus reordering needs a live ARAM A/B for behavioural equivalence.
- **RM-168 LIVE-GATED (filed 2026-08-06, LEDGER 1206) - the log-rate sweep could only sample an IDLE client, so it is CLEAN but WEAKLY POWERED.** mode=client, no game, no dashboard tab: 0.0475 lines/s overall, worst unsuppressed path 0.031/s. The interesting case - in-game, multi-tab, where the suppression list's own comment says the 2 Hz volume lives - is UNMEASURED. Pair it with the RM-01 A/B live ARAM rather than spending a separate game on it. **RE-SAMPLED 2026-08-08 (LEDGER 1236) and it was AGAIN an idle client with League not even running, so the row is now weakly powered for the THIRD time. Stop re-sampling it headless - a fourth idle sample adds nothing.** Adjacent and already actioned: the S4 dedupe (LEDGER 1235) removed 21-25 pct of total log volume by fixing the single loudest line, so the log-rate question this row asks is materially different from when it was filed and should be re-baselined in the same live game, not against the pre-dedupe figures.
- **RM-157 CLOSED 2026-08-04 (`b21d4bf6` + `4cc862d0` + `027d1031`, LEDGER 1186/1188/1189) - duplicate `/done` full-suite dispatch REMOVED; per-push suite priced against the real invoice and KEPT.** Rate **0.006/min**, allotment **~3000/mo**; the old `settings/billing/{actions,shared-storage}` are **410 Gone**, use `users/{u}/settings/billing/usage` with the `user` scope. Suite step is ~64 USD/mo and buys the only pre-merge gate. **Do NOT restore the dispatch** and do NOT re-pitch deleting the suite on cost share - cite a catch rate. Push-volume lever: `BACKLOG.md`. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-04 + 2026-08-06).
- **RM-156 CLOSED 2026-08-04 (LEDGER 1185) - CI step timeouts at `45` plus job ceilings 65 / 60; the filing's own headroom figure was wrong (about 3 minutes, not 13).** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-154 CLOSED 2026-08-04 (`30fa3cae`)** - the three pythonw agents file-log to `logs/<agent>.log` and exit 2 loudly when tokenless. Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-153 CLOSED 2026-08-04 (`04c58336`)** - bounded by OBSERVABILITY, not eviction, because the row's duplicate-retention premise is REFUTED: **2698 of 3123 timelines (86.39 pct) exist in NEITHER `rewind_history` nor the `.rofl` archive**, so eviction destroys the only local copy. Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-152 CLOSED 2026-08-04 (`38fffbc8`)** - `_BODY_READ_TIMEOUT_S = 10.0` armed narrowly around the body read, plus the DS side's header-refused 1 MiB cap. Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ THEM before re-opening this id.
- **RM-151 OPEN (filed 2026-08-03, lane 8 / LEDGER 1180) - the postgame corpus looks like it has NEVER been written by the EOG path, only by the single-participant history fallback. LIVE-GATED.** Full body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ IT before acting on this row; it carries the measurements and the do-not-do list.
- **RM-149 OPEN (filed 2026-08-02, lane 7 / LEDGER 1174) - two operator rulings on out-of-repo cleanup, both blocking any future lane-7 pass over `%USERPROFILE%` paths.** Full body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-06 run-02 block) - READ IT before acting on this row; it carries the measurements and the do-not-do list.
- **[x] Mission Control S8/S9/S10 - lanes 7/8, the INTERRUPT tier, and the dashboard DECOUPLE - ALL SHIPPED 2026-07-31** (`97c74550`, `79cdd590`, `4ad8d535`). Full narrative relocated VERBATIM to `docs/ROADMAP_HISTORY.md` 2026-08-01 (ROADMAP hit 91 percent of its size budget); do not re-derive it here. Residual minors live in `BACKLOG.md` as RM-134 (shipped) plus five adjudicated non-blocking items.
- **RM-113 w-nextbuy live-frame acceptance - 1 row still OPEN (2026-07-23).** OQ16 cadence and the `#am-next-buy` no-reflow hold both PASSED. **OPEN TRINKET:** never activated in 260 samples, and the stage gate cannot explain the silence (`stageFor` returns `mid` on the clock arm alone at `c >= 600`, `web/js/lib/next_buy_model.js:68-72`). Reduces to one unrecorded fact: whether `owned_items` still held `stealth ward`. `tools/overlay_live_frame_probe.py` now records it per sample and **flushes each row** (load-bearing - redirected stdout is block-buffered), so the next SR game closes this with no new analysis. Narrative: `docs/ROADMAP_HISTORY.md`.
### DS defensive-half sweep GAP specs (R132, 2026-07-19) - ALL CLOSED, fences RELOCATED

Relocated VERBATIM 2026-08-06 (run-02 size-budget pass) to `docs/ROADMAP_HISTORY.md`. Nothing
is open. The fences that must not be re-pitched live there: **R134 CLOSED-REFUTED** (do NOT
re-pitch the four-term `total_armor` sum), **R135 CLOSED-INERT** (the movespeed soft cap is real,
correctly stated and unreachable at every live site), and **RM-99 / RM-99b / RM-101 / RM-102 /
RM-103 / RM-104 / RM-105 / RM-108 / RM-114** - read their verdicts before touching the DS
scorer/item surface, in particular RM-99 (the coefficient is 10 percent, NOT the spec's 8) and
RM-102 / RM-104 (Arena mirrors, PLURAL - never re-file as a single-item fix).

### DS per-champion meta-valuation sweep

- **RM-04 DS sweep: ROSTER CLOSED 173/173 - do NOT re-open the roster or re-scan for uncovered champions.** Canonical tracker `docs/DS_SWEEP_TRACKER.md`. Body relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-03) when `tools/drift_guard.py` flagged ROADMAP.md at 100 percent of its 81920-byte budget; the row carried zero open markers. **Two probe hazards survive here because they silently produce plausible-but-WRONG output:** `POST /rank` is the CARRY scorer and silently IGNORES `enemy_ad_share`/`enemy_ap_share` - use `/rank-<archetype>`; and probing at `item_ids=[]` under-ranks amp/complementary items, which is what manufactured the two headline RM-92 instances. Memories `reference_ds_probe_rank_vs_archetype_route` + `reference_ds_probe_empty_build_artifact`. Further growth needs a schema lift, not another scan.
- **RM-81 data currency (open, tooled + measured; RESCOPED 2026-07-18).** Narrative relocated 2026-07-28 - see `docs/ROADMAP_HISTORY.md`. Open work and the measurement traps are unchanged there; nothing was closed by the relocation.

- **RM-35 .. RM-48 DS sweep GAP specs - ALL FOURTEEN RESOLVED; the row is CLOSED.** **RM-36 Ezreal SHIPPED 2026-08-04 (ENGINE 1.275.0, `b510ce20`)** was the last open id: DEFAULT-OFF `apply_ad_axis_dual_scaling_split`, `ap_pct_sum` gate UNCHANGED, MAGIC permanently excluded, and **Sejuani REPLACES Ezreal as the seam's zero-term control**. The default-ON flip stays blocked on RM-98 and belongs to gated row G2-46. The other thirteen are shipped, refuted, or closed-BLOCKED-unfalsifiable - read the per-id verdict in `docs/ROADMAP_HISTORY.md` (2026-08-04 + 2026-08-06 blocks) before re-picking ANY of them, and do NOT re-open the roster.

  | id | champion / scorer | the defect | fix class |
  |---|---|---|---|
  | RM-35 .. RM-48 | 14 champions | **table relocated 2026-07-25 to `docs/ROADMAP_HISTORY.md`** (size budget) | see below |

  The verbatim 14-row table and the RM-40 / RM-45 / RM-47 blocked-prerequisite note now
  live in `docs/ROADMAP_HISTORY.md` (2026-07-25 block). Authoritative record is
  `docs/DS_SWEEP_TRACKER.md` (RM-35..RM-98) plus the per-champion memory files.
  **Status changes made 2026-07-25 (this session):** RM-44 Amumu is CLOSED - its stated
  prerequisite (champion-sensitivity in `ds.ehp`) is REFUTED, see the NOW lane. RM-48 Azir
  is CLOSED BLOCKED-UNFALSIFIABLE - nine mages with and without pets return Liandry's #1 /
  Nashor's #16-#19 at identical params.

### Programs with open kernels

- **RM-12 Haiku-elimination program - the PRIMARY north star (drive live Haiku usage to ZERO via precomputed DS lookups) is OPEN; the item-265/266 foundations are shipped.** Shipped and on disk (inventory relocated to `docs/ROADMAP_HISTORY.md`): Lane A matchup engine + `/v2/matchup`, Lane B build-order tables, Lane C/E/P deterministic generators, W3A/W3C/W3E, item-280 ARAM comp verdict. **STANDING GATE FINDING (do not re-litigate):** the matchup engine validated as a COIN-FLIP vs real outcomes (gold-at-10min 48.8%, solo-kill duel 51-52%, n in the thousands) -> NO coach was flipped off Haiku. The matchup-derived A/B chips are SECONDARY; the correct-by-construction callouts + lead are the valid Haiku-elim wins. **OPEN (operator-gated):** matchup-engine fidelity lift (item-aware + real-level; uncertain payoff, likely a modeling ceiling) OR pivot to non-prediction surfaces; overlay Lane D (Electron Phase 2); champ_select pickban-DB flip after a counter-quality validation; the champ-select brief Haiku flip after shadow-log accrual (served stays Haiku per the item-273 AST guard; the READ-ONLY gate that clause needs now exists - `tools/champ_select_shadow_report.py`, R218 `bb746286` - so what remains is accrual, not tooling: `data/champ_select_shadow.jsonl` is empty and the report reads `state=awaiting_accrual` below MIN_SAMPLE 20). Full shipped narrative: `docs/ROADMAP_HISTORY.md` (2026-07-18 block).
- **RM-14 DS gated flip levers (dedup of the items 227-239 NEXT stacks; full records `docs/ROADMAP_HISTORY.md`).** Operator-gated, ENGINE bump where noted: flip opt-in flags default-ON after live validation (`gate_ammo` / `apply_mode_modifiers` / `aoe_targets_hit` / `apply_ability_haste` / `apply_passive_damage`; `target_hp` Cut Down / Coup de Grace gate R51 `d0b14962` rides `docs/LIVE_GAME_GATED_SYNC.md`); auto-enable assassin keystone in /rank-assassin (operator sign-off); missile combo-clock delay IF a real per-spell cast-range source lands; static-CD consumer (needs slot->ability-name bridge + a live ability-haste model); `assume_missing_hp_heal_amp` flag (hps.py:499/:828 default OFF - the R5 heal-amp seam; the BASE ability-HPS fold-in is ALREADY live per OQ22); enchanter registry omits 5 corpus-proven winners (Dream Maker / Dawncore / Shurelya / Seraph / Luden - FUTURE); the 4 non-every-AA `on_hit` entries structured cadence + live "saner not different" re-rank; Phase D live flag-flips + the 4 different-seam exclusions + the deferred Phase 11 vision-frame collapse; the percent-of-resist "read the parsed block" lift (needs a NEW schema seam); `rune_procs` into live `rank_items_by_burst`; per_attack runes expansion (saturated); mana_sim + runes as scenario_matrix metrics; the operator sends the `Share/` handoff (notates when/to-whom). cc_tags operator-CLOSED - do not re-pitch.
  - _RM-19 / RM-20 / RM-21 were verbatim restatements of the clauses above (`apply_passive_damage` + the 4 non-every-AA `on_hit`; Phase-D flag-flips + the 4 different-seam exclusions + Phase 11; Phase-D + the read-the-parsed-block resist lift). They are FOLDED INTO this bullet - RM-14 is the canonical index. Gated rows: `G2-04`, `G2-21`, `G5-05`. RM-21's own DONE note (the percent-of-resist Poppy/Rell seam, item 268 + R45 ENGINE 1.159.0) is shipped and relocated._
- **RM-15 Electron shell + in-game overlay - phases 1-5 ALL SHIPPED code-side; only the un-headless tails remain.** Plan `docs/ELECTRON_OVERLAY.md`; the shell is `rc-shell/` (shipped feature list relocated to `docs/ROADMAP_HISTORY.md`). **OPEN:** (a) live-game overlay capture with ACTIVE knob interaction - operator in a real match, shell relaunch picks up the shipped slices; (b) operator packaging - `npx electron-builder` + first GitHub Release + a packaged update check against the private repo (`GH_TOKEN`), tracked as a gated row; (c) Phase 6 Pengu in-client panels, OPTIONAL. **Risk register (standing):** the overlay is DWM compositor, not DXGI capture (in-game continuous capture was retired, 1-PC ADR-011); Borderless mandatory; Vanguard-safe (only `:2999` + LCU, no memory reads/injection). Full 5-phase slice narrative: `docs/ROADMAP_HISTORY.md`. **TRAP (2026-07-30, `69404dcc`): the plan doc's section-5 window-state table is no longer prose** - `rc-shell/test/overlay_state_contract.test.js` PARSES it and derives its assertions from the parsed cells, so editing the doc alone can turn the suite red. `rc-shell` `npm test` is a green gate at 326/326 (`5d590a4f`).
- **RM-16 Carry-forwards, compressed (items 215/211/283/277/276; full records `docs/ROADMAP_HISTORY.md`).** **REFUTED + STRUCK 2026-07-19 - do NOT re-file these three:** item 215's *poller* clause, item 212 entirely, item 269 entirely. Each is pinned by a named test and the evidence is in `docs/ROADMAP_HISTORY.md`. Still open: **item 215** OBS DXGI match-end capture watch (lock one resolution + League Borderless while recording). **item 211** `matches.tracked_puuid` can be stale - ALWAYS re-resolve via Account-V1; Live Client rarely exposes `gameId` so `game_id=0` rides the ts-window fallback (the 7 residual orphan rows are gated row `G4-29`). **item 277/321** FUTURE: wire the Anivia egg-resist + Orianna ally-resist seams into a live survivability scorer + per-champion validation. **item 276** Zaahen real curated build; PGR S5 polish / responsive / ARAM-Arena variants + S4 @N; Build Insights skill tab. Do-not-redo: the DS engine is verified across 4 lanes (pen / on-hit / crit / AoE) - do NOT re-audit; item-286 prompt-cache restructure is CLOSED-not-viable (guard `tests/test_prompt_cache_floor_item286.py`).
- **RM-26 vision seeds - ONE thing is open: VALIDATING the anchor model, and it is blocked on an input this machine cannot produce.** Everything else shipped: the tier-2 `resolution_seed` resolver (2026-07-24 `7d947f11`), the `core/vision_tesseract.py` consumer fix + crop-rect guard (LEDGER 1227), the scale-by-height + per-region anchor model `derive_anchored_regions` / `REGION_ANCHORS` DEFAULT-OFF (LEDGER 1229), and the calibrator page (LEDGER 1230). **Do NOT re-open any of those, and do NOT 'fix' seed reachability again - it is reachable and moves 0 of 21 crop rects.** **The blocker, stated precisely so nobody re-measures the wrong thing: at a MATCHING aspect the width ratio equals the height ratio, so left / center / right / top / bottom anchoring all yield the BYTE-IDENTICAL box.** 16:9 therefore cannot discriminate between anchor classes - that, not accuracy, is why the 16:9 error measured exactly 0.00x, and it means no 2560x1440 still can confirm or refute a single row of `REGION_ANCHORS`. **Needed: ONE native full-screen 21:9 or 32:9 capture.** `ingest_reference_from_path` already accepts it, so the frame is the whole remaining gap. Until then the model stays DEFAULT-OFF and `seed_profiles()` keeps no anchor knob (a seed on disk is promotable to a calibration). Measured delta if adopted: 0 of 21 boxes change at 2560x1440, 21 of 21 at every ultrawide, worst 492px at 5120x1440. Second finding, already pinned: under scale-by-height the top/bottom axis is arithmetically INERT (bottom anchoring reduces to top anchoring) - it is kept for intent, not because it moves a pixel.
- **RM-27 External refactor-plan triage (item 312) - triage done, two accepted items unbuilt.** Operator handed 3 Desktop review docs (kept OUT of the repo per the outreach rule); each proposal was triaged against hard rules / frozen list / settled ADRs, proposals treated as INTENT and verified vs live code. SHIPPED: P4.2 `RC_TESSERACT_CMD` env override in `core/vision_tesseract.py`. REJECTED, do not re-pitch: P3.1 amp-DRY (PREMISE FALSE - already centralized in `effects.py`); P5.1 drop-the-em-dash-ban (the settled PS-ParseFile hard rule). GATED on explicit approval: P1.2 / P1.3 / P4.1 (frozen files), P2.1 FastAPI rewrite (undoes ADR-008 + the Vanguard-safe stdlib posture), P5.2 SQLite ledger (already solved by the LEDGER relocation + budget). **OPEN, each its own session:** **P2.2** structured-output (touches the live coach parse, partly redundant); **P3.2** antitank dynamic %HP (a design change - antitank is intentionally static per item 308); **the 6 UNIVERSAL_FILES template convention update** (this is also the whole residual of the old RM-18, whose 2026-06-05 NEXT-UP block is closed and relocated; its P3.2 live-producer route-wiring + ENGINE bump ride the RM-14 Phase-D tail). Loop note: the loop director reads repo-only and picks ONE bounded item per cycle - pin any accepted item in NOW to route it through the loop.
- **RM-06 Overlay redesign owed tail.** Offered, not built: a manual ward tracker; folding the enemy-spell tap-tracker into the CD ledger if the operator prefers one panel. **Do-not-redo fence (standing):** a native-HUD REPLACEMENT is impossible - Live Client exposes NO ability/summoner cooldowns, buffs, wards or XP, so the overlay AUGMENTS only and the enemy tracker stays MANUAL. The Electron live-confirm and the `ward_cue` teardown (a deliberate live-session job: live serializer + test + overlay DOM contract, never a blind headless delete) are gated rows. Detail: LEDGER 760 + `docs/ROADMAP_HISTORY.md`.
- **RM-17 build-ORDER go-live + Phase-3 UI render - code SHIPPED, only the live render review is owed.** UI B+C shipped (`2d7da9e`), `/api/build-order` serves, and the ARAM/Mayhem keystone bug is fixed + proven live (`3eb2e2d`) - shipped detail in `docs/ROADMAP_HISTORY.md`. **OPEN:** the reviewed UI session + per-page UI-audit ritual on a live champ-select (gated). CLOSED-as-stale: the `only_item_ids` threading carry-branch fix (`rank_for` already threads it, verified item 222).

### Rerouted to `docs/LIVE_GAME_GATED_SYNC.md` - table RELOCATED

The 13-row id-to-gated-row mapping (RM-05 / RM-07 / RM-08 / RM-09 / RM-10 / RM-11 / RM-13 /
RM-22 / RM-23 / RM-24 / RM-25) was relocated VERBATIM 2026-08-06 (run-02 size-budget pass) to
`docs/ROADMAP_HISTORY.md`. Those items were purely "owed live capture" or "flip after a real
game"; the gated doc already carried each one, so the ROADMAP copy was a duplicate. **The work
is drained from the gated doc, not from here.** Ids stay reachable via that relocated table, and
its per-row do-not-redo notes travel with it - in particular that per-champion minimap isolation
by colour/size/motion is a validated DEAD END, that replay scroll-wrap is CORRECT and must not be
"fixed", and that RM-13 is a standing per-game ritual which never closes.

## LATER - parked / blocked / low-value

- **RM-166 CLOSED 2026-08-08 - the PGR baseline `(5, 50)` clamp bounds are pinned by a contract test, and the row was TRUE but UNDERCOUNTED (FIVE representations, not two).** Body + fences relocated VERBATIM to `docs/ROADMAP_HISTORY.md` (2026-08-08 pass 11) - READ THEM before re-opening this id.
- **RM-28** [WIP] **DEEP-AUDIT PROGRAM (2026-06-11, operator overnight directive - SUPERSEDES the per-item NEXT-UP queue while it runs).** Standing charter `docs/DEEP_AUDIT_CHARTER.md` (P0 baseline -> P8 docs); loop armed via ops/loop (max_cycles 100); live state on Desktop `RC_DEEP_AUDIT_SYNOPSIS.md`. Frozen files + history + gist + .md rewrites authorized; vanguard/CV + 2-PC/gamepc prune; BOM/encoding retro-sweep; Haiku/Sonnet-to-ZERO completion; Electron overlay to FULL (web dashboard no longer required); the director holds scope questions.
- **RM-29 Repo-audit D1 residual (own session).** The full 362-file Share-mirror de-dup / build-time-gen stays deferred (outward-facing gist + CI `--check` coupling). D1 churn-fix + D4 ratchet + D9 auth test shipped: LEDGER 490/506 + `docs/ROADMAP_HISTORY.md`. Do NOT re-pitch the D4 hot-path-narrowing revert or the D12 logger reverts as bugs (deliberate fixes).
- **RM-30 Dashboard data-wiring gaps (low value).** Open: ~88 `st-*` ADAPTATION rows have no live producer - correctly HIDDEN in-game by `_hideEmptyStatRows()` (`94f1e07b`); wiring live producers only affects the retired Chrome dashboard panel (LEDGER 573). `/api/ward-heat` permanently empty (`core.ward_events.record_ward` has no producer - Live Client emits no WARD_PLACED). Shipped parts (tonight-pick tips, cs_at_10/csd_at_15 producers, phantom active-match fix): `docs/ROADMAP_HISTORY.md`.
- **RM-31** [WIP] **ADR-007 phase 3 - prose-coach deprecation** - mode coaches still narrate continuously in parallel with decision_detector. Detector-by-detector deprecation pass deferred until phase-1 detectors prove out in real games.
- **RM-32 DS calibration pipeline - UNBLOCKED + BUILT 2026-07-25; the residual is CORPUS, not code.** `core/ds_calibration_agreement.py` + `tools/ds_calibration_report.py` (Laplace, `MIN_BUCKET_N = 20`, DS-rank firewall pinned both directions). **The first result is instrumentation, NOT a verdict** - 50 joinable games / 462 observations cannot separate a +0.0677 shrunk delta from noise on one account. The keyless-row blocker is CLOSED (D-01b). **ARAM 5257 is provably UNRECOVERABLE and that is not a bug** - the ARAM played there is Mayhem (`KIWI` / queue 2400), which Match-V5 403s. Measurements: `docs/ROADMAP_HISTORY.md`.
- **RM-33 CLOSED-STALE 2026-08-02 - auto-ops verb expansion, PARKED not built. Do NOT re-open as a build row.** Three probes refuted it: `auto_ops_verbs` exists in NO code or config (prose only), the 95-percent gate has no meter anywhere, and `docs/OVERLAY_BUILD_MASTER_PLAN.md:169` had ALREADY recorded it as an explicit park - ROADMAP just never caught up. Full narrative in `docs/ROADMAP_HISTORY.md`.
- **RM-34 Standing fences (do-not-redo).** Originals in `docs/ROADMAP_HISTORY.md`; the consolidated list relocated 2026-07-28 to the same file. Read it BEFORE re-opening any fenced line.

## Fleet status at a glance

Live status is intentionally NOT duplicated here - a static table goes stale (this one had drifted to pid 1108 / ENGINE 1.63.0 / patch 16.11.1). Sources of truth:

- Processes, pids, listeners, scheduled tasks: `python tools/rc_facts.py` + `ops/runtime/health.json`
- All-node health rollup: `GET /api/health/all` (dashboard `:8888`)
- Daemon Slayer engine / patch / coverage: `GET :8860/health` + `data/daemon_slayer/current.txt`; DS feature + cc_conditional wave history lives in `agents/daemon_slayer/CHANGELOG.md` + `docs/DAEMON_SLAYER.md` (was inlined here as a ~20KB table cell - removed, not lost).
- TFT: Set 17 patch 17.3. **The CODE disagrees with that line and the disagreement is now LOUD, not silent (2026-07-30):** `tft/tft_data.py` declares Set 14, both set modules ship byte-identical `TIER_ODDS`, and `tft/tft_pbe_data.py` has no `UNITS_PER_COST` at all - so both modules carry `CONSTANTS_SET_VERIFIED = False` and a guard hard-fails if either is ever marked verified while stale. Resolving it needs a LIVE IN-CLIENT read (BACKLOG); do NOT correct these from a third-party odds table.

---

## Cross-cutting principles (never violate)

- **Frozen files** - see CLAUDE.md. Explicit operator sign-off required for any change.
- **Atomic writes only** - `tmp.write_text(...); tmp.replace(target)`. Overlays + dashboard poll mid-write.
- **`py_compile` before restart** - syntax errors crash silently under `pythonw.exe`.
- **Restart via `restart_trigger.txt`** - never `Stop-Process`; `taskkill /F /PID` for hard kills.
- **Coach prompt edits require RC restart** - batch edits, restart once.
- **Keep the `vision_token` + `mcp_token` resolvers SEPARATE** - never let one resolver fall back to the other's file.
