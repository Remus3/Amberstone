NEXT SESSION
------------
Task: RSC's reply on the JOINT RE-PIN is the gating item. A two-word sibling
  project name sits at tests/test_loop_concurrency.py:492, INSIDE the
  SHARED_SHA256 byte-identical block (lines 474-539), split across a
  comment-continuation line wrap. It is NOT contiguous in the raw blob, so every
  whole-token sweep ever run here reported it clean. RC cannot fix it alone:
  re-pinning is a JOINT act and digests are never regenerated from local disk.
  Relayed to RSC 2026-09-10; read moon_sync_inbox/ for their answer FIRST.
  If they have not replied, do NOT touch that file and do NOT re-pin. Pick
  RM-398 instead (see below).

Second task if RSC is silent: RM-398 - count_mismatch in
  tools/stop_claim_gate.py cannot distinguish a count asserted as SUCCESS from
  one asserted as a RED or mutation state. THE TRAP IS IN THE FIX: a "mutation:"
  or "red state:" suppressor is an evasion prefix available to the party the
  instrument polices, and would reopen the hole RM-397 just closed. Read the
  RM-398 row before designing anything.

Context:
  - tools/sibling_name_sweep.py + tests/test_sibling_name_sweep.py (RM-399,
    SHIPPED a715baf58). Armed via .githooks/pre-push, sweep runs BEFORE
    git lfs pre-push. Verify live: python tools/sibling_name_sweep.py --tree
    from the REPO ROOT - it resolves its root from __file__, so running it in a
    worktree scans the WORKTREE, not main.
  - Expect EXACTLY 1 finding: the KNOWN byte-pinned site above. Anything else
    is new and must be triaged before any push.
  - ops/moon_sync_repos.json is gitignored per-host config and the ONLY thing
    resolving a code to a real name. Absent in a fresh clone and in CI, where
    the sweep runs DEGRADED (structural arm only, banner says it proved
    nothing).
  - CLAUDE.md "Session Default" holds the adopted boundary. The push half gates
    on DIFF CONTENT, not destination.

Acceptance:
  - Joint re-pin: BOTH trees hash equal, digests re-derived from each tree's
    own disk and compared. A note claiming parity is NOT the acceptance.
  - The sweep's tree arm drops to 0 findings only after that lands.

Do NOT redo:
  - RM-397 SHIPPED (e518786e2), RM-398 FILED, RM-399 SHIPPED (a715baf58).
  - The BOUNDARY was adopted and then SUPERSEDED within the same session. Do
    NOT restore the destination wording, and do NOT restore the own-origin push
    carve-out - RSC proved it removed seam (f) rather than narrowing it.
  - MEMORY.md compaction is DONE (17454 bytes, zero unreachable).
  - The four sibling-name redactions are done at HEAD (f6cf005bb). Publication
    is NOT undone - the names remain in old commits on the public remote and no
    history rewrite was performed. Do not re-file that as a new leak, and do not
    claim it is fixed.
  - Do NOT dispatch a CI run by hand (RM-157). The push IS the full suite.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
