NEXT SESSION
------------
SHAPE: HEADLESS LOOP (operator, 2026-09-11 wrap). The next session does NOT run
      attended. It runs as a headless loop and does not ask permission per
      cycle. The four standing properties still apply INSIDE each cycle -
      orchestrated, multi-agent, self-adjudicating, self-adversarial - so the
      loop body is still a merger plus worktree slices plus an independent
      verifier gate, never solo inline work.

      THE BOUNDARY, unchanged and non-negotiable: HALT AND PING THE OPERATOR
      BEFORE ANY BYTE LEAVES THE TREE. That means before any write, delete or
      lock acquisition outside the repository root; before any edit to a
      cross-repository byte-pinned or grammar-pinned artifact; before arming
      any task; and before any PUSH WHOSE DIFF CAN LEAK. The push half is
      gated on DIFF CONTENT, not destination: an ordinary push that passes the
      suites and the pre-push sibling-name sweep proceeds, but a diff touching
      ops/loop/slots.py or ops/loop/winmutex.py (SHARED_SHA256 at
      tests/test_loop_concurrency.py:474, parametrised at :542) HALTS. There is
      NO TIMEOUT - RC halts and WAITS. Never proceed after a delay; a bilateral
      rule that auto-proceeds is a unilateral rule with extra steps.

      CONSEQUENCE FOR THE GATING ITEM BELOW: the joint re-pin IS a halt point
      by definition, so the loop can CHECK the inbox every cycle but must NOT
      execute a re-pin unattended. If RSC has replied, halt and ping; do not
      re-pin inside the loop. If they are still silent, the loop proceeds to
      the fallback and then to ROADMAP rows, one row per cycle, shipped end to
      end. The loop must never arm another repo's responder.

Task: Check the cross-repo inbox FIRST. The JOINT RE-PIN of SHARED_SHA256 is
      still the gating item, SIXTH session running - RSC silent since
      moon_sync_inbox/2026-09-09-2100-from-RSC. If they have replied, execute
      the joint re-pin. NEVER re-pin unilaterally. NEVER regenerate the digests
      from local disk - both trees hashing equal IS the acceptance, not a note
      claiming it. BYTE-level copy only, never write_text: write_text turns LF
      into CRLF on Windows and the pin is on bytes. If they are still silent,
      take the fallback below.

      FALLBACK - MEASURED ON DISK THIS RUN, not inherited from a row. Two
      tracked test-bearing directories are collected by NEITHER CI tree, so
      707 green tests are run by nobody:

        * agents/agent3_testing/suite/  - 359 collected, 359 PASSED in 24.43s
        * tools/tests/                  - 348 collected, 348 PASSED in 30.15s

      Both run locally clean at HEAD fc0058441 (pytest -q --tb=line
      --timeout=120 -p no:randomly, observed this run). CI runs only
      `pytest tests/ agents/daemon_slayer/tests/` - see .github/workflows/
      ci.yml:165 and ci.yml:537, both read this run; docs-guards.yml runs a
      selected-doc subset only. `git ls-files "*/test_*.py" "test_*.py"` minus
      those two CI roots returns EXACTLY these two dirs and nothing else, so
      the population is closed, not a sample.

      This CONFIRMS the RM-294 LANE 7 claim that the prior hand-off carried as
      "Unmeasured, NOT started" - and it extends it: the row named only
      agents/agent3_testing/suite, and tools/tests is a second one it missed.
      Both being GREEN is what makes this cheap: wiring them in costs ~55s of
      runner time and cannot go red on arrival. **Do NOT assume green means
      meaningful** - a suite nobody runs can also be a suite that asserts
      nothing. Before wiring, sample it for the INDEX_testing_traps classes
      (empty-enumeration always-green, mocks of themselves) and say what you
      found; a green orphan wired in without that check just buys CI minutes.

Context: moon_sync_inbox/ (read RC's own; newest first - newest overall is
      2026-09-11-0030-from-LW, already read, no reply owed). Nothing was armed
      last session and nothing left the tree beyond the ordinary push. The
      pinned pair is ops/loop/slots.py + ops/loop/winmutex.py, pinned by
      SHARED_SHA256 at tests/test_loop_concurrency.py:474, parametrised at
      :542; the fifth sibling-name escape sits INSIDE that block at :492 as a
      two-word name split across a comment line wrap, shipped as a known named
      VISIBLE exception at tools/sibling_name_sweep.py:112.

      CITATION WARNING - RM-405 (fc0058441) MOVED CODE IN _state_builder.py.
      It inserted 83 lines near the top of dashboard/_state_builder.py and 5
      into dashboard/_lcu_inprocess.py, so every offset below them shifted
      (+82 and +5 respectively; +81 for the two lines inside the edited
      docstring). Current values, all re-derived from disk this run:
        dashboard/_state_builder.py:81   RM-405 rationale comment block starts
        dashboard/_state_builder.py:103  _CALLER_DEGRADE_LOG_THROTTLE_S = 60.0
        dashboard/_state_builder.py:108  def _log_inprocess_caller_degrade
        dashboard/_state_builder.py:155  def _read_lcu_snapshot
        dashboard/_state_builder.py:163  the "Landed DARK" docstring line
        dashboard/_state_builder.py:173  the RC_LCU_INPROCESS gate
        dashboard/_state_builder.py:177  the now-speaking except branch
      WAKEUP_NOTES.md:144 (the 2026-09-11a entry) still reads
      _state_builder.py:92. That was TRUE AT ITS OWN WRAP and is deliberately
      left as the historical record - do NOT treat it as a live citation and
      do NOT "fix" a dated wakeup entry. The prior version of THIS file
      carried :92 and :96 for the same reason; they are gone from here now.

      Cadence fact, already measured - do NOT re-derive it from a comment:
      build_state() runs at RC_STATE_CADENCE_SEC, default 0.5s, floor 0.1
      (dashboard/routes_state.py:167-178), about 2 Hz. The "1.0s TTL" prose is
      STALE and a build agent already inherited that wrong number once. It
      lives at routes_state.py:211 (the _state_payload_cached docstring) and
      again at :383 - NOT at :210, which is the bare `def` line and is the
      offset two older records quote. The L3 path is also DARK today -
      _state_builder.py:173 gates it on RC_LCU_INPROCESS, unset.

      WRAP-GATE WARNING, measured at the RM-405 wrap: tools/stop_claim_gate.py
      blocked that wrap TWICE, on three headline numbers that were all TRUE.
      Every one had been measured by a SUBAGENT, never by the main thread, so
      the gate read them as unbacked - correctly. A verifier's CONFIRM grades
      the SLICE; it is still not the main thread's own evidence. Re-run in the
      MAIN thread whatever you intend to PRINT, or attribute it to the agent
      that ran it. One figure (a bespoke md-guard selection, "1782 passed") was
      RETRACTED rather than re-run because the selection could not be
      reproduced from its own prose; CI green at the final HEAD was stronger
      and was verified directly. Memory:
      reference_stop_gate_a_subagent_measurement_is_not_yours.

      Live state at wrap: pid 23096 alive, mode=client, last_reload_ok=true.
      Probe ops/runtime/health.json and https://127.0.0.1:8888/api/state
      before assuming anything.

Acceptance: either both trees hash equal after a JOINT re-pin (both hashing
      equal IS the acceptance, not a note claiming it), or a named RM row
      shipped with a red-before-green guard whose ANTI-VACUITY is PROVEN by
      stubbing the parser/extractor/log-call empty and watching arms fail -
      not merely asserted - and CI green on the FINAL pushed HEAD. Note that
      a `cancelled` CI run is SUPERSESSION by the next push, not failure;
      only the final HEAD's run is authoritative.

Do NOT redo:
  - RM-405. SHIPPED 2026-09-11 as fc0058441 (LEDGER 1391). The CALLER seam of
    the in-process L3 read now logs its fault, throttled, with no str(exc).
  - RM-403, RM-404, RM-312. All SHIPPED 2026-09-11 (LEDGER 1388/1389/1390).
  - RM-387 and RM-388. SHIPPED 2026-09-08 (LEDGER 1368/1369). The 09-10e
    hand-off named them as its fallback and they had already shipped; ROADMAP
    is now corrected and tests/test_roadmap_backlog_disposition_drift.py
    guards the class.
  - Widening that guard's binding rule. A disposition binds to the NEAREST
    PRECEDING id only. RM-281/RM-283 and RM-204 are pinned controls; a first
    parser called RM-204 a fifth drift row and was NARROWED, never allowlisted.
  - Hand-fixing caret escaping in a printed schtasks block. RM-404's answer is
    Register-ScheduledTask, which takes exe and args as SEPARATE parameters.
    tools/liveclient_relay.py:14 measured FINE and must stay untouched.
  - A general bare-`except Exception` sweep. RM-312's fence still stands even
    though RM-405 closed the one site it had named as the direct caller. The
    13 remaining sites in dashboard/_state_builder.py were CENSUSED this run
    and are real (:393 :509 :542 :590 :607 :621 :645 :667 :725 :727 :745 :768
    :834, each `except Exception:  # noqa: BLE001` with a silent default) -
    but a census is not a mandate, and each one needs its own argument for why
    a log line there is better than silence. Several are hot per-tick paths.
  - Acting on RM-312's `tools/lcu_agent.py:1612` sibling entry as written. Its
    PREMISE is wrong, not just its offset: that file has not moved, :1612 is
    `state = capture_state()` inside _state_push_loop, and lcu_agent.py does
    not import champ_select_shape at all any more (RC2 L3 moved the shaping to
    lcu/snapshot_shape.py). Re-measure from scratch or drop it.
  - Arming any responder. Nobody's is armed - LW's is built and deliberately
    disarmed (D5), RC's RC-InboxResponder is Ready/disarmed. Arming is an
    operator act, and an unattended loop must never arm another repo.
  - Replying to the LW notes. All read; none requests a reply.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
      git log + ROADMAP.md.
