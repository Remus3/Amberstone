NEXT SESSION
------------
Task: Continue the fleet TOOLING-TIER lane. RC has attacked pin v1.3 (5 FATAL)
and is waiting on the contract owner's response. Next unit is whichever lands
first: (a) respond to LW's v1.4 or their defence of v1.3, (b) answer the open
fleet question RC put on the table - whether comparability across trees is
reachable at all, or whether every tree should publish raw per-event rows and
let each reader score them (RC has already shipped all 198 of its rows, so it
is testable), or (c) if the fleet lane is quiet, pick up RM-422.

Context to probe first:
  - moon_sync_inbox/ - read newest first, then `python tools/rc_facts.py --mark-inbox-seen`
  - docs/REFUTATION_COST_MEASUREMENT_2026-09-12.md - RC's measurement, now carrying
    a correction and a section 9
  - docs/REFUTATION_GATE_BACKTEST_2026-09-12.md - why the re-grounding gate died
  - docs/_rescore/ - 198 per-event rows, tally.py, adjudication, band sweep,
    v13 attack files. tally.py and band_recompute.py are runnable.
  - docs/LEDGER.md entry 1413, WAKEUP_NOTES 2026-09-12d

Acceptance: any number RC publishes to the fleet carries its aggregation rule and
its individuation convention named, and any claim about another tree is verified
against that tree's own note rather than relayed.

Do NOT redo:
  - Do NOT re-propose the pre-dispatch re-grounding gate. Built, back-tested,
    measured-refuted. 90 pct refusal, 0 genuinely stale.
  - Do NOT re-score RC's corpus against v1.3. Deliberately not done - the contract
    is still under attack and its owner asked that it be attacked first.
  - Do NOT quote RC's bands. Withdrawn: all four intervals are non-monotonic.
    Only fine-grain figures are quotable, with the rule named.
  - Do NOT "repair" the nine baselined citations in BACKLOG.md. They are in
    _KNOWN_BROKEN with written reasons; a repair was tried and reverted.
  - Do NOT re-plumb tools/live_write_tracer.py for pathlib - io.open is already
    patched at :489-495. Confirm with a mutation arm instead.

Two operator steers drained 2026-09-12, still open:
  1. The live-write row gets NO separate recovery pass. Fold a regeneration
     assertion into the SAME row: call tft.placement_aggregator.update_heatmap()
     once, then assert the live heatmap EQUALS a freshly rebuilt one. Make it
     anti-vacuous - seed a rating with tft_unit_positions in a tmp ratings dir as
     a positive control so it cannot pass on two empties.
  2. LW retracted its tracer instrument (patched builtins.open/os.replace/
     os.rename, missed pathlib), so every figure LW put on the channel about RC
     is a FLOOR. RC is unaffected. Worth lifting is LW's IDEA set, not code:
     negative arms (a read is not reported, a write outside the watched root is
     not reported) and an arm asserting its own known limit.

Open and untouched: RM-422 (core.longpaths unset, fresh clone fails partway on
long docs/_archive paths), RM-423 (OPERATOR-GATED, no default recommended).
RC's 18 files in the machine-scope scratch surface outside the repo root are
untouched - removal is a halt-and-ping act.

Standing boundary: halt and ping before any byte leaves the tree, EXCEPT the
sync inboxes, which are pre-authorised for writes (operator 2026-09-12).

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
