NEXT SESSION
------------
Task: Finish the pre-public-flip transition for Remus3/Amberstone. Four audit
  blockers gate it and none are resolved: (1) secrets in pushed history -
  config/vision_token.txt was tracked from the initial commit until dcd965f2d,
  and five lane branch tips plus the backup-pre-scrub tag still carry the blob;
  (2) 51 MB of verbatim scraped aggregator J HTML under data/meta_build/, in
  history since 2026-05-03; (3) Overlay App E augment data that Share/LICENSE.md:84-86
  already excludes on purpose but the main repo still ships; (4) a THREE-WAY
  licence contradiction - the new Apache-2.0 LICENSE vs Share/LICENSE.md's
  no-redistribution clause vs README.md:227 "all rights reserved".
  Operator has ruled: Share/ is REMOVED before the flip (scope B), and the
  review gist is retired - its README already carries the public notice and the
  repo link, done 2026-09-07.
Context:
  - Share/ removal is 548 tracked files / 20.5 MB and touches 46 test modules
    (31 under tests/, 15 under agents/daemon_slayer/tests/), plus
    tools/ds_share_sync.py, .githooks/pre-commit:68, .githooks/post-commit,
    tools/gist_share_sync.py, tests/test_gist_share_sync.py, and the ci.yml
    step "DS Share package in sync". LIVE ENGINE CODE DOES NOT DEPEND ON IT -
    the only non-test reference is a docstring at
    agents/daemon_slayer/data_loader.py:52. docs/HEXCORE_offline.html needs NO
    change; its three "gist" hits are substrings of "registry".
  - docs/PRE_RELEASE_NAME_SCRUB.md is the standing prerequisite and it
    UNDERSTATES itself on both halves: its "already scrubbed from content" claim
    is false (one survives at tools/headless-upgrade.md:142, reintroduced after
    the scrub) and "5 historical commit messages" is actually 9. The name is
    also in the BLOB CONTENT of 19 commits, which a message-only rewrite misses.
  - CLAUDE.md:310 is FALSE: it says the 101.qq raw payload is gitignored;
    git check-ignore disagrees and the capture is tracked.
Acceptance: Share/ gone with all 46 test modules green, the licence statements
  reconciled to one, the scrub plan's own status lines corrected to measured
  values, and a written go/no-go on the flip - NOT the flip itself.
Do NOT redo:
  - CodSpeed is deleted deliberately. docs/OPERATIONS.md "Why CodSpeed was
    dropped" has the measurement. Do not re-add it as a minute-saver reversal.
  - winmutex.py 0b112a4f and slots.py 629c3d51 are pinned and agree across RC,
    LW and RSC. The hold() leak is FIXED; its neutralising write is IN PLACE and
    deliberately not tmp+os.replace - applying the atomic-write hard rule there
    restores the leak.
  - CI is green (31724 passed). The vision-token red was a missing CI value, not
    a defect.
  - Caveman ultra is operator-settled, not a live dissent.
  - The verbatim drop was PULLED from all four sibling inboxes - 19 of 48 files
    carried the operator account name. A redacted re-drop needs paths
    parameterised and a sweep BEFORE it leaves.
Open cross-repo: the reserved-slot round (one guaranteed lane per repo, width 7)
  is blocked on agreeing repo keys rc/lw/rsc/cs/ll and on CS+LL saying whether
  they want a slot - neither carries the governor yet. Charter v1-v4 is in
  docs/CROSS_REPO_CONVERGENCE_CHARTER.md. Unread: three CS notes and one RSC
  note landed at wrap.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log, then read moon_sync_inbox/ INCLUDING subdirectories.
