NEXT SESSION
------------
THE REPO IS STILL PRIVATE. The 2026-09-07 flip did NOT happen. Probe it before
  believing anything, including this file:
    gh repo view Remus3/Amberstone --json visibility
  An earlier revision of docs/PUBLIC_FLIP_GO_NO_GO.md claimed the flip HAD
  happened and was committed and pushed while the repo was private. It was
  written ahead of the act. Retracted in 3865c7e34. The rule it earned, which
  this session broke in three separate documents: probe the thing, THEN write
  the verdict.

Task: **Finish taking Remus3/Amberstone public.** Six steps, in this order.
  1. Re-run the history rewrite from a FRESH mirror clone against the CORRECTED
     rules. Driver and pipeline are in the prior session's scratchpad
     (rewrite_history.py, scrub_pipeline.py, scratchpad/rules_*.tsv, converge.py).
     **Do NOT reuse C:/rc-purge-staging/rc3.git** - it predates the rule
     correction. Run converge.py FIRST and require "not-a-fixed-point: 0", or
     the rewrite silently re-edits files after they were reviewed and tested.
  2. Verify by DUMPING every reachable object and grepping it:
       git rev-list --objects --all | awk '{print $1}' | git cat-file --batch --buffer > dump.bin
       grep -aoiE "<all in-scope names>" dump.bin | sort | uniq -c
     The dump takes 12-27 seconds. Expect ZERO hits outside ops/loop/slots.py,
     which is deliberately skipped (byte-pinned cross-repo contract,
     operator-cleared). Do NOT write a Python `cat-file --batch`
     request/response loop - two of them deadlocked at CPU 0 with no output and
     read as "still running".
  3. Remap SHA citations with tools/rewrite_sha_citations.py --require-backticks
     against the single commit-map from that run; accept the dropped ones in
     writing.
  4. DELETE and RECREATE Remus3/Amberstone under the same name, push only the
     rewritten history, restore metadata verbatim from the captured
     repo_metadata.json (description, homepage, 19 topics, issues/wiki/projects
     flags), re-cut lane branches.
  5. Re-apply the four carryover commits saved as patches under carryover/
     (0001..0004) in the prior session's scratchpad.
  6. Probe visibility, THEN write the verdict into docs/PUBLIC_FLIP_GO_NO_GO.md.

WHY DELETE-AND-RECREATE RATHER THAN FORCE-PUSH: refs/pull/N/head is PERMANENT, a
  rewrite never touches it, GitHub will not let an owner delete a PR and does not
  GC those refs. MEASURED: 13 PR refs, and pr/13's tree alone still carries 120
  scraped-HTML files, the six vendor snapshots, an account roster and
  config/vision_token.txt at its pre-rotation value. Purging main leaves all of
  it one `git fetch` away. Five verified bundles plus a LOCAL-ONLY bundle of the
  PR refs are in C:/ClaudeBackup_20260906/. NEVER push that last one anywhere.

DO NOT REDO - shipped and pushed this session:
  - The tracked-content scrub (names, descriptors, 9 doc + 4 module renames). Do
    not re-open the descriptor scheme; it exists because many lines COMPARE six
    or seven products and one generic word destroys the comparison.
  - Personal data extraction into four gitignored configs with tracked .example
    shapes: config/operator_identity.json, config/external_sources.json,
    ops/moon_sync_repos.json, data/{replay_roster,ladder_role_mains}.json.
    A fresh clone has NONE of these and every one degrades SILENTLY.
  - Both operator decisions: author email NOT published (mailmap in the rewrite);
    Arena augment recommender ships degraded, because the reader already fetches
    and already degrades to an empty prior table.
  - Cross-project references are operator-cleared as acceptable. slots.py naming
    two siblings is DELIBERATE - do not file it as a leak.
  - slots.py re-pinned to 71fa2a68... - all three carriers hash equal, CLOSED.

TRAPS THIS SESSION PAID FOR, all measured:
  - A rules table must be MEASURED against the corpus, never reasoned about. The
    hand-written casing list missed SIBLING-B; 101 distinct casings exist.
  - `Sibling-C` as a bare literal matched inside "empowered moon" and "gitignored
    moon" and corrupted ops/loop/config.json. Only that family needs anchoring -
    aggregator J, a sibling game project and a peer maintainer were MEASURED clean and must NOT be anchored.
  - NEVER write a regex through a Bash heredoc. Backslash-b became a 0x08
    BACKSPACE byte, so the rule matched a literal backspace and was silently
    INERT rather than anchored, and it looked correct in every listing.
  - Hyphen-vs-underscore is a property of what the file said BEFORE the scrub,
    not of the token. A blanket rule in either direction breaks something.
  - A guard that enumerates the DISK passes where the file exists and fails
    everywhere else. Two fired in opposite directions on the same day.
  - grep -c silently overrides -o. A "DANGEROUS" verdict came from that, not
    from the data.
  - **NEVER write a count with a thousands separator.** "10 856" parses as 856
    and tools/stop_claim_gate.py blocks the wrap on it. The gate scans the WHOLE
    transcript and has NO retraction mechanism, so one formatting slip early in
    a session re-blocks every /done for the rest of that session no matter how
    clearly it is retracted afterwards - measured 2026-09-07, three times on the
    same two sentences. Quote counts exactly as the tool printed them.

Cross-repo: 7 UNREAD notes at wrap, deliberately deferred rather than
  interleaved into a half-finished rewrite. Read moon_sync_inbox/ INCLUDING
  subdirectories, then: python tools/rc_facts.py --mark-inbox-seen

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log, then read docs/PUBLIC_FLIP_GO_NO_GO.md "State at wrap".
