NEXT SESSION - BUILD THE RESPONDER RUNNER FROM THE SPEC. SUBAGENT-FIRST.
------------------------------------------------------------------------
Model: operator's choice (Fable 5.1 or Opus 5). Shape: orchestrated, worktree
slices, TDD, verifier gate before every merge - the CLAUDE.md Session Default.
Main thread reads THIS file, the spec's sections 0 / 2 / 12 / 14, and agent
roll-ups. Everything else (notes, module surfaces, probes) goes to agents.
Operator feedback from the planning session: be quiet on the token output.

THE SPEC: docs/RESPONDER_RUNNER_SPEC.md (133 KB, 16 sections + open risks).
Every fork is marked DECIDED with its reason. Do not re-decide; if a DECIDED
item proves wrong when built, record the measurement in the spec and move on.

CONFIDENCE OF THE SPEC, STATED SO YOU DO NOT OVER-TRUST IT
  Produced by a 16-agent workflow, then THREE verifier passes (16 -> 9 -> 3
  must-fix). The last 3 + 13 fixes were applied and grep-presence-checked;
  there was NO fourth adversarial pass. So: re-refute each section AS YOU
  BUILD IT - the failing arm you write first is that refutation.

BUILD ORDER = spec section 14, six worktree-isolable slices on disjoint files:
  S0 tools/inbox_responder_procs.py      (the ONLY literal subprocess call +
                                          taskkill tree-kill; append it to
                                          SCHEDULED_SPAWNERS in
                                          tests/test_no_console_flash_scheduled_tools.py)
  S1 tools/inbox_responder_prompt.py     (SYSTEM_PROMPT, PROPOSAL_SCHEMA, envelope)
  S2 tools/inbox_responder_exec.py       (harden, precheck, scrub, assemble, filter)
  S2b tools/inbox_responder_export.py    (git archive origin/main -> tracked-only cwd)
  S3 tools/inbox_responder_spawn.py      (argv, child_env, real_spawner, parse_result)
  S4 tools/inbox_responder_runner.py     (run_once, _finish, main, gates, metrics,
                                          invocation line, agreement, delivery)
  S5 ops/install_RC_InboxResponder.ps1 + tests/test_inbox_responder_task.py
     + docs/OPERATIONS.md row + ops/moon_sync_repos.example.json "participants"
  Tests per slice per spec section 1 (eight new test files) plus
  tests/test_inbox_responder_mutants.py (the positive control: every gate's
  named mutant must redden its arm, on a TEMP COPY of the module).
  Verification tier: Tier-1 per slice (py_compile + that module's tests);
  the merge runs `pytest tests` once from the repo root (never `pytest .`).

NON-NEGOTIABLES THE SPEC ALREADY ENCODES (check them off at merge)
  - Every gate is ONE tagged `# GATE:<tag>` call site in run_once; the 26-tag
    census list in section 12 equals the tags declared in section 2.
  - Every arm DRIVES run_once with an injected stub spawner and asserts the
    outcome (termination, files in the target dir, metrics row, START/END
    lines, answered record). No predicate-only tests.
  - No test writes under ops/runtime or into any sibling inbox: log_root and
    every path are injected; an autouse arm asserts the live hook log, live
    metrics, live answered record, held dir and ALL FOUR sibling inbox dirs
    are byte-unchanged across the module.
  - No test reaches real_spawner unless RC_RESPONDER_REAL_SPAWN=1 is set; CI
    never sets it. The dry cycle (spec section 13) is the operator-directed
    interactive run that does.
  - The session gets Read/Glob/Grep only, cwd = the origin/main export, no
    --bare, ANTHROPIC_API_KEY popped, note body + filename on stdin.
  - M1 never bare; LATENCY-ONLY and A5 rows cannot be read as each other.
  - Registration lands DISARMED. Arming = the operator writes
    ops/runtime/inbox_responder_agreement.json (counterparty RSC, the note it
    rests on, window, hop_budget, grammar, expiry). STOP flag wins always.

THEN, AND ONLY THEN: RC's three conditions to RSC
  (1) the runner exists and CALLS the validator, with cycle-driving arms
  (2) the spawn is read-only and a dry cycle has run end to end on a stub
      (spec section 13: interactive --dry-cycle, then the task-fired flag run)
  (3) the invocation log records the cycles that did NOTHING
  When all three are MEASURED (not written), send ONE note to RSC per spec
  section 16: measurements, the arm table size, the mutant score, the dry
  cycle's START/END pair - and DISCLOSE the two vocabulary extensions
  (`runner-failed`, `attempt-cap` as HOLD). Until then RC's silence reads as
  unreadiness. Do not send progress notes.

CONFIG THE BUILD NEEDS ON THIS HOST (gitignored, per-host; example tracked)
  ops/moon_sync_repos.json gains "participants": {"RSC": "<path>", ...}.
  The poller reads only "repos" so the key is backward compatible. Sibling
  names and paths never enter tracked files - codes only.

STANDING TRAPS (all measured, all in the spec, repeated here because they
each cost someone a night)
  - Spawn claude.exe BY PATH; bare "claude" raises FileNotFoundError.
  - --bare kills the Max login (API-key-only auth).
  - A scheduled task inherits MACHINE-scope env only: pop the key in code.
  - subprocess timeout kills the direct child only: taskkill /F /T, never
    Stop-Process.
  - Filename stamps run AHEAD of arrival; order by mtime on RC's disk.
  - A subprocess-spawned tool takes the module default path: assert the LIVE
    file is byte-unchanged, do not maintain a redirect list.
  - A fixture sized from the constant under test is an amplifier: literals.
  - A failed spawn recorded as `exhausted` publishes the reassuring result
    every cycle: spawn-failed and runner-failed are their own values.
  - Whole-token sweeps are contiguity claims (LL 2035): plant a SPLIT
    positive control before calling any scrub clean.

ALSO OPEN, NOT THIS SESSION'S SCOPE (operator-gated, from LW's 1813 audit)
  6 post-recreate commits with the personal email and 5 with Claude as author
  or co-author are in the public history; the checkout's user.email is now
  the noreply address (repo-local) so no eighth lands. A rewrite is the
  operator's call. RC's own outbound ratio (sender-side record of notes sent)
  is unmeasured - ten seconds, per CS.
