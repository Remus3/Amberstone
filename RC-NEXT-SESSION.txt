AMBERSTONE (RC) - NEXT SESSION CONTINUATION
-------------------------------------------

ONE FILE. Repo root. OVERWRITTEN every /done - never appended, never
dated-suffixed, never a second copy. The Desktop shortcut RC-NEXT-SESSION.lnk
points here. This file is TRACKED, so each hand-off is versioned and diffable.
Written 2026-09-21 (session 2026-09-21a).


NEXT SESSION
------------

Continue work on Amberstone, the live League / TFT coaching dashboard at
C:\Riot Commander. 1-PC on Legion (ADR-011). The repo is PUBLIC.

Read first, in order: CLAUDE.md (### Settled is binding), MEMORY.md plus the
INDEX_* files (INDEX_ds.md for this task), WAKEUP_NOTES.md (last 3 sessions),
docs/LEDGER.md (this session is 1453 and 1454), ROADMAP.md NOW,
git log --oneline -20 (trust it over any list here), moon_sync_inbox/.
Then: python tools/perseus_recall.py "<the task in your own words>".


FIRST: CI
---------

77dff441a fixed a CI collect error introduced by d1d4bb089 (the new
empty_parameter_set_mark = fail_at_collect met an empty sibling-config
parametrize on CI). Confirm the push `ci` run for 77dff441a (or later) is
GREEN before building anything. If red, fix forward first; collect with the
sibling config ABSENT (memory reference_collect_count_is_a_host_fact).


TASK
----

RM-480 FOLLOW-UP: decide and, if justified, execute the DEFAULT-FLIP of
`apply_ability_base_overrides` so the 1.283.0 ratio + base overrides actually
reach live DS scoring. Today the flag is DEFAULT-OFF (abilities.py ~:1289) and
parsed only by /ability-dps, /rank-mage, /burst, /rank-assassin - NOT
/rank-tank, and Poppy / Thresh / ChoGath are tanks. So RM-480 shipped the
corrected numbers but NO default output moves.

Scope to measure before flipping (adjudicate, do not assume):
- Every consumer that loads abilities with the flag OFF (ability_dps.py
  ~:1158, burst.py ~:658, ability_hps.py ~:848, self_shred.py ~:324,
  data_loader.py ~:527) and whether a flip must reach them or the /rank-tank
  transport (a DS route seam has THREE gates - flag, transport, parse; memory
  reference_ds_route_seam_transport_vs_flag).
- The six pre-existing base overrides ride the same flag. Naafiri R's entry is
  itself STALE (wiki now 125..275, spell renamed) - fix or drop it BEFORE any
  flip, or the flip ships a wrong number.
- Build-order precompute tables would change on a flip: regenerate with
  --champions all, diff the consumer artifact, and state which champions move
  (memory feedback_verified_claim_vs_measured_downstream).
- A default flip weakens tests that assumed OFF (memory
  feedback_default_flip_weakens_tests).
Runner-up if the flip is refuted: RM-479 (augment_stacks on /stats).


CONTEXT
-------

- ENGINE 1.283.0 / patch 16.18.1 live on :8860 (verified this session). Verify,
  do not quote: data/daemon_slayer/current.txt + agents/daemon_slayer/__init__.py
  + /health.
- agents/daemon_slayer/_ability_base_overrides.py: AbilityBaseOverride.field
  (base or one of 13 ratio keys), all-or-nothing per form.
- tools/ds_wiki_staleness_check.py --annotate-only; ability_staleness.json rows
  carry resolved_by_override; _override_resolved = 23; stale_champions stays 112
  ON PURPOSE (core/daemon_slayer_client.py ~:2302 derives ABSENT/STALE/CURRENT).
- Still-stale, named not fixed: Qiyana Q form 1, LeBlanc R other blocks + W
  (and its override hits block 3 while the engine reads block 0), Kennen R
  cooldown (wiki value is a parse error), 118 other engine-stale ratio rows
  (Belveth R and Kalista E look like wiki parse errors).
- ROADMAP.md is ~92 percent of its 80 KB budget; relocate to
  docs/ROADMAP_HISTORY.md verbatim FIRST if new rows push it over.
- Inbox: SS 1900 and SS 1930 arrived after the wrap and are UNREAD. SS 1930
  answers RC's 1030 note (a subscript is SAFE; the leak axis is the attribute;
  SS's own empty-parametrize route is OPEN). Read, reply if owed (pre-authorised
  inbox delivery, re-hash every destination), then --mark-inbox-seen. RC's 1030
  note was delivered to MAIN via the path in MAIN's README, which is not in
  ops/moon_sync_repos.json - consider recording it there.


ACCEPTANCE
----------

1. A written go / no-go on the default flip with the measured blast radius.
2. If GO: Naafiri R corrected first; flip with a failing test first; every
   consumer that must see it proven to see it (transport + parse, not just the
   flag); precompute regenerated and the moved champions named; ENGINE bump,
   full dual suite from the repo root, :8860 bounced, verifier gate.
3. If NO-GO: the reason recorded in ROADMAP and LEDGER; move to RM-479.
4. drift_guard clean; LEDGER entry; CI green.


DO NOT REDO
-----------

- RM-480 itself (49c665b5c, LEDGER 1453): the schema lift, the 18 entries, the
  detector annotations. Shipped and live.
- The breaker boundary flake (19ede4154), the environ-leak asserts + guard and
  the pytest.ini empty-parameter mark (d1d4bb089), the CI collect fix
  (77dff441a) - LEDGER 1454.
- The lol-scouting-replay-kit research: REFERENCE-ONLY, closed (LEDGER 1453).
- Everything in LEDGER 1445-1452. Fiddlesticks Q is note only; ability haste is
  measured INERT; the conditional-target-state arc is operator-CLOSED.
- No task chips, ever: do it now, ROADMAP it, or carry it here.


HARD BOUNDARY - BINDS EVERY SESSION, ATTENDED INCLUDED
------------------------------------------------------

HALT AND PING THE OPERATOR BEFORE ANY BYTE LEAVES THE TREE: any write, delete
or lock outside the repository root; any edit to ops/loop/slots.py,
ops/loop/winmutex.py or docs/CHANNEL.md; any PUSH whose DIFF touches those or
trips the sibling-name sweep. NO TIMEOUT - halt and wait. ONE narrow carve-out:
delivering a channel REPLY into a sibling's moon_sync_inbox/ is pre-authorised;
deliver, then re-hash every destination and publish the reached-count.
Never put a sibling's real name or path in a tracked file - codes only.
