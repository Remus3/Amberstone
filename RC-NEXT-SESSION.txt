NEXT SESSION
------------
Task: RM-397 - narrow `_QUOTED` in tools/stop_claim_gate.py so a possessive or
contraction cannot open a quoted span. Two ordinary prose apostrophes currently
pair as delimiters and the span between them is deleted before any check runs.

SHAPE (standing, operator 2026-09-09): SUBAGENT-FIRST, ALWAYS. The main window is
the operator's surface - it holds the plan, the merge, the gate and the report,
never the doing. Decompose, dispatch disjoint slices, gate with an independent
read-only pass, report a roll-up.

Context:
  - tools/stop_claim_gate.py:184 `_QUOTED`, :212-214 `strip_prose_noise`,
    :257 `collect_evidence` (assistant text ONLY - this is what bounds the defect)
  - tests/test_stop_claim_gate.py - the real-transcript corpus it anchors on
  - Transcripts: C:\Users\Administrator\.claude\projects\C--Riot-Commander\*.jsonl (90 files)
  - Rows: BACKLOG.md RM-397; docs/LEDGER.md 1379; memory
    reference_stop_claim_gate_apostrophe_over_strip

Acceptance:
  - A possessive cannot open a span; genuine single-quoted spans stay suppressed.
  - Measured against all 90 transcripts for NEW false positives before arming.
    THE DIRECTION IS THE RISK: every prior repair to this gate (LEDGER 1154 /
    1156 / 1175 / 1178) NARROWED it to kill a false positive; this one WIDENS
    what it examines.
  - tests/test_stop_claim_gate.py green, docs-guard selection green.

Do NOT redo:
  - RM-392 / RM-393 / RM-394 / RM-395 all SHIPPED 2026-09-09 (LEDGER 1374-1378).
  - ADR-015 records the repo-root enumeration decision (LEDGER 1377).
  - RM-396 is REFUTED - do NOT re-pitch CLAIM_COUNT suppression, a retraction
    token, or per-turn scope. Backtick a refuted figure instead.
  - The responder RUNNER is BUILT: RM-384/385/386 all shipped 2026-09-08.
    The headless-loop program is TESTS, FIXES AND HARDENING plus arming.

Also pending:
  - Read C:\Riot Commander\moon_sync_inbox\ for RSC's reply to RC's two notes of
    2026-09-09 (directive relay + correction). RC asked three questions; the
    live one is whether the operator-ping boundary is ARMING, not building.
  - MEMORY.md is 20 KB against a 24.4 KB read limit and the harness wants it
    under 17.1. Its own footer requires re-deriving reachability over every
    non-exempt memory before a consolidation may be called safe. Own row.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
