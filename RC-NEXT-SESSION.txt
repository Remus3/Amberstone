NEXT SESSION
------------
Task: RM-387 (body in BACKLOG.md "Reliability / hardening") - the reply body
      shows the sender their own filename clipped to 80 chars, because
      `result.note` is the `safe_name` projection that satisfies `ROW_NOTE_RE`
      and `assemble_body(note_filename=result.note)` reuses it. Alternative if
      you want the smaller one: RM-388 (responder_metrics.jsonl is never
      trimmed while its sibling invocation log is).
Context: tools/inbox_responder_runner.py (ROW_NOTE_RE, safe_name, the
      `result.note` assignment near the note-shape gate, assemble_body's call
      site); docs/RESPONDER_RUNNER_SPEC.md sections 2 and 8 plus the three
      "Build measurements" sections; docs/LEDGER.md 1365/1366/1367. Probe live
      state with `curl -k https://127.0.0.1:8888/api/state` and
      ops/runtime/health.json. Read RC's moon_sync_inbox - two RSC notes are
      pending and unanswered (2026-09-08 1640 and 1705).
Acceptance: per the item. For RM-387 specifically: do NOT widen ROW_NOTE_RE by
      reflex - it is a published row interface RSC may parse and it carries a
      mutant needle. Measure the row-consumer side first, then decide whether
      the row and the body should share one projection at all. Whatever you
      change, assert the SENDER-VISIBLE string in the delivered body, not the
      internal field.
Do NOT redo: RM-386 second half (the hold record, `notes_held`, the two
      fail-closed directions, the `notes-held` / `held-record-unreadable`
      vocabulary) and RM-385 (pending_notes admits by name and record;
      `note-shape:not-a-file`; the junction arm is passing and is the last
      xfail in the suite). Both are verifier-confirmed, CI-green and pushed.
      Also do not re-add the GitHub community files or re-check the README
      links - both shipped this session, community health is 100 percent, and
      private vulnerability reporting is now ENABLED on the repository.
Carry this forward: CI caught a POSIX-only bug in RM-385 that EVERY Windows
      gate missed - a directory's `st_nlink` is 2 on POSIX and 1 on Windows, so
      a branch ordered after an `st_nlink != 1` check flips per platform. The
      local suite, the local slice and the independent verifier were all green.
      Fixed in dd156b690; memory `reference_directory_st_nlink_posix_vs_windows`.
      Second trap from the same wrap: `gh run watch --exit-status` exits 0 on a
      CANCELLED run, so read `conclusion`, never the exit code.
Live state: RC-InboxResponder is ARMED (task Ready, 5-minute tick) but the
      agreement's hop_budget 1 is SPENT, so every cycle reads `budget`. A new
      agreement is an OPERATOR act. DISABLE the task before running the suite -
      it writes the live log every 5 minutes and the autouse arm guards that.
      One operator call is still open: ops/runtime/inbox_responder_answered.json
      carries one false entry (the 1456 note, e83297be2bf9, never replied to);
      the command to remove it and the reason not to are in docs/LEDGER.md 1366.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
      git log.
