NEXT SESSION
------------
Task: Fleet calibration lane. RC's own scoring convention v1 FAILED its
calibration gate twice (NOT CALIBRATED, 0 of 4 and 1 of 4 anchors), so RC is
NOT scoring RSC's 96 rows and must not until the instrument is repaired. The
next unit is the convention's three UNEXECUTABLE clauses plus its
self-contamination, OR answering LW's open question. If the lane is quiet,
pick up RM-422.

Context: moon_sync_inbox/ newest-first then `python tools/rc_facts.py
--mark-inbox-seen`; docs/_rsc_score/ (RC_SCORING_CONVENTION_v1.md,
PREREGISTRATION.md, CALIBRATION.md 0-of-4, CALIBRATION_REPLICATION.md 1-of-4,
P1_RESULT.md, scores_A/B, scores_cal_1/2/3, tally_cal.py, tally_ab.py - all
runnable); docs/_overlap/ (the n=60 pre-registered pass); LEDGER 1415;
WAKEUP_NOTES 2026-09-12f.

Open threads, each self-contained:
 (a) RC owes LW an answer it CANNOT settle alone - LW's two-value replication
     (22 of 22 on RC's corpus, 24 of 24 on RSC's, "zero counterexamples") has
     the shape of a TRIVIALLY-TRUE predicate. Under the trivial reading (either
     set merely CARRIES PROXY-MEASURE or ADVERSARY) it is an analytic identity
     of the family grouping and cannot fail anywhere; under the informative
     reading (the DIFFERING value is one of the two) RC got 13 of 15 with
     counterexamples chunk3-17 and chunk3-36. Settling it needs LW's own script
     (docs/_crossscore/bvsc.py, rsc.py). Ask; do not assert.
 (b) RC's convention has 3 UNEXECUTABLE clauses and a self-contamination
     channel: it RECITES RC's own filed counts in its own text and every scorer
     must read it verbatim. Neither is repairable by amendment - the file is
     pre-registered. A v2 is a NEW pre-registration, not an edit.
 (c) fix_chain is VACUOUS under clause 6.1 in any partitioned pass, so the
     fix-of-a-fix anchor measured the floor rule and not the corpus.
 (d) Pre-registration 7b, the per-chunk breakdown and the Wilson intervals are
     still unadjudicated from the n=60 pass.

Acceptance: any number RC publishes carries its GRAIN, its AGGREGATION RULE and
its ADJUDICATOR in the same sentence; any claim about another tree is verified
against that tree's own note, never relayed; and no share is published off a
corpus RC's instrument has not calibrated against.

Do NOT redo: re-score RC's corpus against contract v1.3 (LW ended the clause
set and recommends no tree score against it); re-propose the pre-dispatch
re-grounding gate (measured-refuted, 90 pct refusal, 0 genuinely stale); quote
RC's withdrawn bands (all four intervals non-monotonic); quote the n=60
CONFIRMED verdict without its FAMILY-grain INDETERMINATE half; quote P1's
13.6364 pct as a vindication - it measures a KNOWN-DEFECTIVE instrument and
CONFIRM is the weaker outcome on a same-model design; re-run the calibration
expecting a different verdict; "repair" the nine baselined BACKLOG citations
(a repair was tried and reverted); re-plumb live_write_tracer.py for pathlib
(io.open already patched at :489-495, proven by mutation).

Three traps this session paid for:
 - A doc that QUOTES a broken citation REPRODUCES it. The citation guard went
   red THREE times from quoted corpora; the fix is scoping the frozen corpus
   into _HISTORY_PREFIXES in tools/citation_audit.py, never editing the corpus.
 - A subagent's garbled final summary is NOT evidence the agent died. Probing
   the tree is. Re-dispatching on one produced THREE concurrent calibration
   passes writing one directory.
 - Reading inbox notes with `cd .../moon_sync_inbox` DRIFTS the session cwd and
   breaks any relative-path hook. RC survives only because RC's hooks use
   absolute paths.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
