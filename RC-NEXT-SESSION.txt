NEXT SESSION
------------
Task: Wave 8 - laned orchestrated loop. Merger + worktree slices on disjoint
      files + an independent adversarial verifier before EVERY merge. Three
      waves running, a verifier overturned or amended a producer claim in
      EVERY slice, so budget the grading pass rather than hoping to skip it.
Context: RM-475 (new, filed from wave 7's S4 verification: keep/move accounting
      counts zero-heading BLOCKS as sessions in FILE ORDER, so --keep 3 over a
      file containing one can retain only TWO real sessions - the exact
      under-enforcement class RM-276 exists to fix; NOT live today, the real
      WAKEUP carries zero such blocks, and archive safety is unaffected).
      Files: scripts/wakeup_prune.py + tests/phase7_polish/test_wakeup_prune.py.
      Cheap side item: name the xfail that flipped to xpass by running
      `pytest tests/test_inbox_responder_runner.py
      tests/test_rm450_core_residuals.py tests/test_truth_gate.py -q -rX`
      (wave 7 saw 1 xfailed / 3 xpassed against wave 6's 2 / 2; no file wave 7
      touched carries an xfail marker, so it is environment-conditional).
      Probe live first: tools/rc_facts.py, ops/runtime/health.json,
      python tools/moon_sync_poller.py --status, gh run list.
Acceptance: RM-475's own ACCEPTANCE clause, verifier-CONFIRMED before merge;
      full dual suite ONCE on the frozen merged tree. State disarm numbers as
      counts returned, never as a verdict.
Do NOT redo: RM-474 and RM-276 are SHIPPED (merges 233a3d13b and 06cde1618) -
      read the markers, do not re-investigate. The pid-bounce WATCH ITEM from
      waves 5/6/7 is EXPLAINED and CLOSED: the PRODUCTION hot-reload watcher
      writes restart_trigger.txt whenever a non-frozen .py changes, so a
      .py-bearing merge restarting RC is the path working, not a supervisor
      fault (tests/conftest.py:238-253 documents it and states the attribution
      limit). Do NOT re-open that. STILL OPEN inside it: logs/2026-09-18.log
      shows a CLUSTER of four RC starts inside three minutes at 04:36-04:38
      plus two at 05:00, which merge-driven reload does NOT explain and which
      nobody has investigated.
Operator call, not a slice: RM-239 / RM-242. The rewrite question was put to
      the operator in wave 7 and NO confirmed answer was received, so nothing
      was started and both rows stay OPEN and HELD. Do not start it unilaterally.
Also owed: the wave 7 PUSH DECISION if it is still unmade - check
      `git log @{u}..` before assuming, because wave 7's wrap pushed.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
      docs/LEDGER.md + git log.
