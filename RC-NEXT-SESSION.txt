NEXT SESSION - RC laned headless orchestrated parallel loop (continue)
--------------------------------------------------------------------
Task: Continue laned orchestrated cycles against RC's RC-local open rows until drained
      or interrupted. Same standing SHAPE (merger + worktree slices on disjoint files +
      independent adversarial verifier before every merge; producer never grades).

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES.md
      (2026-09-16b block) + docs/LEDGER.md 1419 and 1420 + git log. Probe live:
      python tools/rc_facts.py ; python tools/moon_sync_poller.py --status ;
      gh run list --branch main --limit 4 (confirm CI green on the wrap commit;
      docs-guards was red on 59b3f6253 from phantom example cites in BACKLOG RM-446,
      fixed in the wrap commit - verify, do not re-fix).

BOUNDARY (unchanged): halt and ping before any byte leaves the tree. No sibling-inbox
      writes, no arming, no edits to docs/CHANNEL.md, ops/loop/slots.py,
      ops/loop/winmutex.py. Ordinary pushes that pass suites + the pre-push sweep proceed.

CYCLE 1: read RC's own moon_sync_inbox/ (triage only, do not reply).

DRAINABLE QUEUE (RC-local, filed 2026-09-16; read each row first, recall gate first):
  RM-433 other ops/moon_sync_repos.json readers degrade in a worktree
  RM-434 inbox_responder_runner.py:2475 whole-file hash of the live hook log (flake class)
  RM-440 skip-hygiene guard accepts constant-true conditions (os.name/sys.platform, OR loophole)
  RM-441 text-mode writers of TRACKED files without newline control (CRLF on Windows)
  RM-442 data_pipeline.py downloaded_at local time labelled Z
  RM-443 17 unclassified core loaders with a cache-failure shape
  RM-444 archetype_picks transient read failure + save overwrites picks
  RM-446 RM-435 residuals (PAST_EOF tail preference, spaced dir + bare filename)
  RM-437 os.scandir-level swallow defeats the listability probe (decide: accept or fix)
  RM-432 hooks execute worktree tool copies - DESIGN question (.githooks shared); may be
         operator-facing, record rather than force
  then older open rows (RM-413, RM-415, RM-417 ...), one row per slice.
  RM-445 (DS does not model 16.18.1 Rageblade 3 -> 4; runes never refreshed) is a DS
  batch decision - Tier-2, full dual suite + DS :8860 restart if taken.

SLICE PROMPT RULES LEARNED 2026-09-16 (put in every slice prompt):
  - STEP ZERO: pwd under .claude\worktrees\, then git merge --ff-only <local main sha>
    (worktrees are cut from ORIGIN, not local main).
  - At MERGE, before push: run tests/test_*hygiene*.py, citation drift, rm-id registry,
    doc size budget, line endings on the merged tree - slices miss repo-wide guards.
  - An un-skip or platform-gated change must be exercised under the OTHER OS's semantics
    (CI is Linux; os.lstat raises UnicodeEncodeError, not OSError, on POSIX).
  - Never widen a guard's accepted vocabulary without a probe proving an always-true
    condition is still rejected.
  - No example path:line cites in BACKLOG/LEDGER prose (citation drift guard).
  - RC-PatchRefresh (Wed 11:07) rewrites tracked data; a dirty data tree mid-session is
    that task, not a slice.

HALT AND RECORD, DO NOT ATTEMPT: criterion (d) flip / CHANNEL_VERSION 2 re-pin;
  docs/BOUNCE_POLICY.md NOT-DECIDED; replies to LL/LW/RSC/CS; the two BLOCKED third-party
  skills; responder MODEL pin tools/inbox_responder_runner.py:95.

DO NOT REDO (shipped + pushed 2026-09-16, LEDGER 1419/1420): ban-arm space bypass;
  always-skipping surrogate tests; worktree hook exposure measurement + sweep DEGRADED
  fix; spec grammar drift + example list (RM-436); ungraded-guard sweep; durable-store
  digest rule in CLAUDE.md; unlistable-inbox watcher/poller + shared probe; hook-log
  teardown flake; RM-414; RM-416; item_advisor residuals; RM-435; RM-438; RM-439;
  CRLF writer fix; DDragon 16.18.1 data commit; CLAUDE.md/ROADMAP relocate-only trim.

When the drainable queue is empty emit NO_WORK. End with /done plus the next prompt.
