NEXT SESSION
------------
Task: FINISH THE LANE WIDGET GATE. The code shipped at `ed759ef4a` (LEDGER 1430)
      and is verifier-confirmed green, but its 5-PHASE UI FIXTURE AUDIT and its
      LIVE ACCEPTANCE RUN never happened - the audit agent was interrupted. Run
      both, fix every MUST-FIX in the same slice, then commit.
Context: `lane-widget/` is a standalone Electron app (not part of rc-shell).
      Launch it with
        "C:\Riot Commander\rc-shell\node_modules\electron\dist\electron.exe" "C:\Riot Commander\lane-widget"
      Renderer: `lane-widget/src/renderer/{index.html,widget.css,widget.js}`.
      Model shape: `lane-widget/src/model.js`. Window/tray: `src/main.js`.
      State file: `%APPDATA%\lane-widget\lane-widget-state.json`.
      Kill orphans with `taskkill /F /PID <pid> /T` - NEVER `Stop-Process`.
      One was left running last session carrying `--remote-debugging-port=9222`;
      check for that again and do not leave a debug port open.
Acceptance (NONE of these is currently verified - prove each on screen):
      1. two launches = ONE window, second raises the first
      2. right-click on the window = Minimize / Close to tray
      3. Close to tray HIDES and does NOT quit - only tray Exit quits
      4. tray right-click = Open / Settings / Exit, icon legible at tray size
      5. resize CLAMPS to content - no empty gutter, never exceeds work area
      6. RC's two STALE locks render as stale, never running
         (`ops/loop/control/lanes/0.lock` pid 7676 and `control/RUNNING.lock`
         pid 22888 were both DEAD when last probed - re-probe, do not inherit)
      7. zero rows renders with NO reflow when rows arrive
      8. settings persist across a tray Exit and relaunch
      Then the 5 phases: STRUCTURE / TYPOGRAPHY / HIT-TARGETS / ASCII / HIERARCHY.
      Watch two known silent-failure classes: a `var()` used but never defined
      fails SILENTLY and inherits, and an innerHTML repaint DESTROYS keyboard
      focus - this panel repaints on a 2 s poll, so check focus survives a tick.
Do NOT redo: the design, the module split, or the test suites. `npm test` is
      217 passed / 0 failed and `tests/test_lane_widget_node_suite.py` +
      `tests/test_lane_widget_script_covers_the_dir.py` are 12 passed, all
      independently verifier-confirmed on the committed tree. Do NOT re-probe
      where lane state lives - it is `<repo>/ops/loop/control/lanes/0.lock` plus
      `control/RUNNING.lock`, pid-probed; `C:\ProgramData\lw-loop\slots` is
      momentary and carries no lane field, and named mutexes cannot be
      enumerated. Both were measured and are dead ends.
Queued after the widget gate, in ROADMAP NOW:
      - MISSION CONTROL REMOVAL gap analysis. The operator wants MC and two
        sibling monitors removed now the widget exists. MC is NOT only a monitor:
        `dashboard/routes_loop_control.py:134-136` carries stop / resume /
        set_directive / fire_lane / queue_intent / steer / interrupt, bearer-authed
        on `:8895`. The widget replaces MC's VIEW, not its CONTROL PLANE. Produce
        the surface-by-surface analysis BEFORE deleting anything, and touch no
        sibling repo.
      - `node --test` silently passes on a nonexistent path (node v24.15.0,
        measured). Correct the false comment in
        `tests/test_rc_shell_node_suite_rm342.py`.
      - ASCII hygiene does not cover `.css` / `.html`. Widen via
        `tests/_repo_walk` per ADR-015; do NOT hand-roll an rglob plus skip set.
      - Then the older queue: RC's stray-work sweep + five-way reconciliation,
        the frozen supervisor walker, RM-239 / RM-242 (OPERATOR call, not yet
        confirmed - do not start), RM-475, ROADMAP-vs-LEDGER drift guard gap.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
      docs/LEDGER.md + git log; recall the widget task before touching the UI.
