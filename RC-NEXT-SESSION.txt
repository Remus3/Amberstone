deep-dive unnamed research page by page for lift: https://github.com/UrSunBro01/lol-scouting-replay-kit

AMBERSTONE (RC) - NEXT SESSION CONTINUATION
-------------------------------------------

ONE FILE. Repo root. OVERWRITTEN every /done - never appended, never
dated-suffixed, never a second copy. The Desktop shortcut RC-NEXT-SESSION.lnk
points here. This file is TRACKED, so each hand-off is versioned and diffable.
Written 2026-09-21 (session 2026-09-20c).


NEXT SESSION
------------

Continue work on Amberstone, the live League / TFT coaching dashboard at
C:\Riot Commander. 1-PC on Legion (ADR-011). The repo is PUBLIC.

Read first, in order: CLAUDE.md (### Settled is binding), MEMORY.md plus the
INDEX_* files (INDEX_ds.md for this task), WAKEUP_NOTES.md (last 3 sessions),
docs/LEDGER.md (this session is 1445 through 1452), ROADMAP.md NOW,
git log --oneline -20 (trust it over any list here), moon_sync_inbox/.
Then: python tools/perseus_recall.py "<the task in your own words>".


TASK
----

RM-480: the RM-81 RATIO-OVERRIDE SCHEMA LIFT for the 7 HIGH stale-ability
champions - Poppy Q, Qiyana Q, Thresh E, Kennen R, ChoGath E, Cassiopeia E,
LeBlanc R.

Why this one: ENGINE 1.282.0 widened the RM-81 detector to ratio-only drift
(97 -> 112 stale), so these seven are MEASURED wrong numbers flowing into live
DS scoring today, and the only blocker is a known, bounded schema gap
(_ability_base_overrides.py can express a BASE override, not a RATIO). The
runner-up, RM-479 (augment_stacks on /stats), is transport plumbing for a
route already recorded as known-unreachable in tests/test_ds_parity_map.py -
real but lower value.


CONTEXT
-------

- ENGINE 1.282.0 / patch 16.18.1. Live DS truth = data/daemon_slayer/current.txt
  + agents/daemon_slayer/__init__.py + /health on :8860. Verify, do not quote.
- The 1.282.0 integration (7fbe7aead) had its verifier gate PENDING at wrap.
  Confirm it landed on main and CI is green before building on it.
- Per-version DS changelog: agents/daemon_slayer/CHANGELOG.md (1.281.0 and
  1.282.0 entries). docs/DAEMON_SLAYER.md carries NO changelog by design.
- ROADMAP.md is now ~92 percent of its 80 KB budget (tests/test_doc_size_budget.py).
  A relocation pass to docs/ROADMAP_HISTORY.md is DUE; if this task's rows
  push it over, relocate FIRST using the verbatim method (type the block into
  ROADMAP_HISTORY.md, then use that text as the old_string that removes it).


ACCEPTANCE
----------

1. Schema lift: the override registry can express a per-rank RATIO (and keep
   BASE working); a failing test is written FIRST for at least one champion.
2. All 7 HIGH rows carry a ratio override sourced from the extract's own
   numbers, one test per champion (per-champion validation, not one generic
   shape), and the RM-81 detector no longer flags them.
3. Grep for sibling cases (other HIGH-adjacent rows, other overrides reading
   the same field) and either cover them or name why not.
4. ENGINE_VERSION bump, full dual suite from the repo root (pytest
   agents/daemon_slayer + pytest tests, never pytest .), DS :8860 restarted,
   /health shows the new version, verifier subagent gate before "done".
5. drift_guard.py <old version> clean; LEDGER entry; RM-480 flipped.


DO NOT REDO
-----------

- The 16.18.1 patch bump, the lolmath extractor fixes, Ultra Hydra 226668 ARAM
  exclusion, cherry_augments refetch, Brawl-pool canary (LEDGER 1450).
- F2 block-index ordinals, F3 Arena Stormrazor/RFC, S1-1 alias dedup, the
  RM-81 ratio-drift detector itself (LEDGER 1451).
- The rewind DB dedup/backfill - already run live and recovered (LEDGER 1449).
  The event-id gap from an earlier unrecorded cleanup is UNRECOVERABLE; do not
  chase it.
- Riot 429 reason plumbing (LEDGER 1447) and stop_claim_gate check 6 (LEDGER
  1448). Their residues are ACCEPTED and recorded in-tool.
- Fiddlesticks Q (RM-485) is NOTE ONLY - the conditional-target-state arc is
  operator-CLOSED. Ability haste is measured INERT - do not spec it again.
- No task chips, ever (operator ruling): do it now, ROADMAP it, or carry it here.


START WITH
----------

    git fetch && git log --oneline origin/main -10
    curl -s http://127.0.0.1:8860/health
    python tools/perseus_recall.py "RM-81 ratio override schema lift stale ability"
    grep -n "def \|OVERRIDES" agents/daemon_slayer/_ability_base_overrides.py

Then spec the schema lift in a Plan subagent BEFORE any code, verified against
the cited file:line ground truth.


HARD BOUNDARY - BINDS EVERY SESSION, ATTENDED INCLUDED
------------------------------------------------------

HALT AND PING THE OPERATOR BEFORE ANY BYTE LEAVES THE TREE: any write, delete
or lock outside the repository root; any edit to ops/loop/slots.py,
ops/loop/winmutex.py or docs/CHANNEL.md; any PUSH whose DIFF touches those or
trips the sibling-name sweep. NO TIMEOUT - halt and wait. ONE narrow carve-out:
delivering a channel REPLY into a sibling's moon_sync_inbox/ is pre-authorised;
deliver, then re-hash every destination and publish the reached-count.
Never put a sibling's real name or path in a tracked file - codes only.
