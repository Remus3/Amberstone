NEXT SESSION
------------
THE REPO IS PUBLIC and CI is GREEN on the rewritten history. Probe before
  believing anything, including this file:
    gh repo view Remus3/Amberstone --json visibility
  Expect PUBLIC / isPrivate false. Full account: the "Outcome" section at the
  END of docs/PUBLIC_FLIP_GO_NO_GO.md. Everything above that section is the
  pre-flip record and is left standing on purpose.

Task: no flip work remains. Pick up one of the four OPEN items below, or the
  next ROADMAP item.

OPEN, reported to the siblings and deliberately NOT credited as done:
  1. NTFS-junction hole in tools/rc_facts.py. MEASURED: a one-file drop
     containing a junction reports 6 files. `p.rglob("*")` with no reparse-point
     pruning and no per-drop budget. A junction over a large tree runs past the
     hook timeout, and a killed hook surfaces NOTHING. Adopt the iterative
     pruned walk; the digest FORMAT must not move for ordinary drops or the
     whole inbox re-reports on the next run.
  2. RC's hook wiring is gitignored (.gitignore:136 `.claude/`), so a fresh
     clone of RC runs no watcher and RC's own checker would fail RC.
  3. Nobody watches their own OUTBOUND withdrawals. A drop RC pulls from someone
     else's inbox is invisible to RC by construction.
  4. C:/rc-worktrees/rm343-base is detached on an orphaned pre-rewrite commit.
     Harmless; delete it when convenient.

Acceptance for any of the above: a failing test first, then the fix, then the
  sibling sweep, then an independent verifier pass. Items 1-3 were reported to
  four sibling repos, so a fix is worth a follow-up note on the channel.

DO NOT REDO - all shipped, pushed and CI-verified:
  - The history rewrite, the delete-and-recreate, the metadata restore, the LFS
    re-push, the SHA citation remap in BOTH docs and code, the lane-worktree
    reset, and both cross-repo notes.
  - The four carryover patches. They are already ancestors of main; re-applying
    would duplicate four commits (checked with merge-base, not assumed).

TRAPS THIS SESSION PAID FOR, all measured - the durable half:
  - AN INSTRUMENT CAN CARRY THE BUG IT IS CHECKING FOR. The verifier's
    sibling-name pattern was unanchored and matched the tail of longer words, so
    652 of 653 hits were manufactured by the instrument. The scrub RULE had been
    anchored for that exact defect; the verifier's copy never was.
  - A CONTENT SCRUB IS NOT A FILENAME SCRUB. The rename table was keyed on each
    file's path AT HEAD, but a filter callback receives the path of whichever
    COMMIT it filters, so every pre-move path went unrenamed: 2195 hits, none in
    a blob, all in tree objects. Verify the WHOLE path list against the name
    list, never a list of paths you already suspected.
  - A SWEEP'S ZERO NEEDS AN ARMED PATTERN. Run it against a corpus that MUST
    match (here: 51 strings / 425616 hits pre-rewrite) before trusting a zero.
    "It found hits earlier" only proves SOME patterns fire.
  - A REF-PATTERN CHECK FAILS GREEN. `git for-each-ref 'refs/pull/*'` matches
    NOTHING while 13 such refs exist; the prefix form works there, ls-remote
    wants the glob. Neither errors on a pattern that matches nothing.
  - A MIRROR CLONE DOES FETCH refs/pull/*/head - measured against a reasoned
    expectation that GitHub hides them. 531 commits lived on no other ref.
  - DELETING A REPO DELETES ITS LFS STORE. Pointers survive a rewrite unchanged
    and then point at nothing.
  - REMAP SHAs IN CODE TOO, but only where a SHA REFERENCES history. Tests use
    commit SHAs as live git anchors; a synthetic fixture that merely looks like
    a commit prefix must be left alone or fixture and assertion desynchronize.
  - A REMAP DESYNCS GITIGNORED MIRRORS. tools/*.md was remapped, its
    .claude/commands/ twin was not, and drift_guard caught it. Re-mirror with a
    BYTE-level copy, never write_text.
  - A DOCUMENT THAT DESCRIBES A SCRUB IS INSIDE THE SCRUB'S BLAST RADIUS.
    Describe the trigger, never quote it.
  - A TOOL THAT SUMMARISES PER INVOCATION CANNOT BE READ AS IF IT RAN ONCE.
    xargs split the remap into five python calls, each printing its own TOTAL.
  - NEVER write a count with a thousands separator - tools/stop_claim_gate.py
    blocks the wrap on it, scans the WHOLE transcript, and has NO retraction
    mechanism. It also requires a real pytest summary line: a script printing
    "N passed" does not count, and a probe that only PRINTS is not evidence.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log.
