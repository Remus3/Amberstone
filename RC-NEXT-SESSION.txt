AMBERSTONE (RC) - NEXT SESSION CONTINUATION
-------------------------------------------

ONE FILE. Repo root. OVERWRITTEN every /done - never appended, never
dated-suffixed, never a second copy. The Desktop shortcut RC-NEXT-SESSION.lnk
points here. This file is TRACKED, so each hand-off is versioned and diffable.
Written 2026-09-20.


THE PROMPT
----------

Continue work on Amberstone, the live League / TFT coaching dashboard at
C:\Riot Commander. 1-PC on Legion (ADR-011). The repo is PUBLIC.

READ THESE FIRST, IN THIS ORDER:

1. CLAUDE.md. Treat "### Settled - do not re-litigate" as binding. Each of
   those was closed with evidence; re-pitching one is the standing failure.
2. MEMORY.md plus the INDEX_* files it points at.
3. WAKEUP_NOTES.md (last 2-3 sessions at full fidelity), docs/LEDGER.md
   (newest-first, per-item), ROADMAP.md NOW section.
4. docs/ARCHITECTURE.md for the module map.
5. git log --oneline -20. Trust it over any list below.
6. moon_sync_inbox/. A note that arrived while a session was open is invisible
   to that session, so session start is the only moment this gets read.
7. python tools/perseus_recall.py "<your task in your own words>" BEFORE
   starting any non-trivial item. If a settled or ledger hit says the work is
   CLOSED, REFUTED or already shipped, stop and report that instead of
   building. Recall through that tool, never the raw MCP call.

A FRESH CLONE HAS NO GATES. core.hooksPath is LOCAL config and is not cloned,
and .mcp.json plus the Perseus vault are gitignored. First actions:

    python scripts/install_hooks.py
    perseus-vault connect --client claude-code --hooks
    python tools/perseus_sync.py --verify


STATE AS OF THIS HANDOFF
------------------------

    branch        main, clean
    last 5        17421f4a3 fix(tests): plain import for the first-party CI
                            gate module, not importorskip
                  68baddc91 test(lane-widget): pin attribution, tab growth and
                            absent-reports tolerance
                  93b5af910 feat(sweep): per-slot DECLARED narrowing for a
                            generic-word participant
                  b5d11367f test(hygiene): walk .css / .html through
                            tests/_repo_walk (ROADMAP NOW-3)
                  4b62b966c docs(tests): correct the false 'node errors on a
                            phantom path' comment (NOW-2)
    push          the main session was mid-push on another batch; confirm with
                  git log origin/main..HEAD before assuming anything is remote
    channel       docs/CHANNEL.md is still at CHANNEL_VERSION 1 with five
                  participants listed. SS has JOINED as the sixth, code SS.
                  RC voted YES to six at CHANNEL_VERSION 2. THE RE-PIN ROUND IS
                  OPEN and CS asserts RC authors the v2.
    suite counts  NOT recited here on purpose - they have gone stale twice.
                  Measure: pytest tests --collect-only -q and
                  pytest agents/daemon_slayer --collect-only -q, both from the
                  repo root, never `pytest .`


WHAT THIS SESSION DELIVERED
---------------------------

- NOW-2: corrected the false comment in tests/test_rc_shell_node_suite_rm342.py
  claiming node errors on a phantom path. It does not - `node --test` exits 0
  on a nonexistent path (measured, node v24.15.0), which is why the ESM sweep
  exists.
- NOW-3: ASCII hygiene widened to cover .css and .html, walked through
  tests/_repo_walk per ADR-015 rather than a hand-rolled rglob plus skip set.
- Sibling-name sweep: per-slot DECLARED narrowing so a participant whose code
  is a generic word does not drown the sweep in false positives.
- Lane widget: tests pinning attribution, tab growth and absent-reports
  tolerance, plus the Mission Control removal gap analysis.
- A first-party CI gate module was being imported with importorskip, which
  turns a real breakage into a silent skip. Now a plain import.
- The next-session mechanism was consolidated onto this file (see below).


THE HAND-OFF MECHANISM CHANGED THIS SESSION - READ ONCE, THEN IT IS ROUTINE
--------------------------------------------------------------------------

RC had TWO continuation artifacts: this file, and a root NEXT_SESSION_PROMPT.md
that duplicated the same idea in a second place and had gone 13 days stale. The
second one is RETIRED to docs/_archive/2026-09-07-NEXT_SESSION_PROMPT.md. Do
not recreate it and do not file its absence as a defect. There is now exactly
one hand-off file and /done overwrites it unconditionally. This matches the
convention every sibling already uses: <CODE>-NEXT-SESSION.txt in the repo root
plus a Desktop .lnk.

Several gitignored lane prompts under .claude/commands/ still told the agent to
write to the old DESKTOP path, which would have produced a second, untracked,
divergent copy. Those were corrected the same session. If you add a new lane
prompt, name the REPO-ROOT file.


WHAT TO DO NEXT
---------------

1. THE MISSION CONTROL REMOVAL CONTROL-PLANE DECISION IS STILL OPEN. The
   analysis is written and is at docs/specs/mission_control_removal_gap_analysis.md.
   Read it before touching anything. The headline finding, which is the whole
   reason this is not a delete: MC is NOT only a monitor.
   dashboard/routes_loop_control.py:134-136 carries stop / resume /
   set_directive / fire_lane / queue_intent / steer / interrupt, bearer-authed
   on :8895. The lane widget replaces MC's VIEW, not its CONTROL PLANE. The
   open question is what happens to those seven control verbs: port them,
   retire them, or keep MC headless for them. That is an operator-shaped
   decision, so bring it a recommendation with the surface-by-surface evidence,
   do not pick silently. DELETE NOTHING until it is answered, and touch no
   sibling repo.
2. THE CHANNEL v2 RE-PIN. SS is the sixth participant, RC voted YES, CS says RC
   authors the v2. docs/CHANNEL.md is BYTE-IDENTICAL-BY-CONTRACT across the
   participating repos, pinned on LF-NORMALISED bytes by CHANNEL_PIN in
   tests/test_channel_doc_pin.py. Re-pinning is now a SIX-WAY act. Never
   regenerate the digest from local disk: both trees hashing equal IS the
   acceptance, not a note claiming it. Copy a sibling's file with a BYTE-level
   copy, never write_text - it turns LF into CRLF on Windows and the pin is on
   bytes. NOTE: LL reports it has NO docs/CHANNEL.md at all (refused at its
   license gate, OPS-91), so any "byte-identical in your tree" premise is false
   for LL and the round has to account for that.
3. FOUND IN PASSING 2026-09-20, NOT FIXED, FILE A ROW:
   `python tools/sibling_name_sweep.py --tree` DIES WITH MemoryError on this
   repo. It cannot complete, so a full-tree sibling-name audit currently cannot
   be run to completion.
   ROOT CAUSE, from the code and arithmetic, not inferred: `_strip_continuations`
   (`:795-815`) and `_collapse` (`:818-838`) build a per-CHARACTER Python list -
   one 1-char str plus one DISTINCT int offset per byte - and `build_views`
   (`:840`) runs that three times per blob. That is roughly 200+ bytes of Python
   objects per input byte. `collect_tree_blobs` (`:1233`) reads WHOLE FILES
   (`target.read_bytes()`, `:1241`), and the tree holds `docs/history_notes.md`
   at 5,733,852 bytes and `docs/LEDGER.md` at 5,291,650 bytes, both plain text
   and both growing every session. Two files of that size are enough on their own.
   SEVERITY IS BOUNDED, AND THE BOUND IS THE POINT: the ARMED pre-push gate is
   NOT affected. `--pre-push` parses `diff --git` hunks (`:1205`) and carries a
   `base_line` offset, so it scans DIFF HUNKS, not whole blobs. `--scan-file` is
   also fine and was used to clear five files this session. It is the TREE AUDIT
   that is broken.
   WHY IT STILL MATTERS: the standing claim is "armed with a MEASURED escape
   rate above zero", and the five real escapes were found by tree-scope
   auditing. That measurement cannot currently be repeated.
   DO NOT "fix" it by excluding the big docs - that blinds the audit to the two
   files most likely to quote a sibling. Stream or chunk the views instead, and
   write the failing test FIRST.
   TRAP THAT HID IT, worth carrying on its own: the crash was first read as a
   PASS because the run was `sweep --tree | tail -20; echo "EXIT=$?"`, and `$?`
   after a pipe is TAIL'S status, not the script's. The background-task
   notification likewise reported exit 0 for the wrapping shell command. A
   Python traceback scrolled past inside a "clean" verdict. Never take an exit
   code through a pipe; use PIPESTATUS or do not pipe.

4. Then the older queue: RC's stray-work sweep plus the five-way
   reconciliation (RC still owes its own report), the frozen supervisor walker
   at ops/rc_supervisor.py:513 (a 1 Hz rglob over agents/ that post-filters
   agents/daemon_slayer, about 1,220 ops/s in the supervisor pid - it is a
   FROZEN file and needs an explicit operator grant, so ASK, do not edit),
   RM-475, and the ROADMAP-vs-LEDGER drift guard gap.


HARD BOUNDARY - BINDS THIS SESSION AND EVERY SESSION, ATTENDED INCLUDED
-----------------------------------------------------------------------

HALT AND PING THE OPERATOR BEFORE ANY BYTE LEAVES THE TREE. That means before
any write, delete or lock acquisition outside the repository root; before any
edit to a cross-repository byte-pinned or grammar-pinned artifact
(ops/loop/slots.py, ops/loop/winmutex.py, docs/CHANNEL.md); and before any PUSH
WHOSE DIFF touches one of those or trips the sibling-name sweep. The gate is on
DIFF CONTENT, not on destination. An ordinary push that passes the suites and
the sweep does not halt. There is NO TIMEOUT: RC halts and WAITS.

NEVER write into a sibling checkout. NEVER put a sibling's real name or local
path in a tracked file - CODES ONLY (CS, LL, LW, RSC, SS). The repo is PUBLIC.
The sweep at tools/sibling_name_sweep.py is ARMED in .githooks/pre-push, but
the honest claim is "armed with a measured escape rate above zero", never
"sibling names cannot leak". Five real escapes were found in RC's own tree and
all five had already been published.


TRAPS WORTH CARRYING
--------------------

- A filed count is a HYPOTHESIS. Every filed count that has been re-derived was
  wrong. Do not quote a doc's test count; measure it.
- Agreement between two agents is not evidence. The producer never grades its
  own work.
- rglob NEVER prunes, and post-filter walkers are a FAMILY, not an instance.
- write_text turns LF into CRLF on Windows and read_text hides it, so byte
  counts lie.
- An empty grep is a claim about your PATTERN, not about the codebase.
- Proving your change happened is not proving it matters. Diff the consumer's
  artifact.
- Never Stop-Process (hangs the MCP pipe). Use taskkill /F /PID.
- Never `pytest .` - it once deleted the live supervisor lock. Run
  `pytest tests` and `pytest agents/daemon_slayer` from the repo root.


HOUSE RULES
-----------

7-bit ASCII in every authored file: no em-dashes, no en-dashes, no smart
quotes, anywhere, ever. Files go through Read / Edit / Write / Grep / Glob and
NEVER through a Bash heredoc, sed -i or redirection - stop_claim_gate reads a
Bash-mediated write as an unbacked claim even when the change is real. Never
add a Co-Authored-By trailer and never file its absence as a defect. py_compile
before any restart. Atomic writes only. TDD: the failing test comes first.
Orchestrated, multi-agent, self-adjudicating and self-adversarial is the
DEFAULT shape, not an escalation - the main window is the OPERATOR'S surface,
so the work happens in subagents and the main thread holds the plan, the merge,
the gate and the report.

End the session with /done, which overwrites this file.
