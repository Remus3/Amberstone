NEXT SESSION
------------
Task: Close every remaining blocking item for the FIVE-WAY ARM that is RC's to close.
  Operator directive 2026-09-15. This SUPERSEDES the earlier "not buildable yet" freeze,
  which was right about the fleet halves and wrong to extend to RC's own.

Context, read first and re-derive rather than trust:
  CLAUDE.md, MEMORY.md, WAKEUP_NOTES.md (2026-09-15 block), docs/LEDGER.md entry 1417,
  docs/CHANNEL.md, and the BACKLOG.md arming-stage row under "Reliability / hardening".
  The eleven arming criteria are restated in the stage-4 prompts artifact on the operator's
  Desktop. RE-MEASURE their state; both that artifact and this prompt are hypotheses.
  Live state to probe: python tools/moon_sync_poller.py --status ;
  curl -k https://127.0.0.1:8888/api/moon-sync-status ; ops/runtime/health.json.

RC's side - buildable now, in rough value order:
  (d) THE NOTE-NAME GRAMMAR, the biggest one. It refuses 21 of 176 live notes today and every
      refusal writes a PERMANENT BOUNCE into the sender's inbox, so arming without this sprays
      bounces across four trees. Widen it to admit the no-HHMM variant while still refusing the
      code-first variant, parametrised over the table INSIDE docs/CHANNEL.md. Editing that doc
      is a CHANNEL_VERSION 2 five-way re-pin, never a unilateral edit - the doc is byte-pinned
      by sha256 and a push whose diff touches it HALTS.
  (g) Re-measure which CLI binary the S4U scheduled task actually resolves. Re-pin or fail
      loudly. It is UNVERIFIED today.
  (h) Name the model id in the arming data contract.
  (i) Draft RC's written BOUNCE-policy position. Never arm with it undecided.
  (j) Design the same-version contract field: a required, fail-closed field of every agreement
      record, checked by load_agreement.
  (f) Re-run tools/outbound_reciprocity_check.py now that live rows exist.

NOT RC's - do not do these:
  (a) the CS exit-code flip; (b) the RSC and LW per-prompt watcher work; (e) the LW
  validate-and-deliver seam. Those are the four sibling-adoption prompts already delivered to
  the inboxes; each tree ingests its own. (c) fleet view is already GREEN as of the
  2026-09-15 poller restart.

Acceptance:
  Each RC-side criterion is either CLOSED with a measurement and a guard test, or recorded as
  still-open with the specific reason and what would close it. No criterion is marked closed on
  reasoning alone. The grammar change ships with a mutation proof that it still REFUSES the
  code-first variant.

DO NOT ARM ANYTHING. "Close the blockers" is not licence to arm.
  Criterion (k) is standing and structural: RC halts and PINGS before writing its own agreement
  record and before enabling RC-InboxResponder; it never touches another repo's record, HALT
  file or task; each repo's operator hand-writes their OWN expiring record; there is no timeout
  and no auto-proceed. Arming is five separate operator acts in five trees, by construction,
  never one prompt. Order when it comes: pairwise RC and RSC first. LW is NEVER armed in its
  current shape - its write path IS a bypassPermissions session.

Do NOT redo - all shipped 2026-09-15, all pushed:
  - The moon-sync five slices (poller, watcher, docs/CHANNEL.md, console-flash tool, the
    /api/moon-sync-status route). Closed.
  - The console-flash attribution. The interpreter-token swap is REFUTED by measurement; no
    swap was made and none may be made - swapping it CREATES a flash.
  - The docs/OPERATIONS.md scheduled-task drift: the missing RC-MoonSyncPoller row, the stale
    count, and the bare task's name (RiotCommander, never renamed by the product rename, which
    is why the documented check command could not see it). Fixed. A different number on
    re-measure is machine state moving, not drift returning.
  - The MSIX shadow delete, the poller restart, and the FYI delivery to four inboxes. Done and
    verified.
  - Any fleet-surfacing mechanism in ledger 1417's do-not-redo list - twelve are refuted with
    reasons.

Traps this session paid for, worth not re-paying:
  - A COUNT GOES STALE INSIDE THE COMMIT THAT WRITES IT. That happened three times here, once
    to a correction that was itself correcting a stale count. Stamp every count to the commit
    it was measured at; never write "at this commit".
  - GREEN ON WINDOWS IS NOT GREEN. CI is Linux and gates the whole tree. A local 22452-passed
    run missed four platform defects. Check the CI run, not just the local suite.
  - A MODULE RUN ALONE HIDES CROSS-MODULE DAMAGE. A test that closed a real OS handle corrupted
    every later subprocess in the same pytest process and was invisible until a second module
    ran beside it.
  - docs/DS_SWEEP_TRACKER.md carries a SECOND, never-demoted "Next free id = " pin far down the
    file, so a bare `grep -c` returns 2 and must NEVER be used as an apply check. No line number
    is given here on purpose.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
Session default applies: orchestrated, multi-agent, self-adjudicating, self-adversarial, all
doing in subagents. Halt and ping before any byte leaves the tree.
