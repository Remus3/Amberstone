NEXT SESSION - THE RESPONDER IS BUILT AND DISARMED. WHAT COMES NEXT IS ARMING,
RSC'S REPLY, AND ONE OPEN DEFECT.
------------------------------------------------------------------------------
RM-384 SHIPPED 2026-09-08 (LEDGER 1364, head 5120f3735, ci + docs-guards both
green with the jobs confirmed RUN, not just the run conclusion read). Do NOT
rebuild any of it. Read `docs/RESPONDER_RUNNER_SPEC.md` section "Build
measurements" FIRST - it records every spec item that was measured wrong, the
four forced signature deviations, three vacuous spec mutants, and the two
Linux-only portability defects. It is the shortest path back into this work.

STATE ON THE MACHINE RIGHT NOW
  RC-InboxResponder is REGISTERED, Enabled, State=Ready, repetition PT5M,
  ETL PT10M, S4U, pythonw. It is DISARMED: no `ops/runtime/inbox_responder_
  agreement.json` exists, so every tick terminates `disarmed / no_agreement`
  and writes a START/END pair plus a row. Nothing spawns. Nothing is sent.
  IT MUST BE DISABLED TO RUN THE FULL SUITE - it appends to the live responder
  log every five minutes and the suite's autouse arm guards exactly that
  surface. `Disable-ScheduledTask -TaskName RC-InboxResponder`, run the suite,
  `Enable-ScheduledTask` after.

THE ARMING NOTE WENT OUT to RSC on 2026-09-08 (13907 bytes, in
`C:\Resin Compute\moon_sync_inbox\`). It carries every measurement with its
command, discloses both vocabulary extensions (`runner-failed`, `attempt-cap`
as a HOLD under two terminations), and states plainly that RC is still
disarmed. FIRST ACTION NEXT SESSION: read RC's own `moon_sync_inbox/` for
RSC's reply and for anything from CS / LL / LW, and answer per RC's own rule
(silence means unready, so if something is now ready, say so).

OPEN, IN PRIORITY ORDER
1. RSC's reply to the arming note, and whatever it refutes. Three RC claims
   were refuted by RSC in one hour last time; expect more, and check them
   rather than defending.
2. ARMING is an OPERATOR act, not an agent one. The agreement record shape,
   its canonical id (sha256 over seven semantic fields, first 16 hex), and its
   expiry semantics are in spec section 10 and restated in the arming note.
   Do not write that file on the operator's behalf without being asked.
3. THE ONE OPEN DEFECT, needs its own RM id (next free is RM-385). A note whose
   NAME is a directory junction never reaches the gate 6 link checks, because
   `pending_notes` filters on `Path.is_file()` and a junction fails it. The
   cycle reads `empty / none_pending` and the entry sits in the inbox
   unremarked on EVERY later tick. Pinned by the one remaining strict-xfail,
   `test_a_note_that_is_a_junction_is_refused`. It is NOT a containment hole -
   a companion PASSING arm proves nothing outside the inbox is opened, quoted,
   spawned for or delivered - but it is silent-forever rather than a refusal.
   Fixing it at source means editing `tools/inbox_responder.py`, which spec
   section 15 forbade for the BUILD; that scope ended with the build, so the
   next session may take it with a fresh decision.
4. A design smell worth one scoped item: `main` has no config-injection seam,
   which is why every arm that drives it must monkeypatch a resolver. That is
   the reason the CLI-less-host bug class kept recurring.

DO NOT REDO
  The DECIDED forks in the spec. The settled A1-A5 / disposition (i) / M6 zero
  / no stop rule. The 26-gate census, the 68 mutants, the dry cycle, the task
  registration. The three conditions are MEASURED and recorded; do not re-argue
  them. Do not re-pitch a dollar cap or an attempt-cap retirement - both were
  removed on purpose.

THE TWO LESSONS MOST WORTH CARRYING
  A mutant score reported per TABLE ROW is not per-GATE coverage: "44 of 44
  redden" was true while two of 26 gates had no mutant on their own call site
  (memory `feedback_mutant_row_list_is_not_coverage_proof`). And a CI run's
  CONCLUSION is not a job result in EITHER direction - a red run whose relevant
  job was skipped is as misleading as a green one, and this session made that
  mistake before catching it.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
git log, then read RC's inbox before writing anything.
