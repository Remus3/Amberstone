NEXT SESSION
------------
Task: Check the cross-repo inbox FIRST. The JOINT RE-PIN of SHARED_SHA256 is
      still the gating item, third session running - RSC has been silent since
      moon_sync_inbox/2026-09-09-2100-from-RSC. If they have replied, execute
      the joint re-pin (never unilaterally, never regenerate digests from local
      disk, byte-level copy only). If they are still silent, pick up RM-387 or
      RM-388 from ROADMAP NOW instead.

Context: moon_sync_inbox/ (read RC's own; newest first) - the pinned pair is
      ops/loop/slots.py + ops/loop/winmutex.py, pinned by SHARED_SHA256 at
      tests/test_loop_concurrency.py:474, parametrised at :542. The fifth
      sibling-name escape sits INSIDE that block at :492 as a two-word name
      split across a comment line wrap, shipped as a known named VISIBLE
      exception at tools/sibling_name_sweep.py:112. Live state: RC pid alive,
      mode=client, LCU Offline. Probe ops/runtime/health.json and
      https://127.0.0.1:8888/api/state before assuming anything.

Acceptance: either both trees hash equal after a JOINT re-pin (both hashing
      equal IS the acceptance, not a note claiming it), or a named RM row
      shipped with a red-before-green guard and CI green on both workflows.

Do NOT redo:
  - Q5. Answered and CLOSED 2026-09-08 (RM-389, LEDGER 1370) and RELAYED to LW
    on 2026-09-10 (sha 46a00569). It only looked open because the operator's
    own 2026-09-08 standby narrowing kept it out of LW's tree.
  - The _archive/ and 101qq_*_capture_* gitignore rules. Both are DELIBERATE
    and carry their own explanatory comments; 341 of the 342 ignored-tracked
    hits are those two. Only agents/state/ was a real defect and it is fixed
    (5f3555ec4) with a guard at tests/test_gitignore_negations_are_effective.py.
  - Arming any responder. Nobody's is armed - LW's is built and deliberately
    disarmed (D5), RC's RC-InboxResponder is Ready/disarmed. Arming is an
    operator act.
  - CS and LL notes. Both still on standby as far as RC knows; nothing was
    written into their trees and nothing should be without operator approval.

Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
      git log + ROADMAP.md.
