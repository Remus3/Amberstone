# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-05-18 - s240: on-demand VLM coach (SCREEN READ) (`306f5f3`, pushed)

Operator-directed off the AUTONOMOUS_AUDIT s5 order - picked competitive opportunity #3 (the on-demand VLM coach). Full vertical slice with a dedicated pill (operator chose recommended scope).

- **`306f5f3` (pushed).** Operator clicks "SCREEN READ" -> `POST /api/command{screen_read}` -> RC does ONE Sonnet pass over the frame it ALREADY holds in the `:8889` relay -> a single tactical coach one-liner on a dedicated RIGHT NOW pill. New `dashboard/_screen_read.py`: `ScreenReadVision(GameVisionReader)` + `run_screen_read()` + `trigger_screen_read()` (sync pending marker + daemon-thread Sonnet pass + in-flight dedupe). Command case in `routes_state._serve_command_post`; `_state_builder` stamps `state.screen_read` (dedicated field, mirrors the s184 archetype_nudge stamp pattern). Frontend mirrors the s184.1 chip exactly: `web/js/panels/screen_read.js` (`renderScreenRead`), button+pill in the RIGHT NOW panel, 3 `/api/state` callsites in `main.js` next to `renderArchetypeNudge`, `.rn-sr-*` CSS in `right_now.css`.
- **Why low-risk (key architecture finding):** RC can do the WHOLE pass server-side in Python - `modes.shared_vision._capture_screen()` reads the relay buffer RC ALREADY has (filled by the s221-safe bettercam DXGI agent during a live game). **NO new screen capture** -> does NOT touch the Vanguard-BSOD surface (that was the retired *continuous PIL all-screens* agent; this only reads the buffer). **NO new Sonnet/cost code** - `ScreenReadVision` subclasses the audited `GameVisionReader` so the moon_proxy->direct-SDK fallback + `cost_tracker` daily-cap + token-bucket gating in `_extract()` apply verbatim (s173 anti-drift: one Sonnet path). ADR-006 compliant: vision/LiveClient self-coaching only (no Riot Web API); prompt = strategic judgement, explicitly NOT input automation / decision-simulation.
- **Dedicated, not clobbered:** `state.screen_read` is its own field (NOT `coach.immediate`), so an on-demand read the operator wants to dwell on survives the next ~2s coach tick. Pill statuses: pending ("reading the screen...") / ok (one-liner + age) / error (friendly per-code text). In-flight guard dedupes double-clicks; HTTP handler never blocks (worker on a daemon thread).
- **Verified:** TDD red->green - **caught + fixed a real `{}`-is-falsy bug** (`if not raw:` mis-routed an empty-dict vision result to `no_fresh_frame` instead of `empty_note`; fixed to `if raw is None:`). 30 new tests (17 backend incl. file-contract/dispatch/state-stamp/prompt-compliance, 13 frontend wiring+ASCII guards). Full `tests/` **1405 / 30 subtests / 0 failed** (s239 1375 + 30). ruff + py_compile + node-check clean; pre-commit hooks green. RC reloaded (pid 11424 -> 9824, reload_ok). **Live-verified end-to-end off-game on :8888:** `POST /api/command{screen_read}` -> `{"ok":true}` -> worker -> graceful `no_fresh_frame` (correct off-game: relay empty) -> `/api/state.screen_read` surfaces `{status:error,error:no_fresh_frame,ts,requested_ts,model}`. Static JS/CSS/HTML auto-served fresh (ADR-008). `data/screen_read.json` gitignored (runtime, like the curated `data/*.json` list).
- **No ENGINE bump / no DS restart / DS suite not run** - zero DS code, non-engine (ROADMAP item-85 / s238/s239 precedent).
- **Don't-redo:** the full slice + pipeline are done + live-proven off-game; do NOT re-investigate the vision/capture path (the "RC can do it server-side from the relay buffer, no MCP capture, no BSOD surface" finding is established). Prompt is ADR-006-shaped - don't re-litigate.
- **OPERATOR-GATED (not done, not blocking):** the live Sonnet-on-a-real-frame pass + the visual pill render only happen in a live game (the relay is fed only mid-game by the screen-agent). Next live game: click SCREEN READ, confirm a sane tactical one-liner renders on the pill (ok status), eyeball wording for ADR-006 tone (judgement, not "press W"), and confirm it does NOT overwrite the RIGHT NOW coach `immediate`. Precondition: the s221 screen-agent feeding `:8889` + League in Borderless (fullscreen-lockup note). Backend + wiring are fully autonomous-verified; only operator-eyes on the live one-liner remain.
- **NEXT (AUTONOMOUS_AUDIT s5):** opportunities #2 (done s239) and #3 (done s240) shipped. Remaining: the autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split; or product opportunity #4 (Arena/Mayhem augment recommender foregrounding - `core/augment_recommender.py` already ships, needs marketing-pillar surfacing) / #6 (local MCP exposing Daemon Slayer + match DB). Optional data enrichment: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc stays operator-CLOSED (s232).

---

# 2026-05-18 - s239: per-user pick-ban contextual read + champ-select foreground (`c0cf54f`, pushed)

Operator-directed off the AUTONOMOUS_AUDIT s5 order (items 1-3 all shipped: Phase 4d `aaa9c5a`, dashboard.js quarantine s236, builders.py split s237). Operator picked item 4 = competitive opportunity #2, full scope ("Contextual read + foreground").

- **`c0cf54f` (pushed).** New read-only `GET /api/champ-select/personal-record?champ=&allies=&enemies=&role=&queue=` in `dashboard/routes_pickban.py` (NOT frozen; additive). The mood queries RECOMMEND picks; this REPORTS the operator's actual lifetime record vs the champions already on the board: WR on the hovered champ (all-roles headline + at-role qualifier), WR alongside each locked ally, WR vs each locked enemy. The per-user differentiator no cohort-averaged SaaS can do. 3 helpers (`_query_champ_record`/`_query_with_ally`/`_query_vs_enemy`) reuse the proven EXISTS/queue-subselect patterns from the synergy + bans queries.
- **Foregrounding:** `champ_select.js` gains `_csvFetchPersonalRecord` (same 60s dedupe as `_csvFetchPickBanRecs`, no queue param by design - A7) + `_csvRenderPersonalRecordBlock`; `_csvRenderPickBan` derives `enemyIds`/`selfCid` and **prepends a titled "YOUR RECORD" headline ABOVE the mood recs** so the per-user model is the first, strongest thing in the (otherwise header-hidden, buried left-col-row-2) pick-ban card. New `.csv-pr-*` CSS (lavender left-accent, WR-tinted chips, font floors >=13px); row-2 grid floor 354->460 to budget the headline (only binds on short viewports; 1080 baseline unaffected).
- **Assumptions (stated, hard rule):** A1 vs-enemy = opposing team any-lane (CS doesn't know lanes; broader = more games; `_query_bans` stays lane-strict for *recs* by design). A2 with-ally = ally on my team regardless of my champ. A4 displayed wr_pct = RAW (s238 invariant - reports, doesn't rank, no smoothing). A5 0-game entries returned (UI shows "first time"). A7 ALL-TIME (no recency window) so it has live data despite rewind_history.db staleness - lifetime head-to-head is the meaningful per-user signal.
- **Verified:** TDD red->green. Full `tests/` **1375 passed / 30 subtests / 0 failed** (s238 1344 + 31 new: 16 backend incl. e2e + 15 frontend wiring/ASCII guards). ruff + py_compile + node --check clean; pre-commit hooks green. RC reloaded (pid 20488 -> 11424, last_reload_ok=true). **Live-verified on :8888 with REAL operator data:** Vayne 45% all-roles (47g) / 18% at BOT (11g), 44% w/ Thresh (27g), 56% vs Darius (18g), 50ms - proves A7 (real data despite recency-stale DB). Static JS/CSS auto-served fresh via ADR-008 asset hash.
- **My own ASCII guard caught 3 self-introduced hard-rule violations** (a `->` comment arrow + 2 `B7` middot separators in rendered strings) before commit - fixed to pure ASCII. The CSS file at large carries pre-existing legacy non-ASCII whose purge is an explicitly separate operator-gated pass (CLAUDE.md) - the ASCII test is correctly scoped to the s239-authored region only, NOT the whole file (asserting the whole file would be wrong-scoped scope-creep).
- **No ENGINE_VERSION bump / no DS restart / DS suite not run** - zero DS code touched, non-engine (ROADMAP item-85 / s238 precedent for non-engine work).
- **Don't-redo:** the contextual-read backend + the foregrounding wire are done + live-proven; do NOT re-derive the queries or re-investigate the champ-select render path. The 6 assumptions are operator-aligned (full scope was explicitly chosen) - don't re-litigate A1/A2 framing.
- **OPERATOR-GATED (not done, not blocking):** the champ-select **visual** UI-audit ritual - the headline only renders in a live SR draft (pick-ban panel is CSS-hidden in ARAM/Arena/Mayhem). Next time a live SR champ-select is open: eyeball the "YOUR RECORD" headline (champ line + WITH/VS chip rows + tints), confirm the 3 mood pick-rows below aren't crushed (the 460px floor budget), then run the per-page UI-audit subagent ritual. Backend + structure are fully autonomous-verified; only operator-eyes on the live render remain.
- **NEXT (AUTONOMOUS_AUDIT s5 order):** product opportunity #3 (on-demand VLM coach - infra owned, involves operator-gated Game-PC capture) OR the autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split. Optional pick/ban data enrichment still open: the one-off 101.qq.com Game-PC CDN capture (ROADMAP). DS conditional arc stays operator-CLOSED (s232).

---

# 2026-05-18 - s238: shared smoothed-rate primitive built + 2 consumers wired (`3f4902d`, pushed)

Operator-directed off the AUTONOMOUS_AUDIT s5 fork (item 4). Picked the LOCKED, zero-open-decision engineering session: CLAUDE.md #90 decision (2), the shared smoothed-rate primitive.

- **`3f4902d` (pushed).** New `core/smoothed_rates.py` - ONE module owning the Laplace/Beta family RC had reimplemented 3x: `laplace_rate(w,g,alpha)=(w+a)/(g+2a)` (Beta == Laplace on pair counts, one primitive serves own + pairwise), `shrink(n,k)=n/(n+k)` (exact original `(n+k)>0 else 0.0` guard), `blend(own,prior,w)=w*own+(1-w)*prior`. Pure/stateless/never-raises. TDD: 25 unit tests incl. guard edges + the exact augment_recommender parity vectors.
- **Consumer 1 - `augment_recommender.py` refactored onto it, provably byte-identical.** `_own_wr`/`_pair_wr`/the blend-weight + synergy-shrink lines now call the primitive; `DEFAULT_ALPHA/K` sourced from it. Equivalence proof = the unchanged `tests/test_augment_recommender.py` (SmoothingMath/BlendRegime/Synergy, exact numeric pins like 2/6, 3/4, 2/7) staying green. Every caller passes alpha=1.0 so the laplace zero-guard is unreachable for real inputs - the extraction cannot shift output.
- **Consumer 2 - `routes_pickban.py` synergy mood** (the locked "today only a raw recent-form proxy" example). New `_rank_by_smoothed_wr` helper; both synergy sub-paths (ally-joint + recent-form fallback) drop the SQL `ORDER BY raw-WR ... LIMIT` and rank in Python via the primitive. A 2-0 no longer outranks a proven 14-8. **Invariant kept:** displayed `wr_pct` is the RAW observed rate (operator sees real WR); only the ranking key is smoothed. `HAVING games>=2` floor kept; comfort/limit/new untouched (out of the locked "pick/ban synergy" scope - changing them is creep). +5 s238 characterization tests pinning the behavior change + the raw-wr_pct invariant; reason text gains a "(smoothed)" transparency marker (existing `assertIn` tests still hold).
- **Consumer 3 (c) NOT wired by design:** the s220 PGR Morello-style 0-100 score is ROADMAP-S3, **not built**. The API is shaped to serve it; there is no consumer to wire. Fold the `noaboa07/Morello` (MIT) reference in when S3 ships - do NOT pre-build it here.
- **Verified:** full `tests/` **1344 passed / 30 subtests / 0 failed** (s237 1314 + 30 new). ruff + py_compile clean; pre-commit hooks green (py_compile + archmap). RC reloaded clean (pid 11020 -> 20488, last_reload_ok=true). Live: synergy no-allies correctly falls back to comfort (`fell_back=True`) because `rewind_history.db` is stale (newest 2025-12-16, 0 matches in the 60d window - **pre-existing**, CLAUDE.md item 14, NOT a regression: my change only reorders non-empty sets, the selection SQL + dispatcher fallback are untouched); synergy **ally-path** (`allies=234` Viego, 16 joint games) verified end-to-end - real smoothed path, `fell_back=None`, "(smoothed)" marker live, raw `wr_pct` correct.
- **No ENGINE_VERSION bump / no DS restart** - behavior-preserving extraction + non-engine consumer (ROADMAP item-85 precedent for behavior-neutral work).
- **Don't-redo:** the primitive + the 2 wired consumers are done + proven; do NOT re-extract or re-derive the math. The augment refactor is byte-identical (locked by the unchanged test_augment_recommender suite). PGR consumer (c) is correctly deferred to S3 - not incomplete.
- **NEXT:** back to the AUTONOMOUS_AUDIT s5 order - product-level competitive opportunity #2 (per-user pick-ban foregrounding) or #3 (on-demand VLM coach), operator-directed; OR the autonomous-safe 4.C `coaches/adaptation_hint.py` (1602) split (`agents/supervisor.py` is FROZEN, `effects.py` is engine reviewed-only-never-autonomous). Optional pick/ban data enrichment: the one-off 101.qq.com Game-PC CDN capture (ROADMAP 🔵). DS conditional arc stays operator-CLOSED (s232).
