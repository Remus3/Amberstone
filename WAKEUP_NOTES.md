# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary + 2026-05-20 housekeeping batch + 2026-05-21 items 121-130 + 2026-05-22 items 133-139 + 2026-05-22 items 140-149 + item 181 + item 187 + item 188 + item 189 + item 190 + item 191 + item 192 + item 193 + item 194 + item 195 + item 196 + item 197 + item 198 + item 199 + item 200 + item 204 + item 215 + item 216 + item 227 + item 228 + item 241 + item 242 + item 245 + item 246 + item 247 + item 248 + item 249 + item 250 + 2026-06-01 Share-docs-reconcile (1.86.0) + item 255 + item 256 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-06-02 - item 260: DS GAP-2 effects-text-only SHIELD registry + 10 SEEDED, default-OFF byte-identical (ENGINE 1.89.0 -> 1.90.0; DS restarted 1.90.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next DS schema lift and exhaust it then /done". Verified-first: the ledger "effects-text lane exhausted" (items 255-258) was HEAL/DAMAGE-only - the override registries inject attribute_kind="heal"/"damage" but NEVER shields, while the consumer ability_hps._eval_heal_shield_block already scores the 56 snapshot shield blocks (kind-agnostic) + compute_ability_hps already sums _select_kind_blocks(form,"shield") into total_shield_per_sec. Effects-text-only shields (no shield block parsed) = the genuinely-next clean headless lift.

FINDING (roster scan all 171 champs, shield/absorb-verb forms with NO attribute_kind=="shield" block = 38 matches): the seedable self-shield set is exactly 10.

SCHEMA LIFT (NEW module _passive_shield_overrides.py, sibling of heal/damage; ZERO consumer change): PassiveShieldEntry (linear_terms + cadence + note + attribute + level_scaled + bilinear_terms=()) + _PASSIVE_SHIELD_OVERRIDES + to_shield_block (builds DamageBlock attribute_kind="shield"). AbilitiesSnapshot.load(apply_passive_shield=False) flag + _apply_passive_shield_overrides (gate: NO existing shield block + registered entry). ONE additive unit "% maximum mana" -> caster_max_mp in ability_hps._HEAL_UNIT_TO_CTX (ctx attr already existed; no snapshot block uses a mana unit -> default byte-identical).

SEEDED (10, default-OFF, exact 16.11.1): Malphite P (10% maxHP) / Camille P (20% maxHP) / Vi P (12% maxHP) / Rakan P (30:225 + 95% AP) / Shen P (47:120 + 13% bonusHP) / Yasuo P (125:600 Flow) / Blitzcrank P (35% maxMANA, sole mana shield) / Skarner W (8% maxHP) / Volibear E (14% maxHP + 75% AP) / Viktor Q (40:115 + 18% AP, level_scaled). EXCLUDED: spell shields (Nocturne W / Sivir E) / damage-stored barriers (Sett W Grit / Mordekaiser W / TahmKench E grey-health) / re-grants (Galio R / Yuumi R) / strips (Blitzcrank R / Rell Q) / verb-but-no-grant.

LIVE in-process (compute_ability_hps apply_passive_shield=True): Malphite P L11 0 off / 157.76 on (= 10% maxHP exact); Blitzcrank P 216.3 (= 35% maxMP exact, mana shield works); Viktor Q level-scaled L11 84.118 / L18 115.0; Skarner W 127.62 (= 8% maxHP); Janna E snapshot shield OFF==ON 2.443 (gate skips); all default false byte-identical.

+31 tests test_passive_shield_overrides_item260.py. ENGINE 1.89.0 -> 1.90.0 + 41 test-pin syncs + CHANGELOG prepend + Share re-sync (262 files, --check clean + authored 04/05/CHANGELOG semantic updates) + living docs (DAEMON_SLAYER/README/BRIEF/ARCHITECTURE 1.90.0/5962). DS suite 5931 -> 5962 (+31, 0 failed); phase8 70/70 post-restart; ruff clean; added-diff 0 non-ASCII. Commit 0a3383b pushed 0d503f5..0a3383b; CI green run 26794150158; gist auto-synced 1.90.0.

Don't-redo: (a) the consumer was already shield-complete - the lift adds ONLY the effects-text override half; do NOT touch the consumer. (b) gate is "no existing shield block" (Janna/Lulu/Karma snapshot shielders byte-identical flag-on); a spell-slot shield (Skarner W / Volibear E / Viktor Q) is admitted because it has a DAMAGE block not a shield block -> adding a shield block never touches the damage blocks so compute_ability_dps is unaffected. (c) "% maximum mana" is the SOLE mana shield (Blitzcrank); caster_max_mp already existed; default byte-identical - do NOT re-pitch a mana ctx field. (d) every seeded shield is a CASTER stat (resolves at the default; NOT target-relative) - resolve_target_relative changes no shield. (e) Viktor Q is level_scaled (indexes by level not Q rank); other 9 flat or P-slot (rank==level-1). (f) the 10 are the EXHAUSTIVE clean self-shield set at 16.11.1; EXCLUDED classes are documented NEGATIVES. (g) DS restart PowerShell taskkill /F /PID + schtasks (NEVER Stop-Process).

NEXT (all live/operator-gated - the clean headless DS effects-text lane is now GENUINELY EXHAUSTED for heal + damage + shield): Phase D live flag-flips (apply_passive_shield 10 forms + apply_passive_damage 28 + apply_passive_heal 24 + apply_ability_amps + apply_build_tenacity/score_by=cc_blended + apply_mode_modifiers + gate_ammo + aoe_targets_hit) each re-ranks + needs a real game; the on-event shield cadence (passive-P -> a fight clock) is a future consumer's job.

---

# 2026-06-01 - items 258 + 259: DS C2 AA-empower authored (Fiora E) + item-240 champ-select UI part-3 (2-slice parallel orchestrator-merge run; ENGINE 1.88.0 -> 1.89.0; DS restarted 1.89.0; RC not restarted)

Operator "start in parallel Remaining open work that is all live/operator-gated"; mode=client (no game) so the named Phase-D flag-flips + item-243 ranked-SR watch cannot validate headlessly -> AskUserQuestion picked "Both in parallel". Two disjoint slices, orchestrator-merge.

SLICE A - item 258 (DS engine; FF merge 43c3d67; ENGINE 1.88.0 -> 1.89.0; DS restarted 1.89.0 + live-verified): authored the 5 C2 base="aa" AmpEntry placeholders (item 239/247 FORWARD-MARKER, amp_per_rank=(0.0,)) against live 16.11.1, verify-first / no-fabrication. Only Fiora E maps to a base-AA single-target DAMAGE multiplier (the _aa_amp_multiplier consumer scales base-AA only, item-proc unamped): Fiora E f0 AUTHORED amp_per_rank (0.0545,0.0700,0.0889,0.1125,0.1429) always_on=True (2nd empowered AA = guaranteed crit 160%:200% by E rank; addend = (m-1)/cd amortized over E cd 11/10/9/8/7s, no-crit build, operator-tunable midpoint). NEW source-rank >=0 guard (E unleveled -> byte-identical). The OTHER 4 left INERT with documented not-fit reasons: Caitlyn W (Headshot trap-sprung passive, not every-AA) / Jayce W f1 Hyper Charge (70:110% AD/hit BELOW 1.0xAD; value is +360% AS the consumer cannot model; modeling as a mult NERFS ranks 1-4) / Sivir W Ricochet (bounce to EXTRA targets) / Nidalee Q f1 Takedown (missing-HP magic conversion off the AA rotation). +18 tests test_aa_empower_authored_item258.py (+2 net from the item-247 forward-marker test split). Share --check clean (255 files 1.89.0; CHANGELOG 1.89.0; 05 5911->5931 / 172->173 files; 04 AA-empower seam "wired; one entry authored"). DS suite 5911 -> 5931 (+20, 0 failed); ruff + hygiene 12 clean. LIVE :8893 POST /dps (independent orchestrator probe on the restarted 1.89.0): Fiora L11 apply_ability_amps=false 31.0544 / true 32.7469 (x1.0545 = 1+0.0545 E rank-0; raw_attack_dps 86.487->91.201 SAME ratio = base-AA scaled); Fiora L1-3 (E rank -1) flag-on byte-identical (guard); Caitlyn/Jayce/Sivir/Nidalee + control flag-on byte-identical; all default false byte-identical.

SLICE B - item 259 (web UI; merge ee33f20 of 465a950 + audit fix fc0363e; non-engine; routes_loadout.py NOT touched -> RC NOT restarted; web/* auto-serves via ADR-008): item-240 UI part-3 (docs/CHAMP_SELECT_UI_SPEC.md section 3 [DEFERRED] -> SHIPPED; parts 1/2/4/5/6 shipped item 213). 3a NEW nested rune panel RIGHT of the build-path cards (2-col grid .csv-build-collapsed-cols minmax(0,1fr)/170px + _csvRunePanelHtml + .csv-rune-panel/.csv-rune-opt). 3b GREEN .is-selected #4ade80 = sticky rune; AMBER .is-recommended #fbbf24 = recommended-for-active-build, WITHHELD when on it; build-card click re-points amber not green (_repointRecommendedRune). 3c per-champ persistence - variant in the item_build-shared rc-ingame-build-<champ> plain-string key, rune in NEW sibling rc-cs-rune-<champ> (variant-only legacy -> empty runeKey). 3d header control on the SR BUILD CHOOSER title: [PUSH] + 3 checkboxes Runes/Spells/Build RIGHT-aligned, GLOBAL rc-cs-push-flags, DEFAULT all UNCHECKED (opt-in). 3e uncheck->check pushes now / in-category change pushes while checked / uncheck no push / [PUSH] force-pushes checked; verbs reuse push_runes / push_items / set_summoner_spell (all pre-existing items 210/165/164); _csvApplyLoadout grew optional pushFlags (omitted -> all-three, back-compat). +30 tests test_csv_build_chooser_part3_item240.py; 115 pass on merged main (incl the 1.89.0 engine pin now matching repo+live). 5-phase UI-audit subagent ALL PASS, MUST-FIX NONE; 1 SHOULD-FIX applied in-slice fc0363e (.csv-builds-push-cat min-height var(--hit-min)). Bug caught in review: push helpers passed cid=0 (empty-placeholder early-return) -> cid=1 so variants resolve from the warm cache.

Post-merge orchestrator steps: moved .git/hooks/post-commit aside for the merges (item-240 gist-sync race corrupted the Slice A agent's first worktree commit) then RESTORED it + manual gist sync at run end. Living docs synced 1.88.0 -> 1.89.0 / 5911 -> 5931 (DAEMON_SLAYER + README + BRIEF + ARCHITECTURE).

Don't-redo: (a) the AA-empower seam fits FEWER champs than the 5 registered (item 239) - only Fiora E; the other 4 are documented NOT-FIT, do NOT force-author. (b) Fiora E addend is amortized / operator-tunable - the live saner-not-different re-rank is still owed (Phase D). (c) Slice B LCU verbs pre-existed; routes + Game-PC agent untouched -> no RC restart; per-category gate keys push_runes/push_items/push_summoners; variant persists in rc-ingame-build-<champ> (do NOT move - cross-panel pre-select depends on it), rune in rc-cs-rune-<champ>. (d) champ_select.js/.css are ASCII-clean now - the old "pre-existing non-ASCII carry" note is STALE. (e) the post-commit gist hook is restored; a future Share-touching commit auto-pushes the gist; when merging a Share-touching worktree branch, move the hook aside first (it raced + corrupted a worktree commit this run). (f) DS restart PowerShell taskkill /F /PID + schtasks (NEVER Stop-Process).

CARRY / NEXT (all live/operator-gated): (1) item 259 LCU push verification at a REAL champ-select (operator was mode=client; build + unit-test + mock-render verified, the live push DEFERRED). (2) Fiora E + AurelionSol Q cross-spell + every Phase D flag-flip need a real game to validate saner-not-just-different. (3) item-240 #7/#8 sign-off + item-243 ranked-SR UI watch. The clean headless DS lane stays EXHAUSTED (item 257).

---

# 2026-06-01 - item 257: DS gap-plan C2x - AurelionSol W CROSS-SPELL self-state ability-amp seam (the LAST named clean headless DS item), default-OFF byte-identical (ENGINE 1.87.0 -> 1.88.0; DS restarted 1.88.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next open items". Per items 247/255/256 the sole remaining named headless DS item = the AurelionSol W cross-spell amp seam (in _STAGED_AMP_CANDIDATES since item 239). All effects-text lifts (HEAL 250-254; DAMAGE 247/248/249/255) + C1 block-routing (246) were exhausted; this resolves the LAST staged self-amp candidate -> the staged self-amp lane is now closed (registry EMPTY).

FINDING (verified vs live 16.11.1 before building): AurelionSol W (Astral Flight) carries ONE modifier block "Breath of Light Flat Damage Modifier" raw_modifiers [108,109,110,111,112]% (x1.08..1.12 by W rank) that amplifies the Q beam (Breath of Light) WHILE W flight is active (effects_descriptions[0]: "During flight ... Breath of Light has no cooldown and maximum channel duration"). W f0 itself has NO damage block -> the amp cannot be keyed to W (the per-form AmpEntry key cannot express a cross-spell target); item 239 staged it for exactly this.

SEAM (NEW cross-spell self-state mechanism; contained to _ability_amp_overrides.py + 1 consumer site in ability_dps.py): NEW CrossSpellAmpEntry (source_key + amp_per_rank + condition + always_on; reuses the existing _amp_multiplier math, type widened to AmpEntry | CrossSpellAmpEntry) + NEW _CROSS_SPELL_AMP_OVERRIDES keyed by the TARGET spell + NEW _cross_spell_amp_for resolver + NEW _COND_W_FLIGHT="w_flight" condition (0.4 midpoint = the magnitude the staged entry always documented). compute_ability_dps consults it ONLY under apply_ability_amps=True: resolves the SOURCE (W) rank via rank_at_level(source_key, level, max_priority) at the current level, gates on the W-flight midpoint, multiplies the target Q's per-cast amp_factor on top of any same-form ability amp; SKIPPED when the source spell is unleveled (src_rank < 0 -> no buff).

SEEDED (1, default-OFF): AurelionSol Q f0, source_key="W", amp_per_rank (0.08,0.09,0.10,0.11,0.12), condition w_flight. NO double-count: W f0 has no damage block (source contributes nothing on its own) + Q has no _ABILITY_AMP_OVERRIDES entry (no same-form amp to stack). _STAGED_AMP_CANDIDATES now EMPTY (Sion Q + Hwei Q f2 resolved item 246 via block-index routing; AurelionSol W resolved here).

LIVE :8893 (POST /ability-dps): AurelionSol Q apply_ability_amps=false 292.5 / true 304.2 (x1.04 = 1 + 0.4*0.10 at W rank 2, lvl 11); in-process lvl1 byte-identical 146.25 (W rank -1 skipped) / lvl16 306.54 (x1.048 = 1 + 0.4*0.12 W rank 4); Caitlyn control flag-on byte-identical; all default apply_ability_amps=False byte-identical.

+25 tests test_cross_spell_amp_item257.py. ENGINE 1.87.0 -> 1.88.0 + 41 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean, 259 files engine 1.88.0) + Share authored-doc semantic update (04 cross-spell shipped-vs-staged moved to section 1, 05 test count 5886->5911 + 171->172 files, CHANGELOG 1.88.0 prepend) + DS restart (PowerShell taskkill /F /PID 12792 + schtasks /Run /TN RC-DaemonSlayer -> /health 1.88.0/16.11.1/172/705). DS suite 5886 -> 5911 (+25, 0 failed); phase8 70/70 post-restart; ruff clean; added-diff 0 non-ASCII.

Don't-redo: (a) CrossSpellAmpEntry + _CROSS_SPELL_AMP_OVERRIDES is the canonical home for ANY amp whose magnitude/gate live on a SOURCE spell but multiply a DIFFERENT TARGET spell; keyed by the TARGET; the consumer resolves SOURCE rank via rank_at_level - do NOT try to key it on the source spell (W f0 has no damage block to scale). (b) reuses _amp_multiplier; _COND_W_FLIGHT 0.4 midpoint matches the staged entry's documented choice. (c) source-rank guard >= 0 is load-bearing (W unleveled = no flight buff = no Q amp -> lvl1 byte-identical) - do NOT remove. (d) no double-count - do NOT add a Q ability-amp entry. (e) _STAGED_AMP_CANDIDATES is now EMPTY - the staged self-amp lane is closed. (f) DS restart PowerShell taskkill /F /PID + schtasks (NEVER Stop-Process). NEXT (all live/operator-gated - the clean headless DS lane is now GENUINELY EXHAUSTED): (1) Phase D live flag-flips (apply_ability_amps incl AurelionSol Q cross-spell + the 5 C2 AA champs once authored; apply_passive_damage 28; apply_passive_heal 24; apply_build_tenacity + score_by=cc_blended; apply_mode_modifiers; gate_ammo; aoe_targets_hit). (2) author the 5 C2 AA-empower amortized values + cadence live. (3) item-240 UI part-3 + #7/#8 sign-off + item-243 NEXT(2) ranked-SR UI watch. The DS gap-plan headless spine (Phases A/B/C/E + all effects-text lifts + C1 + C2x) is COMPLETE.

