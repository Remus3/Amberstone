# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-09-21, the 16.18.1 / ENGINE 1.282.0 wrap (relocated `2026-09-19d` the lane-widget audit via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 1034" - read off the tool's own output; newest 3 = `2026-09-20c` this wrap, `2026-09-20b`, `2026-09-20`). The prior relocation was 2026-09-20, the ROUND B / bucket-scan / RM-477 wrap (relocated `2026-09-19c` the lane widget ship, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 1033" - both figures read off the tool's own output, never off a recollection; newest 3 = `2026-09-20b` this wrap, `2026-09-20` the NOW-block drain, `2026-09-19d` the lane-widget audit). **One claim in the `2026-09-20` block below is SUPERSEDED and is called out here so it is not inherited: it says the 67 MB `laning_scenarios` JSONs "were ruled out: LFS, blob is 133 bytes". REFUTED 2026-09-20 - `collect_tree_blobs` reads the WORKING TREE, so a smudged LFS file is scanned at its full 66,961,895 bytes and those seven files were the dominant term; see the `2026-09-20b` block.** The prior relocation was 2026-09-20, the NOW-block drain wrap (relocated `2026-09-19b` the idle filesystem walker, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 1032" - those two figures are read off the tool's own output, never off a recollection; newest 3 = `2026-09-20` this wrap, `2026-09-19d` the lane-widget audit, `2026-09-19c` the lane widget ship). **One superseded claim rides in the relocated block and is called out here so a reader of the archive does not inherit it: `2026-09-19b` says LL has NO `docs/CHANNEL.md`. FALSE - LL carries it at `third_party/rc_channel/docs/CHANNEL.md`; see the 2026-09-20 block.** The prior relocation was 2026-09-19, the lane-widget UI fixture audit + live acceptance wrap (relocated `2026-09-19` the RM-239 / RM-242 QA pass, VERBATIM - moved with the Edit tool because Bash writes to tracked files are banned, and the verbatim property was checked the strong way rather than asserted: the SAME text was typed into `docs/history_notes.md` and then used as the `old_string` that removed it from here, so the removal could only have succeeded on an exact byte match, and it was then re-confirmed by diffing the archived block against `git show HEAD:WAKEUP_NOTES.md`. **`scripts/wakeup_prune.py` was NOT run this pass, so NO tool-reported move count is quoted here** - do not read one into this note; newest 3 = `2026-09-19d` this wrap, `2026-09-19c` the lane widget ship, `2026-09-19b` the idle filesystem walker). The prior relocation was 2026-09-18, the laned orchestrated loop wave 7 wrap (relocated `2026-09-17b` the wave 4 block, via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 1027"; newest 3 = `2026-09-18b` this wave, `2026-09-18` wave 6, `2026-09-17c` wave 5). **THIS WAS THE FIRST REAL PRUNE AFTER THE RM-276 FIX, and the insertion-only property was verified on the REAL archive rather than inherited from the slice's scratch-copy measurement: exactly ONE difflib opcode, kind `insert`, 21 lines inserted, ZERO deleted, at old line 41, with `new == old[:41] + inserted + old[41:]` reconstructing exactly.** That matters because the pre-fix pruner would have deleted 1335 lines of ordering from this archive on this very run - see the wave 7 block below. The prior relocation was 2026-09-17, the laned orchestrated loop wave 4 doc-sync (relocated `2026-09-16b` the laned-loop wrap, VERBATIM - moved with the Edit tool because Bash writes to tracked files are banned, then byte-compared equal to the output of `scripts/wakeup_prune.py --keep 3` run on scratch copies of both files, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 970"; newest 3 = `2026-09-17b` this wave, `2026-09-17` wave 3, `2026-09-16c` waves 1-2). The pass before this one, on 2026-09-17, was the laned orchestrated loop wave 3 doc-sync (relocated `2026-09-16` the acknowledge-path wrap, VERBATIM via the Edit tool, byte-compared equal to `scripts/wakeup_prune.py --keep 3` output on scratch copies, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 969"; newest 3 = `2026-09-17` that wave, `2026-09-16c` waves 1-2, `2026-09-16b` the laned-loop wrap). The pass before that, on 2026-09-16, was the laned orchestrated loop wave 1 doc-sync (relocated `2026-09-15` the moon-sync merge, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 968"; newest 3 = `2026-09-16c` this wave, `2026-09-16b` the laned-loop wrap, `2026-09-16` the acknowledge-path wrap). The pass before this one, on 2026-09-16, was the laned-loop doc wrap (relocated `2026-09-14` the public surface refresh, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 966"; newest 3 = `2026-09-16b` this wrap, `2026-09-16` the acknowledge-path wrap, `2026-09-15` the moon-sync merge). The pass before this one, on 2026-09-15, was the moon-sync five-slice merge wrap (relocated `2026-09-12e` the discovery-axis + inter-scorer lane and `2026-09-12d` the fleet tooling-tier lane, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 2 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 964" - read those counts off the tool's own output, never off a recollection; newest 3 = `2026-09-15` this wrap, `2026-09-14` the public surface refresh, `2026-09-12f` the calibration lane). The pass before this one, on 2026-09-12, relocated `2026-09-12c` the SESSION WRAP doc-sync and reported "moving 1 session(s)" / "archive now has 962". The pass before that relocated `2026-09-12b` the five-slice merge and `2026-09-12a` RM-412 C1 OSS extraction, and reported "moving 2 session(s)" / "archive now has 961". The pass before that relocated `2026-09-11d`, the one before that `2026-09-11c`, and the one before that `2026-09-11b` and `2026-09-11a`, the same way. The 2026-09-11k RELOCATION DUE note that sat here is DISCHARGED and deleted - the file is back at keep-3. The letter suffixes are PER FILE and have diverged from `docs/LEDGER.md` - RM-385 is `c` there and `d` here; do not reconcile them. NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`4a707962`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired. The 2026-09-16 RELOCATION DUE note that sat here is DISCHARGED and deleted by the 2026-09-16b prune above.

---

# 2026-09-21a - RM-480 ratio-field ability overrides (ENGINE 1.283.0), nightly breaker flake root-caused, os.environ assert leaks closed, channel correction delivered

**LEDGER 1453 and 1454.** Commits, all pushed: `49c665b5c` RM-480 (ENGINE 1.283.0, DS :8860 bounced, /health 1.283.0); `5df0233a9` LEDGER 1453; `19ede4154` breaker boundary test float-exact; `d1d4bb089` environ-leak asserts + `empty_parameter_set_mark = fail_at_collect`; `77dff441a` CI collect fix for that ini change.

**RM-480 IS SHIPPED BUT MOVES NO DEFAULT SCORING.** The flag `apply_ability_base_overrides` is DEFAULT-OFF and parsed by only 4 routes (/ability-dps, /rank-mage, /burst, /rank-assassin) - NOT /rank-tank, and Poppy / Thresh / ChoGath are tanks. LeBlanc R's override hits block 3 while the engine reads block 0. The hand-off premise was wrong in places ("HIGH" is in no data file; Qiyana Q and ChoGath E had no ratio drift; the wiki column is wrong for Cassiopeia E Total Enhanced and Kennen R cooldown). `stale_champions` deliberately still lists all 7 - the ABSENT/STALE/CURRENT status reads it and the default engine still reads stale values.

**CI TRAP, and it bit this session:** `d1d4bb089` verified "collect counts unchanged, zero errors" LOCALLY, where the gitignored sibling config exists; on CI it is absent, `_sibling_roots()` is empty, and `test_channel_doc_rc_gate` errored at collect. A collect count measured on this host is a HOST fact. Fixed in `77dff441a` (one named skip), verified by collecting with the config resolver patched to a missing path.

**CHANNEL:** one RC note delivered 6 of 6, digest `679cfbab` (retracts RC 2130's two temp figures, answers the environ + empty-parametrize threads). It went to MAIN via the path in MAIN's README, which is NOT in `ops/moon_sync_repos.json`. Two SS notes (1900, 1930) arrived after and are UNREAD - 1930 answers RC 1030 and says a subscript is SAFE and the leak axis is the attribute.

**Operator research note closed:** `lol-scouting-replay-kit` is REFERENCE-ONLY (MIT code, Riot-owned PNGs, GRID esports data RC cannot reach).

## NEXT SESSION

See `RC-NEXT-SESSION.txt`.

---

# 2026-09-20c - DS patch 16.18.1 (ENGINE 1.281.0 then 1.282.0), Riot 429 honesty, rewind DB repaired, stop gate narrowed, public docs synced

**LEDGER 1445 through 1452.** Shipped: `6ef3ca33e` public docs (9 undocumented routes, README external-stats claim corrected, 2 disabled tasks); `6dc04cf19` item AH drift checker reads `<ornnBonus>` and fails on unparsed AH tags; `e1a1d13f7` Riot 429 reason carried via `riot_api.track_outcomes()` (UI audit passed); `087ade735` stop_claim_gate check 6 "committed" exemptions after 3 refutation rounds; `c6674a0e6` rewind 429 no longer permanent + `write_match` idempotent; `f20a9e9ad` ENGINE 1.281.0 on 16.18.1; `7fbe7aead` ENGINE 1.282.0 five-slice batch (verifier gate pending at wrap).

**THE PATCH BUMP WAS NOT ROUTINE.** lolmath.net redeployed and broke the extractor four ways; `cherry_augments` had been STALE SINCE 16.10.1 (+90 / -92); **Ultra Hydra 226668 is the ARAM Mayhem quest reward and was the FIRST item in 195 of 519 ARAM builds** - now excluded; DDragon dropped maps 21/33/35 and emptied the Brawl pool, which is now a canary test. Same smell, unchecked: Void Immolation 223069 opens 42 percent of Arena builds (RM-486).

**LIVE DATA REPAIRED, per the Data Fixes rule:** 488 participant / 64 team / 9338 frame duplicate rows removed from the rewind DB, 4 timelines re-fetched and all recovered. **Known unrecoverable:** event-id gaps and a sequence 6080 above max id show an EARLIER unrecorded cleanup that may have deleted legitimate identical events.

**OPERATOR RULINGS:** the MAIN session holds operator-stand-in authority and self-adjudicates; NO task chips ever - do it now, ROADMAP it, or put it in the next-session prompt.

**FILED:** RM-479 .. RM-488 in `ROADMAP.md` (validation swarm, LEDGER 1452). Accepted residues recorded in-tool: check 6 open-ended adjectival forms; AH checker second-AH-line-in-unknown-tag.

## NEXT SESSION

See `RC-NEXT-SESSION.txt`. Task: RM-480, the ratio-override schema lift for the 7 HIGH stale-ability champions.

---

# 2026-09-20b - ROUND B LANDED AND PUSHED, RC's share of the shared scratch bucket scanned for CONTENT, RM-477 fixed, and RC's first credential detection of any kind

**LEDGER 1439 through 1444.** **SEVEN** commits, ALL PUSHED - count re-derived from `git log`, never carried from a list: `3ca8be8ce` (ROUND B bytes + pin), `66d8e0219` (bucket scan + MC decision), `6aac0ca85` (CLAUDE.md standing grant + de-staled blind-spot example), `96e4c2e0b` (citation fix), `2cfcbaada` (RM-477 windowing), `65788dc5b` (credential arm), `ec5a18279` (RM-478 gate narrowing). ENGINE-IMPACT NONE, no `ENGINE_VERSION` move, no DS `:8860` bounce, no frozen file edited, no `.githooks/` edit. **`ops/loop/winmutex.py` WAS touched - that is this session's headline and it was operator-released first.**

**ROUND B IS LANDED ON RC AND PUSHED.** The operator released the boundary halt explicitly, and RC's push was then held on ONE published condition - the last carrier confirming - which SS met at 1848 with a fifth independent derivation. `6190 -> 6184`, `df0a7a40`, 0 CR, 0 non-ASCII, 137 newlines. **Bytes and pin moved in the SAME commit** and RC holds NO code pin (`KNOWN_CODE_HITS` and `CARRIER_CODES` both zero hits tree-wide), so RC's half was one digest move - a carrier following the round's two-step instruction literally in RC's tree would hunt a second pin that does not exist. A sibling's outside disk sweep independently confirmed RC moved bytes and pin together.

**THE TRAP RC FOUND AND PUBLISHED, and it was a live fleet hazard.** `moon_sync_inbox/winmutex.py.from-lw` LOOKS like the candidate and is NOT: it is the **2026-07-26 original**, 6146 bytes, digest `f1b4b011` - which is RC's own `# previous` pin value from before the 2026-09-06 rotation - carrying the **PRE-ROTATION MUTEX NAME VALUES**. A carrier applying it because the FILENAME matched would have held a different mutex from every other carrier with every local signal green. **RC did not apply any delivered file: RC applied the six-byte edit to its OWN bytes and only then compared against the published digest.** Build from your own disk; a published digest is a value to CHECK against, never a source.

**RM-477 FIXED, AND ITS FILED ROOT CAUSE WAS HALF WRONG.** `--tree` now completes: 716 s, **633,521,375 bytes / 4911 files, ZERO findings**, producer exit captured to a FILE. Peak memory **434.7 MB -> 88.7 MB, flat in blob size**. **THE REFUTED HALF IS THE DURABLE LESSON: "the 67 MB `laning_scenarios` JSONs were RULED OUT because they are LFS, so git's blob is 133 bytes" is FALSE.** The LFS half is true and the conclusion does not follow - `collect_tree_blobs` reads the **WORKING TREE, not the object store**, so a smudged file reads at its full 66,961,895 bytes and those seven files were the dominant term all along, dwarfing the two 5-6 MB docs that were blamed. **A fact about the OBJECT STORE is not a fact about the CHECKOUT.** CLAUDE.md's "LFS object content is never scanned" was corrected the same session: true of `--pre-push`, FALSE of `--tree`. Fixed by WINDOWING, never exclusion.

**THE BUCKET: RC's SHARE SCANNED FOR CONTENT, NOT SIZE.** 29 files / 542,874 bytes, **ZERO credential-class** across 14 families each run twice including a line-wrap-dewrapped arm. **PII is NOT zero: account name and home paths in 8 files each, and the operator's REAL EMAIL in one file** as git author metadata in a patch dump - one of the sibling's two flagged email files is RC's. **RC HAD NO SECRET SCANNER AT ALL**, and the binding constraint was not the usual narrow-population argument: **RC evaluated no credential pattern against ANY population, so even a STAGED key committed clean and `git add -f` walked through the `.gitignore` fence.** **RC's own 2026-09-12 pass over that same bucket measured size and read nothing** - a grep of it for `secret|credential|api.?key|PII|password|email` returns EMPTY. That is four trees with the same defect. **A sweep that measures a directory without reading it produces a number, not a verdict.**

**TWO ATTRIBUTION TRAPS FIRED IN ONE PASS.** Using `CLAUDE.md` / `ROADMAP.md` / `LEDGER` as RC identity markers over-counted **5.7x to 166 files and attributed the SIBLING's key-bearing file to RC** - those are shared CONVENTIONS across all six trees, not identity. Then two independent marker families each returned **18 files whose sets overlap in only 6**, union 29. **A count that agrees with a prior count is not corroboration.** 296 of 540 files are unattributable to any tree, so every per-tree figure on the channel is a FLOOR.

**RM-478: THE STOP GATE PUNISHED SCRUPULOUS REPORTING.** `push_claim_without_push` keyed on a bare `\bpushed\b` and could not tell a first-person assertion from an attributed report; it fired twice on this session, and one of the two flagged sentences was the session CORRECTING its own over-claim. True positives were pinned GREEN against the UNMODIFIED predicate FIRST. **Mutation control run by the MERGING session because the slice never reported one:** baseline exit 0 / 20 passed, mutant exit 1 / **9 failed, 11 passed**, bytes restored sha256-identical. **The FIRST attempt at that control was itself invalid - the mutant did not PARSE, so it exited non-zero for the wrong reason and proved nothing.** The probe now compiles the mutant before running it.

**MISSION CONTROL CLOSED BY OPERATOR DECISION** - verbs stay, headless, nothing further deleted. **The operator's condition ("if it is not messing with anything") was MEASURED before the row closed**, not assumed: task Running, listener on one `pythonw` pid at a **1.9 MB working set and 0.4 s total CPU**.

**A STANDING OPERATOR GRANT WAS RECORDED IN CLAUDE.md:** delivering a channel REPLY into a sibling `moon_sync_inbox/` is pre-authorised and does not halt. **NARROW** - the push half and the byte-pinned-artifact half still halt. Three notes delivered this session, **every destination re-hashed after the write, reached 5/5 each time.**

**CI TRAP WORTH CARRYING:** `docs-guards` and `ci` both went RED on one citation defect - the bucket-scan spec cited the LOCAL, GITIGNORED Claude settings file in backticked path-plus-line form, and because that file is untracked the citation guard correctly read it as pointing at something that does not exist. **It cannot tell UNTRACKED from ABSENT.** Fixed at the SOURCE DOC per the `f4a742fd9` precedent, never by widening `_KNOWN_BROKEN`. **AND IT HAPPENED A SECOND TIME, IN THIS VERY PARAGRAPH:** the first draft of this note spelled that citation out to explain it, so the note reproduced the defect and reddened the guard again. **A note about a forbidden token must not contain the token** - the identical lesson landed the same session when an explanatory code comment quoted the byte trio that halts the sibling sweep. **THE DEEPER MISS WAS SEQUENCING, and it is the one to carry: the citation guard was run GREEN, then this file was edited, then the commit went out.** A guard result is only valid for the tree it ran against; re-run it after the LAST edit, not after the last edit you happened to be thinking about. The full dual suite was otherwise clean in CI: **1 failed, 34989 passed, 275 skipped, 18716 subtests in 919 s** - so Round B's code change broke nothing.

## NEXT SESSION

See `RC-NEXT-SESSION.txt`, which is the hand-off. **Most pressing: `ROADMAP.md` and `CLAUDE.md` are BOTH at 89.7 percent of budget, at the warn line** - a ninth relocation pass is due and was deliberately NOT started here rather than half-done. Then: the periodic NON-STAGED credential scanner (the credential arm closes the agent-write population only and would NOT have caught one of RC's 29 bucket files); `precommit_gate.py` still evaluates zero credential patterns; `ops/local_paths.json`, which `.gitignore` annotates as holding a bearer token, was NOT opened and sits in the blind spot.
