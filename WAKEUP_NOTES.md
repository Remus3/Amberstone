# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-08-14, orchestrated-run docs sync (relocated BOTH `2026-08-12` blocks - RM-190 decided + the 3-day-outage recovery; newest 3 = orchestrated run `2026-08-14` + new-project design QA `2026-08-13b` + /sync-all-md `2026-08-13`). NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`2f35163d`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired.

# 2026-08-30n - lane 8 cycle 24: the atomic-write contract was not atomic, and its "always returns a dict" reader raised

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 12/12 mutation-killed, verifier-gated.** Target `core/polled_json.py` (153 -> 218 lines, not frozen) - **the module CLAUDE.md points every new polled-JSON site at**, imported by 20 non-test modules. Closed **RM-254**, which lane 8's OWN cycle 22 had filed as "the one to do first". Full detail in `docs/LEDGER.md` 1283; RM-261/262/263/264 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`. **Next free id = RM-265.**

**MINTING THE ID ALMOST WENT WRONG IN A NEW WAY.** My repo-wide max-id scan returned RM-261 as taken, so I nearly filed 262-264. It was matching the PROSE `Next free id = RM-261` in cycle 23's own ROADMAP row, not a filed row. That is CLAUDE.md's anchored-predicate warning ("a bare grep matches prose ABOUT the trailer") landing on id minting. **Read what the match actually says before treating a number as consumed.** The tracker (`docs/DS_SWEEP_TRACKER.md`) is still stale at RM-232 - the already-filed RM-192 staleness, now three cycles old.

**THE HEADLINE: the module that exists to prevent torn reads was itself producing them.** All three public helpers derived the scratch filename from the DESTINATION alone (`path.with_suffix(path.suffix + ".tmp")`), so two writers of one file opened the SAME scratch file - B truncating and renaming it while A was still filling it. **Measured before any edit, two threads, 240 writes: 5 reads came back as unparseable JSON**, from the module whose docstring promises "readers see either the old content or the new, never a partial write". **Reachability turned out worse than RM-254 had recorded:** `data/force_scan.json` has FOUR writers, not the two the row assumed, and the fourth (`tools/lcu_agent.py:1372`) is a SEPARATE PROCESS - the case no in-process lock can serialize, which is why the pid is in the new scratch name and not just a random token.

**FOUR MORE DEFECTS IN THE SAME FILE, AND ONE HAD BEEN DOCUMENTED BY ITS OWN CONSUMERS FOR MONTHS.** `read_json_dict` promised the default for an "unreadable" file but let `UnicodeDecodeError` escape, because that is a `ValueError` and the handler caught `OSError`. **Three downstream sites already said so in prose** - `core/cost_tracker.py:491` spells out the exact escape, `coaches/_base_coach.py:502` and `:540` each widened a handler to absorb it. Nobody fixed the resolver. That is `feedback_resolver_fix_is_not_a_consumer_fix` with the evidence sitting in the codebase in plain English. All three comments were corrected here, since they now describe behaviour that no longer exists. Also fixed: a shallow `dict(default)` in both `read_json_dict` and `PolledJsonFile.__init__` that leaked nested mutables into the caller's own object, and an exhausted retry that orphaned the scratch file (which HAD to ship with the naming fix - a unique name that is never cleaned up is unbounded litter rather than a bug).

**THE MUTATION PASS CAUGHT A BAD MUTANT OF MY OWN.** 12 mutants, sha256-verified restore each time. First run 10/12: M8 survived because the constructor's deep copy is redundant for `read()` - not equivalent though, it still decides whether construction SNAPSHOTS the caller's dict, so it earned a test. **M10 was my error**: I "mutated" a line by appending a comment, which changes nothing. Rebuilt as three real mutants returning the caller's `default` by reference from each fallback branch - which then exposed that my own W2 test only covered the missing-file branch. Parametrized across missing/malformed/non-dict/undecodable. Final 12/12.

**READING THE ROW'S ACCEPTANCE CRITERIA IS WHAT CAUGHT MY WEAKEST CLAIM.** RM-254 asked for a two-PROCESS test; I had written a two-THREAD one and was about to close on it. The deterministic cross-process guard I then added goes RED under a revert. **The behavioural two-process contention test is recorded as a SMOKE test, not a guard, because I measured it: against the same revert it went red on only 1 of 2 attempts.** Kept anyway (it asserts an absence the fix makes structurally impossible, so it cannot flake red), but labelled in its own docstring so nobody trusts it later. Its two-thread sibling passed pre-fix and is labelled the same way - the cycle-22 lesson applied without having to relearn it.

**THE CONSUMER TRAP THIS CREATED, INCLUDING ON MY OWN LANE'S PREVIOUS WORK.** Three callers had hand-mirrored the helper's PRIVATE scratch name to clean it up after a failure. Two would have silently become no-ops: `dashboard/routes_loop_control.py:177-186`, and `performance_tracker.py:64` - **written by lane 8 cycle 23, five commits earlier on this branch**, with a comment explaining why it mirrored the name. `missing_ok=True` means they break quietly. Fixed all three plus `coaches/arena_coach.py`, the hand-rolled third `force_scan.json` writer that had no retry at all. **Changing a private detail of a shared helper is a breaking change when callers have encoded it.**

**FILED, NOT SWEPT: 96 modules still hand-roll the same destination-derived scratch name** (RM-261). Fixing the contract module reaches NONE of them. Sweeping 96 files is a lane-7-shaped commit, so the row names the small exposed runtime subset instead and notes it is the same writer set as RM-258 - convert each site once, satisfy both. Also filed: RM-262 (four U+2192 arrows in a LIVE Haiku prompt string at `coaches/arena_coach.py:159-160`, proven pre-existing 4-before/4-after against `git show HEAD:`, left alone because rewriting prompt bytes changes coaching output) and **RM-263, the limit this slice did NOT close: there is no `fsync` anywhere in the module**, so it is atomic against a concurrent reader but not against a crash - and `taskkill /F` is CLAUDE.md's prescribed restart path, so that window is exercised routinely. **RM-264 qualifies this slice's own work:** `PolledJsonFile` has ZERO production instantiations, so the W3 half of the shared-mutable fix is LATENT while the `read_json_dict` half is live. Said plainly rather than left to read as bigger than it is.

**THE VERIFIER RETURNED REFUTE AND EVERY PROSE FINDING WAS REAL.** It CONFIRMED the engineering independently - its own mutation matrix (W5 revert kills 3-4 tests, W4 4, W1/W2 9, W3 1), five adversarial failure paths with zero scratch litter, 20000/20000 unique names, zero frozen files, `SHARED_SHA256` untouched. Then it found five prose defects that were on disk. **The one worth carrying forward: I fixed the broken line-69 citation of `dashboard/_writers.py` in the `.md` files because that is what the citation guard reported - and the guard scans `.md` ONLY, so three `.py` copies of the same broken citation survived.** Fixing what the guard reports is not fixing the defect (`reference_guard_domain_narrower_than_its_name`). It also caught that I wrote "three writers" in the contract module and "FOUR" in `arena_coach.py` in the SAME slice, with the module omitting exactly the cross-process writer its own docstring calls the motivating case; that `coaching_data.json` has three writers not two (`coach_integration/_coach.py:713` is unconverted, now in RM-261); that my W2 narrative read as an observed incident when it was a probe; and that the torn-read caveat was in the ledger but missing from the source comment. All fixed before commit. **One finding I re-measured and did NOT adopt:** it put the pre-slice baseline at 19473, but its own figures do not reconcile (19508 reported, 19473 + 33 = 19506 computed). Mine was taken before any edit and the delta is exact, so 19475 + 33 = 19508 stands.

**HAND-OFF FOR THE MERGER.** Baseline taken BEFORE any edit: RC `2 failed / 19475 passed / 144 skipped / 4560 subtests`. Both failure node ids (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`) are byte-identical pre and post, inherited branch lag - the same pair cycle 23 recorded. DS `10684 passed / 13482 subtests / 0 failed`, unchanged. **Take the baseline BEFORE editing; it is what makes "not mine" provable rather than asserted.** One process note worth keeping: I launched a suite mid-slice and then made two more edits, which invalidated that run - I stopped it and re-ran rather than reporting a number measured against a tree that no longer existed. Backfill measured EMPTY with the probe as evidence (no stray `.tmp` anywhere under the live data/runtime roots, every polled file decodes as UTF-8 and parses). **No RC restart was done and none would have helped:** live pid 23644 runs the MAIN-tree copy, so a bounce from this worktree deploys nothing (item-1179 trap) - **until the merge lands, the running process still shares one scratch file across four writers of `force_scan.json`.** CI on this branch was ALREADY red on arrival (a 45-minute `Full Dual Suite` timeout on run 33327341297, plus an in-flight timeout probe) - inherited, noted at pre-flight, not caused here. Branch is ready and NOT merged; lane 8 ships last.

---


# 2026-08-30m - lane 8 cycle 23: the rating write lost a race with the process that reads it, and a null cost the whole save

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 11/11 mutation-killed, verifier-gated.** Target `performance_tracker.py` (439 lines, not frozen), picked on criterion 2 (it reads `API-Key-Claude.txt` at `:406`) and criterion 4 (no test module carries its name). Full detail in `docs/LEDGER.md` 1282; RM-258/259/260 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`. **Next free id = RM-261.**

**RECALL PAID FOR ITSELF AGAIN, second cycle running.** The first pick was `core/riot_api_cache.py`; `perseus_recall` returned item-1182, lane 8's OWN cycle-5 audit of that exact file. Cycle 20 hit the identical thing with `vision_server/_http.py`. The step is mandatory precisely because a hardened file reads exactly like an unaudited one - the evidence that it was already done is not in the file.

**Two real defects, both silent, both costing the operator data.** **W1** - `_atomic_write_json` ended in a BARE `os.replace` while `core/polled_json.py:30-35` already documented the WinError 5 hazard AND the same fix had shipped to `ops/rc_supervisor` on 2026-05-02; this module held a private copy that never got it. Measured on win32: against a reader releasing after 80 ms the bare writer raised in 0.6 ms, the hardened one landed its content. Triple-masked - warning-only log, control falls through so the DB row still lands, `app/_game_lifecycle.py:236` logs "Performance rating saved" anyway, and the stale-mtime file then feeds `agents/supervisor.py:799-805` a PRIOR game's rating into this game's summary. **W2** - `.get(k, default)` only defaults a MISSING key, so a present null flowed into the arithmetic: all six of `ally_kills_total`, `cs_per_min`, `gold`, `game_seconds`, `deaths`, `kills` crashed `save_rating`, and the frozen caller's `except Exception` then ate the rating AND the match row. Fixed with a `_num` boundary coercion, which had to live here because the caller is frozen. **Then the mandated sibling grep found the same root cause in `save_tft_rating`** - null `alive_others` and null `stage` both crash it, and TFT is 8041 of the 8395 rows in the live store, so the sibling is the bigger exposure. The `stage` case is reachable only when `stage_round` fails to parse, so the natural probe hides it.

**The mutation pass is the part worth reading.** 10 of 11 killed on the first attempt; **M8 survived** - deleting the `bool` guard in `_num` left every test green. Not an equivalent mutant, so it got a test (`TestNumCoercion`) rather than a shrug, after which M8 and a new M9 both went RED; M10/M11 on the TFT siblings were killed outright. Exactly `feedback_guard_on_nondefault_call_path_is_untested`, caught by the pass instead of by an incident.

**W3 severity was DOWNGRADED by measurement and I am recording it that way.** The outer handler logged an exception from a callee holding the API key, and an injected raiser proves it WOULD leak. But the live path is refuted twice over: `_call_haiku` catches everything at `experimental_builder.py:236`, and the anthropic SDK's exception surfaces are clean under `%s`. The redaction shipped as defence in depth only, because `repr()` of the `UnicodeEncodeError` from a non-ASCII key file DOES carry the key - the margin is one formatting change wide. The hook is also DORMANT (`mark_active()` has zero callers), which is why the dead key read was FILED (RM-260) and not deleted.

**HAND-OFF FOR THE MERGER.** Suites observed THIS run from the repo root: RC `tests/` **2 failed / 19475 passed / 144 skipped / 4560 subtests**; DS **10684 passed / 13482 subtests / 0 failed**. The baseline taken BEFORE any edit was **2 / 19460 / 144 / 4546**, so the delta is exactly +15 passed / +14 subtests - the new tests one for one - and both failure node ids are byte-identical across the two runs (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`), inherited branch lag. **Take the baseline BEFORE editing; it is what makes "not mine" provable rather than asserted.** No RC restart was done and none would have helped: live pid 23644 runs the MAIN-tree copy, so a bounce from this worktree deploys nothing (item-1179 trap) - **until the merge lands, the running process keeps losing a rating to any concurrent reader and keeps dying on a null field.** Backfill measured EMPTY and the probe is the evidence: no stray `.tmp`, no failure signature in 15 days of logs, and every mode's rating file content matches its newest `match_history.db` row. A `sk-ant-` hit in `logs/agents/supervisor.log` was characterised by length and a masked stub without printing any value - RC's own test canaries, not key material. **Verifier returned CONFIRM** after independently re-running both suites, running a TRUE mutation of the replace line (current lands `NEW` in 77.6 ms; mutant raises `PermissionError` in 1.0 ms and disk keeps `OLD`), and proving against a `git show HEAD:` control that all six nulls really did raise before the fix. It also positively root-caused both inherited failures (live `:8860` serves engine `1.278.1` vs the branch pin `1.278.0`; `claude --version` is 2.1.251 vs the 2.1.220 pin) - environment drift, zero causal path to this edit. **Two honest scope notes:** the verifier correctly caught that my claimed file list understated the tree (the doc filings landed mid-verification), and its CONFIRM predates the TFT sibling fix, which I measured and mutation-tested myself afterwards - the +15/+14 suite delta above is the post-TFT run. Branch is ready and NOT merged; lane 8 ships last.

---

# 2026-08-30l - lane 8 cycle 22: the store ate the operator's builds, then served a build that was never saved

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 11/11 mutation-killed, verifier-gated.** Target `coaches/sr_user_builds.py` + its caller `dashboard/routes_sr_user_builds.py`. LEDGER 1281. Filed RM-253 / RM-254 / RM-255 / RM-256.

**I CHECKED THE SELECTION RATIONALE BEFORE PAYING FOR IT, WHICH IS THE CYCLE-21 LESSON LANDING.** The module looks untested if you grep `tests/` for `test_sr_user_builds` and stop. It is not: `tests/phase8_smoke/test_sr_user_builds.py` has 20 tests and `tests/test_loadout_user_builds_merge.py` another 20. Criterion 4 was REFUTED and the file was audited on criteria 1 and 3 instead. Cycle 21 recorded this trap AFTER paying for it; this is the first cycle that paid nothing.

**THE HEADLINE DEFECT DESTROYS DATA AND ONLY LOGS A WARNING.** `_load`'s blanket `except Exception` turned an unparseable `user_builds.json` into an EMPTY store, and the next `add()` wrote that empty store over the file. Probed end to end: a seeded Tristana build was on disk before the call and absent after. The fix quarantines by RENAMING to `user_builds.corrupt-<stamp>.json` - which preserves the bytes exactly and is self-limiting, because the next `_load` then hits the does-not-exist branch instead of re-quarantining per read.

**THE SECOND DEFECT SERVES A BUILD THAT WAS NEVER SAVED.** `_load()` returned the SHARED cache dict and the SHARED per-champion list; mutators edited it in place BEFORE `_save`. Force `os.replace` to fail and `add()` raises, the route answers 500 - and `list_for` still reports the "ghost build". That is not cosmetic: `coaches/rune_pages.py:142` folds `list_for` output into a live rune page. One root cause covered both this and the unlocked-reader race: mutate a private deep copy, publish the cache only after the write lands, and hold the lock across read AND copy (`_STORE_LOCK` became an `RLock` because the read path legitimately nests).

**TWO OF MY OWN TESTS WERE WEAK AND I KEPT THE RECORD RATHER THAN THE APPEARANCE.** `test_concurrent_reads_and_deletes_never_tear` was ALREADY GREEN pre-fix - the GIL hides the tear - so it is a smoke test, not the guard; the guard for that weakness is the structural `test_load_does_not_hand_out_the_shared_mutable_cache`, and the mutation run killed it. Separately my first W8 test asserted the ABSENCE of the string "16M", which then failed against a corrected docstring that QUOTES the old claim while fixing it. A negative assertion rules out without pinning down; I replaced it with a positive pin on the corrected magnitude.

**I ALMOST SHIPPED A SEVERITY CLAIM THE LIVE SYSTEM REFUTES.** W3 (non-dict POST body) measured as a 500 when I called `_serve_user_builds_post` directly, and I wrote it up as a live defect. Then I probed the actual dashboard: `POST /api/sr-draft/user-builds` with `["not","a","dict"]` returns **400**, because `dashboard/_handler.py:525` already rejects every non-dict POST body at the single trust boundary for all ~40 routes, and `mc/routes.py:16` splices only `routes_loop_status` + `routes_loop_control` into `:8895` so this route has no second surface. The guard I added is defence in depth and nothing more. The function-level measurement was CORRECT and the severity inference from it was WRONG - `feedback_verified_claim_vs_measured_downstream`. Corrected in the ledger and the commit message rather than left to read stronger than it is. **The general rule this cost me: a direct-call probe measures the FUNCTION, not the SURFACE. Probe the surface before assigning severity.**

**THE FOUR FILED ROWS ARE NOT IN THE AUDITED FILE, AND I READ EVERY CITED LINE MYSELF.** A shortlist agent surfaced them; a shortlist is not evidence. RM-254 is the one to do first: `core/polled_json.py` IS the repo's atomic-write contract and derives its tmp name from the destination alone at `:57`, `:75`, `:87`, so concurrent writers share one scratch file - the module cannot deliver the atomicity every other module is told to get from it. RM-253 is the cheapest: cycle 20 fixed the raw-exception echo at `vision_server/_relay.py:50-56` yesterday and left the identical sibling at `:177`.

**ID MINTING: the tracker would have collided.** `docs/DS_SWEEP_TRACKER.md` tops out at RM-232 while `ROADMAP.md` already used RM-252. The lane skill says mint from the tracker; doing that literally would have produced a duplicate id. Minted from the repo-wide max instead. This is the already-filed RM-192 staleness, still open.

**Suites (repo root):** RC `tests/` 19460 passed / 2 failed / 144 skipped, against a pre-slice baseline of 19436 / 2 / 144 measured the same way - the delta is exactly the 24 new tests and the two failures are IDENTICAL node ids in both runs (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`), inherited and unrelated. DS 10684 passed / 0 failed. Ruff clean tree-wide. No RC restart needed - no live runtime path changed. Backfill: the live store was probed and is healthy (2 champions, 4 builds, no cap violations, no stray tmp), so there was nothing to repair.

---

# 2026-08-30k - lane 8 cycle 21: the halt button lost a race with the thing it was halting

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, mutation-tested, verifier-gated.** Target `dashboard/routes_loop_control.py` (491 lines, not frozen) - the privileged remote-control surface that halts the headless loop from the phone. Full detail in `docs/LEDGER.md` 1280; RM-250/251 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`.

**MY SELECTION RATIONALE WAS WRONG AND A GREP TOLD ME SO.** I picked the file partly as "load-bearing and untested" because `ls tests/ | grep routes_loop_control` returned nothing. The tests are called `test_loop_control_*.py`. 72 test modules mention this file; one of them has 48 tests. That is `feedback_empty_grep_is_a_claim_about_the_pattern` landing on the target-selection step rather than on a finding - cheap here because the other two criteria held and the audit stood, but a naming convention I did not check nearly bought a whole cycle of "this file has no tests" reasoning. **Check the naming convention before concluding absence.**

**THE DEFECT: the emergency halt lost a coin-flip against the poller it was halting.** `_awrite` hand-rolled `tmp.write_text` + a bare `os.replace` while its OWN docstring said "loop_controller polls mid-write" - naming the exact condition under which Windows `os.replace` raises `PermissionError` WinError 5, because a reader holds a share lock. `core/polled_json.py:39` `_replace_with_retry` had existed for this the whole time, and `dashboard/_writers.py:29-36` already recorded a measured instance. I proved it with a throwaway probe BEFORE editing: `stop` under a held reader raised, the catch-all turned it into HTTP 500 "internal error - see logs", the halt did not land, and `STOP.tmp` was stranded - which `test_atomic_no_tmp_left` exists to forbid but never catches because it never drives the contended path. **The docstring named the hazard and the code did not handle it; the test named the invariant and never exercised it.**

**MY FIRST TEST WAS UNREALISTIC AND THAT WAS THE USEFUL PART.** Holding the file open for the WHOLE write attempt kept the test RED after a correct fix, because a bounded 275 ms backoff is SUPPOSED to lose to an infinite reader. Realising that split one test into two: a `_transient_reader` that releases inside the window (proving the retry works) and a held-open reader (proving the exhaustion path). The exhaustion path is what produced the best fix of the cycle - a 503 "control file busy - the loop is mid-poll, retry" instead of a generic 500, because the operator must be able to tell "press it again" from "the server is broken".

**A MUTANT SURVIVED AND THE BUG WAS IN MY FIX, NOT MY TEST.** I had written the tmp cleanup as two lines that resolve to the same path, so deleting one left the other working. The instinct is to strengthen the test; the correct move was to delete my redundant line and re-run the mutant, which then went RED (`reference_mutation_survivor_may_be_equivalent`). Final tally 7 mutants, 7 RED, restore sha256-verified after each.

**TWO OF MY FINDINGS WERE REFUTED BEFORE THEY BECAME FIXES, BY A PROBE I DISPATCHED TO ATTACK THEM.** A `run['pid']` KeyError and a `payload.update()` that un-bools a carefully-`bool()`-ed field. `launch_lane` has exactly one return path and raises unless `pid` is truthy; `try_acquire_lane` returns literal bools on all three paths. Both would have been edits to CORRECT code, and both read as obvious defects from inside the file alone. **Reading a caller is not reading its contract.**

**THE TRUST-MODEL DOCSTRING WAS WRONG IN THE SAFE DIRECTION - STILL A DEFECT.** It described the `:8888` perimeter; S10 Task 9 de-registered the route, so the real perimeter is Mission Control `:8895` (narrow bind, mandatory TLS, bearer token failing closed) - STRONGER than documented. Worth correcting anyway, because the next reader could relax this route believing it is only as guarded as `/api/command`, whose gate is inert until `RC_DASH_TOKEN` is set. The same measurement corrected a stale claim inside filed row RM-239.

**THE VERIFIER SAID REFUTE AND SAVED ME FROM TWO REAL DEFECTS - ONE OF THEM A REPEAT OF THE LESSON I WAS WRITING UP.** It measured 3 suite failures where I reported 2, and the extra was MINE: my new ROADMAP/BACKLOG/LEDGER rows cited `test_mission_control_server.py` at a line range running past its end (the file is 476 lines). I took that range from a probe subagent and pasted it forward without opening the file - in the same cycle whose headline finding was that a subagent's cited contract must be re-probed. The citation drift guard caught it; corrected to 468-476. **I re-verified the fix against the file myself rather than trusting the verifier's proposed range either.** It also flagged my new `except RuntimeError` in `_steer` as broader than `MutexTimeout`, which would report a genuine bug as "busy - retry" - hiding the fault and prescribing a useless remedy. Narrowed by name, +1 test, +1 mutant. **Neither was visible from the diff: one needed a suite I had already run and mentally filed as green, the other needed someone asking what ELSE the catch swallows.**

**Suites fresh from the repo root: RC 19436 passed / 144 skipped / 2 failed; DS 10684 passed.** Both RC failures are inherited and I PROVED it rather than asserting it - reverted the three changed files to pristine HEAD, re-ran exactly those two, both still failed, restored with sha256 re-checked. **Not merged; lane 8 ships last.** The live `:8895` runs the main-tree copy, so until the merge the running server still 500s a halt that loses the race with its own poller.
