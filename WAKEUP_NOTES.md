# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary archived to docs/history_notes.md. Only the last 3 sessions kept here (2026-05-20 housekeeping batch + 2026-05-19/20 overnight FULL RUN + 2026-05-20 second headless run).
---
# 2026-05-20 - housekeeping batch (7 commits; non-engine; no DS bump)

User-driven scoped session: 4 picked off the menu, 3 carries from earlier. **7 commits `1a8e0bf`..`58d1e87`**, all pushed.

**Branch + worktree cleanup:**
- `gh pr list` empty; 4 local + 1 remote stale branches deleted (`worktree-agent-a6af6d373393c54c3` + `fix/ds-stat-growth` + `claude/brave-curran-2597b1` + `origin/fix/ds-stat-growth`); they were all 0-ahead of main.
- Locked worktree `worktree-agent-a7da71cb6830f84fc` (107MB, commit 18589ab on main) unlocked + force-removed; corresponding branch deleted.

**Desktop shortcut Claude RC.lnk repointed** to launch Claude Desktop (MS Store AUMID `Claude_pzs8sxrjxfjjc!Claude` via `explorer.exe shell:AppsFolder\...`) instead of the CLI tools/claude-rc.ps1; smoke-launched pid 1368 "Claude" window. **`tools/claude-rc.ps1` deleted** + docstring ref in `tools/verify_bridge_roundtrip.py` updated (`3571e1b`).

**Dated docs archive (`3571e1b`):** 4 of 6 dated 2026-05-02 `docs io RC peer/` artifacts moved to `docs/_archive/2026-05-02-rc-peer/` per CLAUDE.md convention; 3 doc-only ref updates in `core/lessons_{ack_watcher,confidence,revert}.py`. 2 files LEFT IN PLACE because of frozen-file refs (main.py for `PEER_VIP_BRIDGE_MONITOR_FOR_RC_2026-05-02.md`, sync-all-md.md for `RC_PHASE1_LESSON_SCHEMA_2026-05-02.md`).

**`943d2e0` fix(ddragon): retry-with-backoff on transient HTTP errors.** RC-DDragonMirrorRefresh exit_code=2 root cause was 1/6767 assets timing out per cron tick (profileicon/5668.png in the 03:30 run). `_http()` now retries `URLError` (incl. socket.timeout) + HTTPError 429/500/502/503/504 with 1s+2s backoffs (max 2 retries). HTTPError 4xx is NOT retried. +4 tests. Live-verified: prior run hit 1 timeout -> exit 2; re-run with retry -> 0 fails, exit 0.

**`4808f64` chore(gitignore): exclude `_scratch/`** (~1MB of pytest output + iter logs from prior headless run; mirrors `_archive/` quarantine pattern).

**`8f7a71b` feat(ds): expose cdragon calculations on Augment dataclass.** Item 109 carry (d) phase 1. `Augment.calculations: dict` field + `__post_init__` None->{} default + `calculation_keys` property; `from_record()` now captures the cdragon `calculations` formula defs (was being trimmed at the Augment boundary; upstream extractor `tools/daemon_slayer_extract.py:762` already captured them). 83/220 augments at cdragon 16.10.1 ship non-empty calculations (UndyingGuard/Typhoon/ServeBeyondDeath...). NO ENGINE bump - dataclass-layer only, no scorer change, no DS restart. +3 tests. **Still owed:** the formula-evaluator that consumes `mFormulaParts` typed-part interpreter to displace the hand-maintained `_AUGMENT_STAT_OVERLAYS` registry; separate slice.

**`94fe721` + `8d1c28e` docs(backlog): prune + clarify.** BACKLOG 125 -> 105 lines, 0 strikethrough remaining. Dropped 14 shipped entries + dropped the stale `hps_split_aram_heal_shield_mults` entry (was actually shipped iter 17 `816ab4a` in item 109; BACKLOG drift). Rewrote `aramAbilityHaste`/`aramTenacity` entry to reflect actual state: exposure shipped (engine.py:210-221), CONSUMPTION still owed because the DS engine has no ability-haste model at all (`ability_dps.py:49` comment).

**`58d1e87` feat(coach): haste-formula cycle math for summoner cooldown ledger.** Replaced additive-percent CDR in `core/summoner_cooldowns.py` with Riot's canonical haste formula `eff_cd = base / (1 + h/100)` (the additive model double-counted at high stacks: Cosmic+Boots showed -28% when 36 SSH actually compresses to -26.5%). New constants `COSMIC_INSIGHT_SSH=18` / `LUCIDITY_SSH=18` / `LUCIDITY_AH=12` / `MAGICAL_FOOTWEAR_SSH=10` / `HEXTECH_DRAKE_AH_PER_STACK=5`. New helpers `_summoner_haste(runes, items)` / `_ability_haste(items, hextech_drakes=0)`; old `_cdr_summs` / `_cdr_ult` / cdr-fraction `_effective_cd` deleted. Participant dict: new optional `hextech_drakes: int` (defaults 0). Output: +`summs.summoner_haste` + `ult.ability_haste` for panel tooltips. Test suite 14 -> 29 tests (+4 Hextech Drake +3 haste-totals shape +1 negative-haste extreme). Flash @ Cosmic+Boots pin: 216s -> 220.59s (correct compression).

**Verified:** wider RC suite 2096 passed + 67 subtests; py_compile + ruff clean; ASCII-clean. No ENGINE bump (non-engine). No DS restart. No frozen-file edits. CI: not yet observed for the latest commits but local gates all green.

**Don't-redo:** all 7 commits real + pushed. The dated-docs archive deliberately left 2 files in place (`PEER_VIP_BRIDGE_MONITOR_FOR_RC_2026-05-02.md` + `RC_PHASE1_LESSON_SCHEMA_2026-05-02.md`) because moving them requires editing frozen `main.py` + `tools/sync-all-md.md`; the operator's frozen-file grant from item 109 does NOT carry forward. cdragon-arena calculations enrichment is HALF SHIPPED (dataclass-layer only); the formula-evaluator that turns `mFormulaParts` into per-augment stat overlays is the next slice and that will ENGINE-bump. CD ledger output now carries 2 NEW keys (`summoner_haste`, `ability_haste`) - dashboard panel was already forward-compatible since it reads `d_cd_remaining_s` etc., but the new keys can be tooltip-rendered.

**Carries forward:**
- (a) **`aramAbilityHaste` + `aramTenacity` engine CONSUMPTION** still queued in BACKLOG - exposure shipped iter 17 but no scorer reads them. Requires baseline ability-haste model in DS engine first (currently NONE per `ability_dps.py:49`).
- (b) **cdragon `calculations` formula evaluator** queued - the `mFormulaParts` typed-part interpreter (`NamedDataValueCalculationPart`/`StatByNamedDataValueCalculationPart`/`StatByCoefficientCalculationPart`/`NumberCalculationPart` + `mMultiplier`) to displace `_AUGMENT_STAT_OVERLAYS`. ENGINE bump owed when it lands.
- (c) **CD ledger Live Client wire for Hextech drake count** - `dashboard/_state_cooldowns.py` doesn't yet count Hextech drakes from `gameData.events.Events` (DragonKill with DragonType="Hextech"). Phase-2 wire.
- (d) Item 109 carries: HURTS-THEM/HELPS-US frontend, Per-row hover contribution strip, LoLytics WPA framework, Arena S2 Augment Level-Up (gated on patch 26.09 PBE), ward-heat frontend liveclient wire, shaper UI gated on 100% DS coverage, live-game UX validation, standing operator-gated ruff/type-hint pass.
- (e) The locked worktree pattern: when a Claude Code Agent worktree gets locked + abandoned, the cleanup is `git worktree unlock <path>; git worktree remove --force <path>; git branch -D <branch>`. Safe when the commit is already on main (verify with `git branch --contains <sha>`).

---
# 2026-05-20 - second headless run (UX waves 1+2 + research wave + DS audit iter 17-19; 12 commits; ENGINE 1.17.0 -> 1.21.0)

Second autonomous headless run on the same day. Operator pre-approved frozen-file grant + RC-wide coverage + multi-agent dispatch. **12 commits `f4938af`..`9f356a3`** (one same-run ruff-F541 fix `cb73757`), pushed, **CI 12/12 green**. ENGINE_VERSION **1.17.0 -> 1.21.0** (4 minor bumps). DS tests **2730 passed / 1 xfailed / 1663 subtests** (+20). Wider RC suite **2167 passed** (+50). Living synopsis at `C:\Users\Administrator\Desktop\RC_HEADLESS_SYNOPSIS_2026-05-20.md`. CLAUDE.md item 109 carries the full ledger.

**UX wave 1 closed `f4938af`:** ward-heat frontend strip (last item-108 carry-forward (a)). `web/js/panels/ward_heat.js` + CSS + mount in MAP head + producer wire to `core/liveclient_cache` via NEW listener API (`add_listener`/`clear_listeners` + `_fire_listeners` post-fetch in both sync + async loops) + `ward_producer.tick_from_snapshot` adapter + idempotent `install_liveclient_listener` wired in `main.py` (frozen, authorized). +22 tests.

**UX wave 2 shipped `ab3f553` + `00051f3`:** Elo log-odds team-vs-team draft aggregator (closes FUTURE entry from draft tool L (the community fork) triage). `core/draft_elo.py` pure math (winrate_to_rating = -400*log10(1/wr-1), team_score, DraftSide, pair enumerators) + `core/draft_elo_db.py` rewind queries composing on smoothed_rates.py + `dashboard/routes_draft_elo.py` ~50 SELECTs ~50ms cold + 5-min TTL cache + frontend chip `web/js/panels/draft_elo.js` with sample-density n=N/N indicator. +38 tests. Live `/api/draft-elo` ally=64,22,55,89,12 / enemy=42,67,69,33,99 -> ok=True predicted_wr=0.292 team_score=-154.19 min_solo=9.

**HURTS-THEM/HELPS-US ban-suggest `6e7b7e5`:** new `dashboard/routes_ban_suggest.py` returns BOTH ratings per candidate (solo + hurts_them via enemy-pair + cross-team matchup + helps_us via ally-pair) so frontend toggle is explicit - the "genuine market gap" surfaced by the research wave. Mode-selection logic stays on caller. +11 tests. Live champ_id=103 (Ahri) hurts_them=+71.8; champ_id=202 (Jhin) helps_us=+23.2.

**Multi-agent research wave (5 parallel slice agents):** LoL overlay landscape / champ-select UX / item-data sources / Arena+Cherry coaching / post-game review methodology. Triage LOCKED in `BACKLOG.md` "Multi-agent research wave triage 2026-05-20" section. NOW queued: LoLytics WPA framework (MIT) over Match-V5 timeline -> PGR S2; cdragon-arena dataValues+calculations enrichment in augments.py; MQTT-style haste-aware CD math from SummonerTrackerOverlay pattern. FUTURE: Arena S2 Augment Level-Up + Crafting Round patch 26.09 (schema break ahead); Draft Tool Z8/Jfghanimah/loldraft-analyzer (MIT) as second-opinion; Coaching App Z7 ensemble methodology read-only; LDP community tier (watch); CommunityDragon broader 7-file catalog; Riot per-role rubric; per-row contribution hover strip; Aggregator C 8-axis GPI spider. CLOSED: MobaHinted dead Sep 2024, LeagueAkari GPL-only, all spectator-stream OCR HUDs irrelevant, Arena augment WIN-RATE display BANNED by Riot policy 2024-04, persistent counter-pick sidebars (clutter), neural one-shot scorers (destroy provenance), draft tool L "+" fork (zero algo value), Fandom wiki stale, CommunityDragon/LeaguePatchNotes archived 2020.

**DS audit iter 17-19 (3 ENGINE bumps 1.17.0 -> 1.21.0):** Iter 17 `816ab4a` hps_split_aram_heal_shield_mults (item-108 carry-forward (a)) - `_aram_heal_shield_modifiers` returns (heal_mult, shield_mult) reading both Meraki keys with legacy aramShieldsHealing fallback; HpsResult gains heal_mult+shield_mult (mode_multiplier kept as backward-compat @property aliasing heal_mult); per-side application in compute_hps; 24 champs in 16.10.1 had split values (Camille 1.20/1.10, LeeSin 1.10/1.20, Milio 0.95/0.90, Nunu 1.10/1.20, +20 more); 1.17.0 -> 1.18.0. Iter 17 `6bba097` aramAbilityHaste + aramTenacity exposure (item-108 carry-forward (b)) - `_apply_mode_modifiers` writes `scaled['aram_ability_haste']` (flat delta, default 0) and `scaled['aram_tenacity_mult']` (multiplier, default 1.0) in ARAM mode; 22 champs non-zero AH + 17 champs non-1.0 tenacity; exposure-only no scorer consumes yet; 1.18.0 -> 1.19.0. Iter 18 `74d2ba6` Lich Bane Spellblade AP 50% -> 40% per Meraki bulk 16.10.1 ("75% base AD + 40% AP"); both 3100 SR + 223100 Arena mirror; Trinity Force/Sheen/Essence Reaver/Iceborn verified clean; 1.19.0 -> 1.20.0. Iter 19 `9f356a3` Nashor's Tooth Icathian Bite AP 20% -> 15% per Meraki ("15 + 15% AP"); 3115 + 223115 mirror; new value-pinned test `test_nashors_tooth_meraki_16_10_1_coef` (ap=100 -> proc damage 30.0); 1.20.0 -> 1.21.0. Iter 20-26 NO-CHANGE (Stridebreaker / Liandry / Luden / Hollow Radiance / Sunfire / Statikk-RFC / Profane Hydra - all verified clean against Meraki); halted at 7-of-11 no-changes because high-confidence drift candidates exhausted in audit window.

**/headless-upgrade skill `b007783`:** new `tools/headless-upgrade.md` (gitignored mirror at `.claude/commands/headless-upgrade.md`). 11-section skill captures: pre-flight baseline checklist, phase loop discipline (LOCAL ruff before push - the CI killer F541 caught same-run in `cb73757`, test gates per-domain, restart-aware reload, no `git add -A`, HEREDOC commits, CI verification after every push), frozen-file grant scope (this-run-only), ASCII hygiene hard rule, multi-agent dispatch protocol (single message multi-Agent for true parallelism, explicit don't-redo list in every prompt), DS audit iteration (11 no-change halt + Meraki source of truth + property-style tests), interrupt protocol, 10h+ extension trigger, 11 anti-patterns from past runs, final banner format.

**Don't-redo (this run's locks):** all 4 ENGINE iterations are real Meraki 16.10.1 corrections - do NOT revert; `mode_multiplier` @property on HpsResult is the backward-compat alias for heal_mult (SR/ARAM ratio test still works through it); engine's exposure-only posture for aramAbilityHaste + aramTenacity is intentional (no scorer reads them YET but the data lane is now visible); spellblade family + AP-scaling on-hit family are now Meraki-coherent through Lich Bane 40% AP + Nashor 15% AP; research wave's CLOSED list is authoritative (do NOT re-research MobaHinted/LeagueAkari/spectator HUDs/Arena win-rates - all license- or policy-blocked); HURTS-THEM/HELPS-US toggle FRONTEND deferred as operator-gated polish session; the n= chip on draft-elo is the calibrated-trust signal that other tools cannot ship (shrinkage primitive ours).

**Carries forward:** (a) HURTS-THEM/HELPS-US ban-suggest FRONTEND deferred (champ_select.js is 2815 lines; toggle chip + sortable candidate list is a focused polish session; backend wire-complete + tested). (b) Per-row hover contribution strip queued. (c) LoLytics WPA framework integration -> `dashboard/routes_post_game_wpa.py` + `core/post_game_score.py` aligns with s220 PGR S2 reframe. (d) cdragon-arena dataValues+calculations enrichment queued. (e) Arena S2 Augment Level-Up + Crafting Round (patch 26.09) schema break ahead; pre-stage `level` axis on `compute_augment_stats()` when 26.09 PBE confirms. (f) MQTT-style haste-aware CD math queued for `core/summoner_cooldowns.py`. (g) the 2 standing operator-gated follow-ups (pre-existing non-ASCII retro-sweep beyond done + deferred `ruff --unsafe-fixes` + type-hint pass) STILL operator-gated. (h) live-game UX validation for new draft-elo chip + ward-heat strip still pending. (i) frozen-file grant was honored minimally (only `main.py` + ward-producer wire); grant does NOT carry forward.

---
# 2026-05-19/20 - overnight autonomous run END-TO-END (Phases 1-3 complete; 31 commits; ENGINE 1.9.0 -> 1.17.0)

Single overnight autonomous run executed end-to-end in caveman ultra. **31 commits `021c6c4`..`98e059c`**, all pushed, **CI 31/31 green**. ENGINE_VERSION **1.9.0 -> 1.17.0** (8 minor bumps). DS tests **2710 passed / 1 xfailed / 1663 subtests**. Wider RC suite ~2087 / 0 failed. Operator-authorized FULL read/write incl. every CLAUDE.md frozen file - the grant was honored by NOT touching any frozen file (every fix routed around the frozen list). Full off-repo synopsis at `C:\Users\Administrator\Desktop\RC_OVERNIGHT_SYNOPSIS_2026-05-20.md`. CLAUDE.md item 108 carries the full ledger.

**DS audit: 18 iterations, 8 real bugs caught + fixed (8 ENGINE bumps).** Iter 1 `3013500` multi-source %-pen composition was ADDITIVE - fixed to multiplicative (1.9.0 -> 1.9.1). Iter 2 `08c6e03` lethality V14.1 - removed legacy level-scale factor, 1:1 flat pen now (1.9.1 -> 1.10.0). Iter 3 `0ff658a` `hydra_cleave` unique-passive family stops Titanic + Stridebreaker double-proc (1.10.0 -> 1.10.1). Iter 7 `54c40a8` BotRK Mist's Edge melee 8% -> 9% target HP per Meraki (1.10.1 -> 1.10.2). Iter 8 `a9a0bb9` Immolate family per-tick base 14 (was 12) + Titanic Cleave Meraki drift + 8 Arena mirrors (1.10.2 -> 1.11.0). Iter 10 `6fac6cc` The Collector lethality drift - `defensive_only=True` was hiding leth=10 (1.11.0 -> 1.12.0). Iter 11 `884ef06` Statikk Shiv Electrospark 110/3s -> 180/10s per Meraki (1.12.0 -> 1.13.0). Iter 12 `0c550b5` Guinsoo's Wrath flat 30 magic on-hit was missing entirely (1.13.0 -> 1.14.0). Iter 14 `a4ddcb8` Arena mirror audit - Collector 226676 + Guinsoo 223124 sync to SR fixes (1.14.0 -> 1.15.0). Iter 15 `2c38c2d` Hextech Alternator 75 -> 65, Voltaic Firmament `100 + 25% bonus_ad` -> 100 flat phys (1.15.0 -> 1.16.0). Iter 16 `1f1a8d6` structural-drift resolution - Sundered Sky calibrated to `70 + 80% total_AD`, Dead Man's flipped to `defensive_only` with deferral comment (1.16.0 -> 1.17.0). Iter 4-6 + 9 + 13 verifier-clean; iter 17-18 NO-CHANGE with 2 structural drifts queued. Streak at session end: 2/10.

**5 UX backends shipped, 4 with frontends.** Threat tags `0d04829` `/api/personal-vs` (lifetime SR-only WR vs hovered enemy, 60s TTL cache, bands red/amber/green/unknown) - frontend wired same commit. Damage-mix donut `c7698d9` `/api/damage-mix` (per-enemy AD/AP/true) - frontend `de4a2c0` 4-slice SVG donut in Active Match build pane. Spike-curve `ca07e9f` `/api/spike-curve` (team power-curve ally-vs-enemy DPS+EHP across levels 1/6/11/16/18) - frontend `18589ab` 40px sparkline panel above BUILD. CD ledger `97c29f5` + `7cdc681` (`core/summoner_cooldowns.py` watches LCU + chat for "ss top" / "f used" pings) wired into `/api/state` - frontend `8c962f6` right-rail CD ledger panel. Ward-heat `ccfe1c8` + `19a33ef` (`/api/ward-heat` backend + `core/ward_producer.py` infers placements from allPlayers inventory delta) - frontend DEFERRED to operator (highest-info-density UX, wire needs `core/liveclient_cache.py` integration). All 5 UX research designs (aggregator-A-style donut / aggregator-C-style sparkline / overlay app F-style ward heat / overlay app E-style CD ledger / aggregator-B-style threat tags) referenced.

**Shaper backend primitive `c6327d8`:** `core/shaper.py` `ShaperState` + `apply_shaper(weights, shaper) -> dict` pure function (NUDGE_STEP=0.075 per knob click, clamp [0.05, 0.90], renormalize, degenerate-guard); 57 deterministic property tests over full 125-combo knob space x 5 archetype baselines; does NOT touch scorer source-of-truth in `agents/daemon_slayer/rank.py`. UI layer gated on 100% DS coverage.

**pytest_guard docs-skip fix `89d4eb4`:** `tools/pytest_guard.py` empty/unknown payload now correctly SKIPS pytest (was running fail-safe + blocking docs-only commits). 22 new gate tests pin docs-only skip / code-only run / mixed run / empty skip / invalid-JSON skip / MultiEdit edits[] enumeration.

**Active Match step 5 sub-unit `7767126`:** dead fleet-view JS poll + CSS stripped (~158 LOC `web/js/main.js` + ~99 LOC `web/css/panels/bridge_pending.css` + 3 selectors `header.css`); `/api/health/peer` + `/api/health/all` rollup retained; **remaining step-5 count flipped 5 -> 3**.

**ASCII retro-sweep `021c6c4`:** 5 files purged of stray non-ASCII glyphs (50+ hits cumulative).

**Don't-redo:** all 8 ENGINE iterations real Meraki/V14.1 patch-grounded - do NOT revert; iter-1 multiplicative %-pen compose is engine-authoritative, do NOT re-pitch additive; iter-2 lethality 1:1 flat-pen is post-V14.1 doctrine, do NOT re-pitch level-scaled; iter-3 `hydra_cleave` family extension stops Titanic + Stridebreaker double-proc, do NOT re-pitch planner-side family map; iter-16 Dead Man's `defensive_only` flip is intentional (safer than stale flat-magnitude estimate), do NOT undo until stacks-schema session; iter-16 Sundered Sky calibration is calibrated value; 4 UX frontends wire-complete and verified headless via fixtures; shaper primitive intentionally pure-function; fleet view removal final (do NOT re-add JS poll; backend rollup KEPT); pytest_guard fix validated mid-run; no-frozen-file constraint honored despite blanket grant (grant does NOT carry forward); ward producer shippable in isolation.

**Carries forward:** (a) **iter-17 `hps_split_aram_heal_shield_mults`** queued - `hps.py:245` collapses `aramHealing`+`aramShielding` into one `mode_multiplier`; 4 champs Camille/LeeSin/Milio/Nunu have split values; fix needs `HpsResult.mode_multiplier` -> `heal_mult` + `shield_mult` + ripple to `hybrid.py` and 4 test files. (b) iter-17 also flagged `aramAbilityHaste` (22 champs incl. Soraka +10) + `aramTenacity` (17 champs) not consumed by engine. (c) **iter-15 Dead Man's stacks-schema deferral** - Plate Momentum stacks need stack-accumulation schema in periodic-proc family; queued for Phase-2 dedicated session. (d) ward-heat frontend - backend + producer shipped; liveclient wire operator-gated. (e) shaper UI + running-coach wire still blocked on 100% DS coverage. (f) champ-select / live game UX validation - UX-1/2/3/spike proven headless via fixtures; live game smoke pending (may need font/donut/sparkline scale calibration). (g) queued `ruff --unsafe-fixes` + type-hint pass STILL operator-gated. (h) docs-only edits now correctly SKIPPED by pytest_guard - one-line operator gate remains for any future docs commit that needs to RUN pytest.

---
# 2026-05-19 - cross-Claude lessons Phase 4 (BACKLOG drain)

Single discrete unit shipped autonomous in caveman ultra. Operator AskUserQuestion-picked "Cross-Claude lessons Phase 4 (Recommended)" from a 4-option fork (over: streaming vision delta-frames / research triage / OBS publisher wiring).

**`77e3437` feat(lessons): Phase 4 - ack-watcher + confidence scoring + auto-revert.** BACKLOG Cross-Claude infrastructure item. Three sub-units shipped in one slice, all read-only against the existing Phase 1-3 wire format.

- **(a) `core/lessons_ack_watcher.py`** (~220 LOC). Polls `/api/bridge?kind=result` over HTTPS shuttle (skip cert verify, mirrors lessons_receiver), indexes envelopes by `in_reply_to`, merges latest-by-ts ack body into matching `lessons_sent.jsonl` rows, atomic-rewrite via tmp+replace. Fail-soft fetch (URLError/HTTPError/timeout/malformed JSON -> []). Idempotent: rows already carrying `ack_received=true` are skipped. `summarize()` snapshot + `update_sent_acks()` polling entry + `WatcherReport` dataclass.
- **(b) `core/lessons_confidence.py`** (~250 LOC). Reads both ledgers, derives `mem_type` from filename prefix (sender path / receiver memory_path), buckets by `<peer>:<mem_type>`, computes `apply_rate = laplace_rate(applied, decided)` + `took_rate = laplace_rate(took, acked)` + `confidence = shrink(decided + acked) * apply_rate * took_rate` via `core/smoothed_rates.py` - composes on the shared statistical primitive (CLAUDE.md #90 decision). `confidence_for(peer, mem_type)` single-number lookup with 0.0 unknown-bucket fallback.
- **(c) `core/lessons_revert.py`** (~345 LOC). `apply_with_revert(lesson_id, commit_sha=None, *, tests, timeout_s, peer)` wraps `lessons_receiver.post_decision`. 3 branches: tests pass -> applied + no follow-up ack (post_decision already acks); tests fail no commit_sha -> queued fallback; tests fail with commit_sha -> `git revert <sha> --no-edit` + grab new HEAD sha + finalize as applied with auto-revert annotations in receiver_notes + follow-up `kind=result` ack carrying `auto_reverted=True` + `took=False` so origin's ack-watcher flips the took flag. Intentionally NOT daemon-wired - safety net for Claude-driven applies. CLI exit codes: 0=applied-clean, 1=queued, 2=applied+reverted.

**Surfaces:** `tools/lessons_status.py` (JSON + `--plain` tabular, runs ack-watcher refresh + prints summary + confidence buckets) and `GET /api/lessons/status?refresh=0|1` (new `dashboard/routes_lessons.py`, registered in `_dispatch.py` between health_peer + loadout).

**+60 tests across 4 new files:** `tests/test_lessons_ack_watcher.py` (29 tests: fail-soft fetch contract, index-by-reply latest-wins, idempotent merge, atomic rewrite, summarize), `test_lessons_confidence.py` (16 tests: empty ledgers, mem_type derivation, sent/received aggregation, merge, smoothed-rate composition, totals, alpha/k passthrough), `test_lessons_revert.py` (15 tests: _tail trimming, 3 branches, git revert failure, pytest timeout, LookupError catch, CLI exit codes), `test_lessons_status_route.py` (12 tests: build_report shape, --plain/--no-refresh/--json CLI, route registration, refresh query param, fail-soft 500).

**Wider RC suite: 1771 passed / 61 subtests / 0 failed** (s103 baseline 1705 + 66 new). py_compile + ruff clean; pure ASCII (3 stray section-sign chars caught + replaced before commit).

**Live-verified end-to-end:** RC restarted clean (pid 10824 -> 6768, reload_ok=true); `GET /api/lessons/status?refresh=0` returns the 2 existing real buckets (`peer:reference` 1/1 took 1.0; `peer:unknown` 4 discarded); CLI `--plain` renders the tabular view inline. No DS restart (non-engine).

**Docs sync (same commit):** BACKLOG entry flipped to ~~shipped~~ with the full one-liner; `docs/OPERATIONS.md` gained "Cross-Claude lessons (Phase 4 surface)" subsection under Bridge operations covering the curl endpoints, CLI invocation, and the `py -m core.lessons_revert <lesson_id> --commit-sha <sha>` shell.

**Don't-redo:** all 3 modules + CLI + route + 60 tests + docs are real + behavior-proven + live-verified. The "auto-revert is NOT a daemon, it's a Claude-invoked safety net" framing is deliberate (vision section 5 Phase 4 wording); do NOT re-pitch a daemon-fired auto-apply pipeline - that crosses the §6 "lean conservative" constraint. The `mem_type from path basename` derivation is the agreed schema-recovery path (sender path is `memory/<type>_*.md`, receiver memory_path is `<type>_synced_<peer>_*.md`); do NOT add explicit mem_type fields to the existing jsonl rows. The follow-up ack on auto-revert is in ADDITION to post_decision's ack, not a replacement - both fire so the peer sees the timeline; do NOT consolidate into one ack. The HTTPS shuttle (`https://legion-rc:8888/api/bridge`) is the single-source-of-truth boundary; do NOT in-process import `dashboard._bridge_log` (same s245/s105 lesson). pytest tail trimmed to 1.2 KB with line-boundary preference - do NOT widen, the bridge body has a size cap.

**Carries forward:** nothing blocking. The `RC-Bridge-MCP` scheduled task / `.mcp.json` client wiring from item 105 are STILL operator-gated (new always-on listener; register only when a local Claude is pointed at it). The 2 standing Phase-3-flagged operator-gated follow-ups (pre-existing non-ASCII retro-sweep + deferred type-hint/`ruff --unsafe-fixes` pass) are STILL operator-gated.

---
# 2026-05-19 - bridge MCP server + install ritual doc (2 BACKLOG drains)

Two discrete units shipped autonomous in caveman ultra. Both AskUserQuestion-picked Recommended first option of 4-option forks.

**(a) `d4f6140` docs(bridge): install ritual doc fix.** Closes item-103 carry-forward: the frozen `bridge_watcher_install.ps1` only pulls 4 of 7 runtime files the daemon imports at module load (missing `bridge_watcher_actions.py`, `bridge_watcher_history.py`, `bridge_watcher_action_prompt.md` - which the daemon hard-imports, not try/except). Added "Fresh peer install ritual (2-step)" block to `docs/OPERATIONS.md` Bridge section: run installer first, then `bridge_watcher_update_check.ps1 -Apply -Restart` to fill the 3-file gap. Doc-only, no frozen-file edit. BACKLOG entry sync.

**(b) `8c2a200` feat(bridge): local cross-Claude bridge MCP server (:8895).** BACKLOG Cross-Claude infrastructure item. New `tools/bridge_mcp_server.py` (~480 LOC) + `tools/start_bridge_mcp.py` boot launcher mirroring the s245 ds_matchdb_mcp pattern EXACTLY (stdlib ThreadingHTTPServer + JSON-RPC 2.0 over `POST /mcp`, Bearer auth, `GET /health`, `--show-token`, connect-probe port preflight, bounded ThreadPoolExecutor watchdog). **7 tools:** `bridge_search` (filter by since/hours/kind/target/source/limit), `bridge_post_note`, `bridge_post_task` (auto-stamps `task-<uuid>` when id blank - matches `/api/bridge/inbox` auto-stamp), `bridge_post_result`, `bridge_post_lesson`, `bridge_pending` (reads `ops/runtime/bridge_inbox_pending.json` directly so a status probe never needs the dashboard up), `bridge_status`. Reads via HTTPS GET shuttle to `https://127.0.0.1:8888/api/bridge` with self-signed cert skip (mirrors `tools/bridge_cli.py`); writes via POST so the deque + JSONL backup stay in sync. Dashboard-down -> structured `error` dict (URLError -> "unreachable", HTTPError -> http_status + response_body), never crashes. Same token chain as the other RC MCP servers so ONE `RC_MCP_TOKEN` covers all 3 RC MCPs. **+46 tests** in `tests/test_bridge_mcp_server.py` mirroring `test_ds_matchdb_mcp_server.py` (pure tool fns, MCP protocol, real ThreadingHTTPServer auth/JSON-RPC, ASCII guard, hung-tool watchdog). **Live-verified end-to-end:** launcher binds clean pid 10264, /health 200 with 7 tools listed, initialize/tools-list/tools-call round-trip OK, `bridge_search hours=2` returned 3 recent messages from the live bridge, `bridge_status` proxied the live config gate (configured=True, secret_present=True, remote_url=peer-host). Smoke pid taskkilled clean after verify. `docs/OPERATIONS.md` "Local cross-Claude bridge MCP (:8895)" section + RC-Bridge-MCP scheduled-task row; BACKLOG entry flipped to shipped.

**Wider RC suite:** 1705 passed / 61 subtests passed in 30s (s103 baseline 1457 + adjacent growth + 46 new bridge MCP tests). py_compile + ruff clean. No ENGINE bump (non-engine; ROADMAP-85 precedent). No RC restart (additive tools, no existing-process edit).

**Don't-redo:** both ships are real + tests green + bridge MCP live-verified; do NOT revert. The localhost-only + zero-dep + ds_matchdb-mirror architecture is deliberate - do NOT re-pitch a FastMCP/SDK rewrite or LAN exposure (item 245's lesson). The HTTPS shuttle to `127.0.0.1:8888/api/bridge` (not a direct in-process import of `dashboard._bridge_log`) is intentional: the deque lives in the dashboard process, not this one; the HTTP path keeps single-source-of-truth at the `/api/bridge` boundary. Auto-stamp on `bridge_post_task` is load-bearing: local POSTs do NOT auto-stamp (only `/api/bridge/inbox` does), and `bridge_pull_tasks.py` filters id-less entries via `m.get('id')` gate. The frozen `bridge_watcher_install.ps1` was NOT modified for the install ritual fix; the documented workaround stands until operator authorizes the inline frozen edit.

**Operator-gated (not blocking):** `RC-Bridge-MCP` scheduled task NOT created (new always-on listener; register only when a local Claude points at it - same gate as RC-DS-MatchDB-MCP at first ship). `.mcp.json` client wiring NOT done (live-session edit; operator decides when to consume it).

