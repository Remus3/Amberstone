# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-09-17, the laned orchestrated loop wave 4 doc-sync (relocated `2026-09-16b` the laned-loop wrap, VERBATIM - moved with the Edit tool because Bash writes to tracked files are banned, then byte-compared equal to the output of `scripts/wakeup_prune.py --keep 3` run on scratch copies of both files, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 970"; newest 3 = `2026-09-17b` this wave, `2026-09-17` wave 3, `2026-09-16c` waves 1-2). The pass before this one, on 2026-09-17, was the laned orchestrated loop wave 3 doc-sync (relocated `2026-09-16` the acknowledge-path wrap, VERBATIM via the Edit tool, byte-compared equal to `scripts/wakeup_prune.py --keep 3` output on scratch copies, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 969"; newest 3 = `2026-09-17` that wave, `2026-09-16c` waves 1-2, `2026-09-16b` the laned-loop wrap). The pass before that, on 2026-09-16, was the laned orchestrated loop wave 1 doc-sync (relocated `2026-09-15` the moon-sync merge, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 968"; newest 3 = `2026-09-16c` this wave, `2026-09-16b` the laned-loop wrap, `2026-09-16` the acknowledge-path wrap). The pass before this one, on 2026-09-16, was the laned-loop doc wrap (relocated `2026-09-14` the public surface refresh, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 966"; newest 3 = `2026-09-16b` this wrap, `2026-09-16` the acknowledge-path wrap, `2026-09-15` the moon-sync merge). The pass before this one, on 2026-09-15, was the moon-sync five-slice merge wrap (relocated `2026-09-12e` the discovery-axis + inter-scorer lane and `2026-09-12d` the fleet tooling-tier lane, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 2 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 964" - read those counts off the tool's own output, never off a recollection; newest 3 = `2026-09-15` this wrap, `2026-09-14` the public surface refresh, `2026-09-12f` the calibration lane). The pass before this one, on 2026-09-12, relocated `2026-09-12c` the SESSION WRAP doc-sync and reported "moving 1 session(s)" / "archive now has 962". The pass before that relocated `2026-09-12b` the five-slice merge and `2026-09-12a` RM-412 C1 OSS extraction, and reported "moving 2 session(s)" / "archive now has 961". The pass before that relocated `2026-09-11d`, the one before that `2026-09-11c`, and the one before that `2026-09-11b` and `2026-09-11a`, the same way. The 2026-09-11k RELOCATION DUE note that sat here is DISCHARGED and deleted - the file is back at keep-3. The letter suffixes are PER FILE and have diverged from `docs/LEDGER.md` - RM-385 is `c` there and `d` here; do not reconcile them. NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`4a707962`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired. The 2026-09-16 RELOCATION DUE note that sat here is DISCHARGED and deleted by the 2026-09-16b prune above.

---

# 2026-09-18 - laned orchestrated loop wave 6: four rows shipped through verifier-cleared slices, RM-276 re-filed then amended, RM-239 / RM-242 settled and still held

**LOCAL ONLY at `82c1eafda`** plus this doc edit - NOT pushed by the docs pass; origin at `58efb29dc`. Wave 5 and its wrap were pushed and CI is green on `58efb29dc` (ci, docs-guards, pages build and deployment, patch-day-ddragon-sync all success). LEDGER **1426**. Every slice was graded by an independent adversarial verifier; no producer graded its own work; three gap-closing slices (S5, S6, S8) exist ONLY because verifiers refused to wave a declared gap through, and all three returned real findings. Full dual suite run ONCE on the frozen merged tree `82c1eafda`, both exit 0: `agents/daemon_slayer` 10856 passed, 13663 subtests, 132.03s; `tests` 24213 passed, 102 skipped, 2 xfailed, 2 xpassed, 1 warning, 4998 subtests, 6167.87s. Six files changed against the base, 704 insertions, 11 deletions, 0 non-ASCII bytes.

**SHIPPED:** RM-470 (`8c14e8413`; the WAL pragma guard now handles `ast.AsyncFor` - false negative only, zero live sites), RM-473 (same merge; a NEW id minted at merge for the same guard's `with` / `async with` / no-`as` arm, which closed a gap an adversarial verifier declared against RM-470 - the arm is deliberately BROADER than the declared shape and the widening was independently adjudicated CORRECT, because a bare journal-mode execute has been a finding since RM-413 and excluding the with-wrapped form would make the wrapper strictly MORE PERMISSIVE than the statement it wraps), RM-471 (`342f71aeb`; the oss LICENSE drift guard's verdict is now checkout-independent - fixed at the COMPARISON by normalising line endings, NOT at `.gitattributes`, with the reason recorded in the guard, and confirmed working on a tree it was not developed in), RM-472 (`82c1eafda`; CLOSED BY REFUSAL - comprehension- and lambda-shaped platform laundering no longer get capability credit; zero new acceptances over a both-revisions corpus, real-site census IDENTICAL at 181 sites (179 CAPABILITY / 1 DEFECT / 1 UNRESOLVED), zero changed rows).

**HEALTH / WATCH ITEM:** after the suite, pid 18660, alive true, last_reload_ok true, mode client. The RC pid MOVED during the suite (24252 -> 18660), a supervisor bounce. Wave 5 saw the same pattern, so this is the SECOND occurrence - it is a WATCH ITEM, not a defect. Nothing in this wave touched the supervisor. Re-probe it next wave; do not inherit it as a known bug and do not diagnose it from one reading.

**OPEN, filed or re-filed this wave:** RM-276 RE-FILED and then AMENDED, still open - its old acceptance was UNSATISFIABLE (it mandated a fixture from the real `WAKEUP_NOTES.md`, which no longer contains the triggering shape, so satisfying it literally would yield a VACUOUSLY GREEN guard); re-filed with a re-derived census and an acceptance that can actually fail, then amended again when a verifier found a SIBLING VARIANT the re-file missed, in TWO routes, where the prescribed first-segment-only fix is provably INERT. RM-474 NEW - closing RM-471 cost the shipped LICENSE its second independent guard, and the surviving compensating test does not know it is load-bearing.

**RM-239 / RM-242 - SETTLED RESULT, BOTH STAY OPEN AND HELD (`5a5d18d92`):** population settled at 25 sites across 19 modules, with the inclusion rule published alongside it so the next derivation can be compared rather than re-argued. **HALT-LEVEL FINDING: RM-239's own literal acceptance is UNSATISFIABLE for seven of its own sites** - six handlers have no handler object in scope, and the helper the row prescribes emits a payload shape that would destroy keys the UI and the tests depend on. That is a defect in the ROW, not in the code it is about. Neither row was converted, and neither should be touched until the row itself is fixed. Also this slice: the `dashboard/_errors.py` docstring was corrected (docstring only, executable region hash-identical) - a stale line claimed the loop-control tables are served on both listeners when they are Mission Control ONLY, and that line is the probable source of a wrong belief THREE independent passes inherited; the correction was extended at merge to name the sibling module too.

**DO NOT REDO:** RM-470, RM-471, RM-472 and RM-473 are SHIPPED - read the markers, do not re-investigate. RM-276 is decided as RE-FILED-AND-AMENDED, not re-open-for-re-filing. RM-239 / RM-242 carry a SETTLED result and a named row defect; do not re-derive their population from scratch, compare against the published inclusion rule instead.

**TRAPS PAID:** a verifier overturned or amended a producer claim in EVERY slice, for the SECOND wave running - two waves at one hundred percent means an ungraded producer claim is UNVERIFIED by default, so budget the grading pass instead of hoping to skip it. RM-239 / RM-242 INVERTED FOUR TIMES: blanket conversion wins, then blanket is unsound (the probes covered only the capped subset while the conclusion governed all sites), then the prescribed remedy is structurally impossible for most sites, then the fourth pass found the stale docstring that had misled the first three. PROPAGATED CHARACTERISATIONS were the recurring failure, three times - a claim asserted by one pass and inherited by the next without re-derivation - and the most instructive instance is that the MERGER, the main session, was ITSELF the propagation vector: it wrote an unverified mechanism claim into a slice brief and the slice measured it FALSE and pushed back. That is not a lesson about subagents; the orchestrator is inside the blast radius of its own unverified claims, and a brief is exactly the artifact a slice will trust. Also recurring: a census figure staled by its own author's edit in the same change; a control that was PATH-DIVERGENT from the site it stood in for; a producer that DISCLOSED an anomaly it could not explain, which is precisely what let a verifier find the mechanism (disclose the anomaly you cannot explain - it is a handle, not an admission); and a "corpus cardinalities did not reproduce" finding where the conclusion nonetheless HELD over a larger set, so report the mismatch rather than re-deriving it into agreement.

## NEXT SESSION

Push decision for wave 6 first (the push halts on its diff). No restart owed - re-probe, do not inherit, and re-check the pid-bounce watch item rather than assuming it. Then RM-474 and the amended RM-276; RM-239 / RM-242 need their OWN row defect fixed before either is touched, and that is an operator call. Every HALT row still needs the operator.

---

# 2026-09-17c - laned orchestrated loop wave 5: three rows shipped through three verifier-cleared slices, the older-block AGE census done and its four flips applied

**LOCAL ONLY at `36aaf1765`** plus this doc edit - NOT pushed by the docs pass; origin at `d99a716bb`. Wave 4 and its wrap were pushed and CI is green on `d99a716bb` (ci, docs-guards, pages-build-deployment, patch-day-ddragon-sync all success). LEDGER **1425**. Every slice AND every census was graded by an independent adversarial verifier; no producer graded its own work; two verifier-declared coverage gaps were closed by two further agents rather than left as implied coverage. Full dual suite run ONCE on the frozen merged tree `36aaf1765`, both exit 0: `agents/daemon_slayer` 10856 passed, 13663 subtests, 127.33s; `tests` 24164 passed, 102 skipped, 2 xfailed, 2 xpassed, 4998 subtests, 6364.52s. Twelve files changed against the base, 0 non-ASCII bytes.

**SHIPPED:** RM-468 (`fe2fcd52d`; nine WAL pragma discard shapes, each with a paired negative control - for-target and for-tuple-target, walrus in if and while position, f-string conversion and format-spec, f-string concatenation, SQL string concatenation; verifier found 0 new acceptances over 77 rows, a 30-expression differential fuzz against real CPython disagreed nowhere, 5 producer mutants plus 3 of its own killed), RM-469 (`44e816ee5`; hand-rolled fault patches converted to scoped helpers across six test modules plus the `oss/win32_atomic_io` suite, which got a LOCAL stdlib-only helper because its manifest requires the directory to be self-contained - no cross-tree byte pin was created; verifier disarmed every converted fault in turn and each test then failed), RM-467 (`36aaf1765`). **SHIPPED IN PART:** RM-466 (same merge; three laundering shapes survive, named in the marker). RM-466 + RM-467 closed BY REFUSAL - no accepted vocabulary widened, no cache or memo added; verifier rebuilt the corpus at 343 rows harvested from BOTH revisions, 0 new acceptances, 63 accepted -> refused, 0 crashes.

**AGE CENSUS (phase A, read-only, applied):** universe RM-192..RM-198, RM-229..RM-283, RM-377, RM-381 = 64 candidate ids, 58 with a row, 50 OPEN-family at HEAD, 8 closed-family, 6 ids never minted. The brief's "about 49" was wrong; one unit is attributable to treating RM-229 as row-less when it has a real open row. **Four flips applied:** RM-192 and RM-246 to ACCEPTANCE MET, RM-255 to closed-at-head, RM-281 to half-closed. RM-260 SUPERSEDED. RM-239 and RM-242 on HOLD (conflicting acceptances, population unsettled across three disagreeing derivations). RM-276 stays open but must be RE-FILED - its acceptance is unsatisfiable as written and satisfying it literally yields a vacuously green guard.

**DO NOT REDO:** RM-468, RM-469 and RM-467 are SHIPPED and RM-466 is SHIPPED IN PART - do not re-investigate or re-census them, read the markers. The four census flips RM-192, RM-246, RM-255 and RM-281 are DECIDED and applied - do not re-age them. RM-260 (superseded), RM-239 / RM-242 (HOLD) and RM-276 (re-file owed) are decided dispositions too.

**OPEN, filed this wave:** RM-470 (WAL guard does not handle async-for; false negative only, no live sites), RM-471 (a LICENSE byte-compare guard whose verdict depends on when the tree was checked out - an extensionless file resolves no line-ending attribute), RM-472 (comprehension- and lambda-shaped platform laundering still credited). Carried record-only: RM-454, RM-293(a)/(c), RM-292(b-d), RM-290 site 2, RM-409, RM-376, RM-448, RM-445, RM-432, RM-453, RM-378 (HELD on RM-448). Side finding, record only: two python processes resident since 2026-09-15, one a PreToolUse hook that should be short-lived; not investigated.

**Inbox cycle 1:** RC's own `moon_sync_inbox/` triaged read-only at session start; nothing newer than 2026-09-16, matching wave 4. NO reply written, no byte left the tree, responder stays DISARMED. **Restart check:** measured, not inherited - RC main and the LCU agent both started after every wave 4 runtime commit, so both already carry RM-461 and RM-462; no restart owed or performed. **Engine impact NONE** - no `agents/daemon_slayer` path, no `ENGINE_VERSION` move, no DS bounce, no Share mirror, no frozen file, no `.githooks/` edit, `docs/CHANNEL.md` / `ops/loop/slots.py` / `ops/loop/winmutex.py` untouched, so the push gate's diff condition is not tripped.

**TRAPS PAID:** a marker that says CLOSED must survive a gap hunt - the verifier found three surviving shapes and the row became SHIPPED IN PART. A census total that two classifiers dispute is not fixed by picking a third number: S3's repo-wide total stays DISPUTED and the row carries the decomposition instead (widest population 201, test trees 186, minus the shared helpers' own internals 182, install/restore pairs collapsed 163), naming the four figures that DID reproduce as what the acceptance turned on - do not collapse it to one number. A vendored suite that must stay self-contained cannot import the repo `tests` package, however convenient - duplicate the helper locally. A rule scoped narrowly on purpose (walrus in if/while test position) is not an oversight; a blanket version over-fires where the result IS read.

## NEXT SESSION

Push decision for wave 5 first (the push halts on its diff). No restart owed - re-probe, do not inherit. Then RM-470, RM-471 and RM-472, plus the RM-276 RE-FILE; RM-239 / RM-242 need their population settled before either is touched. Every HALT row still needs the operator.

---

# 2026-09-17b - laned orchestrated loop wave 4: four rows shipped whole and two closed on inspection through five verified slices, RM-463 shipped in part by merger adjudication

**LOCAL ONLY at `495d81b24`** plus this doc edit - NOT pushed by the docs pass; origin at `23e20e810`. Wave 3 and its wrap were pushed and CI is green (ci + docs-guards on `23e20e810`). LEDGER **1424**. Every slice had an independent adversarial verifier and no producer graded its own work. S1 (closures only, no commit): the RM-460 closure confirmed, the RM-378 closure refuted. S2 (RM-461) refuted once, confirmed round 2. S3 (RM-462 + RM-459) confirmed round 1 with a should-fix, confirmed round 2. S4 refuted round 1 on both rows; round 2 confirmed RM-465 and refuted RM-463 a second time, settled by merger adjudication. S5 (RM-464 + RM-275) refuted once, confirmed round 2. Full dual suite: `agents/daemon_slayer` 10856 passed, 13663 subtests, rc 0 (2:09) at `667d58861`, before the tests-only S4 merge; `tests` at `495d81b24`: 24068 passed, 1 failed, 102 skipped, 2 xfailed, 2 xpassed, 4998 subtests, 1:54:32 - the one failure was `tests/test_docs_operations_test_scope_rm322.py`, a rotted line cite in `docs/OPERATIONS.md` (the `_TEST_TREES` cite moved from 75 to 78 under the RM-463 edits), fixed in the working tree and that file re-run 3 passed. Merge-time catch: `tests/test_skip_condition_hygiene.py` on the merged tree flagged the new RM-462 test's node skip as UNRESOLVED (its predicate was imported from the RM-382 test module); fixed in `495d81b24` by resolving `shutil.which("node")` in the module itself, no assertion changed.

**SHIPPED:** RM-461 (`cs`, `level` and `players[].creep_score` bad or missing -> None through `_int_or_zero`'s new sibling `_int_or_none`, because round 1's 0 poisoned the adaptation latch; kill participation degrades one value only; trade, swap and round cell ids through `_as_int`, cell 0 kept; a non-string `championName` skipped), RM-462 (`rc_facts` prints `phase=-(client-closed)` / `phase=-(phase-read-failed)` for a null phase, never `None(...)` which is a real gameflow phase; the legacy page's relay-offline text names RC-LCUAgent instead of the retired Game-PC; the 5-phase UI audit RAN before `72808e9ae` and PASSED), RM-464 (census by AST shape: `os.replace` 29 + 3 `Path.replace`, 21 class-level `Path`, 21 `time.sleep`; new `scoped_path_fault`, `scoped_fs_fault` covers `os.open`; sentinel `os.open` patches and the `hot03` asyncio fakes scoped), RM-465 (annotated, unpacked and f-string-name WAL discards caught; empty target pinned as not a discard), RM-459 now SHIPPED (item 3 MEASURED on CLI 2.1.251: `session_id` present in the `-p` JSON and in the SessionStart + UserPromptSubmit payloads, same value; the sync-inbox byte compare ATTRIBUTED, not loosened; the unterminated child tail closed by a content settle, no pid). **SHIPPED IN PART:** RM-463 (unsound RM-449 folds and a round 1 truth-carrying refusal REMOVED; definite raises over known operands refused; probe-laundering taint added). **CLOSED on inspection:** RM-460 (the miner discards `item_order`, the cohort and rank builders never call it, old vs new miner output byte-identical to the tracked file - no backfill owed), RM-275 (fixed by RM-351 `1d7f39e85`; `cd13cd003` adds the finite cap+1 test that kills an off-by-one mutant the 13 old tests missed). **HELD:** RM-378 on RM-448 (write half done in `a1b5ebae8`; two DS text-mode writers keep its sibling clause unmet).

**MERGER ADJUDICATION (RM-463):** the round 2 verifier recommended REMOVING the probe-laundering taint as a second refute. KEPT: the removal rule targets additions that ACCEPT, and the taint is a strict refusal with 0 new acceptances against base, so removing it would only restore base acceptances. The refute was on completeness, so the row is PARTIAL and the bypasses are filed as RM-466. **CLAIM CORRECTIONS:** the S4 commit's "fidelity 0 mismatches" is false (58 unique, 116 per host, all identical to base - not a regression); multi-arg `len` / `str` still grade CAPABILITY, definite-raise coverage is 1-arg `len` and binary + / - only; the real-site census is 180 at base, not 179.

**RESTART NEEDED:** RC plus RC-LCUAgent for RM-461 (`tools/lcu_agent.py` imports the champ-select shaper through `lcu/snapshot_shape.py`). The same RC restart carries RM-462's page wording: `web/legacy_index.html` is read once and cached per process by `dashboard/_static.py`, so it does NOT auto-reload (the wave brief assumed it did). `tools/rc_facts.py` is a CLI, no restart. Merger result: RC restarted via `restart_trigger.txt` and RC-LCUAgent restarted via its scheduled task, both after the merges; `ops/runtime/health.json` now reads pid 19336, alive true, last_reload_ok true, mode client (pid was 2056 before the restart), and the RC-LCUAgent task state is Running.

**OPEN, filed this wave:** RM-466 (probe-laundering bypasses past the RM-463 taint), RM-467 (definite-raise credit residuals, multi-arg `len` / `str`, the `_Truth` BoolOp assumption), RM-468 (five WAL guard shapes: loop target, walrus, f-string conversion or spec, f-string concatenation, string concatenation), RM-469 (RM-464 residual process-wide patches, the `oss/win32_atomic_io` tests, platform-simulation patches unassessed). Carried record-only: RM-454, RM-293(a)/(c), RM-292(b-d), RM-290 site 2, RM-409, RM-376, RM-448, RM-445, RM-432, RM-453, and now RM-378 HELD. Residuals recorded, NOT filed: RM-461's JSDoc `cs` drift, raw kda f-string, kill participation over 100 percent, bare int in `_local_pick_completed`, latch `int()` without an `OverflowError` catch, `routes_state` `phase or "?"`; RM-459's re-appended identical torn tail and vacuous worktree arm. Side finding, record only: `--restricted` ignores user, project and local settings, so RC hooks do not fire in the pinned responder spawn.

**Inbox cycle 1:** RC's own `moon_sync_inbox/` was triaged read-only at session start; nothing newer than 2026-09-16 had arrived, no reply was written, no byte left the tree.

**TRAPS PAID:** a skip predicate IMPORTED from another module is UNRESOLVED to the hygiene guard, and only the merge-time run saw it - resolve the predicate in the module. A second refute that recommends removal must be read against WHAT it removes: the rule removes accepting additions; a strict refusal refuted on completeness stays, and its gap is filed. Degrade-to-zero is not neutral - a 0 latched downstream for the whole game; None is the uncountable value. A null label must not collide with a real enum string ("None" is a real LCU phase). A producer's "0 mismatches" was false - grade the verifier's census, not the commit message. A census by name grep missed `os.open` and `asyncio` shapes; census by AST over every patched attribute. A served HTML page can be process-cached outside ADR-008 hashing - check the loader before claiming auto-reload. A row can name a constant the fix spelled differently (RM-275) - age-check by behaviour, not by name.

## NEXT SESSION

Push decision for wave 4 first (the push halts on its diff; it carries one `web/` file). RC + RC-LCUAgent restart for RM-461 and RM-462 if the wrap did not land it - re-probe, do not inherit. Then wave 5 per `RC-NEXT-SESSION.txt`: a READ-ONLY AGE census of the older open block (RM-229..RM-283, RM-192..RM-198, RM-377, RM-381) before any build, then RM-466 through RM-469. Every HALT row still needs the operator.
