# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-05-18 - s246: effects.py engine-core facade split (`34da8d1`, pushed)

Operator picked "effects.py split" from the s245-NEXT fork (over live-game-gated s220 PGR / one-off 101.qq.com). The **final AUTONOMOUS_AUDIT 4.C item** - the last oversized engine-core module. Full vertical slice, end-to-end.

- **Authorization (don't re-litigate):** `agents/daemon_slayer/effects.py` is **NOT frozen**. The CLAUDE.md frozen-list is explicit and does not contain it; the audit's "operator-gated, never-autonomous" label was engine-correctness *risk*, not a freeze - satisfied by the operator being present to review. **The whole AUTONOMOUS_AUDIT s5 menu is now exhausted** - I verified each item rather than trusting the hand-off: 4.A dashboard.js gone, 4.B `unique_passive_key` on all 6 rankers (`aaa9c5a`, ENGINE 1.4.0), 4.C builders.py (3 siblings) / adaptation_hint.py (s242) / supervisor.py (s243) / **effects.py (this)**, 4.D view-router cross-link (`5843c93`), 4.E `test_builders_enrich.py`. effects.py was the only one left.
- **`34da8d1` (refactor) + this docs-sync.** Same byte-verbatim facade pattern as s242/s243. Original 27 AST top-level nodes -> acyclic `_effects_types <- _effects_data <- effects`: `_effects_types.py` (392 - schema types + damage-type constants, orig lines 37-419 verbatim, zero deps); `_effects_data.py` (4947 - the 547-entry `ITEM_EFFECTS` table, orig 422-5347 verbatim, imports the 6 type/constant names its literals use); `effects.py` 5692 -> 397 (re-export facade: orig docstring + the 14 effect-aggregation logic fns verbatim + full public-surface re-export; orig stdlib import block kept verbatim so `effects.<dataclass|Callable|Iterable|Union>` still resolve - the s243 attribute lesson).
- **Design decisions (don't re-litigate):** the types MUST be a third module (data <-> facade would be a circular import); the 14 functions stay in the facade (they ARE the logic - "leave effects.py pure logic"); NO `__all__` added (the original had none - adding one changes `import *` semantics + is a new attribute); class `__module__` is now `_effects_types` for CallContext/PeriodicProc/ItemEffect (intentional - no consumer asserts it, no pickling; same call as s243 moving `_QuietHandler`); the non-ASCII box-drawing chars in data-table comments are preserved **verbatim** (this is byte-relocation, not authoring - the no-em-dash hard rule governs *authored* content and that purge was a separate operator-gated pass; "fixing" them would also break the equivalence proof).
- **Verified (equivalence proof):** AST self-check vs `git show HEAD` - all 23 substantive top-level nodes (4 constants, 4 type defs, the entire `ITEM_EFFECTS` table, all 14 functions) byte-identical to the pre-split original; facade docstring identical (the 3 import statements are the only re-authored part). py_compile + fresh-import smoke (547 entries, full public surface incl. the typing attrs, dedup/proc-validation/lambda-resolve all functional). Full `tests/` + `agents/daemon_slayer/tests/` = **3727 passed / 778 subtests / 0 failed**, byte-exactly the pre-split baseline (captured before the change) - the DS value-pin suite is the definitive behavior-equivalence proof. ruff + archmap + state_schema pre-commit gates all green. Throwaway slicer script deleted. **No ENGINE_VERSION bump / no DS :8893 restart** - a no-op source relocation, the running engine's math is identical (audit #5 + ROADMAP-85 precedent for behavior-neutral work).
- **Don't-redo:** split done + behavior-proven; do NOT re-pitch a re-merge, a data-format change, or an `__all__`. The 23/23 AST byte-identity + the exact-baseline suite is airtight - no need to re-verify equivalence.
- **NEXT:** AUTONOMOUS_AUDIT fully closed. Autonomous-safe refactor work is exhausted across s237-s246. Remaining pending work is operator-gated, not autonomous: the **s220 aggregator-G-style Post Game Review reframe** (UI; needs a live standard-queue game + the per-page UI-audit ritual) and the one-off **101.qq.com Game-PC CDN capture** (champ-select-safe browser Network-tab grab). DS conditional arc operator-CLOSED (s232).

---

# 2026-05-18 - s245: #6 local DS + match-DB MCP server (`f585933`, pushed)

Operator picked "#6 local DS+matchDB MCP" from the s244-NEXT autonomous-safe fork (over operator-gated effects.py / live-game-gated s220 PGR). Full vertical slice, end-to-end.

- **`f585933` (feature) + docs-sync commit (this).** The DS engine (:8893) + `data/match_history.db` were RC-internal infra reachable only from inside the running dashboard; a local agent doing build research / calibration / post-game analysis had no first-class way in. New `tools/ds_matchdb_mcp_server.py` mirrors `tools/gamepc_mcp_server.py` EXACTLY (stdlib `ThreadingHTTPServer` + JSON-RPC 2.0 `POST /mcp`, Bearer auth, `GET /health`, `--show-token`, connect-probe preflight, **zero new deps**) + `tools/start_ds_matchdb_mcp.py` boot launcher mirroring `tools/start_daemon_slayer.py` (`serve_forever()->int` contract, logs/+ProgramData fallback).
- **8 tools:** 4 DS (`ds_health` / `ds_rank_items` / `ds_build_order` / `ds_archetype_for` wrap `core.daemon_slayer_client.rank_for_primary_archetype` / `core.build_order.plan_build_order` / `core.archetype_picks.get_archetype_for`) + 4 match-DB (`match_recent` / `match_mode_stats` / `match_tft_comps` / `match_tft_streak` wrap `core.match_db.MatchDB` read queries via a lazy WAL-safe process-wide singleton).
- **Design decisions (don't re-litigate):** (1) port **8894** + **HOST 127.0.0.1** - localhost-only because it proxies the *local* DS+DB (no cross-machine need, unlike gamepc's `0.0.0.0`); Bearer auth + same token chain (`RC_MCP_TOKEN` -> `tools/mcp_token.txt` -> `tools/vision_token.txt` -> dev fallback) kept so ONE token covers both RC MCP servers. (2) **Fail-soft** (the `daemon_slayer_client` opt-in-infra contract): DS-down + match-DB-missing both return `{error,...}`, never raise. (3) Blank `archetype` auto-resolves to the champion primary (the same default `dispatch_for_coach` uses). (4) Read-only w.r.t. RC state (never writes the DB).
- **Verified:** 36 new tests (`tests/test_ds_matchdb_mcp_server.py`: tool fns w/ monkeypatched deps + protocol handlers + real ephemeral-port server for the auth/JSON-RPC contract + TOOLS_SCHEMA<->TOOL_FUNCS parity guard + ASCII guard); full `tests/` **1457 / 30 subtests / 0** (s244 baseline 1421 + 36); ruff + py_compile + pure ASCII clean. TDD caught a real bug pre-commit (`if str(i).strip()` turned `None` -> the string `"None"`; fixed to the client's `if i` idiom). **Live-proven end-to-end via the production launcher** vs live DS :8893 (`engine_version=1.4.0`, patch 16.10.1) + real `match_history.db`: initialize/tools-list/all-8-tools 200; `ds_archetype_for(Vayne)` surfaced the persisted `mage` override -> `ds_rank_items` correctly routed to `ds.ability`; `ds_build_order(Vayne)` ran 4 sequential live engine calls -> a real unique-passive-safe order; `match_mode_stats(ARAM)` returned 20-game aggregates. Ad-hoc test instance taskkilled clean (never `Stop-Process`, per the hard rule). No ENGINE bump / no DS restart / no RC restart (standalone non-engine tool; ROADMAP-85 precedent). docs/OPERATIONS.md gained the scheduled-task row + a `## Local DS + match-DB MCP (:8894)` section.
- **OPERATOR-GATED (flagged, NOT blocking):** (a) the `RC-DS-MatchDB-MCP` scheduled task is documented but NOT created - it is a new always-on listener; register only when a local Claude is actually pointed at it. (b) `.mcp.json` client wiring is NOT done - editing it would change a live Claude session's own tool surface (operator's call when/if to consume it). Neither is required for the slice to be complete - the server + launcher + tests + docs are done + behavior-proven.
- **Don't-redo:** server+launcher+tests+docs done + live-proven; the localhost-only + zero-dep + gamepc-mirror architecture is deliberate - do NOT re-pitch a FastMCP/SDK rewrite or LAN exposure. Engine reporting `1.4.0` is pre-existing live state unrelated to this slice (the MCP just proxies whatever `/health` says) - not a regression, do not chase.
- **NEXT (AUTONOMOUS_AUDIT s5):** the only remaining 4.C is `effects.py` (5692) engine-core = reviewed-only-never-autonomous (operator-gated). Autonomous-safe refactor work is largely exhausted across s237-s245; the big pending non-engine item is the **s220 aggregator-G-style Post Game Review reframe** (UI; needs a live standard-queue game + the per-page UI-audit ritual). Optional data: the one-off 101.qq.com Game-PC CDN capture. DS conditional arc operator-CLOSED (s232).

---

# 2026-05-18 - s244: #4 Arena/Mayhem augment recommender FOREGROUNDED (`d7f6033`, pushed)

Operator picked "#4 Augment pillar" from the s243-NEXT autonomous-safe fork (over #6 local MCP / effects.py). Full vertical slice, end-to-end.

- **`d7f6033` (pushed `cb6c8dd..d7f6033`).** The data-driven recommender (`core/augment_recommender.py` + `augment_external_source.py`) shipped+tested at #88 but its ONLY surface was the invisible `#augments-pill` `title` hover tooltip. Now a prominent `#ib-aug-reco-block` INSIDE `#item-build` (new `web/js/panels/augment_reco.js` + `web/css/panels/augment_reco.css` + dashboard.css @import + main.js wire after `renderItemBuild`). Surfaces the causal "why": own WR vs external Mayhem prior, synergy, confidence, sample size. `coaches/arena_coach.py` `_augment_recommendation` gained per-row `"syn"` (additive).
- **Design decisions (don't re-litigate):** (1) toggled build-section block inside #item-build mirroring `#ib-ds-block` - NOT a new grid panel (deliberate: zero layout risk, matches the established no-reflow pattern; that's WHY augments moved to the header pill in 2026-04-23). (2) Presence of `p.aug_reco` IS the mode gate - module is decoupled from the mode authority by design. (3) `#augments-pill` + its tooltip left UNCHANGED (no regression).
- **Verified:** full `tests/` **1421 / 30 subtests / 0** (s243 baseline 1405 + 16 new: 14 DOM-wiring guards `test_augment_reco_panel_dom.py` + 2 Playwright snapshot tests arena+Mayhem); snapshot suite 13/13; node --check + ruff + py_compile clean. Asset hash flipped LIVE to `1819f07ef8` (ADR-008 auto-reload - **no RC restart needed**), confirmed via Game-PC DISPLAY5 capture (clean Home load, footer shows new hash, block correctly absent in CLIENT/no-game). No ENGINE bump.
- **Session-start anomaly RESOLVED = FALSE POSITIVE (don't re-investigate):** "RC-Phase3-Supervisor failing result=2147946720" - that's `0x800710E0` (task-terminated), residue of s243's controlled End+Run. :8890/:8891 listen on **pid 15068** = the exact process s243 verified healthy. s243 split did NOT break Phase-3.
- **NEXT:** (a) **operator-gated** - live Arena/Mayhem augment-select render proven only by snapshot fixtures (no game ran this session; LCU offline); validates for real on next Arena/Mayhem game. (b) `arena_coach` `syn` activates on next coach reload - JS degrades gracefully (synergy column hidden) until then; non-urgent, no restart triggered. (c) Remaining autonomous-safe: #6 local DS+matchDB MCP, or effects.py split (operator-gated engine session).
