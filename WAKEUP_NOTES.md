# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-08-14, orchestrated-run docs sync (relocated BOTH `2026-08-12` blocks - RM-190 decided + the 3-day-outage recovery; newest 3 = orchestrated run `2026-08-14` + new-project design QA `2026-08-13b` + /sync-all-md `2026-08-13`). NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`2f35163d`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired.

# 2026-08-31b - lane 8 cycle 32: the replay scrubber folded item inventories from a column Riot never populates, so its ITEM_UNDO branch had never once fired

**Commit on `lane/true-audit` (NOT merged - lane 8 ships last). Tier-1. LEDGER 1291. RM-290 filed.**

Audited `core/replay_history.py` (criterion 4 load-bearing + **no dedicated test file**, reached live from `dashboard/routes_coach.py:71`/`:89`; criterion 1 untrusted parse). 5 defects, all PROVEN by measurement against the live 1.87 GB `rewind_history.db`, not read off the source.

**Headline.** Riot sends `beforeId`/`afterId` on ITEM_UNDO, never `itemId`. `scripts/rewind_scraper.py:567` maps only `itemId`, so `timeline_events.item_id` is NULL for **28405 of 28405** undo rows, and the fold's `elif ... and item:` branch was structurally dead - it had never fired, while the docstring claimed it reversed purchases. **Both directions were wrong:** 25917 rows undo a PURCHASE (item wrongly left IN), 2488 undo a SALE (item wrongly left OUT).

**Downstream measured, not assumed.** `participants.item0..item6` is ground truth, so the fold is falsifiable: over 400 participant-inventories in the 40 most undo-heavy matches, EXACT reconstruction went **12 -> 37 (3.1x)**, re-measured against the shipped module. Honestly: 37/400 is still not "correct" - the residual is component/trinket modelling that `core/replay_seek.py:29-34` already flagged, and only the undo axis was in scope. **Tie-break caveat, from the verifier and recorded rather than left in chat:** matches ranked 39-41 all sit at 25 undo events, so "the 40 most undo-heavy" is ambiguous at the boundary - under the reverse tie-break the pair is **12 -> 35**. The direction and the ~3x magnitude are robust either way; the headline figure is the natural tie-break.

**Backfill was free and that drove the design.** `raw_json` is populated for **0 NULLs of 28405**, so reading the fold from there repairs every historical row with no migration of a 1.87 GB WAL database (whose `-wal`/`-shm` sidecars make copy-and-replace its own hazard). The 12 -> 37 measurement IS the historical repair.

**Four more, all latent against today's data and all labelled so:** `if queue_filter:` swallowed queue **0** (custom games); `LIMIT -1` is UNLIMITED in SQLite and the function runs a join PER ROW, so a negative limit was a full-table read amplified into an N+1 (route clamps 1..200, the query's owner did not); the frame sampler could never reach the final index (90 frames capped to 60 kept 88, dropped 89 - the END of the game; live corpus maxes at 53 so it never triggers); `max_frames=0` was a ZeroDivisionError.

**Two mutation survivors, both converted to kills by strengthening.** Dropping `raw_json` from the production SELECT **survived the whole suite** - every undo test called `_build_inventory_at` with rows the TEST selected, so `match_detail`'s real query was never exercised. My own defensive `except (IndexError, KeyError)` was masking it, silently degrading back to the exact bug being fixed. A missing COLUMN now raises (caller contract) while bad DATA still degrades; two end-to-end tests close the path. Second survivor coerced a NULL participant id to 0, inventing a phantom participant that `assertNotIn(None, inv)` happily passed.

**Don't-redo / durable:**
- **ITEM_UNDO has NO `itemId`.** Never fold it from `item_id`; read `raw_json`. `core/event_patterns.py:233` already reads the right fields but collapses them with `beforeId or afterId`, which is BACKWARDS for the 2488 undone sales - that is **RM-290**, filed not fixed (offline batch: it feeds `data/event_pattern_rates.json` + cohort baselines).
- The misleading `-- ITEM_PURCHASED / ITEM_SOLD / ITEM_UNDO / ITEM_DESTROYED` comment above `item_id` exists ONLY in the live DB's `sqlite_master`; **no source file carries it**, so it cannot be fixed without a table rebuild. Do not go looking for it in `.py`.
- `data/rewind_history.db` is NOT in the worktree (gitignored, main tree only) - tests must build a fixture DB.
- **Do not run a mutation harness while a full suite is running.** The first full run overlapped mine and was DISCARDED, not reported. Cycle 29's own lesson.
- Both remaining RC failures are INHERITED and were confirmed by probe, not by "does not import my module": `PINNED_CLI = "2.1.220"` vs live CLI **2.1.251**, and worktree `ENGINE_VERSION 1.278.0` vs live DS `:8860` **1.278.1** (resolves on merge).
- `tools/drift_guard.py` still reports its 1 inherited breach (ROADMAP at 99% of budget, **RM-284**); this cycle did not touch `ROADMAP.md`, which is why RM-290 went to `BACKLOG.md`. Two orphaned stashes (**RM-285**) untouched again - shared stack, not this lane's to pop.

**Suites (repo root, FROZEN tree):** `tests/` 2 failed / **19838 passed** / 144 skipped / 4627 subtests. DS **10684 passed** / 13482 subtests, exit 0. 30 new tests, **12/12 mutants killed**, source restored byte-identical. No restart - worktree branch, not deployed.

# 2026-08-31 - lane 8 cycle 31: the fallback that exists for an unreachable endpoint was itself gated behind that endpoint's full timeout, under a lock every reader takes

**Commit `6f575612` on `lane/true-audit` (NOT merged - lane 8 ships last). Tier-1. LEDGER 1290. RM-289 filed.**

Audited `core/synergy_external_source.py` (criterion 1 untrusted third-party input + criterion 3 concurrency-exposed). Two live defects, both PROVEN by measurement, not read off the source.

1. **Network I/O under a process-wide lock.** `smoothed_rates_101qq.py:193` held `_CACHE_LOCK` across up to 3 sequential 6s CN fetches at `:216`. All 7 accessors take that lock; `/api/duo-synergy` + `/api/draft-score` read them on a threaded server. Stubbed slow, a second thread blocked **5.70s**. Default ON (`RC_DUO_SYNERGY_LIVE` -> `"1"`). Now: lock-free build, publish under the lock, serve-stale-while-refreshing behind a separate `_REFRESH_LOCK`, generation counter for a mid-build reset.
2. **Non-finite floats.** `json.loads` takes `NaN` by default, it passes `isinstance(x, float)`, and `json.dumps` re-emits a bare `NaN` that browser `JSON.parse` REJECTS - killing the WHOLE response, not one row. Closed at the parse boundary.

**Sibling sweep found 3 sites repo-wide** via an AST transitive call-graph pass. My first grep-shaped attempt matched `dict.get` 181 times and was junk - the tight predicate is worth keeping. The `rank_tier` pair is the self-described mirror and was strictly worse (20-cell sweep, ~120s); fixed the same way. Third site `core/moon_proxy.py:48` is FROZEN -> audited, reachability opened, FILED as RM-289, NOT edited.

**The verifier earned its keep twice.** It REFUTED my C8 claim: `rank_tier_source.py` asserted in its own docstring that nothing in the module puts the endpoint in an exception message, and that was FALSE at five raise sites, one of which escaped as a raw `ValueError` from outside the try. The guard cited as enforcing it filtered on `"ident"`/`"digest"` and was structurally blind to the `{url}` lines - vacuous against the exact string carrying a credential. **I closed it in-slice rather than filing it**, because a false written invariant is worse than none. Second catch: it flagged an undeclared `BACKLOG.md` in the merge set - that edit was MINE (RM-289) and I had not disclosed it when briefing the re-gate. Fair hit; the artifact caught what an agent-to-agent claim would not have.

**Don't-redo / durable:**
- `RC_DUO_SYNERGY_LIVE` defaults **ON**; `RC_RANK_TIER_LIVE` defaults **OFF**. Do not restate these backwards.
- The CN endpoint IS reachable from Legion (measured 1.28s, 200 rows) - do not file a "stale seed" row without re-measuring.
- `file://` on that seam is blocked incidentally: `urlopen` returns `status=None` so the `!= 200` gate raises. Measured. Not an SSRF.
- The qq drift guard is SHIPPED (RM-131); sentinel `qq_synergy_shape` live and fresh. Do not re-file it.
- `docs/DS_SWEEP_TRACKER.md` is a STALE id registry - tops out at RM-232 while ROADMAP carries RM-288. Mint from the true max across ROADMAP+BACKLOG.
- Two INHERITED suite failures, environmental, not lane 8's: live DS `:8860` = ENGINE `1.278.1` vs `1.278.0` pinned in the worktree, and Claude CLI `2.1.251` vs `PINNED_CLI = "2.1.220"`. CI on this branch has also been red/timeout since 2026-08-30 17:26 - inherited, diagnosed in cycle 29.
- Mutation harness trap re-confirmed: same-size mutants + mtime-to-seconds = stale `.pyc`. Run mutations with `-B` / cleared `__pycache__`.

**Suites (repo root):** `tests/` 2 failed / 19808 passed / 144 skipped / 4627 subtests. DS 10684 passed / 13482 subtests. 71 new tests, 46 mutants all killed (one survivor found and its test strengthened, not excused). No restart - worktree branch, not deployed.

# 2026-08-30t - lane 8 cycle 30: the live TFT coach asked the model for a field its own parser did not know, so the Set 17 headline advice was glued onto the ITEMS string and the key every consumer reads was never written

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD red-first, sole-merger inline.** Target `tft/tft_coach_engine.py` (806 -> 928 lines, not frozen), picked because it hit FOUR selection criteria at once: reads `API-Key-Claude.txt`, writes a file three consumers poll at 0.5 s, parses a model response it does not author, and had **zero dedicated tests**. Closed **RM-211**, filed **RM-286 / RM-287** plus an RM-261 addendum. LEDGER 1289.

**THE ROW I CAME FOR WAS REAL BUT IT WAS NOT THE WORST THING IN THE FILE.** RM-211 (non-atomic `_write_status`) was accurate and is closed. But one screen above it, `TFT_SYSTEM_PROMPT:70` asks the model for `God pick:` every round while `FIELD_MAP` knew only `carousel`. Unmatched prefixes fall into the continuation branch, so the god-pick line was appended to the PRECEDING field. Measured at the parent commit, not reasoned about: `items` came back as `"BF Sword on Jinx God pick: Evelynn boon"` and there was no `god_pick` key at all. The prompt was migrated to Set 17 ("NO carousels in Set 17") and the parser was not migrated with it.

**THREE INDEPENDENT SOURCES AGREED ON THE KEY, AND THE ENGINE DISAGREED WITH ALL THREE.** `core/coaching_payload.py:168`, `coaches/tft_coach.py:147`/`:200`, and `tests/snapshot_panels/fixtures/tft.json:12` all say `god_pick`; the engine wrote `carousel`, and `core/feature_policy.py:94` - a co-writer of the same artifact - was a third spelling. That is how a field goes missing with every test green: nobody owned the name.

**I CHECKED THE TWIN BEFORE ASSUMING IT WAS BROKEN TOO, AND IT WAS NOT.** `tft/tft_pbe_engine.py` uses a THIRD spelling (`god`), which looks like the same bug - but its prompt (`:170` `God:`), its `FIELD_MAP` (`:182`) and its payload all agree, so it is internally consistent and writes its OWN artifact. Left alone deliberately and the fence written into RM-286, because "align the twin" is exactly the plausible follow-up that would break a working engine.

**THE DEAD GUARD WAS PROVEN DEAD BY AST, NOT BY READING.** `fields["_stage_round"]` was assigned one line above `if not fields:`, so the dict always had a key and the guard could never fire. An unparseable response therefore blanked the whole overlay instead of holding the last good advice, and its warning had never once been emitted in production.

**THE CONFIG KNOB THAT DID NOTHING WAS THE ONE HOLDING THE LOCK.** `self._timeout` was assigned twice and read NOWHERE, so `messages.create` ran on the SDK default while `_run_safe` held `self._lock` across it. One slow call wedged TFT coaching and every later `submit` logged "busy - skipping" - a symptom that reads like load, not like a missing kwarg.

**MY OWN TOOLING HIT THE DEFECT CLASS I WAS FILING, THREE TIMES.** `Path.write_text` rewrites LF as CRLF on Windows. My mutation harness did it to `core/polled_json.py` (empty content diff, changed bytes - restored with `git checkout`), and my doc scripts did it to five files wholesale, inflating `ROADMAP.md` against its byte budget and even embedding real CR/LF bytes inside a BACKLOG sentence. All normalized back to LF and verified at 0 CRLF. The Edit/Write tools preserved LF correctly; only the raw `write_text` scripts did not. This is RM-287 in miniature and is why that row exists.

**20/20 MUTATIONS KILLED.** Every guarded production line was broken and each guard went RED, then restored. Two of my own tests were wrong first and I fixed the TESTS, not the code: one pre-poisoned the attribute it then asserted on, and one counted a scratch write belonging to `core/coaching_timestamps` (a different file, RM-261 class).

**BOTH REMAINING RC FAILURES PROVEN NOT MINE.** `test_live_three_profiles` sees `1.278.1` from the LIVE DS service against `1.278.0` on this branch (lane 6 territory); `test_cli_version_still_matches_the_pin` expired on a CLI 2.1.220 -> 2.1.251 upgrade, exactly as its own message says it should. Neither test imports either module I touched.

**NO RESTART, AND THE REASON MATTERS.** The live RC process runs `C:\Riot Commander` (main tree), which does not contain this branch. Restarting would have verified nothing about this code. That step belongs to the merger, and lane 8 ships last by design.

**NEXT.** `tools/drift_guard.py` still reports 1 breach: `ROADMAP.md` sits at 99% of its 81920-byte budget. INHERITED - HEAD was 99.7% and I left it net-NEGATIVE (81687 -> 81481) while the hard budget test passes. Clearing the 90% warn threshold needs ~8 KB relocated, which is a `/sync-all-md` or lane-7 job, not lane-8 depth work. Two orphaned stashes (RM-285) remain untouched - shared stack, not this lane's to pop.

# 2026-08-30s - lane 8 cycle 29: the gate that exists so one operator intent cannot fire twice was a check-then-act on a threaded server, and the branch's inherited CI red was not the platform bug it was filed as

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD red-first, sole-merger inline fix (the two target files are tightly coupled, so splitting them across worktree agents would have collided on one test file) + an independent adversarial refutation pass + TWO independent verifier gates, 5/5 mutants killed after a full re-run. Both gates returned REFUTE and both were right; the second found a latent breakage the entire suite was structurally unable to see.** Target `dashboard/_idempotency.py` and its sole consumer `dashboard/routes_loop_control._apply_idempotent`, neither frozen. HALF-CLOSED **RM-281** and REFUTED its stated diagnosis. Filed **RM-283**. Detail in `docs/LEDGER.md` 1288. **Next free id = RM-284.**

**THE INHERITED RED WAS NOT A PLATFORM BUG, AND THE ROW THAT FILED IT SAID IT WAS.** RM-281 recorded `test_halt_save_raises_STOP_while_a_reader_holds_it` as "an OS divergence, the same class cycle 26 closed", reasoning from the fact that the file gives 13 passed on Windows and fails on the Linux runner. The platform was never the variable - test ORDER was. `dashboard/_idempotency` holds its replay table in a module-global `OrderedDict` with a 900 s TTL and the suite had no global isolation for it, and `test_session_intents.py:43` shares the literal key `a1b2c3d4-0000-4000-8000-000000000001` with `test_loop_control_contention.py:125`. When xdist lands both files on one worker, the second file's `halt_save` POST REPLAYS the first file's answer, short-circuits before the side effect, and `os.replace` is never called - so cycle 26's injector correctly reports `0 matching attempts`. **It reproduces on Windows in 1.8 s** by running those two files in one process (1 failed / 54 passed). Cycle 26's guard and mechanism were both right; only the explanation pinned to the residual was wrong.

**THE PER-FILE FIX ALREADY EXISTED, WHICH IS EXACTLY WHY THE CLASS STAYED OPEN.** `tests/test_loop_control_idempotency.py:56` has carried the correct autouse fixture since S2, docstring and all ("The replay table is process-global - isolate every test"). A guard one file opts into does not cover the file nobody has written yet, so the fix went into `tests/conftest.py`. A cross-file sweep then measured the class: **3 colliding keys across 4 test files**, two of which were safe only by the luck of both files having their own fixture. No `skipif` - RM-281 prohibits it and `feedback_unrun_gate_is_where_the_bug_hides` is why.

**THE AUDIT FINDING IS BIGGER THAN THE CI RED AND DEFEATS THE MODULE'S WHOLE PURPOSE.** `_apply_idempotent` was `seen` -> side effect -> `remember`. Both calls are locked; the SEQUENCE is not, and `dashboard/server.py:54` is a `ThreadingHTTPServer`. Two same-key requests on two threads therefore both passed the gate and both acted - on a control plane whose destructive actions are `fire_lane` (launches a lane) and `interrupt` (kills processes). The module's own docstring states the requirement it was failing: "a phone retrying over Tailscale must not kill twice." **A retry storm is CONCURRENT, not sequential**, so the defended shape was the one that never happens. Replaced with an atomic `claim` / `settle` / `abandon`: one lock decides replay/act/wait, the winner holds an exclusive reservation, a waiter blocks on it (bounded, `INFLIGHT_WAIT_S` 2 s) and inherits the settled answer with `replayed: true`, and a non-200 or a raised side effect ABANDONS so the next caller may act. The old "only settled 200s are remembered" contract is byte-for-byte unchanged.

**A CLIENT MIS-MAPPING DECIDED THE DESIGN, AND IS NOW ITS OWN ROW.** The in-flight timeout returns HTTP 200 + `ok:false` rather than the more natural 503, because `web/mc/mc.js:51-53` maps EVERY 503 to the literal "auth not configured on the server" and returns before `r.json()`. A 503 would have told the operator their auth was broken when the request was merely still running. **RM-283** files the mis-mapping itself, which already misreports a real lane-launch failure the same way - a friendly message that is actively wrong, the Error Handling rule inverted rather than obeyed.

**I STATED THE ONE HOLE IN MY OWN DESIGN RATHER THAN LETTING THE ADVERSARY FIND IT.** If `MAX_ENTRIES` (512) distinct keys are remembered between an owner settling and a waiter waking, the settled answer is evicted and the waiter - which cannot tell an evicted key from an abandoned one, since both must mean "act" - fires twice. It needs 512 distinct intents inside one wake-up on a single-operator surface, and the pre-claim code had the identical hole through `seen()`, so it is a bound, not a regression. The comment at `MAX_ENTRIES` says so.

**BOTH REMAINING LOCAL FAILURES WERE PROVEN NOT MINE BY EXPERIMENT, NOT BY INSPECTION.** The new conftest fixture runs for every test in the suite, so "those tests do not import my module" is not sufficient evidence. Each failure was re-run with the fixture enabled AND disabled and fails identically both ways. One is this branch's `ENGINE_VERSION` 1.278.0 against the live DS on 1.278.1 while the lane sits 26 commits behind `main` (resolves on merge); the other is the Claude CLI pin expiring by design, 2.1.220 -> 2.1.251. Both are already filed under RM-281's siblings.

**THE ADVERSARIAL PASS CORRECTED MY DESIGN TWICE AND ONE CORRECTION REINTRODUCED THE BUG I WAS FIXING.** My `INFLIGHT_WAIT_S` was 10 s on a comment claiming the guarded side effects were "none of them slow" - false both ways, since `ops/loop/interrupt.py:297-298` kills serially at `timeout=20` each (20N seconds) and `_fire_lane` runs `git worktree add` on a first fire, so the ceiling expired in NORMAL operation. My timeout text then said "retry in a moment", and since every client mints a fresh key per send (`mc.js:717`), a re-send is a NEW key this gate cannot dedupe - the advice manufactured the double-fire the protocol exists to prevent. Now 2 s, absorbing only a transport-level re-send, and the text says do not re-send. Second: the refusal carried no `refused` field, and only `_mcFire` treats a bare `ok:false` as a refusal - `_mcFireLane` and `_mcIrqFire` would have rendered a still-running INTERRUPT as FAILED. Also fixed from that pass: `settle`/`abandon` sat outside the unwind so a raising `settle` stranded the key forever (no TTL, no purge, invisible to `size()`); the `_INFLIGHT` "bounded" comment was a caller contract, not an invariant; and my thread-pinning citation named `dashboard/_handler.py`, which is not the server this route runs on (`mc/handler.py` sets no `Handler.timeout` at all - re-probed, not taken on the agent's word). **And it proved one of my own tests VACUOUS:** the abandon test passed on the fully reverted gate because it never proved the waiter had blocked. Mutation B killed it; a full revert did not. Those are not the same check.

**THE VERIFIER RETURNED REFUTE AND WAS RIGHT TO, FOR A REASON I CREATED.** I dispatched it against a tree I was still editing, so it measured three states of two files and caught two real-at-the-time reds: `ROADMAP.md` over its size budget, and the concurrency test failing `KeyError: 'replayed'` once the wait dropped to 2 s inside the test's own probe window. Both fixed (the tests now pin `INFLIGHT_WAIT_S` themselves so they measure the protocol, not the shipped constant), and the gate was re-run against a FROZEN tree. **The lesson is scheduling, not verification:** a verifier pointed at a moving tree cannot return a usable verdict.

**TWO THINGS LEFT DELIBERATELY UNTOUCHED, BOTH FILED.** `tools/drift_guard.py` returns `1 breach(es)` on this branch - `ROADMAP.md` past its 90 percent warn line - and that is **INHERITED**: at cycle 29's parent commit the file was already 98.1 percent of budget on a CRLF checkout, and this cycle took it to 100.0. Clearing it needs ~8.2 KB relocated to `docs/ROADMAP_HISTORY.md`, which is tree-shaped doc gardening (lane 7), not a depth audit of one file - **RM-284**, whose acceptance is calibrated to the guard's own 90 percent line rather than to the raw budget. And `git stash list` is NOT empty, deliberately: two orphaned entries (`29be477b` cycle16, `0236273b` cycle13) hold ~460 lines of uncommitted work in live modules including `coaches/sr_user_builds.py` and `core/smoothed_rates_101qq.py`. The wrap ritual asks for an empty stack; dropping another cycle's work is irreversible and was never authorised, so they are filed as **RM-285** with their SHAs and an `apply`-not-`pop` recovery note instead. The shared stash stack means a bare `git stash pop` in ANY worktree on this machine can eat one of them.

**NEXT.** RM-281 stays OPEN for its other half - the `test_home_week_pool_rows` Playwright timeout alongside unfetched git-LFS pointers, untouched here. RM-283 needs the UI fixture ritual since it is a `web/mc/` page-behaviour change. **RM-284 is the doc-budget row and its sharper half is a CRLF landmine.** `ROADMAP.md` finished 16 bytes under its 81920-byte budget after SIX rounds of compressing this cycle's own entry - but the real find is that the budget is line-ending sensitive. `core.autocrlf=true` and `.gitattributes` forces `eol=lf` for `*.py` only, so a Windows checkout writes it CRLF and it grows 217 bytes. My own Python writes had silently converted all four rewritten `.md` files CRLF -> LF, so the guard read 81917 and passed while the real checkout size was 82134, over budget - green on my disk, green on Linux CI, red on the operator's next checkout. Caught by the verifier, not by the suite. All four files restored to CRLF and the entry retrimmed against the TRUE number. The durable fix is a `*.md text eol=lf` rule, not the trim. Nothing was deployed: Mission Control is live on `:8895` (pid 5616) but loads this code from the main checkout, so the fix needs the merge before a restart means anything.

# 2026-08-30r - lane 8 cycle 28: the lock that serializes every write to coaching_data.json failed OPEN on both halves with no logger at all, and the out-of-process writer its cross-process half exists for does not exist

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 3 parallel agents (tests / adversarial refutation / machine-environment) + sole-merger production fix + verifier gate, 29/29 mutants killed.** Target `core/coaching_data_lock.py` (101 -> 528 lines, not frozen). Closed **RM-199**, filed 2026-08-14 by the lane-5 refill. Filed **RM-277 / RM-278 / RM-279 / RM-280 / RM-281 / RM-282**. Detail in `docs/LEDGER.md` 1287. **Next free id = RM-283.**

**THE PICK SURVIVED RECALL AND THE ROW CARRIED A FENCE THAT MATTERED.** Criterion 3 (holds a cross-process lock) + criterion 4 (four production call sites, no test module of its own). RM-199's DO-NOT-REDO fence - **reentrancy is SETTLED per LEDGER 404, do not propose an RLock** - was honoured; the nesting question was not re-filed.

**THE HEADLINE IS NOT THE FAIL-OPEN, IT IS THAT THE CROSS-PROCESS HALF PROTECTS NOBODY.** The docstring named `aftergame_summary.write_to_client_coaching_data` "(may run out-of-process via supervisor)" as the sharer the file lock exists for. False three ways, each verified twice: that module has **ZERO production importers**, never takes the lock, and targets `data/coaching_data.json` while the SR file is root `coaching_data.json`. RC is ONE process with the dashboard in-process; the only separate process touching the file (`agents.supervisor:774`) merely READS it. **Not deleted on that basis** - it is the only thing that would work if a writer ever moves out of process - but the docstring now says which is true.

**FOUR OF MY OWN DESIGN DECISIONS WERE CORRECTED BY THE AGENTS I ASKED TO CHECK THEM, AND EVERY CORRECTION WAS RIGHT.** (a) My first `_TL_TIMEOUT_S` would have made fail-open ROUTINE - a legitimate holder can occupy the section ~6.2s, so a snug timeout trades a rare hang for frequent lost updates; set to 30s. (b) My mkdir re-arm fired on ANY failure, defeating the memo exactly under a persistent block on the FILE; narrowed to directory-implicating failures. (c) That narrowing then failed in the WORSE direction - portalocker's timeout branch re-raises a saved `LockException` with NO `__cause__`, so a directory-missing failure could read as file-only and the memo would NEVER re-arm, permanent degradation; backed by an exact `is_dir()` check on the failure path only. (d) My log cap re-created the very RM-199 silence it was added to prevent; my replacement counted successful ACQUIRES, which measures the wrong axis (the holders poll, so 50 successes is seconds); rebuilt on a monotonic clock floor - **and the direction in my own comment was also wrong**, since a BACKWARD step suppresses and it is a FORWARD jump that wrongly re-opens.

**RM-199'S OWN THIRD CLAIM WAS REFUTED.** "An exception from the bookkeeping above `_tl.release()` leaves `_LOCK` held" cannot fire - the bookkeeping is `bool(self._fl)` plus two attribute stores, and `portalocker.Lock` defines no `__bool__`. The first test written for it manufactured the defect with a raising `__bool__` the real class lacks (`feedback_subagent_fixture_shaped_to_bug`), caught by an adversarial agent, not by me. The REAL variant is strictly worse and was fixed: no `try/finally` anywhere, so a BaseException in `__enter__` orphaned the process-wide lock with `__exit__` never running.

**ALL THREE NEW GUARDS WERE GREEN-BUT-UNEXERCISED WHEN FIRST WRITTEN.** After adopting the corrections the suite still read 69 passed / 0 failed - which was the problem, not the proof (`feedback_guard_on_nondefault_call_path_is_untested`). Deleting the exact `is_dir()` fallback now goes RED, and restoring the unconditional re-arm now DIES, which is the actual evidence the narrowing is tested. The mutation score was re-measured in FULL after each of three production revisions rather than carried forward, and four anchors were reported DEAD rather than counted as kills once their mechanism was deleted.

**BACKFILL: NOTHING TO REPAIR, MEASURED NOT ASSUMED.** The already-bad state was an absence of records. All three failure modes checked live anyway: no wedged coach thread (pid 16344, `alive` true, `ui_pulse_age_s` 0.3), no stranded OS lock (an agent took `ops/runtime/coaching_data.lock` EXCLUSIVE and released it, mutating nothing), zero `coaching_data_lock` hits in any dated log - expected, since the module had no logger. **Deployment needs the MERGE:** live RC loads this module from `C:\Riot Commander` (a checkout of `main`), so `restart_trigger.txt` alone deploys nothing.

**THE BIGGEST FINDING IS FILED, NOT FIXED, AND THAT IS DELIBERATE. RM-277:** `grep -rn coaching_data_lock app/` returns **ZERO hits** while `app/__init__.py:270` and `:241-253` write the same file unlocked from three call sites, with a read half that can be seconds stale via the 500 ms poller. All three files are FROZEN, so it needs an adjudicating agent. Also filed: RM-278 (two lock-holding writers can atomically clobber the file with `{}`, and the lock makes that land cleanly), RM-279, RM-280, RM-282.

**CI ON THIS BRANCH IS RED AND IT IS NOT MINE - RM-281.** Run 33340966410 failed at HEAD `9862eb32`, before this slice. Two failures: cycle 26's own `replace_fails` guard correctly reporting that a loop-control test proves nothing on Linux (it passes on Windows - 13 passed locally), and a Playwright timeout alongside unfetched git-LFS pointers. **Cycle 26 recorded that five consecutive lane-8 cycles each declared done over an inherited red they had contributed to. This cycle is not doing that** - both are named, proven not mine, and filed rather than absorbed into a depth slice about a lock.

**GATE.** RC `2 failed / 19690 passed / 144 skipped / 4625 subtests`; DS `10684 passed / 13482 subtests`, unchanged. The verifier re-derived the baseline itself rather than trusting cycle 27 (pre-slice collection 19764, post-slice 19836) - delta exactly **+72**, the count of the new file. Both failures proven inherited **by import graph**, not inference: neither failing test pulls `coaching_data_lock` or `_writers` into `sys.modules`. One is branch `ENGINE_VERSION` 1.278.0 vs live DS 1.278.1 while `origin/main` is already 1.278.1; the other is a CLI pin that expires by design. **The verifier also caught two stale line citations in the docstring I had just written, both created by my own edit to `_writers.py` in the same commit** - the exact rot this lane removes, introduced and removed inside one slice.

**ROADMAP BUDGET IS NO LONGER THE EMERGENCY IT WAS.** Cycle 27 handed over 64 bytes of headroom. A relocation pass moved 16 fully-closed pointer stubs to `docs/ROADMAP_HISTORY.md` verbatim (81856 -> 77551), and after this cycle's rows it sits at **80139, 1781 free**. The cycle-25 fence held: **RM-117 was NOT moved** because its body carries a pending operator decision on data retention under a shipped-looking header, and five more were left for naming live siblings or inline fences.

# 2026-08-30q - lane 8 cycle 27: the control that gates every outbound fetch failed OPEN when its own config went missing, and three separate spellings walked past it

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 3 parallel test-writing agents on disjoint files + sole-merger production fix + verifier gate, 12/12 mutants killed.** Target `lib/http/client.py` (277 -> 547 lines, not frozen). Closed **RM-255**, filed by lane 8's own cycle 22. Filed **RM-273 / RM-274 / RM-275 / RM-276**. Detail in `docs/LEDGER.md` 1286. **Next free id = RM-276.**

**THE PICK CAME FROM THE FILED MENU AND SURVIVED RECALL.** `lib/http/client.py` hit criterion 1 (chokepoint for every third-party fetch: DDragon, the icon CDN, the aggregator D / aggregator B scrapers) and criterion 4 (**zero tests**). Reachability was measured, not assumed: `RC-Phase3-Supervisor` is a RUNNING scheduled task and it drives the pipeline that imports this client. Recall returned no CLOSED/REFUTED hit, but it did surface the two adjacent cautions that mattered - item-1274 (a denylist that was INERT against percent-encoding) and item-1179 (a lane-8 test that was green only because of the worktree path).

**THE FILED ROW WAS RIGHT ABOUT THE HEADLINE AND WRONG ABOUT ITS SECOND HALF, AND THAT WRONGNESS IS THE MORE USEFUL FINDING. `requirements.lock` IS UTF-16LE.** RM-255 asserted certifi "appears in NEITHER `requirements.txt` NOR `requirements.lock`". certifi 2026.2.25 is in fact importable on Legion AND present in the lock at line 12 of 91 - but the lock begins with the BOM `ff fe`, does not decode as UTF-8 at all, and `grep -c certifi requirements.lock` returns **0** against a file that plainly contains the string. Every grep-based dependency audit in this repo is blind to that file. Filed **RM-273** (LANE 7, behaviour-free to fix - nothing installs the lock). The residual real defect: certifi is UNDECLARED in `requirements.txt`, the file CI installs, so the CA-store hardening was working on Legion by accident through httpx's dependency chain. Declared it.

**THE HEADLINE, MEASURED BEFORE ANY EDIT.** Missing blocklist -> `is_blocked("https://reddit.com/x")` **False**. Half-written blocklist -> **False**. Good file then truncated mid-life -> flipped a live client **True to False**, because `_maybe_reload_blocklist` re-runs on EVERY call so one mid-write `stat` latched the fail-open. Now fails CLOSED when nothing was ever loaded, and RETAINS last-known-good when a previously good list goes bad - never silently widened, and deliberately NOT fail-closed in that second case, because turning a transient mid-write into a total outage of all outbound fetching is the wrong trade.

**THREE BYPASSES, EACH ONE CHARACTER OR ONE HOP WIDE.** A trailing dot (`reddit.com.` is a legal FQDN resolving identically, and it defeated BOTH the host set and the suffix list, since `"site.ru."` does not end with `".ru"`). A scheme (`file://localhost/...` was a LOCAL FILE READ through the HTTP client, because the default opener carries `FileHandler`). A redirect (the blocklist was checked only on the initial url while the opener follows 302s). All three closed.

**TWO DEFECTS IN MY OWN FIX, BOTH CAUGHT BY THE AGENTS I ASKED TO CHECK IT - THIS IS THE PART TO REMEMBER.** (a) I cached the failed file's mtime to avoid a log flood, which means a writer that truncates and finishes inside the SAME filesystem tick leaves a perfect file the client never re-reads: a PERMANENT fail-closed outage, strictly worse than the fail-open I was fixing. **My own probe passed only because it forced a distinct mtime with `os.utime` - a fixture shaped to the bug.** (b) I added a half-open probe slot without guaranteeing its release; `http.client.IncompleteRead` is an `HTTPException`, so it escapes every handler and would wedge that hostname `CircuitOpen` for the life of the process. Both fixed and both now pinned by tests.

**THE BREAKER NEVER DID WHAT ITS DOCSTRING PROMISED, AND THE SLICE CORRECTED ME IN BOTH DIRECTIONS.** No probe marker existed, so every concurrent caller was admitted at once. Worse and found by the slice, not by me: `_on_failure` only stamped `opened_at` when it was `None`, which an open breaker never is, so **a failed probe never restarted the cooldown and the breaker became a permanent no-op against a permanently-down host**. My own D1 hypothesis was PARTIALLY REFUTED - `_check_breaker` never read `st.failures` at all; it read `opened_at` twice unlocked, and the real consequence is `now - None` raising `TypeError` from a method whose declared failure mode is `CircuitOpen`.

**MUTATION FOUND GAPS THAT 71 GREEN TESTS DID NOT.** First pass: 9 mutants, only 7 killed. Both survivors were real holes - the empty-string filter (one stray `""` in `suffixes` makes `endswith("")` true for every host, so one typo denies the entire internet) and the `request()` scheme guard, which is very nearly an EQUIVALENT mutant because `is_blocked()` also refuses the scheme and only the MESSAGE differs. Pinned the message rather than delete a guard with real diagnostic value. Final 12/12, every anchor asserted present-exactly-once before mutating, every restore sha256-verified.

**ONE AGENT FINDING REFUTED AND RECORDED AS SUCH.** `Blocked` subclasses `HttpError`, so in principle a caller retrying a network blip would retry a forbidden host. All three consumers checked: two enumerate `Blocked` explicitly, the third logs and returns `False`. **No caller retries.** Latent, not live - no row minted.

**BACKFILL: NOTHING TO RECOVER, MEASURED NOT ASSUMED.** `lib/http/blocklist.json` has exactly ONE revision in git and it parses clean, so the fail-open branch could only ever have fired on a filesystem fault. (The supporting log evidence is WEAK on its own and is labelled that way - no dated `logs/YYYY-MM-DD.log` files exist right now.) **DEPLOYMENT IS THE REAL HAND-OFF:** `RC-Phase3-Supervisor` is Running and imports this module from the MAIN tree, so nothing was deployed from the worktree (item-1179); it needs the merge plus a bounce of that task. `restart_trigger.txt` does NOT reload it.

**GATE.** Baseline BEFORE any edit `2 failed / 19547 passed / 144 skipped / 4575 subtests`; after, `2 / 19618 / 144 / 4625` - delta exactly **+71 passed / +50 subtests**, both failure node ids byte-identical and both re-verified inherited (branch `ENGINE_VERSION 1.278.0` vs live DS 1.278.1 while `origin/main` is ALREADY 1.278.1, so it resolves on merge; and the Claude CLI pin expires by design). DS `10684 passed / 13482 subtests`, unchanged.

**OWED TO THE NEXT LANE, AND IT IS NOW URGENT: `ROADMAP.md` sits at 81856 of its 81920-byte cap - 64 bytes, and only because this cycle trimmed its OWN pointer row twice.** The next row added breaks `tests/test_doc_size_budget.py`. Cycle 25 flagged this, cycle 26 bought a little room by tightening its own pointer, and this cycle could only fit by appending to an existing row's tail instead of adding one. **RM-246 covers the relocation pass** and it needs a reader who checks each row's BODY - cycle 25 measured that the largest shipped-looking candidate actually contained a PENDING OPERATOR DECISION under a header reading SHIPPED.

# 2026-08-30p - lane 8 cycle 26: this lane's own tests used the OPERATING SYSTEM as their fault injector, so the atomic-write retry path had never run on Linux

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 3 parallel agents on disjoint files + merger-side re-verification, zero production files changed.** Closed **RM-270**, filed by lane 8's own cycle 25 one cycle earlier. Filed **RM-272**. Detail in `docs/LEDGER.md` 1285. **Next free id = RM-273.**

**THE PICK CAME FROM PRE-FLIGHT, NOT FROM THE MENU - AND IT WAS THIS LANE'S OWN MESS.** `gh run list` showed `lane/true-audit` red. Reading the failed log instead of inheriting a verdict showed all nine failures were tests LANE 8 ITSELF wrote, accumulated across cycles 12, 13, 20, 21 and 23. The lane's wrap step requires green CI, so five consecutive cycles each declared done over an inherited red they had each contributed to. **The mandatory `perseus_recall` did NOT surface RM-270** - it was filed the same day and the vault is a mirror that had not been re-synced. A same-day row is invisible to recall by construction; it was found by reading `BACKLOG.md` during the write-up.

**THE FINDING THAT MATTERS IS THE HALF THAT WAS NEVER RED.** Four tests asserted a failure the OS supplies only on win32 (`with open(target): assertRaises(PermissionError)`) and went red on Linux. **Eight more asserted only the CLEANUP AFTER that failure and were passing VACUOUSLY there** - no fault occurred, so there was nothing to clean up and nothing to assert. Those never appear in a CI failure list, which is why the count kept growing unnoticed. Two thirds of the damage was invisible. Consequence: the exhaustion branch of `core.polled_json._replace_with_retry` - the re-raise in the primitive every polled writer in the tree funnels through - had never executed on a Linux runner.

**RM-270 OFFERED (a) SKIP / (b) A WINDOWS CI JOB / (c) ASSERT THE INVARIANT AND RECOMMENDED (c); THIS RUN CONVERGED ON (c) INDEPENDENTLY, THEN SPLIT THE NINE.** Twelve tests took (c) verbatim via `tests/_replace_faults.replace_fails`, which injects the fault at the `os.replace` boundary so the retry, backoff, cleanup and re-raise run on every platform with every original assertion unchanged - **coverage went UP, not down.** The five OCR failures are NOT (c) and are not dressed as it: `pytesseract` is absent on the runner, which is a capability gap no invariant rewrite can fix, so they skip per-test (not per-class, because `handle_ocr` validates shape BEFORE importing the OCR stack by design and those guards must keep running).

**THREE THINGS NOBODY WENT LOOKING FOR.** A sixth OCR test was green-but-vacuous (`.get("result", {})` on an error dict is `{}`, so its `assertNotIn` proved nothing). **My own stated acceptance was false as written** - "the pre-import tests still passing proves you did not over-skip" failed because those tests asserted only `assertIn("error", r)`, which the ImportError short-circuit also satisfies; the agent said so rather than satisfying it, and three assertions were tightened to pin the exact error token. And `test_reasonable_crop_is_still_processed` was vacuous on Legion too, since a refused crop and a crop that OCRs to nothing both return `{"ok": True, "result": {}}`.

**A PRODUCTION CHANGE I PROPOSED WAS REFUSED BY THE AGENT, CORRECTLY.** I asked for an adjudication on the unrecorded `ImportError` path in `handle_ocr`. The agent declined on three grounds - my premise was inaccurate (the `no crops` path also returns unrecorded, so it matches its real neighbour), recording there would turn the error-count assertions green on a stack-less box for the WRONG reason, and a missing stack is a static machine fact that would flood an 80-entry ring - and filed RM-272 instead. **The agent that produced a thing never grades it, and the merger proposing a change does not make it right.**

**THE REPO'S OWN SKIP-HYGIENE GUARD REJECTED MY FIRST GATING AND WAS RIGHT.** The three real-OS tests gated on an imported `WINDOWS_SHARE_LOCKS` constant; `tests/test_skip_condition_hygiene.py` failed all three as `UNRESOLVED` because it resolves a skip only when the condition names `sys.platform` DOTTED at the site. The named constant was more readable and less checkable. Inlined the dotted form and deleted the now-dead constant. **Excusing the guard was considered and rejected** - self-excusing a hygiene guard is precisely the anti-pattern this cycle exists to close.

**MUTATION TESTING CAUGHT WHAT PASS COUNTS COULD NOT.** One agent's first mutation **did not apply** (CRLF, `s.count(old) == 0`) and the suite reported a clean 18 passed - the `feedback_mutation_that_fails_to_apply_looks_green` trap exactly; only the anchor assertion caught it. One equivalent mutant was found and the MUTANT was swapped, not the test. I also rederived three hardcoded `attempts == 4` assertions to `len(_REPLACE_RETRY_DELAYS_S) + 1`, which introduces a tautology risk on an empty table - so I **tested it rather than reasoned about it**: mutating the table to `()` still produced 9 red.

**ACCEPTANCE MEASURED ONCE BY THE MERGER, NOT INHERITED FROM THREE AGENTS.** One probe applied every CI divergence together: **112 passed, 6 skipped, 4 deselected, 0 failed**, against 9 failed before, with **`real os.replace share-lock PermissionErrors = 0`**. 112 + 6 + 4 = 122, matching the Windows run exactly. On a real Linux runner that projects to **112 passed / 10 skipped**: the 6 capability gaps plus the 4 win32-gated real-OS tests, which skip there by construction and are deselected here rather than faked (forcing `sys.platform` on Windows breaks pytest's own `tmp_path_factory`, which takes the POSIX branch and calls the absent `os.getuid`). **The verifier then caught three overclaims and all three were MINE:** the probe's POSIX-environ simulation was never implemented, a FOURTH win32-gated test had been left selected (so my first 113/6/3 - and a third number already written into BACKLOG - were wrong), and "zero real share-locks" covers `os.replace` only. All corrected before the commit; the lesson is that the merger's own evidence needs the same adversarial pass as the agents'. Two suite failures remain and both were VERIFIED inherited: the Claude CLI moved 2.1.220 -> 2.1.251 (that pin expires on upgrade by design), and the live DS at `:8860` is on ENGINE 1.278.1 while this branch is 1.278.0 - `origin/main` confirmed at 1.278.1, so it is branch lag that resolves on merge.

**DURABLE:** a guard whose fault injector is borrowed from the OS is a guard that only exists on one OS. `replace_fails` raises when it never fires, so a test built on it cannot pass vacuously, and `tests/test_atomic_write_fault_injection_is_portable.py` ratchets the idiom - with its docstring stating what it does NOT catch, so the name cannot overclaim. Its own self-test found a real bug in it.

# 2026-08-30o - lane 8 cycle 25: garbage from a relay agent did not pass through, it DISABLED the self-heal built for that agent dying

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 22 mutations across two harnesses all killed, verifier-REFUTED once then fixed and re-gated.** Target `vision_server/_relay.py` (205 -> 288 lines, not frozen), the seam between the two relay agent processes and the coaching pipeline, picked on criterion 1 (it parses the Live Client `:2999` envelope) and criterion 4 (no test module carried its name). Closed **RM-253**, filed by lane 8's own cycle 22. Full detail in `docs/LEDGER.md` 1284; RM-266/267/268/269 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`. **Next free id = RM-271.**

**RECALL SELECTED THE FILE THIS TIME INSTEAD OF CANCELLING A PICK.** Cycles 20, 22 and 23 each had a candidate killed by the mandatory pre-flight `perseus_recall` (already audited). This run it returned cycle 6 (`tools/liveclient_relay.py`, the agent that POSTS) and cycle 7 (`core/liveclient_cache.py`, the consumer that READS) - the two slices either side of this one - and surfaced RM-253, an open row against this exact file. The middle of that sandwich had never been audited and had a standing lane-8 finding waiting on it. The step keeps paying because a hardened file reads exactly like an unaudited one.

**THE HEADLINE IS THE CONSEQUENCE, NOT THE MISSING GUARD.** Neither upload handler validated SHAPE, only the JSON decode - while the sibling `vision_server/_frame.py:231` has carried that guard since cycle 20 AND this file's own self-read path validates it at `:132`/`:156`. The asymmetry inside one package is what makes it an omission rather than a choice. What it cost: a TRUTHY non-object was cached with a FRESH `ts`, and `get_latest_liveclient` gates the 1-PC self-heal on `age > _SELF_READ_STALE_S`, so junk did not merely reach consumers - **it suppressed the in-process :2999 read that exists to keep coaching alive when the relay agent dies**, 2 s per push, indefinitely if the agent malfunctions fast enough.

**LIVE EVIDENCE BEAT A HYPOTHETICAL ON THE STATS DEFECT.** The LCU counter could not record a failure by construction (empty body returned before any `_record`, bad JSON recorded nothing, success passed a hardcoded 0 ms). Rather than argue it from the source, the live server was probed: `lcu_upload` at **68275 calls, errors 0, total_ms 0** across 19.3 h. That is not a clean record, it is a counter that cannot move - and it is the cycle-20 `_inference.py` class ("counted a failed read as a success") in a second module.

**A RESULT DELIBERATELY WEAKENED AFTER A REACHABILITY CHECK.** One of the seven fixes (`lcu_get_result` handing out its stored dict) is real and mutation-killed but changes NO live behaviour: the function has zero production callers and is not re-exported, because `_http.py:143` hand-rolls the same lookup inline. Counted as a latent-hazard fix, not a live one, and recorded that way in the test docstring (`feedback_verified_claim_vs_measured_downstream`). The duplication became RM-266. Three dimensions came back genuinely CLEAN with evidence: POST is auth-gated before any handler, the body is capped at 10 MiB before `rfile.read`, and the loopback bind was confirmed LIVE via `Get-NetTCPConnection` rather than read off source.

**THE VERIFIER REFUTED MY MUTATION CLAIM AND WAS RIGHT - READ THIS BEFORE TRUSTING A MUTATION SCORE.** My harness ran 14 mutations and killed 14, but it never mutated the DURATION argument of a `_record` call, so the measured `int((time.time() - t0) * 1000)` on the LCU success path could be replaced with a literal `0` and the WHOLE SUITE STAYED GREEN. That is the latency half of W4 - the half the live `total_ms: 0` evidence rests on, and the half the file's own AUDIT comment claims to fix. `grep -c total_ms` over my test file returned 1, and that hit was a setUp reset, not an assertion. **A high mutation score proves the mutations you thought of, nothing more.** Fixed at root and swept for siblings rather than patched at the one site named: all eight `_record` sites were audited and TWO still passed a hardcoded `0` - the LCU empty-body exit written by this very cycle, plus the pre-existing liveclient one. New `UploadLatencyIsMeasuredNotHardcoded` pins all eight with a deterministic stub clock, because asserting `total_ms > 0` against the real clock would be flaky AND false: these handlers finish in well under a millisecond, so the honest measurement really is 0. A second pass mutated all eight BY LINE NUMBER (the expression is not textually unique across them) and killed 8 of 8, each by exactly one test, every restore sha256-verified. Separately, the 7 pre-fix-green tests are deliberate anti-tautology controls, and two mutations that break the CONTROLS themselves were also killed.

**HAND-OFF FOR THE MERGER - THE DEPLOYMENT IS NOT AN RC RESTART.** Dimension 4g caught that `:8889` is served by **pid 12148 running `moon_vision_server.py`, a different process from RC** (RC's own pid rotates - 23644 when I measured, 16344 for the verifier minutes later; the point is that it differs, not the number). That entrypoint is a 21-line shim doing `from vision_server import main`, byte-identical across both trees, so the live process does load this module - but `restart_trigger.txt` bounces RC and would NOT reload it. Deploying needs the merge plus a `taskkill /F /PID` of the vision-server pid, after which the `dashboard/server.py` self-heal respawns it. Nothing was restarted from the worktree (the live process reads the main tree, so a bounce from here deploys nothing - item-1179). Baseline taken BEFORE any edit: RC `2 failed / 19508 passed / 144 skipped / 4560 subtests`; after, `2 / 19538 / 144 / 4575` - delta exactly +30/+15, both failure node ids byte-identical and one self-diagnosing as a CLI version pin (2.1.251 vs 2.1.220). DS `10684 passed / 13482 subtests`, unchanged. **Backfill is honestly NOT repairable and is recorded as such:** in-memory state only, and the 68275 uploads already logged as flawless cannot be recovered - the counter simply starts telling the truth after deploy. Branch ready, NOT merged; lane 8 ships last.

**THE MOST IMPORTANT THING THIS CYCLE FOUND IS NOT IN THE AUDITED FILE: CI ON THIS BRANCH IS RED ON NINE REAL TEST FAILURES, ALL IN TESTS LANE 8 ITSELF WROTE (RM-270).** Run **33332566593** completed its pytest step in 38m52s and reported `9 failed, 30061 passed` - so it is NOT a timeout. The class is one thing five times: **a Windows-only behaviour asserted as universal on a Linux runner.** `test_performance_tracker_lane8_cycle23.py` expects `PermissionError` from `os.replace` over an open file, and its own docstring says "Measured on Python 3.14 / win32" - on POSIX that replace SUCCEEDS, so not raising is correct Linux behaviour. Same root cause in `test_loop_control_contention.py` (cycle 21, expects the 503 that sharing violation produces, gets 200) and `test_decision_detector_lane8_cycle12.py` (cycle 12). `test_handler_trust_boundary_hygiene.py` (cycle 13) dies in SETUP writing a lone surrogate into `os.environ`, which Windows tolerates as UTF-16 and Linux rejects. Five more are cycle 20's OCR tests with `pytesseract`/`PIL` absent and never skipped. **Verified: not one of those five files contains a `skipif`, `sys.platform` or `win32` guard.** **Be careful with cycle 24's note here - it is not wrong, it is about a different run:** 33327341297 emits no `FAILED tests/` lines at all, consistent with the timeout that wrap reported. Both are red; only the newer one is diagnosable. **Do NOT fix this with skip decorators** - RC is a Windows product whose CI runs Linux, so skipping means the concurrency guards lane 8 exists to write are never executed by CI again (`feedback_unrun_gate_is_where_the_bug_hides`). The recommended answer is to assert the INVARIANT (no torn file, no orphaned tmp, no double log) instead of the Windows MECHANISM. Not fixed here: nine failures across five files from five prior cycles is not a one-file depth slice, and the choice between skip / Windows CI job / rewrite is a real decision. **RM-270 has a `BACKLOG.md` body but NO `ROADMAP.md` pointer row, because the doc budget below had no room for one** - which is itself the argument for doing that relocation pass.

**OWED TO THE NEXT LANE: `ROADMAP.md` sits 14 BYTES under its 81920-byte CI cap, so the next row added WILL break `tests/test_doc_size_budget.py`.** Adding this cycle's pointer row breached it; the deficit was paid by relocating the cycle-22 four-id row (two of its four ids now closed) and then by shrinking THIS cycle's own row. A relocation pass is genuinely owed - and it needs a reader who checks each row's BODY, because the largest shipped-looking candidate turned out to contain a PENDING OPERATOR DECISION (tier-1/2/3 data retention) under a header reading SHIPPED. Moving it would have hidden a live decision to make a size check pass. **Second, smaller, same bucket and measured not guessed:** `scripts/wakeup_prune.py --keep 3` reports "3 session(s), nothing to do" while this file carries FIVE `# 2026-08-30*` headers, so the "newest 3 verbatim" contract is silently under-enforcing and blocks accumulate into the auto-loaded budget. Its `SESSION_RE` is fine - it matches all five - so the merge happens in the coarse block split above it. Pre-existing, NOT introduced here: the single-blank-line separator this cycle's block used is the same one cycles 24, 22 and 21 already use.

# 2026-08-30n - lane 8 cycle 24: the atomic-write contract was not atomic, and its "always returns a dict" reader raised

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 12/12 mutation-killed, verifier-gated.** Target `core/polled_json.py` (153 -> 218 lines, not frozen) - **the module CLAUDE.md points every new polled-JSON site at**, imported by 20 non-test modules. Closed **RM-254**, which lane 8's OWN cycle 22 had filed as "the one to do first". Full detail in `docs/LEDGER.md` 1283; RM-261/262/263/264 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`. **Next free id = RM-265.**

**MINTING THE ID ALMOST WENT WRONG IN A NEW WAY.** My repo-wide max-id scan returned RM-261 as taken, so I nearly filed 262-264. It was matching the PROSE `Next free id = RM-261` in cycle 23's own ROADMAP row, not a filed row. That is CLAUDE.md's anchored-predicate warning ("a bare grep matches prose ABOUT the trailer") landing on id minting. **Read what the match actually says before treating a number as consumed.** The tracker (`docs/DS_SWEEP_TRACKER.md`) is still stale at RM-232 - the already-filed RM-192 staleness, now three cycles old.

**THE HEADLINE: the module that exists to prevent torn reads was itself producing them.** All three public helpers derived the scratch filename from the DESTINATION alone (`path.with_suffix(path.suffix + ".tmp")`), so two writers of one file opened the SAME scratch file - B truncating and renaming it while A was still filling it. **Measured before any edit, two threads, 240 writes: 5 reads came back as unparseable JSON**, from the module whose docstring promises "readers see either the old content or the new, never a partial write". **Reachability turned out worse than RM-254 had recorded:** `data/force_scan.json` has FOUR writers, not the two the row assumed, and the fourth (`tools/lcu_agent.py:1372`) is a SEPARATE PROCESS - the case no in-process lock can serialize, which is why the pid is in the new scratch name and not just a random token.

**FOUR MORE DEFECTS IN THE SAME FILE, AND ONE HAD BEEN DOCUMENTED BY ITS OWN CONSUMERS FOR MONTHS.** `read_json_dict` promised the default for an "unreadable" file but let `UnicodeDecodeError` escape, because that is a `ValueError` and the handler caught `OSError`. **Three downstream sites already said so in prose** - `core/cost_tracker.py:491` spells out the exact escape, `coaches/_base_coach.py:502` and `:540` each widened a handler to absorb it. Nobody fixed the resolver. That is `feedback_resolver_fix_is_not_a_consumer_fix` with the evidence sitting in the codebase in plain English. All three comments were corrected here, since they now describe behaviour that no longer exists. Also fixed: a shallow `dict(default)` in both `read_json_dict` and `PolledJsonFile.__init__` that leaked nested mutables into the caller's own object, and an exhausted retry that orphaned the scratch file (which HAD to ship with the naming fix - a unique name that is never cleaned up is unbounded litter rather than a bug).

**THE MUTATION PASS CAUGHT A BAD MUTANT OF MY OWN.** 12 mutants, sha256-verified restore each time. First run 10/12: M8 survived because the constructor's deep copy is redundant for `read()` - not equivalent though, it still decides whether construction SNAPSHOTS the caller's dict, so it earned a test. **M10 was my error**: I "mutated" a line by appending a comment, which changes nothing. Rebuilt as three real mutants returning the caller's `default` by reference from each fallback branch - which then exposed that my own W2 test only covered the missing-file branch. Parametrized across missing/malformed/non-dict/undecodable. Final 12/12.

**READING THE ROW'S ACCEPTANCE CRITERIA IS WHAT CAUGHT MY WEAKEST CLAIM.** RM-254 asked for a two-PROCESS test; I had written a two-THREAD one and was about to close on it. The deterministic cross-process guard I then added goes RED under a revert. **The behavioural two-process contention test is recorded as a SMOKE test, not a guard, because I measured it: against the same revert it went red on only 1 of 2 attempts.** Kept anyway (it asserts an absence the fix makes structurally impossible, so it cannot flake red), but labelled in its own docstring so nobody trusts it later. Its two-thread sibling passed pre-fix and is labelled the same way - the cycle-22 lesson applied without having to relearn it.

**THE CONSUMER TRAP THIS CREATED, INCLUDING ON MY OWN LANE'S PREVIOUS WORK.** Three callers had hand-mirrored the helper's PRIVATE scratch name to clean it up after a failure. Two would have silently become no-ops: `dashboard/routes_loop_control.py:177-186`, and `performance_tracker.py:64` - **written by lane 8 cycle 23, five commits earlier on this branch**, with a comment explaining why it mirrored the name. `missing_ok=True` means they break quietly. Fixed all three plus `coaches/arena_coach.py`, the hand-rolled third `force_scan.json` writer that had no retry at all. **Changing a private detail of a shared helper is a breaking change when callers have encoded it.**

**FILED, NOT SWEPT: 96 modules still hand-roll the same destination-derived scratch name** (RM-261). Fixing the contract module reaches NONE of them. Sweeping 96 files is a lane-7-shaped commit, so the row names the small exposed runtime subset instead and notes it is the same writer set as RM-258 - convert each site once, satisfy both. Also filed: RM-262 (four U+2192 arrows in a LIVE Haiku prompt string at `coaches/arena_coach.py:159-160`, proven pre-existing 4-before/4-after against `git show HEAD:`, left alone because rewriting prompt bytes changes coaching output) and **RM-263, the limit this slice did NOT close: there is no `fsync` anywhere in the module**, so it is atomic against a concurrent reader but not against a crash - and `taskkill /F` is CLAUDE.md's prescribed restart path, so that window is exercised routinely. **RM-264 qualifies this slice's own work:** `PolledJsonFile` has ZERO production instantiations, so the W3 half of the shared-mutable fix is LATENT while the `read_json_dict` half is live. Said plainly rather than left to read as bigger than it is.

**THE VERIFIER RETURNED REFUTE AND EVERY PROSE FINDING WAS REAL.** It CONFIRMED the engineering independently - its own mutation matrix (W5 revert kills 3-4 tests, W4 4, W1/W2 9, W3 1), five adversarial failure paths with zero scratch litter, 20000/20000 unique names, zero frozen files, `SHARED_SHA256` untouched. Then it found five prose defects that were on disk. **The one worth carrying forward: I fixed the broken line-69 citation of `dashboard/_writers.py` in the `.md` files because that is what the citation guard reported - and the guard scans `.md` ONLY, so three `.py` copies of the same broken citation survived.** Fixing what the guard reports is not fixing the defect (`reference_guard_domain_narrower_than_its_name`). It also caught that I wrote "three writers" in the contract module and "FOUR" in `arena_coach.py` in the SAME slice, with the module omitting exactly the cross-process writer its own docstring calls the motivating case; that `coaching_data.json` has three writers not two (`coach_integration/_coach.py:713` is unconverted, now in RM-261); that my W2 narrative read as an observed incident when it was a probe; and that the torn-read caveat was in the ledger but missing from the source comment. All fixed before commit. **One finding I re-measured and did NOT adopt:** it put the pre-slice baseline at 19473, but its own figures do not reconcile (19508 reported, 19473 + 33 = 19506 computed). Mine was taken before any edit and the delta is exact, so 19475 + 33 = 19508 stands.

**HAND-OFF FOR THE MERGER.** Baseline taken BEFORE any edit: RC `2 failed / 19475 passed / 144 skipped / 4560 subtests`. Both failure node ids (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`) are byte-identical pre and post, inherited branch lag - the same pair cycle 23 recorded. DS `10684 passed / 13482 subtests / 0 failed`, unchanged. **Take the baseline BEFORE editing; it is what makes "not mine" provable rather than asserted.** One process note worth keeping: I launched a suite mid-slice and then made two more edits, which invalidated that run - I stopped it and re-ran rather than reporting a number measured against a tree that no longer existed. Backfill measured EMPTY with the probe as evidence (no stray `.tmp` anywhere under the live data/runtime roots, every polled file decodes as UTF-8 and parses). **No RC restart was done and none would have helped:** live pid 23644 runs the MAIN-tree copy, so a bounce from this worktree deploys nothing (item-1179 trap) - **until the merge lands, the running process still shares one scratch file across four writers of `force_scan.json`.** CI on this branch was ALREADY red on arrival (a 45-minute `Full Dual Suite` timeout on run 33327341297, plus an in-flight timeout probe) - inherited, noted at pre-flight, not caused here. Branch is ready and NOT merged; lane 8 ships last.

---


# 2026-08-30m - lane 8 cycle 23: the rating write lost a race with the process that reads it, and a null cost the whole save

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 11/11 mutation-killed, verifier-gated.** Target `performance_tracker.py` (439 lines, not frozen), picked on criterion 2 (it reads `API-Key-Claude.txt` at `:406`) and criterion 4 (no test module carries its name). Full detail in `docs/LEDGER.md` 1282; RM-258/259/260 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`. **Next free id = RM-261.**

**RECALL PAID FOR ITSELF AGAIN, second cycle running.** The first pick was `core/riot_api_cache.py`; `perseus_recall` returned item-1182, lane 8's OWN cycle-5 audit of that exact file. Cycle 20 hit the identical thing with `vision_server/_http.py`. The step is mandatory precisely because a hardened file reads exactly like an unaudited one - the evidence that it was already done is not in the file.

**Two real defects, both silent, both costing the operator data.** **W1** - `_atomic_write_json` ended in a BARE `os.replace` while `core/polled_json.py:30-35` already documented the WinError 5 hazard AND the same fix had shipped to `ops/rc_supervisor` on 2026-05-02; this module held a private copy that never got it. Measured on win32: against a reader releasing after 80 ms the bare writer raised in 0.6 ms, the hardened one landed its content. Triple-masked - warning-only log, control falls through so the DB row still lands, `app/_game_lifecycle.py:236` logs "Performance rating saved" anyway, and the stale-mtime file then feeds `agents/supervisor.py:799-805` a PRIOR game's rating into this game's summary. **W2** - `.get(k, default)` only defaults a MISSING key, so a present null flowed into the arithmetic: all six of `ally_kills_total`, `cs_per_min`, `gold`, `game_seconds`, `deaths`, `kills` crashed `save_rating`, and the frozen caller's `except Exception` then ate the rating AND the match row. Fixed with a `_num` boundary coercion, which had to live here because the caller is frozen. **Then the mandated sibling grep found the same root cause in `save_tft_rating`** - null `alive_others` and null `stage` both crash it, and TFT is 8041 of the 8395 rows in the live store, so the sibling is the bigger exposure. The `stage` case is reachable only when `stage_round` fails to parse, so the natural probe hides it.

**The mutation pass is the part worth reading.** 10 of 11 killed on the first attempt; **M8 survived** - deleting the `bool` guard in `_num` left every test green. Not an equivalent mutant, so it got a test (`TestNumCoercion`) rather than a shrug, after which M8 and a new M9 both went RED; M10/M11 on the TFT siblings were killed outright. Exactly `feedback_guard_on_nondefault_call_path_is_untested`, caught by the pass instead of by an incident.

**W3 severity was DOWNGRADED by measurement and I am recording it that way.** The outer handler logged an exception from a callee holding the API key, and an injected raiser proves it WOULD leak. But the live path is refuted twice over: `_call_haiku` catches everything at `experimental_builder.py:236`, and the anthropic SDK's exception surfaces are clean under `%s`. The redaction shipped as defence in depth only, because `repr()` of the `UnicodeEncodeError` from a non-ASCII key file DOES carry the key - the margin is one formatting change wide. The hook is also DORMANT (`mark_active()` has zero callers), which is why the dead key read was FILED (RM-260) and not deleted.

**HAND-OFF FOR THE MERGER.** Suites observed THIS run from the repo root: RC `tests/` **2 failed / 19475 passed / 144 skipped / 4560 subtests**; DS **10684 passed / 13482 subtests / 0 failed**. The baseline taken BEFORE any edit was **2 / 19460 / 144 / 4546**, so the delta is exactly +15 passed / +14 subtests - the new tests one for one - and both failure node ids are byte-identical across the two runs (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`), inherited branch lag. **Take the baseline BEFORE editing; it is what makes "not mine" provable rather than asserted.** No RC restart was done and none would have helped: live pid 23644 runs the MAIN-tree copy, so a bounce from this worktree deploys nothing (item-1179 trap) - **until the merge lands, the running process keeps losing a rating to any concurrent reader and keeps dying on a null field.** Backfill measured EMPTY and the probe is the evidence: no stray `.tmp`, no failure signature in 15 days of logs, and every mode's rating file content matches its newest `match_history.db` row. A `sk-ant-` hit in `logs/agents/supervisor.log` was characterised by length and a masked stub without printing any value - RC's own test canaries, not key material. **Verifier returned CONFIRM** after independently re-running both suites, running a TRUE mutation of the replace line (current lands `NEW` in 77.6 ms; mutant raises `PermissionError` in 1.0 ms and disk keeps `OLD`), and proving against a `git show HEAD:` control that all six nulls really did raise before the fix. It also positively root-caused both inherited failures (live `:8860` serves engine `1.278.1` vs the branch pin `1.278.0`; `claude --version` is 2.1.251 vs the 2.1.220 pin) - environment drift, zero causal path to this edit. **Two honest scope notes:** the verifier correctly caught that my claimed file list understated the tree (the doc filings landed mid-verification), and its CONFIRM predates the TFT sibling fix, which I measured and mutation-tested myself afterwards - the +15/+14 suite delta above is the post-TFT run. Branch is ready and NOT merged; lane 8 ships last.

---

# 2026-08-30l - lane 8 cycle 22: the store ate the operator's builds, then served a build that was never saved

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, 11/11 mutation-killed, verifier-gated.** Target `coaches/sr_user_builds.py` + its caller `dashboard/routes_sr_user_builds.py`. LEDGER 1281. Filed RM-253 / RM-254 / RM-255 / RM-256.

**I CHECKED THE SELECTION RATIONALE BEFORE PAYING FOR IT, WHICH IS THE CYCLE-21 LESSON LANDING.** The module looks untested if you grep `tests/` for `test_sr_user_builds` and stop. It is not: `tests/phase8_smoke/test_sr_user_builds.py` has 20 tests and `tests/test_loadout_user_builds_merge.py` another 20. Criterion 4 was REFUTED and the file was audited on criteria 1 and 3 instead. Cycle 21 recorded this trap AFTER paying for it; this is the first cycle that paid nothing.

**THE HEADLINE DEFECT DESTROYS DATA AND ONLY LOGS A WARNING.** `_load`'s blanket `except Exception` turned an unparseable `user_builds.json` into an EMPTY store, and the next `add()` wrote that empty store over the file. Probed end to end: a seeded Tristana build was on disk before the call and absent after. The fix quarantines by RENAMING to `user_builds.corrupt-<stamp>.json` - which preserves the bytes exactly and is self-limiting, because the next `_load` then hits the does-not-exist branch instead of re-quarantining per read.

**THE SECOND DEFECT SERVES A BUILD THAT WAS NEVER SAVED.** `_load()` returned the SHARED cache dict and the SHARED per-champion list; mutators edited it in place BEFORE `_save`. Force `os.replace` to fail and `add()` raises, the route answers 500 - and `list_for` still reports the "ghost build". That is not cosmetic: `coaches/rune_pages.py:142` folds `list_for` output into a live rune page. One root cause covered both this and the unlocked-reader race: mutate a private deep copy, publish the cache only after the write lands, and hold the lock across read AND copy (`_STORE_LOCK` became an `RLock` because the read path legitimately nests).

**TWO OF MY OWN TESTS WERE WEAK AND I KEPT THE RECORD RATHER THAN THE APPEARANCE.** `test_concurrent_reads_and_deletes_never_tear` was ALREADY GREEN pre-fix - the GIL hides the tear - so it is a smoke test, not the guard; the guard for that weakness is the structural `test_load_does_not_hand_out_the_shared_mutable_cache`, and the mutation run killed it. Separately my first W8 test asserted the ABSENCE of the string "16M", which then failed against a corrected docstring that QUOTES the old claim while fixing it. A negative assertion rules out without pinning down; I replaced it with a positive pin on the corrected magnitude.

**I ALMOST SHIPPED A SEVERITY CLAIM THE LIVE SYSTEM REFUTES.** W3 (non-dict POST body) measured as a 500 when I called `_serve_user_builds_post` directly, and I wrote it up as a live defect. Then I probed the actual dashboard: `POST /api/sr-draft/user-builds` with `["not","a","dict"]` returns **400**, because `dashboard/_handler.py:525` already rejects every non-dict POST body at the single trust boundary for all ~40 routes, and `mc/routes.py:16` splices only `routes_loop_status` + `routes_loop_control` into `:8895` so this route has no second surface. The guard I added is defence in depth and nothing more. The function-level measurement was CORRECT and the severity inference from it was WRONG - `feedback_verified_claim_vs_measured_downstream`. Corrected in the ledger and the commit message rather than left to read stronger than it is. **The general rule this cost me: a direct-call probe measures the FUNCTION, not the SURFACE. Probe the surface before assigning severity.**

**THE FOUR FILED ROWS ARE NOT IN THE AUDITED FILE, AND I READ EVERY CITED LINE MYSELF.** A shortlist agent surfaced them; a shortlist is not evidence. RM-254 is the one to do first: `core/polled_json.py` IS the repo's atomic-write contract and derives its tmp name from the destination alone at `:57`, `:75`, `:87`, so concurrent writers share one scratch file - the module cannot deliver the atomicity every other module is told to get from it. RM-253 is the cheapest: cycle 20 fixed the raw-exception echo at `vision_server/_relay.py:50-56` yesterday and left the identical sibling at `:177`.

**ID MINTING: the tracker would have collided.** `docs/DS_SWEEP_TRACKER.md` tops out at RM-232 while `ROADMAP.md` already used RM-252. The lane skill says mint from the tracker; doing that literally would have produced a duplicate id. Minted from the repo-wide max instead. This is the already-filed RM-192 staleness, still open.

**Suites (repo root):** RC `tests/` 19460 passed / 2 failed / 144 skipped, against a pre-slice baseline of 19436 / 2 / 144 measured the same way - the delta is exactly the 24 new tests and the two failures are IDENTICAL node ids in both runs (`test_live_three_profiles`, `test_cli_version_still_matches_the_pin`), inherited and unrelated. DS 10684 passed / 0 failed. Ruff clean tree-wide. No RC restart needed - no live runtime path changed. Backfill: the live store was probed and is healthy (2 champions, 4 builds, no cap violations, no stray tmp), so there was nothing to repair.

---

# 2026-08-30k - lane 8 cycle 21: the halt button lost a race with the thing it was halting

**Shape: lane 8 Headless-True-Audit, worktree `lane/true-audit`, Tier-1, TDD, mutation-tested, verifier-gated.** Target `dashboard/routes_loop_control.py` (491 lines, not frozen) - the privileged remote-control surface that halts the headless loop from the phone. Full detail in `docs/LEDGER.md` 1280; RM-250/251 filed in `BACKLOG.md` with a pointer row in `ROADMAP.md`.

**MY SELECTION RATIONALE WAS WRONG AND A GREP TOLD ME SO.** I picked the file partly as "load-bearing and untested" because `ls tests/ | grep routes_loop_control` returned nothing. The tests are called `test_loop_control_*.py`. 72 test modules mention this file; one of them has 48 tests. That is `feedback_empty_grep_is_a_claim_about_the_pattern` landing on the target-selection step rather than on a finding - cheap here because the other two criteria held and the audit stood, but a naming convention I did not check nearly bought a whole cycle of "this file has no tests" reasoning. **Check the naming convention before concluding absence.**

**THE DEFECT: the emergency halt lost a coin-flip against the poller it was halting.** `_awrite` hand-rolled `tmp.write_text` + a bare `os.replace` while its OWN docstring said "loop_controller polls mid-write" - naming the exact condition under which Windows `os.replace` raises `PermissionError` WinError 5, because a reader holds a share lock. `core/polled_json.py:39` `_replace_with_retry` had existed for this the whole time, and `dashboard/_writers.py:29-36` already recorded a measured instance. I proved it with a throwaway probe BEFORE editing: `stop` under a held reader raised, the catch-all turned it into HTTP 500 "internal error - see logs", the halt did not land, and `STOP.tmp` was stranded - which `test_atomic_no_tmp_left` exists to forbid but never catches because it never drives the contended path. **The docstring named the hazard and the code did not handle it; the test named the invariant and never exercised it.**

**MY FIRST TEST WAS UNREALISTIC AND THAT WAS THE USEFUL PART.** Holding the file open for the WHOLE write attempt kept the test RED after a correct fix, because a bounded 275 ms backoff is SUPPOSED to lose to an infinite reader. Realising that split one test into two: a `_transient_reader` that releases inside the window (proving the retry works) and a held-open reader (proving the exhaustion path). The exhaustion path is what produced the best fix of the cycle - a 503 "control file busy - the loop is mid-poll, retry" instead of a generic 500, because the operator must be able to tell "press it again" from "the server is broken".

**A MUTANT SURVIVED AND THE BUG WAS IN MY FIX, NOT MY TEST.** I had written the tmp cleanup as two lines that resolve to the same path, so deleting one left the other working. The instinct is to strengthen the test; the correct move was to delete my redundant line and re-run the mutant, which then went RED (`reference_mutation_survivor_may_be_equivalent`). Final tally 7 mutants, 7 RED, restore sha256-verified after each.

**TWO OF MY FINDINGS WERE REFUTED BEFORE THEY BECAME FIXES, BY A PROBE I DISPATCHED TO ATTACK THEM.** A `run['pid']` KeyError and a `payload.update()` that un-bools a carefully-`bool()`-ed field. `launch_lane` has exactly one return path and raises unless `pid` is truthy; `try_acquire_lane` returns literal bools on all three paths. Both would have been edits to CORRECT code, and both read as obvious defects from inside the file alone. **Reading a caller is not reading its contract.**

**THE TRUST-MODEL DOCSTRING WAS WRONG IN THE SAFE DIRECTION - STILL A DEFECT.** It described the `:8888` perimeter; S10 Task 9 de-registered the route, so the real perimeter is Mission Control `:8895` (narrow bind, mandatory TLS, bearer token failing closed) - STRONGER than documented. Worth correcting anyway, because the next reader could relax this route believing it is only as guarded as `/api/command`, whose gate is inert until `RC_DASH_TOKEN` is set. The same measurement corrected a stale claim inside filed row RM-239.

**THE VERIFIER SAID REFUTE AND SAVED ME FROM TWO REAL DEFECTS - ONE OF THEM A REPEAT OF THE LESSON I WAS WRITING UP.** It measured 3 suite failures where I reported 2, and the extra was MINE: my new ROADMAP/BACKLOG/LEDGER rows cited `test_mission_control_server.py` at a line range running past its end (the file is 476 lines). I took that range from a probe subagent and pasted it forward without opening the file - in the same cycle whose headline finding was that a subagent's cited contract must be re-probed. The citation drift guard caught it; corrected to 468-476. **I re-verified the fix against the file myself rather than trusting the verifier's proposed range either.** It also flagged my new `except RuntimeError` in `_steer` as broader than `MutexTimeout`, which would report a genuine bug as "busy - retry" - hiding the fault and prescribing a useless remedy. Narrowed by name, +1 test, +1 mutant. **Neither was visible from the diff: one needed a suite I had already run and mentally filed as green, the other needed someone asking what ELSE the catch swallows.**

**Suites fresh from the repo root: RC 19436 passed / 144 skipped / 2 failed; DS 10684 passed.** Both RC failures are inherited and I PROVED it rather than asserting it - reverted the three changed files to pristine HEAD, re-ran exactly those two, both still failed, restored with sha256 re-checked. **Not merged; lane 8 ships last.** The live `:8895` runs the main-tree copy, so until the merge the running server still 500s a halt that loses the race with its own poller.
