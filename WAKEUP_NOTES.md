# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary + 2026-05-20 housekeeping batch + 2026-05-21 items 121-130 + 2026-05-22 items 133-139 + 2026-05-22 items 140-149 + item 181 + item 187 + item 188 + item 189 + item 190 + item 191 + item 192 + item 193 + item 194 + item 195 + item 196 + item 197 + item 198 + item 199 + item 200 + item 204 + item 215 + item 216 + item 227 + item 228 + item 241 + item 242 + item 245 + item 246 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-06-01 - item 251: DS GAP-2 LINEAR effects-text-only HEAL registry + 16 SEEDED, default-OFF byte-identical (ENGINE 1.82.0 -> 1.83.0; DS restarted 1.83.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next DS schema lift and exhaust it then /done". Next staged HEADLESS schema need from item 250 NEXT(3) = the LINEAR effects-text-heal registry (Fiora P 35:100 flat + the linear-only effects-text heals - the sibling slice to item 250's 3 bilinear seeds).

FINDING (verified vs live 16.11.1 effects_descriptions before building): an 84-candidate scan (all 171 champs, forms with NO attribute_kind=="heal" block whose effects_descriptions carry a heal/restore verb) surfaces a LINEAR self-heal class - flat ("35 : 100 based on level"), caster-stat-scaled ("+ 20% AP" / "5.5% of max HP"), per-level-pct-of-HP - that the EXISTING `_passive_heal_overrides` machinery (item 250) already evaluates (flat "" unit / `_HEAL_UNIT_TO_CTX` caster stats / per-LEVEL 18-element tuples indexed by rank_at_level("P",level)==level-1).

SCHEMA LIFT (ONE novel bit beyond item 250): NEW `level_scaled` on PassiveHealEntry + DamageBlock (additive default False) + a `level` param threaded compute_ability_hps -> _select_kind_blocks -> _eval_heal_shield_block. A SPELL-slot (Q/W/E/R) heal scaling "based on level" reads its 18-element per-level tuple at champion LEVEL (level-1), NOT the spell rank (a Q-rank 0-4 index mis-reads Rakan Q / Talon Q). `_lerp_per_level`/`_step_per_level` reused from `_passive_damage_overrides`.

SEEDED (16, all default-OFF, exact from verbatim 16.11.1) into the SAME `_PASSIVE_HEAL_OVERRIDES` dict (now 19 = 3 bilinear + 16 linear): 14 P-slot - Ahri P (35:95 + 20% AP, 9-stack; takedown 2nd heal omitted) / Alistar P (5% max HP, 7-stack; ally 7% omitted) / Aurora P (3:20 + 2% AP per Spirit/sec, lower bound) / Cho'Gath P (18:52 on-kill; mana ignored) / Evelynn P (15:150/sec, threshold gate not modeled) / Fiora P (35:100 Vital; the bilinear %-HP is the DAMAGE not heal) / Gragas P (5.5% max HP) / Lillia P (6:90 + 30% AP vs champ) / Maokai P (4%:12.8% max HP empowered AA) / Rek'Sai P (10%:20% max HP at-100-Fury) / Swain P (3%:6% max HP soul fragment) / Trundle P (1.8%:5.5% TARGET max HP on death - 0 at rest, resolves under resolve_target_relative) / Xin Zhao P (3/3.5/4% max HP step + 65% AP on-hit) / Yuumi P (20:110 + 25% AP periodic) - plus 2 SPELL-slot level_scaled Rakan Q (40:210 + 55% AP) / Talon Q (9:55 on-kill).

DEFAULT-OFF story differs from item 250: UNLIKE the 3 bilinear seeds (all target/missing-HP scaled -> 0 at resolve-off), most linear seeds carry a FLAT / caster-stat term that resolves NON-zero at the default resolve_target_relative=False - so apply_passive_heal=True surfaces them without an HP assumption; the apply_passive_heal=False DEFAULT (load_default) is STILL byte-identical (the seam injects only under the flag).

EXHAUSTED (the 84-candidate scan): the 16 seeded are the complete LINEAR seedable set. EXCLUSIONS by reason class - VAMP / "% of post-mitigation damage dealt" (not resolvable at rest, same boundary as the damage registry's omitted vamp) / RESOURCE restores (mana/energy/Courage - NOT health) / REVIVE + grey-health / BESPOKE product/charge/crit/form-gate (Kindred W, Darius Q, Taric Q, TwistedFate W, Aphelios R, Elise R, Nilah P) / Vladimir Q (existing snapshot heal block).

+22 tests test_passive_heal_overrides_linear_item251.py; item-250 test rescoped (registry grew 3 -> 19). ENGINE 1.82.0 -> 1.83.0 + 37 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean, 253 files engine 1.83.0; gist auto-pushes on commit) + DS restart (PowerShell taskkill /F /PID 19148 + schtasks /Run /TN RC-DaemonSlayer). DS suite 5778 -> 5800 (+22, 0 failed); phase8 70/70 post-restart; ruff clean; added-diff 0 non-ASCII. LIVE in-process: Fiora P 35.0 (L1) / 100.0 (L18); Rakan Q level_scaled 140.0 (L11) / 210.0 (L18); Talon Q 55.0 (L18); Trundle P 0 at resolve-off / 99.41 at target_max_hp=2500; Gragas P 90.7; all 0 / skipped at apply_passive_heal=False.

Don't-redo: (a) LINEAR seeds live in the SAME `_PASSIVE_HEAL_OVERRIDES` (19 = 3 bilinear + 16 linear; a linear entry just has empty bilinear_terms) - do NOT split into a 2nd registry. (b) `level_scaled` = SPELL-slot "based on level" heal (per-level tuple indexed by LEVEL not spell rank); P-slot heals see rank==level-1 so leave it False (would double-shift). (c) the flat/caster-stat linear seeds resolve NON-zero at resolve-off under the flag (resting-resolvable, NOT the lower-bound contract); only Trundle P (target-relative) stays 0 at rest; the apply_passive_heal=False DEFAULT is what stays byte-identical. (d) per-cast cadence is metadata only (passive -> heal_per_sec 0). (e) EXCLUSIONS are by reason class (vamp / resource / revive / grey-health / bespoke) - do NOT re-pitch. (f) seeds model the DOMINANT/guaranteed heal; omitted 2nd/ally/monster/tick variants documented per-entry. (g) DS restart PowerShell taskkill /F /PID + schtasks (NEVER Stop-Process).

NEXT (gap-plan, operator-gated / live = Phase D): (1) Phase D live flag-flips (apply_passive_heal now 19 forms; wire resolve_target_relative + a live target-HP / caster-missing-HP feed for the 3 bilinear + Trundle P; the 15 flat/caster-stat seeds surface immediately). (2) the 5 C2 AA-empower amortized values. (3) STAGED: Brand P + Ekko W (conditional-gate); an AS-aware heal seam (Viego's omitted bonus-AS term); a per-charge heal seam (Taric Q). (4) AurelionSol W cross-spell seam. (5) item-240 UI part-3 + #7/#8 sign-off + item-243 NEXT(2) ranked-SR UI watch.

---

# 2026-06-01 - item 250: DS GAP-2 effects-text-only HEAL registry (bilinear AP/AD-on-HP) + 3 SEEDED, default-OFF byte-identical (ENGINE 1.81.0 -> 1.82.0; DS restarted 1.82.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next DS schema lift and exhaust it then /done". Next staged HEADLESS schema need from item 249 NEXT(3) = the bilinear HEAL registry (Karma W f1 + Viego P; the conditional-gate Brand P + Ekko W is operator-deferred to Phase-D).

FINDING (verified vs live 16.11.1 effects_descriptions before building): a class of P/W/R forms parse to NO attribute_kind=="heal" block (heal lives only in stripped effects_descriptions text) yet carry a self-heal whose dominant term is a BILINEAR (base% + per100% * stat) * HP product (Viego P "2% per 100 AP of the target's maximum health" = AP * target_max_hp) no single linear heal unit expresses.

SCHEMA LIFT (NEW module + NEW seam + NEW consumer - heals feed ability_hps NOT ability_dps): NEW _passive_heal_overrides.py (PassiveHealEntry linear_terms + bilinear_terms + to_heal_block building a synthetic DamageBlock(attribute_kind="heal", raw_modifiers=(...linear %-of-HP...), bilinear_terms=(...products...))) + NEW AbilitiesSnapshot.load(apply_passive_heal=False) flag + _apply_passive_heal_overrides (gates on NO existing heal block) + NEW bilinear_ctx eval path in ability_hps._eval_heal_shield_block (threaded via _select_kind_blocks + a bilinear_ctx dict built once in compute_ability_hps). Linear %-of-HP terms resolve through the EXISTING v2 resolve_target_relative/extra_units machinery; bilinear products via bilinear_ctx. _per_100 reused from _passive_damage_overrides.

SEEDED (exact from verbatim 16.11.1): Viego P Sovereign's Domination (heal 2% + 2.5%/100 bonusAD + 2%/100 AP of TARGET max HP on Mist-Wraith consume; +5%/100% bonus-AS term OMITTED - no AS ctx on a heal block, damage-registry-crit-term boundary; takedown-gated per_fight) / Karma W f1 Renewal (heal 17% + 1%/100 AP of CASTER MISSING HP on the Mantra-empowered W form 1; 2nd on-tether-complete heal omitted; form 1 -> consumer routes W->1 via form_index_overrides) / Kayn R Umbral Trespass Darkin (heal 11.25% + 7.5%/100 bonusAD of TARGET max HP; Rhaast-form-only, form gate not modeled = magnitude gate-independent, Zed-P precedent).

STRONGER default-OFF story: every seed scales on a target/caster-MISSING HP quantity compute_ability_hps resolves to 0 unless the caller opts into resolve_target_relative + an HP assumption - so even with apply_passive_heal=True the DEFAULT compute_ability_hps call (resolve_target_relative=False) is BYTE-IDENTICAL (seeds contribute 0 unresolved-lower-bound + the spell row is skipped).

EXHAUSTED scan (171 champs, no-heal-block forms whose effects_descriptions carry heal + "per 100 X" + max/missing health): the bilinear self-heal set is exactly these 3. Documented EXCLUSIONS (scanned, NOT seeded): Fiora P (heal is FLAT 35:100; its bilinear term is the DAMAGE -> future LINEAR effects-text-heal registry) / Vladimir Q (ALREADY has a snapshot heal block; the bilinear missing-HP term is a conditional Crimson-Rush BONUS - no-existing-heal-block gate correctly skips). The other ~54 effects-text heals are LINEAR-only (out of scope for the bilinear lift).

+23 tests test_passive_heal_overrides_item250.py. ENGINE 1.81.0 -> 1.82.0 + 36 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean, 252 files engine 1.82.0; gist auto-pushed 261 files) + DS restart (PowerShell taskkill /F /PID 6532 + schtasks RC-DaemonSlayer -> /health 1.82.0/16.11.1/172/705). DS suite 5755 -> 5778 (+23, 0 failed); phase8 70/70 post-restart; ruff clean repo-wide; added-diff 0 non-ASCII; CI green run 26769846946. LIVE in-process proof: Viego P heal_per_cast 50.0 / Kayn R 281.25 / Karma W f1 155.0 at target_max_hp=2500 resolve-on; all 0 at resolve-off (byte-identical). Commit 8d92c7a.

Don't-redo: (a) _passive_heal_overrides.py is the canonical home for ANY effects-text-only HEAL with a ctx[a]*ctx[b] bilinear product; feeds compute_ability_hps (heals read raw_modifiers; damage registry reads typed fields) - do NOT route heal entries through the damage registry. (b) the seam gates on NO EXISTING heal block (avoid double-count; Vladimir Q precedent). (c) every seed is target/missing-HP scaled -> 0 unless resolve_target_relative=True + an HP assumption; the DEFAULT compute_ability_hps is byte-identical even with apply_passive_heal=True - lower-bound contract, NOT a bug; do NOT add a flat-base term to force a non-zero default. (d) Viego's +5%/100% bonus-AS heal term OMITTED on purpose (no bonus-AS on the heal ctx) - future AS-aware seam, damage-registry-crit-term boundary. (e) Karma is form 1; consumer MUST pass form_index_overrides={"W":1} (default walks W form 0 Focused Resolve, no heal). (f) Kayn R Rhaast/Darkin gate NOT modeled (magnitude gate-independent, Zed-P precedent) - over-credits Shadow Assassin Kayn by design. (g) Fiora P + Vladimir Q are documented EXCLUSIONS - do NOT re-pitch as bilinear heals. (h) DS restart PowerShell taskkill + schtasks (NEVER Stop-Process).

NEXT (gap-plan, operator-gated / live = Phase D): (1) flag-flips (apply_passive_damage 23 forms + NEW apply_passive_heal 3 forms; for the heal flag wire resolve_target_relative + a live target-HP/caster-missing-HP feed to surface the lower bound, AND route Karma W->1; tune Viego/Kayn per-cast cadence). (2) author the 5 C2 AA-empower values + cadence live. (3) remaining STAGED: Brand P + Ekko W conditional-gate (operator-deferred to Phase-D); a future LINEAR effects-text-heal registry (Fiora P 35:100 flat + the ~54 linear-only effects-text heals - a separate sibling slice). (4) AurelionSol W cross-spell seam (own session). (5) item-240 UI part-3 + #7/#8 + item-243 NEXT(2) ranked-SR UI watch.

# 2026-06-01 - item 249: DS GAP-2 PER-STACK passive damage schema lift + 3 SEEDED + 1 UPGRADE, default-OFF byte-identical (ENGINE 1.80.0 -> 1.81.0; DS restarted 1.81.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next DS schema lift and exhaust it then /done". Next staged passive category from item 248 = the per-stack ramp (Kai'Sa P, named NEXT(3)).

FINDING (verified vs live 16.11.1 effects_descriptions before building): Kai'Sa P term "12% : 24% (based on Plasma stacks) AP" is LINEAR in stacks = 12% + 3% per stack. A per-stack term does NOT collapse to existing fields IF the stack count stays a runtime parameter (the lift externalizes the stack assumption, mirrors item-236 tenacity).

SCHEMA LIFT (contained to _passive_damage_overrides.py - NO abilities.py / ability_dps.py change): NEW PerStackTerm dataclass (same scaling fields as PassiveDamageEntry, read 'per stack') + PassiveDamageEntry.per_stack / assumed_stacks; to_damage_block FOLDS field + per_stack.field * assumed_stacks element-wise into the synthetic block (_fold/_as_tuple broadcast flats over per-level tuples), so _evaluate_block needs ZERO new math - the per-stack contribution collapses into ordinary base/scaling fields once stacks fixed. Default-OFF byte-identical (seam injects only under apply_passive_damage=True).

SEEDED (exact from verbatim 16.11.1): Kai'Sa P Caustic Wounds (4:24 by level + 12% AP, + (1:6 by level + 3% AP) per Plasma stack; assumed_stacks 2.0 = 0..4 ramp-cycle-average - the canonical case STAGED through 248) / Darius P Hemorrhage ((13:30 + 30% bonus AD) per stack bleed, dot, cap 5, assumed 3.0) / Twitch P Deadly Venom ((6/12/18/24/30 + 18% AP) per stack true poison, dot, cap 6, assumed 3.0, even-fifths step) / UPGRADE Orianna P (adds the 2:10 + 3% AP per-stack ramp it was missing; assumed 1.0).

EXHAUSTED (15 'per stack'+'damage' no_damage P-forms scanned): only these 4 are per-stack TARGET damage; the other 11 are stat STEROIDS (Belveth/Garen/Irelia/Kayle/Samira/Senna/Volibear/Wukong AS/MS/armor) or stack-gain/store (Mel/Smolder); Sona's Accelerando is a haste steroid (Sona P separately seeded for flat Power Chord). +17 tests test_passive_damage_per_stack_item249.py + flipped 2 item-247/248 'Kaisa staged' pins -> 'seeded by item249'.

ENGINE 1.80.0 -> 1.81.0 + 35 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean, 250 files engine 1.81.0; gist auto-pushes on commit) + DS restart (PowerShell taskkill /F /PID 20740 + schtasks RC-DaemonSlayer -> /health 1.81.0/16.11.1/172/705). DS suite 5738 -> 5755 (+17, 0 failed); phase8 70/70 post-restart; ruff clean repo-wide; added-diff 0 non-ASCII.

Don't-redo: (a) PerStackTerm + assumed_stacks is the canonical home for ANY linear-per-stack damage term; per_stack coefficients EXACT, only assumed_stacks is the documented operator-tunable midpoint - do NOT bake the count into base/ap (loses the Phase-D live-stack seam). (b) to_damage_block FOLDS at build time (no DamageBlock/ctx/evaluator change); do NOT add a ctx.assumed_stacks eval-time path without a real live-stack consumer. (c) the 4 assumed_stacks (Kai'Sa 2.0 / Darius 3.0 / Twitch 3.0 / Orianna 1.0) are documented ramp-midpoints - Phase-D tunes them. (d) Kai'Sa 5th-stack-consume (15% + 6%/100 AP MISSING HP) OMITTED: conditional + missing-HP inert at full-HP ctx (Ekko-W precedent). (e) Twitch breakpoints even-fifths ESTIMATE (Zed/Caitlyn precedent). (f) Sona P seeded for Power Chord NOT per-stack - do NOT re-pitch. (g) DS restart PowerShell taskkill + schtasks (NEVER Stop-Process).

NEXT (gap-plan, operator-gated / live = Phase D): (1) flag-flips (apply_passive_damage now 10 P + 6 C3 + 4 bilinear + 3 per-stack + Orianna upgrade = 23 forms; tune the 4 per-stack assumed_stacks live) / apply_ability_amps / build_tenacity+cc_blended / mode_modifiers / gate_ammo / aoe_targets_hit. (2) author the 5 C2 AA-empower values + cadence live. (3) remaining STAGED slices: Brand P + Ekko W (conditional fire-probability + low-HP ctx = a conditional-gate schema lift, operator deferred to Phase-D) / Karma W f1 + Viego P (bilinear HEALS = need an effects-text-only HEAL registry feeding compute_ability_hps). (4) AurelionSol W cross-spell seam (own session). (5) item-240 UI part-3 + #7/#8 + item-243 NEXT(2) ranked-SR UI watch.

# 2026-06-01 - item 248: DS GAP-2 bilinear AP-on-HP passive schema lift + 4 SEEDED passives, default-OFF byte-identical (ENGINE 1.79.0 -> 1.80.0; DS restarted 1.80.0; RC not restarted - DS engine + tests + Share + docs only)

Operator "start the next DS schema lift and exhaust it". The next staged schema need from item 247 = the bilinear AP-on-HP evaluator term Gwen P needed.

FINDING (verified vs live 16.11.1 effects_descriptions BEFORE building): the AP-scaled %-of-HP passive form "X% (+ Y% per 100 AP) of target HP" is a PRODUCT of two ctx stats (ctx[ap] * ctx[target_hp]) that NO single linear _SCALING_TARGETS field expresses (each field is one pct * one ctx attr; base-X%-only under-models ~2x at 200 AP).

SCHEMA LIFT: NEW DamageBlock.bilinear_terms (flat tuple (factor, ctx_attr_a, ctx_attr_b) summed as factor * ctx[a] * ctx[b] in ability_dps._evaluate_block AFTER the per-rank linear terms; default () = every existing block BYTE-IDENTICAL) + has_damage_scaling True for a bilinear-only block + _passive_damage_overrides._per_100(pct, per_attr, of_attr) authoring helper (factor = pct/10000: /100 pct->fraction, /100 "per 100" denom).

SEEDED (4, default-OFF, exact from verbatim 16.11.1): Gwen P A Thousand Cuts (1% + 0.55%/100AP target max HP, on-hit - the item-247 staged canonical case) / Aurora P Spirit Abjuration (2.5% + 2%/100AP max HP, 3rd-stack consume) / Lillia P Dream-Laden Bough (5% + 1.25%/100AP max HP, dot) / Renata P Leverage (1%:2% level + 2%/100AP max HP, first-hit per_fight). All 4 on target MAX HP (non-zero at default full-HP ctx, NOT inert).

EXHAUSTED (scanned all 171 champs' no_damage forms for "per 100 AP" + "health"): 9 bilinear-on-HP candidates, 4 shipped + 5 STAYED STAGED - Kai'Sa P (dominant per-application term is a Plasma-stack-count ramp; 5th-stack-consume bilinear sub-term partial + conditional) / Brand P + Ekko W (bilinear term CONDITIONAL - ring detonation / sub-30%-HP gate; Ekko's is missing-HP-scaled = ~0 at full-HP ctx) / Karma W f1 + Viego P (bilinear term is a HEAL, not damage).

+22 tests test_passive_damage_bilinear_item248.py + item-247 StagedAbsent split (Gwen now seeded, Kai'Sa stays staged). ENGINE 1.79.0 -> 1.80.0 + 35 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean, 249 files engine 1.80.0) + DS restart (PowerShell taskkill + schtasks RC-DaemonSlayer). DS 5715 -> 5738 (0 failed); ruff clean; added-diff 0 non-ASCII (abilities.py/ability_dps.py/CHANGELOG carry PRE-EXISTING non-ASCII = operator-gated retro-sweep, diff-verified clean).

Don't-redo: (a) bilinear_terms is the canonical home for ANY ctx[a]*ctx[b] product damage term; factor is FLAT (level-independent) by Riot's "% per 100 AP" convention - do NOT make it per-rank without a real level-scaled bilinear case. (b) _per_100 factor = pct/10000 (NOT pct/100) - "per 100 AP" denominator is the second /100. (c) the 4 SEEDED are EXACT for the modeled terms; vs-minion/monster caps + heal sub-clauses are uncapped/utility (Aatrox/Zed precedent) - do NOT model. (d) the 5 STAGED skips are NOT bilinear-coverable: Kai'Sa needs a Plasma-stack-count assumption; Brand/Ekko W need a Phase-D conditional/low-HP assumption; Karma W f1 + Viego P are HEALS. (e) Ekko W is missing-HP-scaled -> ~0 at default full-HP ctx (misleading inert ship). (f) DS restart PowerShell taskkill + schtasks (NEVER Stop-Process).

NEXT (Phase D / live / operator-gated, unchanged from item 247): flag-flips (apply_passive_damage now covers 10 P + 6 C3 exotic + 4 bilinear = 20 passives, per-passive AFTER on_hit->AA cadence wire / apply_ability_amps / build_tenacity+cc_blended / mode_modifiers / gate_ammo / aoe_targets_hit); author the 5 C2 AA-empower values + cadence live; Kai'Sa P per-Plasma-stack ramp (own slice, needs stack-count decision); AurelionSol W cross-spell seam; item-240 UI part-3 + #7/#8 + item-243 NEXT(2) ranked-SR UI watch.

---

# 2026-06-01 (/headless-upgrade) - item 247: DS gap-plan Phase C3 (6/8 exotic passives) + C2 (AA-empower seam), both default-OFF byte-identical (commits 04dbcde C3 ENGINE 1.78.0 / 092a549 C2 1.79.0; pushed 5d8cd59..092a549; CI green; DS restarted 1.79.0; RC pid 13544 not restarted)

Headless run off item-246 NEXT. Pre-flight clean: DS 1.77.0 in sync, CI 6/6 green, 0 PRs, 2 merged worktrees + 7 scratch cleaned.

C3 (04dbcde, ENGINE 1.77.0 -> 1.78.0): authored 6 of 8 gap-plan exotic passives in _passive_damage_overrides.py from VERBATIM 16.11.1 effects_descriptions, default-OFF (injects only under apply_passive_damage=True; 6 new P forms stay no_damage flag OFF). Additive schema (no core DamageBlock change): _step_per_level (3-tier even-thirds level step for slash-notation) + PassiveDamageEntry scaling fields widen to float|tuple (per-level coefficient rides target_max_hp_pct/total_ad_pct) + to_damage_block coerces + wires target_current_hp_pct.
SEEDED: Aatrox P (4%:8% target max HP lerp) / JarvanIV P (8% target current HP flat; min/cap inert in champ band) / Zed P (6/8/10% target max HP step; below-50% gate NOT modeled = magnitude gate-independent) / Caitlyn P (60/90/120% AD step; +crit-chance AD multiplier OMITTED = AA-crit seam) / Ekko P (30:140 + 90% AP; every-3rd-stack cadence metadata) / Gangplank P (50:250 + 100% bonus AD TRUE dot 2.5s; +crit term OMITTED).
STAGED own-slice (NOT shipped guessed): Gwen P (bilinear AP*HP product) + Kai'Sa P (per-Plasma-stack). +31 tests test_passive_damage_exotic_item247.py.

C2 (092a549, ENGINE 1.78.0 -> 1.79.0): the 5 base="aa" AmpEntry champs (Caitlyn W / Fiora E / Jayce W f1 / Sivir W / Nidalee Q), registered item 239 but NO consumer (inert in ability_dps), now wired into the AA scorer. compute_dps gains apply_ability_amps (default False = byte-identical); when True NEW _aa_amp_multiplier scales ONLY base-AA (item procs unamped) via compute_dps -> _phase_weighted_dps -> _rotation_attack_dps (aa_empower_amp) + per-hit displays; route-reachable via /dps. rank_at_level imported function-level (dps<->ability_dps cycle break). FORWARD-MARKER: placeholder (0.0,) + conditional-no-condition (prob 0) -> inert today; Phase D authors the value + flips always_on/a condition per champ. +8 tests test_aa_empower_seam_item247.py.

Each: ENGINE bump + 35 test-pin syncs + CHANGELOG prepend + Share re-sync (--check clean) + DS restart (PowerShell taskkill + schtasks RC-DaemonSlayer). DS 5689 -> 5707 -> 5715, 0 failed; phase8 70/70; ruff clean. Cost/latency 7-lever sweep CLEAN no-commit (top /api/adaptation 0.116/s; 17 RC-* tasks 14+3; 34==34 CSS parity).

Don't-redo: (a) OMITTED crit terms (Caitlyn/Gangplank) belong in an AA-crit/crit-context seam, not a passive block - do NOT bolt a crit field onto DamageBlock here. (b) Zed below-50% gate intentionally not modeled (% of MAX HP = gate-independent); 3-tier breakpoints (Zed/Caitlyn) are EVEN-THIRDS estimates - verify exact 16.11.1 boundaries in Phase D, the live-wiki breakpoints are the DRIFTED current-patch values. (c) Gwen+Kaisa staged on purpose. (d) C2 seam is a forward-marker even flipped on (placeholders + prob 0); Phase D must set always_on or a condition AND a value. The amp scales base-AA only - do NOT fold into damage_amp. (e) dps.py has pre-existing non-ASCII (operator-gated); the C2 ASCII test scopes to the amp module + test file (item-247 additions added 0 non-ASCII, diff-verified).

NEXT (Phase D / live / operator-gated): flag-flips per champ/mode/passive (apply_ability_amps / apply_passive_damage after D1 on_hit->AA cadence / build_tenacity+cc_blended / mode_modifiers / gate_ammo / aoe_targets_hit); author the 5 C2 AA-empower values + cadence live; finish Gwen+Kaisa exotics; AurelionSol W cross-spell seam; item-240 UI part-3 + #7/#8 + item-243 NEXT(2) ranked-SR UI watch.

---
