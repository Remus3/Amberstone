# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-09-18, the laned orchestrated loop wave 7 wrap (relocated `2026-09-17b` the wave 4 block, via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 1027"; newest 3 = `2026-09-18b` this wave, `2026-09-18` wave 6, `2026-09-17c` wave 5). **THIS WAS THE FIRST REAL PRUNE AFTER THE RM-276 FIX, and the insertion-only property was verified on the REAL archive rather than inherited from the slice's scratch-copy measurement: exactly ONE difflib opcode, kind `insert`, 21 lines inserted, ZERO deleted, at old line 41, with `new == old[:41] + inserted + old[41:]` reconstructing exactly.** That matters because the pre-fix pruner would have deleted 1335 lines of ordering from this archive on this very run - see the wave 7 block below. The prior relocation was 2026-09-17, the laned orchestrated loop wave 4 doc-sync (relocated `2026-09-16b` the laned-loop wrap, VERBATIM - moved with the Edit tool because Bash writes to tracked files are banned, then byte-compared equal to the output of `scripts/wakeup_prune.py --keep 3` run on scratch copies of both files, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 970"; newest 3 = `2026-09-17b` this wave, `2026-09-17` wave 3, `2026-09-16c` waves 1-2). The pass before this one, on 2026-09-17, was the laned orchestrated loop wave 3 doc-sync (relocated `2026-09-16` the acknowledge-path wrap, VERBATIM via the Edit tool, byte-compared equal to `scripts/wakeup_prune.py --keep 3` output on scratch copies, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 969"; newest 3 = `2026-09-17` that wave, `2026-09-16c` waves 1-2, `2026-09-16b` the laned-loop wrap). The pass before that, on 2026-09-16, was the laned orchestrated loop wave 1 doc-sync (relocated `2026-09-15` the moon-sync merge, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 968"; newest 3 = `2026-09-16c` this wave, `2026-09-16b` the laned-loop wrap, `2026-09-16` the acknowledge-path wrap). The pass before this one, on 2026-09-16, was the laned-loop doc wrap (relocated `2026-09-14` the public surface refresh, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 966"; newest 3 = `2026-09-16b` this wrap, `2026-09-16` the acknowledge-path wrap, `2026-09-15` the moon-sync merge). The pass before this one, on 2026-09-15, was the moon-sync five-slice merge wrap (relocated `2026-09-12e` the discovery-axis + inter-scorer lane and `2026-09-12d` the fleet tooling-tier lane, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 2 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 964" - read those counts off the tool's own output, never off a recollection; newest 3 = `2026-09-15` this wrap, `2026-09-14` the public surface refresh, `2026-09-12f` the calibration lane). The pass before this one, on 2026-09-12, relocated `2026-09-12c` the SESSION WRAP doc-sync and reported "moving 1 session(s)" / "archive now has 962". The pass before that relocated `2026-09-12b` the five-slice merge and `2026-09-12a` RM-412 C1 OSS extraction, and reported "moving 2 session(s)" / "archive now has 961". The pass before that relocated `2026-09-11d`, the one before that `2026-09-11c`, and the one before that `2026-09-11b` and `2026-09-11a`, the same way. The 2026-09-11k RELOCATION DUE note that sat here is DISCHARGED and deleted - the file is back at keep-3. The letter suffixes are PER FILE and have diverged from `docs/LEDGER.md` - RM-385 is `c` there and `d` here; do not reconcile them. NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`4a707962`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired. The 2026-09-16 RELOCATION DUE note that sat here is DISCHARGED and deleted by the 2026-09-16b prune above.

---

# 2026-09-19b - Idle filesystem walker: 38,782 to 362 metadata ops per second, one root cause, five same-family siblings, export cache reclaimed

**Code commit:** `e25390f36` "fix(walkers): scope every repo-root walker - hot-reload idle rate 38,782 to 362 ops/s" - 13 files, 1233 insertions, 70 deletions. LEDGER 1429 carries the full record.

**SHIPPED.** `core/hot_reload.py` polled `root.rglob("*.py")` every 2 s with the repo root in `_WATCH_DIRS` and post-filtered - it walked 39 agent worktrees and the 14k-file export cache every tick. Fixed by SCOPING in two rounds: round 1 (os.walk + pruning) was REFUTED on rate at `5,858`/s because it re-statted every accepted file; round 2 (scandir walk, mtime off the DirEntry) landed. Merger-measured on the operator's recipe: baseline `38,782`/s on pid 15932, final `362`/s over 60 s on pid 15816, ETW 40 file events/s with no worktree or export path. Watched set 7105 to 2332. Siblings fixed TDD with refuters: `tools/gen_archmap.py` (twice per commit, 22.5k dirs to 556), `scripts/audit_api_surface.py` (117k files to 5k), the DS antitank test's root rglob (4.6 s to 0.4 s), and the sanctioned `tests/_repo_walk.iter_repo_files` itself (22.5k scandir to 0; 774 consumer tests green; plus a loud ValueError for non-basename patterns). Orphan `.tmp-*` export sweep shipped; the two orphans were empty shells and the 1.3 GB was the two stale KEEP_EXPORTS finals, deleted by hand with the responder Disabled - cache now empty. Runner fixture made robust to the operator's new `pytest.ini` tmp-path retention (274 passed under all three policies).

**DUAL SUITE on the frozen tree, merger-run, sequential:** DS `10857 passed` exit 0 in 129 s. RC `tests/`: `24261 passed, 102 skipped, 1 xfailed, 3 xpassed, 1 error` in 5855 s (1 h 37 m; a second `pytest` from another session competed for CPU the whole time). The ONE error was the new positive control firing in `tests/test_inbox_responder_mutants.py`, which imports the runner's module-scoped guard but did not import its new function-scoped observer - fixed by importing the observer with it, then `353 passed` across both modules on an isolated re-run. The full suite was NOT re-run after that one-line import fix; the two affected modules were.

**DO NOT REDO.** Do not re-sweep non-test code for unscoped walkers - the 60-site table is in LEDGER 1429. The frozen supervisor twin (`ops/rc_supervisor.py:513`, ~1.2k ops/s in the supervisor pid) is reported and needs an operator grant. Do not re-open the export retention count: the finals question is a spec change for the operator.

**INBOX.** Two phantom UNREADs were `.pyc` growth inside verbatim payloads (removed, keys restored). Five sibling REVIEW notes on the machine stray-work sweep arrived and were marked seen; RC owes its own report and the reconciliation - see RC-NEXT-SESSION.txt. Note: LL reports it has NO `docs/CHANNEL.md` (refused at its license gate, OPS-91), so the sweep prompt's "byte-identical in your tree" premise is false for LL.

**LESSONS.** (1) rglob never prunes; post-filter walkers are a FAMILY - memory `reference_rglob_never_prunes_post_filter_walkers_are_a_family`. (2) A pruned walk that re-stats is half a fix; verify on the operator's metric, not on "faster". (3) A slice called a fixture error "pre-existing"; the refuter showed it was NEW and caused by a sibling change in the same tree - attribution needs the mechanism, not the timestamp.

**NEXT:** the lane / worker monitor widget (RC-NEXT-SESSION.txt), then the stray-work reconciliation.

---

# 2026-09-19 - RM-239 / RM-242 QA pass: eight defects corrected IN the rows, RM-239's acceptance rewritten, and the first two leak sites converted

**ONE commit, `a0dcae6e6`** "fix(dashboard): RM-239 acceptance rewritten and the two literal-clause sites converted" - 5 files, 113 insertions, 9 deletions, on the remote as `f935d3f02..a0dcae6e6  main -> main`. LEDGER **1428**. Sibling-name sweep ARMED and clean (54560 bytes, 5 files, 1 commit message; 0 binary/LFS blobs, whose content it never scans). **CI for this push is PENDING - run ids `35444275346` (ci) and `35444275387` (docs-guards), both IN PROGRESS; no verdict is claimed.** A prior wave's owed item is CLOSED: CI run `35407781510` is `success` on `f935d3f02`. Tier-1, ENGINE-IMPACT NONE, no `web/` file, responder DISARMED. Shape: FOUR independent read-only QA slices plus TWO adversarial verifier gates; no producer graded its own work.

**SHIPPED.** The settled brief HELD on its core - population 25 sites / 19 modules and its full membership reproduced by two separate independent AST derivations, the cycle-18 guard non-generalisation reproduced at ZERO delta (102 flags over 57 modules, recall 15 of 25, 61 flags in zero-leak modules, all nine uncapped sites missed), and the HALT finding (RM-239's acceptance unsatisfiable at 7 of 25 sites) CONFIRMED. EIGHT defects found IN the brief text were corrected append-and-mark in both rows. RM-239's acceptance was REWRITTEN to be satisfiable and to be able to FAIL, organised on generic-safe versus curated-message via the existing `public_msg` hatch, with a third disposition for the seven unconvertible sites and the `status=500` default trap named. TWO of the 25 sites converted - `dashboard/routes_archetype.py` and `dashboard/routes_damage_mix.py` - and the leak was WIDER than the row described (raw operator input plus the full `ARCHETYPE_SET`; the live snapshot patch). Four breaking string pins re-pinned in the SAME slice; `tests/test_dashboard_error_scrub_rm134.py` byte-untouched. **Both rows stay OPEN and NOT MERGED - their acceptances still conflict.**

**DO NOT REDO.** The eight corrections are recorded IN the rows - read them, do NOT re-derive them. The population 25 / 19 and its membership are SETTLED and reproduced twice - do NOT re-derive, compare against the published inclusion rule. RM-239's acceptance is REWRITTEN - do NOT re-litigate the old literal clause.

**TEST EVIDENCE, run by the merger** over `tests/test_routes_archetype.py`, `tests/test_routes_damage_mix.py`, `tests/test_dashboard_error_scrub_rm134.py`, `tests/test_p2w1_dash_d.py`: `81 passed, 8 subtests passed, exit 0`. Zero non-ASCII across all five changed files; ruff ALL CHECKS PASSED, hygiene trio `14 passed`, `drift_guard` 0 breaches. **ONE UNRESOLVED ITEM, recorded honestly:** the fixer slice's RED baseline of `6 failed / 56 passed` was NOT reproduced by the verifier, because reproducing it needs mutating the tree. It is arithmetically consistent and the three re-pins each failed against the old leaking body, so the SUBSTANCE is confirmed - but the RED count is ATTRIBUTED, not observed.

**STILL OPEN:** 23 of the 25 leak sites remain and the rows are now READY FOR A FIXER; RM-475 (filed wave 7 - `scripts/wakeup_prune.py` keep/move accounting counts zero-heading blocks as sessions in file order); `dashboard/routes_damage_mix.py:152` is an adjacent clean conversion flagged but NOT taken; the `logs/2026-09-18.log` cluster of four RC starts inside three minutes at 04:36-04:38 plus two at 05:00 is still uninvestigated.

**HEALTH.** RC pid moved 2868 -> 5320, a FOURTH consecutive wave. The explained mechanism holds (the production hot-reload watcher writes `restart_trigger.txt` when a non-frozen `.py` changes, and this commit carried `.py` files) and the attribution limit stands - no single bounce is attributable. DS `:8860` ok, ENGINE 1.280.0, patch 16.15.1, 173 champions, 706 items, untouched.

**TWO LESSONS.** (1) **An INHERITED PUSH-STATE CLAIM WENT STALE** - the merger repeated the wave 7 note that waves 6 and 7 were unpushed, re-probed with `git ls-remote origin refs/heads/main`, got `f935d3f02`, and found both waves already on the remote with only this session's commit local. Re-probe remote state; never inherit it from a hand-off block. (2) **A verifier again AMENDED PRODUCER CLAIMS IN EVERY SLICE** - budget the grading pass rather than hoping to skip it, and note this time the amended claims were in a SETTLED BRIEF, not in fresh work.

## NEXT SESSION

Confirm the two PENDING CI runs `35444275346` and `35444275387` before anything else - no verdict exists yet. Then the RM-239 / RM-242 fixer over the remaining 23 sites, which is now unblocked: the row defect is fixed and the acceptance can fail. Read the eight in-row corrections and the rewritten acceptance rather than re-deriving anything; the population is settled. RM-475 is the other filed row. Optionally take `dashboard/routes_damage_mix.py:152`, the adjacent clean conversion. Re-probe health rather than inheriting the pid reading, and if anyone looks at the pid-bounce pattern, the `logs/2026-09-18.log` 04:36-04:38 cluster is the unexplained part. Every HALT row still needs the operator.

---

# 2026-09-18b - laned orchestrated loop wave 7: RM-474 and RM-276 shipped, and the archive rewrite that closing RM-276 would have caused

**LOCAL ONLY at `06cde1618`** plus this doc edit. **THE PUSH DECISION IS OWED AND UNMADE** - both wave 7 merges sit on `main` and NEITHER IS PUSHED; the push halts on its diff per the standing rule, and this is the first thing the next session decides. LEDGER **1427**. Both slices were graded by an independent adversarial verifier and no producer graded its own work. **FULL DUAL SUITE run ONCE by the merger on the frozen merged tree `4a5055dec`, both exit 0:** `agents/daemon_slayer` 10856 passed, 13663 subtests, 128.80s; `tests` 24231 passed, 102 skipped, 1 xfailed, 3 xpassed, 1 warning, 5002 subtests, 5832.55s. **One delta worth carrying, NOT diagnosed from one reading:** wave 6 recorded 2 xfailed / 2 xpassed and this run is 1 xfailed / 3 xpassed, so one conditional xfail flipped to xpass. Neither file this wave touched carries an `xfail` marker - the only three modules in `tests/` that do are `test_inbox_responder_runner.py`, `test_rm450_core_residuals.py` and `test_truth_gate.py`, all untouched here - so it is an environment-conditional marker rather than a consequence of this wave's change. Identifying WHICH one needs a `-rX` run, which is another 97 minutes, and was judged not worth the wrap; next session can name it cheaply by running `-rX` over just those three modules. Tier-1 throughout: RM-474 is tests-only, RM-276 touches `scripts/` production code plus its own tests. ENGINE-IMPACT NONE, no `web/` file, responder stays DISARMED.

**SHIPPED - DO NOT RE-INVESTIGATE EITHER ROW.** **RM-474** (merge `233a3d13b`, chain `67bd6f3be` then `ecf5661fd`; one file, `tests/test_oss_win32_atomic_io_drift.py`, 170 insertions 1 deletion) - the LICENSE guard coupling is now enforced BEHAVIOURALLY: a new helper records which files the survivor actually reads, so the guard grades the enumerated RESULT rather than the source text, and the required set is DERIVED from the non-`.py` members of `_package_all_files()` so a newly added shipped file needs no test edit. RM-471's normalisation is untouched and no byte compare came back. **RM-276** (merge `06cde1618`, chain `f30cd2215` then `32ca4a8ed`; two files, `scripts/wakeup_prune.py` and `tests/phase7_polish/test_wakeup_prune.py`, 564 insertions 25 deletions) - both named blindnesses closed: the header is cut at its first `SESSION_RE` match, and the interior gate moves from `SESSION_RE.match(block.lstrip(...))` to `SESSION_RE.search(block)`. Three fixture arms, one per route shape, each observed at 2 of 3 before and 3 of 3 after; the all-separated arm is a labelled CONTROL, 3 of 3 on both sides. The verifier confirmed the mutants discriminate DISJOINTLY, intersection empty.

**THE CONSEQUENTIAL FINDING, carry it forward: closing RM-276's two named blindnesses would have REWRITTEN PROTECTED HISTORY.** `prune()` re-renders the whole archive unconditionally, and at `f30cd2215` a real prune of `docs/history_notes.md` measured 57 difflib opcodes with 1335 DELETED lines (verifier-measured end to end on scratch copies). The mechanism was a THIRD blindness the row never names - `trailing_extras` hoists zero-heading blocks to the file tail, a byte-identical no-op only because those blocks happened to sit contiguously at the archive's tail. Deleting `trailing_extras` was necessary and NOT sufficient: `render` joins with `SEP`, so re-splitting a glued block invents a `---` rule and left 142 inserted separator lines. Byte identity needed a faithful `Block` / `split_blocks` / `render_blocks` pair plus a pure-insertion `prepend_sessions`, with `prune()` using the faithful pair for the PROTECTED archive and canonical `render` for the LIVE WAKEUP file. **SAFETY VERDICT, verifier-measured end to end on scratch copies: a real prune of the current archive is ONE difflib insert opcode, 21 lines at line 41, ZERO deletions, inserted newest-first, and the result is still round-trip faithful, so the following prune is equally safe. THE NEXT `/done` PRUNE IS SAFE.** **DIGESTS, BOUND EXPLICITLY TO THEIR FILES - do not transpose them, the wave brief listed them in the opposite order to the files it named and the binding had to be re-measured:** at `06cde1618`, `docs/history_notes.md` is `b541566e1cb1fbd6` (5691068 chars) and `WAKEUP_NOTES.md` is `2f17dd341771d096` (23134 chars). **`docs/history_notes.md`, the PROTECTED archive, is UNCHANGED across the ENTIRE wave - both code merges and this docs pass included.** `WAKEUP_NOTES.md` is unchanged across both CODE merges and is then changed by THIS docs pass, which appends this session block by design - so do not carry forward a blanket "both unchanged across the whole wave", which is false for this file the moment the docs commit lands.

**NEWLY FILED: RM-475, OPEN, LANE 7, Tier-1.** Keep/move accounting counts zero-heading BLOCKS as sessions in FILE ORDER, so `--keep 3` over a file containing one can retain only TWO real sessions. Verifier fixture `H,S1,ZERO,S2,S3` at `--keep 3`: old code kept `[S1,S2,S3]` and archived `ZERO`; new code keeps `[S1,ZERO,S2]` and archives the REAL session `S3`. NOT LIVE TODAY - the real WAKEUP file has zero zero-heading blocks - and archive safety is unaffected because the prune stays a pure insertion, which is why it did not block the merge.

**RM-239 / RM-242 remain OPEN and HELD, unchanged.** Their row-defect rewrite is an OPERATOR call; it was put to the operator this wave and is NOT yet confirmed, so NOTHING was started. Do not re-derive their population - compare against the published inclusion rule.

**PID-BOUNCE WATCH ITEM FROM WAVES 5 AND 6: RECURRED, THEN EXPLAINED, AND IT SHOULD STOP BEING CARRIED AS A WATCH ITEM.** This paragraph originally said it did NOT recur, on a mid-session reading of pid 18660 taken before the merges; that reading was correct and the conclusion drawn from it was wrong, and both halves are kept here because the error is the instructive part - a watch item was declared quiet while the very act that moves it had not happened yet. Measured at wrap: pid 18492, alive true, `last_reload_ok` true, started `2026-09-18T22:01:06Z`. So it DID move, a THIRD consecutive wave. **MECHANISM, and it is expected behaviour rather than a defect:** `tests/conftest.py:238-253` already documents that `web_dashboard.py:58-61` calls `start_watcher(_APP_DIR)` at MODULE level, and the watcher writes `restart_trigger.txt` - the supervisor's reload signal - whenever any non-frozen `.py` changes. conftest NEUTRALISES the test-side copy, but the PRODUCTION watcher in the live RC process is untouched and writes the same signal. This wave merged `.py` files twice (one tests-only merge, one carrying `scripts/wakeup_prune.py`) and `logs/2026-09-18.log` records exactly TWO RC starts in that window, at 16:56:45 and 17:01:06, the earlier process dying with no shutdown line and no traceback after a routine LCU poll. A `.py`-bearing merge asking the supervisor for a reload is the hot-reload path working. **TWO FENCES, both load-bearing.** (1) conftest's own docstring states the attribution limit explicitly: two plausible writers can author the same signal, so NO single restart can be attributed, and this note does not claim otherwise - the pattern across three waves is what is being explained, not any one bounce. (2) `logs/2026-09-18.log` shows TEN RC starts across the whole day, including a CLUSTER OF FOUR inside three minutes at 04:36-04:38 plus two more at 05:00. A tight cluster is NOT explained by the merge mechanism above and is NOT claimed to be; it was not investigated this wave and is the part still worth a look. Re-probe rather than inheriting any of this.

**TRAPS PAID.** A verifier OVERTURNED OR AMENDED A PRODUCER CLAIM IN EVERY SLICE for the THIRD WAVE RUNNING - three waves at one hundred percent is the expected cost of a slice, so budget the grading pass. **THE MERGER WAS AGAIN A PROPAGATION VECTOR:** the brief to the fixing slice inherited a verifier's INCOMPLETE framing that the `trailing_extras` hoist was the ENTIRE reorder, and the slice measured it insufficient and disagreed with the brief. Wave 6 recorded the same failure from the same actor, so the orchestrator's own briefs are a NAMED risk surface now - re-derive a mechanism claim AT the turn you brief it, and note that a VERIFIER's framing is every bit as inheritable as a producer's. **AN ORDER-BLIND INVARIANT LET A HISTORY REWRITE LOOK SAFE** - the first slice defended its disclosed side effect with an identical-character-multiset argument, and order is exactly what the no-history-rewrite rule protects; choose an invariant that can SEE the property the rule protects. **ON RM-474 THE CODE WAS RIGHT IN EVERY PASS WHILE THE PROSE ABOUT THE CODE WAS WRONG IN THREE PLACES** (an absolute "cannot silently reach zero", a false "(AST missed this)" parenthetical, and a covered-list inviting the wrong conclusion about a post-read filter; the third sat in an INLINE COMMENT the findings had not named) - a sentence asserting what a guard covers is itself a claim needing its own measurement, and a prose sweep must read the comments. **A SHIFT-DOMINATED METRIC READ 0.27 PERCENT APART ACROSS A CATEGORICAL SAFETY BOUNDARY:** positional line-inequality reads 30772 for the state that deletes 1335 lines of protected history and 30689 for the state that deletes none, with byte delta +304 for BOTH. Only the difflib DELETION count discriminates. Before a metric gates a categorical safety decision, check that it SEPARATES the two categories.

**STANDING ENVIRONMENT NOTE - settle this once per session, not once per slice.** A trailing harness instruction directs agents to make file changes via Bash `sed` and heredocs, which CONTRADICTS CLAUDE.md R1. FOUR slices this wave independently reported IGNORING it for tracked files and confining Bash to reads and scratchpad files. That is the CORRECT resolution - `tools/stop_claim_gate.py` corroborates file claims from the Edit/Write tool-call record, so a Bash-mediated write reads as an UNBACKED CLAIM even when the change is real and committed - and future slices should keep making it.

**STOP-GATE PHRASING TRAP - recorded as AUTHORING DISCIPLINE, not as a gate defect.** Do NOT file it or propose patching it; memory already records that patching `CLAIM_COUNT` is REFUTED, and a true-positive on wording is the behaviour that makes the gate useful. Three measured trigger shapes fired this session, all phrasing rather than substance: (1) `push_claim_without_push` fired on a sentence describing a PRIOR session's remote state, where the evidence was a live `git ls-remote origin refs/heads/main` returning a ref equal to local HEAD - the gate cannot distinguish a measurement about someone else's push from a claim about your own; (2) it fired AGAIN on the ordinary English idiom "pushed back", meaning a slice disagreed with a brief, where no push of any kind was described - the check keys on the word; (3) `count_mismatch` fired twice on VERIFIER-REPORTED counts taken under deliberate mutants (`53 passed / 0 failed`) compared against the merger's own run of the UNMUTATED file (`54 passed`), where both figures were correct for their own tree state and the mismatch was an artefact of stating a subagent's mutant measurement as a bare assertion. **THE DURABLE HABIT: BACKTICK AND ATTRIBUTE ANY COUNT YOU DID NOT RUN YOURSELF, AT AUTHORING TIME.** The gate strips a backticked figure before checking, and it re-reads the WHOLE transcript at every Stop, so a later correction never clears a finding - findings ACCUMULATE. Also avoid "pushed" and "pushed back" as ordinary verbs when you have not pushed.

## NEXT SESSION

**The push decision for wave 7 comes first** - `233a3d13b` and `06cde1618` are on `main` and unpushed, and the push halts on its diff. Then RM-475, the one row this wave filed. RM-474 and RM-276 are SHIPPED; read the markers and do not re-investigate either. RM-239 / RM-242 need their OWN row defect fixed before either is touched, and that is an OPERATOR call already put to the operator and NOT yet confirmed - do not start it on your own. Re-probe the pid-bounce watch item rather than inheriting either wave's reading, and re-probe health rather than assuming a restart is or is not owed. Every HALT row still needs the operator.
