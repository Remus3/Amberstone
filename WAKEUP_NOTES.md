# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary + 2026-05-20 housekeeping batch + 2026-05-21 items 121-125 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---
# 2026-05-21 - item 132 3-slice parallel drain SHIPPED (3 worktree agents + 3 merge commits `cdb0006` `79a956b` `bc055c2`, pushed; non-engine; non-frozen; RC restarted clean pid 8960 -> 11420 last_reload_ok=True; no DS restart; no ENGINE bump)

Operator "continue wakeup_notes and backlog with multi agents in parallel - commit and push for a /done and /clear - notify when done". 3 worktree agents dispatched parallel on 3 disjoint slices composing on item 131 Slice A `core/post_game_rubric.py` (which had ZERO live consumers at session start). All 3 merged + green + pushed. RC suite **2811 -> 2897 / 67 subtests / 0 failed** (+86: A=29, C=56, +1 indirect).

**Slice A `2015d39` feat(coach) - post_game_rubric live wire as `/api/post-game-rubric` + last-match hero block chip (FIRST live consumer of item 131):** NEW `dashboard/routes_post_game_rubric.py` (250 LOC) GET `/api/post-game-rubric?match_id=<id>` returns `{ok, match_id, role, total_score, components, percentile_grade, weights_used, elapsed_ms, cached}`. Reads operator's row from `data/rewind_history.db` participants (PUUID from `data/rewind_catchup.state.json` - reads `puuid` first then `stale_puuid` fallback for PUUID rotation per item 124). Calls `compute_role_grade(stats, role)` with `stats={kills, deaths, assists, cs, game_time_s, vision_score, damage_dealt_to_champions, obj_participation_pct=0.0}` (no obj_participation enrichment yet - noted in code as future from objectives_stolen/dragon/baron); `role` = participant `team_position` (BOTTOM/JUNGLE/UTILITY canonicalized via `_normalize_role`). 5-min TTL cache keyed on match_id (mirrors routes_post_game_wpa pattern). Fail-soft: 400 missing/blank match_id / 404 unknown match / 503 missing rewind_history.db / 503 missing state.json. Registered in `dashboard/_dispatch.py` GET_ROUTES wire (NOT a `register()` callable - followed existing pattern). Frontend chip mounted as 7th column of `.lm-hero-identity` (`#lm-hero-role-grade`) parallel to item 115's `#lm-hero-score`: NEW `_setHeroRoleGrade(m)` helper in `web/js/panels/last_match.js` (+44 LOC) fetches `/api/post-game-rubric?match_id=<m.matchId>` post-enrichment, renders rounded `total_score` + `percentile_grade` tier label (S+/S/A/B/C/D), hidden until enrichment lands. NEW CSS (+57 LOC) for `#lm-hero-role-grade` with 6 data-tier color variants (S+/S/A=good, B=warn, C/D=bad). +30 tests across 2 NEW files: `tests/test_routes_post_game_rubric.py` (17) + `tests/test_last_match_role_grade_dom.py` (13). **Live curl verified end-to-end:** `GET /api/post-game-rubric?match_id=NA1_5560540832` (operator's recent ADC match) returns `ok=true role=ADC total_score=33.97 percentile_grade=D components={kda:0.77, cs_per_min:0.90, obj_participation:0.0, vision:0.6, dpm:1.13} weights_used={kda:2.1, cs_per_min:0.85, obj_participation:0.5, vision_score:0.3, damage_per_min:0.85} elapsed_ms=1`.

**Slice B `ded589c` docs(backlog) - stale-sweep per feedback_backlog_path_stale_check:** BACKLOG.md only (4 lines changed, 2 ins / 2 del). **2 entries flipped:** (1) Dead Man's Plate Momentum stacks-schema (line 14): VERIFIED STALE via grep of `stack_ramp_seconds` in `_effects_types.py` + `_effects_data.py:223742 Shipwrecker` -> flipped to `✅ SHIPPED 2026-05-21` (CLAUDE.md item 122, commit `ce563c4`; ENGINE 1.25.0 -> 1.26.0). (2) Future EHP enemy-CC model (line 13): reframed to reflect 3 live consumers now shipped (`aram_tenacity_line` item 126; `enemy_aram_tenacity_line` item 127; `_PER_SPELL_CC_DURATIONS` engine seam item 130 Slice A `8dc3a6d`) - engine fight-sim CC populator remains the only future work. **Verified-current (no flip needed):** 5 SHIPPED-in-parens BACKLOG research entries (LoLytics WPA / draft-elo top_contributions / augment_formula_eval / _hextech_drakes_by_team / ban_suggest_toggle.js) - source files all present. ROADMAP.md top entries already marked SHIPPED post-2026-05-21 sweep. 4 open ROADMAP items (Active Match step 5 / Phase 3 fixture flow / ADR-007 phase 3 / live ARAM Mayhem test) legitimately operator-gated or live-gated. No new stale items discovered. ASCII clean (0 em/en-dashes, 0 smart quotes).

**Slice C `7a2450b` feat(coach) - role-grade aggregation in postmortem + personal-context card extension (SECOND live consumer of item 131):** Extended `scripts/postmortem_analyze.py` (+163 LOC) to compute role grade per match alongside death patterns. NEW top-level key `role_grades` in `data/coaching/death_patterns.json` (schema bumped v1 -> v2; v1 files return `{}` from loader so route omits the block entirely - backward-compatible). Iterates over participants matching operator's `puuid` + `stale_puuid` from state.json; skips event-mode rows with empty `team_position` (ARAM/Arena/etc.); aggregates by_role (count + median + tier distribution S+/S/A/B/C/D) + overall (cross-role aggregate). Atomic write via tempfile.mkstemp + os.replace (already used in script). Extended `core/death_patterns_loader.py` (+24 LOC) with NEW `role_grades_summary(path=None) -> dict` helper - returns the role_grades dict or `{}` on missing/malformed/empty/v1. Extended `dashboard/routes_personal_context.py` (+25 LOC) to add `role_grades` block to GET response; 60s MTIME-keyed cache unchanged. Extended `web/js/panels/personal_context.js` (+100 LOC) + `web/css/panels/personal_context.css` (+155 LOC) to render NEW card BELOW existing top-3 death-pattern cards: header "Recent role grades", big numeric overall median score, tier strip (S+/S/A/B/C/D) with count chips, per-role mini-rows (only roles with count > 0), card hidden when `overall.count == 0`. **+56 tests across 4 test files:** `test_postmortem_analyze.py` 33 -> 60 (+27) / `test_death_patterns_loader.py` 6 -> 12 (+6) / `test_routes_personal_context.py` 12 -> 17 (+5) / `test_personal_context_panel_dom.py` 20 -> 38 (+18). **Live `--dry-run` sample against operator's data** (puuid + stale_puuid, 624 matches scored): overall median=41 tier_dist {S+:0, S:8, A:56, B:164, C:159, D:237}; by_role ADC=177 (median 41) / SUP=79 (57) / JG=52 (32) / MID=80 (44) / TOP=236 (37). Cadence: RC-PostmortemAnalyze first scheduled run 2026-05-24 04:15 picks up v2 automatically; no manual intervention needed.

**Merge order:** B first (docs-only, no conflict). A second (new route + frontend chip; no conflict). C third (1 auto-merge in `web/index.html` between Slice A's `lm-hero-role-grade` 7th-column mount + Slice C's personal-context card extension; Git's ort strategy resolved cleanly - both touched DIFFERENT subtrees of index.html). Merge commits `cdb0006` / `79a956b` / `bc055c2`.

**Verified:** RC suite **2811 -> 2897 passed / 67 subtests / 0 failed** (+86); py_compile + ruff + ASCII clean across all 13 touched + 4 new test files (0 non-ASCII added by any slice). RC restarted via `restart_trigger.txt` (route module addition triggered restart): pid 8960 -> 11420 alive=True last_reload_ok=True mode=client. Live curl `/api/post-game-rubric?match_id=NA1_5560540832` returned ok=true with full ADC role grade output (see Slice A above). Worktree cleanup pending. No DS restart (non-engine). No ENGINE bump.

**Don't-redo:**
- post_game_rubric NOW has 2 live consumers (route + postmortem aggregation); item 131 Slice A's `core/post_game_rubric.py` is the source-of-truth - do NOT duplicate the rubric math in either consumer. The `compute_role_grade(stats, role)` API contract is the seam.
- The route reads PUUID from state.json's `puuid` key (NOT `current_puuid` - that was a spec assumption that didn't match the actual key). Falls back to `stale_puuid` for PUUID rotation per item 124. Both shapes tested.
- The route stats payload uses `game_time_s` + `damage_dealt_to_champions` (rubric's actual API contract per `core/post_game_rubric.py:212-243`), NOT `game_duration_minutes` + `damage_dealt` from spec - the module is source-of-truth.
- `obj_participation_pct=0.0` is INTENTIONAL for now (no direct DB column; future enrichment via objectives_stolen + dragon + baron counts noted in code comment). The component scores correctly without it; the operator just under-scores the obj_participation axis until that enrichment ships.
- The role-grades JSON schema bumped v1 -> v2 (NOT in-place extension). v1 files return `{}` from loader so route omits the block entirely - backward-compatible without rewriting the existing v1 file (the cadence cron auto-upgrades to v2 on first 2026-05-24 04:15 run).
- The personal-context card hides when `overall.count == 0` (empty state); do NOT render the strip with zeros (clutters the panel for fresh operators without postmortem data yet).
- The 7th-column chip on `.lm-hero-identity` widened the grid; do NOT shrink back - both `#lm-hero-score` (item 115) + `#lm-hero-role-grade` (this slice) coexist now.
- BACKLOG entry for Dead Man's Plate stacks-schema is now ✅ SHIPPED (item 122); do NOT re-list as open. Same for the aram_tenacity_mult line (3 live consumers shipped). The "feedback_backlog_path_stale_check" pattern caught both.

**Carries forward:**
- (a) Item 131 carries-forward unchanged: ADR-010 Arena S2 26.09 PBE-gated; post_game_rubric weight calibration via custom `weights` kwarg or future JSON override loader (still single-source-of-truth on `_DEFAULT_WEIGHTS`); 5 role weight vectors are starting calibration only - operator can tune.
- (b) Item 130 carries unchanged: DD Defy heal-on-takedown deferred / `_PER_SPELL_CC_DURATIONS` registry empty (forward-marker) / live ARAM/SR smoke pending / RC-PostmortemAnalyze first run 2026-05-24 04:15 (LiveTaskResult=0 verification next session) / `_MISSING_HP_SHARE_FOR_HEALS` calibration operator-gated.
- (c) obj_participation_pct enrichment from objectives/dragon/baron columns is the natural follow-up to wire actual obj-participation into the route - the rubric weights it (0.5 ADC / 0.7 JG default) so today's "0.0 always" under-scores by ~5-10 points on participation-heavy roles. Operator-gated (depends on enrichment cadence).
- (d) Item 129 / 128 / 127 / 126 / 125 carries unchanged.

Frozen-file grant NOT used this session.

---
# 2026-05-21 - Phase 6.5 3-slice parallel drain SHIPPED (3 commits + 3 merges; ENGINE 1.28.0 -> 1.29.0)

Operator "continue with the wakeup_notes, multi agent parallel runs". 3 worktree agents dispatched in parallel on the 3 Phase-6.5 carries-forward from item 129 (DD takedown-rate deferred per don't-redo). All 3 merged + green + pushed. DS :8893 restarted via taskkill PID 7540 + schtasks /Run RC-DaemonSlayer; /health returns engine_version=1.29.0. RC not restarted (DS engine slice).

**Slice A `8dc3a6d` feat(ds) - per-spell CC duration extractor seam (item 129 carry (a)):** NEW `AbilitySpellDps.cc_duration_s: tuple[float, ...] = ()` + `cc_duration_post_tenacity: tuple[float, ...] = ()` fields. NEW module-level `_PER_SPELL_CC_DURATIONS: dict[str, dict[str, tuple[float, ...]]] = {}` registry seam in `ability_dps.py` (LEFT EMPTY - forward-marker pattern mirroring s112 `STAT_GRANT_CALC_KEYS`). Consumer in `compute_ability_dps` per-spell loop reads from registry + applies `effective_cc_duration(base, tenacity_mult)` via direct import from `.ehp` (no circular). Production behavior byte-identical at empty registry. 2nd consumer of the `effective_cc_duration` helper shipped 1.25.0 (item 122) with zero consumers; this slice is forward-marker for a future EHP-vs-CC blended scorer. +30 tests in NEW `test_per_spell_cc_phase65.py`. +15 stale ENGINE pin syncs (1.28.0 -> 1.29.0) across 14 DS test files. ENGINE bumped 1.28.0 -> 1.29.0 in `__init__.py` with full changelog block.

**Slice B `0163a34` feat(ds) - SV amp on Phase 1.5 shields (item 129 carry (b)-1):** Closes the Phase 6 deliberate omission "Spirit Visage amp on Phase 1.5 SHIELDS". heal_amp_mult now multiplies shield_any/phys/mag/true at the EHP-math top of damage stack (siblings, NOT nested with heal_total amp). NEW `EhpResult.shield_amp_mult: float = 1.0` field surfaces the multiplier alongside `heal_amp_mult`. Shields stay PRE-amp in their dedicated fields for transparency (matches `heal_item_total`/`heal_lifesteal` pre-amp convention). format_table + to_dict + notes updated. SpiritVisageAmpTests pinning test FLIPPED from "does_not_amp" to "amps_at_125". +22 tests in NEW `test_ehp_shield_amp_phase65.py`. Math: Sett L11 + Sterak (shield_any=240 baseline) + SV (heal_amp_pct=0.25) -> shield_any=480 (compound: SV bumps bonus_hp from 400 to 800 = Sterak's 60% bonus_hp ANY scales 240 -> 480) + shield_amp_mult=1.25 -> physical_ehp lift = +1324.2 EHP (compound of HP bump + amp).

**Slice C `3a86cf9` feat(ds) - Sundered Sky missing-HP heal piece (item 129 carry (b)-2):** Closes the Phase 6 deliberate omission "Sundered Sky 6% missing-HP additive on the heal piece". NEW `ItemHeal.missing_hp_pct: float = 0.0` field + `resolve_magnitude(missing_hp=...)` keyword. Sundered Sky 6610 + Arena 226610 wired with `missing_hp_pct=0.06`. NEW `_MISSING_HP_SHARE_FOR_HEALS = 0.5` module constant in `ehp.py` - Phase 6.5 mid-fight HP-share assumption (50% missing HP) distinct from the full-HP steady-state convention used elsewhere; operator-tunable single constant. `_collect_heals` threads missing_hp through; compute_ehp derives `missing_hp = total_hp * _MISSING_HP_SHARE_FOR_HEALS`. +14 tests in extended `test_ehp_heal_phase6.py` (8 MissingHpAdditiveTests + 6 SunderedSkyMissingHpTests). 2 existing assertions rebaselined for the new mid-fight magnitudes (SunderedSkyHealTests + SpiritVisageAmpTests). Math: Aatrox L11 (HP=2050.35) + Sundered Sky melee at mid-fight (missing_hp=1025.175) -> heal piece = 103.875 + 0.06 * 1025.175 = 165.39 (vs Phase 6 baseline 103.875). Live `compute_ehp` returns 165.39 exact.

**Merge order:** A first (bumps ENGINE + syncs 14 pin sites). B second (auto-merge clean). C third (1 conflict in ehp.py docstring - both B's ENGINE 1.29.0 SV-amp paragraph + C's "SHIPPED in Phase 6.5" entry kept; resolved by combining; 3 historical references "ENGINE 1.28.0+" -> "ENGINE 1.29.0" in `_effects_types.py:311` + `test_ehp_heal_phase6.py:335,399`). Merge commit `98deb10`.

**Verified:** DS suite **2962 -> 3028 / 1 skipped / 1 xfailed / 1663 subtests** (+66: 30+22+14). Wider RC **2780 passed / 67 subtests / 0 failed** (phase8_smoke `test_live_three_profiles` flipped pass post-DS-restart). py_compile + ruff + ASCII clean across all touched files. DS :8893 restarted via taskkill PID 7540 + schtasks /Run RC-DaemonSlayer -> /health serves 1.29.0 patch=16.10.1. Worktrees + 3 worktree branches unlocked + force-removed + branch -d cleaned.

**Don't-redo:**
- `_PER_SPELL_CC_DURATIONS` STAYS EMPTY at 1.29.0 (forward-marker; mirrors s112 `STAT_GRANT_CALC_KEYS` pattern). Production byte-identical to pre-slice. Future patches populate when a CC consumer (fight-sim / coach-prompt / EHP-vs-CC blended scorer) needs the data.
- The SV-on-shields amp uses the SAME `_total_heal_amp` value as heals (today: Spirit Visage 1.25). Two amps are SIBLINGS at EHP-math top of damage stack, NOT nested (heal_total amped once by heal_amp_mult; shield_any amped once by shield_amp_mult; products added). `NoDoubleAmpTests` x4 pin.
- `EhpResult.shield_amp_mult` exposes the multiplier as a SEPARATE field from `heal_amp_mult` so future heal-only or shield-only amp items stay representable - do NOT collapse to one field.
- `_MISSING_HP_SHARE_FOR_HEALS = 0.5` is the Phase 6.5 mid-fight convention - a SINGLE constant operator can tune. The EHP scorer's normal convention is full-HP steady-state; this constant is the ONE deliberate departure. Do NOT vary per-item.
- Sundered Sky's missing-HP piece is composed BEFORE the ranged_modifier (ranged user halves base AD piece AND missing-HP piece together).
- 2 existing assertions in `SunderedSkyHealTests::test_aatrox_l11_sundered_sky_melee_heal` (range 100-110 -> 160-170) and `SpiritVisageAmpTests::test_sundered_sky_plus_spirit_visage_amped` (reframed for amped-build missing_hp) re-baselined NOT monkeypatched - the always-on `_MISSING_HP_SHARE_FOR_HEALS` is what the engine ships.
- Death's Dance Defy heal-on-takedown DELIBERATELY DEFERRED (Phase 6.5 omission carry-forward). The takedown-rate assumption is genuinely uncertain; do NOT pre-wire with a constant probability.

**Carries forward:**
- (a) Death's Dance Defy heal-on-takedown still queued (needs takedown-rate model; operator-gated whether to ship at all - it could over- or under-credit depending on actual takedown frequency in real games).
- (b) `_PER_SPELL_CC_DURATIONS` registry is empty - the engine-side fight-sim consumer that actually reads it is FUTURE work. Today the field is set and propagated through to_dict but no math depends on it.
- (c) Live ARAM/SR smoke STILL pending (operator must play a real game to verify Phase 6 + 6.5 healing/shielding reflects in EHP rankings live).
- (d) RC-PostmortemAnalyze first cron 2026-05-24 04:15 - verify LastTaskResult=0 next session.
- (e) `_MISSING_HP_SHARE_FOR_HEALS = 0.5` may calibrate (operator can tune if real-game data suggests a different mid-fight share).

---
# 2026-05-21 - EHP Phase 6 healing throughput SHIPPED (1 commit `5018e9f`; ENGINE 1.27.0 -> 1.28.0)

Operator AskUserQuestion-picked "EHP Phase 6 healing throughput" from a 4-option fork (Phase 6 / per-spell CC / postmortem pre-flight / housekeeping). The natural follow-up per item 128 carry-forward (a). RC not restarted (DS engine slice, RC consumes over HTTP); DS :8893 restarted once for engine bump. Pushed + CI green; no frozen-file edits.

**Commit `5018e9f` feat(ds):** Closes `ehp.py:23` deliberate Phase-1 omission "Healing throughput (lifesteal, Spirit Visage amp) - fits Phase 6". Three contributions feed an EHP heal pool: (a) item-passive heals via NEW `ItemHeal` dataclass + `ItemEffect.heal` field - Sundered Sky 6610 / Arena 226610 Lightshield Strike (100% base AD melee / 50% ranged, one-trigger-per-fight under 10s/target CD); (b) lifesteal-derived heal `stats.lifesteal * stats.ad * stats.as * _FIGHT_WINDOW_S (6.0)` over standard fight window (pre-mitigation approximation matching EHP's no-enemy-pen Phase-1 posture); (c) multiplicative heal amp via NEW `ItemEffect.heal_amp_pct` field - Spirit Visage 3065 / Arena 223065 Boundless Vitality +25% (stacks multiplicatively per buff-system doctrine). Bloodthirster 3072 / Arena 223072 Ichorshield ships in the Phase 1.5 ItemShield pipeline (full-cap steady-state: 165 L1 -> 315 L18, ANY damage type). NO `unique_passive_key="lifeline"` - BT's Ichorshield is a distinct unique passive and stacks with any single lifeline shield.

**EhpResult new fields:** heal_item_total / heal_lifesteal / heal_amp_mult / heal_total / heal_sources. compute_ehp folds heal_total into physical_ehp / magical_ehp / true_ehp at top of damage stack (heals don't discriminate by damage type, so heal pool acts like an ANY shield).

**Deliberate Phase 6 omissions (deferred to Phase 6.5+):**
- Death's Dance Defy heal-on-takedown (75% bonus AD over 2s) - takedown-rate uncertain; 6333 + 226333 stay defensive_only.
- Spirit Visage amp on Phase 1.5 SHIELDS - engine ships heal-pipeline amp only; SV + lifeline-item under-credits the shield piece by ~15% (60% bonus_hp shield * 0.25 amp lost on a Sterak example).
- Sundered Sky 6% missing-HP additive - needs current-HP-share assumption distinct from full-HP convention.

**Tests:** +56 in NEW `tests/test_ehp_heal_phase6.py` across 11 classes: ItemHealSchemaTests (9: construction + scaling composition + ranged modifier + edge cases), CollectHealsTests (5), TotalHealAmpTests (6), LifestealHealTests (7), BTIchorshieldTests (8: level lerp + non-lifeline unique + EHP lift), SunderedSkyHealTests (4: melee/ranged + Arena mirror + periodic preserved), SpiritVisageAmpTests (5: amp pool only, NOT Phase 1.5 shields), EhpResultHealFieldsTests (4: to_dict + format_table + notes), DeathsDanceDeferredTests (3), BTPlusLifelineStacksTests (2: both shields aggregate at engine layer), EngineVersionCurrentTests (1). +15 stale ENGINE pin syncs across 14 DS test files (1.27.0 -> 1.28.0).

**Verified:** DS suite 2906 -> 2962 / 1 skipped / 1 xfailed / 1663 subtests. Wider RC 2780/0 (phase8_smoke flipped pass post-DS-restart). py_compile + ruff + ASCII clean on all 5 touched source files (0 non-ASCII added). DS :8893 restarted via taskkill PID 8564 + schtasks /Run RC-DaemonSlayer ritual; /health returns engine_version=1.28.0 patch=16.10.1.

**Math verified end-to-end:** Aatrox L11 + Sundered Sky + Spirit Visage = base AD 103.875 * 1.25 amp = 129.84 heal_total exactly. Aatrox L11 + BT alone: shield_any=198.33 (165 + 2/9 * 150 at L11) + heal_lifesteal=134.67 (0.15 * 183.88 AD * 0.8137 AS * 6).

**Don't-redo:** BT shield does NOT share `unique_passive_key="lifeline"` (distinct Ichorshield unique - stacks alongside Sterak/Shieldbow/Maw/Hexdrinker at the engine layer; rank.py upstream dedup is separate). Spirit Visage amp applies to HEAL pool ONLY in this engine (deliberate Phase 6 boundary; SpiritVisageAmpTests.test_spirit_visage_does_not_amp_phase15_shields pins). Lifesteal heal uses PRE-mitigation AD (EHP scorer is enemy-state-agnostic; over-credits by ~30-40% vs 60-90 armor - consistent with the no-enemy-pen Phase 1 omission still in force). Sundered Sky preserves its existing PeriodicProc damage proc - heal field is ADDED, not replacing. The `_FIGHT_WINDOW_S = 6.0` constant matches existing sustained/burst boundary in the DPS layer. DD 6333 stays defensive_only - the Defy heal-on-takedown rate is genuinely uncertain; do NOT pre-wire with a constant takedown probability.

**Next session:** (a) Per-spell CC duration extractor as 2nd consumer of `effective_cc_duration` helper (engine-side; ABILITY_RESULT.cc_duration_s field; would compose with the Phase 6 heal model into a future EHP-vs-CC blended scorer). (b) Phase 6.5 work queued: Death's Dance Defy heal (needs takedown-rate model), Spirit Visage amp on Phase 1.5 shields, Sundered Sky 6% missing-HP additive. (c) Live ARAM/SR smoke STILL pending - operator must play modified-tenacity champ + verify Phase 6 healing reflects in EHP rankings live. (d) RC-PostmortemAnalyze first cron 2026-05-24 04:15 - verify LastTaskResult=0 next session.

---
# 2026-05-21 - DS 3-slice drain SHIPPED (3 commits `c36446b` `13eb869` `d854951`; ENGINE 1.26.0 -> 1.27.0)

Operator: "continue ds" -> 3 scoped slices, each AskUserQuestion-framed. RC restarted 2x (coach prompt edits); DS :8893 restarted once for engine bump. All 3 pushed + CI green; no frozen-file edits.

**Slice 126 `c36446b` feat(coach):** EHP enemy-CC coach consumer (ARAM tenacity). First live consumer of `effective_cc_duration` helper (shipped 1.25.0 with zero consumers). NEW `core/aram_tenacity_context.py` loads `data/daemon_slayer/<patch>/champions.json` once at module-import -> `_TENACITY_MAP` of 17 non-1.0 champs (15 at 1.20x + 2 at 1.10x). `aram_tenacity_line(champion, mode) -> str` renders the prompt line. Wired into `coaches/aram_coach.py` `_USER_TMPL` between `Enemy keystones` and `DS top items`. Cached `_SYSTEM` unchanged (cache-prefix preservation). `tests/test_ds_pick_consumption_p1l11.py` fixture +1 line. +29 tests. RC 2733 -> 2762.

**Slice 127 `13eb869` feat(coach):** Per-enemy ARAM tenacity wire. Symmetric extension of 126 - aramTenacity is the multiplier on ANY CC on that champion (operator's CC on enemy lasts longer too). NEW `enemy_aram_tenacity_line(enemies, mode) -> str` renders `"Enemy ARAM tenacity (your CC on them lasts longer): Talon 1.20x, Zed 1.20x"`, sorted desc-mult then alpha. Wired next to `{aram_tenacity}`. +18 tests. RC 2762 -> 2780.

**Slice 128 `d854951` feat(ds):** EHP Phase 1.5 shield throughput. Closes `ehp.py:21` deliberate Phase-1 omission. 4 LIFELINE-style shields (Sterak 3053 60% bonus_hp ANY; Shieldbow 6673 400->700 ANY ranged x0.80; Maw 3156 200+150% bonus_ad MAGICAL ranged x0.75; Hexdrinker 3155 110->280 MAGICAL ranged x0.75). NEW `ItemShield` dataclass in `_effects_types.py` + `ItemEffect.shield` field + `ANY` constant + `_SHIELD_TYPES`. EhpResult gains shield_any/phys/mag/true/sources fields; `compute_ehp` folds totals at top of damage stack (shields share armor/MR factor with HP per League's damage model). NEW `_is_ranged` (threshold 250) + `_collect_shields` aggregator. Bloodthirster ichor-shield + Doran's Shield block-per-source DEFERRED to Phase 6. ENGINE 1.26.0 -> 1.27.0; +15 stale ENGINE pin syncs across 14 test files. +46 tests. DS 2860 -> 2906; RC 2780 unchanged. DS server taskkill + schtasks /Run RC-DaemonSlayer -> /health 1.27.0 live.

**Don't-redo:** all 4 lifeline items use EXISTING `unique_passive_key="lifeline"` (Phase 4 batch 12). Shields share SAME armor/MR factor as HP (NOT un-resisted). `compute_ehp` does NOT apply unique_passive dedup itself - rank.py's `shares_dead_unique` is upstream. The 17-champ tenacity map is patch-data-driven via `champions.json` -> `lolmath.aram_modifiers.aramTenacity`; do NOT hardcode. Per-call injection via `_USER_TMPL` (cache-prefix preservation); do NOT migrate to system-prompt suffix. Only ARAM coach is wired; Brawl/Arena/SR coaches do NOT have aramTenacity in their modes.

**Next session:** (a) Phase 6 healing throughput (lifesteal + Spirit Visage + Death's Dance + Bloodthirster ichor-shield) - natural follow-up. (b) Per-spell CC duration extractor as a 2nd consumer of the tenacity wire (engine-side). (c) live ARAM smoke still pending - operator must play modified-tenacity champ or face them. (d) RC-PostmortemAnalyze first cron 2026-05-24 04:15 - verify LastTaskResult=0 next session.
