# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary + 2026-05-20 housekeeping batch + 2026-05-21 items 121-130 + 2026-05-22 items 133-139 + 2026-05-22 items 140-149 + item 181 + item 187 + item 188 + item 189 + item 190 + item 191 + item 192 + item 193 + item 194 + item 195 + item 196 + item 197 + item 198 + item 199 + item 200 + item 204 + item 215 + item 216 + item 227 + item 228 + item 241 + item 242 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-06-01 - item 245: DS gap-plan A3 - externalize cc_conditional registry to JSON + live /rank?mode=aram echo (commit 81a8c97; pushed adcdbd9..81a8c97; CI green run 26738527735; NO ENGINE bump - structural 1.76.0 unchanged; DS restarted 1.76.0 to load A3 loaders + item-244 rank.py case-fix; RC not restarted - byte-identical)

Operator: "start next items" (DS_GAP_COMPLETION_PLAN A3 = the item-241 "do FIRST next session"). Shipped A3 + confirmed item-244 NEXT(1).

A3 (commit 81a8c97, NO ENGINE bump): `cc_conditional.py` 4911 -> 938 lines. The 2 hand-authored builders (`_build_per_spell_cc_conditional` 3295L + `_build_per_spell_cc_conditional_forms` 777L) became thin JSON loaders reading NEW co-located `agents/daemon_slayer/cc_conditional_registry.json` (64 primary + 8 forms = 72 entries / 57 champs). Stored `probability` = SEED midpoint; loaders re-apply `_p`/`_p_form` so the calibration-override layer (`data/cc_conditional_calibration.json`, gitignored) is preserved. BYTE-IDENTICAL proven: snapshot sha 8d2073f0 over every entry asdict + get_conditional_entries + get_total_conditional_cc_seconds + compute_cc_pressure(SR/ARAM, include_conditional=True) for all 57 champs matched pre/post; full DS suite 5679 green. Builder source + REJECT rationale relocated VERBATIM to `CC_CONDITIONAL_NOTES.md` (zero token loss, mirrors item-241 A1 CHANGELOG move) - EXCLUDED from Share mirror (dev-context; loader reads only the JSON) alongside CHANGELOG.md in ds_share_sync. NEW `tools/ds_cc_conditional_to_json.py` (one-shot generator + `--check` drift guard; refuses to bake operator overrides). 14 wave source-grep tests re-pointed cc_conditional.py -> CC_CONDITIONAL_NOTES.md (wave17/18 CONCAT cc.py + archive for kept COND_TRAVERSE constant / coexists dataclass-docstring text). NEW `tests/test_cc_conditional_externalization_item245.py` (11) repo guard. Share re-synced 242 -> 245 files (`--check` clean; gist auto-pushed 254 files).

item 244 NEXT(1) CONFIRMED + CLOSED (live :8893 echo): DS restarted (taskkill pid 22200 + schtasks RC-DaemonSlayer) -> 1.76.0/16.11.1/172/705 (now serves rank.py case-fix + A3 loaders). POST /rank mode=aram (lowercase) == mode=ARAM (uppercase) IDENTICAL 20-item pool (case-fix live-proven). Golden Spatula 994403 (maps['12']=True but in _NON_COACHABLE_ITEM_IDS) + 224403 (maps['12']=False) both correctly ABSENT. 223069 in the pool = "Void Immolation" (maps['12']=True, LEGIT ARAM, not an Arena mirror - my 22-prefix heuristic false-positive; verified before asserting).

Don't-redo: (a) cc_conditional_registry.json IS the registry source now; add new entries by hand (ConditionalCcEntry __post_init__ validates at load) - do NOT re-inline builders. Stored `probability` is the SEED (loader re-applies _p); the generator refuses to serialize with active overrides. (b) CC_CONDITIONAL_NOTES.md is the verbatim authoring archive + REJECT rationale - repo-internal, NOT mirrored to Share (excluded in ds_share_sync alongside CHANGELOG.md); the 14 wave source-grep tests now read it (wave17/18 concat cc.py for kept tag-constant/dataclass content). (c) A3 byte-identical (sha 8d2073f0) - no ENGINE bump, no re-rank; RC NOT restarted (byte-identical; picks up on next supervisor restart). (d) /rank?mode=aram is now live-confirmed clean - close that todo; 223069 Void Immolation is a LEGIT ARAM item, not a leak.

NEXT (DS_GAP_COMPLETION_PLAN headless spine; each Phase-C sub-item = own ENGINE bump + DS restart, ships default-OFF byte-identical, flag-flip is Phase D/live): C1 Sion Q + Hwei Q f2 block-index routing (gated under apply_ability_amps; _STAGED_AMP_CANDIDATES in _ability_amp_overrides.py - each form's "Maximum ..." block already models full charge so ROUTE the block-index via _registries.py block-index resolver, do NOT add a double-counting amp; Hwei Q f2 also needs a missing-HP coeff = Gap-2 schema ext) / C2 AA-empower seam in compute_dps (the 5 base="aa" entries inert in ability_dps) / C3 author 8 exotic passives default-OFF in _passive_damage_overrides.py. Then Phase D (live flag-flips) + Phase E (Share re-sync) + item-240 UI part-3 + #7/#8 sign-off + item-243 NEXT(2) ranked-SR UI watch (live).

---

# 2026-06-01 - item 244: rank mode case-insensitivity + champ-select archetype resolver + coach markdown strip + live SR-ranked watch (2 commits fb8f67f + 25b0be3; CI green; NON-engine, ENGINE 1.76.0 unchanged; no DS restart; RC pid 13544 healthy)

Operator: "start next item on wakeup_notes for SR Ranked queue" (item 243 NEXT(1)), then live champ-select/in-game/post-game watch (NEXT(2)), then /done + "do all 3" (fix the 3 backlog flags I surfaced).
- `fb8f67f` fix(ds): item 243 NEXT(1) SHIPPED. `_is_legal_in_mode` (rank.py:329) + `strip_arena_trinkets` (rank.py:163) now `mode.upper()` - a LOWERCASE `/rank?mode=aram` body bypassed the map filter (MODE_MAP_ID keys uppercase -> .get("aram")=None -> allow-all, admitted 224403 Golden Spatula + Arena 22-mirror ids). +28 tests test_rank_mode_case_insensitive_item244.py (pool-equality lc vs uc per mode). Production COACH path uppercase = byte-identical -> NO ENGINE bump, NO DS restart. RED proof captured 224403 admitted; GREEN closes it.
- `25b0be3` fix(coach): "do all 3" follow-ups. F1: `_active_champion` (_state_builder.py) resolves champ-select `my_champion` numeric id -> name via NEW `core.archetype_picks.champion_name_by_key`, so `cs_archetype_pick` populates pre-game (payload carries my_champion+local_cell, not local_pick name; local_pick kept as precedence fallback). F2: `coach_integration/_coach._parse_response` strips stray `**bold**`/`*em*`/leading-trailing `*` per field (Haiku leaked "** FREEZE WAVE" into coach.action; choices JSON untouched). F3 RETRACTED: `data/coaching_data.json` (Alistar 39d) is the client-mode after-game-summary target (core/aftergame_summary.py), NOT a decoy - no change. Share/src re-synced (fb8f67f's rank.py+test were unmirrored; --check now in-sync, engine 1.76.0). +12 tests.
- LIVE WATCH (item-211 + item-243 surfaces verified clean on a real ranked SR Caitlyn game): build chooser populated 3 paths + userbuild, DS-vs-enemy-comp 0 junk, summoners [4,21] Flash+Barrier; post-game ingest row 1805 Caitlyn SR game_id=5572690426 detail=True (no misattribution); live-metrics live_5572690426 15 rows 3 milestones (SR keys by real live_<gameId>; ARAM uses synthetic sess_<mode>_<champ>_N).
- Don't-redo: rank.py mode.upper() is byte-identical for uppercase (production) callers; do NOT bump ENGINE for it. F3 is settled (after-game summary file, not stale - do not delete/re-flag). `MODE_TO_FILE['sr']` reads repo-ROOT `coaching_data.json` (fresh), `data/coaching_data.json` is the separate after-game client summary.
- NEXT carry: live `/rank?mode=aram` :8893 echo deferred (needs DS restart; in-process test already proves the route code path filters). item 243 NEXT(2) watch ongoing; NEXT(3) item 240/241 SR champ-select part-3 + DS Phase C/D still owed.

# 2026-06-01 - CI hygiene gate + RC_LIVE_METRICS wire-all + ARAM Mayhem live test (relay-token + build-chooser + Golden-Spatula fixes) (item 243; 6 commits ca8be3f..770012f; all CI green; ENGINE 1.75.0->1.76.0; DS restarted 1.76.0; RC restarted pid->13544)

Operator: make the Share-data smart-quote gate always-checked; "when can we turn RC_LIVE_METRICS on properly"; then live ARAM Mayhem test. 6 commits, all CI green:
- `35cbe65` ci+test: smart-quote hygiene gate now a dedicated CI step (smart-quote+mojibake+u2500) + in /done §0. Root: `test_smart_quote_hygiene` flagged `Share/src/data/daemon_slayer/**` em-dashes = byte-identical mirror of the already-excluded `data/daemon_slayer/` source (ds_share_sync copies verbatim). Excluded the mirror in BOTH the test + `tools/strip_smart_quotes.py` `_is_external_data` (lockstep).
- `d246efc` feat: RC_LIVE_METRICS wired into ALL 4 live coaches via NEW `core/live_metrics.py` (was ARAM-only). `enabled()` = env OR `config/coach_settings.json live_metrics_enabled` (re-read live, no restart - spend-gate pattern). SR via `coach_integration/_coach._write_fields` gated on `update_ts`. Spend gates re-enabled sr/arena/aram+vision (brawl/tft off) per operator.
- `449f33c` fix: synthetic `match_id` when game_id absent (ARAM has none; item 211) - `sess_<mode>_<champ>_N`, resets on game-clock drop >30s. WITHOUT this ARAM captured nothing.
- `165965f` fix(relay): `gamepc_liveclient_relay.py` `_resolve_auth_token()` reads canonical `config/vision_token.txt` (was stale hardcode `8e8f131e` -> 401 silent -> app never entered game mode -> COACH DEAD). Item-242 bug in the SIBLING relay agent (item-215 LIVE VERIFY gap). Found + fixed MID-GAME; mode flipped client->game, coach ticked.
- `374401e` fix(ui): in-game build chooser (`item_build.js`) was hidden for EVERY champion since the items 178/179 collapse (`variants.length < 2` guard; collapse made it always 1 + carries `build_paths[]`). `_ibFlattenRows` flattens build_paths into `<variant>:<path>` selectable rows (champ_select.js pattern). NOT Varus-specific.
- `770012f` feat(ds): item 243 ENGINE 1.76.0 - `rank.py _NON_COACHABLE_ITEM_IDS={994403 Golden Spatula, 663064 Veigar's Talisman}` skipped in `_filter_candidates`. Root: DDragon mis-flags Golden Spatula `maps['12']=True` (ARAM); item 213 cleaned curated loadouts but the live pool reads the maps flag directly. +7 tests; `test_rank_mode_legality_p1l23` expected-set subtracts the deny; Share re-synced 1.76.0; DS suite 5670.

LIVE-VERIFIED this session (operator played ARAM Mayhem Varus): live-metrics captured `sess_aram_Varus_1` 60 rows (game_start/L6/L11/10min); build chooser renders 3 builds; coach ARAM picks clean (no Golden Spatula). Don't-redo: relay token canonical = `config/vision_token.txt` (BOTH agents now); live-metrics config switch is the durable on-path; the build-chooser collapse fix is global not Varus.

NEXT SESSION (operator agenda): (1) SHIP the `_is_legal_in_mode` `mode.upper()` hardening - the `/rank` endpoint with a LOWERCASE mode silently bypasses the map filter (MODE_MAP_ID keys uppercase -> `.get("aram")`=None -> admits ALL incl Arena 22-prefix mirror ids + maps-false "The Golden Spatula" 4403/224403); the COACH path is UNAFFECTED (uses uppercase `ARAM`, verified) so this is a robustness/Brawl-bug-class fix not a live bug. Then probe `/rank?mode=aram` to confirm it filters + clear this todo. (2) WATCH the ranked-SR champ-select UI + random in-game UI for inconsistencies/missing features (operator queuing ranked SR now - the live game will be in progress; tick the dashboard vs liveclient like this session caught the build-chooser + Golden Spatula). (3) item 240/241 SR champ-select part-3 + DS Phase C/D still owed.

---

