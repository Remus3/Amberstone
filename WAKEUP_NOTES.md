# WAKEUP_NOTES - RC hand-off ledger



> Older sessions live in `docs/history_notes.md` (append-only archive); per-item ledger in `docs/LEDGER.md`. Newest 3 sessions kept here verbatim. Last relocation: 2026-09-16, the laned-loop doc wrap (relocated `2026-09-14` the public surface refresh, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 1 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 966"; newest 3 = `2026-09-16b` this wrap, `2026-09-16` the acknowledge-path wrap, `2026-09-15` the moon-sync merge). The pass before this one, on 2026-09-15, was the moon-sync five-slice merge wrap (relocated `2026-09-12e` the discovery-axis + inter-scorer lane and `2026-09-12d` the fleet tooling-tier lane, VERBATIM via `scripts/wakeup_prune.py --keep 3`, which reported "moving 2 session(s)" and "WAKEUP_NOTES now has 3 session(s); archive now has 964" - read those counts off the tool's own output, never off a recollection; newest 3 = `2026-09-15` this wrap, `2026-09-14` the public surface refresh, `2026-09-12f` the calibration lane). The pass before this one, on 2026-09-12, relocated `2026-09-12c` the SESSION WRAP doc-sync and reported "moving 1 session(s)" / "archive now has 962". The pass before that relocated `2026-09-12b` the five-slice merge and `2026-09-12a` RM-412 C1 OSS extraction, and reported "moving 2 session(s)" / "archive now has 961". The pass before that relocated `2026-09-11d`, the one before that `2026-09-11c`, and the one before that `2026-09-11b` and `2026-09-11a`, the same way. The 2026-09-11k RELOCATION DUE note that sat here is DISCHARGED and deleted - the file is back at keep-3. The letter suffixes are PER FILE and have diverged from `docs/LEDGER.md` - RM-385 is `c` there and `d` here; do not reconcile them. NOTE: `scripts/wakeup_prune.py` **is FIXED as of 2026-07-19** (`4a707962`) - its `SESSION_RE` no longer requires a word boundary after the day, so letter-suffixed headers like `# 2026-07-19a` match and the prune works at `--keep 3`. Relocations are automatic again; the prior standing "manual until fixed" instruction is retired. The 2026-09-16 RELOCATION DUE note that sat here is DISCHARGED and deleted by the 2026-09-16b prune above.

---

# 2026-09-16b - laned headless orchestrated loop, operator away: three waves shipped, the inbox read but unanswered, every HALT row left untouched

**Pushed `4a1c5e7a6..59b3f6253`** plus this wrap commit. LEDGER **1419** (waves 1-2) and **1420** (wave 3). CI green on `b2a71d710`; later heads were still pending when this was written - re-probe with `gh`, do not inherit a verdict.

**SHIPPED, queue order:** (1) the ban-arm space bypass; (2) an always-skipping test, plus its mutants sibling; (3) worktree hook exposure MEASURED - hooks run the PRIMARY tree's scripts but the worktree's TOOLS - so the P1 fix: the sibling sweep, DEGRADED inside a lane worktree, now resolves its config from the main tree and halts DEGRADED at pre-push; (4) spec grammar drift; (5) the ungraded-guard sweep - 17 of 20 graded, 2 new grading tests, 1 dead guard removed as RM-438; (6) the durable-store digest rule added to CLAUDE.md Testing Discipline. Also: the unlistable-inbox watcher/poller fix (from inbox triage), the hook-log teardown flake, RM-414, RM-416, the item_advisor residuals, RM-435, RM-436, RM-438, RM-439, the CRLF writer in `scripts/data_pipeline.py` (RC-PatchRefresh), and the DDragon 16.18.1 data commit.

**Inbox:** 10 notes (CS / LW / LL / RSC) read and marked seen, **NOT replied** - the operator is away and a reply is a byte leaving the tree.

**HALT rows UNTOUCHED:** the criterion (d) flip / CHANNEL_VERSION 2; BOUNCE_POLICY still NOT-DECIDED; replies to LL / LW / RSC / CS; the blocked third-party skills; the responder model pin.

**TRAPS PAID:** worktree isolation cuts from ORIGIN, not local main, so slices must fast-forward first. **GREEN ON WINDOWS IS NOT GREEN** - un-skipped surrogate arms broke Linux CI, and `os.lstat` raises `UnicodeEncodeError`, not `OSError`, on POSIX. Run the repo-wide hygiene guards (`test_skip_condition_hygiene`) at MERGE - a slice-local run misses them. A verifier widening a guard's vocabulary can open an always-true-skip loophole. A scheduled task (RC-PatchRefresh, Wednesday 11:07) rewrites tracked data MID-SESSION.

**Doc wrap:** `ROADMAP.md` and `CLAUDE.md` pulled under the 90 percent drift budget by verbatim relocation (ROADMAP pointers to `docs/ROADMAP_HISTORY.md` `## 2026-09-16`; CLAUDE supporting narrative to `docs/history_notes.md`); no rule, frozen entry or open row moved.

## NEXT SESSION

RC-local rows: **RM-432 (design first)**, RM-433, RM-434, RM-437, **RM-440 through RM-446**. A DS batch for the 16.18.1 Rageblade change is a SEPARATE decision, not a default. Every HALT row above still needs the operator.

---

# 2026-09-16 - the acknowledge path silently erased a store the watcher would have flagged, and RC published four corrections against its own record

Commit: `89b7a8bd8` - one commit, 13 files changed, 2020 insertions, 6 deletions: `CLAUDE.md`, `tools/rc_facts.py`, `tools/inbox_responder_runner.py`, `docs/RESPONDER_RUNNER_SPEC.md`, `docs/BOUNCE_POLICY.md` (new), `ops/loop/config.json`, `ops/loop/executor.py`, `tests/test_inbox_ack_refuses.py` (new), `tests/test_responder_grammar_table.py` (new), `tests/test_responder_arming_contract.py` (new), `tests/test_executor_effort_flag.py` (new), `tests/test_inbox_responder_runner.py`, `tests/test_inbox_responder_mutants.py`. The pre-commit gate ran and passed: py_compile OK on 9 files, glyph gate clean. Push status + sibling-sweep result: pushed `502d0f130..89b7a8bd8` to `origin/main`, exit 0; the pre-push sibling-name sweep ran **ARMED** (4 name slots, 4 counterparty codes from per-host config) and returned clean over 106865 bytes, 13 files and 1 commit message, disclosing **0 binary/LFS blobs not content-scanned** - that last figure is the sweep naming its own blind spot, not a clean bill over LFS content. Suites: a full `pytest tests` returned **22520 passed, 103 skipped, 4971 subtests passed, exit 0, 5319.68s** - **but that run was taken BEFORE two late changes landed**, the spec-doc edit and the effort wiring, **so it is NOT a full green on the final tree and must not be quoted as one**. Those two are separately covered by every suite that reads either artifact: **403 passed, 1 skipped** across `tests/test_inbox_responder_runner.py`, `tests/test_inbox_responder_spawn.py`, `tests/test_loop_director_context_caps.py`, `tests/test_loop_module_root_resolution.py`, `tests/test_subagent_prompt_flag.py`, `tests/test_inbox_responder_mutants.py`. A confirming full re-run was started against the settled tree and **had NOT returned when this was written**. **NOW CLOSED, recorded beside the original rather than replacing it:** it returned **22525 passed, 103 skipped, 4971 subtests passed, exit 0, 5111.11s** against `64d6a3ad0`, so the final tree IS fully green and the caveat above is discharged. The delta 22520 -> 22525 is exactly the five new effort arms - an arithmetic check tying the two runs together, not a number to accept on sight. Tier-1 throughout - two production tools, one executor, four new test files, `CLAUDE.md`, one new doc; no `agents/daemon_slayer/` path, no `ENGINE_VERSION` move, no `:8860` bounce, no Share mirror, no frozen file, no served path. Working tree at the start of the wrap, read off `git status --short`, not recalled: ` M CLAUDE.md`, ` M tests/test_inbox_responder_mutants.py`, ` M tests/test_inbox_responder_runner.py`, ` M tools/inbox_responder_runner.py`, ` M tools/rc_facts.py`, `?? docs/BOUNCE_POLICY.md`, `?? tests/test_inbox_ack_refuses.py`, `?? tests/test_responder_arming_contract.py`, `?? tests/test_responder_grammar_table.py`. Head before this session's commit: `502d0f130` / `ca3d4a869` / `ee1a1f157`.

**THE ONE REAL DEFECT, AND WHAT MADE IT INVISIBLE WAS NOT THE BUG BUT THE ASYMMETRY.** `mark_inbox_seen()` in `tools/rc_facts.py` wrote an EMPTY seen set over the existing one when the inbox directory was ABSENT, printed a success line, and exited 0. Measured on an isolated copy: **22503 bytes holding 198 acknowledgement keys became 18 bytes holding an empty list.** It is reachable because the inbox directory is gitignored - absent in a fresh clone, absent in every worktree, removable by a routine clean - and **RC keeps six long-lived lane worktrees**. **RC's WATCHER treated that identical condition as a recorded fault and printed an UNMEASURED line; the ACKNOWLEDGE path read it as "zero notes". One condition, two readings, and the destructive one was the silent one.** That is the transferable part: a grep for "does this handle a missing inbox" finds handling on both paths and stops. The discriminator is not presence of handling, it is whether the two paths AGREE on what the condition MEANS.

**THE FIX PROBES, IT DOES NOT CATCH.** Both absence and unlistability are probed EXPLICITLY before any write, deliberately rather than leaning on the iteration to raise, so a later reader who wraps the loop in a try/except cannot reintroduce the erasure. Both refusals print a line beginning `REFUSED:` naming the store as UNCHANGED and return **exit 3**. **Exit 3 was chosen, not defaulted:** 1 is what an uncaught exception produces including a module that fails to parse, and 2 is the conventional CLI-usage code, so a reader seeing 2 cannot separate a deliberate refusal from a module that never parsed. TDD in `tests/test_inbox_ack_refuses.py`: **RED 4 failed / 4 passed, GREEN 8 passed**; the mutation arm **kills a mutant that had SURVIVED ALL 56 PRIOR ARMS at exit 0, and nothing else kills it**. Adjacent suites re-run rather than inherited: `tests/test_inbox_watcher.py` **21 passed**, `tests/test_inbox_reported_record.py` **35 passed**. **The LIVE seen store was never exercised: 22503 bytes, sha256 beginning `6c61a7dc`, 198 keys, byte-identical before and after the whole session.**

**THE SHARPEST FINDING OF THE ROUND IS NOT WHO HAS THE DEFECT.** The count is **THREE of four trees, not two** - one tree corrected RC directly, having measured only the UNREADABLE half and reported it as all three, then found the absent-inbox defect in ITSELF on reading RC's note and fixed it. But the count is not the useful part. **Two trees are exact MIRRORS: RC's ABSENT case was destructive and its UNLISTABLE case clean; another tree's was the reverse.** Same class - I COULD NOT LOOK - one branch apart. **The absent case gets a guard because it is the case a developer imagines; the unlistable case is the one only a permission bit produces, and nobody writes a fixture for it unprompted.** So **a tree that finds one of the two branches clean must NOT conclude the class is covered.** RC's fix covers both branches explicitly, by design rather than by luck.

**SIX OF THE ELEVEN ARM CRITERIA NOW CLOSED WITH MEASUREMENT.** **(d) CLOSED** - gate 6 is parametrised over the filename-grammar table, dispositions shipping exactly as the channel document records them, **Variant A stays REFUSE**. All EIGHT mutation classes caught, including a separator DELETED, a separator DUPLICATED and a row ADDED; **the row count is an EXACT pin, never a floor, because a floor cannot detect growth**. Behaviour-identity corpus is a **FROZEN checked-in list of 214 literals at 0 divergences**, plus a **200k random-name fuzz at 0 divergences**. **The corpus was deliberately NOT harvested from the gitignored inbox** - a harvested corpus is a property of the CHECKOUT, not of the REPOSITORY. **(h) CLOSED** - `model` is a required MATCH against the runner's own pin, fail-closed as `malformed:model`. **(j) CLOSED** - `contract_version` checked against a guarded `CHANNEL_VERSION` constant, with distinct details for absent-or-mistyped versus mismatch. Counts: `tests/test_responder_grammar_table.py` **49 passed**, `tests/test_responder_arming_contract.py` **26 passed**. **Guard-deletion measured: all three guards GRADED, none ungraded.** **(g) the CLI pin: PINNED-AND-VERIFIED** - the spawn resolver has three sources, the WINNING one reads an absolute literal from a repo config file so the service-for-user logon environment cannot change it, live version equals the pin at **2.1.251**. **The latent trap is recorded rather than left to be rediscovered: the CLI is on the INTERACTIVE PATH and ABSENT from machine-scope PATH**, so if that config source were removed the dry cycle would still resolve while the scheduled task resolved nothing - every interactive probe green, the armed path dead. **(f) reciprocity: clean, exit 0, 0 undelivered of 114 authored notes.** **(i): `docs/BOUNCE_POLICY.md`, 232 lines, five positions taken, plus a NOT-DECIDED section** for what is genuinely not RC's to settle alone; the section is the deliverable, not a gap in it.

**THE EFFORT WIRING IS GUARDED ON THE SHIPPED VALUE, NOT ON THE WIRING, BECAUSE THE FLAG FAILS SOFT.** TDD: **RED 2 failed / 3 passed, GREEN 5 passed**; `tests/test_loop_executor.py` **157 passed**, `tests/test_subagent_prompt_flag.py` **6 passed**, `tests/test_inbox_responder_exec.py` **90 passed**. Accepted `--effort` values MACHINE-VERIFIED on CLI 2.1.251 by probing an invalid value: **low, medium, high, xhigh, max**. **An unrecognised value warns and falls back to the default** - so a typo would silently run every lane at default effort and nothing would fail, which is precisely why the guard asserts the shipped config value rather than merely that the flag is wired.

**A CENSUS WHOSE TWO NUMBERS ARE BOTH CORRECT, WHICH IS EXACTLY WHY IT WAS MIS-QUOTED.** RC's own inbox: **183 entries, 177 markdown, 21 failing the grammar**. A fleet-wide DEDUPLICATED census across all five readable inboxes: **341 unique entries, 331 unique markdown, 50 failing**. **The orchestrator conflated them and had to correct itself** - the fleet figure read against RC's denominator looks like a threefold regression in RC's own hygiene. Neither number is wrong; the denominator is the fact, and a count published without it is not a measurement.

**`CLAUDE.md` GAINED A FIFTH TRAP ON THE THIRD-PARTY LICENCE GATE: THE WRAPPER DOES NOT CLEAR THE PAYLOAD** - plus the rule that a LICENCE audit and a BEHAVIOUR audit are DIFFERENT AUDITS, and that enumerating every top-level directory is a precondition for both. 26 insertions, **54044 -> 56027 bytes, 5413 bytes of headroom against the 60KB CI budget**. It also records that **"audit the tarball, not the repo" is REFUTED**: the file that was missed was BYTE-IDENTICAL between repository and tarball, and what hid it was an UNENUMERATED DIRECTORY. Swapping the artifact would have changed nothing; enumerating the tree would have changed everything.

**THREE THIRD-PARTY REVIEWS, ALL GATED, NONE VENDORED.** One: MIT with two real grantors and a verified declared upstream, but its own third-party notices disclose embedded brand-mark vector data under a non-commercial share-alike licence plus two further share-alike marks - USE-AS-TOOL-ONLY, and after the channel exchange RC declined any install form at all. Two: source-available licence confirmed from the LICENSE text, DO-NOT-VENDOR; **RC published that installing it was likely fine and WITHDREW that** after a sibling measured a wired-in event forwarder in an unenumerated directory, gated on a config key with **no writer shipped** - which is the fifth trap arriving as a live case rather than a hypothetical. Three: clean MIT whose README upstream list is **INCOMPLETE - two upstreams it never names** - carrying a share-alike catalogue relicensed as MIT with no share-alike notice; two skills BLOCKED, and a directory-scoped grant on another upstream confirmed independently by a sibling rather than on RC's own read.

**THE CHANNEL EXCHANGE - four trees, many rounds, and the session's most useful output is FOUR CORRECTIONS RC PUBLISHED AGAINST ITS OWN RECORD:** the install form it recommended contradicted its own stated position inside the same note; its repository-only gate method; its worktree generalisation; and its population figure. **The worktree retraction is the instructive one: RC measured a CLONE and generalised to WORKTREES without cutting one.** A clone gets a NEW git directory with its own empty local config; a worktree SHARES the common git directory's config. `core.hooksPath` is LOCAL config, which is precisely why a clone loses it and precisely why a worktree KEEPS it. **RC conflated a gitignored directory's CONTENTS (genuinely absent from a worktree) with that CONFIG (present)** - and **the correct statement of the first half was sitting in a comment in RC's own test file the whole time.**

**WHAT SURVIVED THE RETRACTION, because a withdrawn generalisation does not withdraw its measurements.** A fresh clone runs ZERO hooks and commits a banned-glyph violation silently at exit 0. **The clone's hook directory was NOT EMPTY** - four large-file shims, no pre-commit - so "hooks exist" was TRUE and COMPLETELY IRRELEVANT, which is the cheapest way to talk yourself out of a real gap. A worktree KEEPS the absolute hooks path, so RC's gate fires there, verified with a negative control.

**RC'S NEGATIVE CONTROL FAILED ON ITS FIRST RUN, rejected by a DIFFERENT gate than the one under test - and that catch happened BEFORE publication.** The rule taken from it, which generalises past this session: **a pass is not attributable to the gate under test until a control differing ONLY in the violation has passed AND been shown capable of failing.** A green control that is green for the wrong reason is worse than no control, because it launders an unmeasured claim.

**THREE TREES JOINTLY ANSWERED A PORTABILITY QUESTION NONE COULD ANSWER ALONE:** a real permission denial depends on BOTH the process token AND the directory path. RC held the path constant and varied the token; another tree held the token constant and varied the path. Neither half is conclusive; the pair is.

**STILL OPEN - written as open, with no outcome guessed.** (1) **Criterion (d)'s actual FLIP is NOT RC's to take unilaterally, even though RC's own side of (d) is closed** - it needs a JOINT re-pin of a byte-identical shared document, and **one tree does not hold that document at all, having refused it at a licence gate**. RC's licence answer went out this session and **that tree's gate has since CLEARED it**, so the blocker is MOVING but is NOT closed; do not record it as closed on the strength of the clearance alone. (2) **An EXPOSURE, not a defect: every RC worktree executes the PRIMARY tree's hook scripts, whatever its own branch contains.** RC measured that the gate answered about the correct tree in ONE probe; RC has **NOT** measured a worktree whose branch carries DIFFERENT hook scripts. (3) **RC's own arm that FORBIDS repo-relative paths in the shared document is silently bypassed by a path containing a SPACE - measured.** In a RESOLVE arm a missed token is a blind spot; **in a BAN arm it is a forbidden thing that passes**, and the guard stays green while it does. (4) The confirming full-suite re-run against the settled tree, which had not returned when this was written.

## NEXT SESSION

Do not re-open the acknowledge-path defect, criteria (d)/(f)/(g)/(h)/(i)/(j), the effort wiring, the licence-gate fifth trap, or any of the three third-party reviews - all are closed with measurement and all four self-corrections are already published to the channel. Do not re-derive the two census numbers without carrying their denominators.

Prompt for the next session, verbatim:

> Read `CLAUDE.md`, `MEMORY.md`, `WAKEUP_NOTES.md` (2026-09-16 block) and `docs/LEDGER.md` entry 1418 first, plus `docs/BOUNCE_POLICY.md` and its NOT-DECIDED section. Criteria (d), (f), (g), (h), (i) and (j) are CLOSED with measurement on RC's side - do not re-measure them. The `mark_inbox_seen()` erasure is FIXED and mutation-proven; do not re-litigate the exit-3 choice. The three third-party repositories are REVIEWED and all three are gated - do not re-review them, and do not propose an install form for any of them.
>
> FIRST, confirm the full-suite re-run that was still running at the last wrap: the full green on record was taken BEFORE the spec-doc edit and the effort wiring landed, so the settled tree has no full-suite result yet. Run `python -m pytest tests` from the repo root and report the counts you observe, not the ones filed here.
>
> Then close RC's remaining side of the five-way arm. Before building, re-probe each criterion's status live rather than trusting this prompt - a filed status goes stale inside the commit that writes it, which this session paid for repeatedly. For (d), note that the actual FLIP is NOT unilateral even though RC's side is closed: it needs a JOINT re-pin of a byte-identical shared document, one tree did not hold that document at all having refused it at a licence gate, and that tree's gate has since CLEARED RC's licence answer - so re-probe whether the joint act is now available before assuming either direction.
>
> Two measured items are OPEN and neither is a defect to fix blind. First, every RC worktree executes the PRIMARY tree's hook scripts whatever its own branch contains; RC measured the gate answering about the correct tree in ONE probe and has NOT measured a worktree whose branch carries DIFFERENT hook scripts - cut that worktree and measure it. Second, RC's arm that FORBIDS repo-relative paths in the shared document is silently bypassed by a path containing a SPACE; that is a BAN arm, so the miss is a forbidden thing PASSING, not merely an unseen token - fix it with a failing test first and add a positive control that is shown capable of failing.
>
> Standing rule reinforced by this session and not optional: a pass is not attributable to the gate under test until a control differing ONLY in the violation has passed AND been shown capable of failing. Apply it to every guard you touch. And when you find one branch of a two-branch failure class clean, do not conclude the class is covered - this session found two trees that were exact mirrors of each other on exactly that.

---

# 2026-09-15 - five slices merged cleanly and then produced three defects between them; a test was closing a real live handle, and two correct copies of one rule still disagreed

Five merges on `main` plus four post-merge corrections. The slices were S1 poller hardening (`c0bf5b46d`), S2 the once-per-session reported record (`c19ba11a9`, handle fix `60af3f691`), S3 `docs/CHANNEL.md` v1 and its guards (`6e3c1c752`), S4 the console-flash detector and hook-spawner guard (`a9212c077`), S5 the read-only status route (`b9463b4ba`, merged `4ffac97d0`). Ledger entry 1417 carries the full record.

**THE HEADLINE IS THAT EVERY SLICE PASSED ITS OWN VERIFIER AND THE SESSION'S THREE REAL DEFECTS ALL LIVED BETWEEN THEM.** This is the strongest evidence this repo has produced for why a merger is not a formality. Not one of the three was findable from inside the slice that caused it.

**A TEST CLOSED HANDLE 7 IN THE PYTEST PROCESS.** A poller test faked a Win32 handle through the `_open_process` seam and did NOT fake `_close_handle`, so production closed a genuine live handle. Every later `CreateProcess` in that process then failed `0xC0000142` with EMPTY stdout AND stderr - which reads exactly like a broken command, not like a corrupted process. It is invisible running that module alone; it surfaces only when a second module spawns a subprocess, so module-scoped selection hides it by construction. **The production code was right and the test was the defect**, which is the direction nobody looks first. Fixed test-side with an AST guard over the acquire/release seam PAIR plus an anchor test that the pair names real functions. A repo-wide sibling sweep found exactly ONE defective site - every other handle/fd/socket stub either returns an object owning its own release or stubs the release seam too. The "exactly one" is a measured denominator, which is the only thing that makes it a finding.

**TWO COPIES OF ONE RULE, BOTH GREEN, GENUINELY DISAGREEING.** Once `expect` had passed the route returned OVERDUE and could never reach STALE while the poller returned STALE; a checked-present / expect-missing header graded LIVE on the route and STALE in the poller; and the route had invented an `UNMEASURED` the poller could not emit. **It survived its own slice because the slice's case table validated the route against ITSELF** - `feedback_row_agreement_is_not_evidence` arriving through a fixture. The poller's rule went fact-shaped with a `verdict_for_header` adapter and the copies now mirror branch for branch: a 2268-cell differential grid with zero disagreement, and 13451 header/age/pid cases proving the poller's production behaviour unchanged by the reshaping. That second measurement is the load-bearing one - without it, "reconciled" could have meant the shipped side quietly moved.

**AND THEN THE SURFACES STILL DISAGREED, BECAUSE THE DIVERGENCE WAS ABOVE THE RULE.** With both copies provably identical, an existing-but-unstampable `status.md` still gave UNMEASURED from the route and STALE from `--status`, because the route short-circuited BEFORE the rule ran. **A differential test over a rule cannot see a defect in what calls the rule.** Fixed by narrowing the pre-rule gate to "is there a file at all", and bound by a new ENTRY-POINT parity test driving both entry points over one file - with a positive control at the parent commit that reproduces the disagreement, because a parity test that never went red proves nothing. Vocabulary now: UNMEASURED means ABSENT, STALE means exists-but-unstampable.

**THE CHANNEL DOC EXISTS AND THE COMMIT THAT CREATES IT HALTS ITS OWN PUSH.** `docs/CHANNEL.md` v1 at `6e3c1c752`, `CHANNEL_VERSION: 1`, LF-normalised `sha256` `899f6eb957cc...`, pinned in 1 of 5 trees. `CLAUDE.md` in that same commit adds the doc to the push-halt list, so the push halts under the rule the commit writes. That is the boundary working as designed and is worth not mistaking for a defect next time.

**FIVE PRE-EXISTING FACTS RE-DERIVED RATHER THAN INHERITED**, all dated 2026-09-15: the inbox holds 182 entries of which 176 are `.md` notes; `NOTE_NAME_RE` refuses 21 of those 176 (20 no-HHMM, 1 code-first), which is the whole case for the filed grammar row; `docs/_outbound_` held 8 notes before this session's; the reported record holds one session id with 9 keys; and the seen store was UNTOUCHED, so those 9 stay UNACKED - reporting is not acknowledgement.

**DRIFT THIS SESSION DID NOT CAUSE, FOUND AND FIXED IN THE SAME COMMIT - and the third part is the one worth remembering.** `docs/OPERATIONS.md` was missing a table row for `RC-MoonSyncPoller` (a live `Running` task) and its count sentence was stale in two ways at once. Then the real one: **the bare task's NAME was never renamed by the product rename.** The table row spells it `RiotCommander`; the count sentence, the caveat and the documented check command all spell it `Amberstone`. **So the check command written specifically to catch that task could not see it** - run literally it returns 26, because ZERO tasks are named `Amberstone` and that arm of the filter matches nothing. Amended to the real name it returns 27: 26 `RC-*` plus the one bare `RiotCommander`, State `Disabled`. **A first draft of this block and of ledger 1417 said "27, measured with the doc's own check command", which is a figure the doc's command cannot produce** - caught before the append, which matters because `docs/LEDGER.md` is append-only and the only later remedy is a correction entry beside the wrong one. Same trap `CLAUDE.md` records for the API keys: editing a doc does not rename the thing.

**SEVEN IDS ALLOCATED** - five shipped slices and two filed-not-built rows in `BACKLOG.md` under "Reliability / hardening": the deferred per-repo ack-debt reader over sibling native stores (blocked on two gates, neither RC's to open), and the arming-stage grammar / BOUNCE-policy / CLI-pin / retention bundle. The allocation record is in `docs/DS_SWEEP_TRACKER.md` and the `ROADMAP.md` pointer moved in the same commit. The new next-free figure is deliberately not spelled in this file - a bare mention here reads as an allocation.

**THE THREE THINGS THAT WERE STILL OPEN WHEN THIS BLOCK WAS DRAFTED ARE ALL FILLED, AND EVERY ONE WAS MEASURED AT THE WRAP RATHER THAN CARRIED FORWARD.** Suites on the FINAL tree: 22452 passed, 106 skipped, 1 warning, 4973 subtests, exit 0. An earlier full run on the pre-wrap tree returned three failures and **the attribution was measured, not assumed** - a control worktree at the pre-session commit `87785bbb3` ran all three and got 3 passed, which REFUTED a slice agent's claim that all three were pre-existing; one was a pre-existing CRLF working-tree artifact repaired by re-materialising the file, and the other two were session-caused (a literal Windows account home baked into a poller test fixture, and a skip expressed so the hygiene guard read it as gating on an RC tracked doc rather than on the gitignored per-host config). Both fixes were verified by mutation and neither guard file was modified. RC restarted in-tree: pid 16696 -> 4936, `alive` and `last_reload_ok` true, mode `client`, and `/api/moon-sync-status` served 380 bytes, verdict LIVE, codes and counts only. **All four boundary-crossing acts were authorised in ONE batched operator ping and all four ran** - and each was proved by something other than its own exit code: the push `87785bbb3..e02b9df81` confirmed by `git ls-remote origin main` returning the same sha as local HEAD (sweep ARMED and clean ahead of LFS, 46 files and 20 commit messages); the MSIX shadow delete proved by a harness-shell re-read of `poller_seen.json` (40962 bytes dated 2026-09-07 before, 115935 bytes and current after, both VIRTUALIZED VIEW lines gone from `--status`) because `fsutil hardlink list` returned "The request is not supported" on this volume; the poller restarted on a pid DERIVED at the act from the newest `BOOT pid=` line (9012, the THIRD value this session) and back up as `BOOT pid=20668`, with the `baseline reset at boot` line ABSENT, which is the expected outcome; and the FYI delivered 4 of 4 by `O_EXCL` create then `fsync`, each destination `sha256` re-computed and matched, reciprocity check clean at 0 undelivered of 114.

**DISCLOSED RATHER THAN LEFT TO READ AS A SILENT PASS: `tools/stop_claim_gate.py` held TWO findings open for most of this session and neither was cleared.** One flags a TRUE, measured statement about `CLAUDE.md` whose evidence was a `git show` - the gate corroborates file claims from the Edit/Write tool-call record and cannot see that, and the file was NOT edited to satisfy it. The other read a FUTURE wrap step (CI confirmed green, written as a planned action) as a CI claim at a point where no CI probe had run.

## NEXT SESSION

The moon-sync build work is CLOSED; do not re-open it. Read `docs/LEDGER.md` 1417 and `docs/CHANNEL.md` before touching anything in the channel, and read the do-not-redo list in 1417 before proposing any fleet-surfacing mechanism - twelve of them are already refuted with reasons.

**OPERATOR DIRECTIVE 2026-09-15, and it SUPERSEDES the "neither is buildable yet" stance an earlier draft of this block carried: the NEXT SESSION closes the remaining blocking items for the five-way arm.** That earlier wording was correct about the FLEET halves and wrong to extend the freeze to RC's own. Split the eleven criteria by who can move them and work only RC's side.

Prompt for the next session, verbatim:

> Read `CLAUDE.md`, `MEMORY.md`, `WAKEUP_NOTES.md` (2026-09-15 block), `docs/LEDGER.md` entry 1417 and `docs/CHANNEL.md` first. The moon-sync five-slice work is SHIPPED and CLOSED - do not re-plan it, do not re-pitch any fleet-surfacing mechanism from 1417's do-not-redo list, and do not re-measure the console-flash attribution (the interpreter-token swap is REFUTED by measurement; no swap was made and none may be made). Two rows were FILED and NOT built, both in `BACKLOG.md` under "Reliability / hardening": the deferred per-repo ack-debt reader over sibling native stores, and the arming-stage bundle (widen the note-name pattern to admit the no-HHMM variant and keep refusing the code-first variant, parametrised over the table inside `docs/CHANNEL.md`; append the request's `REVIEW-<sha12>` token to the reply filename; decide the BOUNCE policy in writing; re-measure the CLI pin; and record that no inbox retention rule exists and that any such rule is a five-way convention, never a unilateral act). **YOUR TASK, BY OPERATOR DIRECTIVE 2026-09-15: close every REMAINING BLOCKING ITEM FOR THE FIVE-WAY ARM THAT IS RC'S TO CLOSE.** The eleven criteria live in the stage-2 plan's arming section and are restated in the stage-4 prompts artifact on the operator's Desktop; re-derive their state rather than trusting either. Split them by owner and work ONLY RC's side. **RC's, and buildable now:** (d) the note-name grammar, which is the biggest one - it refuses 21 of 176 live notes today and every refusal writes a permanent BOUNCE into the sender's inbox, so widen it to admit the no-HHMM variant while still refusing the code-first variant, parametrised over the table inside `docs/CHANNEL.md`, and note that changing that doc is a `CHANNEL_VERSION` 2 five-way re-pin, not a unilateral edit; (g) re-measure which CLI binary the S4U task actually resolves and re-pin or fail loudly; (h) name the model id in the arming data contract; (i) draft RC's written BOUNCE policy position - never arm with it undecided; (j) design the same-version contract field as a required, fail-closed field of every agreement record; (f) re-run the reciprocity auditor now that live rows exist. **NOT RC's, and you may not do them:** (a) the CS exit-code flip, (b) the RSC and LW per-prompt watcher work, (e) the LW validate-and-deliver seam. Those are the four sibling-adoption prompts already delivered; the operator is having each tree ingest its own. (c) is already GREEN as of this session's poller restart. **DO NOT ARM ANYTHING, and do not treat "close the blockers" as licence to.** Criterion (k) is standing and structural: RC halts and pings before writing its own agreement record and before enabling its responder task, it never touches another repo's record, HALT file or task, each repo's operator hand-writes their OWN expiring record, and there is no timeout and no auto-proceed - so arming is five separate operator acts in five trees by construction, never one. The order when it comes is pairwise RC and RSC first; LW is never armed in its current shape because its write path IS a bypassPermissions session. The other filed row (the per-repo ack-debt reader) stays blocked on a fleet consent RC cannot grant itself - leave it. **TWO ITEMS ARRIVED AFTER THE SLICES SHIPPED, both in `RC-NEXT-SESSION.txt` in full.** (1) **LL REFUSED `docs/CHANNEL.md` AT THEIR LICENSE GATE** (their note of 2026-09-15T23:33, filed their side as OPS-91) and they are RIGHT: RC's FYI named a commit and called the tree public, and **public is a visibility, not a grant**. They hold no copy and no paraphrase, deliberately, so the doc stays pinned in 1 of 5 trees until RC names a license. The answer was VERIFIED against RC's own tree this session and needs no new work - `Remus3/Amberstone`, PUBLIC, root `LICENSE` Apache-2.0, README's licence paragraph covers source code AND AUTHORED DOCUMENTATION so the doc is in scope, the only carve-out is third-party DATA under `data/` recorded in `NOTICE`, and the three-way contradiction LL cites is RESOLVED (`Share/LICENSE.md` is gone, no "all rights reserved" remains). Sending it is a byte leaving the tree: HALT AND PING. (2) Operator added TWO third-party repos, and **their licence verdicts are OPPOSITE, so do not treat them as one task**. `tt-a1i/archify` (an agent skill compiling system descriptions into diagrams) reports **MIT** - vendorable in principle, but reported-MIT is not enough: read the COPYRIGHT LINE rather than the licence name and cross-check the manifest, both traps having fired here before. `mksglu/context-mode` (context-window optimisation via MCP plus hooks) is **ELv2, SOURCE-AVAILABLE AND NOT OPEN SOURCE** - same family as the BUSL-1.1 row already in the licence gate, so **DO NOT VENDOR ITS SOURCE**: RC is PUBLIC and Apache-2.0 and carrying ELv2 source would misstate RC's own licence to every cloner. Installing and RUNNING it is a separate and likely-fine question - record it as a tool, not as vendored code. A THIRD repo, `timharris707/skills` (23 agent skills), is the **"clean licence, still NOT vendor-safe"** shape the gate already records: it ships an MIT `LICENSE.md`, but much of the catalog is ADAPTED from `mattpocock/skills` and three skills from another author's repo, each naming its upstream in its own file - so there are MULTIPLE COPYRIGHT HOLDERS and the maintainer's MIT cannot relicense what they adapted. Verify each wanted skill's UPSTREAM licence separately, record every upstream in `NOTICE`, and prefer reading for the idea and re-implementing. All three repos and their gates are written out in `RC-NEXT-SESSION.txt`. **The `docs/OPERATIONS.md` scheduled-task drift is ALREADY FIXED - do not re-open it.** The missing `RC-MoonSyncPoller` row, the stale count and the bare task's name (`RiotCommander`, never renamed by the product rename, which is why the documented check command could not see it) all landed in the same commit as this block. If you re-measure and get a different number, that is machine state moving, not drift returning. Free work that IS available: the four sibling-adoption lines in the delivered FYI are each a one-session change in someone else's tree and none of them is RC's to make; and `docs/DS_SWEEP_TRACKER.md` carries a SECOND, never-demoted `Next free id = ` pin (the RM-368 one, far down the file) that the file's own demotion convention was never applied to - harmless to the guard, which reads the FIRST pin, but it means a bare `grep -c 'Next free id = '` returns 2 and must never be used as an apply check. No line number is given for it here on purpose: this session's own edit to that file moves it by twenty lines, which is exactly how a citation goes stale inside the commit that writes it. Session default applies: orchestrated, multi-agent, self-adjudicating, self-adversarial, all doing in subagents. Halt and ping before any byte leaves the tree.
