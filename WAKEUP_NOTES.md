# WAKEUP_NOTES - RC hand-off ledger

> Sessions s27-s137 + s166 + s173.5 + s173.1 + s175 + s176 + s177 + s178 + s179 + s180 + s181 + s193 + s194 + s195 + s197 + s198 + s199 + s200 + s201 + s203 + s204 + s214 + s215 + s225 + s226 + 2026-05-19/20 mid-run summary + 2026-05-20 housekeeping batch + 2026-05-21 items 121-130 + 2026-05-22 items 133-139 + 2026-05-22 items 140-149 + item 181 + item 187 + item 188 + item 189 + item 190 + item 191 + item 192 + item 193 + item 194 archived to docs/history_notes.md. Only the last 3 sessions kept here.

---

# 2026-05-25 (night 5) - item 197 ALL-CLEAN no-commit drain: orphan production .py modules SCAN CLEAN (0 modules surface across coaches/+dashboard/+core/+tft/+lcu/+agents/+ops/non-frozen/+coach_integration/+vision_server/) + orphan test fixtures SCAN CLEAN (41 fixtures across 5 dirs all alive) + dead web/js/*.js SCAN CLEAN (39 JS files all imported) + BACKLOG/ROADMAP stale-sweep wave 28 = 0 flips (12 consecutive zero-flip waves 17-28) + cost/latency wave 34 = 34th consecutive CLEAN since item 134 (0 code commits; non-engine; non-frozen; no DS restart; no RC restart - WAKEUP prune only)

Operator "continue". 47th-streak orchestrator pattern preserved (items 134-189 + slim variants 190/191/192/193/194/195/196/197). 3 parallel Explore agents dispatched concurrent + 4 inline Bash probes; 0 commit-bearing slices. AskUserQuestion NOT used (same continuation framing as items 192-196 + carries clearly enumerated as operator-gated). Pre-flight: 0 open PRs; CI 2 runs since item 196 ship (`0aa23c1` green + `3163e4a` WAKEUP commit queued at probe time); HEAD = `3163e4a`; mode_key=client + RC pid None (idle/down) throughout.

**Slice A no-commit (orphan production .py modules scan CLEAN):** Closes a NEW dead-code surface lane after items 186 + 194 + 195 + 196 closed dead routes / orphan one-off scripts / orphan data + runtime artifacts. Explore agent walked `coaches/` + `dashboard/` + `core/` + `tft/` + `lcu/` + `agents/` + `ops/` (NON-FROZEN only per CLAUDE.md hard-rule frozen list) + `coach_integration/` + `vision_server/` for `.py` modules with ZERO importers. Initial 3 candidates flagged: `core/bridge_envelope.py` 72 LOC + `ops/_ws_probe.py` 32 LOC + `ops/rc_bootstrap.py` 93 LOC. Each grep-verified against ALL `.py` + script invocations + scheduled task XML: **ALL 3 have actual callers** (dynamic imports in `dashboard/_dispatch.py:62-83` + `:124-133` route table + `if __name__ == "__main__":` entrypoints with scheduled-task XML references). Final verdict: **0 orphan production modules**. The dynamic-import-via-string pattern in `dashboard/_dispatch.py` defeated the agent's regex-based grep; orchestrator caught the false-positive via direct import-chain trace. Slice A CLEAN.

**Slice B no-commit (orphan test fixtures scan CLEAN):** Explore agent walked `tests/fixtures/` (6 golden snapshots + 1 state_dicts.py module + 3 qq101 probe fixtures + 10 snapshot_panels fixtures) + `tests/fixtures/snapshots/` (6 game-mode golden) + `web/data/ui_mock/` (13 active match + champ_select + last_match + lobby/history/home/session/user_builds/replay mock fixtures). **41 total fixtures** across 5 directories. Each fixture has ACTIVE test consumer with grep evidence: golden snapshots -> `test_snapshot_authority.py` 27 methods; qq101 probes -> `test_probe_101qq_script.py` 11 methods; snapshot_panels -> `test_panel_snapshots.py` 7 parameterized; ui_mock active_match -> `test_active_match_mock_fixtures.py` 16 methods; state_dicts.py -> 6 test files across phase2_smoke + snapshot_regressions. **0 orphans confirmed.** No deprecated queue IDs (920 Brawl / 1700-1710 retired Arena / 480 retired Swiftplay - all per CLAUDE.md Settled) detected in fixture data. Slice B CLEAN.

**Slice C no-commit (dead web/js/*.js scan CLEAN):** Explore agent walked `web/js/` for `.js` files with ZERO importers from `main.js` / `web/index.html` / cross-imports / `dashboard/_static.py` cache-buster references. **39 total JS files** checked: 1 entry point (main.js) + 12 library files (helpers + idempotent_render + items_index + state + state_schema + lol_descriptions + champion_tags + summoner_spells + scorer_units + dedup_fetch + ws_client + 1 more) + 26 panel files (right_now + next + item_build + map_state + champ_select + active_match + last_match + bridge_pending + dev + trigger_pill + spike_curve + ward_heat + draft_elo + cd_ledger + sr_draft + ...). Every file has confirmed reference chain to live entry points. Exclusions respected per CLAUDE.md "Settled" + item 194: `web/js/dashboard.js` (intentionally kept as dead-code reference) + `web/js/sim.js` (item 194 dead-file exclusion). **0 orphans confirmed.** Slice C CLEAN.

**Slice D no-commit (BACKLOG/ROADMAP stale-sweep wave 28 = 0 flips; 12 consecutive zero-flip waves):** Inline Bash probes verified all file:line citations in OPEN entries grep-verified live within +/- 3 tolerance: ROADMAP L13 `dev.js:361` -> live `verdict.team_won` at L362 (within tolerance); ROADMAP L23 `gamepc_lcu_agent.py:246` ARAM Mayhem queue 2400 -> live mapping at L245 (within tolerance); ROADMAP L82 `tools/gamepc_lcu_agent.py:1179-1228` augment_intent 4-PATCH handler -> live at L1179 (`set_augment_intent` handler entry) + L1227 (`augment_intent_all_endpoints_failed` error string, NOT old `augment_intent_unsupported` stub per item 188 Slice C wire); BACKLOG L92 `core/draft_elo.py:141 cross_pairs()` -> live at L141. **Sweep cycle decay:** wave 17=0 / 18=1 / 19=0 / 20=0 / 21=0 / 22=0 / 23=0 / 24=0 / 25=0 / 26=0 / 27=0 / **28=0** = 12 consecutive zero-flip waves = saturation plateau extends.

**Slice E no-commit (cost/latency wave 34 = 34th consecutive CLEAN since item 134):** Inline Bash probes confirmed all 7 levers green. L1 prompt-cache **13 cache_control across 8 production blocks** verified (coach_integration/_coach.py=2 + aram_coach=2 + arena_coach=2 + brawl_coach=2 + champ_select_coach=2 + replay_coach=1 + sr_coach=0 + tft_coach=0 + aram_team_analyzer=1 + experimental_builder=1 = 13 hits across 8 distinct production coach files; matches item 188 + 195 + 196 baseline; sr_coach.py + tft_coach.py historically have 0 cache_control hits because their prompt structure is per-call dynamic per item 188 audit). L2 `_CACHE` module-level **12 files** in `dashboard/routes_*.py` (matches item 188 carry (g) loose-ledger baseline "12-16 fluctuation; do NOT pin"). L6 14 unique RC-* scheduled tasks (`schtasks /Query /FO CSV` deduped: BridgeDaemon + BridgeWatcher + CostHealthWatchdog + DaemonSlayer + DDragonMirrorRefresh + DS-MatchDB-MCP + PatchRefresh + Phase3-PeriodicAudit + Phase3-Supervisor + PostmortemAnalyze + RewindCatchup + Supervisor + VerifyBridgeRoundtrip-Once + VisionServer = 14; CSV view shows 15 lines because CostHealthWatchdog appears with 2 trigger rows but is a single task). L7 27=27 panel parity (`ls web/css/panels/*.css | wc -l` = 27 + `grep -c "@import.*./panels/" web/css/dashboard.css` = 27 PERFECT PARITY; `tests/test_dashboard_css_panel_imports_parity.py` 4/4 PASS). L3 polling + L4 log spam + L5 model tier inherited from item 195 (no JS / dashboard handler / coach prompt edits this session). **Verdict: 34th consecutive CLEAN since item 134; cost/latency lane saturated.**

**Slice F no-commit (bridge watcher Phase 3 ledger probe):** `ops/runtime/bridge_watcher_health.json`: pid 12000 alive=True queue_depth=0 auto_ok_since_boot=0 auto_err_since_boot=0 (unchanged from items 192/193/194/195 - bridge watcher healthy + lanes baked in BUT 0 firings since item 191 resurrection because inbound bridge traffic is zero). `ops/runtime/bridge_action_history.db` unchanged at 2 rows from 2026-05-03 self-test. Phase 3 95% sample gate STILL BLOCKED by zero inbound traffic, not by watcher health.

**Verified:** No code changes. No engine bump (DS :8893 serves 1.59.0 from item 189; not restarted). No RC restart (pid None idle). No frozen-file grant used. 0 worktrees left dangling (Slices A/B/C used Explore agents = no worktree; Slices D/E/F inline Bash).

**Don't-redo:**
- The 3-lane dead-code surface scan (production .py modules + test fixtures + web JS) is now SATURATED across items 195 (scripts) + 196 (.bak + runtime artifacts) + 197 (production modules + fixtures + JS). The orphan-cleanup methodology is durable BUT future waves should expect ZERO new orphans for 30-60 days as the codebase evolves; re-run after 30+ days only.
- The dynamic-import pattern in `dashboard/_dispatch.py:62-83` + `:124-133` defeats regex-based grep for module callers; future "orphan module" Explore agents MUST trace import chains via string match against the literal `"X.Y"` patterns in the dispatch table, NOT just `import X.Y` syntax.
- The 14-vs-15 RC-* task count quirk in `schtasks /Query /FO CSV` is a CSV-view artifact (CostHealthWatchdog has 2 trigger rows in CSV output but is a SINGLE task); future scheduled-task audits should `Get-ScheduledTask -TaskName "RC-*"` in PowerShell OR dedupe the CSV TaskName column to get the unique count.
- The ROADMAP L82 augment_intent handler is now wired (item 188 Slice C); the OLD `augment_intent_unsupported` no-op error string is REPLACED by `augment_intent_all_endpoints_failed` at L1227 (the 4-endpoint PATCH chain returns this if ALL 4 fail). Future stale-sweep waves should NOT flag this line difference - the rewrite is intentional + the L82 ROADMAP entry already cites the correct `:1179-1228` range.
- Per [[feedback_verify_generated_reports]] this session verified 1 subagent false-positive (Slice A initially flagged 3 candidates -> all 3 had dynamic-import or scheduled-task callers; final verdict 0 orphans). The pattern is durable: subagent flags AUDIT-CLAIMS REQUIRE GREP-EVIDENCE per claim before relaying.
- The orchestrator pattern is now 47th-streak (items 134-189 + slim variants 190/191/192/193/194/195/196/197). 6 consecutive ALL-CLEAN or slim cleanup variants (items 192/193/194/195/196/197) confirm headless dead-code lane is GENUINELY saturated; future continuations should expect operator-driven (Cherry / 101.qq.com / live UI captures) or hardware-migration (Legion 1-PC) work before headless dead-code work resurfaces.

**Carries forward:**
(a) Item 196 carries (a)-(l) ALL unchanged.
(b) Cherry augment live verification + Game-PC HTTP-pull redeploy STILL OWED at next Arena 1750 window.
(c) 101.qq.com operator-at-Game-PC Chrome DevTools capture STILL OWED.
(d) Live ARAM/SR smoke STILL pending.
(e) Calibrations STILL operator-gated.
(f) Legion 1-PC consolidation STILL operator-gated.
(g) Auto-ops verb expansion + auto-action lanes Phase 3 95% gate STILL DEFERRED - watcher pid 12000 healthy + lanes baked in BUT 0 firings (unchanged from items 192-196).
(h) Active Match #11/12/13 real in-game capture STILL OWED.
(i) v2.1 visual captures pages #9/10/11/12/13/14/15/16 OWED at next operator-driven Chrome session.
(j) Rell W form 1 cc_conditional candidate STILL operator-decision-gated.
(k) cc_conditional wave 24+ candidates require either NEW Meraki schema field OR registry shape lift to host passive-AA stack-detonation slot - both operator-gated; saturated per items 192/193/195.
(l) Frozen-file grant NOT used this session.

---

# 2026-05-25 (night 4) - item 196 SHIPPED: 7 orphan dead-artifact deletions (2 repair scripts + 3 stale .bak DB snapshots 1.9 GB + 2 stale runtime artifacts) + drift guard `tests/test_orphan_artifacts_item196.py` extends item 195 surface to .bak + runtime-artifact lanes (1 commit `0aa23c1` pushed origin/main `bdc8f20..0aa23c1`; non-engine; non-frozen; no DS restart; no RC restart - file deletions + new test only)

Operator "continue what is next". 46th-streak orchestrator pattern preserved (items 134-189 + slim variants 190/191/192/193/194/195/196). 3 parallel Explore agents dispatched concurrent; 1 commit-bearing slice. AskUserQuestion NOT used (operator's continuation prompt + carries clearly enumerated as operator-gated; same continuation framing as items 192-195). Pre-flight: 0 open PRs; CI 10/10 green since item 194/195 series (`bdc8f20` last green); HEAD = `bdc8f20`; mode_key=client + lcu_phase=None + RC pid None (idle/down at session start) throughout.

**Slice A `0aa23c1` (direct main) chore(housekeeping) item 196 - 7 orphan dead-artifact deletions + drift guard extending item 195 surface to .bak + runtime-artifact lanes:** Closes a SUB-surface of item 195's orphan-script lane + opens 2 NEW lanes (stale .bak DB snapshots + stale runtime artifacts). 3 parallel Explore agents surfaced 5 candidates each (orphan tests / orphan scripts / orphan data fixtures); orchestrator grep-verified per [[feedback_verify_generated_reports]] all 7 had 0 callers across `.py` + `.js` + `.md` + `.ps1` + `.bat` + `.xml` surfaces + 0 docs references in `docs/` (excluding `docs/API_SURFACE_AUDIT.md` + `data/api_surface.csv` which are dated audit artifacts per [[feedback_no_history_rewrite]] + `docs/history_notes.md` + `ROADMAP.md` SHIPPED narrative references which are immutable historical anchors). **Deleted 7 files:** (1) `scripts/patch_comp_control.py` 3430 bytes mtime 2026-04-19 - one-off repair patching `tft/comp_control.py` orphaned `return False`; target file `tft/comp_control.py` DOES NOT EXIST (item 195 already verified via `tests/test_orphan_scripts_item195.py` deletion of the parallel `scripts/fix_comp_control.py`; this `patch_comp_control.py` is the SECOND repair tool for the same dead target, distinct from item 195's set). (2) `scripts/fix_comp_positioning.py` 28379 bytes mtime 2026-05-18 - validation/correction one-shot for `data/tft_set17_meta.json` which DOES NOT EXIST; tool dead-on-arrival. (3) `data/match_history.db.bak-2026-05-09-pre-darkstar-purge` 200704 bytes mtime 2026-05-09 - atomic-write insurance from s167's `prune_synthetic_matches.py`; .gitignored; 16-day-old; ROADMAP L80 narrative reference is shipped-entry historical anchor (per [[feedback_no_history_rewrite]] - file deletes do NOT require ROADMAP rewrite). (4) `data/match_history.db.bak-2026-05-10-prune-synthetic` 204800 bytes mtime 2026-05-10 - same s167 insurance. (5) `data/rewind_history.db.bak-pre-catchup-2026-05-10` **1748443136 bytes (1.7 GB)** mtime 2026-05-10 - largest single orphan in repo working tree; pre-catchup snapshot from `scripts/rewind_scraper.py` operations; .gitignored; never used as restore point in 15 days. (6) `ops/runtime/audit_2026_04_27.md` 3614 bytes mtime 2026-04-27 - 28-day-old one-off audit report; 0 consumers across .py / .js / .md / .html. (7) `ops/runtime/peer_bridge_watcher_feedback.json` 4422 bytes mtime 2026-05-03 - 22-day-old Peer bridge feedback dump; 0 consumers. **Total disk delta: -1.95 GB / -7 files** (1.7 GB rewind .bak dominates). NEW `tests/test_orphan_artifacts_item196.py` ~40 LOC / 1 test class `OrphanArtifactAbsenceTests` / 1 test `test_orphan_artifacts_stay_deleted` pins all 7 deleted paths as PERMANENTLY removed via `_DELETED_ORPHAN_ARTIFACTS: tuple[str, ...]` constant (mirrors item 195's `_DELETED_ORPHAN_FILES` pattern; broadens scope to include `.bak` database snapshots + runtime artifact lanes - distinct test file name reflects the broader surface). `py -m pytest tests/test_orphan_artifacts_item196.py tests/test_orphan_scripts_item195.py tests/test_dead_endpoint_cleanup_item186.py tests/phase8_smoke/ -q` = **76 PASS in 3.98s**. `py -m ruff check tests/test_orphan_artifacts_item196.py` ALL CHECKS PASSED. The 7 deletions are EXCLUSIVELY orphan repair scripts + atomic-write insurance backups + stale audit dumps - none touched live runtime / route registration / coach prompt / DS engine / frozen file.

**Slices B + C no-commit (other Explore agents surfaced):** Slice B (orphan test files) returned ZERO orphans; codebase is clean per existing drift guards `test_dead_endpoint_cleanup_item186.py` (12 routes) + `test_orphan_scripts_item195.py` (7 scripts). All test files import surviving modules; no test file references deleted routes or handler symbols. Slice C (stale data fixtures) surfaced ZERO orphans in `web/data/ui_mock/*.json` - all 13 mock fixtures (champ_select_sr/aram/arena + last_match_aram/arena + lobby + history + home + session + user_builds + replay + active_match_sr/aram/arena) confirmed alive via JS dispatcher consumers (`_csMockLoad` + `_lmMockLoad` + `_amMockLoad` + per-page mock helpers per items 158-188). The 7 orphan artifacts surfaced this session are EXCLUSIVELY in `scripts/` + `data/` + `ops/runtime/`, NOT `web/data/ui_mock/`. No code commits from Slices B + C.

**Verified:** `py -m pytest tests/test_orphan_artifacts_item196.py tests/test_orphan_scripts_item195.py tests/test_dead_endpoint_cleanup_item186.py tests/phase8_smoke/ -q` = **76 PASS** (test_orphan_artifacts_item196 1 + test_orphan_scripts_item195 1 + test_dead_endpoint_cleanup_item186 4 + phase8_smoke 70). `py -m ruff check tests/test_orphan_artifacts_item196.py` ALL CHECKS PASSED. No DS restart (non-engine; DS :8893 serves 1.59.0 from item 189). No RC restart (no route module edits; pure file deletions + new test only). No frozen-file grant used. 0 worktrees left dangling (3 Explore agents = no worktree). RC pid None at probe time (mode_key=client; idle/down outside of any operator-driven action).

**Don't-redo:**
- The 7 deleted orphan artifacts are now CI-blocked from re-introduction by `tests/test_orphan_artifacts_item196.py::OrphanArtifactAbsenceTests`. Do NOT re-add. If a future TFT-repair / DB-snapshot / runtime-dump workflow needs similar functionality, name it differently + ensure live consumer exists before shipping.
- `tft/comp_control.py` + `data/tft_set17_meta.json` BOTH do not exist. Item 195 closed `scripts/fix_comp_control.py` (the FIRST repair tool for `tft/comp_control.py`); item 196 closes `scripts/patch_comp_control.py` (the SECOND repair tool for the same dead target). Two distinct repair-tool wrappers for the same dead target both deleted. Do NOT re-create either.
- `.bak` database files at `data/*.db.bak-*` are atomic-write insurance, NOT persistent restore points. `scripts/prune_synthetic_matches.py` + `scripts/rewind_scraper.py` create them BEFORE write; once the parent operation lands successfully, the `.bak` becomes stale forever. They are .gitignored (NOT tracked) so deletion does not require commit narrative beyond ledger. The 1.7 GB rewind .bak was the dominant disk-space orphan in the working tree.
- `ops/runtime/*.json` + `ops/runtime/*.md` are runtime artifacts; the live ones (`health.json` + `bridge_watcher_health.json` + `bridge_action_history.db`) regenerate every poll cycle. One-off audit reports + bridge feedback dumps that haven't been touched in 22+ days are orphan; the drift guard `test_dead_endpoint_cleanup_item186.py::CallerSurfaceAbsenceTests` exclusion for `ops/runtime/` prefix (per item 194) is the canonical "runtime cache files are OK to exist" pattern - the orphan-artifact drift guard targets SPECIFIC dated artifacts not the directory wholesale.
- The orphan-artifact drift guard pattern (`tests/test_orphan_artifacts_item196.py`) extends item 195's surface (orphan SCRIPTS only) to include `.bak` + runtime-artifact lanes. Future dead-artifact-cleanup waves can extend the `_DELETED_ORPHAN_ARTIFACTS` tuple in this file directly.
- The 3 parallel Explore dispatch pattern (orphan tests / orphan scripts / orphan data) on dead-code surfaces is durable; 2 of 3 lanes returned CLEAN this session (test files + ui_mock fixtures), 1 of 3 surfaced 7 candidates. Repeat this audit pattern every 3-5 sessions to catch new orphans as the codebase evolves.
- Per [[feedback_verify_generated_reports]] this session verified ALL 7 candidates via direct grep before deletion (the agents' verdicts surfaced 9 total candidates; orchestrator verified each independently + grep-verified the `tft/comp_control.py` + `data/tft_set17_meta.json` target absences + confirmed `docs/API_SURFACE_AUDIT.md` + `data/api_surface.csv` + `ROADMAP.md` + `docs/history_notes.md` are dated/narrative artifacts not active consumers).
- The orchestrator pattern is now 46th-streak (items 134-189 + slim variants 190/191/192/193/194/195/196). 1-commit-bearing slice + 2 read-only Explore agents + drift-guard test creation is a valid slim variant for orphan-cleanup work.
- 1.95 GB disk freed in working tree (1.7 GB rewind .bak + 200 KB + 200 KB match .bak + 32 KB scripts + 8 KB runtime); none of this affected `.git` history (the 3 .bak files were .gitignored from inception) so the commit diff shows only -763 lines (the 2 scripts) + 47 lines (new test file).

**Carries forward:**
(a) Item 195 carries (a)-(l) ALL unchanged.
(b) Cherry augment live verification + Game-PC HTTP-pull redeploy STILL OWED at next Arena 1750 window.
(c) 101.qq.com operator-at-Game-PC Chrome DevTools capture STILL OWED.
(d) Live ARAM/SR smoke STILL pending.
(e) Calibrations STILL operator-gated.
(f) Legion 1-PC consolidation STILL operator-gated.
(g) Auto-ops verb expansion + auto-action lanes Phase 3 95% gate STILL DEFERRED - watcher pid 12000 healthy + lanes baked in BUT 0 firings (unchanged from items 192/193/194/195); sample accumulation BLOCKED by zero inbound bridge traffic not by watcher health.
(h) Active Match #11/12/13 real in-game capture STILL OWED.
(i) v2.1 visual captures pages #9/10/11/12/13/14/15/16 OWED at next operator-driven Chrome session.
(j) Rell W form 1 cc_conditional candidate STILL operator-decision-gated.
(k) cc_conditional wave 24+ candidates require either (1) NEW Meraki schema field OR (2) registry shape lift to host passive-AA stack-detonation slot (would unblock Braum P 4-stack stun) - both operator-gated; the 4-field Meraki extraction SATURATED for current-schema candidates per items 192/193/195.
(l) Frozen-file grant NOT used this session.

---

# 2026-05-25 (night 3) - item 195 SHIPPED: 7 orphan dead-script deletions (3 tools/arena_phase3 + 4 scripts) + drift guard `tests/test_orphan_scripts_item195.py` + BACKLOG/ROADMAP stale-sweep wave 27 = 0 flips (11 consecutive zero-flip waves 17-27) + cost/latency wave 33 = 33rd consecutive CLEAN since item 134 (2 subagent measurement-error flags verified false-positive per [[feedback_verify_generated_reports]]) (1 commit pushed origin/main; non-engine; non-frozen; no DS restart; no RC restart - file deletions + new test only)

Operator "continue what is next". 45th-streak orchestrator pattern preserved (items 134-189 + slim variants 190/191/192/193/194/195). 3 parallel Explore agents dispatched concurrent; 1 commit-bearing slice. AskUserQuestion NOT used (operator's continuation prompt + carries clearly enumerated as operator-gated). Pre-flight: 0 open PRs; CI 5/5 green since item 194 (`554ccf9` 1m27s); HEAD = `554ccf9`; mode_key=client + lcu_phase=None + RC pid 12164 alive=True last_reload_ok=True throughout.

**Slice A `50bb0c5` (direct main) chore(housekeeping) item 195 - 7 orphan dead-script deletions + drift guard:** Closes a NEW dead-code surface lane (items 186 + 194 closed dead routes / orphan data / runtime artifacts; item 195 closes orphan one-off repair/fetch/inspect scripts). Subagent surfaced 5 candidates; orchestrator grep-verified per [[feedback_verify_generated_reports]] all 7 had 0 callers across `.py` + `.md` + `.ps1` + `.bat` surfaces + 0 docs references in `docs/` + ROADMAP + BACKLOG + README + BRIEF + CLAUDE + WAKEUP. **Deleted 7 files:** (1) `scripts/extract_cdragon.py` 3824 bytes - one-off CommunityDragon TFT extraction tool from commit `2e7cadb` "audit batch 10 auto-applied"; 0 references; data already integrated. (2) `scripts/fix_comp_control.py` 891 bytes - one-off repair patching `tft/comp_control.py` orphaned `return False`; target file `tft/comp_control.py` DOES NOT EXIST (deleted in a prior cleanup); repair-tool dead. (3) `scripts/syntax_and_stage.py` 656 bytes - pre-commit stager copying TFT files to `ops/staging/`; target dir `ops/staging/` DOES NOT EXIST; tool dead. (4) `scripts/verify_fixes.py` 903 bytes - one-time verification of TFT fixes targeting `tft/tft_overlay.py` (DOES NOT EXIST) + `coaches/tft_coach.py` + `coaches/tft_pbe_coach.py` (last 2 exist but verify-tool is from initial commit and fix-targets are stale); tool dead. (5) `tools/arena_phase3_fetch.py` 3415 bytes - one-off aggregator J fetcher for 109 Arena Phase 3 missing champs; target dir `data/meta_build/refresh_2026-05-02/_phase3_html/` exists + data parsed; fetcher dead. (6) `tools/arena_phase3_inspect.py` 1788 bytes - one-off RSC data inspect tool for parse-strategy discovery; data structure understood + parser shipped; inspect-tool dead. (7) `tools/arena_phase3_parse.py` 15828 bytes - one-off HTML parser; arena data integrated per item 166 hand-curate; parser dead. NEW `tests/test_orphan_scripts_item195.py` ~40 LOC / 1 test class / 1 test pins all 7 deleted file paths as PERMANENTLY removed (mirrors item 186 + 194 dead-endpoint drift guard pattern - cheap defense-in-depth so future maintainer who tries to revive a dead repair script fails CI). Repository total size delta -27305 bytes / -7 files / -570 LOC removed. `py -m pytest tests/test_orphan_scripts_item195.py tests/test_dead_endpoint_cleanup_item186.py -q` = 5 PASS. `py -m ruff check tests/test_orphan_scripts_item195.py` ALL CHECKS PASSED. `py -m pytest tests/phase8_smoke/ -q` = 70/70 PASS (unchanged from item 194 baseline). The 7 deletions are EXCLUSIVELY one-off historical data-pipeline / repair / staging scripts - none touched live runtime / route registration / coach prompt / DS engine / frozen file.

**Slice B no-commit (BACKLOG/ROADMAP stale-sweep wave 27 = 0 flips):** All 5 anchors VERIFIED-LIVE within +/- 3 tolerance per item 193 wave 26 baseline: BACKLOG L92 `core/draft_elo.py:141 cross_pairs()` confirmed at L141; ROADMAP L13 `web/js/panels/dev.js verdict.team_won` confirmed at L361-362 (within tolerance); ROADMAP L23 `tools/gamepc_lcu_agent.py:246` ARAM Mayhem queue 2400 mapping confirmed; ROADMAP L22 `ops/rc_supervisor.py:210/340` CircuitBreaker confirmed at L210 + L350 (within tolerance). **Sweep cycle decay:** wave 17=0 / 18=1 / 19=0 / 20-26=0 / **27=0** = 11 consecutive zero-flip waves = saturation plateau extends.

**Slice C no-commit (cost/latency wave 33 = 33rd consecutive CLEAN since item 134; 2 subagent measurement-error flags verified false-positive):** Subagent reported 2 DRIFTs (L1 prompt-cache 11 vs baseline 13; L2 _CACHE 47 vs baseline 12-16). Per [[feedback_verify_generated_reports]] orchestrator verified each: **(L1 false-positive)** live grep `cache_control` across `*.py` returns 65 occurrences across 24 files (24 production files include 14 production code + 10 tests; baseline 13 across 8 blocks is the production-only narrower scope per item 188 + items 192 + 193 baselines); subagent's "11" was undercount via overly-narrow regex. **(L2 false-positive)** live grep `^_CACHE\s*[:=]` MODULE-level returns 13 files in `dashboard/` (within baseline 12-16 fluctuation per item 188 carry (g) "do NOT pin to a specific number; loose-ledger artifact across waves"); subagent's "47" counted ALL `_CACHE` references including non-module-level. Other 5 levers PASS per subagent: L3 polling 2000ms network tightness confirmed (`setInterval(pollIfStale, 2000)` + `pollLcu`); L4 `_SUPPRESS_LOG_PATHS` 10 entries unchanged; L5 model tier `claude-haiku-4-5-20251001` consistent across 8 active coaches (supervisor-spawn agent registry at `agents/_supervisor_common.py:42-46` is pre-existing supervisor mapping NOT live coach calls per items 192/193 verdict); L6 14 RC-* scheduled tasks (baseline accepted from item 192/193 + session-start rc_facts.py catalog); L7 27=27 panel CSS = dashboard.css @imports parity guard 4/4 PASS. **Verdict: 33rd consecutive CLEAN since item 134; cost/latency lane saturated.**

**Verified:** `py -m pytest tests/test_orphan_scripts_item195.py tests/test_dead_endpoint_cleanup_item186.py tests/phase8_smoke/ -q` = **75 passed**. `py -m ruff check tests/test_orphan_scripts_item195.py` ALL CHECKS PASSED. No DS restart (non-engine; DS :8893 serves 1.59.0 from item 189). No RC restart (no route module edits; pure file deletions + new test only). No frozen-file grant used. 0 worktrees left dangling (Slices B + C used Explore agents = no worktree).

**Don't-redo:**
- The 7 deleted orphan scripts are now CI-blocked from re-introduction by `tests/test_orphan_scripts_item195.py::OrphanScriptAbsenceTests`. Do NOT re-add. If a future patch-refresh / hand-curate / TFT-repair workflow needs similar functionality, name it differently + ensure live consumer exists before shipping.
- `tft/comp_control.py` + `tft/tft_overlay.py` + `ops/staging/` directories ALL DO NOT EXIST. The repair-tool dependency chain (`scripts/fix_comp_control.py` patches `tft/comp_control.py`; `scripts/verify_fixes.py` checks `tft/tft_overlay.py`; `scripts/syntax_and_stage.py` stages to `ops/staging/`) is fully dead.
- `data/meta_build/refresh_2026-05-02/_phase3_html/` directory still exists with parsed HTML; Arena Phase 3 backfill data is INTEGRATED into `data/champion_loadouts.json` (per item 166 hand-curate + item 167 align). DO NOT re-create the fetch/inspect/parse trio; if Arena data needs refresh, use a NEW one-off tool with explicit caller wiring or write directly into the hand-curate pipeline.
- The orphan-script drift guard pattern (`tests/test_orphan_scripts_item195.py`) is now the canonical home for "this one-off script is dead and stays dead" assertions; future dead-tool-cleanup waves can extend the `_DELETED_ORPHAN_FILES` tuple.
- Slice C's 2 measurement-error flags (L1 prompt-cache count + L2 _CACHE module-level count) join the RECURRENT pattern of subagent miscount on these specific levers across items 173/174/175/188/190/192/193/195. Item 188 carry (g) is the durable statement: "Route TTL count fluctuates 12-16 across audits; do NOT pin to a specific number; loose-ledger artifact 12-16-38-41-63 across different scopes". L1 prompt-cache COUNT is also loose-ledger across production-only vs production+tests scopes.
- The orchestrator pattern is now 45th-streak (items 134-189 + slim variants 190/191/192/193/194/195). 1-commit-bearing slice + 2 read-only Explore agents + drift-guard test creation is a valid slim variant for orphan-cleanup work.
- Per [[feedback_verify_generated_reports]] this session verified 2 subagent measurement errors (matching items 173/174/175/188/190/192/193) - the pattern is durable: subagent flags AUDIT-CLAIMS REQUIRE GREP-EVIDENCE per claim before relaying.

**Carries forward:**
(a) Item 194 carries (a)-(l) ALL unchanged.
(b) Cherry augment live verification + Game-PC HTTP-pull redeploy STILL OWED at next Arena 1750 window.
(c) 101.qq.com operator-at-Game-PC Chrome DevTools capture STILL OWED.
(d) Live ARAM/SR smoke STILL pending.
(e) Calibrations STILL operator-gated.
(f) Legion 1-PC consolidation STILL operator-gated.
(g) Auto-ops verb expansion + auto-action lanes Phase 3 95% gate STILL DEFERRED - watcher pid 12000 healthy + lanes baked in BUT 0 firings (unchanged from items 192/193/194); sample accumulation BLOCKED by zero inbound bridge traffic not by watcher health.
(h) Active Match #11/12/13 real in-game capture STILL OWED.
(i) v2.1 visual captures pages #9/10/11/12/13/14/15/16 OWED at next operator-driven Chrome session.
(j) Rell W form 1 cc_conditional candidate STILL operator-decision-gated.
(k) cc_conditional wave 24+ candidates require either (1) NEW Meraki schema field OR (2) registry shape lift to host passive-AA stack-detonation slot (would unblock Braum P 4-stack stun) - both operator-gated; the 4-field Meraki extraction SATURATED for current-schema candidates per items 192/193.
(l) Frozen-file grant NOT used this session.
