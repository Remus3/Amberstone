# Riot Commander - Backlog

_Aspirational / longer-term items. Extracted from ROADMAP.md "Future" section 2026-05-08._
_When an item moves to active work, migrate it to ROADMAP.md "Open items"._
_Shipped items live in git log + docs/LEDGER.md (items 325+; items 1-324 in docs/history_notes.md); this file is for things NOT yet done._

---

## Daemon Slayer scorer calibration

- **(FUTURE) R129 Viego R "Heartbreaker" 120% total-AD primary hit uncredited** _(2026-07-14, surfaced by the R129 DS-sweep champion-spell-scaling hunter; genuine but not blind-shippable - abilities-merge blast radius)._ Viego R carries TWO CDragon mechanical damage blocks (TotalDamage 120% total AD + TotalPercentHealth 12/16/20% missing HP +5%/100AD) but the parsed `champion_abilities.json` snapshot kept only ONE (the missing-HP block). `prefer_cdragon_ratios=True` (item-320) only OVERWRITES existing blocks and never APPENDS surplus CDragon blocks, so the cardinality mismatch (`abilities.py:580-581`) drops the whole form back to Meraki and the 120% AD term is never credited: `compute_ability_dps("Viego", R)` returns raw 0 at FULL HP (should be ~130 from AD alone). Sibling AD-ult class sharing the same CDragon-surplus-block root cause (each needs per-champ validation): Pyke R (80% AD execute), Rengar R (100%), Quinn R (35%), Yorick R (30% ghoul), Jinx Q (110% rocket). REFUTED nearby: Yone W / Teemo E / Shyvana W (reworked-kit / on-hit / stale-CDragon). Fix options: (a) narrow per-(champ,key) damage-block override registry (mirror `_passive_damage_overrides.py`) behind an `apply_*` flag - lowest blast radius; (b) general `_apply_cdragon_ratio_preference` APPEND-unmatched-clean-blocks seam - fixes the whole sibling class but Tier-2 full-suite + per-champ golden diffs. NOT blind-shippable (moves live DPS/burst on real champs). Source: R129 hunter, LEDGER 903.
- **(FUTURE) R129 bruiser/hybrid scorer ability-DPS XOR for AD-axis champs** _(2026-07-14, surfaced by the R129 DS-sweep scorer-refinement hunter; needs a validated per-champ table, not blind-shippable)._ `hybrid.py` `compute_hybrid` / `rank_items_by_hybrid` scores the damage term as strict XOR (auto `weighted_dps` OR ability `total_ability_dps`, never the sum): AP-axis champs score on ability DPS, but AD-axis champs score on AUTO DPS only and their ability DPS (`ability_dps.py:1270`, non-zero for Riven/Camille/Jarvan) is never summed (`hybrid.py:462-469` / `938-945` / `1026-1033`). High-alpha AD bruisers (Riven 0.70 / Camille 0.65 / Renekton / Darius / Jarvan) thus over-value crit/AS and under-value AH/bonus-AD (Shojin/Eclipse/Black Cleaver). Safe fix = a `blend_ability_axis` bool + an empty per-champ opt-in table (default byte-identical), restricted to pure-cast kits; a BLANKET AD-bruiser sum is REFUTED-unsafe (double-counts auto-empower abilities Nasus Q / Renekton W / Camille Q / Sett Q / Vi E that `compute_dps` already counts as an auto). NOT a kit-axis WIN-flip (does not collide with the DSP11 gate) - a pure-sim damage-model completion. Source: R129 hunter, LEDGER 903.
- **(FUTURE) OQ24 build-coherence residuals** _(2026-07-13, moved from ORCHESTRATION_PLAN OQ24 at cycle-11; OQ24 primary ER/Eclipse-on-wrong-kits target CLOSED via Step1a/1b, LEDGER 874/885)._ Four residual tails (tail 1 CLOSED cycle-12 as premise-refuted, see below), none blind-shippable: (1) Stormrazor-3097 valuation dock - CLOSED / NOT-WARRANTED (cycle-12, 2026-07-14, LEDGER 888; over-surface premise refuted - both 3097 recipients Aphelios/Zeri are coherent Energized marksmen; full verdict relocated to docs/history_notes.md). (2) **precompute build_order backfill** (owed since Step 1a) - regenerate the display-keyed static build_order tables to reflect the core-side is_caster_marksman coherence change; regen tool = tools/daemon_slayer_build_orders_generate.py, but OPEN design Q FIRST: does the generator POST raw to :8893 (bypassing the core coherence dock)? if so it needs an engine variant / re-route or the backfill is a no-op (build-coherence spec:140). NOT tools/replay_build_order_validate.py (that is a match-replay validator, no table output, no --static flag). (3) **Step-3 ally-synergy + NL "why X over Y" reasoning** - the deferred build-coherence-refactor STEP 3. (4) **Zeri Lich Bane / Liandry's AP-on-AD-marksman distinct class** - a separate AP-scaling-on-an-AD-marksman valuation class beyond the ER/Eclipse scope.

- **RESOLVED 2026-07-09 (LEDGER 826, ENGINE 1.185.0):** all 3 reds from LEDGER 823/824 closed (bruiser scorer axis-awareness SHIPPED; Kalista-IE test relaxed, on-hit ratified; beam boots_unique=False test retired). Full narrative relocated to docs/history_notes.md.
- **NEW enhancements (surfaced by the operator during LEDGER 826):**
  - **Situational / alternative builds (crit-vs-on-hit).** The engine emits ONE DPS-max build per champ; the operator notes crit Kalista is good in some matchups. A real enhancement = the engine offering >1 build keyed to matchup (vs-squishy -> crit, vs-tanky -> on-hit/pen), not one static order. Larger DS feature (not a test/scorer tweak); would also cover Caitlyn/MF/Tristana (same on-hit shape).
  - **Boot-selection utility-awareness - SHIPPED DEFAULT-OFF 2026-07-09 (LEDGER 827, ENGINE 1.186.0; `agents/daemon_slayer/boot_utility.py`).** Remaining FOLLOW-UPS: the default-ON flip (live-game gated); real per-champion enemy CC into `_select_boots` (v1 uses an AP-share proxy); MS boots need a kite/poke signal to win. Do NOT re-flag as unfixed. Full narrative relocated to docs/history_notes.md.

- **DS registry residuals - R66 adversarial refute pass: ALL 7 CLOSED** (gaps 1-6 SHIPPED as R66-R70, ENGINE 1.173.0-1.177.0, LEDGER 753-757; gap 7 shield-lerp re-verified at the 16.14.1 ingest - pin holds, LEDGER 905). Full block relocated to docs/history_notes.md. Open residual kept from R67: Terminus Light-side caster resists (6-8 armor+MR per stack x3, level pp 1;11;14) still unmodeled - needs an item-keyed conditional resist-grant path into the EHP scorers (`_passive_resist_overrides.py` is champion-keyed only; same schema-lift family as the R68 item-reflect registry).

- **DON'T-REDO anchor (R73/R74 escalation, 2026-07-03): Kraken 6672 + Arena 226672 modeled default-ON Phase 4; 6662 = Iceborn; conditional target-state arc CLOSED s232.** Kraken Slayer "Bring It Down" is ALREADY modeled default-ON (`agents/daemon_slayer/_effects_data.py` PeriodicProc every_n_attacks=3, 150 L1 -> 200 L11+ ramp; Arena mirror 226672; guard test `test_engine_math_correctness_pipeline_c.py`). Item 6662 is Iceborn Gauntlet, NOT Kraken. Target missing-HP / target-state conditional amps = operator-CLOSED s232 - never re-pitch. Directors: any "unmodeled proc" pick MUST be grep-verified against `_effects_data.py` first (stale-digest re-pick family R57/R62/R73). Also modeled already: Statikk Shiv 3087/223087, Rapid Firecannon 3094/223094, Dead Man's Plate 3742/223742.

- **DS cross-eval Tier-2 nominations** (from `ops/audit/ds_cross_eval/SYSTEMIC_FINDINGS.md`, 2026-06-16; report-first gate locked, each gated on per-champion rewind-WIN validation FIRST, each = ENGINE bump + dual suite + Share mirror). (A) ARAM archetype-override table - the kit-default archetype disagrees with the ARAM win-axis for a cluster (Malphite tank->AP, Shaco assassin->AP, Kayle/KogMaw mage->on-hit-carry, Bard/Morgana/Seraphine/Taric/Thresh/Zilean hps->mage/tank); the `data/cs_archetype_picks.json` override mechanism exists, needs the operator/Gemini off-meta-chase decision (NOT auto-flip). (B) champion-kit-aware DPS crediting - the AD scorers (dps/burst/hybrid) emit a near-identical BotRK/Runaan's/Kraken template for every AD champ regardless of kit, empirically losing 25-44% wr for several (Ezreal/Xayah/Nilah); B1 = melee-applicability gate (Runaan's bolts / crit-AS mis-credited on melee autos; widest reach: 16 hybrid MISMATCH). efficiency-sort REFUTED as the fix (buries staples further). (F2) gold-aware top - default sort_by="delta" surfaces the most expensive item (Void Immolation 6000g) at rank 1; a cost-aware top is a minor calibration. (Cluster C Aphelios dps zero-output was FIXED 2026-06-16, ENGINE 1.128.0.)

- **G6 cost-model / cost-ignoring optimum (DSP9-deferred, FUTURE).** lolmath ships an IGNORE-ITEM-COST toggle + a cost-ignoring ULTIMATE global-optimum build that differs from its gold-aware normal build for many champs; DS has no cost model (fixed item order, neither gold-aware nor a cost-ignoring optimum). DSP9 (item 474, `g7_comp_harness.py`) proved this - NOT comp-awareness or runes - is the residual driver of the lolmath-vs-DS gap (lolmath-ULTIMATE tracks DS's pricier-item variants regardless of enemy comp). Gemini-consult 2026-06-17 verdict (c) leave FUTURE/BACKLOG: a raw gold cost model CONFLICTS with DS's empirical-WIN-data anchoring + violates the do-not-blind-build rule. Do NOT build blind; if ever revisited, anchor on Meraki gold-per-stat + rewind-WIN spike-timing, never aggregator D. Distinct from (F2) (a sort-surface tweak); this is a whole cost-aware build MODE.

- **Aggregator N F2 - per-slot item win-rate ladder (FUTURE, 2026-06-22, `docs/COMPETITOR_LIFT_2026-06-22_AGGREGATOR_N.md`).** Aggregator N shows each build-slot candidate with its empirical win-rate + sample. RC ranks the build surface by DS delta-DPS/EHP math, not empirical per-slot win-rate. A ladder could aggregate the local `rewind_history.db` (~2846 matches) per (champ x slot x item), but the solo-player per-cell sample is THIN (<5 games most cells) so it needs `core/smoothed_rates.py` Laplace shrink + a confidence gate; MED-HIGH effort, LOW confidence -> defer until a richer corpus exists. The Aggregator N F1 ARAM per-champion balance grid already SHIPPED (item 588, commit `e0f0ffac`); F3 win-delta/sample shrink + F5 augment pick-rate are already-have; F4 statistical tier list = forbidden external winrate source; F7 = new TFT domain.

- **Aggregator S F1/F2/F7 - pro-build + pick-rate corpus (FUTURE, 2026-07-01, `docs/COMPETITOR_LIFT_2026-07-01_AGGREGATOR_S.md`).** The Aggregator S deep-dive's three FUTURE findings all need a NEW external corpus RC deliberately does not carry, so each is a blind-in-run-dependency reject (do NOT build without operator sign-off + a real data source): F1 per-match PRO probuild rows (a curated pro-account Match-V5 ETL keyed by champion - item buy order + runes + summoners + KDA/CS per pro game); F2 the per-champion AGGREGATED "most common / highest-winrate" pro build+runes rolled up from that corpus; F7 ladder PICK-RATE trend deltas (aggregate patch-over-patch telemetry). RC is simulation-first (`reference_ds_simulation_default_not_winrate_hybrid`) - these are a pro/ladder-DATA epistemology, not a compute lift; the nearest existing seam is the already-live external-aggregator lane (`core/synergy_external_source.py`) but that is duo-synergy, not pro builds. The one NOW finding (F4 skill max-order card) already SHIPPED in-run (R54, merge `7f7b82cb`); F3 runes / F5 counters / F6 buy-order are CLOSED (RC already owns them, stronger). Free-rotation (part of F7) is a cheap DDragon call but low-value for a solo tool -> defer.

- **Item Shaper full re-rank engine seam (FUTURE, 2026-07-02, from OQ14).** OQ14 shipped the Interactive Item Shaper strip as an HONEST emphasis PREVIEW only (GET /api/ds-shape -> core/shaper.apply_shaper over the real archetype_weights.json [alpha,beta] baseline -> base%->shaped% per axis; item 735, `639e2789`). It does NOT yet re-rank the actual item list from the knobs, because `agents/daemon_slayer/rank.py` `rank_items` takes NO weight-dict input (pure DPS-delta ranker) and the archetype blend is a 2-axis [alpha,beta] per champ, not a 3-axis {damage,survivability,utility} dict. A real "nudge -> re-ranked build" needs (a) a defined 3-axis weight surface the shaper nudges, mapped onto the scorer/hybrid weighting, and (b) threading the nudged weights through the rank path - an ENGINE SEAM (schema + ranker change), NOT a UI slice. Do NOT build blind: a wrong re-rank is worse than the current honest preview. MED-HIGH effort; gate on an operator decision about which weight surface the axes drive.

## Coaching depth

- **(FUTURE) Per-enemy ult power-spike readout (R125 Aggregator C lift, 2026-07-14, `docs/research/COMPETITOR_LIFT_AGGREGATOR_C.md`).** Aggregator C' desktop overlay flags when an ENEMY crosses a level spike (6/11/16 -> R / R2 / R3 unlocked). RC has the exact edge-trigger mechanism SELF-only (`web/js/panels/spike_cue.js:45` `crossedSpike` reads only the active player's `lc.level` via normSpikeLevel; `core/event_callouts.py:355` `_level_spike_callouts`; self level pill `web/js/main.js:1048-1052`). Blocker: per-enemy champion level is NOT in the Live Client envelope today - `dashboard/_liveclient.py:101` sets `out["level"]` from the ACTIVE player only, and the emitted `out["players"]` list (`_liveclient.py:177-186`) carries ONLY position/team/creep_score/is_active (verifier-confirmed R125). A per-enemy spike cue therefore needs a one-field backend extract (`"level": p.get("level")` into the players builder) + a new stateful panel (`web/js/panels/enemy_spike_cue.js`) = NOT presentation-only, so NOT headless-blind-shippable. MED effort, no new Riot/Claude dep, no ENGINE bump. Do-not-build-blind: verify against a live enemy roster first. Distinct from + more feasible than the R100 F1 manual enemy-CD tracker (this is a deterministic level-unlock edge, no manual input). The PC-overlay / live-companion / stat-site competitor family is DRAINED 4x (R100/R112/R120/R125) - do NOT re-open a Aggregator C/Overlay App F/Overlay App E overlay teardown; rotate to the Haiku-to-ZERO Lane E CV substrate instead (status + next steps live in the ROADMAP.md Lane-E row; R126 atlas/fusion detail relocated to docs/history_notes.md).

- **EHP enemy-CC model for `aram_tenacity_mult` - SHIPPED 2026-05-21/24** (items 126-177 arc, ENGINE 1.26.0 -> 1.56.0): `cc_blended_ehp` COMPLETE across 4 surfaces + `cc_conditional` COMPLETE across 5 surfaces; 13 condition tags; registries at 108 entries / 89 champs (per-spell CC) + 72 / 57 (cc_conditional, waves 1-23). Conditional-target-state arc operator-CLOSED s232 (CLAUDE.md Settled) - future wave candidates stay operator-gated. Open: live-game validation only. Full wave-by-wave narrative relocated to docs/history_notes.md.
- **DS sidecar bucket wiring (item 225 carry)** - 5 of 6 WIRED (item 231 + 232 + 233), all OPT-IN/additive/byte-identical at default: `ammo` charge model into `mana_sim` `gate_ammo` (item 231); `geometry` (619) -> NEW `geometry.py` AoE-multiplier into burst `aoe_targets_hit` (item 232); `recharge` (20) -> NEW `recharge_ledger.py` time-step charge-availability into fight_report 6th section (item 232); `mode_modifiers` -> dps/ehp `apply_mode_modifiers` dmg mults urf/ofa/usb/nb + engine ar/swift growth-addends, Arena re-ranks only flag-on (item 232); `missile` (416) -> NEW `missile.py` spell_travel_time (distance/missile_speed; speed-band + cone_distance=100/0 sentinel + <200 cast_radius-floor gating; consumed by fight_report 7th MISSILE section, APPROXIMATE default-distance-dominated) (item 233). ALL 6 BUCKETS NOW RESOLVED. Item 234 added the mana_sim opt-in `apply_ability_haste` CDR model (reuses `_item_ability_haste.total_item_ability_haste` + Riot `base_cd/(1+AH/100)`; byte-identical off / no-AH; binds on cooldown-repeating sequences). `static-CD` (55) is a HONEST no-consumer (item 234 verdict): the `ability_static_cd` accessor stays reachable but the wiki static QWER coverage is 3 abilities (Amumu Q / Heimerdinger R / Samira R) with a clear mislabel (Amumu Bandage Toss scales with haste) - too unreliable to gate haste-immunity; the haste model applies uniformly instead. `cc_tags` (186) is BLOCKED - it would feed cc_conditional which is operator-CLOSED (s232). FLIP defaults-on after LIVE-GAME validation (each re-ranks, operator-gated, NOT headless): `gate_ammo` / `apply_mode_modifiers` / `aoe_targets_hit` / `apply_ability_haste`. The clean headless DS sidecar/rune/haste lane is now EXHAUSTED - remaining DS work is operator-gated (live-validation flips + assassin-keystone sign-off) or data-blocked (per-spell cast-RANGE source for the missile combo-clock delay).
- **rewind_history.db SR records with game_id**: post-live SR game; wired in code (d66d14b) but blocked on new records.
- **LCU deeper integration**: Pengu Loader Discord/GitHub research for endpoints RC doesn't use yet (richer pre/post-game data). Research-first, no code scheduled. **Reference catalog identified 2026-05-17:** `KebsCS/lcu-and-riotclient-api` (lcu.kebs.dev) is a current (client 26.05), exhaustive (2839 ops) endpoint map - the authoritative source for the richer endpoints (mastery-by-puuid, `match-history .../games/{gameId}` + `game-timelines`, `loadouts/v4`, `signed-ranked-stats`). No LICENSE -> reference only, don't vendor; optionally diff its `data.json` per patch. This supersedes the Discord crawl as the primary endpoint-discovery method.
- **DS competitor-lift gate slate - ALL 7 lifts SHIPPED 2026-05-30 (items 218-220)** (cooldown-watch card, armor-swept DPS graph, relscore bar, spike markers, engine-knobs panel + fight-length reweight, statcheck sandbox, combo simulator). Owed: live visual captures (render-gated; tracked in docs/LIVE_GAME_GATED_SYNC.md). STALE-PATH: `web/js/panels/cooldown_watch.js` no longer exists on disk (probe 2026-07-17; the `/api/cooldown-watch` route `dashboard/routes_cooldown_watch.py` remains). Full spec narrative relocated to docs/history_notes.md.
- **DS lolmath-wiki sidecar extractor** _(2026-05-30, item 217/221/224; feasibility GO-conditional in `docs/_archive/LOLMATH_WIKI_SOURCE_2026-05-30.md`; TOOL + DATA + combo wire SHIPPED item 224 `c3740d0`)._ The LoL wiki carries STRUCTURED scalar fields Meraki lacks: AA-timing (windup/missile speed), static-cooldown flags, charge/ammo model, NON-ARAM mode multipliers. Thin sidecar JSON merged by champ name; Meraki stays authoritative for ratios/CC. **ACCESS RESOLVED (item 224):** `leagueoflegends.wiki.gg` edge-blocks Legion host-wide (HTTP 401; WebFetch egress too); the Riot alias `wiki.leagueoflegends.com` (same wiki.gg backend) IS reachable. Extractor uses `action=raw` on `Module:ChampionData/data` (ONE request, brace-scan keyed by apiname == DDragon id) + CDragon backfill (`raw.communitydragon.org` char bins, block-immune). **DATA SHIPPED:** `data/daemon_slayer/16.11.1/wiki_stats.json` - 171 champs, cast 171/171 (49 wiki + 12 cdragon + 110 engine-default 0.25), missile_speed 82/171, 0 errors; combo.py consumes per-champ AA windup. **DATA CEILING:** only 61/171 champs have a REAL AA cast time in ANY source (both wiki + CDragon store it only on engine-default override); the 110 default-filled are byte-identical to no-sidecar - do NOT re-hunt them. Re-run `"C:\Users\Administrator\AppData\Local\Programs\Python\Python314\python.exe" tools/daemon_slayer_wiki_stats_extract.py --patch <patch>` on a patch bump (see memory reference_lol_wiki_access). REMAINING (operator-gated, NOT built): `action=bucket` non-ARAM mode-mults (Arena/URF/NB) + static-cooldown flags + charge model - additional scalar lanes, separate sidecar fields.
- **DS target-current-HP% scenario lever for %current-HP item procs** _(2026-06-05, from a lolmath Discord digest; OPERATOR-GATED tuning, NOT a bug; SEAM SHIPPED 2026-06-09 item 374 - `target_current_hp_pct: float = 1.0` on CallContext threaded through compute_dps, scales ONLY BotRK 3153 / Hellfire 4017 / Fulmination 443055, byte-identical at default 1.0 so NO ENGINE bump + the %MAX-HP procs untouched; the FLIP to a live fight-average current-HP value, lolmath's 50%, remains the gated product decision that re-ranks + owes the bump)._ lolmath defaults both sides to 50% HP as a "scenario average"; a user noted this materially shifts %HP-item value (BotRK weighting on Caitlyn flips between 50% and 100% HP). RC bakes the OPPOSITE default: the three genuinely-%CURRENT-HP item procs - BotRK 3153 Mist's Edge (`0.09*target_max_hp`, `_effects_data.py:546`), Hellfire Hatchet 4017 Char (current-HP base, `:2055`), Fulmination 443055 Dynamo (`0.13*target_max_hp`, `:2312`) - are modeled at 100% current HP (each comment self-documents the `current~=max` steady-state approximation + "explicit current_hp_pct field is a future batch"). `CallContext` (`_effects_types.py:80`) carries `target_max_hp` but NO `target_current_hp_pct`, so the `target_current_hp_pct` knob that `ability_dps`/`burst` already honor (default 1.0) never reaches the item-proc path - it is half-wired. The live coach passes a REAL `target_max_hp` (`coach_integration/archetype_dispatch.py:222`), so these procs DO fire at full max-HP in live carry/bruiser ranking (over-credit vs a mid-fight average; the /dps + /rank routes default `target_max_hp=0.0` which zeroes them, so the bias only bites when a real HP is supplied). BUILD (byte-identical at default 1.0): add `target_current_hp_pct: float = 1.0` to `CallContext` (END, after `ult_casts_per_sec`) + thread it through a new `compute_dps(target_current_hp_pct=1.0)` param into the single `CallContext(` construction at `dps.py:744` + multiply ONLY the three current-HP procs above by `c.target_current_hp_pct`. **DO NOT touch the genuine %MAX-HP procs** - Eclipse 6692 (`:590`), Titanic Hydra 3748, Hullbreaker, Azakana's Gaze, Reaper's Toll 443090 - they are correctly max-HP and a blind sweep would corrupt them (an investigating subagent made exactly this Eclipse mistake). The FLIP (choosing a live fight-average current-HP value, lolmath's 50%) is the actual product decision + would re-rank; ENGINE bump owed on build. **Digest provenance:** the same lolmath chat flagged two other behaviors, both checked against RC and found STRUCTURALLY ABSENT - do NOT re-investigate: (1) "marksman preset on a no-autos mage" (Fiddlesticks -> pure-AP degeneration) cannot occur - RC routes mages to `ds.ability` via the DDragon-tag archetype dispatcher (`core/archetype_picks.py`), and the carry/dps DPS=0 path is unreachable in prod; (2) "1 R bundles 2 empowered autos" double-count cannot occur - `burst.py:803` sums ability-blocks + AA separately, the AA-empower + on-hit seams are default-off / AA-only / on disjoint champ sets (Udyr in neither).
- **item-WPA Build Insights - the WPA lane is COMPLETE (items/skills/runes/spells; items 273/275/374/389) - do not re-open without a new product call (e.g. a spell-PAIR lens).** Owed: Legion-local visual capture. Personal-corpus DESCRIPTIVE lens (noisy, min_n+shrink load-bearing) - NOT a meta winrate. Full narrative relocated to docs/history_notes.md.
- **deterministic champ-select brief FLIP (Haiku-elim)** _(2026-06-02, item 273; SUBSTRATE SHIPPED `dashboard/_champ_select_deterministic.py`)._ The deterministic champ-select brief (build+runes from the curated loadout_resolver + an archetype ally_notes heuristic) is built + shadow-logged side-by-side with the live Haiku brief to `logs/champ_select_brief_shadow.jsonl` (build_match/keystone_match per row). The served brief STAYS the live Haiku call (AST drift-guard pins no-flip). To RETIRE the Haiku call: review the shadow-log over a few real champ-selects, confirm the deterministic brief is acceptable, then flip `dashboard/_champ_select.py brief_via_coach` to return `brief_deterministic(...)` (drop the Anthropic call) - operator-gated validation, do NOT flip blind.
- **Arena deterministic choices A/B instrumentation (Haiku-elim, mirror ARAM slice-1)** _(2026-07-06, headless finding)._ `core/arena_deterministic_coach.build_block` emits the seven Arena fields but NO `choices` A/B pair - unlike ARAM's `core/aram_deterministic_coach._safe_choices` (slice 1 `f5939253`), which maps all 5 canonical labels to source_tag-tagged chips and made ARAM choices shadow-measurable. Arena's 5 labels are BUY ITEMS / KITE BACK / ALL IN / PLAY AGGRO / FIGHT SMART (`core/arena_action_rule.decide_action`). Adding a `_safe_choices` mirror would flow into the shadow log automatically (the writer stores the whole block dict), starting choices accrual for the eventual Lane-C flip. DEFERRED because (a) it is a symmetric keyset change - `test_arena_deterministic_coach.py` `set(block)==set(_KEYS)`, `test_arena_coach_shadow.py` + `test_arena_coach_shadow_wire.py` assert BOTH `deterministic` AND `live_haiku` block keys match `_KEYS`/`BLOCK_KEYS`, so the live Arena Haiku block must carry choices symmetrically or the wire test breaks (the arena coach prompt already native-emits `choices`, needs a wiring pass to confirm the served block carries the key); (b) it CANNOT be shadow-validated headless - arena shadow is `awaiting_accrual` (0/20 live-fight rounds), so correctness needs live Arena games. Build shadow-only (no flip; flip stays operator+live-gated). Related: the ARAM choices lever is already code-complete (headless 2026-07-06 finding: the report's 36% was a stale-log artifact, fixed to segment instrumented-vs-raw in `ae579ef0`; among instrumented rows coverage is 91%).
- [SHIPPED] **antiheal/grievous deterministic callout - SHIPPED 2026-06-03 (item 285; `core/heal_threat.py`).** Owed: live in-game visual capture. Full narrative relocated to docs/history_notes.md.
- [CLOSED-not-viable] **cost: prompt-restructure for cacheable static prefixes** - Haiku's 2048-token cache floor makes a `cache_control` marker inert on the small prompts (item 286); guard `tests/test_prompt_cache_floor_item286.py` pins it. Do NOT re-attempt. Full narrative relocated to docs/history_notes.md.
- **HZ replay-narrative deterministic substrate - SUBSTRATE + SHADOW SHIPPED 2026-06-19 (item 506:** `core/precomputed_replay_narrative.py` + `core/replay_narrative_shadow.py`). The served `replay_coach` path is untouched; the FLIP stays an operator call gated on the dormant replay-coach UI ever being wired. Full narrative relocated to docs/history_notes.md.
- **HZ-A laning choice-B is always economy/recall** _(2026-06-16, item 442 scout; FUTURE - coaching content/design)._ Every committed laning cell's `economy.recall` is `recall_now`/`back_soon` (the generator `_recall_verdict` only emits `hold` at L16 / `next_spike==complete`, both beyond the committed L2/L6/L11 bands; live distribution 137600 back_soon + 217408 recall_now, ZERO hold), so the HZ-C1 reader's combat-alt B branch (`core/precomputed_laning_coach.py:195-202`, labels "Hold - poke only" / "Force a short trade" / "Hold position") is production-unreachable and choice B is ALWAYS a recall directive. `tests/test_precomputed_laning_coach.py::test_low_mana_routes_to_low_cell` pins a synthetic `hold` cell the generator never emits. Decide what B should be (always-recall is a monotonous A/B surface) - a coaching-content call, not a mechanical bug.
- **R2 competitor-lift FUTURE gaps** _(2026-06-18, `docs/COMPETITOR_LIFT_2026-06-18_R2.md`; aggregator B / aggregator A / Aggregator Z1 / aggregator G deep-dive)._ RC supersedes/has nearly the whole surface; the one NOW gap (Aggregator Z1 per-minute performance curve) SHIPPED in-run as the Build Insights "Game Flow" tab (`core/perf_curve.py` + `/api/perf-curve` + `web/js/panels/perf_curve.js`, item 494). Residual FUTURE: (1) **aggregator B carry-efficiency grade axes** - gold_share + kp/gold-share folded into `core/post_game_rubric.py` (verified gold_share 0 hits repo-wide = a real gap, BUT folding into the grade is a Tier-2 re-baseline + a product/calibration call, NOT auto-flip; the LOW-risk alternative = surface them as DISPLAY-only stats with NO grade change). _DISPLAY-only gold_share SHIPPED 2026-06-19 (item 505, `a6c4d384`): `core/carry_share.gold_share_pct` on the PGR hero stats grid (fills the empty spacer cell), DISPLAY-only with NO grade fold - the grade re-baseline stays the gated Tier-2 product call. kp_pct already existed. _OPTIONAL grade-axis fold SHIPPED DEFAULT-OFF 2026-06-19 (item 506): `compute_role_grade(carry_efficiency=False)` folds gold_share + KP additively, param appended last, default grades byte-identical; the default-ON grade re-baseline stays the gated Tier-2 product call._ (2) **aggregator A OP-Score per-INTERVAL performance curve** - _SHIPPED 2026-06-19 (item 506): `core/op_score_curve.py` (transparent gold/xp/cs/combat composite in [0,100], win/loss split, own rewind corpus) + `/api/op-score-curve` + build_insights OP Score tab; DISPLAY-only, no Riot/Claude dep._ (3) **DPM damage-share / DPM per-minute curve** - blocked on the cumulative-all-units `total_dmg` timeline field; RC stores no per-frame to-champions damage, so a damage curve would mislead (needs a new frame field / extractor). (4) **aggregator G lane-vs-full +/- WPA totals** - _SHIPPED 2026-06-18 (307273f8): `_wpa_totals` folds a `totals` block into `/api/post-game-wpa` (lane-phase <=900s vs full net/abs swing + lane_share); +6 tests, live-verified._ CLOSED (verified RC already has, do NOT re-pitch): the aggregator G win-prob "match flow" curve (`web/js/panels/pgr_winprob.js`, PGR S3 - the agent's HAVE miss, caught at the verify gate); global tier-list / pick-ban / cross-player MMR / per-game AI-grade / MVP-ACE / AI win-prediction / AI-chatbot (single-player by design / ML win-pred CLOSED / an LLM chatbot runs counter to the Haiku-to-ZERO charter).
- **R10 competitor-lift FUTURE gaps** _(2026-06-21, `docs/COMPETITOR_LIFT_2026-06-21.md`; Aggregator B single-target deep-dive)._ The NOW finding (F8 per-champion benchmark stat-breakdown) SHIPPED in-run as the Build Insights "Benchmarks" tab (`dashboard/routes_champ_benchmarks.py` + `web/js/panels/champ_benchmarks.js`, surfacing `core/benchmarks` / `champion_benchmarks.json` - computed but never rendered). Residual FUTURE: (1) **F1 lane-counter vs win-rate counter split** - Aggregator B ranks counters TWO ways, by full-game WR and by GD@15 (lane-phase), so "stomps lane but throws the game" is visible. RC has only a 1v1 DS sim verdict (`agents/daemon_slayer/matchup.py` -> `/api/ds-matchup`), no winrate/GD@15-ranked counter LIST; `rewind_history.db` has the per-game timeline but no matchup aggregation. HIGH value, but needs a NEW personal-corpus aggregator over the gitignored DB (mirror `core/duration_winrate.py`, needs fixtures) + route + tab -> not built blind. (2) **F6 ARAM Modifications balance block** - render the per-champ ARAM damage-dealt/taken deltas DS already ingests from Meraki `aram_modifiers` as a labeled signed-% block on an ARAM surface; MED, small new render + per-patch upkeep. _SHIPPED 2026-06-21 (item 578, `b541c923`) as a deterministic coach-prompt CONTEXT LINE (not a separate UI block): NEW `core/aram_balance_context.aram_balance_line` (signed whole-% dealt/taken clauses) wired into the ARAM coach user template beside the shipped tenacity line, completing the `aram_tenacity_context` modifier-context module. Self-only (enemy line a deliberate non-goal - 126/172 non-neutral would render every game); +82 tests; no ENGINE / DS / Share / overlay / flip change._ (3) **F7 duo synergy-delta presentation** - the `/api/duo-synergy` backend is live (`core/synergy_external_source` + `smoothed_rates_101qq`) but the champ-select panel was deliberately removed (item 213, "disconnected from the actual draft"); re-surfacing the synergy-delta-vs-solo number needs a NON-champ-select home = a placement product call, do NOT re-add to champ-select. CLOSED (do not re-pitch): global-meta tier list (deliberate no-redistributable-meta stance, ADR-006); rate-with-sample discipline (already practiced, `duration_winrate.js:56`); highest-WR-vs-popular (RC recs are DPS-causal); live overlay auto-import (RC has its own overlay + RuneWriter).
- **R117 competitor-lift FUTURE gaps** _(2026-07-13, `docs/COMPETITOR_LIFT_2026-07-13_TargetC.md`; Target C desktop-companion + build-path/tier-list aggregator deep-dive, brand withheld per pre-release name-scrub)._ NOW finding F1 (team item-value differential) SHIPPED in-run as a client-side lens over `allPlayers[].items` x `ITEM_COSTS`, lighting the previously-dead `web/js/panels/map_state.js` gold-diff bar (relabeled "item value"; `web/js/lib/item_value.js` + node + contract tests). Residual FUTURE: (1) **F2 tier-list WR-delta trend** - a patch-over-patch win-rate delta arrow; RC has current WRs (`core/smoothed_rates*.py`) but retains NO prior-patch WR snapshot to diff, so this needs a new patch-keyed WR snapshot store + a delta field on `dashboard/routes_champions.py`. MED, not presentation-only (cross-patch retention), no new external dep. (2) **F3 ARAM health-relic respawn timer** - a fixed-cadence countdown over `liveclient.game_time_s` mirroring `objective_gauges.js`; LOW-MED, clock-only (no dep) but the Live Client emits no relic-pickup event so it can only show the fixed cadence, not consumed/next (lower fidelity). CLOSED (do not re-pitch): jungle-pathing overlay (no Live Client camp events, off RC's ARAM/Arena axis) + pro-build import (new pro-match data dependency, counter to RC's pure-simulation build philosophy); all other Target C features (benchmarking / ult+summ timers / minimap objective timers / skill order / augments / picks+bans / post-match / profile+history / loading-screen scouting / trinket reminder / auto-import runes+builds / tier lists) already shipped in RC (grep-cited in the lift doc).

## Platform / observability

- **Local / free-tier LLM to replace the paid Gemini advisor** _(operator-raised 2026-07-01; setup = one QA session with Claude, then autonomous)._ GROUND TRUTH (verified 2026-07-01): Gemini is used in THREE roles, not one. (1) `tools/gemini_ask.ps1` - Claude asks a mid-session research/context Q; Claude sends ONLY the question + a short preamble, Gemini AGENTICALLY reads repo files itself (`--approval-mode plan`) bounded by `.geminiignore` (excludes Share/, _archive/, node_modules, .git, *.db, logs/, ops/runtime/, secrets); default model already `gemini-2.5-flash`. (2) `tools/gemini_audit.ps1` - nightly 03:00 RC-GeminiAudit; context is DETERMINISTICALLY assembled (commits + `git diff --stat` + ROADMAP head 120 + BACKLOG head 80 + full diff HARD-CAPPED 60k chars ~17K tok); default flash, fallback flash. (3) `ops/loop/loop_controller.py` director (`/gemini-headless-upgrade`) - `build_director_context` = ORCHESTRATION_PLAN (capped) + 25 commits + LEDGER newest 60 lines (40k-byte cap) + directive chain + ROADMAP head 120; ~30-40K tok (hard-budgeted after a 572KB starvation incident); THIS is the ONLY role on premium `gemini-3-pro-preview`. So: context is CURATED, never a full-repo dump - fits any local/free model's window; and the $ pressure is essentially the director role only (ask+audit are already ~free). PLAN: add a thin `tools/llm_advisor.py` (OpenAI-compatible) behind `RC_LLM_BACKEND=local|groq|gemini`, move ask+audit to a free tier FIRST (Groq Llama-3.3-70B / Cerebras / already-free AI-Studio flash - zero judgment risk, immediate $ drop), keep director on pro OR trial DeepSeek-R1 / a free-model ENSEMBLE -> Claude-arbiter (RC already assumes verify-before-trust, so consensus on the advisory roles is a clean fit), QA once against real director+auditor transcripts before cutover, keep Gemini as the fallback slot the scripts already have. Local option = Ollama on Legion (out-of-game only, GPU shared with League/OBS/Vanguard; Qwen3-32B / Qwen3-30B-A3B MoE / GPT-OSS-20B fit ~16-20GB; Llama-3.3-70B 40GB+ likely too big). The agentic self-read role (ask) needs a tool-calling agent runner locally (aider / small Python loop), not just a raw chat API. Honest caveat: a 20-32B or free-tier model is weaker than gemini-3-pro at director-grade judgment - ensemble narrows but does not fully close it; keep Claude's verification gate hard on the director path.
- **Streaming vision**: delta-encoded frames instead of full JPEG every 2s. ~5x bandwidth reduction.
- **Mode-specific overlay settings (SR vs ARAM) + mode-specific out-of-game stats overlay** _(operator-requested 2026-06-30, queue for planning + implementation)._ The overlay widget field (positions / visibility / opacity / scale) is currently ONE global layout (`web/js/lib/overlay_layout.js` `_layout` keyed by widget id, persisted to localStorage `LS_KEY` + the rc-shell on-disk mirror via `window.rcShell.getWidgetLayout`/`setWidgetLayout`). Operator wants the layout to be PER-MODE so SR and ARAM (and Arena) each remember their own widget arrangement - e.g. the minimap-area widgets differ by map, the ARAM-only panels want a different home than the SR ones. AND the same per-mode notion for the OUT-OF-GAME companion "stats" overlay (the lobby/idle stats surface) so it can show mode-appropriate stats. Design questions for planning: key the layout store by mode_key ({sr,aram,arena}) with a shared/default fallback; migration of the existing single layout into the SR slot; how the rc-shell mirror + Alt+Shift+R reset interact with per-mode slots; whether the launcher menu gets a "copy layout to other mode" affordance. The out-of-game stats panel specifically needs a MODE SELECTOR (SR / ARAM) that AUTO-SELECTS ARAM when the player is in/queued for ARAM (and stays manually switchable), so the idle/lobby stats reflect the right mode by default (operator add 2026-06-30). Touches overlay_layout.js (layout keyspace), the rc-shell preload bridge (mirror schema), and the companion stats surface (the mode selector + auto-select-on-ARAM wiring off mode_key). Each its own session + the per-page UI-audit ritual.

## Data pipeline

- **cdragon by-level ratio resolver** (DEFERRED from cdragon hard-tail, item 351, 2026-06-07): 111 Meraki-fallback ratio blocks are per-champ-level (1..18) arrays (`ByCharLevelBreakpoints/Interpolation`) the per-rank Meraki schema cannot represent. Drift-checked = ALIGNED not stale (different axis, not a stale value; blocks are auxiliary caps/minion/heal/duration/modifiers, primary-damage pairs agree <=~7%). Only worth doing if a future surface needs a champ-level-indexed ratio axis (NEW engine schema axis + extractor evaluator + consumer branch, ~2 batches). Evidence: prior `docs/_scratch/bylevel_drift_*` (gitignored). NOT a calc-graph resolver - the conditional/buff-counter/target-HP tail is live-state, permanently Meraki (item-232 class).
- **HZ-A Lane A v4 laning-scenario data-table regen** (FUTURE, games-gated; surfaced 2026-06-30 R37). The v4 CODE shipped (commit `84248459`: item-state axis + `cooldown_window` + `spike_timing` + `kill_threshold_met`; schema `laning_scenarios/v4`, 52 tests green), but the committed `data/daemon_slayer/laning_scenarios/16.13.1/laning_scenarios_{sr,aram,arena}.json` are still `laning_scenarios/v3` (64MB/mode LFS, full 171-roster). Regen = `core/laning_scenario_precompute.py --mode <m> --champions <full 171-roster>` (`SEED_CHAMPIONS` default is only 10; the reader is v3-backward-compatible so v3 tables stay functional - this is fidelity-additive, not a bug). DEFERRED because the v4 tables serve NOTHING live until the Slice-F coach flip (EXCLUDED, do-not-flip-blind) and cannot be shadow-validated until a real ALIVE laning tick exists ([[reference_hz_laning_agreement_gate]]; spec Blocker 1, zero today), so a regen is pure repo weight now: v4 ~doubles cells (band-pruned item-states L2:1/L6:2/L11:3) + 2 new blocks/cell -> ~190MB/mode LFS. CARRIES a monolith-vs-shard size decision (`docs/specs/LANE_A_SCENARIO_PRECOMPUTE_SPEC.md` Risk 4) - do NOT commit the 190MB monolith blind; evaluate per-bucket reader shards first. Generation path PROVEN green on a bounded sample (R37).

## Reliability / hardening

- **Pre-release name-scrub** _(operator-authorized standing prerequisite, 2026-06-02; canonical plan: `docs/PRE_RELEASE_NAME_SCRUB.md`)._ Scrub third-party/competitor names from file content + full commit history (force-push pre-authorized for that pass) before any public release, keeping the credited public data sources. DEFERRED to the release/open-source trigger - do NOT execute early.
- **`/api/analyze` streaming response**: current 30s timeout fine for ARAM (sub-second); may need SSE if full-mode analyses scale.
- **Same-state Haiku-skip debounce - BUILT DEFAULT-OFF** (ARAM `RC_ARAM_STATE_DEBOUNCE` item 506 + Arena `RC_ARENA_STATE_DEBOUNCE` item 507; SR was ALREADY covered by the always-on `coach_integration/_coach.py::_state_signature` skip). Remaining: the default-ON FLIP only - fidelity-gated, validate vs a live/replayed game first (a stale coach is worse than the cost; do-not-flip-blind). NB `cost_health_watchdog` exit 1 = its by-design breach signal, not a crash. Full narrative relocated to docs/history_notes.md.

## Speculative

- **`.rofl` file replay coaching**: `coaches/replay_coach.py` shipped lite version (rewind_history.db). Full `.rofl` packet-parse **REVISITED 2026-06-03** (operator asked to re-open). **Record corrected:** the old "only EOG `PlayerStatistics[]`, a subset of Match-V5" claim was WRONG - it described fraxiinus `roflxd`'s SCOPE, not the format. The `.rofl` chunk/keyframe stream DOES carry per-cast timestamps, windup / projectile timing, ability sequence, and sub-second positions (maknee reverse-engineered exactly this). Two encryption layers: **Layer 1** (container/chunk Blowfish - key embedded in the file, solved offline, patch-stable) and **Layer 2** (per-packet field obfuscation - hundreds of generated transforms baked into the game binary, **rotates every patch**; no maintained open library; maknee did not release code). **Verdict: keep the FEATURE out of scope** for a solo biweekly-patching tool - Layer-2 is recurring binary RE and the payoff only CALIBRATES the existing wiki `attack_delay_offset` + Meraki windup model rather than adding a capability. Optional patch-stable **Layer-1 spike** (parse header/metadata + Blowfish-decrypt/decompress chunks, ~1.5-2 days) is the only keep-able win if offline self-replay chunk inspection is ever wanted; steps 3-6 (Layer-2 decode -> cast/windup events -> validate vs wiki offsets -> wire to DS) stay out of scope. The s220 **S5 Replay page stays Match-V5-timeline-driven**. Sources: maknee League-Data-Scraping blog, fraxiinus/roflxd, robertabcd/lol-ob ROFL Container Notes.
- **PyInstaller packaging**: `riot-commander.spec` exists as opt-in starter. Not prioritized.
- **OBS publisher activation**: `T3 #11` shipped (264 LOC, opt-in); not wired to any active workflow.

## Research / inspiration (s219, 2026-05-15)

- **competitor teardown** - EXECUTED via the 2026-07-05 competitor deep-research queue (operator-directed; findings NON-repo on the Legion Desktop per the name-scrub policy). Residual option: the operator can re-subscribe for a month if a deeper paid-content teardown is ever warranted.
- **Pengu.lol Discord crawl** _(demoted 2026-05-17)_: the static KebsCS catalog now covers released endpoint knowledge; keep this only as a *secondary* sweep for unreleased / in-progress community work (replay-timeline access, novel Mayhem detection). Research-only; surface findings before scheduling code work. **One concrete lift already surfaced** (Mayhem-Doctor, 2026-05-17): the static asset `/lol-game-data/assets/v1/cherry-augments.json` resolves augment id -> name/icon/rarity (`kSilver/kGold/kPrismatic`) - authoritative augment table without screen-OCR for *naming* (OCR still needed for *which* augments are offered live). Independently re-confirmed: zero `/lol-cherry/*` and zero `/lol-game-augments/*` live endpoints exist (KebsCS full 26.05 catalog) - augment-OCR stays; do not re-pitch an LCU augment API.

### `open item research.txt` (~90 links) triage - 2026-05-17 (multi-agent, s234 methodology)

3 decisions LOCKED + integrated to ROADMAP + CLAUDE #90 (do not re-litigate): fold `Morello` (MIT) into the s220 PGR 0-100 score; build ONE shared smoothed-rate primitive (Laplace/Beta over own match DB) for #88 augment + pick/ban synergy + PGR score; 101.qq.com CN duo-synergy is worth a one-off Network-tab capture (done Legion-local + live-wired, item 277).

**FUTURE (noted, not scheduled):**
- **baronbuff.com** - closed-source *methodology* template: draft = single win-prob model fusing counters+synergy+comp+pool; PGR = "the decisions/phases that mattered, with evidence." Frames pick/ban + the s220 Deep-Review. Reference-only.
- **league_record** (GPLv3 - **cleanroom only, do NOT vendor**) - local OBS *video* capture keyed to LCU game-lifecycle + an event-timeline sidecar. NOT .rofl (sidesteps the CLOSED .rofl dead-end). The architecture reference for the s220 **S5 Replay** page.
- **aggregator Z13** builds-tier-list (ranks champxrunexfirst-item *tuples*, not just champs) - **aggregator J** (item tierlist bucketed by stat-type / tankiness / early-late) - METHOD refs for ARAM build data + DS presentation. Reference-only (closed Next.js SPAs, no JSON API exposed).
- **quick-aggregator-a-scraper** (MIT) - clean browserless Axios+Cheerio + TTL-cache aggregator A scrape = the *fallback* technique if RC ever needs an aggregator A tier/meta source.
- **AsperaDesu/LoL-AI-Draft-Picker** (MIT) draft-as-ordered-token-sequence idea - **junlarsen/league-connect** (MIT) per-endpoint WS `subscribe()` event-bus pattern (if RC ever moves champ-select/lobby off polling) - **CookieDecide/LeagueBuilds** (GPL, actively maintained) server-side build/rune/skill data pipeline (study, don't vendor) - **lolsite** `Greatest(field,1)` zero-guard per-min idiom. All reference-only.

**CLOSED - recorded so it is never re-spent (canonical summary: CLAUDE.md Settled, research-list triage CLOSED negatives):** all 27 LCU client / event-viewer / codegen repos are inferior to RC's lockfile client (KebsCS stays the reference catalog; the corpus has ZERO Arena/Cherry/Mayhem lobby-create payloads); the ML / CV / voice / offline-mode repo set is closed; Pengu `league-client-mcp` rejected. Full 27-repo list + per-repo reasons relocated to docs/history_notes.md.

### `draft tool L (the community fork)` (HuyTheSkeleton fork of `draft tool L`) triage - 2026-05-19

Single-repo liftability review (operator-handed GitHub link; focused triage, not the multi-agent list method).

**FUTURE (noted, not scheduled):** the **Elo log-odds draft-composition aggregator** is the team-vs-team draft layer `dashboard/routes_pickban.py` does not have - `winrateToRating = -400*log10(1/w-1)`, team score = sum(ally champ + ally-duo + matchup ratings) minus sum(enemy champ + enemy-duo), `ratingToWinrate = 1/(1+10^(-d/400))`. Composes **on top of** `core/smoothed_rates.py` (smoothed rates feed the rating transform - orthogonal, NOT a duplicate; smoothing/priors are RC's existing edge and are absent here). Trigger: when RC wants true team-vs-team draft scoring beyond the current recent-form / joint-WR proxy, with pairwise WR sourced off `rewind_history.db`. Reference-algorithm-reimplement only.

**CLOSED - recorded so it is never re-spent:** (1) **No license** on draft tool L (the community fork) AND upstream `draft tool L` (neither OSI nor permissive) - vendoring either is legally unsafe; algorithm-only, reimplement clean from the formula above. (2) Its data is a **aggregator D HTML scrape** (`apps/dataset/.../aggregator D/qwik.ts` parsing the embedded `qwik/json` blob) RC cannot legally cache or redistribute - the dataset half is a dead end; pairwise WR must come from `rewind_history.db`, not aggregator D. (3) The "+" fork adds **zero algorithmic value** (UI delta column / dynamic tier-list only; 2 stars, solo) - reason from upstream's math, never prefer the fork. (4) Does NOT duplicate `core/smoothed_rates` (no smoothing/priors anywhere). (5) Tauri/Qwik/SolidJS desktop stack - irrelevant to RC's Python/JS dashboard.

_(SHIPPED 2026-05-20: `core/draft_elo.py` + `core/draft_elo_db.py` + `dashboard/routes_draft_elo.py` + `web/js/panels/draft_elo.js` chip - the FUTURE trigger above is satisfied; composes on `core/smoothed_rates`. Full narrative relocated to docs/history_notes.md.)_

### Multi-agent research wave triage - 2026-05-20 (overnight headless)

5 parallel slice agents (LoL overlay landscape / champ-select UX / item-data sources / Arena augment + Cherry coaching / post-game review methodology). Decisions LOCKED + integrated to BACKLOG (do not re-litigate):

**NOW (shippable within 1-2 weeks):**
- **All three NOW items SHIPPED 2026-05-20** (LoLytics WPA framework `core/post_game_score.py` + `/api/post-game-wpa`; cdragon-arena dataValues+calculations enrichment + `agents/daemon_slayer/augment_formula_eval.py`; MQTT-style haste-aware CD math in `core/summoner_cooldowns.py` + Hextech-drake live wire). The cdragon-arena `STAT_GRANT_CALC_KEYS` displacement seam stays intentionally EMPTY until Arena S2 Level-Up patches populate it (no further engine change needed). Full narratives relocated to docs/history_notes.md.

**FUTURE (worth noting; condition to act):**
- **Arena S2 Augment Level-Up + Crafting Round system** (Riot 2026 S2, patch 26.09) - per-augment level 1->3 mid-match, round 8 = Crafting Round (add slot OR remove augment to level another), Keystone tier at max level. Schema break for `modes/arena_coach.py` + cherry-augments OCR. Trigger: 26.09 PBE confirms schema (likely `levels[]` axis on `Augment.dataValues`); pre-stage a `level` axis on `compute_augment_stats()`.
- **Brawl mode re-enable** (Riot disabled 2026-11-19 patch 25.23). Existing brawl backend stays deadcode per CLAUDE.md s214; touch only when Riot announces re-enable.
- **Draft Tool Z8 (Jfghanimah/loldraft-analyzer)** (`https://github.com/Jfghanimah/loldraft-analyzer`, MIT, May 2025) - 56-57% accuracy draft-WR predictor with FastAPI surface. Trigger: pick up as a second-opinion alongside our Elo aggregator-after PGR S2 lands.
- **Coaching App Z7 AI Drafting Model layered-ensemble methodology** (`https://coaching-app-z7.invalid/articles/the-draft-model`) - closed-source but documents architecture (linear + GBT layered ensemble, time-split validation, +7.5% WR lift attributed to draft alone). Read-only methodology reference for tuning the Elo aggregator.
- **LoL Esports Data Portal community tier** (`https://lolesports.com/.../introducing-the-new-lol-esports-data-portal`) - Riot 2,500GB/game pro telemetry, currently teams-only; "community access in the near future." Trigger: when Riot opens the community tier.
- **CommunityDragon broader `lol-game-data` JSON catalog** (`raw.communitydragon.org/.../plugins/rcp-be-lol-game-data/global/default/v1/`) - 7 files RC doesn't yet consume: `summoner-spells.json` (could ground CD ledger), `perks.json` + `perkstyles.json` (rune metadata for PGR), `maps.json` + `queues.json` + `game-mode-mutators.json` (canonical mode detection beyond KIWI/queueId hacks), `champion-rune-recommendations.json` (Riot's own recommended runes per champ). Cdragon redistributes Riot game data under Riot's Legal Jibber Jabber - same gray zone as the cherry-augments.json we already accept. Trigger: when PGR S2 wants better rune analysis or when CD-ledger has surplus capacity for a richer summoner-spell substrate.
- **Riot per-role grading rubric - CALIBRATED 2026-06-07 (item 335, `42780b3d`):** `core/post_game_rubric.py` weights re-ordered to Riot's per-role emphasis + `_ROLE_BASELINES` re-anchored to empirical per-role medians (median grades B). FUTURE tail: CC-score is a source-cited SUP signal with no rubric axis yet (schema lift); baselines are SR-only (event modes grade against SR medians). Full narrative relocated to docs/history_notes.md.
- **HURTS-THEM/HELPS-US ban-suggest toggle + draft-elo per-row contribution hover strip - both SHIPPED 2026-05-20.** STALE-PATH: `web/js/panels/ban_suggest_toggle.js` no longer exists on disk (probe 2026-07-17). Full narratives relocated to docs/history_notes.md.
- **`mwrogue` MediaWiki Cargo client** (`RheingoldRiver/mwrogue`, MIT) - wraps `wiki.leagueoflegends.com` (CC-BY-SA 3.0) for `Module:ChampionData/data` + `Module:ArenaAugmentData/data` access. Trigger: only as numeric backstop when cdragon `@variable@` template resolution gaps appear. We already have the aram modifiers (`aramHealing`/`aramShielding`/`aramAbilityHaste`/`aramTenacity`) in our existing Meraki champion bulk, verified live `0c550b5`; the wiki source is for cdragon templating gaps, not the aram modifier wire.

**CLOSED - recorded so we never re-spend:**
- **MobaHinted/client** (dead since Sep 6 2024; GPLv3 anyway). Only Python in-process competitor to RC is no longer maintained.
- **LeagueAkari, overlay app Z6 npm LCU wrapper, LeagueBroadcast/OSL/LeagueOCR/Dan-Shields/floh22 OCR HUDs, lol-vision/LeagueAI/PlayerImageRecognizer** - either GPL (incompatible), spectator-stream-only (irrelevant for player-side coach), or thinner than RC's existing surfaces.
- **Coaching App Z7 closed-source coach + Hakko AI RTC voice + Replays.lol + Insights.gg + Aggregator C GPI** - closed SaaS; methodology-only references.
- **AGGREGATOR A / Overlay App E / Aggregator C / AGGREGATOR N Arena augment WIN-RATE data at large** - **Riot dev policy explicitly forbids** displaying winrates for Arena augments or Arena Mode items across all third-party tools (Riot DevRel 2024-04, still in force). Pick-rate is allowed (we already pull overlay app E iesdev `core/augment_external_source.py`); winrate is not. Stick to pick-rate priors.
- **Reasoning-trace LLM agent ("Strategist/Critic")** (Draft Tool L-AI Devpost) - cost-prohibitive per champ-select + latency-prohibitive vs 30s timer; our aggregator already produces the inputs that would feed it.
- **Persistent counter-pick sidebar** (Overlay App E/Overlay App F pattern) - clutters at 1920x1080; hover-only wins. Aligns with the dashboard's tight Chrome-chrome viewport.
- **Riot prodraft.leagueoflegends.com integration** - tournament-only flow, no solo-queue value.
- **First-item-by-archetype-by-patch with permissive license** - no such public dataset exists; `rewind_history.db` + smoothed-rates pipeline IS the better path for RC's sparse-play cadence.
- **Fandom `leagueoflegends.fandom.com` wiki** - the OLD fork; the official wiki moved to `wiki.leagueoflegends.com` in 2023 and Fandom is stale within ~1 patch.
- **CommunityDragon/LeaguePatchNotes** - archived 2020, patches 8.13-8.22 only, 0 active forks. Patch-aware change tracking stays manual in the DS audit loop.
- **All other generic LCU client repos, OCR HUDs, win-predictor clones, replay parsers** - duplicates or subsets of RC's existing surfaces. The complete CLOSED list across the 5 research slices is in the agent transcripts; do not re-research.

### Competitor lift teardown - 2026-06-16 (Section 7b deep-dive, run -03)

Full artifact: `docs/COMPETITOR_LIFT_2026-06-16.md`. 3 net-new targets (Aggregator C GPI / Overlay App E overlay / Overlay App F companion); RC-HAVE flags ground-truth re-verified. The overlay app E live-benchmarking finding's net-new half SHIPPED in-run (item 443, `core/live_benchmark_band.py` - live cs+level vs the player's own per-champion percentile; the personal-percentile half RC only used post-game).

**FUTURE (worth noting; condition to act):**
- **Aggregator C GPI 8-axis player skill radar - SHIPPED** (radar item 450; weakest-axis tip item 451; per-champion drill-down item 511 - live via `core/player_gpi.py` + `/api/player-profile` + `web/js/panels/player_gpi.js`). FUTURE tail (do not rebuild the core): fold the radar into a dedicated Player Profile page after PGR S2. Full narrative relocated to docs/history_notes.md.
- **Overlay App F pre-game lobby player-tags** (smurf / loss-streak / one-trick / tilt) - rule classifiers over each lobby participant's recent Match-V5. MED: new per-lobby Riot fan-out (10 players, rate-limit + caching work); uses the existing key (ADR-006 champ-select team context). Trigger: when champ-select wants opponent reads.
- **[CLOSED-not-viable 2026-06-25] Overlay App F post-game warding heatmap** - premise FALSIFIED. Match-V5 timelines carry NO ward position. Probed `data/rewind_history.db` `timeline_events`: WARD_PLACED = 294518 rows / 0 with kill_pos_x+kill_pos_y, vs CHAMPION_KILL = 270807 / 270807 with x+y. Ward x/y does not exist in the stored corpus (nor in Match-V5 at all - wards only carry creatorId/wardType/timestamp), so a binned heatmap is ungroundable - the same wall as the live `/api/ward-heat` (no WARD_PLACED producer, `core/ward_events.py:12`). Do NOT re-pitch a ward heatmap from stored timelines.
- **Overlay App E enemy ult / ability CD timers** - DS already has ability-haste-aware CD math; the gap is the ENEMY cast-detection trigger (Live Client does not expose enemy CDs). MED: cast-detect plumbing (vision / event heuristics). Self/ally ult timers are the cheaper subset.
- **Overlay App E LCU rune-page auto-write + personal-WR build override** - push DS-recommended runes into the client (mirror lcu_rune_writer) + promote the player's own high-WR build over the DS default when rewind_history beats it. MED: rune-write touches frozen lcu_client.py (needs a non-frozen wrapper); the personal-WR override is the cheaper local-data half. **Local-data half SHIPPED 2026-06-19 (item, run 2026-06-19-01, `43acb128`):** `core/personal_build_wr.py` + `GET /api/personal-build?champion=X[&mode=]` - per-champion item win-rate lift from rewind_history.db (smoothed_rates blend, legendary+boots gold floor). FUTURE tail: a UI consumer (champ-select panel or the deterministic brief ally_notes line) - deferred, needs the OWED Legion-local visual capture; the rune-page auto-write half still needs the non-frozen lcu wrapper.
- **Laning precompute hold-band recalibration (PRIMARY Haiku-zero, operator-gated Tier-2)** - the #1-frequency laning flip is blocked at 39% precompute-vs-Haiku agreement (160/406 comparable over the accrued real-game shadow log; `tools/hz_shadow_report.py` confusion matrix, run 2026-06-19-01 `a4db6aab`). The precompute verdict vocabulary lacks a `hold`/farm band (only back_off/trade/even) and is back_off-biased. NEXT: give the report a distinct `even` bucket, decide the `even`<->`hold` mapping (**report half DONE 2026-06-19 item 508 `4623edd7`: even bucket + even<->hold map shipped in `tools/hz_shadow_report.py`, worth +57 ticks 39%->53%; report-only, no flip**), add a hold band + soften the back_off threshold in the laning scenario verdict generator (`agents/daemon_slayer/matchup.py` `_TRADE_MARGIN`/`_EVEN_BAND` = Tier-2), regen the `data/daemon_slayer/laning_scenarios` LFS tables, re-run the report and confirm agreement climbs before any flip. Full record: `ops/audit/HZ_HAIKU_CALL_INVENTORY.md` UPDATE 2026-06-19. Product-calibration call - NOT a blind overnight edit.
- **LBAND1 live wire-in** - feed `core/live_benchmark_band` into the deterministic-coaching `/api/state` surface + the overlay, validate vs a real/replayed game, then flip (do-not-flip-blind). Item 443 shipped the generator; this is the wire + validate + flip.

**SUPERSEDED (no lift):**
- **Overlay App F per-ability/per-phase damage breakdown** - DS per-spell DPS/burst is already richer than the observed split. No lift.
- **Overlay App E post-game behavioral grading** - mostly at-parity with RC's PGR + the aggregator G reframe; only the "biggest swing moment" timeline-delta localizer is net-new and folds into the PGR work.

### Competitor lift teardown - 2026-06-27 (Aggregator D, Section 7b deep-dive, R34)

Full artifact: `docs/COMPETITOR_LIFT_2026-06-27_AGGREGATOR_D.md`. Signature = delta-vs-baseline on every row + the popular-vs-winrate dichotomy. F1 (popular build shown next to the win-rate build + survivorship insight) SHIPPED in-run on the personal_build champ-select panel (presentation over the already-served `most_common_build`).

**FUTURE (worth noting; condition to act):**
- **F3 per-opponent matchup delta-stats table (TOP candidate)** - a new aggregator over `core/match_metrics.py` `csd_at_15`/`team_gold_diff_trend`/`matchup_history` + `rewind_history.db`, surfacing "vs champ X over my corpus: avg gold/CS/kill diff @15" + a new route + a Build Insights tab. The raw fields are RECORDED (only surfaced as a single live in-game value at `dashboard/_adaptation_latch.py:177`); `/api/personal-vs` returns W/L + threat only, no stat diffs. MED: new aggregation over a gitignored DB (needs fixtures, clean-checkout-probe risk). Same class as the Aggregator B F1 deferral - do not build blind in-run. Trigger: a Build Insights matchup expansion.
- **F8 early/mid/late + snowball/comeback rating bar** - a per-champ corpus aggregation over the already-computed `core/lead_projection.py` `_phase` + `comeback_odds` (today a single live pill, not a per-champ rating panel). MED new-compute. Trigger: a Player Profile / PGR expansion.
- **F2/F5/F6 (defer)** - delta column on the WPA tabs (headline per-item lift already shipped); skill priority(lvl10) vs order(lvl15) split (needs timeline frames); per-rune pick% column on rune-WPA. All MED, touch don't-redo tabs.

### Competitor lift teardown - 2026-06-30 (Guide Site Q, Section 7b deep-dive, R44)

Full artifact: `docs/COMPETITOR_LIFT_2026-06-30_GUIDE_SITE_Q.md`. Guide Site Q is a human-authored guide site, NOT a stats aggregator - its distinct surface is hand-curated (THREATS danger grid, CHEAT SHEET, Pros/Cons, situational reasoning). RECOMMENDED IN-RUN SHIP = NONE (CLEAN no-op): the one Guide Site Q feature with real compute (theorycraft stat-totals) is already shipped + stronger in RC (ds-statcheck, `routes_ds_statcheck.py:213` + `ds_statcheck.js:45`); the rest is author-authored content a solo algorithmic tool replaces with computed surfaces RC already owns. No finding cleared the HIGH-lift + LOW-risk + one-payload-presentation gate. Triage NOW=0 / FUTURE=2 / CLOSED=4.

**FUTURE (worth noting; condition to act):**
- **F1 lane/fight threat column (TOP candidate, MED)** - extend the existing first-enemy ds-matchup card (`web/js/panels/ds_matchup.js:247-251`, today `enemyIds[0]` only) into an all-5-enemy danger grid: loop `cs.their_team`, call the EXISTING per-pair-cached `/api/ds-matchup` per enemy, bin `verdict`/`swing_pct` red->green. No engine/route/schema change - pure frontend multi-fetch + a small verdict->severity binning. Deferred only because it is multi-fetch, not a one-served-field re-render. Trigger: a champ-select threat-readout expansion.
- **F3 skill-order max-priority grid (FUTURE, new compute)** - a champ-select Q/W/E/R max-priority glance needs a NEW served max-priority payload for the locked champ; the engine-side skill data exists (`core/skill_wpa.py`, `tools/daemon_slayer_extract.py`) but is not served to champ-select. New compute/route, a separate DS slice - not a presentation lift.

**CLOSED (covered / not-distinct):**
- F2 theorycraft stat-totals (shipped: ds-statcheck, RC's is engine-resolved not a flat item sum), F4 Pros/Cons prose (no author in a solo tool; the DS Profile 8-axis radar covers strengths data-driven), F5 situational-items-with-reasoning (the enemy-reactive build_order + anti-tank hint A3 + damage_mix already deliver the substance), F6 counters / tier list (Aggregator N lift + generic-aggregator CLOSEDs; not Guide Site Q-distinct).

### Competitor lift teardown - 2026-07-03 (Aggregator H, Section 7b deep-dive, R71)

Full artifact: `docs/COMPETITOR_LIFT_2026-07-03_AGGREGATOR_H.md`. Aggregator H tells its whole early/late power story with ONE correlational graph family (Winrate / Game Duration, server-baked inline Flot arrays, zero client math, no explicit early/late tag - the reader infers from slope). F1+F2 SHIPPED in-run: the per-champion filter on the Game Length tab (the `&champion=` param was already live on `/api/duration-winrate` but never sent by `web/js/panels/duration_winrate.js`) + a computed early/flat/late tendency chip (n-weighted half-vs-half winrate delta over non-null buckets) - presentation-only, JS/CSS slice.

**FUTURE (worth noting; condition to act):**
- **F3 snowball elasticity: WR by (K-D)@10/@20 (TOP candidate, MED)** - quantifies lead conversion per champion (steep = snowball-dependent, flat = comeback-capable). Raw data fully local: `data/rewind_history.db` `timeline_events` carries `event_type, timestamp_ms, killer_id, victim_id` (2954 matches, probed 2026-07-03); NO aggregator exists (only coach prose at `core/lead_projection.py:107`). Build = new `core/` aggregator mirroring `core/duration_winrate.py` (conn-injection seam, MIN_BUCKET_N gate, coarse bins <=-2..>=+2, `core/smoothed_rates.py` laplace shrink) + thin route + Build Insights row. Descriptive only - never fed into DS rank. Trigger: next Build Insights expansion.
- **F5 personal learning curve: WR by nth game on champ (MED-LOW)** - order matches by `game_creation_ts`, bucket by cumulative game index ("first 10 vs last 10 Nasus games"). Gate on a champ-games histogram first - most champs are <20 games and the readout risks noise-as-signal even with shrink.

**CLOSED (covered / boundary):**
- F4 stat-vs-duration curves (RC's `core/perf_curve.py` ships gold/CS richer - win/loss split, which Aggregator H lacks), F6 inline-flat delivery pattern (RC discipline parity: precompute + route cache + sig-dedup + ADR-008), F7 correlational-vs-sim merge (DS default rank stays pure simulation - duration surfaces stay descriptive), global cross-player curve basis (single-player tool, deliberate non-goal - same stance as Aggregator B F3).

### Competitor lift teardown - 2026-07-05 (live-game overlay, Section 7b deep-dive, R81)

Full artifact: `docs/COMPETITOR_LIFT_2026-07-05_R81.md`. First lift of the LIVE-GAME desktop OVERLAY category (distinct from the pre/post-game stat sites already covered). The live-companion overlay's signature feature is a three-phase early/mid/late GREEN/YELLOW/RED strength stoplight (per champ + per team), a static per-champ heuristic. F1+F2 SHIPPED in-run: an additive `phases` block on `/api/spike-curve` (comparative ally-vs-enemy phase-mean power at a 5% relative margin - NOT the vendor's "vs own mean" recipe, which degenerates to early=red/late=green for every team on RC's monotonic fraction-normalized team curve) + a 2x3 stoplight strip beneath the active-match sparkline. Presentation-only over EXISTING `/api/spike-curve` arrays, no ENGINE bump, no new data source.

**FUTURE (worth noting; condition to act):**
- **F5 matchup card reads the ROLE-MATCHED lane opponent (MED)** - `web/js/panels/ds_matchup.js:247` selects `enemyIds[0]` (the first enemy champ) instead of the role-matched laner; RC HAS role data (Live Client `position` = ROLE). A pure-JS opponent-selection fix over the existing `agents/daemon_slayer/matchup.py compute_matchup` engine, own slice + 5-phase audit. Trigger: next champ-select / matchup UI pass.
- **F4 mount the matchup VERDICT on the live in-game surface (MED, pair with F5)** - `ds_matchup.js renderDsMatchupForChampSelect` mounts champ-select-only (`active_match.js:982`); the verdict half is liftable to the live active-match surface reading `liveclient` champ ids. The ability-cooldown half is DATA-BLOCKED (Live Client exposes no live cooldowns). Own slice + live mount + audit.
- **F3 self power-spike transition TOAST (LOW)** - `web/js/panels/spike_markers.js` already computes the operator's own level (6/11/16) + item-completion spikes as a static strip; a toast-on-crossing adds marginal value. The enemy-spike half is data-blocked (no live enemy level/item feed - `core/lead_projection.py:20`).

**CLOSED (data-blocked / already owned - do NOT re-pitch):**
- F6 gold-lead-per-lane overview + F7 ally ult/summ portrait timers - no live per-player gold or cooldown feed from any API RC can read (`core/lead_projection.py:20`; CLAUDE.md live-input note). Epic-objective timers are ALREADY owned via `core/event_callouts.py` + `web/js/panels/objective_gauges.js` (OQ16 live ring gauges).

### Competitor lift teardown - 2026-07-10 (Aggregator C, Section 7b deep-dive, R89)

Full artifact: `docs/COMPETITOR_LIFT_2026-07-10.md`. Two features torn down: F1 lane-matchup tags + F2 player combat-style tags. Aggregator C' web matchup surface is win-rate-driven (a counter table sorted by enemy WR, a curated 3-card "how to play against" prose block, a strong/weak chip row) plus a per-champion Riot-CLASS icon + learning-difficulty word; the GPI is a player-behavior radar. F2 combat-style SHIPPED in-run: a read-only archetype tag chip (CARRY/BRUISER/TANK/MAGE/ASSASSIN/ENCHANTER) on the champ-select My Pick card, sourced from the archetype RC already resolves + client-caches (`_CSV_ARCH_CACHE[champ].primary`, route `/api/cs-archetype-pick`) but never rendered since the picker was removed (LEDGER 823) - new `web/js/panels/archetype_chip.js` reusing the `.csv-build-badge` tint family, presentation-only, no backend / Claude / schema.

**FUTURE (worth noting; condition to act):**
- **F1a lane-matchup WR "best/worst counters" list (MED)** - Aggregator C sorts a counter table by enemy-vs-you WR + a strong/weak chip row. RC has the WR lanes (`core/smoothed_rates*.py`, `rewind_history.db`) but the champ-select matchup card (`web/js/panels/ds_matchup.js`) is a SIM verdict, not a WR list. Needs the live-WR lane wired into champ-select + a new render (data dependency, cache, rank/region filters). Trigger: next champ-select matchup UI pass.
- **F2a Arena My Pick chip parity (LOW)** - the archetype chip shipped on the SR/ARAM My Pick body; Arena uses a separate `_csvArenaPaneHtml` layout (`champ_select.js:931`) and did not get the chip. Pure-JS parity add + audit. Trigger: next Arena champ-select pass.

**CLOSED (dependency / already-owned - do NOT re-pitch):**
- F1b curated "how to play against" prose tip cards - a content / Claude authoring dependency that collides with the no-new-Claude-dependency + Error-Handling guardrails. RC's `/api/ds-matchup` verdict chip (ALL IN / TRADE / BACK OFF / EVEN) already covers the cheap dependency-free half of the matchup read.
- F2b pre-game behavioral player badges (Fatigue / Hot Streak) - per-summoner history aggregation RC does not compute; out of the presentation-over-DS-math lane. GPI radar already owned (`web/js/panels/player_gpi.js`).

### Competitor lift teardown - 2026-07-10 (Overlay App F, Section 7b deep-dive, R100)

Full artifact: `docs/COMPETITOR_LIFT_2026-07-10_OVERLAY_APP_F.md`. First teardown of the live pre-game + in-game SCOUTING companion category. Verdict: ~90 percent DUPLICATE - Overlay App F's whole value chain needs a Riot PRODUCTION spectator-v4 key + a scraped stats warehouse (arbitrary-summoner live-game lookup), which is a CLOSED anchor for RC's personal key (ADR-006; enemies client-hidden until the `:2999` loading screen). Its pre-game per-player card (rank / LP / recent WR / mastery / mains / recent W-L streak) is already BUILT in the FU02 team-context fan-out (`dashboard/routes_team_context.py:200-239`, `core/riot_api.py:611-670`); its premade detection + playstyle-tendency labels + self-tilt cluster are already QUEUED under the R81 scouting fold-in; its matchup / power-spike / objective-timer surfaces are BUILT or queued under R89 / R81 / `core/event_callouts.py`. VERIFY-PREMISES CORRECTION: the research pass nominated a "ship in-run" candidate (render the fetched W-L streak as dots) - REFUTED live, `web/js/panels/team_context.js:126-133` ALREADY renders `w_l_streak_7` as text ("4W 3L", "Last 7 games" tooltip, ally + enemy cards). NO in-run ship (research-only cycle, R85/R94/R98 tradition).

**FUTURE (worth noting; condition to act):**
- **F1 manual click-to-track enemy summoner-spell / ult cooldown overlay (MED, do NOT build blind)** - the one genuinely-NEW mechanic. A pure client-side UI state machine (click an enemy portrait on a burn -> tick down the static base CD), NO data feed, so it sidesteps the "no live per-player cooldown feed" data-block. MED-lift: a new Electron overlay widget + in-game input via `hotkey_listener.py` (globalShortcut is dead under League per `reference_overlay_ingame_hotkey_win32`). Low fit for a solo player mid-fight; outside the deterministic-precompute lane. Trigger: an operator-directed live overlay session, not a headless slice.
- **F2 recent-form W-L "dots" restyle + tilt hint (LOW, fold into R81)** - cosmetic upgrade of the already-rendered `team_context.js:126-133` streak text to a green/red pip strip + a soft "on a skid" flag. LOW-lift but overlaps R81's snapshot-card lane, needs Laplace-shrink (`core/smoothed_rates.py`) before any 7-game tilt claim, and its value is purely visual so it needs a live overlay capture to validate. Fold into the R81 snapshot card when that lands live; do NOT ship standalone blind.

**CLOSED / DUPLICATE (do NOT re-pitch):** live-game search by arbitrary summoner (production spectator-v4 key + warehouse + user base = ADR-006, CLOSED); the pre-game scouting card, premade detection, playstyle labels, self-tilt (already BUILT via FU02 or queued via R81); lane-matchup / power-spike / objective timers (BUILT/queued R89/R81/event_callouts); global champion tier list (deliberate non-goal - RC is per-champ simulation, single-player); one-click build/rune import (DS + `lcu/lcu_rune_writer.py` own it); post-game review (PGR + `core/perf_curve.py`). LOOP-HEALTH: the live-scouting / live-overlay competitor CATEGORY is now DRAINED (FU02 + R81 + R89 + event_callouts); a future competitor pick should target a DIFFERENT category (draft theory / replay-VOD / economy-wave tooling) or accept the well is running dry - the meatier open lanes are the DS-sweep Meraki refute rotation + the Haiku-to-ZERO Lane A/E precompute programs.

### Competitor + data-acquisition deep-research fold-in - 2026-07-05 (R81 incorporation)

Operator-directed fold-in of the broad 89-agent competitor + data-acquisition research run (21 NOW / 10 FUTURE / 13 CLOSED). The FULL artifact with competitor names + the 5 cheap probe experiments lives NON-repo on the Legion Desktop (`RC_COMPETITOR_RESEARCH_2026-07-05.md`) per the name-scrub policy (project_pre_release_name_scrub); the technical substance is captured here name-scrubbed. Companion local-tool teardown (an open-source LCU toolkit) also NON-repo on Desktop. None are auto-built - they enter the actionable queue for operator-picked slices.

**Rate-wall data-acquisition lane (attacks the ADR-006 personal-key scouting wall; consumer = the player-snapshot card spec 2026-07-04):**
- **(NOW, zero-API, do-first) Offline premade + tendency mining of `rewind_history.db`** - match-intersection premade detection (fetch/read one roster member's match-id set, filter locally for matches whose participants contain the other lobby PUUIDs -> N-player premade + played-together WR for ~0 calls) + role-conditioned behavior labels + timeline-derived gank-pressure / jungle-help metrics. Zero API, zero ToS risk (RC's own stored Match-V5). Degrade to the "-" sentinel on thin coverage. New `core/` aggregator + snapshot-card interpretation layer.
- **(NOW, official-API, already-fetched) Derived opponent-scouting layer over the shipped Mastery-V4 + League-V4 fan-out (FU02, `core/riot_api.py` + `routes_team_context.py`)** - comfort-pool depth (top-N mastery `championPoints` + `lastPlayTime` recency), banScore = counter-WR x mastery-pool weighting, a red RISK badge on pick cards when an opponent pool holds a high-WR counter. Pure computation on calls RC already makes; feeds the snapshot-card spec.
- **(NOW-compliant) Game-start targeted Match-V5 timeline burst** - at `:2999` load-in the 5 enemy names are exposed; 5 enemies x (puuid + ids + 2-3 timelines) fits the 100-req/2min window; derive jungle-start-side / invade-rate / first-gank-lane tendency priors, cache to `rewind_history.db`. Anchors on GAME-START, not champ-select (ranked draft hides enemies); event modes (queue 2400) excluded.
- **(FUTURE, ToS-MED) Aggregator XHR-first scrape via the item-277 pattern** (live-first + static seed + kill switch) for per-player scouting + once-per-patch matchup priors; sniff the JSON/XHR endpoint FIRST, persistent-profile headless Chrome only as the anti-bot fallback tier, selector specs as versioned JSON; mandatory `core/smoothed_rates` Laplace shrink (samples as small as 17 games observed).
- **(FUTURE, ToS-HIGHEST, live-probe-gated) SGP service-gateway channel** - LCU `GET entitlements/v1/token` accessToken -> regional SGP host `:21019` -> `match-history-query/v1/products/lol/player/{puuid}/SUMMARY` returns Match-V5-shaped history for ARBITRARY lobby PUUIDs with NO dev-key rate limit (+ Riot-Client `player-account/aliases/v1/lookup` for riotId -> PUUID). Highest raw payoff, highest ToS-gray (unofficial private Riot API; auth is Riot's own token, no injection). DE-RISKED 2026-07-05: the companion open-source LCU-toolkit teardown (Desktop, non-repo) CONFIRMED the concrete host-map (Riot `.sgp.pvp.net` regional hosts + Tencent `:21019`) + token flow (LCU `entitlements/v1/token` -> `Bearer` on the SGP host + `league-session-token`) + endpoints - so the "open unknown" is now a known recipe. The ONE remaining live unknown = a Legion NA-region host reachability probe (research experiment #1). Still ToS-HIGHEST -> FUTURE, do-not-ship-blind. See the toolkit fold-in row below.

**Draft + coach lane (Haiku-to-ZERO north star; every serious commercial coach converged on precompute-over-live-LLM):**
- **(NOW) Five-layer deterministic draft score** - fuse lane-matchup 30% (lane WR + GD@15) / pairwise synergy 20% / AD-AP-tank damage balance 15% / early-late scaling coherence 10% / base WR into one bounded score + HIGH/MED/LOW confidence tiers. RC owns every primitive (`core/smoothed_rates`, item-277 duo-synergy, DS damage-mix + spike data); the lift is fusion weights + bounded-score UX. Honest 42-58 band (do NOT fake a 0-100 spread - draft explains little).
- **(NOW) Session-hygiene + tilt-nudge cluster** - from `rewind_history.db` timestamps only: games-this-session counter with validated thresholds (first game worst; peak WR games 6-7; decay past 7-8; 2+ day rust penalty; same-champ requeue = best odds; tilt-lock warning on comfort-pick-after-loss), a 0-100 "Should I Queue" readiness + WR-by-hour/day. Zero API, zero Haiku, all local SQL; phrase as odds-shifts, never causal advice. (Executes RC's own already-specced gap 1d.)
- **(NOW) Playstyle-inference deterministic labels for the snapshot card** - role-conditioned over stats RC already stores (jungler KD/CS -> "ganks a lot / weak jungle"; per-game score variance -> "coinflip all-in"; support deaths -> "likely autofill"), "-" sentinel on thin data; emit a VERDICT, not raw stats.

**Overlay + radar lane (deterministic, zero-LLM, existing inputs):**
- **(NOW, MED) Radar target-profile reference polygon** - a better-WR reference polygon behind the player polygon on the GPI radar; pairs with the CHI-paper mechanics-radar static bands (positioning / combo-diversity / monitoring / pings separate Diamond+; zero new capture).
- **(NOW) Overlay HUD micro-lifts** - 90s/10s double-alert cadence on the OQ16 objective gauges; gold-remaining-to-next-DS-recommended-item readout (`:2999` gold + DS build order); team item-value differential (sum owned_items priced via DS authoritative costs into the reserved st-gold-diff slot); trinket-swap timing nudge as a precomputed rule line.
- **(NOW, increment on shipped plumbing) Predicted roam/invade route** - from `last_seen_zone` + `missing_for_s` via a static zone-adjacency heuristic, escalating the shipped MIA badge to a directional danger toast (no CV identity - does not touch the CLOSED anchor).

**Dev-loop / audit tooling:**
- **(FUTURE) Vite-style push hot-swap** - on asset change push a ws event naming the file; client hot-swaps the CSS `<link ...?v=hash>` in place (state-preserving), full reload only for JS; upgrade to ADR-008 after the wss:// overlay fix lands.
- **(NOW-cheap) uxpatterns.dev + taste-skill design dials** folded into the 5-phase UI-audit prompt (Tooltip-vs-Popover, Comparison-Table / Card-Grid / Statistics-Display, AI Loading/Error states; variance/motion/density dials).

**FUTURE tail (one-liners -> lane):** domain-decomposed post-game sub-scores (farming/ganks/itemization/vision, each 1 strength + 1 mistake) -> PGR S3-S5; per-phase timeline mistake-pinpointing (failed-engage / missed-objective) -> PGR; standardized GD@10/XPD@10/CSD@10 delta + replacement-level vocabulary -> PGR 0-100 axes; decoded-replay HF dataset (Apache-2.0, patch-12 behavioral only) -> a Haiku-to-ZERO precompute mining SESSION; OBS-overlay URL-param browser-source route on `:8888`; patch-diff "what changed for YOUR champs" card off the existing golden-diff.

**CLOSED anchors (do NOT re-pitch):** Riot production key (ADR-006 - needs a registered public product + users); install-base telemetry (needs a user base); Overlay Platform M events (= `:2999` repackaged, RC already polls it); enemy identity at ranked champ-select (client-hidden - live scouting must anchor on game-start `:2999` names); Match-V5 on event modes (queue 2400 KIWI 403/empty is permanent); `.rofl` full packet-parse as a shipping feature (per-patch RE cost; the Apache-2.0 decoded HF dataset is the only consumable byproduct); all-CV / per-frame VLM data pipelines (the exact cost model RC's precompute strategy exists to avoid; no opponent-scouting relevance - local capture sees only your own screen); whole-repo framework swaps (fastapi / vue / langchain / pytorch = whole-repo-vendoring anchor over a working tree).

### Champ-Select SR UI-audit residual - 2026-07-14 (R118, LEDGER 892)
- **(FUTURE, LOW/NICE, non-baseline) Champ-Select SR `.csv-rune-side-tree` narrow-width overflow** - the R118 5-phase SR audit was CLEAN at the 1920x1080 design baseline (recon desktop worstRight=1920, 0 pageerrors, guard `tests/test_champ_select_sr_ui_audit.py`), but the ui_recon companion-920 pass surfaced a ~105px horizontal overflow on `.csv-rune-side-tree` (the SR build-chooser rune side panel). 920px is below the `@media<=1200px` single-column reflow AND below the shipping 1920 baseline, so this is a non-baseline NICE-TO-HAVE, not a MUST-FIX. Fix `web/css/panels/champ_select_view.css` (clamp/wrap the rune-side-tree width) ONLY if a companion/narrow champ-select surface is ever a shipping target.

### Open-source LCU-toolkit teardown - 2026-07-05 (R81 companion, Section 7b)

Operator-directed scope-out of a locally-installed open-source LCU toolkit (named artifact + full endpoint map NON-repo on Desktop: `RC_LEAGUEAKARI_TEARDOWN_2026-07-05.md`) for its app functions + League-client interaction. GPL-class copyleft -> never vendor; re-implement any lift in RC's own code. It is push/event-driven (LCU WebSocket) where RC polls, and its automation half is CLOSED for RC (ToS/product-fit: RC is a coaching dashboard, not a bot).

**Client-interaction lift verdicts (name-scrubbed):**
- **(NOW, in-transport LCU, no ToS) Legal-move champ-select getters** - `/lol-champ-select/v1/{pickable,bannable,disabled}-champion-ids` (RC does not call these; improves pick-card legality). New getters in `lcu/lcu_pregame.py` + surface in `dashboard/_champ_select.py`. Existing Basic-auth transport, no new dep.
- **(NOW, in-transport LCU) Riot recommended-runes read as a degrade fallback** - `/lol-perks/v1/recommended-pages/champion/{id}` when RC's own DS-driven rune plan is thin (`lcu/lcu_rune_writer.py` WRITES runes today but never READS Riot's recommendation). Cheap; RC's DS runes stay primary.
- **(FUTURE, MED, architecture) LCU WebSocket event stream** - subscribe `OnJsonApiEvent` over `wss://riot:<token>@127.0.0.1:<port>` (WAMP) for instant gameflow / champ-select / rune-write-timing pushes instead of RC's ~1 Hz poll. New `lcu/lcu_events.py` sidecar owning reconnect-on-lockfile-rotation into the asyncio AppLoop (do NOT edit the frozen `lcu/lcu_client.py`). Read-only event stream, no ToS issue; latency/efficiency win.
- **(FUTURE, HIGH value, ToS-HIGHEST) SGP all-10 scouting = the de-risked rate-wall recipe above** - concrete host-map + token flow + endpoints now known. New `core/sgp_client.py` off the existing lockfile+Basic-auth plumbing (`lcu/lcu_client.py:71`), feeding `routes_team_context`/scouting + `core/riot_api_cache.py`. Gate: the one live unknown = a Legion NA-host reachability probe. Do-not-ship-blind.
- **(FUTURE, LOW-MED) Tagged-players note layer** - user-authored notes/tags on encountered players (a table alongside `rewind_history.db`), surfaced on the scouting card.

**CLOSED (ToS / product-fit - do NOT lift):** all automation - auto-accept, auto-matchmaking, auto-pick/auto-ban, queue-dodge (`/lol-gameflow/v1/session/dodge`), auto-honor, auto-reply, auto-invitation, native key-input injection, chat-status spoofing, auto-send-stats-to-chat. The external stats-site champion-API it embeds is a named-competitor source -> keep out of repo; it only re-confirms the item-277 aggregator XHR pattern already captured above.

### OBS + deterministic-CV + minimap capability - 2026-07-05 (R81 expansion 3)

Canonical home: `docs/OBS_CV_MINIMAP_PLAN.md` (full phased plan - OCR-field shadow-first coach wire-in NOW-next; OBS extension O1-O3; new deterministic-CV modules; minimap champion IDENTITY via `opencv-python`, the only genuinely new dep). Status + next steps: the ROADMAP.md Lane-E row.

Don't-redo anchors (scoping CORRECTED premises, keep): OBS is ALREADY integrated - `core/obs_publisher.py` is a full OBS-WebSocket v5 client wired in `dashboard/server.py` (EXTEND it, do NOT re-scaffold; `websockets` is already a dep). The deterministic-CV parsers are ALREADY built (`core/vision_tesseract.py` + calibrated `data/vision_regions.json` + 173 champ icon templates on disk) - the gap is WIRING into coach `TIERED_FIELDS`, not authoring. Every coach-serving flip is live-gated (a wrong deterministic read is worse than an LLM call; nothing ships blind headless). Full section text relocated to docs/history_notes.md.
