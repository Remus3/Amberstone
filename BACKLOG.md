# Riot Commander - Backlog

_Aspirational / longer-term items. Extracted from ROADMAP.md "Future" section 2026-05-08._
_When an item moves to active work, migrate it to ROADMAP.md "Open items"._
_Shipped items live in git log + the CLAUDE.md "Active priorities" ledger; this file is for things NOT yet done._

---

## Coaching depth

_2026-05-21 sweep: aramAbilityHaste + augment formula evaluator + item-AH lane + ward-heat frontend entries all SHIPPED - see CLAUDE.md ledger items 113 (`4e5d818` + `21657eb`) + 119 + 121 (`f4938af`) + ENGINE 1.24.0 (`f273acb`); not duplicated here._

- ✅ **Future EHP enemy-CC model for `aram_tenacity_mult`** _SHIPPED 2026-05-21/24 (CLAUDE.md items 126/127/130/134-177; ENGINE 1.26.0 -> 1.56.0)._ Both ecosystems COMPLETE: `cc_blended_ehp` across 4 surfaces (engine math `compute_ehp(enemy_champions=...)` + coach prompt `cc_blended_ehp_impact_line()` + DS scorer `compute_hybrid(enemy_champions=...)` + dashboard chip `/api/cc-blended-ehp-threat`); `cc_conditional` across 5 surfaces (engine `cc_pressure.compute_cc_pressure(include_conditional=...)` + ehp/hybrid kwarg threading + coach prompt `cc_conditional_impact_line()` + dashboard chip `/api/cc-conditional-pressure`). `_PER_SPELL_CC_DURATIONS` seeded at 108 entries / 89 champs (waves 1-9); `cc_conditional` registry at 72 entries / 57 champs (waves 1-23; item 153 wave 9 Meraki extractor schema lift adds `effects_descriptions` + closes Renekton W frenzy_state + Hwei E form 1 + Neeko E; item 154 wave 10 same-spell-slot sidecar registry `_PER_SPELL_CC_CONDITIONAL_FORMS` closes Karma W form_index=1 R-empowered Renewal root + Hwei E form_index=2 Gaze of the Abyss root; item 156 wave 11 closes Sion R Unstoppable Onslaught primary channel_completion + Gnar W form_index=1 Wallop sidecar Mega-form-gated frenzy_state; item 170 wave 12 closes Singed E target_debuffed Mega-Adhesive overlap root + Alistar E nth_hit Trample 5-stack stun + Sylas E form_index=1 sidecar channel_completion Abduct stun; item 171 wave 13 closes Sejuani E nth_hit Permafrost + Renata Q + Shaco R + Warwick E channel_completion + Fizz R + Aphelios Q form 3 sidecar target_debuffed + TahmKench W channel_completion FIRST 3-slot champ; item 172 wave 14 Meraki extractor cast_time schema lift + Jayce E form 0 cast-time root sidecar channel_completion FIRST Jayce CC registration; item 173 wave 15 closes Zac Q nth_hit different-target root + Ornn E terrain-collision stun FIRST COND_TERRAIN consumer + Rell W form 0 sidecar channel_completion stun; item 174 wave 16 closes Garen Q nth_hit empowered-AA silence FIRST Garen CC + Syndra E target_debuffed Dark-Sphere knockback stun FIRST Syndra CC + Udyr E nth_hit empowered-AA stun FIRST Udyr CC + KSante W channel_completion recast stun multi-wave coexistence with wave 1 KSante Q; item 175 wave 17 adds COND_TRAVERSE 13th tag + Taliyah E Unweaver's Wall traversal knockup nth_hit 0.75s multi-wave coexistence with Taliyah W wave 1; item 176 wave 18 FULL schema lift adds NEW `coexists_with_unconditional` flag + consumer MAX-rule semantics + Maokai R Nature's Grasp distance-gated root COND_RANGE_GATED (FIRST consumer of wave 7 forward-marker tag) + Briar R Certain Death Hematomania impact fear COND_TARGET_DEBUFFED (SECOND 3-slot cc_conditional champion after TahmKench); item 177 wave 19 Meraki extractor state-tracking schema lift adds `parent_resource` field per spell form (FURY / BLOOD_WELL / FRENZY / RAGE / HEAT / ENERGY / GRIT / MANA) - 0 net-new registry entries (67/54 unchanged); ALL 6 brief-named wave-7 schema-lift carries verified ALREADY-SHIPPED via prior waves + 3 REJECT-verified - forward-marker infrastructure for future COND_FRENZY_STATE consumers beyond Renekton W + Karma W + Gnar W; item 188 wave 20 Meraki extractor `notes` field schema lift per spell form + Vayne E Condemn terrain-collision stun 1.5s primary COND_TERRAIN coexists_with_unconditional=True SECOND COND_TERRAIN consumer after Ornn E wave 15 FIRST Vayne first-order CC; REJECTS Kindred E + Diana P + Vayne P; item 189 wave 21 cc_conditional re-audit against `notes` field schema lift + Xin Zhao Q Three Talon Strike 3rd-attack knockup primary nth_hit 0.75s + Xin Zhao R Crescent Sweep target-NOT-Challenged stun primary target_debuffed 0.75s INVERSE-gated FIRST Xin Zhao Q + R first-order CC anywhere - Xin Zhao becomes 2-slot champion in same wave; 38 REJECTS; item 199 wave 22 Rell W form_index=1 Ferromancy: Mount Up empowered-AA stun sidecar COND_CHANNEL_COMPLETION 0.4 0.6s flat all 5 W ranks FOURTH same-spell-slot multi-form-coexistence sidecar after Karma W 0+1 / Hwei E 0+1+2 / Sylas E 0+1 coexists with wave 15 Rell W form 0 sidecar on same (Rell, W) slot via form_index discriminator; item 202 wave 23 Hecarim R Onslaught of Shadows distance-gated fear 1.5s primary COND_RANGE_GATED 0.4 coexists_with_unconditional=True SECOND COND_RANGE_GATED consumer after Maokai R wave 18 FIRST Hecarim cc_conditional entry anywhere; 6 REJECTS Jhin W / Anivia Q vague duration / Heimerdinger E spatial / Lillia R COND_DREAM_STACK still empty / Lissandra W+R + Jinx E unconditional) + 13 condition tags (COND_NTH_HIT / COND_GOLD_CARD / COND_TERRAIN / COND_CHANNEL_COMPLETION / COND_DREAM_STACK / COND_DEVOUR_TARGET / COND_TARGET_HP_BELOW / COND_TARGET_DEBUFFED / COND_DUAL_ENEMY / COND_MODE_GATED / COND_FRENZY_STATE / COND_RANGE_GATED / COND_TRAVERSE). Personal calibration via gitignored `data/cc_conditional_calibration.json`. Open: live-game validation (operator plays real ARAM); future cc_conditional wave 13+ candidates remain operator-gated (Jayce E cast_time still needs patch data with explicit cast_time field; Maokai R distance-gated root would double-count existing unconditional entry; Renekton W Fury / Aatrox post-R passive / Volibear R passive / Briar W frenzy all still schema-lift-blocked at the parse-strip level despite item 153 lift).
- ✅ **obj_participation widening to 10+ column model** _OPTION (c) SHIPPED 2026-05-22 (CLAUDE.md item 149, commit `7b3d351`); semantic decision CLOSED 2026-05-23 - Option (b) keep 9-col additive (operator)._ Current model 9-column (6 SQL + riftHeraldTakedowns + voidMonsterKill + non-overlapping turretTakedowns via `max(turretTakedowns - first_tower_kill - first_tower_assist, 0)` clamp). The remaining 3 challenges_json fields (`baronTakedowns`, `dragonTakedowns`, `epicMonsterSteals`) FULLY OVERLAP existing SQL columns (baron_kills / dragon_kills / objectives_stolen) - adding them would DOUBLE-COUNT (Match-V5 takedowns = kills+assists vs SQL kills = last-hits only). Option (a) REPLACE SQL kills with Match-V5 takedowns would rebaseline existing 624 historic role-grades for marginal participation-math gain; operator-chosen Option (b) keep current additive model. CLOSED.
- ✅ **Dead Man's Plate Momentum stacks-schema** _SHIPPED 2026-05-21 (CLAUDE.md item 122, commit `ce563c4`; ENGINE 1.25.0 -> 1.26.0)._ NEW `PeriodicProc.stack_ramp_seconds` field is the family extension for the stack-accumulation -> discharge pattern; Dead Man's 3742 + Arena 223742 re-encoded with Shipwrecker = `lambda c: 40.0 + c.base_ad` PHYSICAL `every_n_attacks=1 stack_ramp_seconds=3.57` (replaces iter-16 `defensive_only` deferral). Sustained model gates at `max(ramp, attack_period)`; burst path skips ramp-gated procs. Aatrox L11 vs 100-armor delta = 20.15 DPS verified exact. The schema lift is the canonical example for any future stack-discharge items.
- ✅ **DS V2 next slices** _ALL 4 SHIPPED across items 228-231 (substrate item 227; see `docs/DS_V2_PLAN.md`)._ (1) wire `ability_hps` v2 into the LIVE `ds.hps` enchanter scorer - item 228 (`965bbb9`, ENGINE 1.64.0). (2) unified V2 fight-report composing mana_sim + rune_procs + self_shred + ability_hps - item 229 `agents/daemon_slayer/fight_report.py` + POST `/v2/fight-report` (ENGINE 1.65.0). (3) wire `rune_procs` into burst/combo damage totals - item 229 S3 (optional `runes` param, byte-identical when omitted, ENGINE 1.65.0). (4) expand `rune_procs` rune coverage + `mana_sim` bounded_dps as a `scenario_matrix` metric - item 230 (rune_procs 14 -> 16 per_attack Hail of Blades/Lethal Tempo + scenario_matrix mana_bounded_dps/rune_burst metrics, ENGINE 1.66.0). Rune coverage CLOSED honestly item 231 (DDragon 16.11.1 well dry); item 232 LIFTED the proc-signature (RuneProc.condition + caster_hp_pct/game_time_s/role/bonus_as) so Last Stand 8299 / Absolute Focus 8233 / Gathering Storm 8236 are now SHIPPED with honest gates (byte-identical default - the item-231 anti-inflation reasoning is honored by the defaults, the lift makes the gates EXPRESSIBLE; registry 16 -> 19). CLOSED.
- **DS sidecar bucket wiring (item 225 carry)** - 5 of 6 WIRED (item 231 + 232 + 233), all OPT-IN/additive/byte-identical at default: `ammo` charge model into `mana_sim` `gate_ammo` (item 231); `geometry` (619) -> NEW `geometry.py` AoE-multiplier into burst `aoe_targets_hit` (item 232); `recharge` (20) -> NEW `recharge_ledger.py` time-step charge-availability into fight_report 6th section (item 232); `mode_modifiers` -> dps/ehp `apply_mode_modifiers` dmg mults urf/ofa/usb/nb + engine ar/swift growth-addends, Arena re-ranks only flag-on (item 232); `missile` (416) -> NEW `missile.py` spell_travel_time (distance/missile_speed; speed-band + cone_distance=100/0 sentinel + <200 cast_radius-floor gating; consumed by fight_report 7th MISSILE section, APPROXIMATE default-distance-dominated) (item 233). ALL 6 BUCKETS NOW RESOLVED. Item 234 added the mana_sim opt-in `apply_ability_haste` CDR model (reuses `_item_ability_haste.total_item_ability_haste` + Riot `base_cd/(1+AH/100)`; byte-identical off / no-AH; binds on cooldown-repeating sequences). `static-CD` (55) is a HONEST no-consumer (item 234 verdict): the `ability_static_cd` accessor stays reachable but the wiki static QWER coverage is 3 abilities (Amumu Q / Heimerdinger R / Samira R) with a clear mislabel (Amumu Bandage Toss scales with haste) - too unreliable to gate haste-immunity; the haste model applies uniformly instead. `cc_tags` (186) is BLOCKED - it would feed cc_conditional which is operator-CLOSED (s232). FLIP defaults-on after LIVE-GAME validation (each re-ranks, operator-gated, NOT headless): `gate_ammo` / `apply_mode_modifiers` / `aoe_targets_hit` / `apply_ability_haste`. The clean headless DS sidecar/rune/haste lane is now EXHAUSTED - remaining DS work is operator-gated (live-validation flips + assassin-keystone sign-off) or data-blocked (per-spell cast-RANGE source for the missile combo-clock delay).
- **rewind_history.db SR records with game_id**: post-live SR game; wired in code (d66d14b) but blocked on new records.
- **LCU deeper integration**: Pengu Loader Discord/GitHub research for endpoints RC doesn't use yet (richer pre/post-game data). Research-first, no code scheduled. **Reference catalog identified 2026-05-17:** `KebsCS/lcu-and-riotclient-api` (lcu.kebs.dev) is a current (client 26.05), exhaustive (2839 ops) endpoint map - the authoritative source for the richer endpoints (mastery-by-puuid, `match-history .../games/{gameId}` + `game-timelines`, `loadouts/v4`, `signed-ranked-stats`). No LICENSE -> reference only, don't vendor; optionally diff its `data.json` per patch. This supersedes the Discord crawl as the primary endpoint-discovery method.
- **Interactive Item Shaper (post-DS-100%)** _(noted s214, 2026-05-15; backend primitive shipped 2026-05-19, re-confirmed by overnight run `c6327d8`)_: final-polish layer on top of the per-archetype scorers. UI exposes 3 modifiers - `DAMAGE` / `SURVIVABILITY` / `UTILITY` - each with a +/- nudge that reshapes the active scorer's weighting on the fly (e.g. Bruiser alpha=0.65/beta=0.35 default -> DAMAGE+ pushes toward alpha=0.80, SURVIVABILITY+ pushes toward alpha=0.45). The shaper reads + writes the running coach's archetype weights without persisting them; on match end the weights snap back to the archetype default. Intended as a real-time "I'm in a fight-heavy comp, push damage" / "they're snowballing, hold survivability" hint loop. **Backend primitive SHIPPED 2026-05-19** as `core/shaper.py`: `ShaperState` frozen dataclass + `apply_shaper(weights, shaper) -> dict` pure function (NUDGE_STEP=0.075 per knob click, clamp [0.05, 0.90], renormalize to sum 1.0, degenerate-guard snaps back to baseline). 57 deterministic property tests cover identity, sum-to-1 over the full 125-combo knob space x 5 archetype baselines, monotonicity per axis, ceiling clamp, degenerate guard. The primitive does NOT touch the scorer source-of-truth in `agents/daemon_slayer/rank.py` - it transforms a copy at call-time. No ENGINE bump owed. **Still blocked on**: DS engine reaches 100% champion coverage before the UI layer + the running-coach wire are exposed; the underlying scorer surface needs to be complete before operator-driven tuning rides on it.
- **DS competitor-lift gate slate** _(2026-05-30, item 217; researched + speced; pick-list in `docs/COMPETITOR_LIFT_2026-05-30.md`)._ Five HIGH lifts from the item-213 breadth scan turned into ready-to-build RC integration specs (6-point depth checklist + exact file/layer), all OPERATOR-GATE (product-direction calls). Pick-list order: (1) matchup cooldown-watch on the CC card _SHIPPED 2026-05-30 (item 218): NEW `agents/daemon_slayer/cooldown_watch.py` + `dashboard/routes_cooldown_watch.py` GET `/api/cooldown-watch?enemy=...` + `web/js/panels/cooldown_watch.js` + `#csv-sugg-cooldown-watch` champ-select card. Read-only JOIN of `_PER_SPELL_CC_DURATIONS` + `cc_conditional` highest-threat CC to per-rank `cooldown` from `champion_abilities.json`; base-cd-by-rank v1 (no live enemy haste). 55 tests; live curl verified; LIVE VISUAL CAPTURE owed (render-gated on committed enemies)._; (2) target-armor-swept DPS graph _SHIPPED 2026-05-30 (item 219): `agents/daemon_slayer/dps_sweep.py` `compute_dps_sweep` (armor/mr/level axes, level delegates to compute_dps_curve) + `/api/ds-sweep` + `web/js/panels/ds_sweep.js` (reuses spike_curve sparkline; champ-select card `#csv-sugg-ds-sweep`); LIVE VISUAL CAPTURE owed (render-gated on locked champ)._; (3) Aggregator P relative-score bar _SHIPPED 2026-05-30 (item 220): surface decision RESOLVED - shipped as its OWN panel `#csv-ds-relscore` (NOT the item-200-deleted DS-top-picks row). NEW `dashboard/routes_ds_relscore.py` GET `/api/ds-relscore` READ-ONLY (auto target stats, no knobs) wraps `rank_items` -> per-row `score_pct = delta_dps/top_delta*100` (row0=100.0) + `web/js/panels/ds_relscore.js` (horizontal bar fill = score_pct%); LIVE VISUAL CAPTURE owed (render-gated on locked champ)._; (4) live power-spike markers on Active Match _SHIPPED 2026-05-30 (item 219): `agents/daemon_slayer/spike_markers.py` + `/api/spike-markers` + `web/js/panels/spike_markers.js` on active-match (`#am-spike-markers`); level 6/11/16 + item 1/2/3 crossed/next/future markers keyed to live p.level+p.items; live-clock cursor OWED (live-game-only)._; (5) engine-knobs control panel _SHIPPED 2026-05-30 (item 219) v1: `dashboard/routes_ds_knobs.py` `/api/ds-knobs` wraps `rank_items` (NOT the :8893 dispatcher - no budget arg) with target_armor/mr/budget overrides + `web/js/panels/ds_knobs.js` 3-input strip; fight-length-reweight DEFERRED (only knob needing a new engine arg). Fight-length-reweight SHIPPED 2026-05-30 (item 220): `rank_items` gains optional `fight_length` arg (`effective_score = burst_delta + delta_dps*fight_length`, byte-identical when omitted, NOT an ENGINE bump - in-process helper); ds-knobs 4th "Fight length (s)" input._; (7) statcheck stat-sandbox _SHIPPED 2026-05-30 (item 220): NEW `dashboard/routes_ds_statcheck.py` GET `/api/ds-statcheck` wraps `compute_dps` for the locked champ at operator-set TARGET stats (auto curve when blank) -> resolved champion stat block + dps + `web/js/panels/ds_statcheck.js` (editable enemy armor/MR/HP/bonusHP inputs); champ-side AD/AS/crit displayed not editable (no engine stat-injection arg); DpsResult has no ttk; LIVE VISUAL CAPTURE owed._; (6) action-queue combo simulator _SHIPPED 2026-05-30 (item 219): NEW `agents/daemon_slayer/combo.py` `compute_combo` (composes compute_burst_damage + mitigation per-hit) + `/api/ds-combo` + `web/js/panels/ds_combo.js` per-hit timeline; v1 fixed cast-times (0.25s fallback), on-CD recast skipped._. Per-lift issue bodies in doc section (b).
- **DS lolmath-wiki sidecar extractor** _(2026-05-30, item 217/221/224; feasibility GO-conditional in `docs/LOLMATH_WIKI_SOURCE_2026-05-30.md`; TOOL + DATA + combo wire SHIPPED item 224 `c3740d0`)._ The LoL wiki carries STRUCTURED scalar fields Meraki lacks: AA-timing (windup/missile speed), static-cooldown flags, charge/ammo model, NON-ARAM mode multipliers. Thin sidecar JSON merged by champ name; Meraki stays authoritative for ratios/CC. **ACCESS RESOLVED (item 224):** `leagueoflegends.wiki.gg` edge-blocks Legion host-wide (HTTP 401; WebFetch egress too); the Riot alias `wiki.leagueoflegends.com` (same wiki.gg backend) IS reachable. Extractor uses `action=raw` on `Module:ChampionData/data` (ONE request, brace-scan keyed by apiname == DDragon id) + CDragon backfill (`raw.communitydragon.org` char bins, block-immune). **DATA SHIPPED:** `data/daemon_slayer/16.11.1/wiki_stats.json` - 171 champs, cast 171/171 (49 wiki + 12 cdragon + 110 engine-default 0.25), missile_speed 82/171, 0 errors; combo.py consumes per-champ AA windup. **DATA CEILING:** only 61/171 champs have a REAL AA cast time in ANY source (both wiki + CDragon store it only on engine-default override); the 110 default-filled are byte-identical to no-sidecar - do NOT re-hunt them. Re-run `py tools/daemon_slayer_wiki_stats_extract.py --patch <patch>` on a patch bump (see memory reference_lol_wiki_access). REMAINING (operator-gated, NOT built): `action=bucket` non-ARAM mode-mults (Arena/URF/NB) + static-cooldown flags + charge model - additional scalar lanes, separate sidecar fields.
- **item-WPA Build Insights view** _(2026-06-02, item 273; BACKEND+ROUTE+VIEW SHIPPED)._ `/api/item-wpa` (`core/item_wpa.py`) decomposes per-item winrate into expected (WPA model @purchase) vs observed residual over the rewind corpus. The PANEL shipped (item 273 slice G `f089a119`) after the operator dropped the competitor dashboard visuals (`Desktop/competitor dashboard.7z`) which confirmed RC's column mapping 1:1 + resolved the placement to a dedicated VIEW (the competitor's Items-page model): NEW `build-insights` view, sortable WPA table [Item | WPA + 5-seg confidence bar | Buys | Expected WP | Win Rate], `?ui_mock=1#build-insights`. REMAINING: (a) VISUAL CAPTURE OWED (Game-PC MCP :8892 down - operator can view live now). (b) optional EXTENSION - the competitor applies the SAME WPA mechanic to Spells / Runes / Skills (per-pick residual, pre-game baseline); RC's WPA engine could compute spell-WPA + rune-WPA over the corpus via new timeline-event walks (`SKILL_LEVEL_UP` for skill order; spell/rune are pre-game so expected = pre-game WP) - each its own backend+view slice. Personal-corpus DESCRIPTIVE lens (noisy, min_n+shrink load-bearing) - NOT a meta winrate.
- **deterministic champ-select brief FLIP (Haiku-elim)** _(2026-06-02, item 273; SUBSTRATE SHIPPED `dashboard/_champ_select_deterministic.py`)._ The deterministic champ-select brief (build+runes from the curated loadout_resolver + an archetype ally_notes heuristic) is built + shadow-logged side-by-side with the live Haiku brief to `logs/champ_select_brief_shadow.jsonl` (build_match/keystone_match per row). The served brief STAYS the live Haiku call (AST drift-guard pins no-flip). To RETIRE the Haiku call: review the shadow-log over a few real champ-selects, confirm the deterministic brief is acceptable, then flip `dashboard/_champ_select.py brief_via_coach` to return `brief_deterministic(...)` (drop the Anthropic call) - operator-gated validation, do NOT flip blind.

## Platform / observability

- **Streaming vision**: delta-encoded frames instead of full JPEG every 2s. ~5x bandwidth reduction.
- **Mobile-native dashboard**: current PWA adds ~2s touch latency. Native iOS/Android would reduce that.

## Data pipeline

- **TFT vision relay frame dimensions**: lock OCR pipeline to 1920x1080 once validated (pair with NOTE-025 calibration).

## Developer experience

- ✅ **Type hints + ruff one-time pass** _SHIPPED across items 153/154/155 + 219 (2026-05-23..30)._ Public-API return + param types annotated across coaches/ + core/ + dashboard/ + tft/ + lcu/ + coach_integration/ + agents/ (non-frozen); `ruff check .` clean repo-wide. Remaining unannotated surfaces are private `_helpers` + dunders + frozen files (intentionally out of scope). CLOSED.

## Reliability / hardening

- **`/api/analyze` streaming response**: current 30s timeout fine for ARAM (sub-second); may need SSE if full-mode analyses scale.

## Speculative

- **`.rofl` file replay coaching**: `coaches/replay_coach.py` shipped lite version (rewind_history.db). Full `.rofl` parsing **confirmed dead-end 2026-05-17** - fraxiinus `roflxd` (the maintained .ROFL parser family) shows a parsed .ROFL exposes only end-of-game aggregate `PlayerStatistics[]`, a strict *subset* of RC's existing Match-V5 SQLite; the in-game packet stream (frame positions / ability casts / event stream a replay-review page would want) is Riot-obfuscated and unparseable on the current patch. The planned s220 **S5 Replay page stays Match-V5-timeline-driven**; do not re-pitch .rofl parsing.
- **PyInstaller packaging**: `riot-commander.spec` exists as opt-in starter. Not prioritized.
- **OBS publisher activation**: `T3 #11` shipped (264 LOC, opt-in); not wired to any active workflow.

## Research / inspiration (s219, 2026-05-15)

- **competitor teardown**: analyze https://the competitor site/ - both the site and its app - for build patterns, post-game analysis UX, and any insight worth lifting into the Post Game Review or future Deep Review page. Operator can re-subscribe for a month to access the locked content if a deeper teardown is warranted.
- **Pengu.lol Discord crawl** _(demoted 2026-05-17)_: the static KebsCS catalog now covers released endpoint knowledge; keep this only as a *secondary* sweep for unreleased / in-progress community work (replay-timeline access, novel Mayhem detection). Research-only; surface findings before scheduling code work. **One concrete lift already surfaced** (Mayhem-Doctor, 2026-05-17): the static asset `/lol-game-data/assets/v1/cherry-augments.json` resolves augment id -> name/icon/rarity (`kSilver/kGold/kPrismatic`) - authoritative augment table without screen-OCR for *naming* (OCR still needed for *which* augments are offered live). Independently re-confirmed: zero `/lol-cherry/*` and zero `/lol-game-augments/*` live endpoints exist (KebsCS full 26.05 catalog) - augment-OCR stays; do not re-pitch an LCU augment API.

### `open item research.txt` (~90 links) triage - 2026-05-17 (multi-agent, s234 methodology)

3 decisions LOCKED + integrated to ROADMAP + CLAUDE #90 (do not re-litigate): fold `Morello` (MIT) into the s220 PGR 0-100 score; build ONE shared smoothed-rate primitive (Laplace/Beta over own match DB) for #88 augment + pick/ban synergy + PGR score; 101.qq.com CN duo-synergy is worth a one-off Game-PC Network-tab capture.

**FUTURE (noted, not scheduled):**
- **baronbuff.com** - closed-source *methodology* template: draft = single win-prob model fusing counters+synergy+comp+pool; PGR = "the decisions/phases that mattered, with evidence." Frames pick/ban + the s220 Deep-Review. Reference-only.
- **league_record** (GPLv3 - **cleanroom only, do NOT vendor**) - local OBS *video* capture keyed to LCU game-lifecycle + an event-timeline sidecar. NOT .rofl (sidesteps the CLOSED .rofl dead-end). The architecture reference for the s220 **S5 Replay** page.
- **aggregator Z13** builds-tier-list (ranks champxrunexfirst-item *tuples*, not just champs) - **aggregator J** (item tierlist bucketed by stat-type / tankiness / early-late) - METHOD refs for ARAM build data + DS presentation. Reference-only (closed Next.js SPAs, no JSON API exposed).
- **quick-aggregator-a-scraper** (MIT) - clean browserless Axios+Cheerio + TTL-cache aggregator A scrape = the *fallback* technique if RC ever needs an aggregator A tier/meta source.
- **AsperaDesu/LoL-AI-Draft-Picker** (MIT) draft-as-ordered-token-sequence idea - **junlarsen/league-connect** (MIT) per-endpoint WS `subscribe()` event-bus pattern (if RC ever moves champ-select/lobby off polling) - **CookieDecide/LeagueBuilds** (GPL, actively maintained) server-side build/rune/skill data pipeline (study, don't vendor) - **lolsite** `Greatest(field,1)` zero-guard per-min idiom. All reference-only.

**CLOSED - recorded so it is never re-spent:**
- **All 27 LCU client / event-viewer / swagger-codegen repos** (jjmaldonis - sousa-andre - Willump - PoniLCU - junlarsen - overlay app Z6 - GlassLCU - lcu-api-generator - clean-cuts - Briar - lcu-event-viewer - sunneydev/lcu-events - stirante/lol-client-java-api - bryanhitc/lcu-sharp - Pupix/rift-explorer - ratRequ3ster - RitoClient - ImOlli/go-lcu - legendary-rune-maker - Guava - CharmingDays/LeagueCU - zquaa/LCU-Stats - zquaa/league-refresher - Aventurine-League-Tools - LolyTools - simple-debugger): every one a generic client / event-viewer / codegen strictly inferior to RC's out-of-process lockfile Python client; **KebsCS stays the canonical endpoint catalog**; the entire corpus contains **ZERO Arena/Cherry/Mayhem lobby-create payloads** -> #89's bespoke payload must come from live LCU capture or KebsCS, not a library (a confirmed negative result - close that search path). `CharmingDays/LeagueCU` (MIT) only re-confirms the Practice-Tool `{"queueId":"PRACTICETOOL"}` string-sentinel RC already implements.
- ML / CV / misc: `LoL-Match-Predictor-ML` (Kaggle 10-min win-pred, no draft) - `League-Of-Legend-Items-Analysis` (item K-means - DS already richer) - `ARAMNet` (solves champ-pool *fairness*, not balance) - `LeagueMinimapDetectionOpenCV` / `LAVA` (RC's Live-Client position-freshness already covers; GPL anyway) - `LeagueAiCoach` / `LoLvoice` (voice) - `riot-offline-mode` (verified: Windows-Firewall toggle, **not** an offline-LCU test harness) - `Neeko-Server` (replay emulator, broken @ patch 14.1+) - `letter.gg` (TFT) - `malPHPhite` (PHP) - plus archived/dead/stale. None re-open.
- **Pengu.lol MCP - `rumi-chan/league-client-mcp`** (MIT, rejected 2026-05-17): thin generic `fetch()` passthrough that requires Pengu Loader injected + a live client just to make an LCU call, inherits the exact same LCU surface RC already hits. Strictly worse than RC's out-of-process Python lockfile client. Exposes no richer endpoints + no augment access. Do not re-adopt or re-research.

### `draft tool L (the community fork)` (HuyTheSkeleton fork of `draft tool L`) triage - 2026-05-19

Single-repo liftability review (operator-handed GitHub link; focused triage, not the multi-agent list method).

**FUTURE (noted, not scheduled):** the **Elo log-odds draft-composition aggregator** is the team-vs-team draft layer `dashboard/routes_pickban.py` does not have - `winrateToRating = -400*log10(1/w-1)`, team score = sum(ally champ + ally-duo + matchup ratings) minus sum(enemy champ + enemy-duo), `ratingToWinrate = 1/(1+10^(-d/400))`. Composes **on top of** `core/smoothed_rates.py` (smoothed rates feed the rating transform - orthogonal, NOT a duplicate; smoothing/priors are RC's existing edge and are absent here). Trigger: when RC wants true team-vs-team draft scoring beyond the current recent-form / joint-WR proxy, with pairwise WR sourced off `rewind_history.db`. Reference-algorithm-reimplement only.

**CLOSED - recorded so it is never re-spent:** (1) **No license** on draft tool L (the community fork) AND upstream `draft tool L` (neither OSI nor permissive) - vendoring either is legally unsafe; algorithm-only, reimplement clean from the formula above. (2) Its data is a **aggregator D HTML scrape** (`apps/dataset/.../aggregator D/qwik.ts` parsing the embedded `qwik/json` blob) RC cannot legally cache or redistribute - the dataset half is a dead end; pairwise WR must come from `rewind_history.db`, not aggregator D. (3) The "+" fork adds **zero algorithmic value** (UI delta column / dynamic tier-list only; 2 stars, solo) - reason from upstream's math, never prefer the fork. (4) Does NOT duplicate `core/smoothed_rates` (no smoothing/priors anywhere). (5) Tauri/Qwik/SolidJS desktop stack - irrelevant to RC's Python/JS dashboard.

_(SHIPPED 2026-05-20 `ab3f553` + `00051f3`: `core/draft_elo.py` + `core/draft_elo_db.py` + `dashboard/routes_draft_elo.py` + frontend chip `web/js/panels/draft_elo.js`. +38 tests, live `/api/draft-elo` verified. Composes on `core/smoothed_rates` per CLAUDE.md #90. Backend cache 5min; client cache 5min; ASCII clean.)_

### Multi-agent research wave triage - 2026-05-20 (overnight headless)

5 parallel slice agents (LoL overlay landscape / champ-select UX / item-data sources / Arena augment + Cherry coaching / post-game review methodology). Decisions LOCKED + integrated to BACKLOG (do not re-litigate):

**NOW (shippable within 1-2 weeks):**
- _(SHIPPED 2026-05-20 `21657eb` 3-agent parallel: LoLytics WPA framework - `core/post_game_score.py` pure-Python LR via batch gradient descent (no sklearn dep on fleet) + `data/post_game_wpa_model.json` trained coefficients (200 most-recent SR matches, 800 samples, sane weights gold_diff +1.32 xp_diff +0.65) + `dashboard/routes_post_game_wpa.py` GET `/api/post-game-wpa?match_id=<id>` + `web/js/panels/post_game_phases.js` phases-that-mattered card list mounted in last-match view. +68 tests; live curl on `NA1_5439050124` returns ok=true event_count=86 elapsed_ms=2 top-3 CHAMPION_KILL phases t=60/65/73s WPA +2.3% each.)_
- ✅ **cdragon-arena dataValues+calculations enrichment** _SHIPPED 2026-05-20 (`8f7a71b` dataclass-layer + `21657eb` formula-evaluator)._ `Augment.calculations: dict` field + `agents/daemon_slayer/augment_formula_eval.py` (4 typed-part shapes + `mMultiplier` + part-sum composition). Displacement seam `STAT_GRANT_CALC_KEYS` intentionally EMPTY at cdragon 16.10.1 (82 unique calc keys all damage/heal/shield/conversion, not stat grants); future Arena S2 Level-Up 26.09 patches populate without further engine changes.
- _(SHIPPED 2026-05-20 `58d1e87` + `21657eb` 3-agent parallel: MQTT-style haste-aware CD math FULLY CLOSED - `58d1e87` replaced additive CDR with Riot's canonical `eff_cd = base / (1 + h/100)` in `core/summoner_cooldowns.py` (Cosmic + Lucidity + Magical Footwear + Hextech Drake AH per stack), output keys `summs.summoner_haste` + `ult.ability_haste`. `21657eb` wired Hextech drake live-client count into `dashboard/_state_cooldowns.py` via new `_hextech_drakes_by_team()` helper - watches `gameData.events.Events` for DragonKill+DragonType="Hextech" + maps KillerName to team via allPlayers roster + passes `hextech_drakes` per enemy. +10 tests. Live-client wire complete; CD ledger now ships accurate ability-haste from real dragons.)_

**FUTURE (worth noting; condition to act):**
- **Arena S2 Augment Level-Up + Crafting Round system** (Riot 2026 S2, patch 26.09) - per-augment level 1->3 mid-match, round 8 = Crafting Round (add slot OR remove augment to level another), Keystone tier at max level. Schema break for `modes/arena_coach.py` + cherry-augments OCR. Trigger: 26.09 PBE confirms schema (likely `levels[]` axis on `Augment.dataValues`); pre-stage a `level` axis on `compute_augment_stats()`.
- **Brawl mode re-enable** (Riot disabled 2026-11-19 patch 25.23). Existing brawl backend stays deadcode per CLAUDE.md s214; touch only when Riot announces re-enable.
- **Draft Tool Z8 (Jfghanimah/loldraft-analyzer)** (`https://github.com/Jfghanimah/loldraft-analyzer`, MIT, May 2025) - 56-57% accuracy draft-WR predictor with FastAPI surface. Trigger: pick up as a second-opinion alongside our Elo aggregator-after PGR S2 lands.
- **Coaching App Z7 AI Drafting Model layered-ensemble methodology** (`https://coaching-app-z7.invalid/articles/the-draft-model`) - closed-source but documents architecture (linear + GBT layered ensemble, time-split validation, +7.5% WR lift attributed to draft alone). Read-only methodology reference for tuning the Elo aggregator.
- **LoL Esports Data Portal community tier** (`https://lolesports.com/.../introducing-the-new-lol-esports-data-portal`) - Riot 2,500GB/game pro telemetry, currently teams-only; "community access in the near future." Trigger: when Riot opens the community tier.
- **CommunityDragon broader `lol-game-data` JSON catalog** (`raw.communitydragon.org/.../plugins/rcp-be-lol-game-data/global/default/v1/`) - 7 files RC doesn't yet consume: `summoner-spells.json` (could ground CD ledger), `perks.json` + `perkstyles.json` (rune metadata for PGR), `maps.json` + `queues.json` + `game-mode-mutators.json` (canonical mode detection beyond KIWI/queueId hacks), `champion-rune-recommendations.json` (Riot's own recommended runes per champ). Cdragon redistributes Riot game data under Riot's Legal Jibber Jabber - same gray zone as the cherry-augments.json we already accept. Trigger: when PGR S2 wants better rune analysis or when CD-ledger has surplus capacity for a richer summoner-spell substrate.
- **Riot's per-role grading rubric** (`https://www.unrankedsmurfs.com/blog/what-is-riot-algorithm-for-determining-s-and-s+-ranks`) - single public source for per-role weight vectors (ADC ~2.1 KDA + 50% obj-participation, SUP 2.5 KDA + weighted ward count + zero CS weight, JG 66-75% obj-participation, MID/TOP standard). Calibration reference for `core/post_game_rubric.py`.
- _(SHIPPED 2026-05-20 `e0eabb7` 3-agent parallel wave 2: HURTS-THEM vs HELPS-US frontend toggle - NEW `web/js/panels/ban_suggest_toggle.js` + CSS + 25 snapshot DOM tests. Mode persists `localStorage[rc-cs-bansugg-mode]` default `"hurts_them"`; `data-bs-active` attr-driven CSS flip (red HURTS / green HELPS); per-row shows active-mode rating + other-mode in muted text + `n=solo/pair` sample-density chip with band tier. Reuses cached `/api/champ-select/ban-suggestions` for portrait metadata. Wired into `champ_select.js` via `_csvRenderBanSuggestToggle()` helper.)_
- _(SHIPPED 2026-05-20 `ea9b88f` 3-agent parallel wave 2: per-row contribution breakdown hover strip - NEW `Contribution` dataclass + `top_contributions()` sibling aggregator in `core/draft_elo.py:141-238` (NOT inside `team_score` - pure sibling). Route gained `?breakdown=1` query param adding `top_contributions: [{kind, a, b, delta, n}, ...]` field (max 3); cache stores full + strips at serve. Frontend uses PURE CSS `:hover` (zero JS event handlers, zero click-to-pin, `pointer-events: none` on overlay); `HoverOnlyContractTests` guard FAILS if any future change adds `addEventListener('click'`, `.onclick`, or `pinned` class. +31 tests. Live curl breakdown=1 returned top-3 enemy-pair / matchup / ally-pair with sample densities.)_
- **`mwrogue` MediaWiki Cargo client** (`RheingoldRiver/mwrogue`, MIT) - wraps `wiki.leagueoflegends.com` (CC-BY-SA 3.0) for `Module:ChampionData/data` + `Module:ArenaAugmentData/data` access. Trigger: only as numeric backstop when cdragon `@variable@` template resolution gaps appear. We already have the aram modifiers (`aramHealing`/`aramShielding`/`aramAbilityHaste`/`aramTenacity`) in our existing Meraki champion bulk, verified live `0c550b5`; the wiki source is for cdragon templating gaps, not the aram modifier wire.

**CLOSED - recorded so we never re-spend:**
- **MobaHinted/client** (dead since Sep 6 2024; GPLv3 anyway). Only Python in-process competitor to RC is no longer maintained.
- **LeagueAkari, overlay app Z6 npm LCU wrapper, LeagueBroadcast/OSL/LeagueOCR/Dan-Shields/floh22 OCR HUDs, lol-vision/LeagueAI/PlayerImageRecognizer** - either GPL (incompatible), spectator-stream-only (irrelevant for player-side coach), or thinner than RC's existing surfaces.
- **Coaching App Z7 closed-source coach + Hakko AI RTC voice + Replays.lol + Insights.gg + Aggregator C GPI** - closed SaaS; methodology-only references.
- **AGGREGATOR A / Overlay App E / Aggregator C / AGGREGATOR N Arena augment WIN-RATE data at large** - **Riot dev policy explicitly forbids** displaying winrates for Arena augments or Arena Mode items across all third-party tools (Riot DevRel 2024-04, still in force). Pick-rate is allowed (we already pull overlay app E iesdev `core/augment_external_source.py`); winrate is not. Stick to pick-rate priors.
- **Reasoning-trace LLM agent ("Strategist/Critic")** (Draft Tool L-AI Devpost) - cost-prohibitive per champ-select + latency-prohibitive vs 30s timer; our aggregator already produces the inputs that would feed it.
- **Persistent counter-pick sidebar** (Overlay App E/Overlay App F pattern) - clutters at 1920x1080; hover-only wins. Aligns with the dashboard's tight Chrome-chrome viewport.
- **Riot prodraft.leagueoflegends.com integration** - tournament-only flow, no solo-queue value.
- **First-item-by-archetype-by-patch with permissive license** - no such public dataset exists; `rewind_history.db` + smoothed-rates pipeline IS the better path for RC's sparse-play cadence.
- **Fandom `leagueoflegends.fandom.com` wiki** - the OLD fork; the official wiki moved to `wiki.leagueoflegends.com` in 2023 and Fandom is stale within ~1 patch.
- **CommunityDragon/LeaguePatchNotes** - archived 2020, patches 8.13-8.22 only, 0 active forks. Patch-aware change tracking stays manual in the DS audit loop.
- **All other generic LCU client repos, OCR HUDs, win-predictor clones, replay parsers** - duplicates or subsets of RC's existing surfaces. The complete CLOSED list across the 5 research slices is in the agent transcripts; do not re-research.
